import { SECTION, CALLOUT_TONE, DIFFICULTY, EXERCISE_KIND, QUIZ_KIND } from '../../schema/types.js';

const M = 'm11';

export default [
  /* ================================================================== */
  {
    id: 'l-m11-01',
    slug: 'the-for-loop',
    moduleId: M,
    order: 1,
    title: 'The for Loop',
    description: 'Why iteration exists, the three parts of a for header, and the off-by-one errors that follow.',
    difficulty: DIFFICULTY.BEGINNER,
    estimatedMinutes: 16,
    xp: 30,
    topicIds: ['loops'],
    prerequisites: ['l-m10-05'],
    learningObjectives: [
      'Explain what problem loops solve',
      'Describe the three parts of a `for` header and when each runs',
      'Avoid off-by-one errors when iterating an array',
      'Loop backwards and in steps other than one',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Functions let you reuse a behaviour. **Loops** let you repeat one — a fixed number of times, or once per item in a collection, or until some condition changes.',
          'Almost every program processes lists: orders, users, search results, rows of data. Without loops you would need one line per item, and you never know how many items there will be.',
        ],
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Without a loop',
          code: [
            'const scores = [8, 5, 9];',
            '',
            'let total = 0;',
            'total += scores[0];',
            'total += scores[1];',
            'total += scores[2];',
            '',
            '// Breaks the moment the',
            '// array has a different',
            '// number of items.',
          ].join('\n'),
        },
        right: {
          title: 'With a loop',
          code: [
            'const scores = [8, 5, 9];',
            '',
            'let total = 0;',
            'for (let i = 0; i < scores.length; i++) {',
            '  total += scores[i];',
            '}',
            '',
            '// Works for any length,',
            '// including zero.',
          ].join('\n'),
        },
        note: 'The loop version is not merely shorter — it is *correct for input you have not seen yet*, which the unrolled version can never be.',
      },
      {
        kind: SECTION.HEADING,
        text: 'The three parts of a for header',
      },
      {
        kind: SECTION.ANNOTATED_CODE,
        language: 'javascript',
        code: [
          'for (let i = 0; i < 3; i++) {',
          '  console.log(i);',
          '}',
        ].join('\n'),
        annotations: [
          { line: 1, text: '**Initialization** — `let i = 0` runs exactly once, before the first iteration. It usually declares the counter.' },
          { line: 1, text: '**Condition** — `i < 3` is checked *before every* iteration. While it is truthy the body runs; the moment it is falsy the loop ends.' },
          { line: 1, text: '**Update** — `i++` runs *after every* iteration, before the condition is checked again.' },
          { line: 2, text: 'The body runs once per iteration, with `i` holding the current value.' },
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The exact order, made visible.',
        code: [
          'console.log("before");',
          '',
          'for (let i = 0; i < 3; i++) {',
          '  console.log("  body, i =", i);',
          '}',
          '',
          'console.log("after");',
          '',
          '// A condition that is false immediately means zero iterations',
          'for (let i = 5; i < 3; i++) {',
          '  console.log("never runs");',
          '}',
          'console.log("zero-iteration loop finished");',
        ].join('\n'),
        output: [
          'before',
          '  body, i = 0',
          '  body, i = 1',
          '  body, i = 2',
          'after',
          'zero-iteration loop finished',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Because the condition is checked **before** the first iteration, a `for` loop can run zero times. That is exactly what you want when the array is empty — no special case needed.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Iterating an array',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The standard pattern, and why the condition uses `<`.',
        code: [
          'const items = ["a", "b", "c"];',
          '',
          'for (let i = 0; i < items.length; i++) {',
          '  console.log(i, items[i]);',
          '}',
          '',
          'console.log("length is", items.length);',
          'console.log("last valid index is", items.length - 1);',
        ].join('\n'),
        output: [
          '0 a',
          '1 b',
          '2 c',
          'length is 3',
          'last valid index is 2',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'The off-by-one error',
        body: [
          'Indexes are zero-based, so an array of length 3 has valid indexes 0, 1 and 2. The condition must be `i < length`, **not** `i <= length`.',
          'Using `<=` runs one extra iteration where `items[3]` is `undefined`. It does not throw — you get `undefined` in your results, or `NaN` once it enters arithmetic, and the cause is several steps away.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The classic off-by-one, and its consequence.',
        code: [
          'const nums = [10, 20, 30];',
          '',
          'let wrong = 0;',
          'for (let i = 0; i <= nums.length; i++) {',
          '  wrong += nums[i];',
          '}',
          '',
          'let right = 0;',
          'for (let i = 0; i < nums.length; i++) {',
          '  right += nums[i];',
          '}',
          '',
          'console.log("with <= :", wrong);',
          'console.log("with <  :", right);',
        ].join('\n'),
        output: ['with <= : NaN', 'with <  : 60'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The extra iteration reads `nums[3]`, which is `undefined`. Adding `undefined` to a number gives `NaN`, and `NaN` is contagious, so the whole total is destroyed. No error is thrown at any point.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Counting backwards and in steps',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'All three header parts are ordinary expressions you control.',
        code: [
          '// Backwards',
          'const letters = ["a", "b", "c"];',
          'for (let i = letters.length - 1; i >= 0; i--) {',
          '  console.log("reverse:", letters[i]);',
          '}',
          '',
          '// Every second item',
          'for (let i = 0; i < 6; i += 2) {',
          '  console.log("step 2:", i);',
          '}',
          '',
          '// Counting down by three',
          'for (let i = 10; i > 0; i -= 3) {',
          '  console.log("down 3:", i);',
          '}',
        ].join('\n'),
        output: [
          'reverse: c',
          'reverse: b',
          'reverse: a',
          'step 2: 0',
          'step 2: 2',
          'step 2: 4',
          'down 3: 10',
          'down 3: 7',
          'down 3: 4',
          'down 3: 1',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Note the backwards loop starts at `length - 1` and its condition is `i >= 0`. Both boundaries change when you reverse direction, and getting only one of them right is a common source of a missing first or last item.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Use `let`, never `var`, for the counter',
        body: [
          '`let` gives each iteration its own binding, which matters as soon as the loop body creates a function — the classic `setTimeout` puzzle from Module 01. With `var` there is one shared counter for the whole function and every callback sees its final value.',
          '`const` will not work for a classic `for` counter, because `i++` reassigns it. It *does* work in `for...of`, which you meet in lesson 4.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const arr = [1, 2, 3];',
          'let count = 0;',
          '',
          'for (let i = 0; i <= arr.length; i++) {',
          '  count++;',
          '}',
          '',
          'console.log(count, arr.length);',
        ].join('\n'),
        options: ['4 3', '3 3', '3 4', '4 4'],
        correct: 0,
        explanation:
          'The condition `i <= arr.length` is `i <= 3`, so the loop runs for `i` equal to 0, 1, 2 and 3 — four iterations rather than three. The array length is unchanged at 3. This is the off-by-one error: with `<=` you always run exactly one iteration too many, and the extra one reads an index that does not exist. The correct condition is `i < arr.length`.',
      },
    ],
    exercises: [
      {
        id: 'ex-m11-01-a',
        title: 'Predict the iteration count',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['loops'],
        instructions: 'How many times does the body run?',
        code: [
          'let n = 0;',
          'for (let i = 1; i < 5; i++) {',
          '  n++;',
          '}',
          'console.log(n);',
        ].join('\n'),
        options: ['4', '5', '3', '6'],
        correct: 0,
        hints: [
          'List the values `i` takes while the condition is still true.',
        ],
        solution: '4',
        solutionExplanation:
          '`i` starts at 1 and the body runs while `i < 5`, so it runs for `i` equal to 1, 2, 3 and 4 — four times. When `i` becomes 5 the condition is false and the loop ends without running the body again. A quick way to count iterations for a `for` loop counting up by one: `end - start`, here `5 - 1 = 4`.',
      },
      {
        id: 'ex-m11-01-b',
        title: 'Sum an array',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 20,
        topicIds: ['loops'],
        instructions:
          'Write `sumAll(numbers)` returning the total of every element, using a classic `for` loop. Return `0` for an empty array.',
        starterCode: [
          'function sumAll(numbers) {',
          '  // Accumulate into a total, then return it.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'sums three numbers', body: 'expect(sumAll([1, 2, 3])).toBe(6);' },
          { name: 'returns 0 for an empty array', body: 'expect(sumAll([])).toBe(0);' },
          { name: 'handles a single element', body: 'expect(sumAll([42])).toBe(42);' },
          { name: 'handles negatives', body: 'expect(sumAll([5, -3, 2])).toBe(4);' },
          { name: 'handles decimals', body: 'expect(sumAll([0.5, 0.25, 0.25])).toBe(1);' },
          { name: 'includes the last element', body: 'expect(sumAll([1, 1, 1, 1, 99])).toBe(103);', hidden: true },
        ],
        hints: [
          'Start the accumulator at `0`, not `""`.',
          'The condition must be `i < numbers.length`.',
          'Add `numbers[i]` to the total on each pass.',
        ],
        solution: [
          'function sumAll(numbers) {',
          '  let total = 0;',
          '',
          '  for (let i = 0; i < numbers.length; i++) {',
          '    total += numbers[i];',
          '  }',
          '',
          '  return total;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The accumulator starts at `0` — the identity value for addition — which is why the empty-array case returns `0` correctly without any special handling. Using `<` rather than `<=` in the condition is what stops the loop reading `numbers[length]`, which would be `undefined` and would turn the total into `NaN`. The hidden test puts a large value last specifically to catch an implementation that stops one iteration early with `i < numbers.length - 1`.',
      },
      {
        id: 'ex-m11-01-c',
        title: 'Find the highest number',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['loops'],
        instructions:
          'Write `highest(numbers)` returning the largest value, using a `for` loop rather than `Math.max`. Return `null` for an empty array.',
        starterCode: [
          'function highest(numbers) {',
          '  // Track the best value seen so far.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'finds the largest', body: 'expect(highest([3, 9, 2])).toBe(9);' },
          { name: 'finds it when first', body: 'expect(highest([9, 3, 2])).toBe(9);' },
          { name: 'finds it when last', body: 'expect(highest([3, 2, 9])).toBe(9);' },
          { name: 'handles a single element', body: 'expect(highest([7])).toBe(7);' },
          { name: 'returns null for an empty array', body: 'expect(highest([])).toBe(null);' },
          { name: 'handles all negatives', body: 'expect(highest([-5, -2, -9])).toBe(-2);' },
          { name: 'handles duplicates of the maximum', body: 'expect(highest([4, 4, 1])).toBe(4);', hidden: true },
        ],
        hints: [
          'Guard the empty case first.',
          'Start the best value at `numbers[0]`, not at `0` — otherwise all-negative arrays break.',
          'Loop from index 1 and replace the best value whenever you find something larger.',
        ],
        solution: [
          'function highest(numbers) {',
          '  if (numbers.length === 0) return null;',
          '',
          '  let best = numbers[0];',
          '',
          '  for (let i = 1; i < numbers.length; i++) {',
          '    if (numbers[i] > best) {',
          '      best = numbers[i];',
          '    }',
          '  }',
          '',
          '  return best;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Starting `best` at `numbers[0]` rather than `0` is the key decision — the all-negative test exists precisely to catch that mistake, since every negative number fails `> 0` and the function would wrongly return `0`. Because the first element is already the starting value, the loop can begin at index 1 and skip a redundant comparison. The empty guard comes first because `numbers[0]` would be `undefined` otherwise, and every subsequent comparison against `undefined` is `false`.',
      },
      {
        id: 'ex-m11-01-d',
        title: 'Fix the off-by-one',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 15,
        topicIds: ['loops'],
        instructions:
          'This function should reverse an array into a new one, but it drops the first element and adds an `undefined`. Fix the loop boundaries.',
        starterCode: [
          'function reversed(items) {',
          '  const result = [];',
          '  for (let i = items.length; i > 0; i--) {',
          '    result.push(items[i]);',
          '  }',
          '  return result;',
          '}',
        ].join('\n'),
        tests: [
          { name: 'reverses three items', body: 'expect(reversed(["a", "b", "c"])).toEqual(["c", "b", "a"]);' },
          { name: 'reverses two items', body: 'expect(reversed([1, 2])).toEqual([2, 1]);' },
          { name: 'handles a single item', body: 'expect(reversed([1])).toEqual([1]);' },
          { name: 'handles an empty array', body: 'expect(reversed([])).toEqual([]);' },
          { name: 'does not modify the original', body: 'const src = [1, 2, 3]; reversed(src); expect(src).toEqual([1, 2, 3]);', hidden: true },
        ],
        hints: [
          'The highest valid index is `length - 1`, not `length`.',
          'The loop must continue down to index 0, so the condition needs `>=`.',
          'Both boundaries are wrong.',
        ],
        solution: [
          'function reversed(items) {',
          '  const result = [];',
          '',
          '  for (let i = items.length - 1; i >= 0; i--) {',
          '    result.push(items[i]);',
          '  }',
          '',
          '  return result;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Both boundaries were off. Starting at `items.length` reads an index past the end, pushing `undefined` first; and stopping at `i > 0` means index 0 is never reached, dropping the original first element. Correcting to `length - 1` and `i >= 0` fixes both. When reversing a loop, remember that *both* the start and the condition change — fixing only one is the usual half-correction. Building into a new array rather than mutating also keeps the function pure, which the hidden test checks.',
      },
    ],
    quiz: {
      id: 'qz-m11-01',
      questions: [
        {
          id: 'q-m11-01-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['loops'],
          prompt: 'In `for (let i = 0; i < n; i++)`, when does the update `i++` run?',
          options: [
            'Before each iteration, alongside the condition check',
            'After each iteration of the body, before the condition is checked again',
            'Only once, at the start',
            'Only after the loop ends',
          ],
          correct: 1,
          optionExplanations: [
            'The condition is checked first; the update comes after the body.',
            'Correct — body, then update, then condition.',
            'That describes the initialization.',
            'It runs on every iteration.',
          ],
          explanation:
            'The order is: initialization once, then repeatedly — check the condition, run the body, run the update. Because the condition is checked before the first body run, a loop can execute zero times.',
        },
        {
          id: 'q-m11-01-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['loops'],
          prompt: 'What does this produce?',
          code: [
            'const a = [1, 2, 3];',
            'let s = 0;',
            'for (let i = 0; i <= a.length; i++) s += a[i];',
            'console.log(s);',
          ].join('\n'),
          options: ['6', 'NaN', '7', 'undefined'],
          correct: 1,
          optionExplanations: [
            'That would require the correct `<` condition.',
            'Correct — the extra iteration adds `undefined`.',
            'No index holds a value that would give 7.',
            'The variable holds a number, just not a useful one.',
          ],
          explanation:
            '`<=` runs one iteration too many, reading `a[3]` which is `undefined`. Adding `undefined` to a number gives `NaN`, and `NaN` contaminates the rest of the sum. Nothing throws, which is what makes off-by-one errors hard to spot.',
        },
        {
          id: 'q-m11-01-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['loops'],
          prompt: 'Why should the loop counter be declared with `let` rather than `var`?',
          options: [
            '`var` is not allowed in a for header',
            '`let` gives each iteration its own binding, which matters when the body creates functions',
            '`let` is faster',
            '`var` cannot be incremented',
          ],
          correct: 1,
          optionExplanations: [
            '`var` is legal there, just worse.',
            'Correct — with `var` all callbacks share one counter.',
            'Performance is not the issue.',
            '`var` increments normally.',
          ],
          explanation:
            '`let` creates a fresh binding per iteration, so a function created inside the body captures that iteration\'s value. With `var` there is a single function-scoped counter, so every captured function sees its final value — the classic `setTimeout` loop puzzle.',
        },
      ],
    },
    summary:
      'Loops repeat a behaviour, which is what makes a program correct for input whose size you do not know in advance. A `for` header has three parts: initialization runs once, the condition is checked before every iteration, and the update runs after each body execution. Because the condition is checked first, a loop can run zero times — exactly right for an empty array. Iterating an array uses `i < length`, never `i <= length`, because indexes are zero-based; the off-by-one reads an index that does not exist, yielding `undefined` and usually `NaN`, with no error thrown. Reversing a loop changes both the start and the condition. Always declare the counter with `let`, so each iteration gets its own binding.',
    keyTakeaways: [
      'Initialization once, then condition → body → update, repeatedly',
      'The condition is checked first, so zero iterations is possible',
      'Use `i < length` — `i <= length` reads one index too far',
      'An off-by-one gives `undefined`, then `NaN`, with no error',
      'Reversing a loop changes both the start value and the condition',
      'Declare the counter with `let`, never `var`',
    ],
    relatedLessons: ['l-m10-05', 'l-m11-02'],
    interviewConnections: [
      'What are the three parts of a for loop header?',
      'What is an off-by-one error?',
      'Why use let rather than var for a loop counter?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m11-02',
    slug: 'while-and-do-while',
    moduleId: M,
    order: 2,
    title: 'while and do...while',
    description: 'Looping when you do not know the count in advance, and the one loop that always runs at least once.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 14,
    xp: 30,
    topicIds: ['loops'],
    prerequisites: ['l-m11-01'],
    learningObjectives: [
      'Write a `while` loop with a condition that eventually becomes false',
      'Explain how `do...while` differs and when it fits',
      'Recognise the three causes of an infinite loop',
      'Choose between `for` and `while` deliberately',
    ],
    sections: [
      {
        kind: SECTION.HEADING,
        text: 'while',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'A `while` loop repeats as long as its condition is truthy. It has no built-in counter — you manage any state yourself, which makes it the right choice when the number of iterations is not known up front.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The condition is checked before every iteration, including the first.',
        code: [
          'let count = 0;',
          '',
          'while (count < 3) {',
          '  console.log("count is", count);',
          '  count++;',
          '}',
          '',
          '// A condition false at the start means zero iterations',
          'let n = 10;',
          'while (n < 5) {',
          '  console.log("never runs");',
          '  n++;',
          '}',
          '',
          'console.log("done");',
        ].join('\n'),
        output: ['count is 0', 'count is 1', 'count is 2', 'done'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Any `for` loop can be written as a `while`, and vice versa. The difference is where the three parts live: `for` gathers them into the header, while `while` leaves them scattered.',
        ],
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'for — parts collected',
          code: [
            'for (let i = 0; i < 3; i++) {',
            '  console.log(i);',
            '}',
            '',
            '// Counter, condition and',
            '// update are all visible',
            '// in one line.',
          ].join('\n'),
        },
        right: {
          title: 'while — parts scattered',
          code: [
            'let i = 0;',
            'while (i < 3) {',
            '  console.log(i);',
            '  i++;',
            '}',
            '',
            '// Easy to forget the i++',
            '// and loop forever.',
          ].join('\n'),
        },
        note: 'Use `for` when you are counting — the header makes it impossible to forget the update. Use `while` when the ending condition is not a count.',
      },
      {
        kind: SECTION.HEADING,
        text: 'When `while` is the better choice',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Iteration count unknown until it happens.',
        code: [
          '// Halve until below a threshold',
          'let value = 100;',
          'let steps = 0;',
          '',
          'while (value > 1) {',
          '  value = value / 2;',
          '  steps++;',
          '}',
          '',
          'console.log(`took ${steps} steps, ended at ${value}`);',
          '',
          '// Consume a queue until it is empty',
          'const queue = ["a", "b", "c"];',
          'while (queue.length > 0) {',
          '  console.log("processing", queue.shift());',
          '}',
          'console.log("queue length:", queue.length);',
        ].join('\n'),
        output: [
          'took 7 steps, ended at 0.78125',
          'processing a',
          'processing b',
          'processing c',
          'queue length: 0',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Neither of those has a count you could put in a `for` header. The queue example is particularly characteristic: the collection shrinks as you process it, so the ending condition is about the data rather than an index.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'do...while',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`do...while` checks its condition **after** the body, which means the body always runs at least once.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The same false condition, two different outcomes.',
        code: [
          'let a = 10;',
          'while (a < 5) {',
          '  console.log("while body");',
          '  a++;',
          '}',
          'console.log("while ran 0 times");',
          '',
          'let b = 10;',
          'do {',
          '  console.log("do...while body");',
          '  b++;',
          '} while (b < 5);',
          'console.log("do...while ran once anyway");',
        ].join('\n'),
        output: [
          'while ran 0 times',
          'do...while body',
          'do...while ran once anyway',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Note the semicolon',
        body: [
          '`do { ... } while (condition);` ends with a semicolon. This is one of the few places a semicolon is genuinely required rather than optional — omitting it can confuse Automatic Semicolon Insertion.',
        ],
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`do...while` is much rarer than `while`. It fits when the work must happen before you can evaluate whether to stop — generating a value and checking it, prompting a user until the input is valid, or attempting an operation before deciding whether to retry.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A genuine do...while: you must roll before you can check the roll.',
        code: [
          '// A deterministic stand-in for a random source, so the output is stable',
          'const rolls = [3, 3, 6, 2];',
          'let index = 0;',
          'let roll;',
          'let attempts = 0;',
          '',
          'do {',
          '  roll = rolls[index];',
          '  index++;',
          '  attempts++;',
          '} while (roll !== 6 && index < rolls.length);',
          '',
          'console.log(`rolled a ${roll} after ${attempts} attempts`);',
        ].join('\n'),
        output: ['rolled a 6 after 3 attempts'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Infinite loops',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'A loop whose condition never becomes false runs forever, freezing the page. There are exactly three ways to cause one, and knowing them makes the mistake easy to spot.',
        ],
      },
      {
        kind: SECTION.LIST,
        ordered: true,
        items: [
          '**No update at all.** `let i = 0; while (i < 3) { console.log(i); }` — `i` never changes, so the condition stays true forever.',
          '**Updating in the wrong direction.** `for (let i = 0; i < 3; i--)` — `i` moves away from the condition instead of towards it.',
          '**A condition that can never be false.** `while (true)` with no `break` inside, or a condition comparing values that never converge.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A guard counter turns a potential hang into a diagnosable error.',
        code: [
          'function halveUntil(start, target, maxSteps = 1000) {',
          '  let value = start;',
          '  let steps = 0;',
          '',
          '  while (value > target) {',
          '    value = value / 2;',
          '    steps++;',
          '',
          '    if (steps > maxSteps) {',
          '      throw new Error(`no convergence after ${maxSteps} steps`);',
          '    }',
          '  }',
          '',
          '  return steps;',
          '}',
          '',
          'console.log(halveUntil(100, 1));',
          '',
          'try {',
          '  // Halving never reaches 0, so this would loop forever without the guard',
          '  halveUntil(100, 0);',
          '} catch (error) {',
          '  console.log("caught:", error.message);',
          '}',
        ].join('\n'),
        output: ['7', 'caught: no convergence after 1000 steps'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Halving never reaches zero',
        body: [
          'The second call is a genuinely subtle infinite loop: `value > 0` stays true through hundreds of halvings, because a positive number divided by two is always positive. It eventually reaches zero through floating-point underflow, but only after well over a thousand iterations.',
          'Whenever a loop condition depends on a value converging, ask whether it *can* actually reach the boundary. A maximum-iteration guard is cheap insurance in any loop whose termination is not obvious.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'How JSPath protects you',
        body: [
          'The lesson sandbox injects a counter into every loop and enforces a wall-clock timeout, so an accidental infinite loop stops with a clear message rather than freezing the tab. A real browser has no such protection — an infinite loop in your own page will lock it up until the tab is closed.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'let i = 0;',
          'let result = "";',
          '',
          'do {',
          '  result += i;',
          '  i++;',
          '} while (i < 0);',
          '',
          'console.log(result);',
        ].join('\n'),
        options: ['""', '"0"', '"01"', 'It loops forever'],
        correct: 1,
        explanation:
          '`do...while` runs its body before checking anything, so the first iteration happens regardless — appending `"0"` and incrementing `i` to 1. Only then is `i < 0` evaluated, which is false, so the loop ends. The result is `"0"`. A plain `while (i < 0)` would have checked first, found the condition false, and produced an empty string.',
      },
    ],
    exercises: [
      {
        id: 'ex-m11-02-a',
        title: 'Predict the do...while',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['loops'],
        instructions: 'How many times does the body run?',
        code: [
          'let n = 5;',
          'let runs = 0;',
          '',
          'do {',
          '  runs++;',
          '  n++;',
          '} while (n < 3);',
          '',
          'console.log(runs);',
        ].join('\n'),
        options: ['0', '1', '3', 'It loops forever'],
        correct: 1,
        hints: [
          'When is the condition checked in a `do...while`?',
        ],
        solution: '1',
        solutionExplanation:
          '`do...while` checks its condition after the body, so the body always runs at least once. On that first pass `runs` becomes 1 and `n` becomes 6; the condition `n < 3` is then false and the loop ends. A plain `while (n < 3)` would have checked first, found `5 < 3` false, and run zero times. That guaranteed first iteration is the only difference between the two forms.',
      },
      {
        id: 'ex-m11-02-b',
        title: 'Count digits with while',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['loops'],
        instructions:
          'Write `countDigits(n)` returning how many digits a non-negative whole number has, using a `while` loop and arithmetic rather than converting to a string. `countDigits(0)` must return 1.',
        starterCode: [
          'function countDigits(n) {',
          '  // Repeatedly divide by 10 until nothing is left.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'counts a single digit', body: 'expect(countDigits(7)).toBe(1);' },
          { name: 'counts zero as one digit', body: 'expect(countDigits(0)).toBe(1);' },
          { name: 'counts three digits', body: 'expect(countDigits(123)).toBe(3);' },
          { name: 'counts a large number', body: 'expect(countDigits(1000000)).toBe(7);' },
          { name: 'counts a boundary value', body: 'expect(countDigits(99)).toBe(2); expect(countDigits(100)).toBe(3);' },
          { name: 'does not convert to a string', body: 'expect(/toString|String\\(|\\.length/.test(countDigits.toString())).toBe(false);', hidden: true },
        ],
        hints: [
          'Handle `0` before the loop, since a loop on zero would run zero times.',
          '`Math.floor(n / 10)` removes the last digit.',
          'Count how many times you can do that before reaching 0.',
        ],
        solution: [
          'function countDigits(n) {',
          '  if (n === 0) return 1;',
          '',
          '  let count = 0;',
          '  let remaining = n;',
          '',
          '  while (remaining > 0) {',
          '    remaining = Math.floor(remaining / 10);',
          '    count++;',
          '  }',
          '',
          '  return count;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Each division by ten with `Math.floor` removes one digit from the right, so counting the divisions counts the digits. `while` is the natural fit because the iteration count depends on the value rather than being known up front. The zero guard is essential: `0 > 0` is false immediately, so the loop would run zero times and return `0` rather than the correct `1`. Copying `n` into `remaining` avoids mutating the parameter, which keeps the intent clear — though as Module 08 noted, reassigning a parameter would not affect the caller anyway.',
      },
      {
        id: 'ex-m11-02-c',
        title: 'Simulate retry logic',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['loops'],
        instructions:
          'Write `attemptUntil(results, maxAttempts)` that walks the `results` array in order, counting attempts, and stops as soon as it finds a truthy value or runs out of attempts. Return `{ value, attempts }` — with `value` being the found item or `null`. Never make more than `maxAttempts` attempts.',
        starterCode: [
          'function attemptUntil(results, maxAttempts) {',
          '  // Use a while loop with two stopping conditions.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'succeeds on the first attempt', body: 'expect(attemptUntil(["ok"], 3)).toEqual({ value: "ok", attempts: 1 });' },
          { name: 'succeeds on a later attempt', body: 'expect(attemptUntil([null, null, "ok"], 5)).toEqual({ value: "ok", attempts: 3 });' },
          { name: 'gives up at the attempt limit', body: 'expect(attemptUntil([null, null, "ok"], 2)).toEqual({ value: null, attempts: 2 });' },
          { name: 'handles an empty results list', body: 'expect(attemptUntil([], 3)).toEqual({ value: null, attempts: 0 });' },
          { name: 'stops when results run out', body: 'expect(attemptUntil([null, null], 10)).toEqual({ value: null, attempts: 2 });' },
          { name: 'treats 0 as a failed attempt', body: 'expect(attemptUntil([0, "ok"], 5)).toEqual({ value: "ok", attempts: 2 });', hidden: true },
        ],
        hints: [
          'Track an index and an attempt count together.',
          'The loop continues while no value is found, attempts remain, and the index is in range.',
          'A falsy result means keep going — which is why `0` counts as a failure.',
        ],
        solution: [
          'function attemptUntil(results, maxAttempts) {',
          '  let index = 0;',
          '  let attempts = 0;',
          '  let value = null;',
          '',
          '  while (value === null && attempts < maxAttempts && index < results.length) {',
          '    const result = results[index];',
          '    attempts++;',
          '    index++;',
          '',
          '    if (result) {',
          '      value = result;',
          '    }',
          '  }',
          '',
          '  return { value, attempts };',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Three conditions guard the loop, and each one prevents a different failure: `value === null` stops on success, `attempts < maxAttempts` enforces the retry budget, and `index < results.length` prevents reading past the end of the array. Omitting the third would read `undefined` forever if `maxAttempts` exceeded the array length. The `if (result)` check treats any falsy value as a failed attempt, which the hidden test confirms with `0` — a deliberate design choice worth noting, since a real retry helper handling numeric results would need an explicit `!= null` check instead.',
      },
    ],
    quiz: {
      id: 'qz-m11-02',
      questions: [
        {
          id: 'q-m11-02-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['loops'],
          prompt: 'What is the difference between `while` and `do...while`?',
          options: [
            '`do...while` can only be used with numbers',
            '`while` checks the condition before the body; `do...while` checks after, so its body always runs at least once',
            '`do...while` runs twice as fast',
            'They are identical',
          ],
          correct: 1,
          optionExplanations: [
            'Both accept any condition.',
            'Correct — that guaranteed first iteration is the whole difference.',
            'Performance is identical.',
            'The condition timing genuinely differs.',
          ],
          explanation:
            '`while` may run zero times if its condition is false at the start. `do...while` runs the body first and checks afterwards, so it always executes at least once — useful when you must do the work before you can decide whether to stop.',
        },
        {
          id: 'q-m11-02-2',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['loops'],
          prompt: 'Which of these cause an infinite loop? (Select all that apply.)',
          options: [
            '`let i = 0; while (i < 3) { console.log(i); }`',
            '`for (let i = 0; i < 3; i--) {}`',
            '`while (true) {}` with no break',
            '`for (let i = 3; i > 0; i--) {}`',
          ],
          correct: [0, 1, 2],
          explanation:
            'The first never updates `i`. The second decrements away from the condition, so `i < 3` stays true forever. The third has a condition that can never become false and no escape. The fourth counts down correctly from 3 to 1 and terminates normally.',
        },
        {
          id: 'q-m11-02-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['loops'],
          prompt: 'When is `while` a better choice than `for`?',
          options: [
            'When iterating an array by index',
            'When the number of iterations is not known in advance and depends on a changing condition',
            'When you need better performance',
            'When the loop body is longer than one line',
          ],
          correct: 1,
          optionExplanations: [
            'A `for` header keeps the counter, condition and update together for that case.',
            'Correct — such as consuming a queue or converging on a value.',
            'They perform identically.',
            'Body length is irrelevant.',
          ],
          explanation:
            'Use `for` when you are counting, because the header makes the update impossible to forget. Use `while` when termination depends on something other than a count — a queue emptying, a value converging, or a flag changing.',
        },
      ],
    },
    summary:
      '`while` repeats as long as its condition is truthy, checking before every iteration so it can run zero times. It has no built-in counter, which makes it right when the number of iterations depends on changing state rather than a count — consuming a queue, converging on a value. `do...while` checks after the body, so it always runs at least once; it fits when the work must happen before you can judge whether to stop, and it requires a trailing semicolon. Infinite loops have exactly three causes: no update, updating in the wrong direction, or a condition that can never be false. A maximum-iteration guard is cheap insurance whenever termination is not obviously guaranteed — a condition such as `value > 0` under repeated halving looks convergent but is not.',
    keyTakeaways: [
      '`while` checks first and can run zero times; `do...while` always runs once',
      'Prefer `for` for counting, `while` when termination is not a count',
      'Three causes of infinite loops: no update, wrong direction, unsatisfiable condition',
      'Add a max-iteration guard when convergence is not obviously guaranteed',
      '`do { } while ();` needs its trailing semicolon',
    ],
    relatedLessons: ['l-m11-01', 'l-m11-03'],
    interviewConnections: [
      'What is the difference between while and do...while?',
      'What causes an infinite loop?',
      'When would you choose while over for?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m11-03',
    slug: 'break-continue-nested',
    moduleId: M,
    order: 3,
    title: 'break, continue and Nested Loops',
    description: 'Leaving early, skipping an iteration, and working with loops inside loops.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 15,
    xp: 30,
    topicIds: ['loops'],
    prerequisites: ['l-m11-02'],
    learningObjectives: [
      'Use `break` to exit a loop as soon as the answer is known',
      'Use `continue` to skip the rest of an iteration',
      'Write and trace a nested loop',
      'Explain that `break` only exits the innermost loop, and how to escape both',
    ],
    sections: [
      {
        kind: SECTION.HEADING,
        text: 'break',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`break` exits the loop immediately. Nothing else in the body runs, the update does not run, and execution continues after the loop.',
          'Its main use is searching: once you have found what you were looking for, continuing to iterate is wasted work.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Stop as soon as the answer is known.',
        code: [
          'const names = ["Ada", "Grace", "Alan", "Katherine"];',
          '',
          'let foundIndex = -1;',
          '',
          'for (let i = 0; i < names.length; i++) {',
          '  console.log("checking", names[i]);',
          '',
          '  if (names[i] === "Alan") {',
          '    foundIndex = i;',
          '    break;',
          '  }',
          '}',
          '',
          'console.log("found at index", foundIndex);',
        ].join('\n'),
        output: [
          'checking Ada',
          'checking Grace',
          'checking Alan',
          'found at index 2',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`Katherine` is never checked. On a four-element array that saves nothing measurable; on ten thousand records where the match is early, it matters a great deal.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'continue',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`continue` skips the rest of the current iteration and moves to the next one. In a `for` loop the update still runs — which is what makes it safe there and dangerous in a `while`.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Skipping unwanted items.',
        code: [
          'const values = [1, -2, 3, -4, 5];',
          'let total = 0;',
          '',
          'for (let i = 0; i < values.length; i++) {',
          '  if (values[i] < 0) {',
          '    continue;',
          '  }',
          '  total += values[i];',
          '}',
          '',
          'console.log("sum of positives:", total);',
        ].join('\n'),
        output: ['sum of positives: 9'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: '`continue` in a `while` can loop forever',
        body: [
          'In a `for` loop the update lives in the header, so `continue` still runs it. In a `while` loop the update is usually the last line of the body — and `continue` skips it.',
          '`while (i < 5) { if (skip) continue; i++; }` never increments `i` when `skip` is true, so it loops forever. Increment *before* the `continue`, or use a `for` loop.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The safe ordering in a while loop.',
        code: [
          'const items = [1, 2, 3, 4, 5];',
          'let i = 0;',
          'const kept = [];',
          '',
          'while (i < items.length) {',
          '  const current = items[i];',
          '  i++;                       // advance BEFORE any continue',
          '',
          '  if (current % 2 === 0) {',
          '    continue;',
          '  }',
          '',
          '  kept.push(current);',
          '}',
          '',
          'console.log(kept);',
        ].join('\n'),
        output: ['[1, 3, 5]'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Guard clauses inside loops',
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Nested condition',
          code: [
            'for (const item of items) {',
            '  if (item.active) {',
            '    if (item.price > 0) {',
            '      process(item);',
            '    }',
            '  }',
            '}',
          ].join('\n'),
        },
        right: {
          title: '`continue` as a guard',
          code: [
            'for (const item of items) {',
            '  if (!item.active) continue;',
            '  if (item.price <= 0) continue;',
            '',
            '  process(item);',
            '}',
          ].join('\n'),
        },
        note: 'The same guard-clause reasoning from Module 07, applied inside a loop body. `continue` is to a loop iteration what an early `return` is to a function.',
      },
      {
        kind: SECTION.HEADING,
        text: 'Nested loops',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'A loop inside another loop. The inner loop completes fully on every single iteration of the outer one, so the body runs `outer × inner` times.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The inner loop restarts each time.',
        code: [
          'for (let row = 1; row <= 3; row++) {',
          '  let line = "";',
          '',
          '  for (let col = 1; col <= 3; col++) {',
          '    line += `${row}x${col}=${row * col}  `;',
          '  }',
          '',
          '  console.log(line.trim());',
          '}',
        ].join('\n'),
        output: [
          '1x1=1  1x2=2  1x3=3',
          '2x1=2  2x2=4  2x3=6',
          '3x1=3  3x2=6  3x3=9',
        ].join('\n'),
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Nested loops over nested data.',
        code: [
          'const teams = [',
          '  { name: "Platform", members: ["Ada", "Grace"] },',
          '  { name: "Design", members: ["Alan"] },',
          '];',
          '',
          'for (let t = 0; t < teams.length; t++) {',
          '  const team = teams[t];',
          '',
          '  for (let m = 0; m < team.members.length; m++) {',
          '    console.log(`${team.members[m]} — ${team.name}`);',
          '  }',
          '}',
        ].join('\n'),
        output: [
          'Ada — Platform',
          'Grace — Platform',
          'Alan — Design',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Nested loops multiply the work',
        body: [
          'Two nested loops over the same array of `n` items run the body `n²` times. At 100 items that is 10,000 iterations; at 10,000 items it is 100 million.',
          'This is the difference between O(n) and O(n²) that Module 39 covers. When you find yourself nesting loops to compare every item against every other item, a `Map` or `Set` can often reduce it to a single pass.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: '`break` only exits the innermost loop',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The outer loop keeps going.',
        code: [
          'for (let i = 1; i <= 3; i++) {',
          '  for (let j = 1; j <= 3; j++) {',
          '    if (j === 2) break;',
          '    console.log(`i=${i} j=${j}`);',
          '  }',
          '}',
        ].join('\n'),
        output: ['i=1 j=1', 'i=2 j=1', 'i=3 j=1'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The `break` ends the inner loop each time, but the outer loop continues to its next iteration and starts a fresh inner loop. To leave both, you need either a flag, a labelled break, or — usually best — to extract the search into a function and `return`.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Three ways to escape both loops.',
        code: [
          'const grid = [[1, 2], [3, 4], [5, 6]];',
          '',
          '// 1. A labelled break',
          'outer:',
          'for (let r = 0; r < grid.length; r++) {',
          '  for (let c = 0; c < grid[r].length; c++) {',
          '    if (grid[r][c] === 4) {',
          '      console.log("label found at", r, c);',
          '      break outer;',
          '    }',
          '  }',
          '}',
          '',
          '// 2. Extract to a function and return — usually clearest',
          'function findValue(g, target) {',
          '  for (let r = 0; r < g.length; r++) {',
          '    for (let c = 0; c < g[r].length; c++) {',
          '      if (g[r][c] === target) return [r, c];',
          '    }',
          '  }',
          '  return null;',
          '}',
          '',
          'console.log("function found at", findValue(grid, 4));',
          'console.log("missing gives", findValue(grid, 99));',
        ].join('\n'),
        output: [
          'label found at 1 1',
          'function found at [1, 1]',
          'missing gives null',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Prefer extracting a function to a labelled break',
        body: [
          'Labels are legal and occasionally the right tool, but they are rare enough that most developers have to stop and think about what they do. Extracting the search into a function and using `return` achieves the same thing with a construct everyone reads fluently — and gives the operation a name.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'let count = 0;',
          '',
          'for (let i = 0; i < 3; i++) {',
          '  for (let j = 0; j < 3; j++) {',
          '    if (j === 1) continue;',
          '    count++;',
          '  }',
          '}',
          '',
          'console.log(count);',
        ].join('\n'),
        options: ['6', '9', '3', '4'],
        correct: 0,
        explanation:
          'The inner loop runs three times per outer iteration, with `j` equal to 0, 1 and 2. The `continue` skips the increment only when `j` is 1, so two increments happen per outer iteration. Three outer iterations times two increments gives 6. Note `continue` skips the rest of that *inner* iteration, not the outer one — the inner loop simply moves on to `j = 2`.',
      },
    ],
    exercises: [
      {
        id: 'ex-m11-03-a',
        title: 'Predict the nested break',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['loops'],
        instructions: 'How many lines does this print?',
        code: [
          'for (let i = 0; i < 3; i++) {',
          '  for (let j = 0; j < 5; j++) {',
          '    if (j === 1) break;',
          '    console.log(i, j);',
          '  }',
          '}',
        ].join('\n'),
        options: ['3', '5', '15', '1'],
        correct: 0,
        hints: [
          'Which loop does `break` exit?',
          'How many times does the inner body run before breaking?',
        ],
        solution: '3',
        solutionExplanation:
          'On each outer iteration the inner loop runs with `j = 0`, prints one line, then reaches `j = 1` and breaks out of the **inner** loop only. The outer loop continues to its next iteration and starts a fresh inner loop. Three outer iterations, one printed line each, gives 3 lines. To exit both loops you would need a labelled break, a flag, or to extract the code into a function and `return`.',
      },
      {
        id: 'ex-m11-03-b',
        title: 'Search with early exit',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['loops'],
        instructions:
          'Write `indexOfValue(items, target)` returning the index of the first element strictly equal to `target`, or `-1` if absent. Use a `for` loop and stop as soon as you find it. Do not use the built-in `indexOf`.',
        starterCode: [
          'function indexOfValue(items, target) {',
          '  // Return as soon as you find a match.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'finds an element', body: 'expect(indexOfValue(["a", "b", "c"], "b")).toBe(1);' },
          { name: 'finds the first element', body: 'expect(indexOfValue(["a", "b"], "a")).toBe(0);' },
          { name: 'returns -1 when absent', body: 'expect(indexOfValue(["a", "b"], "z")).toBe(-1);' },
          { name: 'returns -1 for an empty array', body: 'expect(indexOfValue([], "a")).toBe(-1);' },
          { name: 'returns the first of duplicates', body: 'expect(indexOfValue(["a", "b", "a"], "a")).toBe(0);' },
          { name: 'uses strict equality', body: 'expect(indexOfValue([1, 2], "1")).toBe(-1);' },
          { name: 'finds a value at the last index', body: 'expect(indexOfValue([1, 2, 3], 3)).toBe(2);', hidden: true },
        ],
        hints: [
          'Returning from inside the loop exits it immediately — no `break` needed.',
          'Use `===` so `1` and `"1"` do not match.',
          'Return `-1` after the loop if nothing matched.',
        ],
        solution: [
          'function indexOfValue(items, target) {',
          '  for (let i = 0; i < items.length; i++) {',
          '    if (items[i] === target) {',
          '      return i;',
          '    }',
          '  }',
          '',
          '  return -1;',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`return` from inside a loop exits the loop *and* the function in one step, which is why no `break` is needed here — a common simplification worth recognising. Returning on the first match naturally gives the earliest index when duplicates exist. Using `===` rather than `==` is what makes the string-versus-number test pass. The `-1` after the loop is only reached when every element failed, including for an empty array where the body never runs at all.',
      },
      {
        id: 'ex-m11-03-c',
        title: 'Build a multiplication table',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['loops'],
        instructions:
          'Write `multiplicationTable(n)` returning an array of `n` arrays, where row `r` and column `c` hold `(r + 1) * (c + 1)`. `multiplicationTable(2)` gives `[[1, 2], [2, 4]]`. Return an empty array for `n` of 0 or less.',
        starterCode: [
          'function multiplicationTable(n) {',
          '  // An outer loop for rows, an inner loop for columns.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'builds a 2x2 table', body: 'expect(multiplicationTable(2)).toEqual([[1, 2], [2, 4]]);' },
          { name: 'builds a 3x3 table', body: 'expect(multiplicationTable(3)).toEqual([[1, 2, 3], [2, 4, 6], [3, 6, 9]]);' },
          { name: 'builds a 1x1 table', body: 'expect(multiplicationTable(1)).toEqual([[1]]);' },
          { name: 'returns empty for zero', body: 'expect(multiplicationTable(0)).toEqual([]);' },
          { name: 'returns empty for negatives', body: 'expect(multiplicationTable(-2)).toEqual([]);' },
          { name: 'has the right dimensions', body: 'const t = multiplicationTable(4); expect(t.length).toBe(4); expect(t[0].length).toBe(4);', hidden: true },
        ],
        hints: [
          'Build a fresh row array inside the outer loop, before the inner loop.',
          'Push each computed value into the row, then push the row into the table.',
          'Indexes are zero-based, so multiply `(r + 1)` by `(c + 1)`.',
        ],
        solution: [
          'function multiplicationTable(n) {',
          '  const table = [];',
          '',
          '  for (let r = 0; r < n; r++) {',
          '    const row = [];',
          '',
          '    for (let c = 0; c < n; c++) {',
          '      row.push((r + 1) * (c + 1));',
          '    }',
          '',
          '    table.push(row);',
          '  }',
          '',
          '  return table;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Declaring `row` *inside* the outer loop is the critical detail — each outer iteration needs a fresh array. Declaring it outside would push the same array reference `n` times, producing a table where every row is the same over-long array, which is a classic nested-loop bug. The `+ 1` adjustments convert zero-based indexes into the one-based values a multiplication table shows. Negative and zero input need no special case: the outer condition `r < n` is false immediately, so the empty `table` is returned as-is.',
      },
      {
        id: 'ex-m11-03-d',
        title: 'Fix the continue infinite loop',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['loops'],
        instructions:
          'This `while` loop should collect the odd numbers, but it hangs because `continue` skips the increment. Fix it so the index always advances, keeping the `while` loop and the `continue`.',
        starterCode: [
          'function oddsOnly(numbers) {',
          '  const result = [];',
          '  let i = 0;',
          '',
          '  while (i < numbers.length) {',
          '    if (numbers[i] % 2 === 0) {',
          '      continue;',
          '    }',
          '    result.push(numbers[i]);',
          '    i++;',
          '  }',
          '',
          '  return result;',
          '}',
        ].join('\n'),
        tests: [
          { name: 'keeps the odd numbers', body: 'expect(oddsOnly([1, 2, 3, 4, 5])).toEqual([1, 3, 5]);' },
          { name: 'handles all evens', body: 'expect(oddsOnly([2, 4, 6])).toEqual([]);' },
          { name: 'handles all odds', body: 'expect(oddsOnly([1, 3])).toEqual([1, 3]);' },
          { name: 'handles an empty array', body: 'expect(oddsOnly([])).toEqual([]);' },
          { name: 'handles negatives', body: 'expect(oddsOnly([-3, -2])).toEqual([-3]);' },
          { name: 'still uses a while loop', body: 'expect(/while/.test(oddsOnly.toString())).toBe(true);', hidden: true },
        ],
        hints: [
          'When `continue` runs, the `i++` at the bottom is skipped entirely.',
          'Read the current value and advance the index before the `continue`.',
          'Capture `numbers[i]` in a variable first.',
        ],
        solution: [
          'function oddsOnly(numbers) {',
          '  const result = [];',
          '  let i = 0;',
          '',
          '  while (i < numbers.length) {',
          '    const current = numbers[i];',
          '    i++;',
          '',
          '    if (current % 2 === 0) {',
          '      continue;',
          '    }',
          '',
          '    result.push(current);',
          '  }',
          '',
          '  return result;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Moving the increment above the `continue` guarantees the index advances on every path through the body — which is the general rule for `continue` inside a `while`. Capturing `numbers[i]` into `current` first is what makes that reordering readable, since `i` has already moved on by the time the value is used. Note the negative test still works because `-3 % 2` is `-1`, which is not `0`, so odd negatives are correctly kept. A `for` loop would avoid the whole problem, since its update lives in the header and `continue` still runs it.',
      },
    ],
    quiz: {
      id: 'qz-m11-03',
      questions: [
        {
          id: 'q-m11-03-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['loops'],
          prompt: 'What is the difference between `break` and `continue`?',
          options: [
            '`break` skips one iteration; `continue` exits the loop',
            '`break` exits the loop entirely; `continue` skips to the next iteration',
            'They are identical',
            '`continue` only works in `while` loops',
          ],
          correct: 1,
          optionExplanations: [
            'This has the two reversed.',
            'Correct.',
            'They do genuinely different things.',
            'Both work in every loop form.',
          ],
          explanation:
            '`break` ends the loop immediately and execution continues after it. `continue` abandons the rest of the current iteration and proceeds to the next one — the loop itself carries on.',
        },
        {
          id: 'q-m11-03-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['loops'],
          prompt: 'Which loop does the break exit?',
          code: [
            'let n = 0;',
            'for (let i = 0; i < 2; i++) {',
            '  for (let j = 0; j < 2; j++) {',
            '    break;',
            '  }',
            '  n++;',
            '}',
            'console.log(n);',
          ].join('\n'),
          options: ['0', '1', '2', '4'],
          correct: 2,
          optionExplanations: [
            'The outer body still runs.',
            'The outer loop is not affected by the inner break.',
            'Correct — only the inner loop is exited, so the outer runs twice.',
            'The inner loop breaks immediately each time.',
          ],
          explanation:
            '`break` exits only the innermost enclosing loop. The outer loop continues normally, so `n++` runs on each of its two iterations. To exit both you need a labelled break, a flag, or to extract the code into a function and `return`.',
        },
        {
          id: 'q-m11-03-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['loops'],
          prompt: 'Why can `continue` cause an infinite loop in a `while` but not a `for`?',
          options: [
            '`continue` is not allowed in `while` loops',
            'In a `for` loop the update is in the header and still runs; in a `while` it is usually in the body and gets skipped',
            '`while` loops have no condition',
            'It cannot — both are equally safe',
          ],
          correct: 1,
          optionExplanations: [
            'It is perfectly legal there.',
            'Correct — the update placement is the whole difference.',
            '`while` loops certainly have a condition.',
            'The risk is genuinely different.',
          ],
          explanation:
            'A `for` loop\'s update lives in the header, so `continue` runs it before the next condition check. In a `while` the update is normally the last statement of the body, and `continue` jumps past it — leaving the condition unchanged forever. Advance the counter before any `continue`.',
        },
        {
          id: 'q-m11-03-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['loops', 'algorithms'],
          prompt: 'Two nested loops over the same array of n items run the body roughly n² times.',
          options: ['True', 'False'],
          correct: 0,
          optionExplanations: [
            'Correct — the inner loop completes fully for each outer iteration.',
            'The work genuinely multiplies rather than adding.',
          ],
          explanation:
            'The inner loop runs to completion on every outer iteration, so the total is the product. At 100 items that is 10,000 iterations; at 10,000 items it is 100 million. This is the O(n²) growth Module 39 covers, and it is often avoidable with a `Map` or `Set`.',
        },
      ],
    },
    summary:
      '`break` exits a loop immediately, which is what makes searching efficient — stop as soon as the answer is known. `continue` abandons the rest of the current iteration and moves to the next, working as a guard clause inside a loop body. In a `while` loop `continue` skips any update written at the bottom of the body, so the counter must be advanced *before* it or the loop hangs; a `for` loop is immune because its update lives in the header. Nested loops run the inner loop to completion on every outer iteration, so the work multiplies — two nested loops over `n` items is `n²`. `break` exits only the innermost loop; to leave both, prefer extracting the search into a function and returning over a labelled break.',
    keyTakeaways: [
      '`break` exits the loop; `continue` skips to the next iteration',
      '`continue` in a `while` skips a trailing update — advance the counter first',
      '`continue` works as a guard clause inside a loop body',
      'Nested loops multiply the work: `n` outer × `m` inner',
      '`break` exits only the innermost loop; prefer a function and `return` over labels',
    ],
    relatedLessons: ['l-m11-02', 'l-m11-04'],
    interviewConnections: [
      'What is the difference between break and continue?',
      'How do you break out of nested loops?',
      'Why can continue cause an infinite loop in a while?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m11-04',
    slug: 'for-of-and-for-in',
    moduleId: M,
    order: 4,
    title: 'for...of and for...in',
    description: 'Two similar-looking loops that do completely different things, and the bug that follows from confusing them.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 16,
    xp: 35,
    topicIds: ['loops'],
    prerequisites: ['l-m11-03'],
    learningObjectives: [
      'Iterate values with `for...of` and keys with `for...in`',
      'Explain why `for...in` is the wrong choice for arrays',
      'Use `entries()` when you need both index and value',
      'State when `for...in` is genuinely appropriate',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'These two loops look almost identical and behave completely differently. Confusing them is one of the most common sources of surprising results in JavaScript, so the distinction is worth fixing firmly.',
        ],
      },
      {
        kind: SECTION.TABLE,
        headers: ['Loop', 'Gives you', 'Works on'],
        rows: [
          ['`for...of`', 'The **values**', 'Arrays, strings, Maps, Sets — anything iterable'],
          ['`for...in`', 'The **keys**, as strings', 'Any object; includes inherited enumerable properties'],
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The same array, two very different results.',
        code: [
          'const colours = ["red", "green", "blue"];',
          '',
          'for (const value of colours) {',
          '  console.log("of:", value);',
          '}',
          '',
          'for (const key in colours) {',
          '  console.log("in:", key, typeof key);',
          '}',
        ].join('\n'),
        output: [
          'of: red',
          'of: green',
          'of: blue',
          'in: 0 string',
          'in: 1 string',
          'in: 2 string',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'The indexes from `for...in` are strings',
        body: [
          'Object keys are always strings, and array indexes are no exception. So `for (const i in arr)` gives you `"0"`, `"1"`, `"2"` — not numbers.',
          'That means `i + 1` gives `"01"` rather than `1`, because `+` concatenates when either operand is a string. This is a genuinely confusing bug because the loop otherwise appears to work.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The string-index trap.',
        code: [
          'const arr = ["a", "b", "c"];',
          '',
          'for (const i in arr) {',
          '  console.log(`index ${i}, next would be ${i + 1}`);',
          '}',
          '',
          '// Correct with a classic for loop',
          'for (let i = 0; i < arr.length; i++) {',
          '  console.log(`index ${i}, next would be ${i + 1}`);',
          '}',
        ].join('\n'),
        output: [
          'index 0, next would be 01',
          'index 1, next would be 11',
          'index 2, next would be 21',
          'index 0, next would be 1',
          'index 1, next would be 2',
          'index 2, next would be 3',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Why `for...in` is wrong for arrays',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Beyond the string indexes, `for...in` visits **every enumerable property**, not just the numeric indexes. If anything has been added to the array — or to `Array.prototype` by an older library — it shows up.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A non-index property appears in `for...in` but not `for...of`.',
        code: [
          'const arr = ["a", "b"];',
          'arr.label = "extra";',
          '',
          'console.log("for...in sees:");',
          'for (const key in arr) {',
          '  console.log("  ", key);',
          '}',
          '',
          'console.log("for...of sees:");',
          'for (const value of arr) {',
          '  console.log("  ", value);',
          '}',
          '',
          'console.log("length is still", arr.length);',
        ].join('\n'),
        output: [
          'for...in sees:',
          '   0',
          '   1',
          '   label',
          'for...of sees:',
          '   a',
          '   b',
          'length is still 2',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The specification also does not guarantee `for...in` order for all key types. In practice engines iterate integer-like keys in ascending order first, so arrays usually come out in order — but relying on unspecified behaviour is not a habit worth forming.',
          'The rule is simple: **never use `for...in` on an array.**',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'for...of in detail',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: '`for...of` works on anything iterable.',
        code: [
          '// Arrays',
          'for (const n of [1, 2]) console.log("array:", n);',
          '',
          '// Strings — by code point, so emoji stay intact',
          'for (const ch of "hi") console.log("string:", ch);',
          '',
          '// Sets',
          'for (const item of new Set(["x", "x", "y"])) console.log("set:", item);',
          '',
          '// Maps give [key, value] pairs',
          'const map = new Map([["a", 1], ["b", 2]]);',
          'for (const [key, value] of map) console.log("map:", key, value);',
        ].join('\n'),
        output: [
          'array: 1',
          'array: 2',
          'string: h',
          'string: i',
          'set: x',
          'set: y',
          'map: a 1',
          'map: b 2',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: '`const` works in a `for...of` header',
        body: [
          'Each iteration creates a **new binding**, so nothing is ever reassigned and `const` is correct — even though the value differs every pass. A classic `for (let i = 0; ...; i++)` genuinely reassigns its counter, so it needs `let`.',
          'Use `const` in `for...of` unless you actually reassign the variable inside the body.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Getting both index and value',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: '`entries()` gives index-value pairs, destructured in the header.',
        code: [
          'const names = ["Ada", "Grace"];',
          '',
          'for (const [index, name] of names.entries()) {',
          '  console.log(index, name, typeof index);',
          '}',
        ].join('\n'),
        output: ['0 Ada number', '1 Grace number'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Note the index is a genuine **number** here, unlike the strings `for...in` produces. `entries()` is the correct way to get both, and array destructuring in the loop header — which Module 12 covers — makes it readable.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'When `for...in` is appropriate',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`for...in` exists for **plain objects**, where you genuinely want the keys. Even there, modern code usually prefers `Object.keys()` with `for...of`, because it returns only the object\'s own keys and produces a real array you can filter or map.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Iterating an object three ways.',
        code: [
          'const settings = { theme: "dark", fontSize: 14 };',
          '',
          '// for...in — includes inherited enumerable properties',
          'for (const key in settings) {',
          '  console.log("in:", key, settings[key]);',
          '}',
          '',
          '// Object.keys — own keys only, as a real array',
          'for (const key of Object.keys(settings)) {',
          '  console.log("keys:", key, settings[key]);',
          '}',
          '',
          '// Object.entries — key and value together',
          'for (const [key, value] of Object.entries(settings)) {',
          '  console.log("entries:", key, value);',
          '}',
        ].join('\n'),
        output: [
          'in: theme dark',
          'in: fontSize 14',
          'keys: theme dark',
          'keys: fontSize 14',
          'entries: theme dark',
          'entries: fontSize 14',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: '`for...in` walks the prototype chain',
        body: [
          'It visits inherited enumerable properties as well as the object\'s own, so a property added to a shared prototype appears in every loop over every object of that kind. That is why older code wraps the body in `if (Object.hasOwn(obj, key))`.',
          '`Object.keys()` returns only own enumerable keys, which removes the problem entirely and is why it is the modern default. Module 15 covers these utilities properly.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const arr = ["x", "y"];',
          'let out = "";',
          '',
          'for (const i in arr) {',
          '  out += i;',
          '}',
          '',
          'for (const v of arr) {',
          '  out += v;',
          '}',
          '',
          'console.log(out);',
        ].join('\n'),
        options: ['"01xy"', '"xy01"', '"xyxy"', '"0101"'],
        correct: 0,
        explanation:
          'The `for...in` loop iterates the **keys**, which for an array are the indexes `"0"` and `"1"` as strings, so it appends `"01"`. The `for...of` loop iterates the **values**, appending `"xy"`. Concatenated in that order the result is `"01xy"`. Remembering which one gives keys and which gives values is the whole distinction — "of" gives you the things, "in" gives you their positions.',
      },
    ],
    exercises: [
      {
        id: 'ex-m11-04-a',
        title: 'Predict the for...in result',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['loops'],
        instructions: 'What does this print?',
        code: [
          'const items = ["a", "b"];',
          '',
          'for (const x in items) {',
          '  console.log(x);',
          '}',
        ].join('\n'),
        options: ['a then b', '0 then 1', 'undefined twice', '"a" then "b"'],
        correct: 1,
        hints: [
          'Does `for...in` give keys or values?',
          'What are an array\'s keys?',
        ],
        solution: '0 then 1',
        solutionExplanation:
          '`for...in` iterates over an object\'s enumerable **keys**, and an array\'s keys are its indexes. So it prints `0` then `1` — and importantly these are *strings*, not numbers, so `x + 1` would give `"01"` rather than `1`. To get the values you want `for...of`. Never use `for...in` on arrays: besides the string keys, it also visits any non-index properties.',
      },
      {
        id: 'ex-m11-04-b',
        title: 'Choose the right loop',
        kind: EXERCISE_KIND.CHOOSE_IMPLEMENTATION,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['loops'],
        instructions:
          'You need each element of an array together with its numeric index. Which is correct?',
        options: [
          'for (const i in arr)',
          'for (const [i, v] of arr.entries())',
          'for (const v of arr)',
          'for (const [i, v] in arr)',
        ],
        correct: 1,
        hints: [
          'Which option gives both pieces, with the index as a number?',
        ],
        solution: 'for (const [i, v] of arr.entries())',
        solutionExplanation:
          '`entries()` yields `[index, value]` pairs with a genuine numeric index, and destructuring in the header unpacks them. `for...in` would give string keys and no values. `for...of` alone gives values but no index. The fourth option is not valid — `for...in` yields single keys, so destructuring an array pattern from a string key would not work as intended. A classic `for (let i = 0; ...)` loop is also a perfectly good answer when you need both.',
      },
      {
        id: 'ex-m11-04-c',
        title: 'Sum object values',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['loops', 'objects'],
        instructions:
          'Write `sumValues(obj)` returning the total of every numeric value in a plain object. Ignore non-numeric values. Return `0` for an empty object.',
        starterCode: [
          'function sumValues(obj) {',
          '  // Iterate the values and add up the numbers.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'sums numeric values', body: 'expect(sumValues({ a: 1, b: 2, c: 3 })).toBe(6);' },
          { name: 'returns 0 for an empty object', body: 'expect(sumValues({})).toBe(0);' },
          { name: 'ignores non-numeric values', body: 'expect(sumValues({ a: 1, b: "x", c: 2 })).toBe(3);' },
          { name: 'handles a single value', body: 'expect(sumValues({ only: 5 })).toBe(5);' },
          { name: 'handles negatives and zero', body: 'expect(sumValues({ a: -5, b: 0, c: 5 })).toBe(0);' },
          { name: 'ignores nested objects', body: 'expect(sumValues({ a: 1, b: { c: 99 } })).toBe(1);', hidden: true },
        ],
        hints: [
          '`Object.values(obj)` gives an array of the values.',
          'Iterate it with `for...of`.',
          'Use `typeof v === "number"` to skip non-numbers — that also excludes nested objects.',
        ],
        solution: [
          'function sumValues(obj) {',
          '  let total = 0;',
          '',
          '  for (const value of Object.values(obj)) {',
          '    if (typeof value === "number") {',
          '      total += value;',
          '    }',
          '  }',
          '',
          '  return total;',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`Object.values` converts the object into a real array, which `for...of` then iterates by value — clearer than `for...in` plus a lookup, and it returns only the object\'s own properties so inherited ones cannot leak in. The `typeof` guard skips strings and, as the hidden test confirms, nested objects too, since neither is a number. Note it correctly keeps `0`, which a truthiness check like `if (value)` would have wrongly skipped.',
      },
      {
        id: 'ex-m11-04-d',
        title: 'Fix the for...in array bug',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 20,
        topicIds: ['loops'],
        instructions:
          'This function should return the sum of an array, but `for...in` gives string indexes so it concatenates instead. Fix it using `for...of`.',
        starterCode: [
          'function total(numbers) {',
          '  let sum = 0;',
          '  for (const n in numbers) {',
          '    sum += n;',
          '  }',
          '  return sum;',
          '}',
        ].join('\n'),
        tests: [
          { name: 'sums the values', body: 'expect(total([10, 20, 30])).toBe(60);' },
          { name: 'returns a number', body: 'expect(typeof total([1, 2])).toBe("number");' },
          { name: 'returns 0 for an empty array', body: 'expect(total([])).toBe(0);' },
          { name: 'handles a single element', body: 'expect(total([42])).toBe(42);' },
          { name: 'handles negatives', body: 'expect(total([5, -3])).toBe(2);' },
          { name: 'uses for...of rather than for...in', body: 'expect(/for\\s*\\(\\s*const\\s+\\w+\\s+of/.test(total.toString())).toBe(true);', hidden: true },
        ],
        hints: [
          '`for...in` gives keys — the string indexes `"0"`, `"1"`, `"2"`.',
          'Adding a number to a string concatenates.',
          'Change `in` to `of` so the loop yields the values.',
        ],
        solution: [
          'function total(numbers) {',
          '  let sum = 0;',
          '',
          '  for (const n of numbers) {',
          '    sum += n;',
          '  }',
          '',
          '  return sum;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'One keyword fixes it. With `in`, `n` held the string indexes, so `0 + "0"` gave `"00"` and the result was the string `"0012"` for `[10, 20, 30]` — a plausible-looking value that fails silently. With `of`, `n` holds the actual numeric values and `+=` performs addition. This is the clearest demonstration of why the two loops must not be confused: the code runs either way, and only the result reveals the mistake.',
      },
    ],
    quiz: {
      id: 'qz-m11-04',
      questions: [
        {
          id: 'q-m11-04-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['loops'],
          prompt: 'What is the essential difference between `for...of` and `for...in`?',
          options: [
            '`for...of` is faster',
            '`for...of` iterates values of an iterable; `for...in` iterates enumerable keys of an object',
            '`for...in` only works on arrays',
            'They are interchangeable',
          ],
          correct: 1,
          optionExplanations: [
            'Speed is not the distinction.',
            'Correct — values versus keys.',
            '`for...in` works on any object; arrays are where it is least appropriate.',
            'They produce genuinely different results.',
          ],
          explanation:
            '`for...of` yields the values of anything iterable — arrays, strings, Maps, Sets. `for...in` yields the enumerable property keys of an object, as strings, including inherited ones.',
        },
        {
          id: 'q-m11-04-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['loops', 'coercion'],
          prompt: 'Why does this concatenate rather than add?',
          code: [
            'const a = [10, 20];',
            'let s = 0;',
            'for (const i in a) s += i;',
            'console.log(s);',
          ].join('\n'),
          options: ['30', '"001"', '1', '"01"'],
          correct: 1,
          optionExplanations: [
            'That would need `for...of` to yield the values.',
            'Correct — `0 + "0"` gives `"00"`, then `+ "1"` gives `"001"`.',
            'The indexes are strings, so no numeric addition occurs.',
            'The initial `0` is part of the result.',
          ],
          explanation:
            '`for...in` yields the string keys `"0"` and `"1"`. Since `s` starts as the number `0`, the first `+=` produces `"00"` — a string, because `+` concatenates when either operand is a string — and the second appends `"1"`, giving `"001"`.',
        },
        {
          id: 'q-m11-04-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['loops'],
          prompt: 'How do you get both index and value in a `for...of` loop?',
          options: [
            'Use `for...in` instead',
            'Use `arr.entries()` and destructure: `for (const [i, v] of arr.entries())`',
            'It is not possible',
            'Use `arr.keys()`',
          ],
          correct: 1,
          optionExplanations: [
            'That gives string keys and no values.',
            'Correct — and the index is a genuine number.',
            '`entries()` handles it cleanly.',
            '`keys()` gives indexes only, without the values.',
          ],
          explanation:
            '`entries()` yields `[index, value]` pairs, destructured in the loop header. The index is a real number, unlike the strings `for...in` produces. A classic `for` loop is also perfectly acceptable when you need both.',
        },
        {
          id: 'q-m11-04-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['loops'],
          prompt: '`const` is valid in a `for...of` header even though the value changes each iteration.',
          options: ['True', 'False'],
          correct: 0,
          optionExplanations: [
            'Correct — each iteration creates a fresh binding rather than reassigning one.',
            'It is valid, and is the recommended form.',
          ],
          explanation:
            '`for...of` creates a new binding on every iteration, so nothing is ever reassigned and `const` is correct. A classic `for (let i = 0; ...; i++)` genuinely reassigns its counter via `i++`, so it requires `let`.',
        },
      ],
    },
    summary:
      '`for...of` iterates the **values** of anything iterable — arrays, strings, Maps, Sets — and creates a fresh binding each iteration, so `const` is the right declaration. `for...in` iterates the enumerable **keys** of an object as **strings**, including inherited ones from the prototype chain. Using `for...in` on an array is a mistake on three counts: the indexes are strings so arithmetic concatenates, non-index properties are included, and the order is not guaranteed by the specification. When you need both index and value, `arr.entries()` with destructuring gives a genuine numeric index. For plain objects, prefer `Object.keys()`, `Object.values()` or `Object.entries()` with `for...of` over `for...in`, since they return only own properties and produce real arrays.',
    keyTakeaways: [
      '`for...of` gives values; `for...in` gives keys as strings',
      'Never use `for...in` on an array',
      '`for...in` includes inherited enumerable properties',
      'Use `arr.entries()` for a numeric index alongside the value',
      '`const` is correct in a `for...of` header — each iteration is a new binding',
      'Prefer `Object.keys/values/entries` with `for...of` for plain objects',
    ],
    relatedLessons: ['l-m11-03', 'l-m11-05'],
    interviewConnections: [
      'What is the difference between for...of and for...in?',
      'Why should you not use for...in on an array?',
      'How do you get the index in a for...of loop?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m11-05',
    slug: 'choosing-a-loop',
    moduleId: M,
    order: 5,
    title: 'Choosing a Loop',
    description: 'A decision guide across every loop form, and a first look at the declarative alternatives.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 14,
    xp: 30,
    topicIds: ['loops', 'clean-code'],
    prerequisites: ['l-m11-04'],
    learningObjectives: [
      'Choose the right loop form for a given task',
      'Explain the difference between imperative and declarative iteration',
      'Recognise which array method replaces which loop pattern',
      'Explain why mutating an array while iterating it causes bugs',
    ],
    sections: [
      {
        kind: SECTION.HEADING,
        text: 'The decision guide',
      },
      {
        kind: SECTION.TABLE,
        headers: ['Task', 'Use', 'Why'],
        rows: [
          ['Do something with each value', '`for...of`', 'Cleanest; no index bookkeeping'],
          ['Need the index as well', '`for` or `entries()`', 'Both give a numeric index'],
          ['Need to count, step, or go backwards', '`for`', 'The header holds all three parts'],
          ['Repeat until a condition changes', '`while`', 'No count exists to put in a header'],
          ['Must run at least once', '`do...while`', 'The condition is checked afterwards'],
          ['Iterate a plain object', '`Object.entries()` with `for...of`', 'Own keys only, and a real array'],
          ['Transform every item into a new array', '`map`', 'Expresses intent; returns a new array'],
          ['Keep only some items', '`filter`', 'Expresses selection directly'],
          ['Reduce to a single value', '`reduce`', 'Sum, count, group, build an object'],
          ['Find one item', '`find` / `findIndex`', 'Stops at the first match'],
          ['Check whether any or all match', '`some` / `every`', 'Returns a boolean, stops early'],
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Module 13 covers the array methods properly',
        body: [
          'This lesson previews them so you can recognise when a loop you are writing has a name. Module 13 is devoted to them — how each one works, what its callback receives, and the mistakes each invites.',
          'Nothing here suggests loops are obsolete. A `for` loop remains the right tool for stepping, breaking early, and cases where an index calculation drives the logic.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Imperative versus declarative',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'A loop is **imperative**: it describes the mechanics — start a counter, check a condition, push into an accumulator. An array method is **declarative**: it names the operation and leaves the mechanics implicit.',
        ],
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Imperative — how',
          code: [
            'const doubled = [];',
            '',
            'for (let i = 0; i < nums.length; i++) {',
            '  doubled.push(nums[i] * 2);',
            '}',
            '',
            '// Reader must infer the intent',
            '// from the mechanics.',
          ].join('\n'),
        },
        right: {
          title: 'Declarative — what',
          code: [
            'const doubled = nums.map((n) => n * 2);',
            '',
            '',
            '',
            '',
            '// "map" states the intent.',
            '// The mechanics are implied.',
          ].join('\n'),
        },
        note: 'Both produce the same array. The second removes three opportunities for an off-by-one error and tells the reader what is happening rather than making them work it out.',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Four common loop patterns and the method that names each.',
        code: [
          'const nums = [1, 2, 3, 4, 5];',
          '',
          '// Transform',
          'const doubled = nums.map((n) => n * 2);',
          'console.log("map   :", doubled);',
          '',
          '// Select',
          'const evens = nums.filter((n) => n % 2 === 0);',
          'console.log("filter:", evens);',
          '',
          '// Reduce to one value',
          'const total = nums.reduce((sum, n) => sum + n, 0);',
          'console.log("reduce:", total);',
          '',
          '// Find one',
          'const firstBig = nums.find((n) => n > 3);',
          'console.log("find  :", firstBig);',
          '',
          '// The originals are untouched',
          'console.log("source:", nums);',
        ].join('\n'),
        output: [
          'map   : [2, 4, 6, 8, 10]',
          'filter: [2, 4]',
          'reduce: 15',
          'find  : 4',
          'source: [1, 2, 3, 4, 5]',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'When a loop is still the right answer',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Cases the array methods do not cover well.',
        code: [
          '// Stepping by something other than one',
          'const letters = ["a", "b", "c", "d", "e", "f"];',
          'const everySecond = [];',
          'for (let i = 0; i < letters.length; i += 2) {',
          '  everySecond.push(letters[i]);',
          '}',
          'console.log("stepped:", everySecond);',
          '',
          '// Comparing neighbouring elements',
          'const readings = [1, 3, 2, 8];',
          'const jumps = [];',
          'for (let i = 1; i < readings.length; i++) {',
          '  jumps.push(readings[i] - readings[i - 1]);',
          '}',
          'console.log("deltas :", jumps);',
          '',
          '// Looping a fixed number of times with no collection at all',
          'let row = "";',
          'for (let i = 0; i < 5; i++) row += "*";',
          'console.log("repeat :", row);',
        ].join('\n'),
        output: [
          'stepped: ["a", "c", "e"]',
          'deltas : [2, -1, 6]',
          'repeat : *****',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Each of these depends on index arithmetic — a step other than one, a comparison with the previous element, or a count with no collection behind it. Forcing them into array methods produces less readable code, not more.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Do not mutate an array while iterating it',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Removing items during a loop skips elements.',
        code: [
          '// Broken: removing shifts everything left, so the loop skips items',
          'const broken = ["a", "b", "b", "c"];',
          'for (let i = 0; i < broken.length; i++) {',
          '  if (broken[i] === "b") {',
          '    broken.splice(i, 1);',
          '  }',
          '}',
          'console.log("broken:", broken);',
          '',
          '// Correct: build a new array instead',
          'const source = ["a", "b", "b", "c"];',
          'const cleaned = source.filter((item) => item !== "b");',
          'console.log("filter:", cleaned);',
        ].join('\n'),
        output: [
          'broken: ["a", "b", "c"]',
          'filter: ["a", "c"]',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Why one `"b"` survives',
        body: [
          'When `splice` removes the `"b"` at index 1, everything after shifts left — so the second `"b"` moves into index 1. The loop then increments to index 2 and never looks at index 1 again.',
          'The fixes are to iterate backwards, so removals do not affect indexes you have yet to visit, or — much better — to build a new array with `filter` and leave the original alone.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'If you must remove in place, iterate backwards.',
        code: [
          'const items = ["a", "b", "b", "c"];',
          '',
          'for (let i = items.length - 1; i >= 0; i--) {',
          '  if (items[i] === "b") {',
          '    items.splice(i, 1);',
          '  }',
          '}',
          '',
          'console.log(items);',
        ].join('\n'),
        output: ['["a", "c"]'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Iterating backwards works because removing an element only shifts the ones *after* it, and those have already been visited. It is correct but easy to get wrong; `filter` is preferable whenever you can accept a new array.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const nums = [1, 2, 3];',
          '',
          'const a = nums.map((n) => n * 2);',
          'const b = [];',
          'for (const n of nums) b.push(n * 2);',
          '',
          'console.log(a.length === b.length, nums.length);',
        ].join('\n'),
        options: ['true 3', 'true 6', 'false 3', 'false 6'],
        correct: 0,
        explanation:
          'Both approaches produce `[2, 4, 6]`, so the lengths match and the comparison is `true`. Neither modifies the source: `map` returns a brand-new array by design, and the `for...of` version pushes into a separate `b`. So `nums.length` is still 3. The point is that the two are equivalent in effect — the choice between them is about readability, not behaviour.',
      },
    ],
    exercises: [
      {
        id: 'ex-m11-05-a',
        title: 'Choose the right construct',
        kind: EXERCISE_KIND.CHOOSE_IMPLEMENTATION,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['loops', 'clean-code'],
        instructions:
          'You need to turn an array of prices into an array of prices with tax added, keeping the same number of items. Which is the clearest choice?',
        options: [
          'A `for` loop pushing into a new array',
          '`prices.map((p) => p * 1.2)`',
          '`prices.filter((p) => p * 1.2)`',
          'A `while` loop with an index',
        ],
        correct: 1,
        hints: [
          'Same number of items out as in — which method names that operation?',
        ],
        solution: 'prices.map((p) => p * 1.2)',
        solutionExplanation:
          '`map` is exactly "transform each item into a new one, same length", so it names the intent and removes any chance of an index error. The `for` and `while` versions produce the same result but make a reader infer the purpose from the mechanics. `filter` is wrong: it selects items rather than transforming them, and since `p * 1.2` is truthy for every non-zero price it would return the original values rather than the taxed ones.',
      },
      {
        id: 'ex-m11-05-b',
        title: 'Fix the mutate-while-iterating bug',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['loops'],
        instructions:
          'This function should remove every zero from the array in place, but consecutive zeros survive. Fix it by iterating backwards, keeping the in-place `splice`.',
        starterCode: [
          'function removeZeros(items) {',
          '  for (let i = 0; i < items.length; i++) {',
          '    if (items[i] === 0) {',
          '      items.splice(i, 1);',
          '    }',
          '  }',
          '  return items;',
          '}',
        ].join('\n'),
        tests: [
          { name: 'removes a single zero', body: 'expect(removeZeros([1, 0, 2])).toEqual([1, 2]);' },
          { name: 'removes consecutive zeros', body: 'expect(removeZeros([1, 0, 0, 2])).toEqual([1, 2]);' },
          { name: 'removes leading zeros', body: 'expect(removeZeros([0, 0, 1])).toEqual([1]);' },
          { name: 'removes trailing zeros', body: 'expect(removeZeros([1, 0, 0])).toEqual([1]);' },
          { name: 'handles all zeros', body: 'expect(removeZeros([0, 0, 0])).toEqual([]);' },
          { name: 'handles no zeros', body: 'expect(removeZeros([1, 2])).toEqual([1, 2]);' },
          { name: 'mutates in place', body: 'const src = [1, 0]; const out = removeZeros(src); expect(out).toBe(src);', hidden: true },
        ],
        hints: [
          'Removing at index `i` shifts later elements left, so the next one is skipped.',
          'Start at `items.length - 1` and count down to 0.',
          'Removals then only affect indexes you have already passed.',
        ],
        solution: [
          'function removeZeros(items) {',
          '  for (let i = items.length - 1; i >= 0; i--) {',
          '    if (items[i] === 0) {',
          '      items.splice(i, 1);',
          '    }',
          '  }',
          '',
          '  return items;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Iterating backwards means a removal only shifts elements that have already been visited, so nothing is skipped — which is what the consecutive-zeros test verifies. The forward version failed because removing index 1 moved the second zero into index 1 while the counter advanced to 2. The hidden test confirms the function still mutates the caller\'s array rather than returning a copy, since that was the stated requirement; if a new array were acceptable, `items.filter((n) => n !== 0)` would be simpler and safer.',
      },
      {
        id: 'ex-m11-05-c',
        title: 'Compare neighbouring values',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['loops'],
        instructions:
          'Write `isIncreasing(numbers)` returning `true` when every element is strictly greater than the one before it. Arrays of length 0 or 1 count as increasing. Use a loop, since this compares neighbours.',
        starterCode: [
          'function isIncreasing(numbers) {',
          '  // Compare each element with the previous one.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'accepts an increasing sequence', body: 'expect(isIncreasing([1, 2, 3])).toBe(true);' },
          { name: 'rejects a decrease', body: 'expect(isIncreasing([1, 3, 2])).toBe(false);' },
          { name: 'rejects equal neighbours', body: 'expect(isIncreasing([1, 1, 2])).toBe(false);' },
          { name: 'accepts a single element', body: 'expect(isIncreasing([5])).toBe(true);' },
          { name: 'accepts an empty array', body: 'expect(isIncreasing([])).toBe(true);' },
          { name: 'handles negatives', body: 'expect(isIncreasing([-5, -1, 0])).toBe(true);' },
          { name: 'rejects a decrease at the end', body: 'expect(isIncreasing([1, 2, 3, 0])).toBe(false);', hidden: true },
        ],
        hints: [
          'Start the loop at index 1, so there is always a previous element.',
          'Return `false` as soon as `numbers[i] <= numbers[i - 1]`.',
          'Return `true` after the loop if nothing failed.',
        ],
        solution: [
          'function isIncreasing(numbers) {',
          '  for (let i = 1; i < numbers.length; i++) {',
          '    if (numbers[i] <= numbers[i - 1]) {',
          '      return false;',
          '    }',
          '  }',
          '',
          '  return true;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Starting at index 1 guarantees `numbers[i - 1]` always exists, which removes any need to special-case the first element. Returning `false` on the first violation exits immediately — the early-exit benefit of `break`, achieved with `return`. The empty and single-element cases need no special handling: the loop condition is false straight away and the function falls through to `true`, which matches the convention that a sequence too short to violate the rule satisfies it. Using `<=` rather than `<` is what makes equal neighbours count as a failure, since "strictly greater" was required.',
      },
    ],
    quiz: {
      id: 'qz-m11-05',
      questions: [
        {
          id: 'q-m11-05-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['loops', 'clean-code'],
          prompt: 'What is the difference between imperative and declarative iteration?',
          options: [
            'Declarative code is always shorter',
            'Imperative code describes the mechanics of iterating; declarative code names the operation being performed',
            'Imperative code is faster',
            'They produce different results',
          ],
          correct: 1,
          optionExplanations: [
            'Usually shorter, but that is a side effect rather than the definition.',
            'Correct — "how" versus "what".',
            'Any difference is negligible and not the point.',
            'Both can produce identical results.',
          ],
          explanation:
            'A loop spells out the counter, condition and accumulation. `map` or `filter` names the operation and leaves the mechanics implicit, so the reader learns the intent immediately rather than deducing it.',
        },
        {
          id: 'q-m11-05-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['loops'],
          prompt: 'Why does one "b" survive?',
          code: [
            'const a = ["a", "b", "b", "c"];',
            'for (let i = 0; i < a.length; i++) {',
            '  if (a[i] === "b") a.splice(i, 1);',
            '}',
            'console.log(a.length);',
          ].join('\n'),
          options: ['2', '3', '4', '0'],
          correct: 1,
          optionExplanations: [
            'That would require both "b" values to be removed.',
            'Correct — removing the first "b" shifts the second into its place, which the loop then skips.',
            'One element is removed.',
            'Not all elements are removed.',
          ],
          explanation:
            'Removing the element at index 1 shifts the second `"b"` into index 1, but the counter has already advanced to 2, so it is never examined. Iterate backwards, or build a new array with `filter`.',
        },
        {
          id: 'q-m11-05-3',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['loops'],
          prompt: 'For which tasks is a classic `for` loop still the best choice? (Select all that apply.)',
          options: [
            'Stepping by two',
            'Comparing each element with the previous one',
            'Transforming every item into a new array',
            'Repeating something a fixed number of times with no collection',
          ],
          correct: [0, 1, 3],
          explanation:
            'Stepping, neighbour comparison and plain repetition all depend on index arithmetic that array methods do not express well. Transforming every item into a new array is exactly what `map` names, so a loop there is more mechanics for no benefit.',
        },
      ],
    },
    summary:
      'Choose the loop that matches the task: `for...of` for values, `for` when you need an index or a step, `while` when termination depends on changing state, and `do...while` when the body must run at least once. For plain objects, `Object.entries()` with `for...of` is preferable to `for...in`. Many loops have a name — `map` for transforming, `filter` for selecting, `reduce` for collapsing to one value, `find` for locating one item — and using them states intent rather than mechanics, which is the difference between declarative and imperative code. Loops remain the right tool for index arithmetic: stepping by more than one, comparing neighbouring elements, or repeating a fixed number of times. Never remove items from an array while iterating it forwards; iterate backwards or, better, build a new array with `filter`.',
    keyTakeaways: [
      '`for...of` for values, `for` for indexes and steps, `while` for condition-driven repetition',
      'Array methods name the operation; loops spell out the mechanics',
      'Loops still win for stepping, neighbour comparison and plain repetition',
      'Removing while iterating forwards skips elements',
      'Iterate backwards for in-place removal, or use `filter` for a new array',
    ],
    relatedLessons: ['l-m11-04', 'l-m11-01'],
    interviewConnections: [
      'When would you use a for loop instead of map or filter?',
      'What happens if you remove items from an array while looping over it?',
      'What is the difference between imperative and declarative code?',
    ],
  },
];
