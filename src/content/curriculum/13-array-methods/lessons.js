import { SECTION, CALLOUT_TONE, DIFFICULTY, EXERCISE_KIND, QUIZ_KIND } from '../../schema/types.js';

const M = 'm13';

export default [
  /* ================================================================== */
  {
    id: 'l-m13-01',
    slug: 'foreach-and-map',
    moduleId: M,
    order: 1,
    title: 'forEach and map',
    description: 'The two most-used iteration methods, what their callbacks receive, and the missing-return bug that defines map.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 17,
    xp: 35,
    topicIds: ['array-methods', 'higher-order'],
    prerequisites: ['l-m12-04'],
    learningObjectives: [
      'Use `forEach` for side effects and `map` for transformation',
      'Name the three arguments every iteration callback receives',
      'Explain why a `map` callback must return a value',
      'Choose between `forEach` and `map` deliberately',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Module 11 introduced these briefly. This module gives them the treatment they deserve, because array methods are the daily language of professional JavaScript — you will write more of them than loops.',
          'All of them take a **callback**, which is the idea from Module 09: a function you hand over for the method to call.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Every iteration callback receives three arguments',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'element, index, and the whole array.',
        code: [
          'const letters = ["a", "b", "c"];',
          '',
          'letters.forEach((element, index, array) => {',
          '  console.log(`${index}: ${element} of ${array.length}`);',
          '});',
          '',
          '// Most callbacks only need the first',
          'letters.forEach((letter) => console.log(letter.toUpperCase()));',
        ].join('\n'),
        output: [
          '0: a of 3',
          '1: b of 3',
          '2: c of 3',
          'A',
          'B',
          'C',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Declare only the parameters you need',
        body: [
          'Extra arguments are silently ignored by a callback that declares fewer parameters, so `(letter) => ...` is perfectly fine even though three are supplied.',
          'The hazard is accidentally *accepting* one you did not want — the `map(parseInt)` bug from Module 03, where `parseInt` treats the index as a radix. Always know how many arguments a method supplies.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'forEach: for side effects',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`forEach` calls your function once per element and **always returns `undefined`**. It exists to *do* something — log, update the page, push into something else — not to produce a value.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: '`forEach` returns undefined, always.',
        code: [
          'const nums = [1, 2, 3];',
          '',
          'const result = nums.forEach((n) => n * 2);',
          'console.log("forEach returned:", result);',
          '',
          '// Its purpose is the side effect',
          'let total = 0;',
          'nums.forEach((n) => { total += n; });',
          'console.log("accumulated:", total);',
        ].join('\n'),
        output: ['forEach returned: undefined', 'accumulated: 6'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'You cannot break out of forEach',
        body: [
          '`break` is a SyntaxError inside a `forEach` callback, and `return` only exits that one callback invocation — the iteration continues. There is no way to stop early.',
          'If you need early exit, use `for...of` with `break`, or a method designed to stop: `find`, `some` or `every`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'map: for transformation',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`map` calls your function once per element and collects the **returned values** into a new array of the same length. The original is untouched.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Same length out as in, original unchanged.',
        code: [
          'const prices = [10, 20, 30];',
          '',
          'const withTax = prices.map((p) => p * 1.2);',
          'console.log("mapped  :", withTax);',
          'console.log("original:", prices);',
          'console.log("lengths :", prices.length, withTax.length);',
          '',
          '// Any transformation, including changing type',
          'const labels = prices.map((p) => `£${p}`);',
          'console.log("labels  :", labels);',
          '',
          '// Using the index',
          'const numbered = prices.map((p, i) => `${i + 1}. £${p}`);',
          'console.log("numbered:", numbered);',
        ].join('\n'),
        output: [
          'mapped  : [12, 24, 36]',
          'original: [10, 20, 30]',
          'lengths : 3 3',
          'labels  : ["£10", "£20", "£30"]',
          'numbered: ["1. £10", "2. £20", "3. £30"]',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'The missing-return bug',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This is the defining `map` mistake, and it comes straight from the arrow-function body rules in Module 09.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Braces without `return` give an array of undefined.',
        code: [
          'const nums = [1, 2, 3];',
          '',
          '// Broken — block body, no return',
          'const broken = nums.map((n) => {',
          '  n * 2;',
          '});',
          'console.log("broken:", broken);',
          '',
          '// Fixed with an explicit return',
          'const fixedA = nums.map((n) => {',
          '  return n * 2;',
          '});',
          '',
          '// Fixed by removing the braces',
          'const fixedB = nums.map((n) => n * 2);',
          '',
          'console.log("fixed :", fixedA, fixedB);',
        ].join('\n'),
        output: [
          'broken: [undefined, undefined, undefined]',
          'fixed : [2, 4, 6] [2, 4, 6]',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'The symptom to recognise',
        body: [
          'An array of the right **length** full of `undefined` almost always means a `map` callback with braces and no `return`. The length is right because `map` always produces one output per input — it simply collected `undefined` each time.',
          'Remember the same trap when returning an object: `(n) => { value: n }` is a block, not an object literal. Write `(n) => ({ value: n })`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'map versus forEach',
      },
      {
        kind: SECTION.TABLE,
        headers: ['', '`forEach`', '`map`'],
        rows: [
          ['Returns', '`undefined`', 'A new array, same length'],
          ['Callback return value', 'Ignored', 'Becomes the new element'],
          ['Purpose', 'Side effects', 'Transformation'],
          ['Chainable', 'No', 'Yes'],
          ['Mutates the original', 'No', 'No'],
          ['Can stop early', 'No', 'No'],
        ],
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Wrong — map used for side effects',
          code: [
            'items.map((item) => {',
            '  console.log(item);',
            '});',
            '',
            '// Builds an array of undefined',
            '// and throws it away. Misleads',
            '// the reader into expecting',
            '// a transformation.',
          ].join('\n'),
        },
        right: {
          title: 'Wrong — forEach used to transform',
          code: [
            'const out = [];',
            'items.forEach((item) => {',
            '  out.push(item * 2);',
            '});',
            '',
            '// Works, but map says the',
            '// same thing in one line',
            '// with no external array.',
          ].join('\n'),
        },
        note: 'Use the method whose *return value* you actually want. If you are discarding what `map` returns, you wanted `forEach`. If you are pushing into an array from a `forEach`, you wanted `map`.',
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Keep map callbacks pure',
        body: [
          'A `map` callback should take an element and return a new value — nothing else. Mutating the element, or writing to an outer variable, makes the result depend on iteration order and is exactly the impurity Module 09 warned about.',
          'Mutating objects inside a `map` is especially deceptive: the new array holds the *same* objects, so `items.map((o) => { o.seen = true; return o; })` modifies every original too.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const values = [1, 2, 3];',
          '',
          'const result = values.map((value) => {',
          '  value * 2;',
          '});',
          '',
          'console.log(result.length, result[0]);',
        ].join('\n'),
        options: [
          '3 undefined',
          '3 2',
          '0 undefined',
          'undefined undefined',
        ],
        correct: 0,
        explanation:
          'The braces make this a block body, so the implicit return does not apply — `value * 2` is computed and discarded, and the callback returns `undefined`. `map` still produces one output per input, so the result has length 3, filled entirely with `undefined`. That combination of correct length and `undefined` contents is the signature of this bug. Removing the braces, or adding `return`, fixes it.',
      },
    ],
    exercises: [
      {
        id: 'ex-m13-01-a',
        title: 'Predict the forEach return',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['array-methods'],
        instructions: 'What does this print?',
        code: [
          'const r = [1, 2].forEach((n) => n * 2);',
          'console.log(r);',
        ].join('\n'),
        options: ['[2, 4]', 'undefined', '[1, 2]', '2'],
        correct: 1,
        hints: [
          'What is `forEach` designed to produce?',
        ],
        solution: 'undefined',
        solutionExplanation:
          '`forEach` always returns `undefined`, regardless of what the callback returns. It exists to perform side effects, not to build a value. The callback here computes `n * 2` and the result is discarded on every iteration. Use `map` when you want the transformed values collected into a new array.',
      },
      {
        id: 'ex-m13-01-b',
        title: 'Fix the missing return',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 20,
        topicIds: ['array-methods'],
        instructions:
          'This function should return the names in uppercase, but every element comes back as `undefined`. Fix it.',
        starterCode: [
          'function shoutNames(names) {',
          '  return names.map((name) => {',
          '    name.toUpperCase();',
          '  });',
          '}',
        ].join('\n'),
        tests: [
          { name: 'uppercases each name', body: 'expect(shoutNames(["ada", "grace"])).toEqual(["ADA", "GRACE"]);' },
          { name: 'preserves the length', body: 'expect(shoutNames(["a", "b", "c"]).length).toBe(3);' },
          { name: 'handles an empty array', body: 'expect(shoutNames([])).toEqual([]);' },
          { name: 'does not modify the input', body: 'const src = ["ada"]; shoutNames(src); expect(src[0]).toBe("ada");' },
          { name: 'handles an empty string', body: 'expect(shoutNames([""])).toEqual([""]);', hidden: true },
        ],
        hints: [
          'The braces make it a block body, so the implicit return is switched off.',
          'Either add `return`, or remove the braces entirely.',
        ],
        solution: [
          'function shoutNames(names) {',
          '  return names.map((name) => name.toUpperCase());',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Removing the braces restores the implicit return, so each transformed value becomes an element of the new array. Writing `{ return name.toUpperCase(); }` is equally correct. The input is untouched either way, because `map` builds a new array and `toUpperCase` returns a new string rather than modifying one — strings are immutable, as Module 05 established.',
      },
      {
        id: 'ex-m13-01-c',
        title: 'Format a price list',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['array-methods'],
        instructions:
          'Write `formatList(products)` taking an array of `{ name, price }` objects and returning an array of strings like `"1. Lamp — £25.00"`, numbered from 1. Use `map`.',
        starterCode: [
          'function formatList(products) {',
          '  // The index is the second callback argument.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'formats one product', body: 'expect(formatList([{ name: "Lamp", price: 25 }])).toEqual(["1. Lamp — £25.00"]);' },
          { name: 'numbers from one', body: 'expect(formatList([{ name: "A", price: 1 }, { name: "B", price: 2 }])[1]).toBe("2. B — £2.00");' },
          { name: 'pads decimals', body: 'expect(formatList([{ name: "X", price: 9.5 }])[0]).toBe("1. X — £9.50");' },
          { name: 'handles an empty array', body: 'expect(formatList([])).toEqual([]);' },
          { name: 'returns the same length', body: 'expect(formatList([{ name: "A", price: 1 }, { name: "B", price: 2 }]).length).toBe(2);' },
          { name: 'handles a zero price', body: 'expect(formatList([{ name: "Free", price: 0 }])[0]).toBe("1. Free — £0.00");', hidden: true },
        ],
        hints: [
          'The callback signature is `(product, index)`.',
          'Add 1 to the index for one-based numbering.',
          '`price.toFixed(2)` gives two decimal places.',
        ],
        solution: [
          'function formatList(products) {',
          '  return products.map((product, index) =>',
          '    `${index + 1}. ${product.name} — £${product.price.toFixed(2)}`);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The second callback parameter gives the zero-based index, and adding 1 produces the human numbering. `toFixed(2)` returns a string with exactly two decimals, padding with zeros — which is why `9.5` becomes `"9.50"` and the hidden zero-price test passes as `"£0.00"`. `map` guarantees one output per input, so the length always matches and the empty case needs no special handling.',
      },
      {
        id: 'ex-m13-01-d',
        title: 'Choose forEach or map',
        kind: EXERCISE_KIND.CHOOSE_IMPLEMENTATION,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['array-methods'],
        instructions:
          'You need to log every item in an array to the console, producing no new data. Which is correct?',
        options: [
          'items.map((i) => console.log(i))',
          'items.forEach((i) => console.log(i))',
          'items.filter((i) => console.log(i))',
          'items.reduce((a, i) => console.log(i))',
        ],
        correct: 1,
        hints: [
          'Which method exists specifically for side effects?',
          'Are you producing a new array?',
        ],
        solution: 'items.forEach((i) => console.log(i))',
        solutionExplanation:
          '`forEach` is designed for side effects and returns `undefined`, which matches exactly what is wanted. The `map` version would build an array of `undefined` values — since `console.log` returns `undefined` — and immediately discard it, which is wasteful and misleads a reader into expecting a transformation. `filter` and `reduce` are both wrong tools with additional confusing behaviour.',
      },
    ],
    quiz: {
      id: 'qz-m13-01',
      questions: [
        {
          id: 'q-m13-01-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['array-methods'],
          prompt: 'What is the essential difference between `map` and `forEach`?',
          options: [
            '`map` is faster',
            '`map` returns a new array built from the callback’s return values; `forEach` returns `undefined`',
            '`map` mutates the original array',
            '`forEach` can transform elements but `map` cannot',
          ],
          correct: 1,
          optionExplanations: [
            'Speed is not the distinction.',
            'Correct — the return value is the whole difference.',
            'Neither mutates the original.',
            'This has the two reversed.',
          ],
          explanation:
            '`map` collects whatever the callback returns into a new array of the same length. `forEach` ignores the return value entirely and yields `undefined`. Choose the one whose return value you actually want.',
        },
        {
          id: 'q-m13-01-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['array-methods'],
          prompt: 'Why is this array full of undefined?',
          code: [
            'console.log([1, 2].map((n) => { n; }));',
          ].join('\n'),
          options: [
            '[1, 2]',
            '[undefined, undefined]',
            '[]',
            'undefined',
          ],
          correct: 1,
          optionExplanations: [
            'The callback returns nothing, so the values are not preserved.',
            'Correct — a block body with no `return` yields `undefined` each time.',
            '`map` always produces one element per input.',
            '`map` returns an array, not `undefined`.',
          ],
          explanation:
            'The braces create a block body, which switches off the implicit return. The callback returns `undefined` on every call, and `map` faithfully collects those. The length is still correct, which is the signature of this bug.',
        },
        {
          id: 'q-m13-01-3',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['array-methods'],
          prompt: 'Which arguments does an iteration callback receive? (Select all that apply.)',
          options: ['The current element', 'The current index', 'The whole array', 'The previous element'],
          correct: [0, 1, 2],
          explanation:
            'Every iteration callback is called with `(element, index, array)`. There is no previous-element argument — `reduce` provides an accumulator instead, which serves that purpose. Declaring fewer parameters is fine, but accidentally accepting one you did not want causes bugs such as `map(parseInt)`.',
        },
        {
          id: 'q-m13-01-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['array-methods'],
          prompt: 'You can use `break` to stop a `forEach` early.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            '`break` is a SyntaxError inside a callback function.',
            'Correct — `forEach` cannot be stopped early.',
          ],
          explanation:
            '`break` only works inside a loop, not inside a callback function, and `return` merely ends that one callback invocation. For early exit use `for...of` with `break`, or `find`, `some` or `every`, which stop as soon as the answer is known.',
        },
      ],
    },
    summary:
      'Every iteration callback receives `(element, index, array)`, and declaring fewer parameters is safe — the hazard is accepting one you did not want. `forEach` calls the callback for its **side effects** and always returns `undefined`; it cannot be stopped early, since `break` is invalid in a callback and `return` only ends that invocation. `map` collects the callback\'s **return values** into a new array of the same length, leaving the original untouched. The defining `map` mistake is a block body with no `return`, which produces an array of the correct length full of `undefined` — and the same trap applies when returning an object literal, which needs wrapping parentheses. Choose the method whose return value you actually want: discarding a `map` result means you wanted `forEach`, and pushing from a `forEach` means you wanted `map`.',
    keyTakeaways: [
      'Callbacks receive `(element, index, array)`',
      '`forEach` is for side effects and returns `undefined`',
      '`map` returns a new array of the same length from the callback’s return values',
      'Braces without `return` give an array of `undefined` — the classic `map` bug',
      '`forEach` cannot be stopped early; use `for...of`, `find`, `some` or `every`',
      'Keep `map` callbacks pure — mutating elements affects the originals',
    ],
    relatedLessons: ['l-m12-04', 'l-m13-02'],
    interviewConnections: [
      'What is the difference between map and forEach?',
      'Why would a map produce an array of undefined?',
      'Can you break out of a forEach?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m13-02',
    slug: 'filter',
    moduleId: M,
    order: 2,
    title: 'filter, and map versus filter',
    description: 'Selecting a subset by a predicate, and the distinction between transforming and choosing.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 15,
    xp: 35,
    topicIds: ['array-methods', 'higher-order'],
    prerequisites: ['l-m13-01'],
    learningObjectives: [
      'Select elements with a predicate callback',
      'Explain why the result length varies while `map`’s never does',
      'Explain how truthiness decides what `filter` keeps',
      'Choose between `map` and `filter` for a given task',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          '`filter` calls a **predicate** — a function returning true or false — once per element, and builds a new array of the elements for which it returned a truthy value.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The result length depends on how many pass.',
        code: [
          'const numbers = [1, 2, 3, 4, 5, 6];',
          '',
          'const evens = numbers.filter((n) => n % 2 === 0);',
          'console.log("evens :", evens);',
          '',
          'const big = numbers.filter((n) => n > 4);',
          'console.log("big   :", big);',
          '',
          'const none = numbers.filter((n) => n > 100);',
          'console.log("none  :", none);',
          '',
          'const all = numbers.filter(() => true);',
          'console.log("all   :", all.length);',
          '',
          'console.log("original:", numbers);',
        ].join('\n'),
        output: [
          'evens : [2, 4, 6]',
          'big   : [5, 6]',
          'none  : []',
          'all   : 6',
          'original: [1, 2, 3, 4, 5, 6]',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: '`filter` never returns `null`',
        body: [
          'When nothing matches you get an **empty array**, not `null` or `undefined`. That is convenient: you can safely call `.length` or chain another method without a guard.',
          'It also means `if (results)` is always true — an empty array is an object and therefore truthy. Check `results.length > 0` instead, as Module 03 established.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Truthiness decides',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`filter` keeps an element when the callback returns anything **truthy** — not strictly `true`. That is usually convenient and occasionally surprising.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Any truthy return keeps the element.',
        code: [
          'const words = ["hello", "", "world", ""];',
          '',
          '// Returning a length: 0 is falsy, so empty strings are dropped',
          'console.log(words.filter((w) => w.length));',
          '',
          '// The compact idiom for removing all falsy values',
          'const mixed = [0, 1, "", "a", null, undefined, NaN, false, 2];',
          'console.log(mixed.filter(Boolean));',
        ].join('\n'),
        output: [
          '["hello", "world"]',
          '[1, "a", 2]',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`filter(Boolean)` passes the `Boolean` function itself as the predicate, so each element is converted to a boolean and only truthy ones survive. It is a widely-recognised idiom for stripping empty values — but remember from Module 03 that it also removes `0` and `false`, which may be legitimate data.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Filtering objects',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The common real-world shape.',
        code: [
          'const products = [',
          '  { name: "Lamp", price: 25, inStock: true },',
          '  { name: "Desk", price: 150, inStock: false },',
          '  { name: "Pen", price: 2, inStock: true },',
          '];',
          '',
          'const available = products.filter((p) => p.inStock);',
          'console.log("available:", available.map((p) => p.name));',
          '',
          'const affordable = products.filter((p) => p.price < 100);',
          'console.log("under 100:", affordable.map((p) => p.name));',
          '',
          '// Combining conditions',
          'const both = products.filter((p) => p.inStock && p.price < 100);',
          'console.log("both     :", both.map((p) => p.name));',
        ].join('\n'),
        output: [
          'available: ["Lamp", "Pen"]',
          'under 100: ["Lamp", "Pen"]',
          'both     : ["Lamp", "Pen"]',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'The filtered array holds the same objects',
        body: [
          '`filter` copies **references**, not objects. So `available[0]` and `products[0]` are the same object, and mutating one is visible through both.',
          'That is usually what you want — but if you intend to modify the results independently, map to copies as well: `.filter(...).map((p) => ({ ...p }))`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'map versus filter',
      },
      {
        kind: SECTION.TABLE,
        headers: ['', '`map`', '`filter`'],
        rows: [
          ['Purpose', 'Transform each element', 'Select a subset'],
          ['Result length', 'Always the same as the input', 'Zero up to the input length'],
          ['Callback returns', 'The new element', 'Whether to keep the element'],
          ['Elements in the result', 'New values', 'The original elements'],
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The same array, two different questions.',
        code: [
          'const nums = [1, 2, 3, 4];',
          '',
          '// "What is each one, transformed?" — same length',
          'console.log("map   :", nums.map((n) => n > 2));',
          '',
          '// "Which ones qualify?" — fewer elements',
          'console.log("filter:", nums.filter((n) => n > 2));',
        ].join('\n'),
        output: [
          'map   : [false, false, true, true]',
          'filter: [3, 4]',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Identical callbacks, completely different results. `map` recorded the *answer* for each element and kept them all; `filter` used the answer to *decide* and kept the elements themselves. If you find yourself with an array of booleans and wanting the matching items, you used `map` where you meant `filter`.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Do not use `filter` to transform',
        body: [
          '`items.filter((i) => i * 2)` does not double anything. The callback\'s return value is only used as a yes-or-no decision, and since every non-zero number is truthy, this returns the original items minus any zeros.',
          'It looks like it might work and produces a plausible array, which is what makes it worth flagging.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Filter then map',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The most common chain in real code.',
        code: [
          'const orders = [',
          '  { id: 1, total: 50, paid: true },',
          '  { id: 2, total: 30, paid: false },',
          '  { id: 3, total: 90, paid: true },',
          '];',
          '',
          'const paidIds = orders',
          '  .filter((o) => o.paid)',
          '  .map((o) => o.id);',
          '',
          'console.log("paid ids:", paidIds);',
          '',
          '// Order matters for efficiency — filter first shrinks the work',
          'const paidTotals = orders',
          '  .filter((o) => o.paid)',
          '  .map((o) => `£${o.total}`);',
          '',
          'console.log("totals  :", paidTotals);',
        ].join('\n'),
        output: [
          'paid ids: [1, 3]',
          'totals  : ["£50", "£90"]',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Filtering before mapping means the transformation runs on fewer elements. It also reads in the order you would describe the task aloud: "take the paid ones, then get their ids."',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const nums = [0, 1, 2, 3];',
          '',
          'const a = nums.filter((n) => n > 1);',
          'const b = nums.map((n) => n > 1);',
          '',
          'console.log(a.length, b.length);',
        ].join('\n'),
        options: ['2 4', '2 2', '4 4', '4 2'],
        correct: 0,
        explanation:
          '`filter` keeps only the elements for which the predicate is truthy — `2` and `3` — so its result has length 2. `map` records the boolean answer for **every** element and always returns an array of the same length as the input, so `b` is `[false, false, true, true]` with length 4. The identical callback produces two different shapes because the methods use its return value for different purposes.',
      },
    ],
    exercises: [
      {
        id: 'ex-m13-02-a',
        title: 'Predict the filter length',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['array-methods'],
        instructions: 'What does this print?',
        code: [
          'const r = [1, 2, 3].filter((n) => n > 10);',
          'console.log(r, r.length);',
        ].join('\n'),
        options: ['null 0', '[] 0', 'undefined 0', '[1, 2, 3] 3'],
        correct: 1,
        hints: [
          'What does `filter` return when nothing matches?',
        ],
        solution: '[] 0',
        solutionExplanation:
          '`filter` always returns an array — an empty one when nothing passes the predicate. It never returns `null` or `undefined`, which is convenient because you can safely chain another method or read `.length` without a guard. Note that an empty array is truthy, so `if (r)` would be true even with no results; test `r.length > 0` instead.',
      },
      {
        id: 'ex-m13-02-b',
        title: 'Filter valid entries',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['array-methods'],
        instructions:
          'Write `validScores(entries)` returning only the entries whose `score` is a finite number between 0 and 100 inclusive. Entries are objects like `{ name, score }`.',
        starterCode: [
          'function validScores(entries) {',
          '  // A predicate combining three conditions.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'keeps valid scores', body: 'expect(validScores([{ score: 50 }, { score: 80 }]).length).toBe(2);' },
          { name: 'drops scores above 100', body: 'expect(validScores([{ score: 150 }])).toEqual([]);' },
          { name: 'drops negative scores', body: 'expect(validScores([{ score: -1 }])).toEqual([]);' },
          { name: 'keeps the boundaries', body: 'expect(validScores([{ score: 0 }, { score: 100 }]).length).toBe(2);' },
          { name: 'drops non-numeric scores', body: 'expect(validScores([{ score: "50" }, { score: null }])).toEqual([]);' },
          { name: 'drops NaN', body: 'expect(validScores([{ score: NaN }])).toEqual([]);' },
          { name: 'returns the original objects', body: 'const e = { score: 50 }; expect(validScores([e])[0]).toBe(e);', hidden: true },
        ],
        hints: [
          '`Number.isFinite` rejects non-numbers, NaN and the infinities in one call.',
          'Then check the range with `>= 0 && <= 100`.',
          '`filter` returns the original objects, not copies.',
        ],
        solution: [
          'function validScores(entries) {',
          '  return entries.filter((entry) =>',
          '    Number.isFinite(entry.score) && entry.score >= 0 && entry.score <= 100);',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`Number.isFinite` does the heavy lifting: it rejects the string `"50"`, `null` and `NaN` without any coercion, which three separate tests verify. Checking it *first* matters because every comparison with `NaN` is false, so a range check alone would silently drop `NaN` without making the reason clear. The boundary test confirms `>=` and `<=` rather than strict inequalities. The hidden test shows `filter` copies references — the result holds the same objects, not clones.',
      },
      {
        id: 'ex-m13-02-c',
        title: 'Filter then transform',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['array-methods'],
        instructions:
          'Write `activeNames(users)` returning an array of the uppercase names of users where `active` is true. Chain `filter` and `map`, filtering first.',
        starterCode: [
          'function activeNames(users) {',
          '  // Select, then transform.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'selects and transforms', body: 'expect(activeNames([{ name: "ada", active: true }, { name: "bob", active: false }])).toEqual(["ADA"]);' },
          { name: 'handles all active', body: 'expect(activeNames([{ name: "a", active: true }, { name: "b", active: true }])).toEqual(["A", "B"]);' },
          { name: 'handles none active', body: 'expect(activeNames([{ name: "a", active: false }])).toEqual([]);' },
          { name: 'handles an empty array', body: 'expect(activeNames([])).toEqual([]);' },
          { name: 'does not modify the input', body: 'const src = [{ name: "ada", active: true }]; activeNames(src); expect(src[0].name).toBe("ada");' },
          { name: 'treats a missing active flag as inactive', body: 'expect(activeNames([{ name: "a" }])).toEqual([]);', hidden: true },
        ],
        hints: [
          '`.filter((u) => u.active)` keeps the active users.',
          '`.map((u) => u.name.toUpperCase())` transforms them.',
          'Chain the two calls.',
        ],
        solution: [
          'function activeNames(users) {',
          '  return users',
          '    .filter((user) => user.active)',
          '    .map((user) => user.name.toUpperCase());',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Filtering first means `map` runs on fewer elements and, more importantly, the chain reads in the order you would state the task. The predicate uses truthiness rather than `=== true`, which is why a missing `active` property counts as inactive — `undefined` is falsy, satisfying the hidden test. Both methods return new arrays, so the input objects are never touched. Note the order matters for correctness too: mapping first would produce strings, and the filter would then have no `active` property to check.',
      },
      {
        id: 'ex-m13-02-d',
        title: 'Remove falsy values safely',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['array-methods'],
        instructions:
          'Write `removeEmpty(items)` returning a new array without `null`, `undefined` or empty strings — but **keeping** `0` and `false`, which are legitimate values.',
        starterCode: [
          'function removeEmpty(items) {',
          '  // filter(Boolean) would wrongly drop 0 and false.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'removes null and undefined', body: 'expect(removeEmpty([1, null, 2, undefined])).toEqual([1, 2]);' },
          { name: 'removes empty strings', body: 'expect(removeEmpty(["a", "", "b"])).toEqual(["a", "b"]);' },
          { name: 'keeps zero', body: 'expect(removeEmpty([0, 1])).toEqual([0, 1]);' },
          { name: 'keeps false', body: 'expect(removeEmpty([false, true])).toEqual([false, true]);' },
          { name: 'keeps NaN', body: 'expect(removeEmpty([NaN]).length).toBe(1);' },
          { name: 'handles an empty array', body: 'expect(removeEmpty([])).toEqual([]);' },
          { name: 'keeps whitespace-only strings', body: 'expect(removeEmpty([" "])).toEqual([" "]);', hidden: true },
        ],
        hints: [
          '`filter(Boolean)` is too aggressive — it drops `0`, `false` and `NaN`.',
          'Test explicitly for the three values you want removed.',
          '`item != null` covers both `null` and `undefined`.',
        ],
        solution: [
          'function removeEmpty(items) {',
          '  return items.filter((item) => item != null && item !== "");',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The idiomatic `item != null` from Module 03 covers `null` and `undefined` together and nothing else, and the explicit `!== ""` removes empty strings. Everything else survives — which is what the `0`, `false` and `NaN` tests verify, and precisely where `filter(Boolean)` would fail. The hidden test confirms a whitespace-only string is kept, since `" "` is not `""`; if you wanted to drop those too you would trim first, but that is a different requirement worth stating explicitly rather than assuming.',
      },
    ],
    quiz: {
      id: 'qz-m13-02',
      questions: [
        {
          id: 'q-m13-02-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['array-methods'],
          prompt: 'How does the result length of `filter` compare with `map`?',
          options: [
            'Both always match the input length',
            '`map` always matches the input length; `filter` returns zero up to that many',
            '`filter` always matches; `map` varies',
            'Both vary depending on the callback',
          ],
          correct: 1,
          optionExplanations: [
            '`filter` removes elements that fail the predicate.',
            'Correct — one output per input versus a subset.',
            'This has the two reversed.',
            '`map` never varies.',
          ],
          explanation:
            '`map` produces exactly one output per input, so the length always matches. `filter` keeps only elements whose predicate is truthy, so the result can be anything from empty to the full length.',
        },
        {
          id: 'q-m13-02-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['array-methods'],
          prompt: 'What does this filter produce?',
          code: [
            'console.log([0, 1, 2].filter((n) => n * 2));',
          ].join('\n'),
          options: ['[0, 2, 4]', '[1, 2]', '[0, 1, 2]', '[2, 4]'],
          correct: 1,
          optionExplanations: [
            'That would be `map`; `filter` never transforms.',
            'Correct — `0 * 2` is `0`, which is falsy, so `0` is dropped.',
            'The zero is removed.',
            'The original elements are returned, not the doubled values.',
          ],
          explanation:
            '`filter` uses the callback\'s return value only as a truthiness decision and returns the **original** elements. `0 * 2` is `0`, which is falsy, so `0` is dropped; the others return truthy numbers and survive unchanged. Using `filter` to transform is a common mistake that produces a plausible-looking wrong answer.',
        },
        {
          id: 'q-m13-02-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['array-methods'],
          prompt: 'What does `arr.filter(Boolean)` do?',
          options: [
            'Converts every element to a boolean',
            'Removes every falsy element, including `0`, `""` and `false`',
            'Keeps only elements that are literally `true`',
            'Throws, because `Boolean` is not a predicate',
          ],
          correct: 1,
          optionExplanations: [
            'The elements themselves are returned unchanged.',
            'Correct — a compact idiom, but it removes more than people expect.',
            'Any truthy value is kept, not just `true`.',
            '`Boolean` is a perfectly valid one-argument function.',
          ],
          explanation:
            'Passing `Boolean` as the predicate converts each element and keeps the truthy ones, returning the original values. It is concise and widely recognised, but it also removes `0`, `false` and `NaN` — check that none of those are legitimate data before using it.',
        },
      ],
    },
    summary:
      '`filter` runs a predicate on each element and returns a new array of the elements for which it was **truthy** — the original elements, not transformed values. The result length varies from zero to the input length, and it is always an array, never `null`, so chaining is safe; but an empty array is truthy, so test `.length > 0` rather than the array itself. The distinction from `map` is transformation versus selection: with an identical callback, `map` records the answer for every element while `filter` uses it to decide. Using `filter` to transform silently returns the originals minus any falsy results. The filtered array holds the **same objects**, so mutating a result is visible in the source. Filtering before mapping both shrinks the work and reads in the order you would describe the task.',
    keyTakeaways: [
      '`filter` selects; `map` transforms',
      'The predicate result is used for truthiness, and the original element is kept',
      '`filter` always returns an array — empty when nothing matches',
      '`filter(Boolean)` also removes `0`, `false` and `NaN`',
      'The result holds the same objects, not copies',
      'Filter before mapping: less work, and it reads in task order',
    ],
    relatedLessons: ['l-m13-01', 'l-m13-03'],
    interviewConnections: [
      'What is the difference between map and filter?',
      'What does filter return when nothing matches?',
      'What does filter(Boolean) do?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m13-03',
    slug: 'find-and-findindex',
    moduleId: M,
    order: 3,
    title: 'find, findIndex and Their Last Variants',
    description: 'Locating one element, stopping as soon as it is found, and the distinction from filter.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 15,
    xp: 35,
    topicIds: ['array-methods'],
    prerequisites: ['l-m13-02'],
    learningObjectives: [
      'Locate a single element with `find` and its position with `findIndex`',
      'Explain what each returns when nothing matches',
      'Search from the end with `findLast` and `findLastIndex`',
      'Choose between `find`, `filter` and `includes`',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          '`filter` answers "which ones?". `find` answers "**which one?**" — it returns the first matching element and stops looking.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'One element, not an array.',
        code: [
          'const users = [',
          '  { id: 1, name: "Ada", role: "admin" },',
          '  { id: 2, name: "Grace", role: "user" },',
          '  { id: 3, name: "Alan", role: "admin" },',
          '];',
          '',
          'const admin = users.find((u) => u.role === "admin");',
          'console.log("find     :", admin.name);',
          '',
          'const index = users.findIndex((u) => u.role === "admin");',
          'console.log("findIndex:", index);',
          '',
          '// Not found',
          'console.log("missing find     :", users.find((u) => u.role === "ghost"));',
          'console.log("missing findIndex:", users.findIndex((u) => u.role === "ghost"));',
        ].join('\n'),
        output: [
          'find     : Ada',
          'findIndex: 0',
          'missing find     : undefined',
          'missing findIndex: -1',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'The two "not found" values differ',
        body: [
          '`find` returns **`undefined`** when nothing matches. `findIndex` returns **`-1`**, consistent with `indexOf`.',
          'That means `find` results must be guarded before use — `const u = users.find(...); u.name` throws `Cannot read properties of undefined` when there is no match. This is one of the most common runtime errors in front-end code.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Guarding a find result.',
        code: [
          'const users = [{ id: 1, name: "Ada" }];',
          '',
          'const missing = users.find((u) => u.id === 99);',
          '',
          '// Unguarded — throws',
          'try {',
          '  console.log(missing.name);',
          '} catch (error) {',
          '  console.log(error.name + ":", error.message);',
          '}',
          '',
          '// Optional chaining',
          'console.log("optional:", missing?.name);',
          '',
          '// Explicit guard with a fallback',
          'console.log("fallback:", missing?.name ?? "not found");',
        ].join('\n'),
        output: [
          "TypeError: Cannot read properties of undefined (reading 'name')",
          'optional: undefined',
          'fallback: not found',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'find stops early',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The callback is not called for elements after the match.',
        code: [
          'const numbers = [1, 2, 3, 4, 5];',
          '',
          'let findCalls = 0;',
          'numbers.find((n) => {',
          '  findCalls++;',
          '  return n === 2;',
          '});',
          '',
          'let filterCalls = 0;',
          'numbers.filter((n) => {',
          '  filterCalls++;',
          '  return n === 2;',
          '});',
          '',
          'console.log("find visited  :", findCalls);',
          'console.log("filter visited:", filterCalls);',
        ].join('\n'),
        output: ['find visited  : 2', 'filter visited: 5'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`find` examined two elements and stopped. `filter` examined all five, because it cannot know whether a later element also matches. On a long array where the match is early, that difference is substantial.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'find versus filter',
      },
      {
        kind: SECTION.TABLE,
        headers: ['', '`find`', '`filter`'],
        rows: [
          ['Returns', 'The first matching **element**', 'An **array** of all matches'],
          ['Not found', '`undefined`', '`[]`'],
          ['Stops early', 'Yes, at the first match', 'No, always visits everything'],
          ['Use when', 'You expect one result', 'You want every result'],
        ],
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Wrong — filter for one item',
          code: [
            'const user = users',
            '  .filter((u) => u.id === id)[0];',
            '',
            '// Scans the whole array,',
            '// builds a throwaway array,',
            '// then takes index 0.',
          ].join('\n'),
        },
        right: {
          title: 'Right — find',
          code: [
            'const user = users',
            '  .find((u) => u.id === id);',
            '',
            '// Stops at the match.',
            '// States the intent:',
            '// exactly one is expected.',
          ].join('\n'),
        },
        note: 'Both give `undefined` when nothing matches — `[][0]` is `undefined` too — so they are equivalent in result. `find` is clearer and does less work.',
      },
      {
        kind: SECTION.HEADING,
        text: 'Searching from the end',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: '`findLast` and `findLastIndex` scan backwards.',
        code: [
          'const events = [',
          '  { type: "click", at: 1 },',
          '  { type: "scroll", at: 2 },',
          '  { type: "click", at: 3 },',
          '];',
          '',
          'console.log("first click:", events.find((e) => e.type === "click").at);',
          'console.log("last click :", events.findLast((e) => e.type === "click").at);',
          '',
          'console.log("first index:", events.findIndex((e) => e.type === "click"));',
          'console.log("last index :", events.findLastIndex((e) => e.type === "click"));',
          '',
          'console.log("missing:", events.findLastIndex((e) => e.type === "hover"));',
        ].join('\n'),
        output: [
          'first click: 1',
          'last click : 3',
          'first index: 0',
          'last index : 2',
          'missing: -1',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'These were added in ES2023 and are genuinely useful for chronological data, where the most recent matching entry is usually the one you want. Before them you would reverse a copy and search, which is both slower and easy to get wrong with indexes.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'find versus includes versus indexOf',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Value equality versus a predicate.',
        code: [
          'const nums = [1, 2, 3];',
          'const objs = [{ id: 1 }, { id: 2 }];',
          '',
          '// For primitives, includes is simplest',
          'console.log("includes:", nums.includes(2));',
          'console.log("indexOf :", nums.indexOf(2));',
          '',
          '// For objects, includes compares identity and fails',
          'console.log("includes obj:", objs.includes({ id: 1 }));',
          '',
          '// find with a predicate compares contents',
          'console.log("find obj    :", objs.find((o) => o.id === 1));',
          'console.log("findIndex   :", objs.findIndex((o) => o.id === 1));',
        ].join('\n'),
        output: [
          'includes: true',
          'indexOf : 1',
          'includes obj: false',
          'find obj    : {id: 1}',
          'findIndex   : 0',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Choosing among the four',
        body: [
          'Use `includes` when you have a **value** and want a yes-or-no answer about primitives. Use `indexOf` when you need that value\'s position. Use `find` when you need an **element matching a condition**, and `findIndex` for its position.',
          'The rule of thumb: value equality is `includes`/`indexOf`; a condition is `find`/`findIndex`.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const items = [{ n: 1 }, { n: 2 }, { n: 3 }];',
          '',
          'const a = items.find((i) => i.n > 1);',
          'const b = items.filter((i) => i.n > 1);',
          '',
          'console.log(a.n, Array.isArray(a), b.length);',
        ].join('\n'),
        options: [
          '2 false 2',
          '2 true 2',
          '2 false 3',
          '3 false 2',
        ],
        correct: 0,
        explanation:
          '`find` returns the **first** matching element itself — the object `{ n: 2 }` — so `a.n` is 2 and `a` is not an array. `filter` returns an array of *all* matches, which is `{ n: 2 }` and `{ n: 3 }`, so its length is 2. The key distinction is the return shape: one element versus an array, which is why `find` results need a null guard while `filter` results can be chained safely.',
      },
    ],
    exercises: [
      {
        id: 'ex-m13-03-a',
        title: 'Predict the missing find',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['array-methods'],
        instructions: 'What do these two print?',
        code: [
          'const a = [1, 2, 3];',
          'console.log(a.find((n) => n > 10), a.findIndex((n) => n > 10));',
        ].join('\n'),
        options: [
          'undefined -1',
          'null -1',
          '-1 -1',
          'undefined undefined',
        ],
        correct: 0,
        hints: [
          'One returns an element, the other a position.',
          'What is the conventional "no position" value?',
        ],
        solution: 'undefined -1',
        solutionExplanation:
          '`find` returns an element, so its "not found" value is `undefined`. `findIndex` returns a position, so its "not found" value is `-1`, consistent with `indexOf`. Knowing which is which matters: a `find` result must be guarded before you read a property from it, or you get `Cannot read properties of undefined`.',
      },
      {
        id: 'ex-m13-03-b',
        title: 'Look up a user safely',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['array-methods'],
        instructions:
          'Write `nameById(users, id)` returning the `name` of the user with the matching `id`, or the string `"Unknown"` if there is no match. Users are `{ id, name }` objects.',
        starterCode: [
          'function nameById(users, id) {',
          '  // find returns undefined when nothing matches.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'finds a user', body: 'expect(nameById([{ id: 1, name: "Ada" }], 1)).toBe("Ada");' },
          { name: 'finds a later user', body: 'expect(nameById([{ id: 1, name: "Ada" }, { id: 2, name: "Grace" }], 2)).toBe("Grace");' },
          { name: 'returns Unknown when absent', body: 'expect(nameById([{ id: 1, name: "Ada" }], 99)).toBe("Unknown");' },
          { name: 'handles an empty array', body: 'expect(nameById([], 1)).toBe("Unknown");' },
          { name: 'uses strict equality on the id', body: 'expect(nameById([{ id: 1, name: "Ada" }], "1")).toBe("Unknown");' },
          { name: 'handles a user with an empty name', body: 'expect(nameById([{ id: 1, name: "" }], 1)).toBe("");', hidden: true },
        ],
        hints: [
          '`users.find((u) => u.id === id)` gives the user or `undefined`.',
          'Use optional chaining to read `name` safely.',
          'Use `??` rather than `||` so an empty name survives.',
        ],
        solution: [
          'function nameById(users, id) {',
          '  const user = users.find((u) => u.id === id);',
          '  return user?.name ?? "Unknown";',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Optional chaining short-circuits to `undefined` when `find` returned nothing, and `??` then supplies the fallback — the pairing from Module 04. Using `??` rather than `||` is what makes the hidden test pass: a user whose name is genuinely `""` keeps that empty string, whereas `||` would have replaced it with `"Unknown"`. Strict equality on the id means the string `"1"` does not match the number `1`, which the fifth test checks.',
      },
      {
        id: 'ex-m13-03-c',
        title: 'Find the most recent entry',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['array-methods'],
        instructions:
          'Write `lastErrorIndex(logs)` returning the index of the **last** entry whose `level` is `"error"`, or `-1` if there is none. Logs are `{ level, message }` objects.',
        starterCode: [
          'function lastErrorIndex(logs) {',
          '  // There is a method that searches from the end.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'finds the last error', body: 'expect(lastErrorIndex([{ level: "error" }, { level: "info" }, { level: "error" }])).toBe(2);' },
          { name: 'finds a single error', body: 'expect(lastErrorIndex([{ level: "info" }, { level: "error" }])).toBe(1);' },
          { name: 'finds an error at index 0', body: 'expect(lastErrorIndex([{ level: "error" }, { level: "info" }])).toBe(0);' },
          { name: 'returns -1 when there is no error', body: 'expect(lastErrorIndex([{ level: "info" }])).toBe(-1);' },
          { name: 'returns -1 for an empty array', body: 'expect(lastErrorIndex([])).toBe(-1);' },
          { name: 'does not modify the input', body: 'const src = [{ level: "error" }]; lastErrorIndex(src); expect(src.length).toBe(1);', hidden: true },
        ],
        hints: [
          '`findLastIndex` scans from the end and returns the first match it meets.',
          'It returns `-1` when nothing matches, just like `findIndex`.',
          'It does not modify the array, unlike a reverse-then-search approach.',
        ],
        solution: [
          'function lastErrorIndex(logs) {',
          '  return logs.findLastIndex((log) => log.level === "error");',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`findLastIndex` searches backwards and returns the index in the **original** array, which is what makes it so much simpler than the alternative. Doing this manually would mean reversing a copy, finding the index, then converting it back with `length - 1 - index` — arithmetic that is easy to get wrong by one. The hidden test guards against an implementation using `reverse()`, which mutates the caller\'s array. Chronological logs are the classic use: the most recent matching entry is usually the interesting one.',
      },
      {
        id: 'ex-m13-03-d',
        title: 'Replace filter[0] with find',
        kind: EXERCISE_KIND.REFACTOR,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['array-methods'],
        instructions:
          'Rewrite this to use `find` instead of `filter` followed by an index. Behaviour must be identical, including when nothing matches.',
        starterCode: [
          'function firstInStock(products) {',
          '  return products.filter((p) => p.inStock)[0];',
          '}',
        ].join('\n'),
        tests: [
          { name: 'returns the first in stock', body: 'expect(firstInStock([{ id: 1, inStock: false }, { id: 2, inStock: true }]).id).toBe(2);' },
          { name: 'returns the first when several match', body: 'expect(firstInStock([{ id: 1, inStock: true }, { id: 2, inStock: true }]).id).toBe(1);' },
          { name: 'returns undefined when none match', body: 'expect(firstInStock([{ id: 1, inStock: false }])).toBe(undefined);' },
          { name: 'returns undefined for an empty array', body: 'expect(firstInStock([])).toBe(undefined);' },
          { name: 'uses find rather than filter', body: 'expect(/\\.find\\(/.test(firstInStock.toString())).toBe(true); expect(/\\.filter\\(/.test(firstInStock.toString())).toBe(false);', hidden: true },
        ],
        hints: [
          '`find` returns the first match directly.',
          'It already returns `undefined` when nothing matches, matching `[][0]`.',
        ],
        solution: [
          'function firstInStock(products) {',
          '  return products.find((p) => p.inStock);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The behaviour is identical because `[][0]` is `undefined`, which is exactly what `find` returns for no match — so the not-found tests pass unchanged. The gains are that `find` stops at the first match instead of scanning the whole array, it avoids building a throwaway intermediate array, and it states the intent that exactly one result is expected. The `filter(...)[0]` pattern is common in older code and is always worth replacing.',
      },
    ],
    quiz: {
      id: 'qz-m13-03',
      questions: [
        {
          id: 'q-m13-03-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['array-methods'],
          prompt: 'What is the difference between `find` and `filter`?',
          options: [
            '`find` is a newer name for `filter`',
            '`find` returns the first matching element and stops; `filter` returns an array of every match',
            '`find` returns an array with one element',
            '`filter` stops at the first match',
          ],
          correct: 1,
          optionExplanations: [
            'They serve different purposes.',
            'Correct — one element versus an array, and early exit versus a full scan.',
            '`find` returns the element itself, not wrapped in an array.',
            '`filter` must visit everything to collect all matches.',
          ],
          explanation:
            '`find` answers "which one?" and returns the element itself, stopping at the first match. `filter` answers "which ones?" and returns an array, visiting every element. Their not-found values differ too: `undefined` versus `[]`.',
        },
        {
          id: 'q-m13-03-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['array-methods'],
          prompt: 'Why does this throw?',
          code: [
            'const users = [{ name: "Ada" }];',
            'console.log(users.find((u) => u.name === "Bob").name);',
          ].join('\n'),
          options: [
            'It prints undefined',
            'It prints "Bob"',
            'It throws a TypeError',
            'It prints null',
          ],
          correct: 2,
          optionExplanations: [
            'Reading a property of `undefined` fails before anything can print.',
            'No such user exists.',
            'Correct — `find` returned `undefined`, and `undefined.name` throws.',
            '`find` never returns `null`.',
          ],
          explanation:
            '`find` returns `undefined` when nothing matches, and reading `.name` from `undefined` throws `Cannot read properties of undefined`. Always guard a `find` result — `result?.name` or an explicit check — before reading from it.',
        },
        {
          id: 'q-m13-03-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['array-methods'],
          prompt: 'When should you use `find` rather than `includes`?',
          options: [
            'Never — they are interchangeable',
            'When you need to match a condition rather than a specific value, such as finding an object by one of its properties',
            'When the array contains only numbers',
            'When you want a boolean result',
          ],
          correct: 1,
          optionExplanations: [
            'They answer different questions.',
            'Correct — `includes` compares values, `find` evaluates a predicate.',
            '`includes` handles numbers perfectly well.',
            '`includes` is the one returning a boolean.',
          ],
          explanation:
            '`includes` tests value equality, which fails for objects because two objects with identical contents are not `===`. `find` takes a predicate, so it can match on any condition — `users.find((u) => u.id === 5)`.',
        },
        {
          id: 'q-m13-03-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['array-methods'],
          prompt: '`findLastIndex` returns the index counted from the end of the array.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'It searches from the end but reports a normal index.',
            'Correct — the index is relative to the start, as usual.',
          ],
          explanation:
            '`findLastIndex` scans backwards but returns an ordinary zero-based index measured from the start. That is precisely what makes it more convenient than reversing a copy and converting the result back.',
        },
      ],
    },
    summary:
      '`find` returns the **first matching element** and stops looking; `findIndex` returns its position. Their not-found values differ deliberately — `undefined` for an element, `-1` for a position — and the `undefined` from `find` must be guarded before reading a property, or you get the most common runtime TypeError in front-end code. Because `find` exits early it does less work than `filter`, which must visit every element to collect all matches. The `filter(...)[0]` pattern is always better written as `find`. `findLast` and `findLastIndex` scan backwards while still reporting an ordinary index, which suits chronological data. Use `includes`/`indexOf` for value equality on primitives, and `find`/`findIndex` when matching a condition — the only option that works for objects.',
    keyTakeaways: [
      '`find` returns an element; `findIndex` returns a position',
      'Not found: `undefined` from `find`, `-1` from `findIndex`',
      'Always guard a `find` result before reading a property',
      '`find` stops early; `filter` always visits everything',
      'Replace `filter(...)[0]` with `find`',
      'Value equality is `includes`; a condition is `find`',
    ],
    relatedLessons: ['l-m13-02', 'l-m13-04'],
    interviewConnections: [
      'What is the difference between find and filter?',
      'What does find return when nothing matches?',
      'Why does includes fail to find an object?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m13-04',
    slug: 'some-and-every',
    moduleId: M,
    order: 4,
    title: 'some and every',
    description: 'Asking whether any or all elements satisfy a condition, and the empty-array results that surprise people.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 13,
    xp: 30,
    topicIds: ['array-methods'],
    prerequisites: ['l-m13-03'],
    learningObjectives: [
      'Test for any match with `some` and all matches with `every`',
      'Explain the early-exit behaviour of each',
      'Explain why `every` on an empty array is `true`',
      'Choose between `some`, `every`, `find` and `filter`',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Both return a **boolean**, and both stop as soon as the answer is certain.',
          '`some` asks "is there **at least one** that matches?" and stops at the first that does. `every` asks "do **all** of them match?" and stops at the first that does not.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Booleans, not arrays.',
        code: [
          'const numbers = [2, 4, 6, 7];',
          '',
          'console.log("any odd :", numbers.some((n) => n % 2 !== 0));',
          'console.log("all even:", numbers.every((n) => n % 2 === 0));',
          'console.log("any > 5 :", numbers.some((n) => n > 5));',
          'console.log("all > 0 :", numbers.every((n) => n > 0));',
        ].join('\n'),
        output: [
          'any odd : true',
          'all even: false',
          'any > 5 : true',
          'all > 0 : true',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Both exit early',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Counting how many elements each one visits.',
        code: [
          'const nums = [1, 2, 3, 4, 5];',
          '',
          'let someCalls = 0;',
          'nums.some((n) => { someCalls++; return n === 2; });',
          '',
          'let everyCalls = 0;',
          'nums.every((n) => { everyCalls++; return n < 3; });',
          '',
          'let allCalls = 0;',
          'nums.every((n) => { allCalls++; return n > 0; });',
          '',
          'console.log("some, match at 2  :", someCalls);',
          'console.log("every, fails at 3 :", everyCalls);',
          'console.log("every, all pass   :", allCalls);',
        ].join('\n'),
        output: [
          'some, match at 2  : 2',
          'every, fails at 3 : 3',
          'every, all pass   : 5',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`some` stopped after finding its match at position 2. `every` stopped at the first failure — the element `3`, which is not less than 3. When every element passes, `every` necessarily visits them all, because it cannot conclude "all" without checking.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The empty-array results',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'This surprises almost everyone the first time.',
        code: [
          'console.log("some on empty :", [].some((n) => n > 0));',
          'console.log("every on empty:", [].every((n) => n > 0));',
          '',
          '// Even with a predicate that can never be true',
          'console.log("every, impossible:", [].every(() => false));',
        ].join('\n'),
        output: [
          'some on empty : false',
          'every on empty: true',
          'every, impossible: true',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Why `every` on an empty array is `true`',
        body: [
          '`every` returns `true` unless it finds a counter-example. An empty array contains no elements, so there is nothing that could fail — the claim "every element matches" is **vacuously true**.',
          'This is standard across mathematics and other languages, not a JavaScript quirk. But it does mean a validation like `fields.every(isValid)` passes when there are no fields at all, which may not be what you intended. Check `length > 0` separately if emptiness should fail.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Guarding against the vacuous case.',
        code: [
          'function allValid(fields) {',
          '  return fields.every((f) => f.valid);',
          '}',
          '',
          'function allValidAndPresent(fields) {',
          '  return fields.length > 0 && fields.every((f) => f.valid);',
          '}',
          '',
          'console.log("empty, lenient:", allValid([]));',
          'console.log("empty, strict :", allValidAndPresent([]));',
          'console.log("valid, strict :", allValidAndPresent([{ valid: true }]));',
        ].join('\n'),
        output: [
          'empty, lenient: true',
          'empty, strict : false',
          'valid, strict : true',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Practical uses',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Validation, permission checks and membership tests.',
        code: [
          'const form = [',
          '  { name: "email", value: "a@b.com" },',
          '  { name: "age", value: "" },',
          '];',
          '',
          'console.log("any empty:", form.some((f) => f.value === ""));',
          'console.log("all filled:", form.every((f) => f.value !== ""));',
          '',
          '// Checking membership by a condition — where includes cannot help',
          'const users = [{ role: "user" }, { role: "admin" }];',
          'console.log("has admin:", users.some((u) => u.role === "admin"));',
          '',
          '// Nested checks',
          'const teams = [',
          '  { members: ["Ada"] },',
          '  { members: [] },',
          '];',
          'console.log("all staffed:", teams.every((t) => t.members.length > 0));',
        ].join('\n'),
        output: [
          'any empty: true',
          'all filled: false',
          'has admin: true',
          'all staffed: false',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: '`some` is the object version of `includes`',
        body: [
          '`includes` compares values, so it cannot find an object by its contents. `some` with a predicate can: `users.some((u) => u.id === 5)`.',
          'It is also more efficient than `filter(...).length > 0`, which builds an entire array just to check whether it is empty.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Choosing among the four',
      },
      {
        kind: SECTION.TABLE,
        headers: ['Question', 'Method', 'Returns'],
        rows: [
          ['Is there at least one match?', '`some`', 'boolean'],
          ['Do all of them match?', '`every`', 'boolean'],
          ['Which one matches?', '`find`', 'element or `undefined`'],
          ['Which ones match?', '`filter`', 'array'],
          ['Where is the match?', '`findIndex`', 'index or `-1`'],
        ],
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The pattern is consistent: pick the method whose **return type** answers your question. If you only need a yes or no, `some` and `every` say so directly and stop as early as possible.',
          'Note also that `!some(predicate)` is equivalent to `every(negatedPredicate)` — "none match" can be expressed either way, and the `some` form usually reads better.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const empty = [];',
          '',
          'console.log(empty.some(() => true), empty.every(() => false));',
        ].join('\n'),
        options: [
          'false true',
          'true false',
          'false false',
          'true true',
        ],
        correct: 0,
        explanation:
          '`some` needs to find at least one matching element to return `true`. An empty array has none, so it returns `false` — even though the predicate would always say yes if it were ever called, which it is not. `every` returns `true` unless it finds a counter-example, and an empty array cannot provide one, so it is vacuously `true` even with a predicate that always returns `false`. Neither callback is invoked at all.',
      },
    ],
    exercises: [
      {
        id: 'ex-m13-04-a',
        title: 'Predict the empty-array results',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['array-methods'],
        instructions: 'What does this print?',
        code: ['console.log([].every((n) => n > 100));'].join('\n'),
        options: ['true', 'false', 'undefined', 'A TypeError'],
        correct: 0,
        hints: [
          '`every` returns `true` unless it finds something that fails.',
          'Can an empty array provide a counter-example?',
        ],
        solution: 'true',
        solutionExplanation:
          '`every` looks for a counter-example and returns `false` only if it finds one. An empty array has no elements, so no element can fail and the result is vacuously `true` — the callback is never even called. This is consistent with mathematics and other languages, but it means a validation like `fields.every(isValid)` passes with zero fields. Add `fields.length > 0 &&` if emptiness should fail.',
      },
      {
        id: 'ex-m13-04-b',
        title: 'Validate a form',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['array-methods'],
        instructions:
          'Write `isFormComplete(fields)` returning `true` only when there is at least one field and every field has a non-empty `value` after trimming. Fields are `{ name, value }` objects.',
        starterCode: [
          'function isFormComplete(fields) {',
          '  // Remember the vacuous-truth case.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'accepts a complete form', body: 'expect(isFormComplete([{ value: "a" }, { value: "b" }])).toBe(true);' },
          { name: 'rejects an empty value', body: 'expect(isFormComplete([{ value: "a" }, { value: "" }])).toBe(false);' },
          { name: 'rejects a whitespace-only value', body: 'expect(isFormComplete([{ value: "  " }])).toBe(false);' },
          { name: 'rejects an empty form', body: 'expect(isFormComplete([])).toBe(false);' },
          { name: 'accepts a single filled field', body: 'expect(isFormComplete([{ value: "x" }])).toBe(true);' },
          { name: 'returns a real boolean', body: 'expect(typeof isFormComplete([])).toBe("boolean");', hidden: true },
        ],
        hints: [
          '`every` alone returns `true` for an empty array.',
          'Combine a length check with `every` using `&&`.',
          '`value.trim() !== ""` rejects whitespace-only entries.',
        ],
        solution: [
          'function isFormComplete(fields) {',
          '  return fields.length > 0 && fields.every((f) => f.value.trim() !== "");',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The `fields.length > 0 &&` guard is the important part — without it an empty form would report complete, because `every` is vacuously true. Trimming before comparing rejects whitespace-only input, which an `=== ""` check alone would let through. Note `&&` returns a boolean here because both operands are comparisons, satisfying the hidden type test; had the left side been `fields.length` rather than `fields.length > 0`, an empty array would have returned the number `0` rather than `false`.',
      },
      {
        id: 'ex-m13-04-c',
        title: 'Check for a matching object',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['array-methods'],
        instructions:
          'Write `hasRole(users, role)` returning `true` when at least one user has that `role`. Use `some` rather than building an intermediate array.',
        starterCode: [
          'function hasRole(users, role) {',
          '  // some stops at the first match and returns a boolean.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'finds a matching role', body: 'expect(hasRole([{ role: "user" }, { role: "admin" }], "admin")).toBe(true);' },
          { name: 'returns false when absent', body: 'expect(hasRole([{ role: "user" }], "admin")).toBe(false);' },
          { name: 'returns false for an empty array', body: 'expect(hasRole([], "admin")).toBe(false);' },
          { name: 'matches the first user', body: 'expect(hasRole([{ role: "admin" }], "admin")).toBe(true);' },
          { name: 'uses strict equality', body: 'expect(hasRole([{ role: "1" }], 1)).toBe(false);' },
          { name: 'returns a real boolean', body: 'expect(typeof hasRole([], "x")).toBe("boolean");', hidden: true },
        ],
        hints: [
          'The predicate compares `user.role` with `role`.',
          '`some` already returns a boolean, so no conversion is needed.',
        ],
        solution: [
          'function hasRole(users, role) {',
          '  return users.some((user) => user.role === role);',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`some` returns a genuine boolean and stops at the first match, so nothing is scanned unnecessarily and no intermediate array is built. The alternative `users.filter((u) => u.role === role).length > 0` produces the same answer but visits every element and allocates an array purely to check whether it is empty. `includes` cannot help here, because it compares whole values and these are objects. Strict equality in the predicate is what makes the string-versus-number test pass.',
      },
      {
        id: 'ex-m13-04-d',
        title: 'Choose the right method',
        kind: EXERCISE_KIND.CHOOSE_IMPLEMENTATION,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['array-methods'],
        instructions:
          'You need a boolean answering "is any product out of stock?". Which is the best choice?',
        options: [
          'products.filter((p) => !p.inStock).length > 0',
          'products.some((p) => !p.inStock)',
          'products.find((p) => !p.inStock)',
          'products.every((p) => !p.inStock)',
        ],
        correct: 1,
        hints: [
          'You want a boolean, and you want to stop at the first match.',
        ],
        solution: 'products.some((p) => !p.inStock)',
        solutionExplanation:
          '`some` returns exactly the boolean asked for and stops at the first out-of-stock product. The `filter` version gives the right answer but scans everything and allocates a throwaway array. `find` returns the product or `undefined` rather than a boolean — usable via truthiness, but it says the wrong thing and would misbehave if a matching element were itself falsy. `every` asks whether *all* products are out of stock, which is a different question.',
      },
    ],
    quiz: {
      id: 'qz-m13-04',
      questions: [
        {
          id: 'q-m13-04-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['array-methods'],
          prompt: 'What do `some` and `every` return?',
          options: [
            'The matching elements',
            'A boolean',
            'The index of the first match',
            'A new array',
          ],
          correct: 1,
          optionExplanations: [
            'That is `filter`.',
            'Correct — they answer a yes-or-no question.',
            'That is `findIndex`.',
            'Neither builds an array.',
          ],
          explanation:
            '`some` returns `true` if at least one element satisfies the predicate; `every` returns `true` only if all do. Both stop as soon as the answer is certain.',
        },
        {
          id: 'q-m13-04-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['array-methods'],
          prompt: 'What do these two give on an empty array?',
          code: [
            'console.log([].some(() => true), [].every(() => false));',
          ].join('\n'),
          options: ['false true', 'true false', 'false false', 'true true'],
          correct: 0,
          optionExplanations: [
            'Correct — `some` needs a match it cannot find; `every` is vacuously true.',
            'This has both reversed.',
            '`every` returns true with no counter-example.',
            '`some` cannot find a match in an empty array.',
          ],
          explanation:
            '`some` requires at least one matching element, and an empty array has none, so it is `false`. `every` returns `false` only on finding a counter-example, and an empty array cannot supply one, so it is vacuously `true`. Neither callback runs.',
        },
        {
          id: 'q-m13-04-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['array-methods'],
          prompt: 'Why prefer `some` over `filter(...).length > 0`?',
          options: [
            'They behave differently',
            '`some` stops at the first match and returns a boolean directly, without allocating an intermediate array',
            '`filter` cannot be used with objects',
            '`some` works on more types',
          ],
          correct: 1,
          optionExplanations: [
            'Both give the same answer.',
            'Correct — less work and a clearer statement of intent.',
            '`filter` works with any element type.',
            'Both are array methods with the same reach.',
          ],
          explanation:
            'Both produce the same result, but `filter` must visit every element and build an array purely to check whether it is empty. `some` exits at the first match and returns the boolean you actually wanted.',
        },
        {
          id: 'q-m13-04-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['array-methods'],
          prompt: '`every` visits every element even when an early one fails the predicate.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'It stops as soon as the answer is determined.',
            'Correct — the first failure ends the check.',
          ],
          explanation:
            '`every` stops at the first element that fails, since one counter-example settles the question. It only visits everything when all elements pass, because it cannot conclude "all" without checking each one.',
        },
      ],
    },
    summary:
      '`some` and `every` return a **boolean** and both exit as soon as the answer is certain — `some` at the first match, `every` at the first failure. On an empty array `some` is `false` because it needs a match it cannot find, while `every` is vacuously `true` because there is no counter-example; that is standard logic rather than a quirk, but it means a validation using `every` passes with zero items unless you add a `length > 0` check. `some` is the object-capable counterpart to `includes`, since a predicate can match on contents where value equality cannot, and it is preferable to `filter(...).length > 0`, which allocates an array just to check emptiness. Choose the method whose return type answers your question: boolean for `some`/`every`, element for `find`, array for `filter`.',
    keyTakeaways: [
      '`some` = at least one; `every` = all; both return booleans',
      'Both exit early — `some` at the first match, `every` at the first failure',
      '`[].every(...)` is `true`; `[].some(...)` is `false`',
      'Add `length > 0 &&` when an empty collection should fail validation',
      'Prefer `some` over `filter(...).length > 0`',
      '`some` with a predicate is the object version of `includes`',
    ],
    relatedLessons: ['l-m13-03', 'l-m13-05'],
    interviewConnections: [
      'What is the difference between some and every?',
      'Why does every return true for an empty array?',
      'How do you check whether an array contains a matching object?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m13-05',
    slug: 'reduce',
    moduleId: M,
    order: 5,
    title: 'reduce',
    description: 'The most powerful and most misunderstood array method — accumulators, initial values, and far more than summing.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 22,
    xp: 45,
    topicIds: ['array-methods', 'higher-order'],
    prerequisites: ['l-m13-04'],
    learningObjectives: [
      'Explain the accumulator and how it flows between iterations',
      'Explain why the initial value matters and what happens without one',
      'Use `reduce` to build counts, groups and objects — not only sums',
      'Recognise when `reduce` harms readability',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          '`reduce` collapses an array into a **single value**. That value can be a number, a string, an object, or even another array — which is why it can express almost every other array method.',
          'It has a reputation for being hard. It is not, once you see the accumulator clearly.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The shape',
      },
      {
        kind: SECTION.ANNOTATED_CODE,
        language: 'javascript',
        code: [
          'const total = numbers.reduce((accumulator, current) => {',
          '  return accumulator + current;',
          '}, 0);',
        ].join('\n'),
        annotations: [
          { line: 1, text: '`accumulator` is the value carried between iterations. On the first call it is the initial value; afterwards it is whatever the previous callback returned.' },
          { line: 1, text: '`current` is the element being visited on this iteration. The callback also receives `index` and the whole `array` as third and fourth arguments.' },
          { line: 2, text: '**Whatever you return becomes the accumulator for the next iteration.** Forgetting to return is the single most common `reduce` bug.' },
          { line: 3, text: 'The second argument to `reduce` is the **initial value** — the accumulator for the very first call. Almost always supply it.' },
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Watching the accumulator flow',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Every step made visible.',
        code: [
          'const numbers = [10, 20, 30];',
          '',
          'const total = numbers.reduce((acc, current, index) => {',
          '  const next = acc + current;',
          '  console.log(`step ${index}: acc=${acc} + current=${current} → ${next}`);',
          '  return next;',
          '}, 0);',
          '',
          'console.log("final:", total);',
        ].join('\n'),
        output: [
          'step 0: acc=0 + current=10 → 10',
          'step 1: acc=10 + current=20 → 30',
          'step 2: acc=30 + current=30 → 60',
          'final: 60',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Read the trace carefully. The accumulator starts at the initial value `0`. Each step\'s returned value becomes the next step\'s `acc`. After the last element, whatever the callback returned is the result of the whole `reduce`.',
          'That is the entire mechanism. Everything else is a matter of what you choose the accumulator to be.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The initial value matters',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Without an initial value, the first element becomes the accumulator.',
        code: [
          'const nums = [1, 2, 3];',
          '',
          '// With an initial value: 4 calls? No — 3 calls, starting from 0',
          'let withCalls = 0;',
          'nums.reduce((acc, n) => { withCalls++; return acc + n; }, 0);',
          '',
          '// Without: the first element is the accumulator, so only 2 calls',
          'let withoutCalls = 0;',
          'nums.reduce((acc, n) => { withoutCalls++; return acc + n; });',
          '',
          'console.log("calls with initial   :", withCalls);',
          'console.log("calls without initial:", withoutCalls);',
          '',
          '// Both give the same sum here',
          'console.log(nums.reduce((a, n) => a + n, 0), nums.reduce((a, n) => a + n));',
        ].join('\n'),
        output: [
          'calls with initial   : 3',
          'calls without initial: 2',
          '6 6',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Two reasons to always supply an initial value',
        body: [
          '**An empty array throws.** `[].reduce((a, b) => a + b)` throws `TypeError: Reduce of empty array with no initial value`, because there is no first element to use as the accumulator. With an initial value it simply returns that value.',
          '**The accumulator type becomes unpredictable.** Without an initial value the accumulator starts as the first *element*, so if you are building an object or a string from an array of numbers, the first iteration receives a number instead. Supplying `{}`, `[]`, `0` or `""` states your intent and fixes the type.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The empty-array difference.',
        code: [
          'try {',
          '  [].reduce((a, b) => a + b);',
          '} catch (error) {',
          '  console.log(error.name + ":", error.message);',
          '}',
          '',
          'console.log("with initial:", [].reduce((a, b) => a + b, 0));',
        ].join('\n'),
        output: [
          'TypeError: Reduce of empty array with no initial value',
          'with initial: 0',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Beyond summing: counting',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'An object accumulator counting occurrences.',
        code: [
          'const votes = ["red", "blue", "red", "green", "red"];',
          '',
          'const counts = votes.reduce((acc, colour) => {',
          '  acc[colour] = (acc[colour] ?? 0) + 1;',
          '  return acc;',
          '}, {});',
          '',
          'console.log(counts);',
        ].join('\n'),
        output: ['{red: 3, blue: 1, green: 1}'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The accumulator is an object rather than a number. `acc[colour] ?? 0` handles the first time a colour is seen — the property does not exist yet, so it is `undefined`, and `??` supplies `0`. Then the `return acc` passes the same object along to the next iteration.',
          'Note that this mutates the accumulator object each time. That is idiomatic and efficient in `reduce`, because the object was created by the `reduce` itself and nothing outside can see it.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Beyond summing: grouping',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Collecting items into buckets.',
        code: [
          'const people = [',
          '  { name: "Ada", dept: "eng" },',
          '  { name: "Grace", dept: "eng" },',
          '  { name: "Alan", dept: "design" },',
          '];',
          '',
          'const byDept = people.reduce((acc, person) => {',
          '  acc[person.dept] ??= [];',
          '  acc[person.dept].push(person.name);',
          '  return acc;',
          '}, {});',
          '',
          'console.log(byDept);',
        ].join('\n'),
        output: ['{eng: ["Ada", "Grace"], design: ["Alan"]}'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`acc[person.dept] ??= []` is the logical nullish assignment operator: it assigns only when the left side is `null` or `undefined`. It creates the bucket the first time a department is seen and leaves it alone afterwards.',
          'Grouping is common enough that JavaScript is gaining a built-in `Object.groupBy`, but the `reduce` version works everywhere today and shows the mechanism clearly.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Beyond summing: several results at once',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'One pass computing multiple statistics.',
        code: [
          'const readings = [4, 9, 2, 7];',
          '',
          'const stats = readings.reduce((acc, n) => ({',
          '  count: acc.count + 1,',
          '  total: acc.total + n,',
          '  min: Math.min(acc.min, n),',
          '  max: Math.max(acc.max, n),',
          '}), { count: 0, total: 0, min: Infinity, max: -Infinity });',
          '',
          'console.log(stats);',
          'console.log("average:", stats.total / stats.count);',
        ].join('\n'),
        output: [
          '{count: 4, total: 22, min: 2, max: 9}',
          'average: 5.5',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The initial `min: Infinity` and `max: -Infinity` are the identity values from Module 06 — any real number beats them, so the first comparison always sets a sensible value. Note the callback returns a **new object** each time here rather than mutating, using the wrapping-parentheses form from Module 09.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'reduceRight',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The same mechanism, from the end backwards.',
        code: [
          'const letters = ["a", "b", "c"];',
          '',
          'console.log("reduce     :", letters.reduce((acc, l) => acc + l, ""));',
          'console.log("reduceRight:", letters.reduceRight((acc, l) => acc + l, ""));',
        ].join('\n'),
        output: ['reduce     : abc', 'reduceRight: cba'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`reduceRight` is rarely needed — for reversing a string or array there are clearer options. It matters when the operation is genuinely order-dependent, such as composing functions right to left.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'When reduce hurts readability',
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'reduce doing filter’s job',
          code: [
            'const evens = nums.reduce((acc, n) => {',
            '  if (n % 2 === 0) acc.push(n);',
            '  return acc;',
            '}, []);',
            '',
            '// Works. Requires reading',
            '// four lines to work out',
            '// what it does.',
          ].join('\n'),
        },
        right: {
          title: 'filter saying it directly',
          code: [
            'const evens = nums.filter(',
            '  (n) => n % 2 === 0);',
            '',
            '',
            '',
            '// The method name is the',
            '// documentation.',
          ].join('\n'),
        },
        note: '`reduce` can express `map`, `filter`, `some` and `every` — but a named method states the intent immediately. Reach for `reduce` when no other method fits: building an object, computing several results in one pass, or collapsing to a single value.',
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'The forgotten return',
        body: [
          'A callback that does not return the accumulator hands `undefined` to the next iteration, and everything after breaks. With a block body you must write `return acc;` explicitly.',
          'The symptom is a result of `undefined`, or a `TypeError` when the next iteration tries to read a property from it. If a `reduce` fails on the second element, check for a missing return.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The forgotten return, made visible.',
        code: [
          'const nums = [1, 2, 3];',
          '',
          'const broken = nums.reduce((acc, n) => {',
          '  acc + n;   // computed and discarded',
          '}, 0);',
          '',
          'console.log("broken:", broken);',
          '',
          'const fixed = nums.reduce((acc, n) => acc + n, 0);',
          'console.log("fixed :", fixed);',
        ].join('\n'),
        output: ['broken: undefined', 'fixed : 6'].join('\n'),
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const nums = [1, 2, 3, 4];',
          '',
          'const result = nums.reduce((acc, n) => {',
          '  if (n % 2 === 0) {',
          '    acc.push(n);',
          '  }',
          '  return acc;',
          '}, []);',
          '',
          'console.log(result, Array.isArray(result));',
        ].join('\n'),
        options: [
          '[2, 4] true',
          '[1, 2, 3, 4] true',
          '10 false',
          '6 false',
        ],
        correct: 0,
        explanation:
          'The initial value is `[]`, so the accumulator is an array throughout. Each iteration pushes only the even numbers and returns the same array, so the final result is `[2, 4]` — and it is genuinely an array. This is `reduce` doing `filter`\'s job: it works correctly, but `nums.filter((n) => n % 2 === 0)` says the same thing in one obvious line. Note the explicit `return acc` is essential, since the block body has no implicit return.',
      },
    ],
    exercises: [
      {
        id: 'ex-m13-05-a',
        title: 'Predict the missing return',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['array-methods'],
        instructions: 'What does this print?',
        code: [
          'const r = [1, 2, 3].reduce((acc, n) => {',
          '  acc + n;',
          '}, 0);',
          'console.log(r);',
        ].join('\n'),
        options: ['6', 'undefined', '0', 'NaN'],
        correct: 1,
        hints: [
          'Does the callback return anything?',
          'What becomes the accumulator for the next iteration?',
        ],
        solution: 'undefined',
        solutionExplanation:
          'The block body computes `acc + n` and discards it, returning `undefined`. That `undefined` becomes the accumulator for the next iteration, and after the final call `reduce` returns whatever the last callback returned — `undefined`. Adding `return` fixes it, or removing the braces so the implicit return applies. If a `reduce` produces `undefined` or throws on the second element, a missing return is nearly always the cause.',
      },
      {
        id: 'ex-m13-05-b',
        title: 'Sum object values',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['array-methods'],
        instructions:
          'Write `cartTotal(items)` returning the total cost of `{ price, quantity }` objects. Use `reduce` with an initial value so an empty cart returns `0`.',
        starterCode: [
          'function cartTotal(items) {',
          '  // Accumulate price * quantity.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'totals one item', body: 'expect(cartTotal([{ price: 10, quantity: 2 }])).toBe(20);' },
          { name: 'totals several items', body: 'expect(cartTotal([{ price: 10, quantity: 2 }, { price: 5, quantity: 3 }])).toBe(35);' },
          { name: 'returns 0 for an empty cart', body: 'expect(cartTotal([])).toBe(0);' },
          { name: 'handles a zero quantity', body: 'expect(cartTotal([{ price: 10, quantity: 0 }])).toBe(0);' },
          { name: 'handles decimals', body: 'expect(cartTotal([{ price: 2.5, quantity: 2 }])).toBe(5);' },
          { name: 'does not modify the input', body: 'const src = [{ price: 1, quantity: 1 }]; cartTotal(src); expect(src[0].price).toBe(1);', hidden: true },
        ],
        hints: [
          'The accumulator is a running number starting at `0`.',
          'Each step adds `item.price * item.quantity`.',
          'An expression body gives the implicit return for free.',
        ],
        solution: [
          'function cartTotal(items) {',
          '  return items.reduce((total, item) => total + item.price * item.quantity, 0);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The initial value of `0` does two jobs: it makes the empty-cart case return `0` instead of throwing, and it guarantees the accumulator is a number from the start. Using an expression body means the implicit return applies, so the classic forgotten-return bug cannot occur. Multiplication binds tighter than addition, so no brackets are needed around `item.price * item.quantity` — the precedence rules from Module 04.',
      },
      {
        id: 'ex-m13-05-c',
        title: 'Count occurrences',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['array-methods'],
        instructions:
          'Write `countBy(items)` returning an object mapping each value to how many times it appears. `countBy(["a","b","a"])` gives `{ a: 2, b: 1 }`. Return an empty object for an empty array.',
        starterCode: [
          'function countBy(items) {',
          '  // The accumulator is an object.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'counts repeats', body: 'expect(countBy(["a", "b", "a"])).toEqual({ a: 2, b: 1 });' },
          { name: 'counts a single item', body: 'expect(countBy(["x"])).toEqual({ x: 1 });' },
          { name: 'returns an empty object for an empty array', body: 'expect(countBy([])).toEqual({});' },
          { name: 'counts numbers', body: 'expect(countBy([1, 1, 2])).toEqual({ 1: 2, 2: 1 });' },
          { name: 'handles all-unique values', body: 'expect(countBy(["a", "b"])).toEqual({ a: 1, b: 1 });' },
          { name: 'counts many repeats', body: 'expect(countBy(["z", "z", "z", "z"])).toEqual({ z: 4 });', hidden: true },
        ],
        hints: [
          'The initial value is `{}`.',
          'A key not yet present gives `undefined` — use `?? 0` before adding 1.',
          'Remember to return the accumulator.',
        ],
        solution: [
          'function countBy(items) {',
          '  return items.reduce((counts, item) => {',
          '    counts[item] = (counts[item] ?? 0) + 1;',
          '    return counts;',
          '  }, {});',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The accumulator is an object rather than a number, which is what makes `reduce` more than a summing tool. `counts[item] ?? 0` handles the first sighting of a value, since reading a missing property gives `undefined`. Mutating the accumulator is idiomatic here because the object was created by this `reduce` and nothing outside can observe it. Note that object keys are strings, so the numeric test produces keys `"1"` and `"2"` — `toEqual` compares them correctly because the expected object literal has its numeric keys converted the same way.',
      },
      {
        id: 'ex-m13-05-d',
        title: 'Group by a property',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 30,
        topicIds: ['array-methods'],
        instructions:
          'Write `groupBy(items, key)` returning an object where each distinct value of `item[key]` maps to an array of the matching items. `groupBy([{t:"a"},{t:"b"},{t:"a"}], "t")` gives `{ a: [{t:"a"},{t:"a"}], b: [{t:"b"}] }`.',
        starterCode: [
          'function groupBy(items, key) {',
          '  // Create the bucket the first time each value is seen.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'groups by a property', body: 'expect(groupBy([{ t: "a" }, { t: "b" }, { t: "a" }], "t")).toEqual({ a: [{ t: "a" }, { t: "a" }], b: [{ t: "b" }] });' },
          { name: 'handles a single group', body: 'expect(groupBy([{ t: "x" }], "t")).toEqual({ x: [{ t: "x" }] });' },
          { name: 'returns an empty object for an empty array', body: 'expect(groupBy([], "t")).toEqual({});' },
          { name: 'preserves order within a group', body: 'const r = groupBy([{ t: "a", n: 1 }, { t: "a", n: 2 }], "t"); expect(r.a.map((i) => i.n)).toEqual([1, 2]);' },
          { name: 'groups by a different key', body: 'expect(groupBy([{ dept: "eng" }], "dept")).toEqual({ eng: [{ dept: "eng" }] });' },
          { name: 'keeps the original objects', body: 'const o = { t: "a" }; expect(groupBy([o], "t").a[0]).toBe(o);', hidden: true },
        ],
        hints: [
          'The accumulator starts as `{}`.',
          'Read the group name with bracket notation: `item[key]`.',
          '`acc[name] ??= []` creates the bucket only when it does not exist.',
        ],
        solution: [
          'function groupBy(items, key) {',
          '  return items.reduce((groups, item) => {',
          '    const name = item[key];',
          '    groups[name] ??= [];',
          '    groups[name].push(item);',
          '    return groups;',
          '  }, {});',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Bracket notation is essential because the property name comes from a variable — `item.key` would look for a literal property called `"key"`. The `??=` operator creates each bucket exactly once, on first sighting, and leaves existing buckets alone. Pushing preserves input order within each group, which the fourth test checks. The hidden test confirms the grouped arrays hold the **original** objects rather than copies, which is the expected behaviour and matters if the caller later mutates one.',
      },
    ],
    quiz: {
      id: 'qz-m13-05',
      questions: [
        {
          id: 'q-m13-05-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['array-methods'],
          prompt: 'What becomes the accumulator for the next iteration?',
          options: [
            'The current element',
            'Whatever the callback returns',
            'The initial value, every time',
            'The array itself',
          ],
          correct: 1,
          optionExplanations: [
            'That is the second parameter, not the accumulator.',
            'Correct — which is why forgetting to return breaks everything.',
            'The initial value is used only for the first call.',
            'The array is the fourth callback argument.',
          ],
          explanation:
            'The callback\'s return value becomes the accumulator for the next call, and after the last element it becomes the result of the whole `reduce`. A callback that returns nothing hands `undefined` onward and the reduction collapses.',
        },
        {
          id: 'q-m13-05-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['array-methods'],
          prompt: 'What happens here?',
          code: ['console.log([].reduce((a, b) => a + b));'].join('\n'),
          options: ['0', 'undefined', 'A TypeError', 'NaN'],
          correct: 2,
          optionExplanations: [
            'That would require an initial value of 0.',
            'It throws rather than returning anything.',
            'Correct — there is no first element to seed the accumulator.',
            'No arithmetic happens.',
          ],
          explanation:
            'Without an initial value, `reduce` uses the first element as the accumulator — and an empty array has none, so it throws `TypeError: Reduce of empty array with no initial value`. Supplying `0` makes it return `0` instead. This is the strongest reason to always provide an initial value.',
        },
        {
          id: 'q-m13-05-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['array-methods'],
          prompt: 'How many times is the callback invoked for `[1,2,3].reduce(fn)` with no initial value?',
          options: ['3', '2', '4', '1'],
          correct: 1,
          optionExplanations: [
            'That would be the count with an initial value.',
            'Correct — the first element seeds the accumulator, so iteration starts at the second.',
            'There is no extra call.',
            'The callback runs more than once.',
          ],
          explanation:
            'Without an initial value the first element becomes the starting accumulator and is not passed to the callback, so the callback runs `length - 1` times. With an initial value it runs once per element.',
        },
        {
          id: 'q-m13-05-4',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['array-methods'],
          prompt: 'Which are good uses of `reduce`? (Select all that apply.)',
          options: [
            'Building an object of counts from an array',
            'Grouping items into buckets by a property',
            'Selecting elements that match a condition',
            'Computing several statistics in a single pass',
          ],
          correct: [0, 1, 3],
          explanation:
            'Building objects, grouping and computing several results at once are all things no other array method does well. Selecting matching elements is exactly what `filter` names, so using `reduce` for it produces working but needlessly opaque code — the method name should be the documentation.',
        },
      ],
    },
    summary:
      '`reduce` collapses an array to a single value by carrying an **accumulator** between iterations: whatever the callback returns becomes the next accumulator, and the final return is the result. Always supply an initial value — without one, an empty array throws `TypeError: Reduce of empty array with no initial value`, the first element silently becomes the accumulator so the callback runs one fewer time, and the accumulator\'s type is dictated by the data rather than your intent. The accumulator can be any type, which is what takes `reduce` far beyond summing: an object for counting, an object of arrays for grouping, or a multi-field object for computing several statistics in one pass. The most common bug is a block-bodied callback with no `return`, which hands `undefined` onward. Use `reduce` when no named method fits — using it to do `filter`\'s job works but hides the intent.',
    keyTakeaways: [
      'The callback’s return value becomes the next accumulator',
      'Always supply an initial value — an empty array otherwise throws',
      'Without an initial value the callback runs `length - 1` times',
      'The accumulator can be an object or array, not just a number',
      'Forgetting `return` in a block body collapses the reduction to `undefined`',
      'Prefer a named method when one fits; reach for `reduce` when none does',
    ],
    relatedLessons: ['l-m13-04', 'l-m13-06'],
    interviewConnections: [
      'How does reduce work?',
      'What happens if you call reduce with no initial value?',
      'How would you count occurrences in an array?',
      'How would you group an array of objects by a property?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m13-06',
    slug: 'flat-flatmap-and-from',
    moduleId: M,
    order: 6,
    title: 'flat, flatMap, Array.from and Array.of',
    description: 'Flattening nested arrays, mapping with a variable output count, and building arrays from other things.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 15,
    xp: 35,
    topicIds: ['array-methods'],
    prerequisites: ['l-m13-05'],
    learningObjectives: [
      'Flatten nested arrays to any depth with `flat`',
      'Use `flatMap` to map and flatten in one pass',
      'Build arrays from iterables and array-likes with `Array.from`',
      'Explain why `Array.of` exists alongside `Array()`',
    ],
    sections: [
      {
        kind: SECTION.HEADING,
        text: 'flat',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`flat(depth)` returns a new array with nested arrays merged into it. The default depth is **1**.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Depth controls how many levels are unwrapped.',
        code: [
          'const nested = [1, [2, 3], [4, [5, 6]]];',
          '',
          'console.log("default (1):", nested.flat());',
          'console.log("depth 2    :", nested.flat(2));',
          'console.log("infinite   :", nested.flat(Infinity));',
          '',
          'console.log("original   :", nested);',
          '',
          '// It also removes empty slots',
          'const sparse = [1, , 3];',
          'console.log("sparse flat:", sparse.flat());',
        ].join('\n'),
        output: [
          'default (1): [1, 2, 3, 4, [5, 6]]',
          'depth 2    : [1, 2, 3, 4, 5, 6]',
          'infinite   : [1, 2, 3, 4, 5, 6]',
          'original   : [1, [2, 3], [4, [5, 6]]]',
          'sparse flat: [1, 3]',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`flat(Infinity)` flattens completely regardless of depth, which is what you want when the nesting is unknown. Note it also drops empty slots — a useful side effect when cleaning up sparse arrays.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'flatMap',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`flatMap` is `map` followed by `flat(1)`, in a single pass. Its real value is that the callback may return **any number of elements** — zero, one, or many — where `map` always produces exactly one.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'One input can produce several outputs.',
        code: [
          'const sentences = ["hello world", "goodbye"];',
          '',
          'console.log("map    :", sentences.map((s) => s.split(" ")));',
          'console.log("flatMap:", sentences.flatMap((s) => s.split(" ")));',
          '',
          '// Returning an empty array removes the element entirely',
          'const numbers = [1, 2, 3, 4];',
          'const doubledEvens = numbers.flatMap((n) => (n % 2 === 0 ? [n * 2] : []));',
          'console.log("filter+map in one:", doubledEvens);',
        ].join('\n'),
        output: [
          'map    : [["hello", "world"], ["goodbye"]]',
          'flatMap: ["hello", "world", "goodbye"]',
          'filter+map in one: [4, 8]',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Returning `[]` drops an element',
        body: [
          'Because `flatMap` merges the returned arrays, returning an empty array contributes nothing — so the element disappears. That lets you filter and transform in one pass.',
          'Whether that is clearer than `.filter(...).map(...)` depends on the case. Two named methods usually read better; `flatMap` wins when the decision and the transformation are genuinely the same computation.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A realistic flatMap: expanding nested data.',
        code: [
          'const orders = [',
          '  { id: 1, items: ["pen", "book"] },',
          '  { id: 2, items: ["lamp"] },',
          '  { id: 3, items: [] },',
          '];',
          '',
          'const allItems = orders.flatMap((o) => o.items);',
          'console.log("all items:", allItems);',
          '',
          '// Tagging each item with its order',
          'const tagged = orders.flatMap((o) => o.items.map((item) => `${o.id}:${item}`));',
          'console.log("tagged   :", tagged);',
        ].join('\n'),
        output: [
          'all items: ["pen", "book", "lamp"]',
          'tagged   : ["1:pen", "1:book", "2:lamp"]',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The order with no items simply contributes nothing, without needing a filter. This "expand a nested collection into a flat list" shape is extremely common when working with API responses.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: '`flatMap` only flattens one level',
        body: [
          'It is `map` plus `flat(1)`, with no depth argument. If your callback returns nested arrays, the inner nesting survives.',
          'For deeper flattening, use `.map(...).flat(depth)` explicitly.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Array.from',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`Array.from` builds a real array from anything **iterable** or **array-like**, with an optional mapping function.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Four things you can build an array from.',
        code: [
          '// From a string',
          'console.log(Array.from("abc"));',
          '',
          '// From a Set — the standard way to deduplicate',
          'console.log(Array.from(new Set([1, 2, 2, 3])));',
          '',
          '// From an array-like: an object with a length',
          'console.log(Array.from({ length: 3 }));',
          '',
          '// With a mapping function — the second argument',
          'console.log(Array.from({ length: 4 }, (_, i) => i * i));',
          '',
          '// From a Map',
          'console.log(Array.from(new Map([["a", 1]])));',
        ].join('\n'),
        output: [
          '["a", "b", "c"]',
          '[1, 2, 3]',
          '[undefined, undefined, undefined]',
          '[0, 1, 4, 9]',
          '[["a", 1]]',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'The mapping function is not just a convenience',
        body: [
          '`Array.from({ length: 3 })` produces real `undefined` **elements**, not empty slots — unlike `Array(3)`. That is why `Array.from({ length: n }, fn)` works where `Array(n).map(fn)` does not.',
          'The `_` for the unused first parameter is a convention meaning "I must accept this but do not use it". The index is what matters when generating a sequence.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Generating sequences and deduplicating.',
        code: [
          '// A range of numbers',
          'const range = Array.from({ length: 5 }, (_, i) => i + 1);',
          'console.log("range     :", range);',
          '',
          '// Deduplicating with spread is equivalent and shorter',
          'const dupes = [3, 1, 3, 2, 1];',
          'console.log("from Set  :", Array.from(new Set(dupes)));',
          'console.log("spread Set:", [...new Set(dupes)]);',
          '',
          '// Splitting a string by code point',
          'console.log("emoji safe:", Array.from("a👋b").length);',
          'console.log("split(\'\')  :", "a👋b".split("").length);',
        ].join('\n'),
        output: [
          'range     : [1, 2, 3, 4, 5]',
          'from Set  : [3, 1, 2]',
          'spread Set: [3, 1, 2]',
          'emoji safe: 3',
          "split('')  : 4",
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The last pair repeats the Unicode point from Module 05: `Array.from` and spread iterate by **code point**, keeping surrogate pairs intact, while `split("")` works in code units and breaks emoji apart.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Array.of',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'It exists to fix the `Array()` inconsistency.',
        code: [
          '// The problem',
          'console.log("Array(3)   :", Array(3).length, Array(3));',
          'console.log("Array(1,2) :", Array(1, 2));',
          '',
          '// Array.of always treats arguments as elements',
          'console.log("Array.of(3):", Array.of(3));',
          'console.log("of(1,2)    :", Array.of(1, 2));',
        ].join('\n'),
        output: [
          'Array(3)   : 3 [undefined, undefined, undefined]',
          'Array(1,2) : [1, 2]',
          'Array.of(3): [3]',
          'of(1,2)    : [1, 2]',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`Array.of(3)` gives `[3]` where `Array(3)` gives a length-3 array of empty slots. It removes the special case, which matters when the argument comes from a variable and might be a single number.',
          'In practice you will rarely write it — an array literal is clearer — but it explains the inconsistency you met in Module 12.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const words = ["a b", "c"];',
          '',
          'const a = words.map((w) => w.split(" "));',
          'const b = words.flatMap((w) => w.split(" "));',
          '',
          'console.log(a.length, b.length);',
        ].join('\n'),
        options: [
          '2 3',
          '3 3',
          '2 2',
          '3 2',
        ],
        correct: 0,
        explanation:
          '`map` produces exactly one output per input, so `a` is `[["a","b"], ["c"]]` — two elements, each an array. `flatMap` merges those returned arrays one level, giving `["a","b","c"]` — three elements. This is the whole point of `flatMap`: it lets a callback contribute a variable number of elements, where `map` always contributes exactly one.',
      },
    ],
    exercises: [
      {
        id: 'ex-m13-06-a',
        title: 'Predict the flat depth',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['array-methods'],
        instructions: 'What does this print?',
        code: ['console.log([1, [2, [3]]].flat());'].join('\n'),
        options: [
          '[1, 2, 3]',
          '[1, 2, [3]]',
          '[1, [2, [3]]]',
          '[1, 2]',
        ],
        correct: 1,
        hints: [
          'What is the default depth?',
        ],
        solution: '[1, 2, [3]]',
        solutionExplanation:
          '`flat()` defaults to a depth of 1, so it unwraps only the outermost level of nesting. The inner `[3]` survives as an array. Use `flat(2)` to go one level deeper, or `flat(Infinity)` to flatten completely when the nesting depth is unknown.',
      },
      {
        id: 'ex-m13-06-b',
        title: 'Flatten order items',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['array-methods'],
        instructions:
          'Write `allItems(orders)` returning a single flat array of every item across all orders. Each order is `{ id, items }`. Orders with no items contribute nothing. Use `flatMap`.',
        starterCode: [
          'function allItems(orders) {',
          '  // flatMap merges the returned arrays.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'flattens several orders', body: 'expect(allItems([{ items: ["a", "b"] }, { items: ["c"] }])).toEqual(["a", "b", "c"]);' },
          { name: 'skips empty orders', body: 'expect(allItems([{ items: [] }, { items: ["a"] }])).toEqual(["a"]);' },
          { name: 'handles all empty', body: 'expect(allItems([{ items: [] }])).toEqual([]);' },
          { name: 'handles no orders', body: 'expect(allItems([])).toEqual([]);' },
          { name: 'preserves order', body: 'expect(allItems([{ items: ["x"] }, { items: ["y", "z"] }])).toEqual(["x", "y", "z"]);' },
          { name: 'returns a flat array', body: 'expect(allItems([{ items: ["a"] }]).every((i) => typeof i === "string")).toBe(true);', hidden: true },
        ],
        hints: [
          'The callback returns each order’s `items` array.',
          '`flatMap` merges those arrays one level.',
          'An empty array contributes nothing automatically.',
        ],
        solution: [
          'function allItems(orders) {',
          '  return orders.flatMap((order) => order.items);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Returning the `items` array directly lets `flatMap` merge it into the result, so each order contributes as many elements as it has — including zero. That is why empty orders need no filtering. The equivalent `orders.map((o) => o.items).flat()` produces the same result in two steps; `flatMap` does it in one pass and states the intent. The hidden test confirms the result is genuinely flat rather than an array of arrays.',
      },
      {
        id: 'ex-m13-06-c',
        title: 'Build a range',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['array-methods'],
        instructions:
          'Write `range(start, end)` returning an array of the whole numbers from `start` up to and including `end`. Return an empty array when `end` is less than `start`. Use `Array.from` rather than a loop.',
        starterCode: [
          'function range(start, end) {',
          '  // Array.from({ length: n }, (_, i) => ...)',
          '}',
        ].join('\n'),
        tests: [
          { name: 'builds an ascending range', body: 'expect(range(1, 5)).toEqual([1, 2, 3, 4, 5]);' },
          { name: 'includes both ends', body: 'expect(range(3, 4)).toEqual([3, 4]);' },
          { name: 'handles a single value', body: 'expect(range(2, 2)).toEqual([2]);' },
          { name: 'returns empty when reversed', body: 'expect(range(5, 1)).toEqual([]);' },
          { name: 'handles zero', body: 'expect(range(0, 2)).toEqual([0, 1, 2]);' },
          { name: 'handles negatives', body: 'expect(range(-2, 1)).toEqual([-2, -1, 0, 1]);' },
          { name: 'returns real elements', body: 'expect(range(1, 3).map((n) => n * 2)).toEqual([2, 4, 6]);', hidden: true },
        ],
        hints: [
          'The length is `end - start + 1`.',
          'Guard against a negative length before calling `Array.from`.',
          'The mapping function is `(_, i) => start + i`.',
        ],
        solution: [
          'function range(start, end) {',
          '  const length = end - start + 1;',
          '  if (length <= 0) return [];',
          '',
          '  return Array.from({ length }, (_, i) => start + i);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The `+ 1` makes the range inclusive at both ends, and the guard handles a reversed range where the computed length would be zero or negative — `Array.from` with a negative length gives an empty array anyway, but stating the intent is clearer. The mapping function receives the index and offsets it by `start`, which handles negative starts without any extra work. The hidden test matters: `Array.from` produces **real elements**, so `map` works on them, whereas `Array(n)` would give empty slots that `map` skips entirely.',
      },
      {
        id: 'ex-m13-06-d',
        title: 'Deduplicate and sort',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['array-methods'],
        instructions:
          'Write `uniqueSorted(numbers)` returning a new array of the distinct values in ascending order. The input must not be modified.',
        starterCode: [
          'function uniqueSorted(numbers) {',
          '  // A Set removes duplicates; then sort a copy numerically.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'removes duplicates and sorts', body: 'expect(uniqueSorted([3, 1, 3, 2])).toEqual([1, 2, 3]);' },
          { name: 'handles an already-unique array', body: 'expect(uniqueSorted([2, 1])).toEqual([1, 2]);' },
          { name: 'handles all duplicates', body: 'expect(uniqueSorted([5, 5, 5])).toEqual([5]);' },
          { name: 'handles an empty array', body: 'expect(uniqueSorted([])).toEqual([]);' },
          { name: 'sorts numerically, not as strings', body: 'expect(uniqueSorted([10, 9, 100])).toEqual([9, 10, 100]);' },
          { name: 'handles negatives', body: 'expect(uniqueSorted([-1, -5, -1])).toEqual([-5, -1]);' },
          { name: 'does not modify the input', body: 'const src = [3, 1]; uniqueSorted(src); expect(src).toEqual([3, 1]);', hidden: true },
        ],
        hints: [
          '`new Set(numbers)` removes duplicates.',
          'Spread or `Array.from` converts it back to an array.',
          'Supply a numeric comparator to `sort`, or `[10, 9]` will sort as strings.',
        ],
        solution: [
          'function uniqueSorted(numbers) {',
          '  return [...new Set(numbers)].sort((a, b) => a - b);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'A `Set` stores only distinct values, and spreading it produces a fresh array — which is why sorting it does not touch the caller\'s array, satisfying the hidden test. The numeric comparator is essential: without it `[10, 9, 100]` would sort as strings to `[10, 100, 9]`, which the fifth test catches. `Array.from(new Set(numbers))` is exactly equivalent to the spread. Module 34 covers `Set` properly, including why it is O(n) rather than the O(n²) of repeated `includes` checks.',
      },
    ],
    quiz: {
      id: 'qz-m13-06',
      questions: [
        {
          id: 'q-m13-06-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['array-methods'],
          prompt: 'What is `flatMap` equivalent to?',
          options: [
            '`flat()` followed by `map()`',
            '`map()` followed by `flat(1)`',
            '`map()` followed by `filter()`',
            '`reduce()` with an array accumulator',
          ],
          correct: 1,
          optionExplanations: [
            'The order is the other way round.',
            'Correct — map first, then flatten one level.',
            'No filtering is involved, though returning `[]` does drop an element.',
            'It could be written that way, but that is not the definition.',
          ],
          explanation:
            '`flatMap` maps each element and then flattens the result by exactly one level, in a single pass. It only flattens one level, so a callback returning nested arrays leaves the inner nesting intact.',
        },
        {
          id: 'q-m13-06-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['array-methods'],
          prompt: 'What is the difference between these two?',
          code: ['console.log(Array.of(3), Array(3).length);'].join('\n'),
          options: [
            '[3] 3',
            '[3] 1',
            '[undefined, undefined, undefined] 3',
            '3 3',
          ],
          correct: 0,
          optionExplanations: [
            'Correct — `Array.of` treats the argument as an element; `Array` treats it as a length.',
            '`Array(3)` has length 3.',
            '`Array.of(3)` returns a one-element array.',
            '`Array.of` returns an array, not a number.',
          ],
          explanation:
            '`Array.of(3)` always treats its arguments as elements, giving `[3]`. `Array(3)` treats a single numeric argument as a length, giving a length-3 array of empty slots. `Array.of` exists precisely to remove that special case.',
        },
        {
          id: 'q-m13-06-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['array-methods'],
          prompt: 'Why does `Array.from({ length: 3 }, (_, i) => i)` work when `Array(3).map((_, i) => i)` does not?',
          options: [
            '`map` is not available on arrays created with `Array()`',
            '`Array.from` creates real elements, while `Array(3)` creates empty slots that `map` skips',
            '`Array.from` is faster',
            'They both work identically',
          ],
          correct: 1,
          optionExplanations: [
            '`map` is available; it simply has nothing to visit.',
            'Correct — the callback is never called for empty slots.',
            'Performance is not the distinction.',
            'The second produces empty slots rather than a sequence.',
          ],
          explanation:
            '`Array(3)` creates empty slots, and `map` skips them entirely — the callback never runs, so the result is still three empty slots. `Array.from({ length: 3 })` creates genuine `undefined` elements, so the mapping function is called for each one.',
        },
        {
          id: 'q-m13-06-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['array-methods'],
          prompt: 'Returning an empty array from a `flatMap` callback removes that element from the result.',
          options: ['True', 'False'],
          correct: 0,
          optionExplanations: [
            'Correct — an empty array contributes no elements when merged.',
            'Merging an empty array adds nothing.',
          ],
          explanation:
            'Because `flatMap` merges each returned array into the result, an empty array contributes nothing and the element effectively disappears. That allows filtering and transforming in a single pass, though two named methods often read more clearly.',
        },
      ],
    },
    summary:
      '`flat(depth)` merges nested arrays into a new one, defaulting to a depth of 1 and accepting `Infinity` for unknown nesting; it also drops empty slots. `flatMap` is `map` plus `flat(1)` in one pass, and its value is that a callback may contribute any number of elements — returning `[]` removes the element entirely, allowing filter-and-transform together. It flattens only one level. `Array.from` builds a real array from any iterable or array-like, with an optional mapping function; crucially it creates genuine elements, which is why `Array.from({ length: n }, fn)` generates a sequence where `Array(n).map(fn)` cannot. It also iterates strings by code point, keeping emoji intact. `Array.of` treats every argument as an element, removing the `Array(3)` special case.',
    keyTakeaways: [
      '`flat()` defaults to depth 1; use `flat(Infinity)` for unknown nesting',
      '`flatMap` is `map` + `flat(1)` and only flattens one level',
      'Returning `[]` from a `flatMap` callback drops that element',
      '`Array.from` creates real elements; `Array(n)` creates empty slots',
      '`Array.from({ length: n }, (_, i) => ...)` is the idiom for generating a sequence',
      '`[...new Set(arr)]` is the standard deduplication idiom',
    ],
    relatedLessons: ['l-m13-05', 'l-m13-07'],
    interviewConnections: [
      'What is the difference between map and flatMap?',
      'How do you flatten a deeply nested array?',
      'How do you remove duplicates from an array?',
      'Why does Array(3).map() not work?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m13-07',
    slug: 'chaining-and-choosing',
    moduleId: M,
    order: 7,
    title: 'Chaining and Choosing the Right Method',
    description: 'Combining methods into pipelines, the cost of doing so, and a decision guide for the whole family.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 16,
    xp: 40,
    topicIds: ['array-methods', 'clean-code'],
    prerequisites: ['l-m13-06'],
    learningObjectives: [
      'Chain array methods into a readable pipeline',
      'Explain which methods can and cannot be chained',
      'Reason about the cost of multiple passes',
      'Choose the right method for any iteration task',
    ],
    sections: [
      {
        kind: SECTION.HEADING,
        text: 'Why chaining works',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Every method that returns an **array** can be followed by another array method. `map` and `filter` both do, so they chain freely. `forEach` returns `undefined`, so nothing can follow it — a common source of `Cannot read properties of undefined`.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A pipeline reading top to bottom.',
        code: [
          'const orders = [',
          '  { id: 1, total: 120, status: "paid" },',
          '  { id: 2, total: 40, status: "pending" },',
          '  { id: 3, total: 200, status: "paid" },',
          '  { id: 4, total: 80, status: "paid" },',
          '];',
          '',
          'const summary = orders',
          '  .filter((o) => o.status === "paid")',
          '  .filter((o) => o.total > 50)',
          '  .map((o) => `#${o.id}: £${o.total}`);',
          '',
          'console.log(summary);',
          '',
          '// Ending in a non-array value stops the chain',
          'const revenue = orders',
          '  .filter((o) => o.status === "paid")',
          '  .reduce((sum, o) => sum + o.total, 0);',
          '',
          'console.log("revenue:", revenue);',
        ].join('\n'),
        output: [
          '["#1: £120", "#3: £200", "#4: £80"]',
          'revenue: 400',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'You cannot chain after `forEach`',
        body: [
          '`items.forEach(...).map(...)` throws `Cannot read properties of undefined (reading \'map\')`, because `forEach` returned `undefined`.',
          'The same applies to `sort` and `reverse` in a subtle way: they *do* return an array so they chain fine, but they return the **same, mutated** array — so `arr.sort().map(...)` has already reordered the caller\'s array as a side effect.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The hidden mutation in a chain.',
        code: [
          'const source = [3, 1, 2];',
          '',
          'const result = source.sort((a, b) => a - b).map((n) => n * 10);',
          '',
          'console.log("result:", result);',
          'console.log("source was reordered:", source);',
          '',
          '// Copy first to keep the source intact',
          'const safeSource = [3, 1, 2];',
          'const safe = [...safeSource].sort((a, b) => a - b).map((n) => n * 10);',
          'console.log("safe  :", safe, "source:", safeSource);',
        ].join('\n'),
        output: [
          'result: [10, 20, 30]',
          'source was reordered: [1, 2, 3]',
          'safe  : [10, 20, 30] source: [3, 1, 2]',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'The cost of multiple passes',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Each method in a chain is a separate loop over the data, and each intermediate `map` or `filter` allocates a new array. Three chained methods over 1,000 items means 3,000 iterations and two throwaway arrays.',
          'For typical application data this is entirely irrelevant — readability wins comfortably. It starts to matter with very large arrays or expensive callbacks.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Counting the passes.',
        code: [
          'const nums = [1, 2, 3, 4, 5, 6];',
          'let visits = 0;',
          '',
          'const chained = nums',
          '  .filter((n) => { visits++; return n % 2 === 0; })',
          '  .map((n) => { visits++; return n * 10; });',
          '',
          'console.log("chained:", chained, "visits:", visits);',
          '',
          'let reduceVisits = 0;',
          'const single = nums.reduce((acc, n) => {',
          '  reduceVisits++;',
          '  if (n % 2 === 0) acc.push(n * 10);',
          '  return acc;',
          '}, []);',
          '',
          'console.log("single :", single, "visits:", reduceVisits);',
        ].join('\n'),
        output: [
          'chained: [20, 40, 60] visits: 9',
          'single : [20, 40, 60] visits: 6',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The chained version visits 9 times — six for the filter, three for the map over the survivors. The single `reduce` visits 6. Both are correct; the chain is markedly clearer, and the difference only becomes meaningful at scale.',
          'A useful ordering habit costs nothing: **filter before map**, so the transformation runs on fewer elements.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Optimise for readability first',
        body: [
          'Measure before rewriting a clear chain into an opaque `reduce`. Module 43 covers performance properly; the summary is that a readable chain over a few thousand items is not your bottleneck.',
          'The exception worth knowing: `find` and `some` stop early, so replacing `filter(...)[0]` with `find` or `filter(...).length > 0` with `some` is both clearer *and* faster, with no trade-off at all.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The complete decision guide',
      },
      {
        kind: SECTION.TABLE,
        headers: ['You want', 'Method', 'Returns', 'Stops early'],
        rows: [
          ['To do something with each element', '`forEach`', '`undefined`', 'No'],
          ['A transformed array of the same length', '`map`', 'Array', 'No'],
          ['A subset', '`filter`', 'Array', 'No'],
          ['The first matching element', '`find`', 'Element or `undefined`', 'Yes'],
          ['The index of the first match', '`findIndex`', 'Number or `-1`', 'Yes'],
          ['The last match', '`findLast` / `findLastIndex`', 'Element / number', 'Yes'],
          ['Whether any match', '`some`', 'Boolean', 'Yes'],
          ['Whether all match', '`every`', 'Boolean', 'Yes'],
          ['A single accumulated value', '`reduce`', 'Anything', 'No'],
          ['To flatten nesting', '`flat` / `flatMap`', 'Array', 'No'],
          ['To know whether a value is present', '`includes`', 'Boolean', 'Yes'],
          ['An array from something else', '`Array.from`', 'Array', '—'],
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Worked example: a full pipeline',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Answering several questions about one dataset.',
        code: [
          'const students = [',
          '  { name: "Ada", score: 92, active: true },',
          '  { name: "Grace", score: 78, active: true },',
          '  { name: "Alan", score: 45, active: false },',
          '  { name: "Katherine", score: 88, active: true },',
          '];',
          '',
          'const active = students.filter((s) => s.active);',
          '',
          'console.log("names   :", active.map((s) => s.name));',
          'console.log("any fail:", active.some((s) => s.score < 50));',
          'console.log("all pass:", active.every((s) => s.score >= 50));',
          'console.log("top     :", active.find((s) => s.score > 90)?.name);',
          '',
          'const average = active.reduce((sum, s) => sum + s.score, 0) / active.length;',
          'console.log("average :", average);',
          '',
          'const ranked = [...active]',
          '  .sort((a, b) => b.score - a.score)',
          '  .map((s, i) => `${i + 1}. ${s.name} (${s.score})`);',
          '',
          'console.log(ranked);',
        ].join('\n'),
        output: [
          'names   : ["Ada", "Grace", "Katherine"]',
          'any fail: false',
          'all pass: true',
          'top     : Ada',
          'average : 86',
          '["1. Ada (92)", "2. Katherine (88)", "3. Grace (78)"]',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Each line uses the method whose return type matches the question. Note the spread before `sort` — without it, `active` would be reordered, and since `filter` returns an array holding the same objects, the ordering of that intermediate result would silently change for any later code using it.',
          'Note also `?.name` after `find`, guarding the case where no student scores above 90.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'A note on breaking up long chains',
        body: [
          'A chain of two or three methods reads well. Beyond that, naming intermediate results is usually clearer — `const active = ...` then `const ranked = ...` gives each stage a name and something to inspect while debugging.',
          'There is no rule about the number. The test is whether a reader can state what each stage does without re-reading it.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const nums = [1, 2, 3, 4];',
          '',
          'const result = nums',
          '  .filter((n) => n % 2 === 0)',
          '  .map((n) => n * 10)',
          '  .reduce((sum, n) => sum + n, 0);',
          '',
          'console.log(result, Array.isArray(result));',
        ].join('\n'),
        options: [
          '60 false',
          '[20, 40] true',
          '100 false',
          '60 true',
        ],
        correct: 0,
        explanation:
          'The filter keeps `2` and `4`. The map turns them into `20` and `40`. The reduce sums them to `60`. Because `reduce` returns a single accumulated value rather than an array, the result is the number `60` and `Array.isArray` is `false` — which is also why nothing further could be chained onto it. Each stage feeds the next, and the final return type is decided by the last method in the chain.',
      },
    ],
    exercises: [
      {
        id: 'ex-m13-07-a',
        title: 'Why does chaining after forEach fail?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['array-methods'],
        instructions: 'Why does `items.forEach((i) => i).map((i) => i)` throw?',
        options: [
          'Because `forEach` mutates the array',
          'Because `forEach` returns `undefined`, and `undefined` has no `map` method',
          'Because `map` cannot follow another method',
          'Because the callback returns nothing',
        ],
        correct: 1,
        hints: [
          'What does `forEach` return?',
        ],
        solution: 'Because `forEach` returns `undefined`, and `undefined` has no `map` method',
        solutionExplanation:
          '`forEach` always returns `undefined`, so the chain attempts `undefined.map(...)` and throws `Cannot read properties of undefined (reading \'map\')`. Only methods returning an array can be chained onward — `map`, `filter`, `flat`, `flatMap`, `slice`, `concat`, and the mutating `sort` and `reverse`. Replace the `forEach` with `map` if you want the results.',
        },
      {
        id: 'ex-m13-07-b',
        title: 'Build a pipeline',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['array-methods'],
        instructions:
          'Write `topScorerNames(students, minScore)` returning the names of active students scoring at least `minScore`, sorted highest first. Students are `{ name, score, active }`. The input must not be modified.',
        starterCode: [
          'function topScorerNames(students, minScore) {',
          '  // filter, sort a copy, then map.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'filters and sorts', body: 'expect(topScorerNames([{ name: "A", score: 70, active: true }, { name: "B", score: 90, active: true }], 60)).toEqual(["B", "A"]);' },
          { name: 'excludes inactive students', body: 'expect(topScorerNames([{ name: "A", score: 90, active: false }], 60)).toEqual([]);' },
          { name: 'excludes low scores', body: 'expect(topScorerNames([{ name: "A", score: 50, active: true }], 60)).toEqual([]);' },
          { name: 'includes the boundary score', body: 'expect(topScorerNames([{ name: "A", score: 60, active: true }], 60)).toEqual(["A"]);' },
          { name: 'handles an empty list', body: 'expect(topScorerNames([], 60)).toEqual([]);' },
          { name: 'does not reorder the input', body: 'const src = [{ name: "A", score: 70, active: true }, { name: "B", score: 90, active: true }]; topScorerNames(src, 60); expect(src[0].name).toBe("A");', hidden: true },
        ],
        hints: [
          'Filter on both `active` and the score.',
          '`filter` already returns a new array, so sorting it does not touch the input.',
          'Sort descending with `(a, b) => b.score - a.score`.',
        ],
        solution: [
          'function topScorerNames(students, minScore) {',
          '  return students',
          '    .filter((s) => s.active && s.score >= minScore)',
          '    .sort((a, b) => b.score - a.score)',
          '    .map((s) => s.name);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The `sort` here is safe without an explicit copy because `filter` has already produced a **new** array — sorting that intermediate result cannot reorder the caller\'s array, which the hidden test confirms. That is a genuine subtlety: `students.sort(...).filter(...)` would have mutated the input, whereas filtering first makes the sort harmless. The descending comparator subtracts in the reverse order, and `>=` includes the boundary score.',
      },
      {
        id: 'ex-m13-07-c',
        title: 'Choose the right method',
        kind: EXERCISE_KIND.CHOOSE_IMPLEMENTATION,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['array-methods'],
        instructions:
          'You need to know whether an array of orders contains any that are unpaid, as a boolean. Which is best?',
        options: [
          'orders.filter((o) => !o.paid).length > 0',
          'orders.some((o) => !o.paid)',
          'orders.map((o) => !o.paid).includes(true)',
          'orders.find((o) => !o.paid) !== undefined',
        ],
        correct: 1,
        hints: [
          'Which method returns a boolean and stops at the first match?',
        ],
        solution: 'orders.some((o) => !o.paid)',
        solutionExplanation:
          '`some` returns the boolean directly and stops at the first unpaid order, doing the least work and stating the intent most clearly. The `filter` version scans everything and allocates an array just to check its length. The `map` version allocates an array of booleans and then scans that. The `find` version stops early and is correct, but returns an element that must then be compared — more indirection for the same answer.',
      },
      {
        id: 'ex-m13-07-d',
        title: 'Summarise a dataset',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 30,
        topicIds: ['array-methods'],
        instructions:
          'Write `summarise(orders)` returning `{ count, revenue, average, hasPending }` for the paid orders only. `orders` are `{ total, status }`. `average` is `0` when there are no paid orders, and `hasPending` reports whether **any** order has status `"pending"`.',
        starterCode: [
          'function summarise(orders) {',
          '  // Use the method that answers each question directly.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'summarises paid orders', body: 'expect(summarise([{ total: 100, status: "paid" }, { total: 50, status: "paid" }])).toEqual({ count: 2, revenue: 150, average: 75, hasPending: false });' },
          { name: 'ignores non-paid in revenue', body: 'expect(summarise([{ total: 100, status: "paid" }, { total: 50, status: "pending" }]).revenue).toBe(100);' },
          { name: 'detects pending orders', body: 'expect(summarise([{ total: 10, status: "pending" }]).hasPending).toBe(true);' },
          { name: 'handles no paid orders', body: 'expect(summarise([{ total: 10, status: "pending" }])).toEqual({ count: 0, revenue: 0, average: 0, hasPending: true });' },
          { name: 'handles an empty array', body: 'expect(summarise([])).toEqual({ count: 0, revenue: 0, average: 0, hasPending: false });' },
          { name: 'computes a decimal average', body: 'expect(summarise([{ total: 10, status: "paid" }, { total: 5, status: "paid" }]).average).toBe(7.5);' },
          { name: 'does not modify the input', body: 'const src = [{ total: 1, status: "paid" }]; summarise(src); expect(src.length).toBe(1);', hidden: true },
        ],
        hints: [
          'Filter the paid orders once and reuse that array.',
          '`reduce` with an initial value of `0` gives the revenue safely.',
          'Guard the division so an empty list gives `0` rather than `NaN`.',
          '`some` answers the pending question over the *original* array.',
        ],
        solution: [
          'function summarise(orders) {',
          '  const paid = orders.filter((o) => o.status === "paid");',
          '  const revenue = paid.reduce((sum, o) => sum + o.total, 0);',
          '',
          '  return {',
          '    count: paid.length,',
          '    revenue,',
          '    average: paid.length === 0 ? 0 : revenue / paid.length,',
          '    hasPending: orders.some((o) => o.status === "pending"),',
          '  };',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Filtering once and naming the result avoids repeating that pass for the count, revenue and average — a small example of why naming intermediate stages beats one enormous chain. The `reduce` initial value of `0` makes an empty list return `0` rather than throwing. The division guard is essential: `0 / 0` is `NaN`, which would fail the empty-array test. And `hasPending` deliberately queries the **original** array rather than the filtered one, since pending orders were exactly what the filter removed.',
      },
    ],
    quiz: {
      id: 'qz-m13-07',
      questions: [
        {
          id: 'q-m13-07-1',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['array-methods'],
          prompt: 'Which methods can be followed by another array method in a chain? (Select all that apply.)',
          options: ['`map`', '`filter`', '`forEach`', '`flatMap`'],
          correct: [0, 1, 3],
          explanation:
            '`map`, `filter` and `flatMap` all return arrays, so the chain continues. `forEach` returns `undefined`, so anything after it throws. `sort` and `reverse` also return arrays and chain — but they return the same mutated array, so they change the source as a side effect.',
        },
        {
          id: 'q-m13-07-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['array-methods'],
          prompt: 'What is the source array afterwards?',
          code: [
            'const src = [3, 1, 2];',
            'const out = src.sort((a, b) => a - b).map((n) => n);',
            'console.log(src);',
          ].join('\n'),
          options: ['[3, 1, 2]', '[1, 2, 3]', '[]', 'undefined'],
          correct: 1,
          optionExplanations: [
            '`sort` mutated it before `map` ran.',
            'Correct — `sort` reordered the original in place.',
            'Nothing was removed.',
            '`src` is still an array.',
          ],
          explanation:
            '`sort` mutates the array it is called on and returns that same array, so the chain reordered `src` as a side effect. Copy first — `[...src].sort(...)` — or use `toSorted` to leave the source untouched.',
        },
        {
          id: 'q-m13-07-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['array-methods', 'clean-code'],
          prompt: 'When is rewriting a readable chain as a single `reduce` worthwhile?',
          options: [
            'Always — one pass is better than several',
            'Rarely — only when profiling shows the chain is a genuine bottleneck on large data',
            'Never — `reduce` cannot replace a chain',
            'Whenever the chain has more than two methods',
          ],
          correct: 1,
          optionExplanations: [
            'Readability normally matters far more than a few extra passes.',
            'Correct — measure before trading clarity for speed.',
            '`reduce` can express most chains, just less legibly.',
            'Method count is not the criterion.',
          ],
          explanation:
            'Each chained method is another pass and another intermediate array, but for typical application data that cost is negligible. Optimise for readability and only collapse into a `reduce` when measurement justifies it. Swapping `filter(...)[0]` for `find` is different — that is clearer *and* faster.',
        },
        {
          id: 'q-m13-07-4',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['array-methods'],
          prompt: 'Why put `filter` before `map` in a chain?',
          options: [
            'Because `map` cannot follow `filter`',
            'Because the transformation then runs on fewer elements, and the chain reads in task order',
            'Because `filter` mutates the array',
            'The order makes no difference at all',
          ],
          correct: 1,
          optionExplanations: [
            'Either order is syntactically valid.',
            'Correct — less work, and it matches how you would describe the task.',
            '`filter` never mutates.',
            'It can change correctness as well as cost.',
          ],
          explanation:
            'Filtering first means `map` transforms only the survivors. It can also affect correctness: mapping first may discard the very properties the filter needs to inspect.',
        },
      ],
    },
    summary:
      'Any method returning an array can be chained onward, so `map`, `filter`, `flatMap`, `slice` and `concat` compose freely; `forEach` returns `undefined` and ends a chain with a TypeError. `sort` and `reverse` do chain but return the **same mutated array**, so copy first with spread or use `toSorted` to protect the source — although a `sort` applied to an intermediate result from `filter` is already safe. Each stage is a separate pass with an intermediate allocation, which is almost never a real cost for application-sized data; filter before map both reduces the work and matches how you would describe the task aloud. Choose each method by the return type that answers your question: `undefined` for `forEach`, an array for `map`/`filter`, an element for `find`, a boolean for `some`/`every`, and anything at all for `reduce`. Where a method stops early — `find`, `some`, `every`, `includes` — it is both clearer and faster than the `filter`-based alternative.',
    keyTakeaways: [
      'Methods returning arrays chain; `forEach` ends a chain with a TypeError',
      '`sort` and `reverse` chain but mutate — copy first, or sort a filtered result',
      'Each chained method is another pass; readability normally wins',
      'Filter before map: less work, and it can matter for correctness',
      'Pick the method whose return type answers your question',
      '`find` over `filter(...)[0]`, and `some` over `filter(...).length > 0`',
    ],
    relatedLessons: ['l-m13-06', 'l-m13-01'],
    interviewConnections: [
      'Why can you not chain after forEach?',
      'Does sort mutate the original array?',
      'Should you replace a chain with a single reduce?',
      'How do you choose between map, filter, find, some and reduce?',
    ],
  },
];
