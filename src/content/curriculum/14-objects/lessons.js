import { SECTION, CALLOUT_TONE, DIFFICULTY, EXERCISE_KIND, QUIZ_KIND } from '../../schema/types.js';

const M = 'm14';

export default [
  /* ================================================================== */
  {
    id: 'l-m14-01',
    slug: 'objects-and-property-access',
    moduleId: M,
    order: 1,
    title: 'Objects and Property Access',
    description: 'Storing named data, and the two notations for reading it — including when only one of them works.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 16,
    xp: 35,
    topicIds: ['objects'],
    prerequisites: ['l-m12-01'],
    learningObjectives: [
      'Create objects with the literal syntax',
      'Read properties with dot and bracket notation',
      'Explain exactly when bracket notation is required',
      'Explain why property keys are always strings or symbols',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'An array holds values in a numbered sequence. An **object** holds values under **names**. When order does not matter but identity does — a user has a name, an email and an age — an object is the right shape.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The object literal.',
        code: [
          'const user = {',
          '  name: "Ada",',
          '  age: 36,',
          '  isActive: true,',
          '};',
          '',
          'console.log(user);',
          'console.log(user.name);',
          'console.log(typeof user);',
        ].join('\n'),
        output: [
          '{name: "Ada", age: 36, isActive: true}',
          'Ada',
          'object',
        ].join('\n'),
      },
      {
        kind: SECTION.TERMS,
        terms: [
          { term: 'Property', definition: 'A name-value pair stored on an object. `name: "Ada"` is one property.' },
          { term: 'Key', definition: 'The name half of a property. Always a string or a symbol — never a number, despite appearances.' },
          { term: 'Value', definition: 'The data half. Any type at all, including another object, an array, or a function.' },
          { term: 'Method', definition: 'A property whose value is a function. Covered properly in lesson 4.' },
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Dot notation',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The default, and what happens when a property is missing.',
        code: [
          'const book = { title: "Dune", pages: 412 };',
          '',
          'console.log(book.title);',
          'console.log(book.pages);',
          '',
          '// A missing property is undefined, not an error',
          'console.log(book.author);',
          'console.log(typeof book.author);',
        ].join('\n'),
        output: ['Dune', '412', 'undefined', 'undefined'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Reading a missing property is safe; reading *through* one is not',
        body: [
          '`book.author` gives `undefined` quietly. But `book.author.name` throws, because you are then reading a property of `undefined`.',
          'This one-level-safe, two-levels-fatal behaviour is the single most common source of `Cannot read properties of undefined` in JavaScript. Lesson 3 covers the fix.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Bracket notation',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Brackets take an **expression** that evaluates to a key. Dot notation takes a literal identifier that you type out. That difference is the whole story.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Three cases where dot notation cannot work.',
        code: [
          'const settings = {',
          '  theme: "dark",',
          '  "font-size": 14,',
          '  "2fa": true,',
          '};',
          '',
          '// 1. The key is in a variable',
          'const key = "theme";',
          'console.log("variable key:", settings[key]);',
          'console.log("dot instead :", settings.key);',
          '',
          '// 2. The key is not a valid identifier',
          'console.log("hyphen:", settings["font-size"]);',
          '',
          '// 3. The key starts with a digit',
          'console.log("digit :", settings["2fa"]);',
        ].join('\n'),
        output: [
          'variable key: dark',
          'dot instead : undefined',
          'hyphen: 14',
          'digit : true',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: '`settings.key` looks for a property literally named "key"',
        body: [
          'It does **not** look up the variable `key`. Dot notation never evaluates anything — it takes the identifier you typed as the property name.',
          'This bites whenever a key comes from a function parameter. `function get(obj, prop) { return obj.prop; }` always returns `undefined` unless the object happens to have a property called `"prop"`. Write `obj[prop]`.',
        ],
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Dot notation',
          code: [
            'user.name',
            'user.age',
            '',
            '// Key typed literally.',
            '// Shorter, and the',
            '// usual choice.',
            '',
            '// Cannot use a variable,',
            '// a hyphen, a space, or',
            '// a leading digit.',
          ].join('\n'),
        },
        right: {
          title: 'Bracket notation',
          code: [
            'user["name"]',
            'user[keyVariable]',
            'user["font-size"]',
            'user[`row-${i}`]',
            '',
            '// Key is any expression',
            '// producing a string.',
            '',
            '// Required for dynamic',
            '// or awkward keys.',
          ].join('\n'),
        },
        note: 'Use dot notation by default because it is shorter and clearer. Reach for brackets when the key is computed, or when it is not a valid identifier.',
      },
      {
        kind: SECTION.HEADING,
        text: 'Keys are always strings',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Anything you use as a key is converted to a string.',
        code: [
          'const scores = {};',
          '',
          'scores[1] = "one";',
          'scores[true] = "yes";',
          'scores[null] = "nothing";',
          '',
          'console.log(scores);',
          '',
          '// The number 1 and the string "1" are the same key',
          'console.log(scores["1"]);',
          'console.log(scores[1] === scores["1"]);',
        ].join('\n'),
        output: [
          '{1: "one", true: "yes", null: "nothing"}',
          'one',
          'true',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This is why `scores[1]` and `scores["1"]` are interchangeable — the number is coerced to the string `"1"` before lookup. It is also why an array index is really a string key on an array object, as Module 12 mentioned.',
          'The one exception is a **Symbol**, which can be a key without conversion. Symbols appear in Module 33; you will not need them yet.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Objects make terrible keys',
        body: [
          'Using an object as a key converts it with `String()`, which gives `"[object Object]"` for almost every object — so two different objects collide on the same key.',
          'When you need object keys, use a `Map` (Module 34), which stores keys by identity without conversion.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Two different objects, one key.',
        code: [
          'const store = {};',
          'const a = { id: 1 };',
          'const b = { id: 2 };',
          '',
          'store[a] = "first";',
          'store[b] = "second";',
          '',
          'console.log(store);',
          'console.log(store[a]);',
        ].join('\n'),
        output: [
          '{[object Object]: "second"}',
          'second',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Values can be anything',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Objects nest freely.',
        code: [
          'const product = {',
          '  name: "Desk lamp",',
          '  price: 24.99,',
          '  tags: ["lighting", "office"],',
          '  dimensions: { width: 12, height: 40 },',
          '  describe() {',
          '    return `${this.name} — £${this.price}`;',
          '  },',
          '};',
          '',
          'console.log(product.tags[0]);',
          'console.log(product.dimensions.height);',
          'console.log(product.describe());',
        ].join('\n'),
        output: ['lighting', '40', 'Desk lamp — £24.99'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Reading nested data chains the notations — `product.tags[0]` and `product.dimensions.height` mix brackets and dots as needed. That chaining is exactly what becomes dangerous when part of the path might be missing.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const data = { name: "Ada" };',
          'const key = "name";',
          '',
          'console.log(data[key], data.key);',
        ].join('\n'),
        options: [
          'Ada undefined',
          'Ada Ada',
          'undefined Ada',
          'name Ada',
        ],
        correct: 0,
        explanation:
          '`data[key]` evaluates the expression `key`, which is the string `"name"`, and looks up that property — giving `"Ada"`. `data.key` does not evaluate anything: it looks for a property literally named `"key"`, which does not exist, so it gives `undefined`. This is the defining difference between the two notations, and the reason a function taking a property name as a parameter must use brackets.',
      },
    ],
    exercises: [
      {
        id: 'ex-m14-01-a',
        title: 'Predict the dynamic lookup',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['objects'],
        instructions: 'What does this print?',
        code: [
          'const o = { a: 1 };',
          'const k = "a";',
          'console.log(o.k);',
        ].join('\n'),
        options: ['1', 'undefined', '"a"', 'A ReferenceError'],
        correct: 1,
        hints: [
          'Does dot notation evaluate the identifier after the dot?',
        ],
        solution: 'undefined',
        solutionExplanation:
          'Dot notation takes the literal identifier you typed as the property name, so it looks for a property called `"k"`. The object has no such property, so the result is `undefined` — reading a missing property is not an error. To use the variable, write `o[k]`, which evaluates the expression and looks up `"a"`.',
      },
      {
        id: 'ex-m14-01-b',
        title: 'Write a dynamic getter',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 20,
        topicIds: ['objects'],
        instructions:
          'Write `pick(obj, key)` returning the value of that property, or the string `"missing"` when the property is not present. Treat a property whose value is genuinely `undefined` as present.',
        starterCode: [
          'function pick(obj, key) {',
          '  // The key is in a variable.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'reads a property', body: 'expect(pick({ a: 1 }, "a")).toBe(1);' },
          { name: 'reads a string value', body: 'expect(pick({ name: "Ada" }, "name")).toBe("Ada");' },
          { name: 'reports a missing key', body: 'expect(pick({ a: 1 }, "b")).toBe("missing");' },
          { name: 'handles an empty object', body: 'expect(pick({}, "a")).toBe("missing");' },
          { name: 'handles an awkward key', body: 'expect(pick({ "font-size": 14 }, "font-size")).toBe(14);' },
          { name: 'keeps a falsy value', body: 'expect(pick({ a: 0 }, "a")).toBe(0);' },
          { name: 'treats an explicit undefined as present', body: 'expect(pick({ a: undefined }, "a")).toBe(undefined);', hidden: true },
        ],
        hints: [
          'Bracket notation is required, because the key is a variable.',
          '`in` tests for presence without reading the value.',
          'Checking `=== undefined` would misreport the hidden case.',
        ],
        solution: [
          'function pick(obj, key) {',
          '  return key in obj ? obj[key] : "missing";',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Bracket notation is mandatory here — `obj.key` would look for a property literally named `"key"`. The `in` operator answers "does this property exist?" without reading its value, which is what separates a genuinely missing key from one holding `undefined`; a `obj[key] === undefined` check would fail the hidden test by reporting `"missing"` for a present property. Note the falsy test too: returning `0` correctly requires a presence check rather than truthiness.',
      },
      {
        id: 'ex-m14-01-c',
        title: 'Fix the property access',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 20,
        topicIds: ['objects'],
        instructions:
          'This function should return an array of the values of the given properties, but it returns `undefined` for each. Fix it.',
        starterCode: [
          'function pluck(obj, keys) {',
          '  return keys.map((k) => obj.k);',
          '}',
        ].join('\n'),
        tests: [
          { name: 'plucks two properties', body: 'expect(pluck({ a: 1, b: 2 }, ["a", "b"])).toEqual([1, 2]);' },
          { name: 'plucks one property', body: 'expect(pluck({ x: "hi" }, ["x"])).toEqual(["hi"]);' },
          { name: 'gives undefined for missing keys', body: 'expect(pluck({ a: 1 }, ["z"])).toEqual([undefined]);' },
          { name: 'handles no keys', body: 'expect(pluck({ a: 1 }, [])).toEqual([]);' },
          { name: 'handles awkward keys', body: 'expect(pluck({ "a-b": 3 }, ["a-b"])).toEqual([3]);', hidden: true },
        ],
        hints: [
          '`obj.k` looks for a property named `"k"`.',
          'The key is in a variable, so brackets are required.',
        ],
        solution: [
          'function pluck(obj, keys) {',
          '  return keys.map((k) => obj[k]);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Changing `obj.k` to `obj[k]` evaluates the variable and looks up the right property. The hidden test confirms brackets also handle keys that are not valid identifiers — `obj.a-b` would not even parse as a property access, it would parse as `obj.a` minus `b`. Missing keys still yield `undefined` rather than throwing, which the third test relies on.',
      },
      {
        id: 'ex-m14-01-d',
        title: 'When are brackets required?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['objects'],
        instructions: 'Which of these can only be written with bracket notation?',
        options: [
          'obj.userName',
          'obj["user name"]',
          'obj.user_name',
          'obj.$user',
        ],
        correct: 1,
        hints: [
          'Which key is not a valid JavaScript identifier?',
        ],
        solution: 'obj["user name"]',
        solutionExplanation:
          'A key containing a space is not a valid identifier, so dot notation cannot express it. Underscores and dollar signs *are* valid in identifiers, so `obj.user_name` and `obj.$user` work with dots. The rule for dot notation is the identifier rule from Module 01: letters, digits, `_` and `$`, not starting with a digit — anything else needs brackets, as does any key held in a variable.',
      },
    ],
    quiz: {
      id: 'qz-m14-01',
      questions: [
        {
          id: 'q-m14-01-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['objects'],
          prompt: 'When must you use bracket notation instead of dot notation?',
          options: [
            'When the value is an object',
            'When the key is in a variable, or is not a valid identifier',
            'When the object has more than one property',
            'Bracket notation is always required',
          ],
          correct: 1,
          optionExplanations: [
            'The value type is irrelevant.',
            'Correct — brackets evaluate an expression; dots take a literal identifier.',
            'Property count makes no difference.',
            'Dot notation is the usual choice.',
          ],
          explanation:
            'Brackets take any expression producing a key, so they are needed for computed keys and for names containing hyphens, spaces or a leading digit. Otherwise prefer dots for brevity.',
        },
        {
          id: 'q-m14-01-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['objects'],
          prompt: 'What does this print?',
          code: [
            'const o = {};',
            'o[1] = "a";',
            'console.log(o["1"]);',
          ].join('\n'),
          options: ['undefined', '"a"', 'a', 'A TypeError'],
          correct: 2,
          optionExplanations: [
            'The two keys are the same after conversion.',
            '`console.log` prints a top-level string without quotes.',
            'Correct — the number `1` becomes the string key `"1"`.',
            'Nothing here throws.',
          ],
          explanation:
            'Object keys are always strings (or symbols), so the number `1` is converted to `"1"` when used as a key. `o[1]` and `o["1"]` therefore address the same property.',
        },
        {
          id: 'q-m14-01-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['objects'],
          prompt: 'What does reading a property that does not exist give you?',
          options: [
            '`null`',
            '`undefined`',
            'A ReferenceError',
            'An empty string',
          ],
          correct: 1,
          optionExplanations: [
            '`null` is only ever a value you assign deliberately.',
            'Correct — missing properties read as `undefined`.',
            'Only undeclared *variables* throw a ReferenceError.',
            'No conversion happens.',
          ],
          explanation:
            'Reading a missing property gives `undefined` without error. Reading a property *of* that `undefined` is what throws — `obj.missing` is fine, `obj.missing.deeper` is not.',
        },
        {
          id: 'q-m14-01-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['objects'],
          prompt: 'Two different objects used as keys on the same object address two different properties.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'Both convert to the same string.',
            'Correct — they collide on `"[object Object]"`.',
          ],
          explanation:
            'A key that is not a string or symbol is converted with `String()`, and almost every plain object converts to `"[object Object]"` — so distinct objects collide. Use a `Map` when you need keys compared by identity.',
        },
      ],
    },
    summary:
      'An object stores values under **names** rather than positions. Dot notation takes a literal identifier you type out; bracket notation evaluates an **expression** to produce the key, which is why brackets are required for computed keys and for names containing hyphens, spaces or a leading digit. The classic bug is `obj.key` when `key` is a variable — it silently looks for a property called `"key"` and gives `undefined`. Keys are always strings or symbols, so `o[1]` and `o["1"]` are the same property, and any object used as a key collapses to `"[object Object]"`, colliding with every other object. Reading a missing property is safe and yields `undefined`; reading *through* that `undefined` is what throws.',
    keyTakeaways: [
      'Objects store values by name; arrays store them by position',
      'Dot notation takes a literal identifier; brackets evaluate an expression',
      '`obj.key` with a variable `key` is the classic silent bug — use `obj[key]`',
      'Keys are always strings or symbols; numbers are converted',
      'Objects as keys all collapse to `"[object Object]"` — use a `Map`',
      'A missing property reads as `undefined`, not an error',
    ],
    relatedLessons: ['l-m12-01', 'l-m14-02'],
    interviewConnections: [
      'When do you need bracket notation?',
      'What type are object keys?',
      'What happens when you read a property that does not exist?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m14-02',
    slug: 'modifying-objects',
    moduleId: M,
    order: 2,
    title: 'Adding, Updating and Removing Properties',
    description: 'Changing an object after creation, computed keys, shorthand, and the three ways to test whether a property exists.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 16,
    xp: 35,
    topicIds: ['objects'],
    prerequisites: ['l-m14-01'],
    learningObjectives: [
      'Add, update and delete properties',
      'Explain why `const` does not prevent modifying an object',
      'Use shorthand properties, shorthand methods and computed keys',
      'Choose between `in`, `hasOwnProperty` and an `undefined` check',
    ],
    sections: [
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Assignment adds or updates; `delete` removes.',
        code: [
          'const user = { name: "Ada" };',
          '',
          '// Add',
          'user.age = 36;',
          'user["is-admin"] = true;',
          '',
          '// Update — identical syntax',
          'user.name = "Ada Lovelace";',
          '',
          'console.log(user);',
          '',
          '// Remove',
          'delete user.age;',
          'console.log(user);',
          'console.log("age is now:", user.age);',
        ].join('\n'),
        output: [
          '{name: "Ada Lovelace", age: 36, is-admin: true}',
          '{name: "Ada Lovelace", is-admin: true}',
          'age is now: undefined',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: '`const` protects the binding, not the contents',
        body: [
          'This object was declared with `const` and we changed it freely. `const` prevents **reassignment** of the variable — `user = {}` would throw — but says nothing about the object it points at.',
          'That is not an inconsistency: `const` is about the binding, exactly as Module 01 explained. To make the contents resistant to change you need `Object.freeze`, which Module 15 covers along with its limits.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The distinction, made explicit.',
        code: [
          'const config = { debug: false };',
          '',
          'config.debug = true;      // fine — mutating the object',
          'config.level = "verbose"; // fine — adding to it',
          'console.log(config);',
          '',
          'try {',
          '  config = { debug: false }; // not fine — rebinding',
          '} catch (error) {',
          '  console.log(error.name + ":", error.message);',
          '}',
        ].join('\n'),
        output: [
          '{debug: true, level: "verbose"}',
          'TypeError: Assignment to constant variable.',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'delete, and why it is rare',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`delete` removes the property entirely — the key stops existing, rather than holding `undefined`. That difference is observable.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Deleted versus set to undefined.',
        code: [
          'const a = { x: 1 };',
          'const b = { x: 1 };',
          '',
          'delete a.x;',
          'b.x = undefined;',
          '',
          'console.log("a.x:", a.x, " b.x:", b.x);',
          'console.log("in a:", "x" in a, " in b:", "x" in b);',
          'console.log("keys:", Object.keys(a).length, Object.keys(b).length);',
        ].join('\n'),
        output: [
          'a.x: undefined  b.x: undefined',
          'in a: false  in b: true',
          'keys: 0 1',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Reading them looks identical, but `in` and `Object.keys` tell the two apart. In practice `delete` is uncommon in modern code — building a new object without the property, usually with rest destructuring from lesson 6, is preferred because it keeps the original intact.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Shorthand properties',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'When the key and the variable share a name.',
        code: [
          'const name = "Ada";',
          'const age = 36;',
          '',
          '// Longhand',
          'const longhand = { name: name, age: age };',
          '',
          '// Shorthand — identical result',
          'const shorthand = { name, age };',
          '',
          'console.log(longhand);',
          'console.log(shorthand);',
          '',
          '// Mixing is fine',
          'const mixed = { name, role: "engineer", age };',
          'console.log(mixed);',
        ].join('\n'),
        output: [
          '{name: "Ada", age: 36}',
          '{name: "Ada", age: 36}',
          '{name: "Ada", role: "engineer", age: 36}',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Shorthand is the norm',
        body: [
          'You will see `{ name, age }` constantly in modern code — building an object from variables that already have the right names is extremely common, especially when returning from functions.',
          'It also encourages naming your variables after the concept rather than the context, which is good practice anyway.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Computed property names',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Brackets inside a literal evaluate the key.',
        code: [
          'const field = "email";',
          'const index = 3;',
          '',
          'const record = {',
          '  [field]: "ada@example.com",',
          '  [`row${index}`]: true,',
          '  ["is" + "Active"]: false,',
          '};',
          '',
          'console.log(record);',
          '',
          '// Without brackets, the key would be the literal text',
          'const wrong = { field: "ada@example.com" };',
          'console.log(wrong);',
        ].join('\n'),
        output: [
          '{email: "ada@example.com", row3: true, isActive: false}',
          '{field: "ada@example.com"}',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The bracket syntax inside a literal follows the same rule as bracket access: it **evaluates** the expression to get the key. It is the tool for building objects whose shape depends on data — form field names, dynamic filters, or the grouping keys you built with `reduce` in Module 13.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Shorthand methods',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Three ways to attach a function, two of them equivalent.',
        code: [
          'const counter = {',
          '  count: 0,',
          '',
          '  // Longhand',
          '  incrementLong: function () {',
          '    this.count += 1;',
          '  },',
          '',
          '  // Shorthand method — preferred',
          '  increment() {',
          '    this.count += 1;',
          '  },',
          '};',
          '',
          'counter.increment();',
          'counter.incrementLong();',
          'console.log(counter.count);',
        ].join('\n'),
        output: ['2'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Do not use an arrow function as a method that needs `this`',
        body: [
          'The third way — `increment: () => { this.count += 1; }` — compiles fine and then does not work, because an arrow function has no `this` of its own. Module 09 explained why; lesson 4 shows the failure concretely.',
          'Use shorthand methods for anything touching `this`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Checking whether a property exists',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Three checks that disagree in interesting cases.',
        code: [
          'const user = { name: "Ada", nickname: undefined };',
          '',
          'console.log("--- present with a value ---");',
          'console.log("undefined check:", user.name !== undefined);',
          'console.log("in operator    :", "name" in user);',
          'console.log("hasOwn         :", Object.hasOwn(user, "name"));',
          '',
          'console.log("--- present but undefined ---");',
          'console.log("undefined check:", user.nickname !== undefined);',
          'console.log("in operator    :", "nickname" in user);',
          'console.log("hasOwn         :", Object.hasOwn(user, "nickname"));',
          '',
          'console.log("--- inherited ---");',
          'console.log("in operator:", "toString" in user);',
          'console.log("hasOwn     :", Object.hasOwn(user, "toString"));',
        ].join('\n'),
        output: [
          '--- present with a value ---',
          'undefined check: true',
          'in operator    : true',
          'hasOwn         : true',
          '--- present but undefined ---',
          'undefined check: false',
          'in operator    : true',
          'hasOwn         : true',
          '--- inherited ---',
          'in operator: true',
          'hasOwn     : false',
        ].join('\n'),
      },
      {
        kind: SECTION.TABLE,
        headers: ['Check', 'Own property with a value', 'Own property holding `undefined`', 'Inherited property'],
        rows: [
          ['`obj.x !== undefined`', 'true', '**false**', 'true'],
          ['`"x" in obj`', 'true', 'true', '**true**'],
          ['`Object.hasOwn(obj, "x")`', 'true', 'true', 'false'],
        ],
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Every object inherits properties such as `toString` from `Object.prototype` — Module 27 explains the mechanism. `in` finds those; `Object.hasOwn` does not.',
          '**`Object.hasOwn` is the right default.** It answers the question you almost always mean: does this object itself have this property? The older `obj.hasOwnProperty("x")` does the same thing but breaks on objects created with `Object.create(null)`, which have no inherited methods at all — which is why `Object.hasOwn` was added.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const o = { a: undefined };',
          '',
          'console.log(o.a !== undefined, "a" in o, Object.hasOwn(o, "a"));',
        ].join('\n'),
        options: [
          'false true true',
          'false false false',
          'true true true',
          'false true false',
        ],
        correct: 0,
        explanation:
          'The property `a` genuinely exists — it simply holds the value `undefined`. So the `!== undefined` check reports `false`, wrongly concluding the property is absent, while both `in` and `Object.hasOwn` correctly report `true`. This is exactly why a value check is not a presence check. `hasOwn` and `in` agree here because the property is the object\'s own; they would disagree for an inherited property such as `toString`.',
      },
    ],
    exercises: [
      {
        id: 'ex-m14-02-a',
        title: 'Predict the const mutation',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['objects'],
        instructions: 'What does this print?',
        code: [
          'const o = { a: 1 };',
          'o.a = 2;',
          'console.log(o.a);',
        ].join('\n'),
        options: ['1', '2', 'A TypeError', 'undefined'],
        correct: 1,
        hints: [
          'What exactly does `const` prevent?',
        ],
        solution: '2',
        solutionExplanation:
          '`const` prevents reassigning the **variable**, not modifying the object it points at. `o = {}` would throw `Assignment to constant variable`, but `o.a = 2` mutates the existing object and is perfectly legal. Freezing the contents requires `Object.freeze`, which Module 15 covers along with the fact that it is only shallow.',
      },
      {
        id: 'ex-m14-02-b',
        title: 'Build an object with a computed key',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['objects'],
        instructions:
          'Write `makeEntry(key, value)` returning a new object with that single property. `makeEntry("age", 36)` gives `{ age: 36 }`.',
        starterCode: [
          'function makeEntry(key, value) {',
          '  // A computed property name inside the literal.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'builds an entry', body: 'expect(makeEntry("age", 36)).toEqual({ age: 36 });' },
          { name: 'builds a string entry', body: 'expect(makeEntry("name", "Ada")).toEqual({ name: "Ada" });' },
          { name: 'does not use the literal word key', body: 'expect(Object.hasOwn(makeEntry("a", 1), "key")).toBe(false);' },
          { name: 'handles an awkward key', body: 'expect(makeEntry("font-size", 14)["font-size"]).toBe(14);' },
          { name: 'has exactly one property', body: 'expect(Object.keys(makeEntry("a", 1)).length).toBe(1);' },
          { name: 'keeps an undefined value as a real property', body: 'expect(Object.hasOwn(makeEntry("a", undefined), "a")).toBe(true);', hidden: true },
        ],
        hints: [
          '`{ key: value }` would create a property literally named `"key"`.',
          'Wrap the key in square brackets: `{ [key]: value }`.',
        ],
        solution: [
          'function makeEntry(key, value) {',
          '  return { [key]: value };',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Square brackets inside an object literal evaluate the expression to produce the key, exactly as they do in bracket access. Without them the key would be the literal text `"key"`, which the third test explicitly checks for. The hidden test confirms that assigning `undefined` still creates a real property — it exists with the value `undefined`, which `Object.hasOwn` detects and a `!== undefined` check would not.',
      },
      {
        id: 'ex-m14-02-c',
        title: 'Check for a property correctly',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['objects'],
        instructions:
          'Write `hasOwnField(obj, key)` returning `true` only when the object has that property **as its own** — inherited properties such as `toString` must return `false`, and a property holding `undefined` must return `true`.',
        starterCode: [
          'function hasOwnField(obj, key) {',
          '  // Three checks are available; only one satisfies both requirements.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'finds an own property', body: 'expect(hasOwnField({ a: 1 }, "a")).toBe(true);' },
          { name: 'rejects a missing property', body: 'expect(hasOwnField({ a: 1 }, "b")).toBe(false);' },
          { name: 'finds a property holding undefined', body: 'expect(hasOwnField({ a: undefined }, "a")).toBe(true);' },
          { name: 'rejects inherited properties', body: 'expect(hasOwnField({ a: 1 }, "toString")).toBe(false);' },
          { name: 'handles an empty object', body: 'expect(hasOwnField({}, "a")).toBe(false);' },
          { name: 'finds a falsy value', body: 'expect(hasOwnField({ a: 0 }, "a")).toBe(true);' },
          { name: 'returns a real boolean', body: 'expect(typeof hasOwnField({}, "a")).toBe("boolean");', hidden: true },
        ],
        hints: [
          '`obj[key] !== undefined` fails the undefined-value test.',
          '`"key" in obj` fails the inherited test.',
          '`Object.hasOwn` satisfies both.',
        ],
        solution: [
          'function hasOwnField(obj, key) {',
          '  return Object.hasOwn(obj, key);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Each of the three tests fails for a different reason, and the exercise is built so only `Object.hasOwn` passes them all. A `!== undefined` check confuses "absent" with "present but undefined", failing the third test. The `in` operator walks the prototype chain and so finds inherited `toString`, failing the fourth. `Object.hasOwn` asks precisely the intended question and returns a genuine boolean. It also works on objects created with `Object.create(null)`, where the older `obj.hasOwnProperty(key)` would throw.',
      },
      {
        id: 'ex-m14-02-d',
        title: 'Update without mutating',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['objects'],
        instructions:
          'Write `withField(obj, key, value)` returning a **new** object with all the original properties plus the given one. The original must be unchanged, and an existing key must be overwritten.',
        starterCode: [
          'function withField(obj, key, value) {',
          '  // Copy the properties into a fresh object, then set the new one.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'adds a property', body: 'expect(withField({ a: 1 }, "b", 2)).toEqual({ a: 1, b: 2 });' },
          { name: 'overwrites an existing property', body: 'expect(withField({ a: 1 }, "a", 9)).toEqual({ a: 9 });' },
          { name: 'does not modify the original', body: 'const src = { a: 1 }; withField(src, "b", 2); expect(src).toEqual({ a: 1 });' },
          { name: 'returns a different object', body: 'const src = { a: 1 }; expect(withField(src, "b", 2) === src).toBe(false);' },
          { name: 'handles an empty object', body: 'expect(withField({}, "a", 1)).toEqual({ a: 1 });' },
          { name: 'handles an awkward key', body: 'expect(withField({}, "a-b", 1)["a-b"]).toBe(1);', hidden: true },
        ],
        hints: [
          'Spread copies the existing properties: `{ ...obj }`.',
          'A computed key sets the new one: `{ ...obj, [key]: value }`.',
          'Later properties win, which is what makes overwriting work.',
        ],
        solution: [
          'function withField(obj, key, value) {',
          '  return { ...obj, [key]: value };',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Object spread copies every own enumerable property into a fresh object, and the computed key that follows either adds a new property or overwrites an existing one — because later properties win, which the second test checks. The original is untouched, satisfying tests three and four. This immutable-update pattern is standard in modern JavaScript and essential in React state, and lesson 6 covers spread in full. Note the copy is **shallow**: nested objects are still shared, which lesson 5 examines.',
      },
    ],
    quiz: {
      id: 'qz-m14-02',
      questions: [
        {
          id: 'q-m14-02-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['objects'],
          prompt: 'Why can you modify an object declared with `const`?',
          options: [
            'Because `const` only applies to primitives',
            'Because `const` prevents reassigning the variable, not changing the object it points at',
            'Because objects are exempt from `const`',
            'You cannot — it throws',
          ],
          correct: 1,
          optionExplanations: [
            '`const` applies to every binding.',
            'Correct — it protects the binding, not the contents.',
            'There is no exemption; the rule simply concerns the binding.',
            'Only reassignment throws.',
          ],
          explanation:
            '`const` guarantees the variable will always point at the same value. For an object, that value is a reference — so the reference cannot change but the object it addresses can. `Object.freeze` is the tool for restricting the contents.',
        },
        {
          id: 'q-m14-02-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['objects'],
          prompt: 'Which check correctly distinguishes a missing property from one holding `undefined`, without finding inherited properties?',
          options: [
            '`obj.x !== undefined`',
            '`"x" in obj`',
            '`Object.hasOwn(obj, "x")`',
            '`obj.x != null`',
          ],
          correct: 2,
          optionExplanations: [
            'This reports a property holding `undefined` as missing.',
            'This also finds inherited properties such as `toString`.',
            'Correct — own properties only, regardless of value.',
            'This has the same flaw as the first, and also excludes `null`.',
          ],
          explanation:
            '`Object.hasOwn` asks whether the object itself has the property, independent of its value. `in` includes the prototype chain, and a value comparison confuses absence with an `undefined` value.',
        },
        {
          id: 'q-m14-02-3',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['objects'],
          prompt: 'What does this build?',
          code: [
            'const k = "id";',
            'console.log({ k: 1 });',
          ].join('\n'),
          options: ['{id: 1}', '{k: 1}', '{"k": 1}', 'A SyntaxError'],
          correct: 1,
          optionExplanations: [
            'That would need computed brackets: `{ [k]: 1 }`.',
            'Correct — without brackets the key is the literal text.',
            'The console prints keys without quotes here.',
            'The syntax is valid.',
          ],
          explanation:
            'Inside an object literal, a bare identifier before the colon is used as literal text, not evaluated. Wrap it in square brackets — `{ [k]: 1 }` — to use the variable\'s value as the key.',
        },
        {
          id: 'q-m14-02-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['objects'],
          prompt: 'Setting a property to `undefined` is the same as deleting it.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'Reading both gives `undefined`, but the property still exists.',
            'Correct — `in`, `Object.keys` and `Object.hasOwn` tell them apart.',
          ],
          explanation:
            'Assigning `undefined` leaves the key in place holding that value; `delete` removes the key entirely. Reading either gives `undefined`, but `Object.keys` counts one and not the other.',
        },
      ],
    },
    summary:
      'Assignment both adds and updates properties, and `const` does not stand in the way — it protects the **binding**, so `obj.x = 1` is legal while `obj = {}` throws. `delete` removes a key entirely, which is observably different from assigning `undefined`: reading both gives `undefined`, but `in`, `Object.keys` and `Object.hasOwn` distinguish them. Modern literals offer shorthand properties (`{ name, age }`), shorthand methods (`increment() { }` — never an arrow when `this` is involved), and computed keys (`{ [field]: value }`), which evaluate an expression to produce the name. For existence checks, `Object.hasOwn` is the right default: a `!== undefined` comparison mistakes an `undefined` value for absence, and `in` also reports inherited properties such as `toString`.',
    keyTakeaways: [
      '`const` prevents rebinding, not mutation',
      '`delete` removes the key; assigning `undefined` does not',
      'Shorthand `{ name, age }` and computed `{ [key]: value }` are everyday syntax',
      'Use shorthand methods, never arrows, when `this` is involved',
      '`Object.hasOwn` is the right existence check',
      '`{ ...obj, [key]: value }` updates without mutating',
    ],
    relatedLessons: ['l-m14-01', 'l-m14-03'],
    interviewConnections: [
      'Why can you modify a const object?',
      'What is the difference between deleting a property and setting it to undefined?',
      'How do you check whether an object has a property?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m14-03',
    slug: 'nested-objects-and-safe-access',
    moduleId: M,
    order: 3,
    title: 'Nested Objects and Safe Access',
    description: 'Reading deep structures without crashing — optional chaining, nullish coalescing, and what they do not protect against.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 15,
    xp: 35,
    topicIds: ['objects'],
    prerequisites: ['l-m14-02'],
    learningObjectives: [
      'Read and write deeply nested data',
      'Use optional chaining to avoid TypeErrors',
      'Pair `?.` with `??` for safe defaults',
      'Explain what optional chaining does *not* guard against',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Real data nests. An API response is objects inside arrays inside objects, and any level might be absent.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Chaining down through a structure.',
        code: [
          'const response = {',
          '  user: {',
          '    name: "Ada",',
          '    address: { city: "London", postcode: "SW1" },',
          '    orders: [{ id: 1, items: ["pen"] }],',
          '  },',
          '};',
          '',
          'console.log(response.user.name);',
          'console.log(response.user.address.city);',
          'console.log(response.user.orders[0].items[0]);',
          '',
          '// Writing works the same way',
          'response.user.address.country = "UK";',
          'console.log(response.user.address);',
        ].join('\n'),
        output: [
          'Ada',
          'London',
          'pen',
          '{city: "London", postcode: "SW1", country: "UK"}',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'The crash',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'One missing level breaks the whole chain.',
        code: [
          'const response = { user: { name: "Grace" } };',
          '',
          '// Safe — one level',
          'console.log("address:", response.user.address);',
          '',
          '// Fatal — two levels',
          'try {',
          '  console.log(response.user.address.city);',
          '} catch (error) {',
          '  console.log(error.name + ":", error.message);',
          '}',
        ].join('\n'),
        output: [
          'address: undefined',
          "TypeError: Cannot read properties of undefined (reading 'city')",
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Read the error carefully — it names the property that failed (`city`), not the one that was missing (`address`). The thing that was `undefined` is always one step to the **left** of the property named in the message. Knowing that turns a confusing error into an instant diagnosis.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Optional chaining',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: '`?.` short-circuits to `undefined` instead of throwing.',
        code: [
          'const response = { user: { name: "Grace" } };',
          '',
          'console.log(response.user?.address?.city);',
          'console.log(response.missing?.deeply?.nested?.value);',
          '',
          '// It also works with brackets and calls',
          'const key = "address";',
          'console.log(response.user?.[key]?.city);',
          'console.log(response.user.getName?.());',
          '',
          '// And with arrays',
          'console.log(response.user.orders?.[0]?.id);',
        ].join('\n'),
        output: ['undefined', 'undefined', 'undefined', 'undefined', 'undefined'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'How the short-circuit works',
        body: [
          'When the value to the left of `?.` is `null` or `undefined`, the **whole chain** stops immediately and evaluates to `undefined`. Nothing further is read, so nothing can throw.',
          'Note the syntax for brackets and calls: `obj?.[key]` and `fn?.()`. The question mark and dot come first, then the bracket or parenthesis.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Pairing with ??',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Safe access plus a sensible default.',
        code: [
          'const settings = { display: { fontSize: 0, theme: null } };',
          '',
          'console.log("missing:", settings.audio?.volume ?? 50);',
          'console.log("null   :", settings.display?.theme ?? "light");',
          '',
          '// ?? preserves 0, where || would not',
          'console.log("with ??:", settings.display?.fontSize ?? 14);',
          'console.log("with ||:", settings.display?.fontSize || 14);',
        ].join('\n'),
        output: [
          'missing: 50',
          'null   : light',
          'with ??: 0',
          'with ||: 14',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`?.` handles the *path* being absent; `??` handles the *value* being absent. Together they express "read this if you can, otherwise use that" without a single `if`.',
          'The last two lines repeat the Module 04 point that matters here: `||` replaces every falsy value, so a legitimate `0` becomes `14`. `??` only replaces `null` and `undefined`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'What optional chaining does not do',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Three limits worth knowing.',
        code: [
          'const data = { user: { age: 0 } };',
          '',
          '// 1. It does not guard the FIRST identifier',
          'try {',
          '  console.log(notDeclared?.anything);',
          '} catch (error) {',
          '  console.log(error.name + ":", error.message);',
          '}',
          '',
          '// 2. It only guards null and undefined, not other wrong types',
          'try {',
          '  console.log(data.user.age?.toUpperCase());',
          '} catch (error) {',
          '  console.log(error.name + ":", error.message);',
          '}',
          '',
          '// 3. It cannot be used on the left of an assignment',
          'console.log("read ok:", data.missing?.value);',
        ].join('\n'),
        output: [
          'ReferenceError: notDeclared is not defined',
          'TypeError: data.user.age?.toUpperCase is not a function',
          'read ok: undefined',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Do not sprinkle `?.` everywhere',
        body: [
          'Writing `a?.b?.c?.d` when only `c` is ever genuinely optional hides real bugs. If `a.b` should always exist, a missing one is a defect you want to hear about loudly, not silence into `undefined`.',
          'Use `?.` at the levels that are genuinely optional. Elsewhere, let it throw — a clear early error beats an `undefined` that surfaces three functions later.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Writing to a nested path',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Intermediate levels must exist before you can write.',
        code: [
          'const config = {};',
          '',
          '// This throws — config.display does not exist',
          'try {',
          '  config.display.theme = "dark";',
          '} catch (error) {',
          '  console.log(error.name + ":", error.message);',
          '}',
          '',
          '// Create the level first',
          'config.display ??= {};',
          'config.display.theme = "dark";',
          'console.log(config);',
        ].join('\n'),
        output: [
          "TypeError: Cannot set properties of undefined (setting 'theme')",
          '{display: {theme: "dark"}}',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Note the wording: **"Cannot set properties of undefined"** rather than "cannot read". Reading and writing fail in the same way for the same reason, and `??=` from Module 13\'s grouping example is the tidy fix.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const data = { a: { b: null } };',
          '',
          'console.log(data.a?.b?.c, data.x?.y, data.a.b?.c ?? "fallback");',
        ].join('\n'),
        options: [
          'undefined undefined fallback',
          'null undefined fallback',
          'undefined undefined null',
          'A TypeError',
        ],
        correct: 0,
        explanation:
          '`data.a?.b` is `null`, and `null?.c` short-circuits the chain to `undefined` — optional chaining treats `null` and `undefined` identically. `data.x` is `undefined`, so `data.x?.y` also short-circuits to `undefined` without throwing. The third expression short-circuits to `undefined` and `??` then supplies `"fallback"`, because `??` replaces both `null` and `undefined`. Nothing throws, because every risky step is guarded.',
      },
    ],
    exercises: [
      {
        id: 'ex-m14-03-a',
        title: 'Predict the short-circuit',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['objects'],
        instructions: 'What does this print?',
        code: [
          'const o = {};',
          'console.log(o.a?.b?.c);',
        ].join('\n'),
        options: ['undefined', 'null', 'A TypeError', '{}'],
        correct: 0,
        hints: [
          'What happens when the value left of `?.` is `undefined`?',
        ],
        solution: 'undefined',
        solutionExplanation:
          '`o.a` is `undefined`, so the `?.` short-circuits the **entire remaining chain** and the expression evaluates to `undefined`. Neither `b` nor `c` is read, which is why nothing throws. The result is always `undefined` — never `null` — regardless of whether the short-circuit was triggered by `null` or `undefined`.',
      },
      {
        id: 'ex-m14-03-b',
        title: 'Read a nested value safely',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['objects'],
        instructions:
          'Write `cityOf(response)` returning `response.user.address.city`, or the string `"Unknown"` when any part of that path is missing. An empty-string city must be returned as-is.',
        starterCode: [
          'function cityOf(response) {',
          '  // Guard the path, then default the value.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'reads a full path', body: 'expect(cityOf({ user: { address: { city: "London" } } })).toBe("London");' },
          { name: 'handles a missing address', body: 'expect(cityOf({ user: {} })).toBe("Unknown");' },
          { name: 'handles a missing user', body: 'expect(cityOf({})).toBe("Unknown");' },
          { name: 'handles a null address', body: 'expect(cityOf({ user: { address: null } })).toBe("Unknown");' },
          { name: 'handles a missing city', body: 'expect(cityOf({ user: { address: {} } })).toBe("Unknown");' },
          { name: 'preserves an empty-string city', body: 'expect(cityOf({ user: { address: { city: "" } } })).toBe("");', hidden: true },
        ],
        hints: [
          'Optional chaining at each risky level.',
          'Use `??` rather than `||` for the fallback.',
        ],
        solution: [
          'function cityOf(response) {',
          '  return response.user?.address?.city ?? "Unknown";',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Each `?.` guards one level that might be absent, and any of them short-circuits the whole chain to `undefined`, which `??` then turns into `"Unknown"` — covering the missing-user, missing-address and null-address tests identically. The hidden test is why `??` matters rather than `||`: an empty-string city is falsy, so `||` would wrongly replace it with `"Unknown"`. Note `response.user` itself needs no guard, since `response` is assumed to be an object.',
      },
      {
        id: 'ex-m14-03-c',
        title: 'Fix the unsafe access',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['objects'],
        instructions:
          'This crashes when a user has no profile. Fix it so it returns `"anonymous"` in that case, without changing the behaviour for users that do have one.',
        starterCode: [
          'function displayName(user) {',
          '  return user.profile.displayName;',
          '}',
        ].join('\n'),
        tests: [
          { name: 'reads an existing name', body: 'expect(displayName({ profile: { displayName: "Ada" } })).toBe("Ada");' },
          { name: 'handles a missing profile', body: 'expect(displayName({})).toBe("anonymous");' },
          { name: 'handles a null profile', body: 'expect(displayName({ profile: null })).toBe("anonymous");' },
          { name: 'handles a missing displayName', body: 'expect(displayName({ profile: {} })).toBe("anonymous");' },
          { name: 'preserves an empty name', body: 'expect(displayName({ profile: { displayName: "" } })).toBe("");', hidden: true },
        ],
        hints: [
          '`user.profile?.displayName` stops the crash.',
          'Add `?? "anonymous"` for the fallback.',
        ],
        solution: [
          'function displayName(user) {',
          '  return user.profile?.displayName ?? "anonymous";',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The `?.` after `profile` handles both a missing and a `null` profile — optional chaining treats the two identically, which is why tests two and three both pass with one guard. A missing `displayName` needs no extra `?.` because reading a missing property is already safe; it simply gives `undefined`, which `??` converts. The hidden test again shows why `??` beats `||` here: an empty display name is legitimate data and must survive.',
      },
      {
        id: 'ex-m14-03-d',
        title: 'Set a nested value',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['objects'],
        instructions:
          'Write `setTheme(config, theme)` that sets `config.display.theme`, creating the `display` object if it is absent, and returns the config. Existing properties on `display` must be preserved.',
        starterCode: [
          'function setTheme(config, theme) {',
          '  // Optional chaining does not help when writing.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'sets on an existing display', body: 'expect(setTheme({ display: { fontSize: 14 } }, "dark")).toEqual({ display: { fontSize: 14, theme: "dark" } });' },
          { name: 'creates a missing display', body: 'expect(setTheme({}, "dark")).toEqual({ display: { theme: "dark" } });' },
          { name: 'overwrites an existing theme', body: 'expect(setTheme({ display: { theme: "light" } }, "dark").display.theme).toBe("dark");' },
          { name: 'handles a null display', body: 'expect(setTheme({ display: null }, "dark")).toEqual({ display: { theme: "dark" } });' },
          { name: 'returns the config', body: 'const c = {}; expect(setTheme(c, "dark")).toBe(c);' },
          { name: 'preserves other top-level keys', body: 'expect(setTheme({ locale: "en" }, "dark").locale).toBe("en");', hidden: true },
        ],
        hints: [
          'Writing to `config.display.theme` throws when `display` is missing.',
          '`config.display ??= {}` creates it only when absent.',
          'Return `config` itself, not a copy.',
        ],
        solution: [
          'function setTheme(config, theme) {',
          '  config.display ??= {};',
          '  config.display.theme = theme;',
          '',
          '  return config;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Optional chaining cannot help on the left of an assignment, so the intermediate level must be created first. `??=` assigns only when the left side is `null` or `undefined`, which handles both the missing and the null-display tests while leaving an existing `display` — and its `fontSize` — untouched. Returning `config` itself rather than a copy satisfies the identity test; this function deliberately mutates, which is fine for a builder-style helper but would be wrong for React state, where lesson 6\'s spread approach is required.',
      },
    ],
    quiz: {
      id: 'qz-m14-03',
      questions: [
        {
          id: 'q-m14-03-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['objects'],
          prompt: 'What does `a?.b` evaluate to when `a` is `null`?',
          options: ['`null`', '`undefined`', 'A TypeError', '`false`'],
          correct: 1,
          optionExplanations: [
            'The short-circuit result is always `undefined`.',
            'Correct — regardless of whether the trigger was `null` or `undefined`.',
            'Avoiding that throw is the point of `?.`.',
            'No boolean conversion happens.',
          ],
          explanation:
            'Optional chaining short-circuits on both `null` and `undefined`, and the result is always `undefined`. The rest of the chain is not evaluated at all.',
        },
        {
          id: 'q-m14-03-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['objects'],
          prompt: 'Which of these throws?',
          code: [
            'const o = { a: 1 };',
            'console.log(o.missing);',
          ].join('\n'),
          options: [
            'It throws a TypeError',
            'It prints `undefined`',
            'It throws a ReferenceError',
            'It prints `null`',
          ],
          correct: 1,
          optionExplanations: [
            'Reading one missing level is safe.',
            'Correct — a missing property reads as `undefined`.',
            'ReferenceErrors are for undeclared variables.',
            '`null` is never produced by a missing property.',
          ],
          explanation:
            'Reading a missing property gives `undefined`. It is reading a property *of* that `undefined` — `o.missing.deeper` — that throws, which is why one level is safe and two are not.',
        },
        {
          id: 'q-m14-03-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['objects'],
          prompt: 'Why pair `?.` with `??` rather than `||`?',
          options: [
            '`||` does not work with optional chaining',
            '`||` replaces every falsy value, so a legitimate `0`, `""` or `false` would be overwritten',
            '`??` is faster',
            'They are identical in effect',
          ],
          correct: 1,
          optionExplanations: [
            'It works syntactically; the semantics are the problem.',
            'Correct — `??` only replaces `null` and `undefined`.',
            'Speed is not the concern.',
            'They differ for every falsy non-nullish value.',
          ],
          explanation:
            'Optional chaining produces `undefined` when the path is absent, and `??` targets exactly `null` and `undefined`. `||` would also replace `0`, `""`, `false` and `NaN`, silently discarding valid data.',
        },
        {
          id: 'q-m14-03-4',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['objects'],
          prompt: 'What does `Cannot read properties of undefined (reading \'city\')` tell you?',
          options: [
            '`city` is undefined',
            'The value to the left of `.city` is `undefined`',
            '`city` is not a function',
            'The object is frozen',
          ],
          correct: 1,
          optionExplanations: [
            '`city` was never read, so its value is unknown.',
            'Correct — the missing thing is one step to the left.',
            'That would be a different message.',
            'Freezing produces no such error.',
          ],
          explanation:
            'The error names the property it *tried* to read, not the one that was missing. The `undefined` value is always one step to the left in the chain — so in `a.b.city`, it is `a.b` that is absent.',
        },
      ],
    },
    summary:
      'Reading one missing level is safe and gives `undefined`; reading *through* it throws, and the error names the property it tried to read rather than the one that was missing — so the absent value is always one step to the **left**. Optional chaining `?.` short-circuits the whole remaining chain to `undefined` when the value on its left is `null` or `undefined`, and works with brackets (`obj?.[key]`) and calls (`fn?.()`). Pair it with `??`, which replaces exactly `null` and `undefined`, rather than `||`, which would also discard a legitimate `0` or `""`. Optional chaining does not guard an undeclared first identifier, does not protect against a value of the wrong type, and cannot appear on the left of an assignment — for writing, create the intermediate level with `??=` first. Do not apply `?.` at levels that should always exist, or you silence real bugs.',
    keyTakeaways: [
      'A missing property reads as `undefined`; reading through it throws',
      'The `undefined` value is one step left of the property named in the error',
      '`?.` short-circuits the whole chain on `null` or `undefined`',
      '`obj?.[key]` and `fn?.()` are the bracket and call forms',
      'Pair with `??`, not `||`, to preserve falsy data',
      '`?.` cannot help when writing — create the level with `??=`',
    ],
    relatedLessons: ['l-m14-02', 'l-m14-04'],
    interviewConnections: [
      'What does optional chaining do?',
      'What is the difference between ?? and ||?',
      'How would you read a deeply nested value safely?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m14-04',
    slug: 'methods-and-this',
    moduleId: M,
    order: 4,
    title: 'Methods and this',
    description: 'Functions stored on objects, what `this` refers to, and the three ways it goes wrong.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 18,
    xp: 40,
    topicIds: ['objects'],
    prerequisites: ['l-m14-03'],
    learningObjectives: [
      'Write methods that operate on their own object',
      'Explain that `this` is decided by how a function is *called*',
      'Recognise the detached-method and arrow-method failures',
      'Fix a lost `this` with an arrow wrapper or `bind`',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'A **method** is a property whose value is a function. Inside it, `this` refers to the object the method was called on — which lets one function serve many objects.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A method reading its own object.',
        code: [
          'const account = {',
          '  owner: "Ada",',
          '  balance: 100,',
          '',
          '  describe() {',
          '    return `${this.owner} has £${this.balance}`;',
          '  },',
          '',
          '  deposit(amount) {',
          '    this.balance += amount;',
          '    return this.balance;',
          '  },',
          '};',
          '',
          'console.log(account.describe());',
          'console.log(account.deposit(50));',
          'console.log(account.describe());',
        ].join('\n'),
        output: [
          'Ada has £100',
          '150',
          'Ada has £150',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'this is decided at call time',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This is the single most important fact about `this`, and the source of every problem with it.',
          '`this` is **not** bound to where the function was written. It is decided by **how the function is called** — specifically, by what appears to the left of the dot at the call site.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'One function, three objects.',
        code: [
          'function describe() {',
          '  return `I am ${this.name}`;',
          '}',
          '',
          'const a = { name: "Ada", describe };',
          'const b = { name: "Grace", describe };',
          '',
          'console.log(a.describe());',
          'console.log(b.describe());',
          '',
          '// Explicitly choosing the object',
          'console.log(describe.call({ name: "Alan" }));',
        ].join('\n'),
        output: [
          'I am Ada',
          'I am Grace',
          'I am Alan',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The same function body produced three different answers. Nothing about where it was written changed — only what was to the left of the dot. `call` makes that explicit by supplying the object directly; Module 26 covers `call`, `apply` and `bind` in depth.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Failure 1: the detached method',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Pulling a method out of its object loses `this`.',
        code: [
          'const account = {',
          '  owner: "Ada",',
          '  describe() {',
          '    return `owner is ${this?.owner}`;',
          '  },',
          '};',
          '',
          '// Called as a method — fine',
          'console.log(account.describe());',
          '',
          '// Detached — this is lost',
          'const detached = account.describe;',
          'console.log(detached());',
          '',
          '// Fixed by binding',
          'const bound = account.describe.bind(account);',
          'console.log(bound());',
        ].join('\n'),
        output: [
          'owner is Ada',
          'owner is undefined',
          'owner is Ada',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Why `this` is `undefined` rather than an error',
        body: [
          'In a module — and every ES module is strict, as Module 05 noted — a plain function call sets `this` to `undefined`. So `this.owner` would throw `Cannot read properties of undefined`; the example uses `this?.owner` to show the value rather than crash.',
          'In sloppy mode `this` would instead be the global object, which silently produces `undefined` for the property and hides the bug. Strict mode failing loudly is the better behaviour.',
        ],
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This matters constantly in real code: passing `account.describe` as a callback to `setTimeout`, an event listener or `map` detaches it in exactly this way.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The callback version, and two fixes.',
        code: [
          'const counter = {',
          '  count: 5,',
          '  report() {',
          '    return this?.count;',
          '  },',
          '};',
          '',
          'const nums = [1];',
          '',
          '// Broken — the method is detached when passed',
          'console.log("passed  :", nums.map(counter.report));',
          '',
          '// Fix 1: an arrow wrapper preserves the call shape',
          'console.log("arrow   :", nums.map(() => counter.report()));',
          '',
          '// Fix 2: bind',
          'console.log("bound   :", nums.map(counter.report.bind(counter)));',
        ].join('\n'),
        output: [
          'passed  : [undefined]',
          'arrow   : [5]',
          'bound   : [5]',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Failure 2: the arrow method',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'An arrow function has no `this` of its own.',
        code: [
          'const broken = {',
          '  name: "Ada",',
          '  greet: () => `Hello, ${this?.name}`,',
          '};',
          '',
          'const working = {',
          '  name: "Ada",',
          '  greet() {',
          '    return `Hello, ${this.name}`;',
          '  },',
          '};',
          '',
          'console.log("arrow    :", broken.greet());',
          'console.log("shorthand:", working.greet());',
        ].join('\n'),
        output: [
          'arrow    : Hello, undefined',
          'shorthand: Hello, Ada',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Arrows inherit `this` from where they were written',
        body: [
          'An arrow function does not get its own `this` — it uses the `this` of the enclosing **scope**, decided when it was written. An object literal is not a scope, so the arrow looks past it to the module top level, where `this` is `undefined`.',
          'Calling `broken.greet()` cannot change that. `this` is fixed lexically for arrows, which is precisely why they are wrong for methods and right for callbacks.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Failure 3: the nested function',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A regular function inside a method also loses `this` — and here an arrow is the fix.',
        code: [
          'const team = {',
          '  name: "Core",',
          '  members: ["Ada", "Grace"],',
          '',
          '  brokenList() {',
          '    return this.members.map(function (m) {',
          '      return `${m} of ${this?.name}`;',
          '    });',
          '  },',
          '',
          '  workingList() {',
          '    return this.members.map((m) => `${m} of ${this.name}`);',
          '  },',
          '};',
          '',
          'console.log(team.brokenList());',
          'console.log(team.workingList());',
        ].join('\n'),
        output: [
          '["Ada of undefined", "Grace of undefined"]',
          '["Ada of Core", "Grace of Core"]',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The regular function passed to `map` is called by `map` with no object to its left, so its `this` is `undefined`. The arrow function has no `this` of its own, so it uses the enclosing method\'s — which is `team`.',
          'That is the whole rule in one example: **arrows are wrong as methods and right inside them.**',
        ],
      },
      {
        kind: SECTION.TABLE,
        headers: ['Call form', '`this` is'],
        rows: [
          ['`obj.method()`', '`obj` — whatever is left of the dot'],
          ['`method()` (plain call, strict)', '`undefined`'],
          ['`fn.call(obj)` / `fn.bind(obj)()`', '`obj` — supplied explicitly'],
          ['Arrow function', 'Inherited from the enclosing scope, never its own'],
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const obj = {',
          '  name: "Ada",',
          '  regular() { return this?.name; },',
          '  arrow: () => this?.name,',
          '};',
          '',
          'const detached = obj.regular;',
          '',
          'console.log(obj.regular(), obj.arrow(), detached());',
        ].join('\n'),
        options: [
          'Ada undefined undefined',
          'Ada Ada Ada',
          'Ada Ada undefined',
          'undefined undefined undefined',
        ],
        correct: 0,
        explanation:
          '`obj.regular()` has `obj` to the left of the dot, so `this` is `obj` and the result is `"Ada"`. `obj.arrow()` looks like a method call, but an arrow has no `this` of its own — it inherited the module-level `this`, which is `undefined`, and calling it as a method cannot change that. `detached()` is a plain call with nothing to the left of a dot, so in strict mode `this` is `undefined` too. Two of the three fail, for two different reasons.',
      },
    ],
    exercises: [
      {
        id: 'ex-m14-04-a',
        title: 'Predict the arrow method',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['objects'],
        instructions: 'What does this print?',
        code: [
          'const o = { n: 1, get: () => this?.n };',
          'console.log(o.get());',
        ].join('\n'),
        options: ['1', 'undefined', 'A TypeError', 'null'],
        correct: 1,
        hints: [
          'Does an arrow function have its own `this`?',
          'Is an object literal a scope?',
        ],
        solution: 'undefined',
        solutionExplanation:
          'An arrow function has no `this` of its own — it uses the `this` of the scope where it was **written**. An object literal is not a scope, so the arrow looks past it to the surrounding module, where `this` is `undefined`. Calling it as `o.get()` cannot change that, because arrow `this` is fixed lexically rather than at call time. Use a shorthand method for anything that needs `this`.',
      },
      {
        id: 'ex-m14-04-b',
        title: 'Write a method using this',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['objects'],
        instructions:
          'Write `makeCounter(start)` returning an object with a `count` property and two methods: `increment()`, which adds 1 and returns the new count, and `reset()`, which sets the count back to `start` and returns it.',
        starterCode: [
          'function makeCounter(start) {',
          '  // Use shorthand methods so `this` works.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'starts at the given value', body: 'expect(makeCounter(5).count).toBe(5);' },
          { name: 'increments', body: 'const c = makeCounter(0); expect(c.increment()).toBe(1);' },
          { name: 'increments cumulatively', body: 'const c = makeCounter(0); c.increment(); c.increment(); expect(c.count).toBe(2);' },
          { name: 'resets to the start value', body: 'const c = makeCounter(3); c.increment(); expect(c.reset()).toBe(3);' },
          { name: 'resets the stored count too', body: 'const c = makeCounter(3); c.increment(); c.reset(); expect(c.count).toBe(3);' },
          { name: 'creates independent counters', body: 'const a = makeCounter(0); const b = makeCounter(0); a.increment(); expect(b.count).toBe(0);', hidden: true },
        ],
        hints: [
          'Return an object literal with `count`, `increment()` and `reset()`.',
          '`this.count += 1` modifies the object the method was called on.',
          'The `start` parameter stays available to `reset` through closure.',
        ],
        solution: [
          'function makeCounter(start) {',
          '  return {',
          '    count: start,',
          '    increment() {',
          '      this.count += 1;',
          '      return this.count;',
          '    },',
          '    reset() {',
          '      this.count = start;',
          '      return this.count;',
          '    },',
          '  };',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Shorthand methods get their `this` from the call site, so `c.increment()` modifies the object `c`. Each call to `makeCounter` builds a brand-new object, which is why the two counters in the hidden test are independent — a single shared object would have failed it. Note `reset` reads `start` from the enclosing function rather than from `this`: the parameter is still in scope inside the returned methods, which is closure at work, covered properly in Module 24.',
      },
      {
        id: 'ex-m14-04-c',
        title: 'Fix the lost this',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['objects'],
        instructions:
          'The `labels` method returns `undefined` for every name instead of the team name. Fix it without changing the object structure.',
        starterCode: [
          'const team = {',
          '  name: "Core",',
          '  members: ["Ada", "Grace"],',
          '  labels() {',
          '    return this.members.map(function (m) {',
          '      return `${m}@${this.name}`;',
          '    });',
          '  },',
          '};',
        ].join('\n'),
        tests: [
          { name: 'labels each member', body: 'expect(team.labels()).toEqual(["Ada@Core", "Grace@Core"]);' },
          { name: 'returns one label per member', body: 'expect(team.labels().length).toBe(2);' },
          { name: 'still reads the team name', body: 'expect(team.labels()[0].endsWith("@Core")).toBe(true);' },
          { name: 'does not modify members', body: 'team.labels(); expect(team.members).toEqual(["Ada", "Grace"]);' },
          { name: 'works after the name changes', body: 'team.name = "Ops"; const r = team.labels(); team.name = "Core"; expect(r[0]).toBe("Ada@Ops");', hidden: true },
        ],
        hints: [
          'The regular function passed to `map` has its own `this`, which is `undefined`.',
          'An arrow function would inherit `this` from `labels`.',
        ],
        solution: [
          'const team = {',
          '  name: "Core",',
          '  members: ["Ada", "Grace"],',
          '  labels() {',
          '    return this.members.map((m) => `${m}@${this.name}`);',
          '  },',
          '};',
        ].join('\n'),
        solutionExplanation:
          'The regular function was called by `map` with nothing to the left of a dot, so its `this` was `undefined` in strict mode. An arrow function has no `this` of its own and therefore uses the enclosing method\'s — which is `team`. This is the mirror image of the previous exercise: arrows are **wrong** as methods and **right** inside them. The hidden test confirms `this.name` is read at call time rather than captured, so a later name change is reflected.',
      },
      {
        id: 'ex-m14-04-d',
        title: 'Why does the detached method fail?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['objects'],
        instructions:
          'Why does `const f = obj.method; f();` lose access to the object?',
        options: [
          'Because assigning a function copies it',
          'Because `this` is decided by how the function is called, and a plain call has no object to its left',
          'Because methods cannot be stored in variables',
          'Because `const` freezes the function',
        ],
        correct: 1,
        hints: [
          'What decides `this` — where the function was written, or how it was called?',
        ],
        solution: 'Because `this` is decided by how the function is called, and a plain call has no object to its left',
        solutionExplanation:
          'The assignment copies only the reference to the function; the function itself has no memory of which object it came from. `this` is resolved at call time from whatever precedes the dot, and `f()` has nothing there — so in strict mode `this` is `undefined`. Fix it by preserving the call shape with an arrow wrapper, `() => obj.method()`, or by fixing `this` permanently with `obj.method.bind(obj)`.',
      },
    ],
    quiz: {
      id: 'qz-m14-04',
      questions: [
        {
          id: 'q-m14-04-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['objects'],
          prompt: 'What determines the value of `this` inside a regular function?',
          options: [
            'Where the function was defined',
            'How the function is called — specifically what is to the left of the dot',
            'The file it lives in',
            'Whether it was declared with `function` or `const`',
          ],
          correct: 1,
          optionExplanations: [
            'That is true for arrow functions, not regular ones.',
            'Correct — `this` is resolved at the call site.',
            'Location is irrelevant.',
            'The declaration form does not affect `this`.',
          ],
          explanation:
            '`this` is decided at call time by the call form: `obj.method()` gives `obj`, a plain call gives `undefined` in strict mode, and `call`/`bind` supply it explicitly. Only arrow functions differ, inheriting `this` from their enclosing scope.',
        },
        {
          id: 'q-m14-04-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['objects'],
          prompt: 'What does this print?',
          code: [
            'const o = { n: 1, get: () => this?.n };',
            'console.log(o.get());',
          ].join('\n'),
          options: ['1', 'undefined', 'A TypeError', '{}'],
          correct: 1,
          optionExplanations: [
            'The arrow never sees the object.',
            'Correct — an arrow inherits `this` from the surrounding scope.',
            '`?.` prevents the throw.',
            'No object is produced.',
          ],
          explanation:
            'An object literal is not a scope, so the arrow inherits the module-level `this`, which is `undefined`. Calling it as a method cannot rebind an arrow. Use a shorthand method instead.',
        },
        {
          id: 'q-m14-04-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['objects'],
          prompt: 'Inside a method, why does an arrow function passed to `map` work while a regular one does not?',
          options: [
            'Arrows are faster',
            'The arrow inherits the method’s `this`; the regular function gets its own, which `map` leaves as `undefined`',
            '`map` only accepts arrow functions',
            'Regular functions cannot be used as callbacks',
          ],
          correct: 1,
          optionExplanations: [
            'Performance is unrelated.',
            'Correct — this is exactly why arrows suit callbacks.',
            '`map` accepts any function.',
            'They can, but they bring their own `this`.',
          ],
          explanation:
            '`map` calls its callback with no object to the left of a dot, so a regular function\'s `this` is `undefined`. An arrow has no `this` of its own and uses the enclosing method\'s, which is the object. Arrows are wrong as methods and right inside them.',
        },
        {
          id: 'q-m14-04-4',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['objects'],
          prompt: 'Which fix a detached method that has lost its `this`? (Select all that apply.)',
          options: [
            '`obj.method.bind(obj)`',
            'Wrapping it: `() => obj.method()`',
            'Declaring the method as an arrow function',
            'Calling it as `obj.method()`',
          ],
          correct: [0, 1, 3],
          explanation:
            '`bind` fixes `this` permanently, an arrow wrapper preserves the `obj.method()` call shape, and calling it as a method never detaches it in the first place. Declaring the method as an arrow makes things worse — it would then inherit the surrounding scope\'s `this` and never see the object at all.',
        },
      ],
    },
    summary:
      'A method is a property holding a function, and `this` inside it refers to the object it was called on. The governing rule is that `this` is decided by **how the function is called**, not where it was written — `obj.method()` gives `obj`, a plain call gives `undefined` in strict mode, and `call`/`bind` supply it explicitly. Three failures follow from that rule: a **detached method** (`const f = obj.method`) loses its object because the reference carries no memory of it; an **arrow used as a method** never had its own `this`, and since an object literal is not a scope it inherits the module-level `undefined`; and a **regular function nested inside a method** is called with no object to its left. The fixes are an arrow wrapper or `bind` for the first, a shorthand method for the second, and an arrow for the third — arrows are wrong as methods and right inside them.',
    keyTakeaways: [
      '`this` is decided by the call site, not the definition site',
      '`obj.method()` gives `obj`; a plain call gives `undefined` in strict mode',
      'Detaching a method loses `this` — fix with `bind` or an arrow wrapper',
      'An arrow as a method inherits the surrounding scope, never the object',
      'An arrow *inside* a method correctly inherits the method’s `this`',
      'Use shorthand methods for anything touching `this`',
    ],
    relatedLessons: ['l-m14-03', 'l-m14-05'],
    interviewConnections: [
      'What determines the value of this?',
      'Why does a detached method lose this?',
      'Why should you not use an arrow function as an object method?',
      'When is an arrow function the right choice for this?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m14-05',
    slug: 'references-equality-and-copying',
    moduleId: M,
    order: 5,
    title: 'References, Equality and Copying',
    description: 'Why two identical-looking objects are not equal, and what "shallow copy" actually costs you.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 18,
    xp: 45,
    topicIds: ['objects'],
    prerequisites: ['l-m14-04'],
    learningObjectives: [
      'Explain that object variables hold references, not objects',
      'Explain why `===` compares identity for objects',
      'Make a shallow copy and state precisely what it shares',
      'Choose an appropriate deep-copy technique and know its limits',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Module 12 established this for arrays. It applies to every object, and it is the concept that separates people who debug JavaScript confidently from people who guess.',
          'A variable holding an object does not contain the object. It contains a **reference** — an address. Copying the variable copies the address, not the thing at that address.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Two names, one object.',
        code: [
          'const original = { name: "Ada", age: 36 };',
          'const alias = original;',
          '',
          'alias.age = 37;',
          '',
          'console.log("original:", original);',
          'console.log("alias   :", alias);',
          'console.log("same object:", original === alias);',
        ].join('\n'),
        output: [
          'original: {name: "Ada", age: 37}',
          'alias   : {name: "Ada", age: 37}',
          'same object: true',
        ].join('\n'),
      },
      {
        kind: SECTION.DIAGRAM,
        diagram: 'value-vs-reference',
        caption: 'Both bindings store the same address, so only one object exists. A primitive variable would instead hold its data directly, which is why primitives never alias.',
      },
      {
        kind: SECTION.HEADING,
        text: 'Equality compares identity',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Contents are irrelevant to `===`.',
        code: [
          'const a = { x: 1 };',
          'const b = { x: 1 };',
          'const c = a;',
          '',
          'console.log("identical contents:", a === b);',
          'console.log("same reference    :", a === c);',
          '',
          '// Even empty ones',
          'console.log({} === {}, [] === []);',
          '',
          '// Primitives compare by value, which is why they behave differently',
          'console.log("primitives:", "ada" === "ada", 1 === 1);',
        ].join('\n'),
        output: [
          'identical contents: false',
          'same reference    : true',
          'false false',
          'primitives: true true',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'There is no built-in deep equality',
        body: [
          '`===` on objects asks "is this the same object?", never "do these have the same contents?". JavaScript provides no built-in structural comparison.',
          'For a quick check on plain data you can compare `JSON.stringify` output, but that is fragile — key order matters, and `undefined`, functions and `Map`/`Set` do not survive. Test frameworks provide a proper deep-equal, which is what the `toEqual` in these exercises does.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Why the JSON trick is unreliable.',
        code: [
          'const a = { x: 1, y: 2 };',
          'const b = { y: 2, x: 1 };',
          '',
          'console.log("same contents:", JSON.stringify(a) === JSON.stringify(b));',
          'console.log(JSON.stringify(a));',
          'console.log(JSON.stringify(b));',
        ].join('\n'),
        output: [
          'same contents: false',
          '{"x":1,"y":2}',
          '{"y":2,"x":1}',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Shallow copying',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Two ways, identical result.',
        code: [
          'const original = { name: "Ada", age: 36 };',
          '',
          'const spread = { ...original };',
          'const assigned = Object.assign({}, original);',
          '',
          'spread.age = 99;',
          '',
          'console.log("original:", original);',
          'console.log("spread  :", spread);',
          'console.log("distinct:", original !== spread);',
          'console.log("assign  :", assigned);',
        ].join('\n'),
        output: [
          'original: {name: "Ada", age: 36}',
          'spread  : {name: "Ada", age: 99}',
          'distinct: true',
          'assign  : {name: "Ada", age: 36}',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Spread is the modern choice. `Object.assign(target, source)` does the same job but also **mutates its first argument**, which is why the empty `{}` is there — passing `original` as the target would modify it.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'What "shallow" means',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The copy shares every nested object.',
        code: [
          'const original = {',
          '  name: "Ada",',
          '  address: { city: "London" },',
          '  tags: ["admin"],',
          '};',
          '',
          'const copy = { ...original };',
          '',
          '// Top-level change: independent',
          'copy.name = "Grace";',
          '',
          '// Nested change: shared',
          'copy.address.city = "Paris";',
          'copy.tags.push("staff");',
          '',
          'console.log("original name   :", original.name);',
          'console.log("original city   :", original.address.city);',
          'console.log("original tags   :", original.tags);',
          'console.log("same address obj:", original.address === copy.address);',
        ].join('\n'),
        output: [
          'original name   : Ada',
          'original city   : Paris',
          'original tags   : ["admin", "staff"]',
          'same address obj: true',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'The precise rule',
        body: [
          'A shallow copy copies the **values** of the top-level properties. For a primitive, the value is the data itself, so it is genuinely independent. For an object or array, the value is a **reference** — so the copy points at the same nested object.',
          'This is the source of the classic "I copied it but changing the copy still changed the original" bug. The top level *was* copied; the nesting was not.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Deep copying',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: '`structuredClone` is the modern built-in.',
        code: [
          'const original = {',
          '  name: "Ada",',
          '  address: { city: "London" },',
          '  tags: ["admin"],',
          '};',
          '',
          'const deep = structuredClone(original);',
          '',
          'deep.address.city = "Paris";',
          'deep.tags.push("staff");',
          '',
          'console.log("original city:", original.address.city);',
          'console.log("original tags:", original.tags);',
          'console.log("shared?      :", original.address === deep.address);',
        ].join('\n'),
        output: [
          'original city: London',
          'original tags: ["admin"]',
          'shared?      : false',
        ].join('\n'),
      },
      {
        kind: SECTION.TABLE,
        headers: ['Technique', 'Depth', 'Limits'],
        rows: [
          ['`{ ...obj }`', 'Shallow', 'Nested objects shared'],
          ['`Object.assign({}, obj)`', 'Shallow', 'Same; also mutates its target'],
          ['`structuredClone(obj)`', 'Deep', 'Throws on functions; no prototypes or getters'],
          ['`JSON.parse(JSON.stringify(obj))`', 'Deep', 'Drops `undefined` and functions; `Date` becomes a string'],
          ['Manual nested spread', 'Controlled', 'Verbose; only copies the levels you name'],
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'What the JSON round-trip silently destroys.',
        code: [
          'const original = {',
          '  when: new Date("2024-01-15T00:00:00Z"),',
          '  missing: undefined,',
          '  run: () => "hello",',
          '  count: NaN,',
          '  nested: { ok: true },',
          '};',
          '',
          'const copy = JSON.parse(JSON.stringify(original));',
          '',
          'console.log("date type :", typeof copy.when);',
          'console.log("undefined :", "missing" in copy);',
          'console.log("function  :", "run" in copy);',
          'console.log("NaN       :", copy.count);',
          'console.log("nested ok :", copy.nested.ok, original.nested !== copy.nested);',
        ].join('\n'),
        output: [
          'date type : string',
          'undefined : false',
          'function  : false',
          'NaN       : null',
          'nested ok : true true',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Which to reach for',
        body: [
          'For plain data with no functions, **`structuredClone`** is the correct modern answer — it handles `Date`, `Map`, `Set`, `RegExp`, typed arrays and even circular references, all of which the JSON trick mangles or crashes on.',
          'Most of the time you do not need a deep copy at all. Immutable updates with nested spread — `{ ...obj, address: { ...obj.address, city } }` — copy only the levels you are changing, which is both cheaper and clearer about intent.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Targeted immutable update: copy only the path you change.',
        code: [
          'const user = {',
          '  name: "Ada",',
          '  address: { city: "London", postcode: "SW1" },',
          '};',
          '',
          'const moved = {',
          '  ...user,',
          '  address: { ...user.address, city: "Paris" },',
          '};',
          '',
          'console.log("original:", user.address.city);',
          'console.log("moved   :", moved.address.city);',
          'console.log("postcode kept:", moved.address.postcode);',
          'console.log("new object   :", user.address !== moved.address);',
        ].join('\n'),
        output: [
          'original: London',
          'moved   : Paris',
          'postcode kept: SW1',
          'new object   : true',
        ].join('\n'),
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const a = { list: [1, 2] };',
          'const b = { ...a };',
          '',
          'b.list.push(3);',
          'b.other = "new";',
          '',
          'console.log(a.list.length, a.other, a.list === b.list);',
        ].join('\n'),
        options: [
          '3 undefined true',
          '2 undefined false',
          '3 "new" true',
          '2 "new" false',
        ],
        correct: 0,
        explanation:
          'Spread made a shallow copy, so `b.list` holds the **same array reference** as `a.list` — pushing through `b` is visible through `a`, giving a length of 3, and the identity comparison is `true`. Adding `b.other` is a top-level change on the copy alone, so `a.other` remains `undefined`. That split is exactly what "shallow" means: top-level properties are independent, nested objects are shared.',
      },
    ],
    exercises: [
      {
        id: 'ex-m14-05-a',
        title: 'Predict the equality',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['objects'],
        instructions: 'What does this print?',
        code: ['console.log({ a: 1 } === { a: 1 });'].join('\n'),
        options: ['true', 'false', 'undefined', 'A TypeError'],
        correct: 1,
        hints: [
          'Does `===` compare contents or identity for objects?',
        ],
        solution: 'false',
        solutionExplanation:
          'These are two separate objects that happen to hold the same data. `===` on objects compares **identity** — whether both operands are the same object in memory — never contents. So two independently-created objects are never `===`, even when empty. JavaScript has no built-in structural comparison; test frameworks supply one, which is what `toEqual` uses.',
      },
      {
        id: 'ex-m14-05-b',
        title: 'Copy without sharing',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['objects'],
        instructions:
          'Write `renameCity(user, city)` returning a **new** user object with `address.city` changed. Neither the original user nor its original address object may be modified, and `address.postcode` must be preserved.',
        starterCode: [
          'function renameCity(user, city) {',
          '  // A single spread is not enough — the address is nested.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'changes the city', body: 'expect(renameCity({ name: "A", address: { city: "London" } }, "Paris").address.city).toBe("Paris");' },
          { name: 'preserves other address fields', body: 'expect(renameCity({ address: { city: "L", postcode: "SW1" } }, "P").address.postcode).toBe("SW1");' },
          { name: 'preserves top-level fields', body: 'expect(renameCity({ name: "A", address: { city: "L" } }, "P").name).toBe("A");' },
          { name: 'does not modify the original', body: 'const u = { address: { city: "London" } }; renameCity(u, "Paris"); expect(u.address.city).toBe("London");' },
          { name: 'returns a new outer object', body: 'const u = { address: { city: "L" } }; expect(renameCity(u, "P") === u).toBe(false);' },
          { name: 'returns a new address object', body: 'const u = { address: { city: "L" } }; expect(renameCity(u, "P").address === u.address).toBe(false);', hidden: true },
        ],
        hints: [
          '`{ ...user }` copies the top level but shares the address.',
          'Spread the address too, then override the city.',
          '`{ ...user, address: { ...user.address, city } }`',
        ],
        solution: [
          'function renameCity(user, city) {',
          '  return {',
          '    ...user,',
          '    address: { ...user.address, city },',
          '  };',
          '}',
        ].join('\n'),
        solutionExplanation:
          'A single `{ ...user }` would have copied only the top level, leaving `address` as a shared reference — mutating it would have failed the fourth test and the hidden identity test. Spreading the address as well creates a fresh nested object, and the shorthand `city` sets the new value since the parameter already has the right name. This copies exactly the levels being changed, which is cheaper and clearer than a full deep clone.',
      },
      {
        id: 'ex-m14-05-c',
        title: 'Compare two objects by contents',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 30,
        topicIds: ['objects'],
        instructions:
          'Write `sameShallow(a, b)` returning `true` when both objects have exactly the same keys with `===`-equal values. Nested objects need only be compared by reference.',
        starterCode: [
          'function sameShallow(a, b) {',
          '  // Compare key counts first, then every value.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'matches identical contents', body: 'expect(sameShallow({ a: 1 }, { a: 1 })).toBe(true);' },
          { name: 'ignores key order', body: 'expect(sameShallow({ a: 1, b: 2 }, { b: 2, a: 1 })).toBe(true);' },
          { name: 'rejects different values', body: 'expect(sameShallow({ a: 1 }, { a: 2 })).toBe(false);' },
          { name: 'rejects extra keys', body: 'expect(sameShallow({ a: 1 }, { a: 1, b: 2 })).toBe(false);' },
          { name: 'rejects missing keys', body: 'expect(sameShallow({ a: 1, b: 2 }, { a: 1 })).toBe(false);' },
          { name: 'matches two empty objects', body: 'expect(sameShallow({}, {})).toBe(true);' },
          { name: 'compares nested objects by reference', body: 'const n = { x: 1 }; expect(sameShallow({ n }, { n })).toBe(true); expect(sameShallow({ n: { x: 1 } }, { n: { x: 1 } })).toBe(false);', hidden: true },
        ],
        hints: [
          '`Object.keys(a).length` gives the key count.',
          'Compare counts first, then check every key of `a` against `b`.',
          '`every` from Module 13 is a natural fit.',
        ],
        solution: [
          'function sameShallow(a, b) {',
          '  const keysA = Object.keys(a);',
          '  if (keysA.length !== Object.keys(b).length) return false;',
          '',
          '  return keysA.every((key) => Object.hasOwn(b, key) && a[key] === b[key]);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Comparing key counts first is what catches extra keys on `b` — checking only `a`\'s keys would report `{a:1}` and `{a:1,b:2}` as equal, failing the fourth test. The `Object.hasOwn` check inside `every` then handles the same-count-but-different-keys case. Because the values are compared with `===`, nested objects are compared by identity: the hidden test shows the same nested object matching while two structurally-identical ones do not. A genuine deep comparison would need to recurse, which is Module 39 territory.',
      },
      {
        id: 'ex-m14-05-d',
        title: 'Which copy technique?',
        kind: EXERCISE_KIND.CHOOSE_IMPLEMENTATION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['objects'],
        instructions:
          'You need a fully independent copy of an object containing nested objects and a `Date`. Which is correct?',
        options: [
          '{ ...obj }',
          'structuredClone(obj)',
          'JSON.parse(JSON.stringify(obj))',
          'Object.assign({}, obj)',
        ],
        correct: 1,
        hints: [
          'Which options are shallow?',
          'What happens to a `Date` in a JSON round-trip?',
        ],
        solution: 'structuredClone(obj)',
        solutionExplanation:
          '`structuredClone` copies every level and preserves `Date`, `Map`, `Set`, `RegExp` and typed arrays, and it handles circular references. Spread and `Object.assign` are both shallow, so the nested objects would still be shared. The JSON round-trip is deep but lossy: a `Date` comes back as a string, `undefined` values and functions vanish, and `NaN` becomes `null`. The one thing `structuredClone` cannot handle is a function value, which makes it throw.',
      },
    ],
    quiz: {
      id: 'qz-m14-05',
      questions: [
        {
          id: 'q-m14-05-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['objects'],
          prompt: 'What does a variable holding an object actually contain?',
          options: [
            'The object itself',
            'A reference to the object',
            'A copy of the object',
            'A string version of the object',
          ],
          correct: 1,
          optionExplanations: [
            'The object lives elsewhere in memory.',
            'Correct — copying the variable copies the address.',
            'No copying happens on assignment.',
            'No conversion happens.',
          ],
          explanation:
            'Object variables hold references. Assigning one to another copies the reference, so both names address the same object — which is why a change through one is visible through the other.',
        },
        {
          id: 'q-m14-05-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['objects'],
          prompt: 'What does this print?',
          code: [
            'const a = { n: { v: 1 } };',
            'const b = { ...a };',
            'b.n.v = 9;',
            'console.log(a.n.v);',
          ].join('\n'),
          options: ['1', '9', 'undefined', 'A TypeError'],
          correct: 1,
          optionExplanations: [
            'The nested object was not copied.',
            'Correct — spread is shallow, so `a.n` and `b.n` are the same object.',
            'The property exists.',
            'Nothing throws.',
          ],
          explanation:
            'Spread copies the top-level property values, and the value of `n` is a reference. So `a.n` and `b.n` address the same object, and a change through either is visible through both.',
        },
        {
          id: 'q-m14-05-3',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['objects'],
          prompt: 'What does `JSON.parse(JSON.stringify(obj))` lose? (Select all that apply.)',
          options: [
            'Properties holding `undefined`',
            'Function values',
            '`Date` objects, which become strings',
            'Nested objects, which stay shared',
          ],
          correct: [0, 1, 2],
          explanation:
            'The JSON round-trip drops `undefined` and functions entirely, converts `Date` to an ISO string and `NaN` to `null`, and throws on circular references. It does copy deeply, so nested objects are genuinely independent — that part works. `structuredClone` avoids all of these losses.',
        },
        {
          id: 'q-m14-05-4',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['objects'],
          prompt: 'Why is `Object.assign({}, obj)` written with an empty object first?',
          options: [
            'For readability only',
            'Because `Object.assign` mutates its first argument, so a fresh object is needed as the target',
            'Because it requires three arguments',
            'To make it deep rather than shallow',
          ],
          correct: 1,
          optionExplanations: [
            'It is a functional requirement, not a style choice.',
            'Correct — the target is modified in place and returned.',
            'Two arguments are enough.',
            'It is shallow either way.',
          ],
          explanation:
            '`Object.assign(target, source)` copies into `target`, mutating and returning it. Passing `obj` as the target would modify the original, so a fresh `{}` is supplied. Spread has no such trap and is the modern preference.',
        },
      ],
    },
    summary:
      'An object variable holds a **reference**, so assignment copies the address rather than the object, and `===` compares identity — two objects with identical contents are never equal, and JavaScript offers no built-in structural comparison. A shallow copy (`{ ...obj }` or `Object.assign({}, obj)`, the latter mutating its first argument) copies the top-level property **values**; since the value of a nested object is a reference, the copy shares every nested object. That is the precise reason behind "I copied it but changing the copy changed the original". `structuredClone` copies deeply and preserves `Date`, `Map`, `Set` and circular references, though it throws on functions; `JSON.parse(JSON.stringify(obj))` is deep but lossy, dropping `undefined` and functions and turning `Date` into a string. Most of the time a targeted nested spread — `{ ...obj, address: { ...obj.address, city } }` — is better than any full clone, copying only the levels you change.',
    keyTakeaways: [
      'Object variables hold references; assignment copies the address',
      '`===` compares identity, never contents',
      'Shallow copies share every nested object',
      '`structuredClone` is the modern deep copy; JSON round-trips are lossy',
      '`Object.assign` mutates its first argument — always pass a fresh `{}`',
      'A targeted nested spread beats a full clone in most real code',
    ],
    relatedLessons: ['l-m12-04', 'l-m14-06'],
    interviewConnections: [
      'Why is {a:1} === {a:1} false?',
      'What is the difference between a shallow and a deep copy?',
      'How do you deep clone an object?',
      'What does JSON.parse(JSON.stringify(obj)) lose?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m14-06',
    slug: 'destructuring-and-spread',
    moduleId: M,
    order: 6,
    title: 'Object Destructuring, Spread and Rest',
    description: 'Pulling values out and putting objects together — renaming, defaults, nesting, and the merge rules.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 20,
    xp: 45,
    topicIds: ['destructuring', 'objects'],
    prerequisites: ['l-m14-05'],
    learningObjectives: [
      'Destructure with renaming, defaults and nesting',
      'Destructure function parameters, including with a default object',
      'Merge objects with spread and predict which value wins',
      'Collect remaining properties with rest',
    ],
    sections: [
      {
        kind: SECTION.HEADING,
        text: 'Basic destructuring',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Pulling properties into variables by name.',
        code: [
          'const user = { name: "Ada", age: 36, city: "London" };',
          '',
          '// Longhand',
          'const nameLong = user.name;',
          '',
          '// Destructured — matched by property name, not position',
          'const { name, age } = user;',
          'console.log(name, age, nameLong);',
          '',
          '// Order is irrelevant',
          'const { city, name: n2 } = user;',
          'console.log(city, n2);',
          '',
          '// A missing property gives undefined',
          'const { missing } = user;',
          'console.log("missing:", missing);',
        ].join('\n'),
        output: [
          'Ada 36 Ada',
          'London Ada',
          'missing: undefined',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Objects match by name; arrays match by position',
        body: [
          'Array destructuring from Module 12 uses position — `const [first, second] = arr`. Object destructuring uses the property **name**, so the order you write them in makes no difference.',
          'That also means the variable name must match the property name, unless you rename.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Renaming',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The colon reads as "into".',
        code: [
          'const response = { id: 1, name: "Ada" };',
          'const other = { id: 2, name: "Grace" };',
          '',
          '// Rename to avoid a clash',
          'const { id: userId, name: userName } = response;',
          'const { id: otherId } = other;',
          '',
          'console.log(userId, userName, otherId);',
          '',
          '// Required for awkward keys',
          'const styles = { "font-size": 14 };',
          'const { "font-size": fontSize } = styles;',
          'console.log("fontSize:", fontSize);',
        ].join('\n'),
        output: ['1 Ada 2', 'fontSize: 14'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'The colon direction confuses everyone once',
        body: [
          '`{ id: userId }` means "take the property `id` and put it **into** a variable called `userId`". The property name is on the left, the new variable on the right.',
          'That is the opposite of an object literal, where `{ id: userId }` means "make a property `id` whose value comes from `userId`". Same syntax, mirrored meaning — because one is building and the other is unpacking.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Defaults',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Defaults apply only for `undefined`.',
        code: [
          'const settings = { theme: "dark", fontSize: 0, locale: null };',
          '',
          'const { theme = "light", timeout = 30 } = settings;',
          'console.log("present:", theme, " missing:", timeout);',
          '',
          '// A default does NOT apply to null or 0',
          'const { fontSize = 14, locale = "en" } = settings;',
          'console.log("zero  :", fontSize);',
          'console.log("null  :", locale);',
          '',
          '// Renaming and defaulting together',
          'const { theme: colourScheme = "light" } = {};',
          'console.log("both  :", colourScheme);',
        ].join('\n'),
        output: [
          'present: dark  missing: 30',
          'zero  : 0',
          'null  : null',
          'both  : light',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Destructuring defaults behave exactly like parameter defaults from Module 08: they apply **only** when the value is `undefined`. A property holding `null` or `0` keeps that value.',
          'When renaming and defaulting together, the order is `property: newName = default`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Nested destructuring',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Mirroring the shape of the data.',
        code: [
          'const response = {',
          '  user: {',
          '    name: "Ada",',
          '    address: { city: "London" },',
          '  },',
          '  meta: { page: 1 },',
          '};',
          '',
          'const {',
          '  user: {',
          '    name,',
          '    address: { city },',
          '  },',
          '  meta: { page },',
          '} = response;',
          '',
          'console.log(name, city, page);',
          '',
          '// Note: the intermediate names are NOT created as variables',
          'console.log("typeof user:", typeof user);',
        ].join('\n'),
        output: ['Ada London 1', 'typeof user: undefined'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Nested destructuring throws if a level is missing',
        body: [
          '`const { user: { name } } = {}` throws `Cannot read properties of undefined (reading \'name\')` — the *exact* error from lesson 3, because a nested pattern compiles to the same property reads. Destructuring gives you no protection that plain access does not.',
          'Guard it with a default at the intermediate level: `const { user: { name } = {} } = response`. But two or three levels deep, a plain `response.user?.address?.city` is usually clearer.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The throw, and the guard.',
        code: [
          'try {',
          '  const { user: { name } } = {};',
          '  console.log(name);',
          '} catch (error) {',
          '  console.log(error.name + ":", error.message);',
          '}',
          '',
          '// Defaulting the intermediate level',
          'const { user: { name = "anonymous" } = {} } = {};',
          'console.log("guarded:", name);',
        ].join('\n'),
        output: [
          "TypeError: Cannot read properties of undefined (reading 'name')",
          'guarded: anonymous',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Destructuring parameters',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Named arguments, effectively.',
        code: [
          '// Positional — the caller must remember the order',
          'function createUserPositional(name, age, isAdmin) {',
          '  return `${name} ${age} ${isAdmin}`;',
          '}',
          '',
          '// Destructured — order-independent and self-documenting',
          'function createUser({ name, age = 18, isAdmin = false }) {',
          '  return `${name} ${age} ${isAdmin}`;',
          '}',
          '',
          'console.log(createUserPositional("Ada", 36, true));',
          'console.log(createUser({ isAdmin: true, name: "Ada", age: 36 }));',
          'console.log(createUser({ name: "Grace" }));',
        ].join('\n'),
        output: [
          'Ada 36 true',
          'Ada 36 true',
          'Grace 18 false',
        ].join('\n'),
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Making the whole argument optional needs its own default.',
        code: [
          'function withoutDefault({ theme = "light" }) {',
          '  return theme;',
          '}',
          '',
          'function withDefault({ theme = "light" } = {}) {',
          '  return theme;',
          '}',
          '',
          'console.log(withDefault());',
          '',
          'try {',
          '  console.log(withoutDefault());',
          '} catch (error) {',
          '  console.log(error.name + ":", error.message);',
          '}',
        ].join('\n'),
        output: [
          'light',
          "TypeError: Cannot read properties of undefined (reading 'theme')",
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The `= {}` at the end is what makes calling the function with no arguments legal. Without it, the parameter is `undefined` and destructuring it throws — the inner `theme = "light"` default never gets a chance to apply.',
          'This `({ ... } = {})` pattern appears constantly in library code for options objects.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Spread: merging objects',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Later properties win.',
        code: [
          'const defaults = { theme: "light", fontSize: 14, locale: "en" };',
          'const userPrefs = { theme: "dark" };',
          '',
          'console.log("defaults last:", { ...userPrefs, ...defaults });',
          'console.log("prefs last   :", { ...defaults, ...userPrefs });',
          '',
          '// An explicit property also wins if it comes later',
          'console.log("override     :", { ...defaults, fontSize: 20 });',
          'console.log("overridden   :", { fontSize: 20, ...defaults });',
        ].join('\n'),
        output: [
          'defaults last: {theme: "light", fontSize: 14, locale: "en"}',
          'prefs last   : {theme: "dark", fontSize: 14, locale: "en"}',
          'override     : {theme: "light", fontSize: 20, locale: "en"}',
          'overridden   : {fontSize: 14, theme: "light", locale: "en"}',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Order is everything',
        body: [
          'Putting the defaults **last** overwrites the user\'s choices — the single most common spread bug. The overriding values must come after the ones they override.',
          'Remember the merge is also **shallow**: a nested object in a later source replaces the earlier one entirely rather than merging into it.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Nested objects are replaced, not merged.',
        code: [
          'const defaults = { display: { theme: "light", fontSize: 14 } };',
          'const prefs = { display: { theme: "dark" } };',
          '',
          'const merged = { ...defaults, ...prefs };',
          'console.log("shallow merge:", merged);',
          '',
          '// Merging the nested level explicitly',
          'const deepMerged = {',
          '  ...defaults,',
          '  display: { ...defaults.display, ...prefs.display },',
          '};',
          'console.log("nested merge :", deepMerged);',
        ].join('\n'),
        output: [
          'shallow merge: {display: {theme: "dark"}}',
          'nested merge : {display: {theme: "dark", fontSize: 14}}',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The shallow merge silently lost `fontSize`. This is one of the most common configuration bugs in JavaScript, and it is why libraries ship dedicated deep-merge helpers.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Rest: collecting what is left',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The idiomatic way to omit a property.',
        code: [
          'const user = { id: 1, name: "Ada", password: "secret", role: "admin" };',
          '',
          'const { password, ...safe } = user;',
          'console.log("safe    :", safe);',
          'console.log("original:", Object.keys(user).length);',
          '',
          '// Extract several, keep the rest',
          'const { id, ...withoutId } = user;',
          'console.log("id      :", id);',
          'console.log("rest    :", Object.keys(withoutId));',
        ].join('\n'),
        output: [
          'safe    : {id: 1, name: "Ada", role: "admin"}',
          'original: 4',
          'id      : 1',
          'rest    : ["name", "password", "role"]',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This is the modern replacement for `delete`: it produces a new object without the property while leaving the original intact, which the key count confirms. Rest must come **last** in the pattern, and there can only be one.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const defaults = { a: 1, b: 2 };',
          'const custom = { b: 9 };',
          '',
          'const x = { ...defaults, ...custom };',
          'const y = { ...custom, ...defaults };',
          '',
          'console.log(x.b, y.b);',
        ].join('\n'),
        options: [
          '9 2',
          '9 9',
          '2 2',
          '2 9',
        ],
        correct: 0,
        explanation:
          'Spread applies properties left to right, and a later property overwrites an earlier one with the same key. In `x`, `custom` comes last so its `b: 9` wins. In `y`, `defaults` comes last so its `b: 2` overwrites the custom value — which is the classic bug: putting the defaults last silently discards the user\'s choices. The overriding source must always come after the one it overrides.',
      },
    ],
    exercises: [
      {
        id: 'ex-m14-06-a',
        title: 'Predict the merge order',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['destructuring'],
        instructions: 'What does this print?',
        code: [
          'console.log({ ...{ a: 1 }, ...{ a: 2 } }.a);',
        ].join('\n'),
        options: ['1', '2', 'undefined', 'A SyntaxError'],
        correct: 1,
        hints: [
          'Which source is applied last?',
        ],
        solution: '2',
        solutionExplanation:
          'Spread applies sources left to right, and a later property with the same key overwrites the earlier one — so the second `a: 2` wins. This ordering rule is why defaults must be spread **first** and user overrides second; reversing them silently discards the user\'s choices.',
      },
      {
        id: 'ex-m14-06-b',
        title: 'Destructure with defaults',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['destructuring'],
        instructions:
          'Write `describe(options)` returning `"<theme> at <size>px"`. Default the theme to `"light"` and the size to `14`, and make calling `describe()` with no argument work.',
        starterCode: [
          'function describe(options) {',
          '  // Destructure the parameter directly, with defaults.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'uses supplied values', body: 'expect(describe({ theme: "dark", size: 20 })).toBe("dark at 20px");' },
          { name: 'defaults the size', body: 'expect(describe({ theme: "dark" })).toBe("dark at 14px");' },
          { name: 'defaults the theme', body: 'expect(describe({ size: 20 })).toBe("light at 20px");' },
          { name: 'defaults both for an empty object', body: 'expect(describe({})).toBe("light at 14px");' },
          { name: 'works with no argument at all', body: 'expect(describe()).toBe("light at 14px");' },
          { name: 'keeps a size of zero', body: 'expect(describe({ size: 0 })).toBe("light at 0px");', hidden: true },
        ],
        hints: [
          'Destructure in the parameter list: `function describe({ theme = "light", size = 14 })`.',
          'Add `= {}` after the pattern so a missing argument does not throw.',
        ],
        solution: [
          'function describe({ theme = "light", size = 14 } = {}) {',
          '  return `${theme} at ${size}px`;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The inner defaults handle missing properties, and the trailing `= {}` handles a missing argument entirely — without it, `describe()` would try to destructure `undefined` and throw, failing the fifth test. The hidden test confirms destructuring defaults apply only to `undefined`: an explicit `0` is kept, which a `size || 14` fallback would have wrongly replaced with 14.',
      },
      {
        id: 'ex-m14-06-c',
        title: 'Omit a property with rest',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['destructuring'],
        instructions:
          'Write `omitPassword(user)` returning a new object with every property except `password`. The original must be unchanged. Use rest destructuring rather than `delete`.',
        starterCode: [
          'function omitPassword(user) {',
          '  // Extract the unwanted key; collect the rest.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'removes the password', body: 'expect(omitPassword({ id: 1, password: "x" })).toEqual({ id: 1 });' },
          { name: 'keeps every other property', body: 'expect(omitPassword({ a: 1, b: 2, password: "x" })).toEqual({ a: 1, b: 2 });' },
          { name: 'does not modify the original', body: 'const u = { id: 1, password: "x" }; omitPassword(u); expect(u.password).toBe("x");' },
          { name: 'handles an object without a password', body: 'expect(omitPassword({ id: 1 })).toEqual({ id: 1 });' },
          { name: 'handles an empty object', body: 'expect(omitPassword({})).toEqual({});' },
          { name: 'returns a new object', body: 'const u = { id: 1 }; expect(omitPassword(u) === u).toBe(false);', hidden: true },
        ],
        hints: [
          '`const { password, ...rest } = user;`',
          'Return `rest`.',
          'Rest must come last in the pattern.',
        ],
        solution: [
          'function omitPassword(user) {',
          '  const { password, ...rest } = user;',
          '',
          '  return rest;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Rest collects every own enumerable property that was not named earlier in the pattern, producing a fresh object — which is why the original keeps its password and the hidden identity test passes. `delete user.password` would have mutated the caller\'s object and failed the third test. The pattern works unchanged when the property is absent: `password` is simply `undefined` and `rest` holds everything. Note the extracted `password` variable is unused here, which some linters flag; configuring an ignore rule for rest-siblings is standard.',
      },
      {
        id: 'ex-m14-06-d',
        title: 'Merge configuration correctly',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 30,
        topicIds: ['destructuring'],
        instructions:
          'Write `mergeConfig(defaults, custom)` returning a merged object where `custom` wins at the top level **and** inside the nested `display` object. Neither input may be modified.',
        starterCode: [
          'function mergeConfig(defaults, custom) {',
          '  // A single spread merges only the top level.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'merges top-level keys', body: 'expect(mergeConfig({ a: 1 }, { b: 2 })).toEqual({ a: 1, b: 2 });' },
          { name: 'custom wins at the top level', body: 'expect(mergeConfig({ a: 1 }, { a: 9 }).a).toBe(9);' },
          { name: 'merges the nested display object', body: 'expect(mergeConfig({ display: { theme: "light", size: 14 } }, { display: { theme: "dark" } }).display).toEqual({ theme: "dark", size: 14 });' },
          { name: 'handles a missing custom display', body: 'expect(mergeConfig({ display: { theme: "light" } }, {}).display).toEqual({ theme: "light" });' },
          { name: 'handles a missing default display', body: 'expect(mergeConfig({}, { display: { theme: "dark" } }).display).toEqual({ theme: "dark" });' },
          { name: 'does not modify the inputs', body: 'const d = { display: { theme: "light" } }; mergeConfig(d, { display: { theme: "dark" } }); expect(d.display.theme).toBe("light");' },
          { name: 'adds no display key when neither has one', body: 'expect(Object.hasOwn(mergeConfig({ a: 1 }, { b: 2 }), "display")).toBe(false);', hidden: true },
        ],
        hints: [
          'Spread both at the top level, with `custom` last.',
          'Then merge the two `display` objects into a fresh one, again with custom last.',
          'Only add the `display` key when at least one side actually has one.',
        ],
        solution: [
          'function mergeConfig(defaults, custom) {',
          '  const merged = { ...defaults, ...custom };',
          '',
          '  if (defaults.display || custom.display) {',
          '    merged.display = { ...defaults.display, ...custom.display };',
          '  }',
          '',
          '  return merged;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The top-level spread with `custom` last satisfies the override tests, but on its own it would have replaced `display` wholesale and lost `size` — which is exactly what the third test checks. Re-merging `display` afterwards fixes the nested level. The guard matters: unconditionally writing `display: { ...defaults.display, ...custom.display }` would invent an empty `display` on configs that never had one, failing the first test and the hidden one. Inside the guard, spreading an `undefined` side contributes nothing rather than throwing, so the two one-sided tests need no further handling. Note `merged` is already a fresh object, so assigning to it cannot touch the inputs.',
      },
    ],
    quiz: {
      id: 'qz-m14-06',
      questions: [
        {
          id: 'q-m14-06-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['destructuring'],
          prompt: 'What does `const { id: userId } = obj` do?',
          options: [
            'Creates a property `id` from the variable `userId`',
            'Reads `obj.id` into a new variable called `userId`',
            'Reads `obj.userId` into a variable called `id`',
            'Creates both variables',
          ],
          correct: 1,
          optionExplanations: [
            'That is what the same syntax means in an object *literal*.',
            'Correct — property name on the left, new variable on the right.',
            'The direction is the other way round.',
            'Only `userId` is created.',
          ],
          explanation:
            'In a destructuring pattern the property name comes first and the new variable name follows the colon — read it as "take `id` **into** `userId`". Only `userId` is declared; `id` is not.',
        },
        {
          id: 'q-m14-06-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['destructuring'],
          prompt: 'What does this print?',
          code: [
            'const { a = 5 } = { a: null };',
            'console.log(a);',
          ].join('\n'),
          options: ['5', 'null', 'undefined', '0'],
          correct: 1,
          optionExplanations: [
            'Defaults apply only to `undefined`.',
            'Correct — `null` is a real value and is kept.',
            'The property exists.',
            'No conversion happens.',
          ],
          explanation:
            'A destructuring default applies only when the value is `undefined`, exactly like a parameter default. A property holding `null`, `0`, `""` or `false` keeps that value.',
        },
        {
          id: 'q-m14-06-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['destructuring'],
          prompt: 'Why does `function f({ a }) {}` throw when called as `f()`?',
          options: [
            'Because `a` is required',
            'Because the parameter is `undefined`, and you cannot destructure `undefined`',
            'Because destructuring is not allowed in parameters',
            'Because there is no default for `a`',
          ],
          correct: 1,
          optionExplanations: [
            'Missing properties are simply `undefined`.',
            'Correct — add `= {}` after the pattern.',
            'It is perfectly valid syntax.',
            'A default for `a` would not help, as the pattern fails first.',
          ],
          explanation:
            'With no argument the parameter is `undefined`, and destructuring reads properties from it — which throws. `function f({ a } = {})` gives the parameter a default object so the pattern always has something to read from.',
        },
        {
          id: 'q-m14-06-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['destructuring'],
          prompt: 'Spreading two objects merges their nested objects together.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'The merge is shallow.',
            'Correct — a later nested object replaces the earlier one entirely.',
          ],
          explanation:
            'Spread is shallow, so a nested object in a later source replaces the earlier one wholesale rather than merging into it — silently losing any keys the later one lacks. Merge each nested level explicitly, or use a dedicated deep-merge helper.',
        },
      ],
    },
    summary:
      'Object destructuring matches by **property name**, so order is irrelevant. `{ id: userId }` renames — property on the left, new variable on the right, the mirror image of an object literal. Defaults apply only when the value is `undefined`, so `null` and `0` are preserved. Nested patterns mirror the data\'s shape but **throw** when an intermediate level is missing, which a `= {}` default at that level fixes. Destructured parameters give effectively named arguments, and need their own `= {}` for the function to be callable with no argument at all. Spread merges left to right so **later properties win** — putting defaults last silently discards user choices — and the merge is shallow, so a nested object replaces rather than merges. Rest collects the remaining own properties into a new object, which is the modern, non-mutating replacement for `delete`; it must come last and there can be only one.',
    keyTakeaways: [
      'Objects destructure by name; arrays by position',
      '`{ id: userId }` renames — property left, variable right',
      'Defaults apply only to `undefined`, not `null` or `0`',
      'Nested destructuring throws on a missing level — default it with `= {}`',
      'In a spread merge, later properties win, and the merge is shallow',
      '`const { x, ...rest } = obj` is the non-mutating way to omit a property',
    ],
    relatedLessons: ['l-m14-05', 'l-m12-04'],
    interviewConnections: [
      'How do you rename a variable while destructuring?',
      'When do destructuring defaults apply?',
      'How do you remove a property without mutating the object?',
      'What happens when you spread two objects with the same key?',
    ],
  },
];
