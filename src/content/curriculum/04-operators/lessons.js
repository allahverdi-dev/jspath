import { SECTION, CALLOUT_TONE, DIFFICULTY, EXERCISE_KIND, QUIZ_KIND } from '../../schema/types.js';

const M = 'm04';

export default [
  /* ================================================================== */
  {
    id: 'l-m04-01',
    slug: 'arithmetic-operators',
    moduleId: M,
    order: 1,
    title: 'Arithmetic Operators',
    description: 'The six arithmetic operators, the remainder operator that is not quite modulo, and the exponentiation quirk that is a syntax error.',
    difficulty: DIFFICULTY.BEGINNER,
    estimatedMinutes: 14,
    xp: 25,
    topicIds: ['operators', 'numbers'],
    prerequisites: ['l-m03-06'],
    learningObjectives: [
      'Use all six arithmetic operators correctly',
      'Explain why `%` is a remainder rather than a true modulo',
      'Use `**` and explain its right-associativity',
      'Apply the common `%` patterns for even/odd, wrapping and digit extraction',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'JavaScript has six arithmetic operators. Four of them behave exactly as you would expect from school mathematics; two have behaviour worth learning deliberately.',
        ],
      },
      {
        kind: SECTION.TABLE,
        headers: ['Operator', 'Name', 'Example', 'Result'],
        rows: [
          ['`+`', 'Addition', '`7 + 3`', '`10`'],
          ['`-`', 'Subtraction', '`7 - 3`', '`4`'],
          ['`*`', 'Multiplication', '`7 * 3`', '`21`'],
          ['`/`', 'Division', '`7 / 3`', '`2.333…`'],
          ['`%`', 'Remainder', '`7 % 3`', '`1`'],
          ['`**`', 'Exponentiation', '`7 ** 3`', '`343`'],
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Division always produces a decimal',
        body: [
          'There is one `number` type, so `/` never truncates. `7 / 2` is `3.5`, not `3`. If you want integer division, divide and then truncate: `Math.trunc(7 / 2)` gives `3`, and `Math.floor` gives the same for positive numbers.',
          'Be careful with negatives: `Math.floor(-7 / 2)` is `-4` while `Math.trunc(-7 / 2)` is `-3`. `Math.trunc` simply removes the fractional part; `Math.floor` rounds towards negative infinity.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The remainder operator',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`%` gives what is left after dividing. `7 % 3` is `1`, because 3 goes into 7 twice with 1 left over.',
          'It is very commonly called the "modulo" operator, and for positive numbers that is harmless. But it is genuinely a **remainder** operator, and the two differ when the left operand is negative.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The result takes the sign of the dividend, not the divisor.',
        code: [
          'console.log(7 % 3);     // 1',
          'console.log(-7 % 3);    // -1   ← a true modulo would give 2',
          'console.log(7 % -3);    // 1',
          'console.log(-7 % -3);   // -1',
          '',
          'console.log(5.5 % 2);   // 1.5  — works with decimals too',
        ].join('\n'),
        output: ['1', '-1', '1', '-1', '1.5'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Negative remainders break wrapping logic',
        body: [
          'A common pattern is wrapping an index around an array: `index % length`. It works perfectly while the index is positive, and breaks the moment it goes negative — `-1 % 5` is `-1`, not `4`, so you get an out-of-range index.',
          'The fix is a small helper: `((n % m) + m) % m`. The first `% m` brings the value into range, adding `m` makes it positive, and the second `% m` handles the case where it was already positive.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A true modulo that handles negatives.',
        code: [
          'function mod(n, m) {',
          '  return ((n % m) + m) % m;',
          '}',
          '',
          'console.log(-1 % 5);       // -1  — remainder',
          'console.log(mod(-1, 5));   // 4   — modulo',
          'console.log(mod(7, 5));    // 2   — unchanged for positives',
          'console.log(mod(-7, 5));   // 3',
        ].join('\n'),
        output: ['-1', '4', '2', '3'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Three things `%` is genuinely useful for',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Patterns you will use repeatedly.',
        code: [
          '// 1. Even or odd',
          'console.log(10 % 2 === 0);   // true  — even',
          'console.log(7 % 2 === 0);    // false — odd',
          '',
          '// 2. Every nth item',
          'for (let i = 1; i <= 10; i++) {',
          '  if (i % 3 === 0) console.log("divisible by 3:", i);',
          '}',
          '',
          '// 3. Splitting a total into units',
          'const totalSeconds = 3725;',
          'const hours = Math.floor(totalSeconds / 3600);',
          'const minutes = Math.floor((totalSeconds % 3600) / 60);',
          'const seconds = totalSeconds % 60;',
          'console.log(hours + "h " + minutes + "m " + seconds + "s");',
        ].join('\n'),
        output: [
          'true', 'false',
          'divisible by 3: 3', 'divisible by 3: 6', 'divisible by 3: 9',
          '1h 2m 5s',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Exponentiation',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`**` raises the left operand to the power of the right. It replaced `Math.pow` in ES2016 and reads far better in expressions.',
          'It has one property that differs from every other arithmetic operator: it is **right-associative**. `2 ** 3 ** 2` groups as `2 ** (3 ** 2)`, which is `2 ** 9`, which is `512` — not `(2 ** 3) ** 2`, which would be `64`. This matches mathematical convention.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Right-associativity, and the syntax error nobody expects.',
        code: [
          'console.log(2 ** 3);        // 8',
          'console.log(2 ** 3 ** 2);   // 512 — right-associative',
          'console.log((2 ** 3) ** 2); // 64  — grouped explicitly',
          '',
          'console.log(9 ** 0.5);      // 3   — a square root',
          'console.log(2 ** -1);       // 0.5 — a negative exponent',
          '',
          '// -2 ** 2 is a SyntaxError. It must be written explicitly:',
          'console.log((-2) ** 2);     // 4',
          'console.log(-(2 ** 2));     // -4',
        ].join('\n'),
        output: ['8', '512', '64', '3', '0.5', '4', '-4'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: '`-2 ** 2` is deliberately a SyntaxError',
        body: [
          'Most languages resolve this silently, and the two possible readings — `(-2) ** 2 = 4` and `-(2 ** 2) = -4` — give different answers. Rather than pick one and let people guess wrong, JavaScript refuses to parse it and requires you to add brackets.',
          'This is one of the few places the language chose an error over an arbitrary rule, and it is worth appreciating: an unambiguous SyntaxError is far better than a silently wrong number.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Unary plus and minus',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Unary `+` is a numeric conversion.',
        code: [
          'console.log(-5);        // -5  — unary minus negates',
          'console.log(-(-5));     // 5',
          '',
          'console.log(+"42");     // 42  — unary plus converts to number',
          'console.log(+true);     // 1',
          'console.log(+"");       // 0',
          'console.log(+"abc");    // NaN',
          '',
          'console.log(typeof +"42");  // number',
        ].join('\n'),
        output: ['-5', '5', '42', '1', '0', 'NaN', 'number'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Unary `+` performs exactly the same conversion as `Number()`, following the same table from the coercion module. It is concise, and you will certainly read it, but `Number(value)` states the intent more clearly and is generally preferred in code you write.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'console.log(10 % 4, -10 % 4, 2 ** 3 ** 2);',
        ].join('\n'),
        options: [
          '2 2 512',
          '2 -2 512',
          '2 -2 64',
          '2 2 64',
        ],
        correct: 1,
        explanation:
          '`10 % 4` is `2`, since 4 goes into 10 twice with 2 remaining. `-10 % 4` is `-2`, because `%` is a remainder operator and the result takes the sign of the **left** operand — a true modulo would give `2`. And `**` is right-associative, so `2 ** 3 ** 2` groups as `2 ** (3 ** 2)` = `2 ** 9` = `512`, not `64`.',
      },
    ],
    exercises: [
      {
        id: 'ex-m04-01-a',
        title: 'Predict the negative remainder',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['operators'],
        instructions: 'What does this print?',
        code: ['console.log(-7 % 3);'].join('\n'),
        options: ['2', '-1', '1', '-2'],
        correct: 1,
        hints: [
          '`%` is a remainder operator, not a true modulo.',
          'Which operand determines the sign of the result?',
        ],
        solution: '-1',
        solutionExplanation:
          'The result of `%` takes the sign of the **dividend** — the left operand. Since `-7` is negative, the result is negative: `-7 % 3` is `-1`. A true modulo operation, as found in Python, would give `2`. This distinction breaks array-wrapping code such as `index % length` the moment the index goes negative; use `((n % m) + m) % m` when you need genuine modulo behaviour.',
      },
      {
        id: 'ex-m04-01-b',
        title: 'Format a duration',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['operators', 'numbers'],
        instructions:
          'Write `formatDuration(totalSeconds)` that converts a number of seconds into a string of the form `"1:02:05"` — hours, minutes and seconds, with minutes and seconds always two digits. For durations under an hour, omit the hours entirely and return `"2:05"` style. Assume the input is a non-negative whole number.',
        starterCode: [
          'function formatDuration(totalSeconds) {',
          '  // Use Math.floor for whole units and % for the remainder.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'formats an hour-long duration', body: 'expect(formatDuration(3725)).toBe("1:02:05");' },
          { name: 'omits hours when under an hour', body: 'expect(formatDuration(125)).toBe("2:05");' },
          { name: 'handles zero', body: 'expect(formatDuration(0)).toBe("0:00");' },
          { name: 'pads seconds below ten', body: 'expect(formatDuration(65)).toBe("1:05");' },
          { name: 'handles exactly one hour', body: 'expect(formatDuration(3600)).toBe("1:00:00");' },
          { name: 'handles multiple hours', body: 'expect(formatDuration(7322)).toBe("2:02:02");' },
          { name: 'handles 59 seconds', body: 'expect(formatDuration(59)).toBe("0:59");', hidden: true },
        ],
        hints: [
          'Hours: `Math.floor(totalSeconds / 3600)`.',
          'Minutes: take the remainder after hours with `% 3600`, then divide by 60 and floor it.',
          'Seconds: `totalSeconds % 60`.',
          '`String(n).padStart(2, "0")` adds a leading zero when needed.',
        ],
        solution: [
          'function formatDuration(totalSeconds) {',
          '  const hours = Math.floor(totalSeconds / 3600);',
          '  const minutes = Math.floor((totalSeconds % 3600) / 60);',
          '  const seconds = totalSeconds % 60;',
          '',
          '  const paddedSeconds = String(seconds).padStart(2, "0");',
          '',
          '  if (hours > 0) {',
          '    return hours + ":" + String(minutes).padStart(2, "0") + ":" + paddedSeconds;',
          '  }',
          '',
          '  return minutes + ":" + paddedSeconds;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'This is the canonical use of `%` alongside division: divide to get whole units, take the remainder to get what is left for the next unit down. The `% 3600` before dividing by 60 is the key step — without it, `Math.floor(3725 / 60)` would give 62 minutes rather than 2. Minutes are padded only when hours are shown, because `"2:05"` is the natural short form while `"1:02:05"` needs the padding for alignment. The seconds are always padded, since `"1:5"` would be misread.',
      },
      {
        id: 'ex-m04-01-c',
        title: 'Implement a true modulo',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 20,
        topicIds: ['operators'],
        instructions:
          'Write `wrapIndex(index, length)` that returns a valid array index for any integer input, wrapping around in both directions. `wrapIndex(-1, 5)` should give `4`, and `wrapIndex(7, 5)` should give `2`.',
        starterCode: [
          'function wrapIndex(index, length) {',
          '  // Plain % returns negatives for negative input. Correct for that.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'wraps a negative index to the end', body: 'expect(wrapIndex(-1, 5)).toBe(4);' },
          { name: 'wraps an index past the end', body: 'expect(wrapIndex(7, 5)).toBe(2);' },
          { name: 'leaves an in-range index alone', body: 'expect(wrapIndex(3, 5)).toBe(3);' },
          { name: 'handles zero', body: 'expect(wrapIndex(0, 5)).toBe(0);' },
          { name: 'handles a large negative index', body: 'expect(wrapIndex(-7, 5)).toBe(3);' },
          { name: 'handles exactly the length', body: 'expect(wrapIndex(5, 5)).toBe(0);' },
          { name: 'always returns a valid index', body: 'for (let i = -20; i <= 20; i++) { const r = wrapIndex(i, 4); expect(r >= 0 && r < 4).toBe(true); }', hidden: true },
        ],
        hints: [
          '`index % length` is correct for positives but negative for negative input.',
          'Adding `length` to a negative remainder brings it into range.',
          'A second `% length` handles the case where it was already positive and adding pushed it out of range.',
        ],
        solution: [
          'function wrapIndex(index, length) {',
          '  return ((index % length) + length) % length;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Each step earns its place. The first `% length` reduces the magnitude into range, but leaves the sign of `index`. Adding `length` makes any negative result positive — `-1` becomes `4`. The second `% length` is needed because a value that was already in range, say `3`, would have become `8` after the addition; the final remainder brings it back to `3`. This three-step idiom is the standard way to get true modulo behaviour and appears in carousels, circular buffers and any wrap-around navigation. The hidden test checks the whole range at once, which is a good habit for functions with a mathematical invariant.',
      },
    ],
    quiz: {
      id: 'qz-m04-01',
      questions: [
        {
          id: 'q-m04-01-1',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['operators'],
          prompt: 'What is the result of this exponentiation chain?',
          code: ['console.log(2 ** 3 ** 2);'].join('\n'),
          options: ['64', '512', '12', '36'],
          correct: 1,
          optionExplanations: [
            'That would be `(2 ** 3) ** 2`, which requires explicit brackets.',
            'Correct — `**` is right-associative, so it groups as `2 ** (3 ** 2)`.',
            'Exponentiation is not multiplication.',
            'This is not how the grouping works.',
          ],
          explanation:
            '`**` is the only right-associative arithmetic operator, matching mathematical convention. It groups as `2 ** (3 ** 2)` = `2 ** 9` = `512`. Every other arithmetic operator is left-associative.',
        },
        {
          id: 'q-m04-01-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['operators'],
          prompt: 'Why is `%` described as a remainder rather than a modulo operator?',
          options: [
            'Because it only works with integers',
            'Because the result takes the sign of the left operand, so `-7 % 3` is `-1` rather than `2`',
            'Because it rounds the result',
            'Because it returns a string',
          ],
          correct: 1,
          optionExplanations: [
            'It works with decimals too — `5.5 % 2` is `1.5`.',
            'Correct — a true modulo always returns a non-negative result for a positive divisor.',
            'No rounding is involved.',
            'It returns a number.',
          ],
          explanation:
            'A remainder takes the sign of the dividend; a true modulo takes the sign of the divisor. `-7 % 3` is `-1` in JavaScript but `2` in Python. Use `((n % m) + m) % m` when you need genuine modulo behaviour, such as wrapping an array index.',
        },
        {
          id: 'q-m04-01-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['operators'],
          prompt: 'Why does `-2 ** 2` produce a SyntaxError?',
          options: [
            'Because `**` cannot be used with negative numbers',
            'Because the grouping is ambiguous, so the language requires explicit brackets rather than guessing',
            'Because unary minus is not a real operator',
            'Because the result would be NaN',
          ],
          correct: 1,
          optionExplanations: [
            '`(-2) ** 2` works fine.',
            'Correct — `(-2) ** 2` is 4 while `-(2 ** 2)` is -4, and the language refuses to pick.',
            'Unary minus is a perfectly ordinary operator.',
            'Both readings produce a valid number.',
          ],
          explanation:
            'The two possible groupings give different answers — `4` and `-4` — so rather than choosing arbitrarily and letting developers guess wrong, JavaScript makes it a parse error and requires brackets. An unambiguous error is far better than a silently wrong number.',
        },
      ],
    },
    summary:
      'JavaScript has six arithmetic operators. Division never truncates, since there is a single number type — use `Math.trunc` or `Math.floor` for integer division, noting that they differ for negatives. `%` is a **remainder** operator whose result takes the sign of the left operand, so `-7 % 3` is `-1`; use `((n % m) + m) % m` when you need true modulo behaviour for wrapping. `**` is the only right-associative arithmetic operator, so `2 ** 3 ** 2` is `512`, and `-2 ** 2` is a deliberate SyntaxError because the grouping would otherwise be ambiguous. Unary `+` performs the same conversion as `Number()`, though the named function states the intent more clearly.',
    keyTakeaways: [
      'Division always produces a decimal; use `Math.trunc` or `Math.floor` for integers',
      '`%` is a remainder — the sign follows the left operand',
      'Use `((n % m) + m) % m` for true modulo when wrapping indexes',
      '`**` is right-associative: `2 ** 3 ** 2` is 512',
      '`-2 ** 2` is a SyntaxError; write `(-2) ** 2` or `-(2 ** 2)`',
      'Unary `+` converts to number, exactly like `Number()`',
    ],
    relatedLessons: ['l-m03-04', 'l-m04-02'],
    interviewConnections: [
      'What is the difference between remainder and modulo?',
      'What does 2 ** 3 ** 2 evaluate to and why?',
      'How would you check whether a number is even?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m04-02',
    slug: 'assignment-and-increment',
    moduleId: M,
    order: 2,
    title: 'Assignment, Compound Assignment and Increment',
    description: 'Every assignment operator, and the prefix-versus-postfix distinction that shows up in every interview.',
    difficulty: DIFFICULTY.BEGINNER,
    estimatedMinutes: 13,
    xp: 25,
    topicIds: ['operators', 'variables'],
    prerequisites: ['l-m04-01'],
    learningObjectives: [
      'Use every compound assignment operator',
      'Explain the difference between `i++` and `++i`',
      'Explain that assignment is an expression with a value',
      'Choose the clearer form when both would work',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'You met `=` in the variables module. Every arithmetic operator has a **compound assignment** form that reads the current value, applies the operation, and stores the result back.',
        ],
      },
      {
        kind: SECTION.TABLE,
        headers: ['Compound form', 'Equivalent to'],
        rows: [
          ['`x += 5`', '`x = x + 5`'],
          ['`x -= 5`', '`x = x - 5`'],
          ['`x *= 5`', '`x = x * 5`'],
          ['`x /= 5`', '`x = x / 5`'],
          ['`x %= 5`', '`x = x % 5`'],
          ['`x **= 5`', '`x = x ** 5`'],
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Compound assignment in practice.',
        code: [
          'let total = 100;',
          '',
          'total += 50;   console.log(total);   // 150',
          'total -= 30;   console.log(total);   // 120',
          'total *= 2;    console.log(total);   // 240',
          'total /= 4;    console.log(total);   // 60',
          'total %= 7;    console.log(total);   // 4',
          'total **= 2;   console.log(total);   // 16',
          '',
          '// += also concatenates, because + is overloaded',
          'let message = "Hello";',
          'message += ", world";',
          'console.log(message);',
        ].join('\n'),
        output: ['150', '120', '240', '60', '4', '16', 'Hello, world'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: '`+=` inherits the overloaded plus',
        body: [
          'Since `x += y` means `x = x + y`, it concatenates whenever either side is a string. Starting an accumulator at `""` instead of `0` turns a sum into a concatenation: `let total = ""; total += 5; total += 3;` gives `"53"`, not `8`.',
          'Always initialise a numeric accumulator to `0`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Increment and decrement',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`++` adds exactly 1 and `--` subtracts exactly 1. Each has two forms, and the difference between them is not about what happens to the variable — it is about **what the expression evaluates to**.',
        ],
      },
      {
        kind: SECTION.STEPS,
        steps: [
          {
            title: 'Postfix — `i++`',
            body: 'Returns the value **before** incrementing, then increments. "Give me the old value, then add one."',
          },
          {
            title: 'Prefix — `++i`',
            body: 'Increments first, then returns the **new** value. "Add one, then give me the result."',
          },
          {
            title: 'When used alone, they are identical',
            body: 'On a line by itself, `i++;` and `++i;` do exactly the same thing — the returned value is discarded either way. The difference only matters when the expression\'s value is used.',
          },
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The variable ends up the same; the expression value differs.',
        code: [
          'let a = 5;',
          'console.log(a++);   // 5 — returns the old value',
          'console.log(a);     // 6 — but a was incremented',
          '',
          'let b = 5;',
          'console.log(++b);   // 6 — increments first, returns the new value',
          'console.log(b);     // 6',
          '',
          '// Used as a statement, they are interchangeable',
          'let c = 0;',
          'c++;',
          '++c;',
          'console.log(c);     // 2',
        ].join('\n'),
        output: ['5', '6', '6', '6', '2'].join('\n'),
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The classic interview trace.',
        code: [
          'let i = 0;',
          'const results = [];',
          '',
          'results.push(i++);   // pushes 0, i becomes 1',
          'results.push(++i);   // i becomes 2, pushes 2',
          'results.push(i--);   // pushes 2, i becomes 1',
          'results.push(--i);   // i becomes 0, pushes 0',
          '',
          'console.log(results);',
          'console.log("final i:", i);',
        ].join('\n'),
        output: ['[0, 2, 2, 0]', 'final i: 0'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Write it plainly instead',
        body: [
          'Code that depends on the difference between `i++` and `++i` is code that requires careful reading. In a `for` loop header the choice is irrelevant, so `i++` is conventional and fine. Anywhere else, splitting the operation onto its own line is almost always clearer than embedding it in a larger expression.',
          'Knowing the rule matters because you will be asked about it and you will read it. Relying on it in code you write is a different question.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Assignment is an expression',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Assignment produces a value — the value that was assigned. This is why chained assignment works, and why an accidental `=` inside an `if` condition is such a dangerous typo.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Assignment evaluates to the assigned value.',
        code: [
          'let x;',
          'console.log(x = 5);      // 5 — the assignment itself has a value',
          '',
          'let a, b, c;',
          'a = b = c = 10;          // chained, right to left',
          'console.log(a, b, c);',
          '',
          '// Why `if (x = 5)` is dangerous:',
          'let value = 0;',
          'if (value = 5) {',
          '  console.log("this runs — the assignment produced 5, which is truthy");',
          '}',
          'console.log("value is now:", value);',
        ].join('\n'),
        output: ['5', '10 10 10', 'this runs — the assignment produced 5, which is truthy', 'value is now: 5'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'A single `=` in a condition',
        body: [
          'Writing `if (value = 5)` instead of `if (value === 5)` assigns 5 to `value` and then tests whether 5 is truthy — which it is. The branch always runs, and the variable has been silently overwritten.',
          'This is why linters flag assignment inside a condition, and why `===` being three characters longer is a price worth paying. If you genuinely want an assignment in a condition, wrap it in extra brackets — `if ((match = regex.exec(text)))` — which signals to both readers and linters that it was deliberate.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'let n = 3;',
          'const out = n++ + ++n;',
          '',
          'console.log(out, n);',
        ].join('\n'),
        options: ['7 5', '8 5', '6 5', '7 4'],
        correct: 0,
        explanation:
          'Work left to right. `n++` returns the current value `3` and then makes `n` equal to `4`. Next, `++n` increments `n` to `5` and returns `5`. So the sum is `3 + 5 = 7`, and `n` finishes at `5`. This is precisely the kind of expression you should never write in real code — but it is a common interview question, and tracing it carefully proves you understand the two forms.',
      },
    ],
    exercises: [
      {
        id: 'ex-m04-02-a',
        title: 'Predict postfix increment',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['operators'],
        instructions: 'What does this print?',
        code: [
          'let x = 5;',
          'console.log(x++);',
        ].join('\n'),
        options: ['5', '6', 'undefined', '4'],
        correct: 0,
        hints: [
          'Postfix returns the value before the increment happens.',
        ],
        solution: '5',
        solutionExplanation:
          'Postfix `x++` returns the value the variable held *before* incrementing, so `console.log` receives `5`. The increment still happens — `x` is `6` immediately afterwards — but the expression evaluated to the old value. The prefix form `++x` would increment first and print `6`.',
      },
      {
        id: 'ex-m04-02-b',
        title: 'Fix the accumulator type bug',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['operators', 'coercion'],
        instructions:
          'This function should add up the numbers in an array, but it returns a string of digits instead. Fix it so `sumAll([1, 2, 3])` returns the number `6`.',
        starterCode: [
          'function sumAll(numbers) {',
          '  let total = "";',
          '  for (let i = 0; i < numbers.length; i++) {',
          '    total += numbers[i];',
          '  }',
          '  return total;',
          '}',
        ].join('\n'),
        tests: [
          { name: 'sums three numbers', body: 'expect(sumAll([1, 2, 3])).toBe(6);' },
          { name: 'returns a number, not a string', body: 'expect(typeof sumAll([1, 2, 3])).toBe("number");' },
          { name: 'returns 0 for an empty array', body: 'expect(sumAll([])).toBe(0);' },
          { name: 'handles negatives', body: 'expect(sumAll([5, -3])).toBe(2);' },
          { name: 'handles decimals', body: 'expect(sumAll([0.5, 0.25])).toBe(0.75);' },
          { name: 'handles a single element', body: 'expect(sumAll([42])).toBe(42);', hidden: true },
        ],
        hints: [
          'What is `"" + 1`?',
          '`+=` concatenates whenever either side is a string.',
          'The accumulator must start as a number.',
        ],
        solution: [
          'function sumAll(numbers) {',
          '  let total = 0;',
          '  for (let i = 0; i < numbers.length; i++) {',
          '    total += numbers[i];',
          '  }',
          '  return total;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'One character fixes it. Because `total += n` expands to `total = total + n`, and `+` concatenates whenever either operand is a string, starting at `""` made every step a concatenation: `"" + 1` gives `"1"`, then `"1" + 2` gives `"12"`, and so on. Starting at `0` keeps both operands numeric so `+` performs addition. Note that the empty-array test also passes correctly, because `0` is the right identity value for a sum — a detail that matters when the loop body never runs.',
      },
      {
        id: 'ex-m04-02-c',
        title: 'Trace prefix and postfix',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['operators'],
        instructions: 'What is the final value of `results`?',
        code: [
          'let i = 1;',
          'const results = [i++, ++i, i--, --i];',
          'console.log(results.join(","));',
        ].join('\n'),
        options: ['1,3,3,1', '1,2,3,2', '2,3,2,1', '1,3,2,1'],
        correct: 0,
        hints: [
          'Evaluate the array elements strictly left to right.',
          'Track the value of `i` after each one.',
        ],
        solution: '1,3,3,1',
        solutionExplanation:
          'Trace each element in order. `i++` returns 1 and leaves `i` at 2. `++i` raises `i` to 3 and returns 3. `i--` returns 3 and leaves `i` at 2. `--i` lowers `i` to 1 and returns 1. So the array is `[1, 3, 3, 1]` and `i` ends at 1. The pattern to remember: the postfix form gives you the value *before* the change, the prefix form gives you the value *after*.',
      },
    ],
    quiz: {
      id: 'qz-m04-02',
      questions: [
        {
          id: 'q-m04-02-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['operators'],
          prompt: 'What is the difference between `i++` and `++i`?',
          options: [
            '`i++` adds one and `++i` subtracts one',
            'Both increment `i`, but `i++` evaluates to the old value while `++i` evaluates to the new value',
            '`++i` is faster',
            '`i++` only works inside loops',
          ],
          correct: 1,
          optionExplanations: [
            'Both add one; `--` is the operator that subtracts.',
            'Correct — the variable ends up the same, only the expression value differs.',
            'Any difference is negligible and not the point.',
            'Both work anywhere a variable can be incremented.',
          ],
          explanation:
            'Both leave `i` one higher. The difference is what the expression itself evaluates to: postfix returns the value before the increment, prefix returns the value after. When used as a standalone statement they are interchangeable.',
        },
        {
          id: 'q-m04-02-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['operators', 'coercion'],
          prompt: 'What is the result of this accumulation?',
          code: [
            'let total = "";',
            'total += 5;',
            'total += 3;',
            'console.log(total);',
          ].join('\n'),
          options: ['8', '"53"', '"8"', 'NaN'],
          correct: 1,
          optionExplanations: [
            'That would require the accumulator to start as a number.',
            'Correct — `+=` concatenates because the accumulator is a string.',
            'The value is a string, but its contents are the digits joined, not the sum.',
            'Concatenation succeeds, so nothing produces NaN.',
          ],
          explanation:
            '`total += n` expands to `total = total + n`, and `+` concatenates whenever either operand is a string. Starting at `""` makes every step a concatenation: `"" + 5` gives `"5"`, then `"5" + 3` gives `"53"`. Always initialise a numeric accumulator to `0`.',
        },
        {
          id: 'q-m04-02-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['operators'],
          prompt: 'Why does `if (x = 5) { ... }` always execute its block?',
          options: [
            'Because `=` compares values and 5 equals 5',
            'Because the assignment evaluates to 5, and 5 is truthy',
            'Because `if` always runs its first branch',
            'Because assignment inside a condition is a SyntaxError that is silently ignored',
          ],
          correct: 1,
          optionExplanations: [
            '`=` assigns rather than compares; comparison is `===`.',
            'Correct — assignment is an expression whose value is what was assigned.',
            '`if` evaluates its condition normally.',
            'It parses perfectly well, which is exactly what makes it dangerous.',
          ],
          explanation:
            'Assignment is an expression that evaluates to the assigned value. `x = 5` produces `5`, which is truthy, so the branch always runs — and `x` has been silently overwritten. This is a classic typo for `===`, and linters flag it for good reason.',
        },
      ],
    },
    summary:
      'Every arithmetic operator has a compound assignment form that reads, operates and stores back in one step. `+=` inherits the overloaded plus, so a numeric accumulator must start at `0` rather than `""` or every step concatenates. `++` and `--` change the variable by exactly one; the postfix form evaluates to the value *before* the change and the prefix form to the value *after*, though as standalone statements they are interchangeable. Assignment is itself an expression whose value is what was assigned, which enables chained assignment and explains why `if (x = 5)` always runs — a typo for `===` that linters rightly flag.',
    keyTakeaways: [
      '`x += y` is `x = x + y`, and inherits the string behaviour of `+`',
      'Initialise numeric accumulators to `0`, never `""`',
      '`i++` returns the old value; `++i` returns the new one',
      'As standalone statements they are identical — the difference is the expression value',
      'Assignment is an expression, which is why `if (x = 5)` always runs',
    ],
    relatedLessons: ['l-m04-01', 'l-m04-03'],
    interviewConnections: [
      'What is the difference between i++ and ++i?',
      'What does `let n = 3; n++ + ++n` evaluate to?',
      'Why is `if (x = 5)` a bug?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m04-03',
    slug: 'comparison-operators',
    moduleId: M,
    order: 3,
    title: 'Comparison Operators',
    description: 'Relational and equality comparison, and why comparing strings does not work the way you would expect.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 14,
    xp: 25,
    topicIds: ['operators', 'coercion'],
    prerequisites: ['l-m04-02'],
    learningObjectives: [
      'Use all six comparison operators correctly',
      'Explain how strings are compared, and why `"10" < "9"`',
      'Explain why chained comparisons such as `1 < 2 < 3` are misleading',
      'Sort and compare mixed data safely',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Comparison operators produce a boolean. There are four relational operators and two equality operators — plus their negations.',
        ],
      },
      {
        kind: SECTION.TABLE,
        headers: ['Operator', 'Meaning', 'Example'],
        rows: [
          ['`>`', 'Greater than', '`5 > 3` → `true`'],
          ['`<`', 'Less than', '`5 < 3` → `false`'],
          ['`>=`', 'Greater than or equal', '`5 >= 5` → `true`'],
          ['`<=`', 'Less than or equal', '`5 <= 4` → `false`'],
          ['`===`', 'Strictly equal', '`5 === 5` → `true`'],
          ['`!==`', 'Strictly not equal', '`5 !== "5"` → `true`'],
          ['`==` / `!=`', 'Loosely (un)equal', 'Avoid — see the coercion module'],
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Comparing strings',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Relational operators work on strings, comparing them character by character using each character\'s UTF-16 code unit — essentially dictionary order, but based on character codes rather than human alphabetical rules.',
          'This produces three results that surprise people.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Character-code ordering, not human ordering.',
        code: [
          'console.log("apple" < "banana");   // true  — as expected',
          'console.log("a" < "b");            // true',
          '',
          '// 1. Uppercase sorts before lowercase',
          'console.log("Z" < "a");            // true  — Z is 90, a is 97',
          'console.log("apple" < "Banana");   // false — B is 66, a is 97',
          '',
          '// 2. Numeric strings compare character by character',
          'console.log("10" < "9");           // true  — "1" is less than "9"',
          'console.log(10 < 9);               // false — actual numbers',
          '',
          '// 3. A shorter string that is a prefix comes first',
          'console.log("app" < "apple");      // true',
        ].join('\n'),
        output: ['true', 'true', 'true', 'false', 'true', 'false', 'true'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'The `"10" < "9"` bug in sorting',
        body: [
          'This is exactly why `[10, 9, 1].sort()` gives `[1, 10, 9]`. The default `sort` converts every element to a string and compares those strings, so `"10"` sorts before `"9"`.',
          'For numbers you must supply a comparator: `arr.sort((a, b) => a - b)`. This is one of the most frequently encountered surprises in the language, and it comes directly from string comparison rules.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Sorting correctly.',
        code: [
          'const numbers = [10, 9, 1, 100];',
          '',
          'console.log([...numbers].sort());              // string order',
          'console.log([...numbers].sort((a, b) => a - b)); // numeric order',
          '',
          'const names = ["banana", "Apple", "cherry"];',
          'console.log([...names].sort());                // uppercase first',
          'console.log([...names].sort((a, b) => a.localeCompare(b)));',
        ].join('\n'),
        output: [
          '[1, 10, 100, 9]',
          '[1, 9, 10, 100]',
          '["Apple", "banana", "cherry"]',
          '["Apple", "banana", "cherry"]',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`localeCompare` is the right tool for human-facing text. It handles case sensitivity and accented characters according to language rules rather than raw code units, which matters as soon as your users are not all writing plain ASCII English.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Mixed types in relational comparisons',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'If both operands are strings, they are compared as strings. Otherwise **both are converted to numbers** — which is a different rule from loose equality, and the source of the `null >= 0` oddity you met earlier.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'One string operand is not enough to force string comparison.',
        code: [
          'console.log("10" > 9);      // true  — "10" becomes 10',
          'console.log("10" > "9");    // false — both strings, character order',
          '',
          'console.log(true > 0);      // true  — true becomes 1',
          'console.log(null >= 0);     // true  — null becomes 0',
          'console.log(null > 0);      // false — 0 > 0 is false',
          'console.log(null == 0);     // false — equality has a special rule',
          '',
          'console.log(undefined > 0); // false — undefined becomes NaN',
          'console.log(NaN > 0);       // false — every NaN comparison is false',
          'console.log(NaN < 0);       // false',
        ].join('\n'),
        output: ['true', 'false', 'true', 'true', 'false', 'false', 'false', 'false', 'false'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Every comparison with `NaN` is false',
        body: [
          '`NaN > 0`, `NaN < 0` and `NaN === 0` are all `false` — and so is `NaN === NaN`. This is the only value for which both `a > b` and `a <= b` are false at the same time.',
          'The practical consequence: a sort comparator that receives `NaN` produces an unpredictable order, and a range check such as `if (x >= 0 && x <= 100)` silently rejects `NaN` rather than flagging it. Validate with `Number.isFinite` before comparing.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Chained comparisons do not work',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'This parses, runs, and means something entirely different from what it looks like.',
        code: [
          'console.log(1 < 2 < 3);   // true  — but not for the reason you think',
          'console.log(3 > 2 > 1);   // false — same trap',
          '',
          '// 1 < 2 < 3  →  (1 < 2) < 3  →  true < 3  →  1 < 3  →  true',
          '// 3 > 2 > 1  →  (3 > 2) > 1  →  true > 1  →  1 > 1  →  false',
          '',
          '// Write it explicitly instead:',
          'const x = 2;',
          'console.log(x > 1 && x < 3);   // true',
        ].join('\n'),
        output: ['true', 'false', 'true'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Relational operators are left-associative and each produces a boolean, so a chain compares that boolean against the next operand. `1 < 2 < 3` happens to give the "right" answer by coincidence; `3 > 2 > 1` gives the wrong one, which reveals the trap.',
          'Always write range checks with `&&`.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'console.log("10" > 9, "10" > "9", 3 > 2 > 1);',
        ].join('\n'),
        options: [
          'true false false',
          'true true true',
          'false false false',
          'true false true',
        ],
        correct: 0,
        explanation:
          '`"10" > 9` mixes a string and a number, so both convert to numbers: `10 > 9` is `true`. `"10" > "9"` has two strings, so they compare character by character: `"1"` has a lower code unit than `"9"`, so the result is `false`. And `3 > 2 > 1` evaluates left to right as `(3 > 2) > 1`, which is `true > 1`, which converts to `1 > 1`, which is `false`. Three different mechanisms in one line.',
      },
    ],
    exercises: [
      {
        id: 'ex-m04-03-a',
        title: 'Predict the string comparison',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['operators'],
        instructions: 'What does this print?',
        code: ['console.log("10" < "9");'].join('\n'),
        options: ['true', 'false', 'NaN', 'TypeError'],
        correct: 0,
        hints: [
          'Both operands are strings, so no numeric conversion happens.',
          'Compare the first character of each.',
        ],
        solution: 'true',
        solutionExplanation:
          'When both operands are strings they are compared character by character using UTF-16 code units. The first characters are `"1"` (code 49) and `"9"` (code 57), and 49 is less than 57, so the comparison is `true` and no further characters are examined. This is exactly why `[10, 9].sort()` gives `[10, 9]` — the default sort compares stringified values.',
      },
      {
        id: 'ex-m04-03-b',
        title: 'Fix the sort',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['operators', 'arrays'],
        instructions:
          'This function should return the numbers sorted from smallest to largest, without modifying the original array. It currently sorts them as strings. Fix it.',
        starterCode: [
          'function sortNumbers(numbers) {',
          '  return [...numbers].sort();',
          '}',
        ].join('\n'),
        tests: [
          { name: 'sorts multi-digit numbers correctly', body: 'expect(sortNumbers([10, 9, 1, 100])).toEqual([1, 9, 10, 100]);' },
          { name: 'sorts single digits', body: 'expect(sortNumbers([3, 1, 2])).toEqual([1, 2, 3]);' },
          { name: 'handles negatives', body: 'expect(sortNumbers([-5, 10, -20])).toEqual([-20, -5, 10]);' },
          { name: 'handles decimals', body: 'expect(sortNumbers([1.5, 1.25, 1.75])).toEqual([1.25, 1.5, 1.75]);' },
          { name: 'does not modify the original', body: 'const a = [3, 1, 2]; sortNumbers(a); expect(a).toEqual([3, 1, 2]);' },
          { name: 'handles an empty array', body: 'expect(sortNumbers([])).toEqual([]);', hidden: true },
        ],
        hints: [
          'The default `sort` converts every element to a string before comparing.',
          'Supply a comparator function that returns a negative number, zero, or a positive number.',
          '`(a, b) => a - b` is the standard numeric ascending comparator.',
        ],
        solution: [
          'function sortNumbers(numbers) {',
          '  return [...numbers].sort((a, b) => a - b);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The comparator tells `sort` how to order any two elements: a negative result means `a` comes first, positive means `b` comes first, and zero means their order is unchanged. Subtraction produces exactly that, which is why `(a, b) => a - b` is the idiomatic ascending numeric comparator — and `(a, b) => b - a` sorts descending. The spread in `[...numbers]` was already correct and matters because `sort` mutates the array it is called on; without it, the caller\'s array would be reordered, which the fifth test checks.',
      },
      {
        id: 'ex-m04-03-c',
        title: 'Write a safe range check',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['operators', 'numbers'],
        instructions:
          'Write `inRange(value, min, max)` that returns `true` when `value` is a real number between `min` and `max` inclusive. It must return `false` for `NaN`, for non-numbers such as `"5"`, and for `null` or `undefined` — even though some of those would pass a naive comparison.',
        starterCode: [
          'function inRange(value, min, max) {',
          '  // Validate that value is a real number before comparing.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'accepts a value inside the range', body: 'expect(inRange(5, 1, 10)).toBe(true);' },
          { name: 'is inclusive at both ends', body: 'expect(inRange(1, 1, 10)).toBe(true); expect(inRange(10, 1, 10)).toBe(true);' },
          { name: 'rejects a value outside the range', body: 'expect(inRange(11, 1, 10)).toBe(false); expect(inRange(0, 1, 10)).toBe(false);' },
          { name: 'rejects NaN', body: 'expect(inRange(NaN, 1, 10)).toBe(false);' },
          { name: 'rejects a numeric string', body: 'expect(inRange("5", 1, 10)).toBe(false);' },
          { name: 'rejects null', body: 'expect(inRange(null, 1, 10)).toBe(false);' },
          { name: 'rejects undefined', body: 'expect(inRange(undefined, 1, 10)).toBe(false);' },
          { name: 'works with negative ranges', body: 'expect(inRange(-5, -10, 0)).toBe(true);', hidden: true },
        ],
        hints: [
          '`null >= 1` is `false`, so null happens to fail — but `null` would pass a range of `-1` to `1`. Reject it explicitly.',
          '`Number.isFinite(value)` rejects NaN, the infinities, and every non-number in one call.',
          'Then the comparison itself is straightforward.',
        ],
        solution: [
          'function inRange(value, min, max) {',
          '  if (!Number.isFinite(value)) return false;',
          '  return value >= min && value <= max;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The guard does all the difficult work. `Number.isFinite` performs no coercion, so `"5"`, `null` and `undefined` are rejected on type alone, and `NaN` is rejected because it is not finite. That matters because `NaN` makes every comparison `false`, which would look like "out of range" rather than "invalid input" — a meaningfully different diagnosis. Without the guard, `null` would be converted to `0` by the relational operators and would pass a range such as `-1` to `1`, which the hidden negative-range test is designed to expose. Note also the range check uses `&&` rather than a chained `min <= value <= max`, which would not work.',
      },
    ],
    quiz: {
      id: 'qz-m04-03',
      questions: [
        {
          id: 'q-m04-03-1',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['operators'],
          prompt: 'Why does the default sort produce this order?',
          code: ['console.log([10, 9, 1].sort());'].join('\n'),
          options: ['[1, 9, 10]', '[1, 10, 9]', '[10, 9, 1]', '[9, 10, 1]'],
          correct: 1,
          optionExplanations: [
            'That requires a numeric comparator.',
            'Correct — elements are converted to strings and compared character by character.',
            'Sort does reorder the array.',
            'String order puts `"1"` before `"9"`.',
          ],
          explanation:
            'Without a comparator, `sort` converts every element to a string and compares those. `"1"` sorts before `"9"`, so `"10"` comes before `"9"`. Supply `(a, b) => a - b` for numeric order.',
        },
        {
          id: 'q-m04-03-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['operators'],
          prompt: 'Why is this chained comparison false?',
          code: ['console.log(3 > 2 > 1);'].join('\n'),
          options: ['true', 'false', 'TypeError', 'NaN'],
          correct: 1,
          optionExplanations: [
            'Chained comparisons do not work the way they do in mathematics.',
            'Correct — `(3 > 2)` gives `true`, which converts to `1`, and `1 > 1` is false.',
            'The expression parses and runs fine.',
            'Every step produces a boolean or a number.',
          ],
          explanation:
            'Relational operators are left-associative and each produces a boolean. `3 > 2` gives `true`, then `true > 1` converts `true` to `1`, and `1 > 1` is `false`. Write range checks as `x > 1 && x < 3`.',
        },
        {
          id: 'q-m04-03-3',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['operators', 'numbers'],
          prompt: 'Which of these are `false`? (Select all that apply.)',
          options: ['NaN > 0', 'NaN < 0', 'NaN === NaN', 'null >= 0'],
          correct: [0, 1, 2],
          explanation:
            'Every comparison involving `NaN` is false, including equality with itself — it is the only value for which both `a > b` and `a <= b` fail simultaneously. `null >= 0` is the odd one out: relational operators convert `null` to `0`, and `0 >= 0` is true. Note that `null == 0` is false, because loose equality has a special rule that relational operators do not share.',
        },
      ],
    },
    summary:
      'Comparison operators return booleans. When both operands are strings they are compared character by character using UTF-16 code units, which puts uppercase before lowercase and makes `"10" < "9"` true — the direct cause of `[10, 9].sort()` producing the wrong order. Otherwise both operands are converted to numbers, which is why `null >= 0` is true even though `null == 0` is false. Every comparison involving `NaN` is false, so validate with `Number.isFinite` before comparing. Chained comparisons such as `3 > 2 > 1` parse and run but mean something quite different from the mathematical notation; always use `&&` for range checks, and `localeCompare` for human-facing text.',
    keyTakeaways: [
      'Two strings compare character by character; anything else converts to numbers',
      '`"10" < "9"` is true — and that is why the default `sort` misorders numbers',
      'Sort numbers with `(a, b) => a - b`; sort text with `localeCompare`',
      'Every comparison with `NaN` is false, including `NaN === NaN`',
      '`3 > 2 > 1` is false — chained comparisons do not work; use `&&`',
    ],
    relatedLessons: ['l-m03-05', 'l-m04-04'],
    interviewConnections: [
      'Why does [10, 9, 1].sort() give [1, 10, 9]?',
      'How are strings compared in JavaScript?',
      'What does 3 > 2 > 1 evaluate to and why?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m04-04',
    slug: 'logical-operators',
    moduleId: M,
    order: 4,
    title: 'Logical Operators and Short-Circuiting',
    description: 'Why `&&` and `||` do not return booleans, and the patterns that follow from that.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 15,
    xp: 30,
    topicIds: ['operators', 'booleans'],
    prerequisites: ['l-m04-03'],
    learningObjectives: [
      'Explain what `&&` and `||` actually return',
      'Describe short-circuit evaluation and why it matters',
      'Use `&&` and `||` for guards and defaults, and know when not to',
      'Explain why `!` is the only logical operator that always returns a boolean',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'This lesson contains the single most useful fact about logical operators in JavaScript, and it is one that most beginners never learn explicitly:',
          '**`&&` and `||` do not return `true` or `false`. They return one of their operands.**',
          'Everything else in this lesson follows from that.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'What they actually return',
      },
      {
        kind: SECTION.STEPS,
        steps: [
          {
            title: '`a && b` — "and"',
            body: 'Evaluates `a`. If `a` is **falsy**, return `a` immediately without evaluating `b`. Otherwise return `b`. In short: the first falsy value, or the last value.',
          },
          {
            title: '`a || b` — "or"',
            body: 'Evaluates `a`. If `a` is **truthy**, return `a` immediately without evaluating `b`. Otherwise return `b`. In short: the first truthy value, or the last value.',
          },
          {
            title: '`!a` — "not"',
            body: 'Converts `a` to a boolean and negates it. This is the only one of the three that always returns an actual boolean.',
          },
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The returned values are the operands themselves.',
        code: [
          'console.log(0 && "hello");        // 0        — first falsy',
          'console.log("hi" && "hello");     // "hello"  — last value',
          'console.log("hi" && 0 && "bye");  // 0        — first falsy',
          '',
          'console.log(0 || "hello");        // "hello"  — first truthy',
          'console.log("hi" || "hello");     // "hi"     — first truthy',
          'console.log(0 || null || "");     // ""       — last value, all falsy',
          '',
          'console.log(!0);                  // true     — always a boolean',
          'console.log(!"hi");               // false',
        ].join('\n'),
        output: ['0', 'hello', '0', 'hello', 'hi', '', 'true', 'false'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'In a condition it does not matter',
        body: [
          'Inside `if (a && b)`, the returned value is converted to a boolean anyway, so the distinction is invisible. It becomes visible the moment you *assign* the result — which is exactly what the default-value and guard patterns below do.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Short-circuit evaluation',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Both operators stop as soon as the answer is determined. With `&&`, a falsy left operand means the result cannot be truthy, so the right side is never evaluated. With `||`, a truthy left operand settles it the same way.',
          'This is not merely an optimisation — it is observable behaviour that you can rely on, and that you must account for when the right-hand side has side effects.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The right-hand side genuinely does not run.',
        code: [
          'function sideEffect(label) {',
          '  console.log("evaluated:", label);',
          '  return true;',
          '}',
          '',
          'console.log("--- && with falsy left ---");',
          'false && sideEffect("right side of &&");',
          '',
          'console.log("--- || with truthy left ---");',
          'true || sideEffect("right side of ||");',
          '',
          'console.log("--- && with truthy left ---");',
          'true && sideEffect("this one does run");',
        ].join('\n'),
        output: [
          '--- && with falsy left ---',
          '--- || with truthy left ---',
          '--- && with truthy left ---',
          'evaluated: this one does run',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Pattern 1: guarding a property access',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The classic use of short-circuiting.',
        code: [
          'const user = null;',
          '',
          '// Without a guard this throws:',
          'try {',
          '  console.log(user.name);',
          '} catch (error) {',
          '  console.log("threw:", error.name);',
          '}',
          '',
          '// && short-circuits before the access happens',
          'console.log(user && user.name);   // null — never reads .name',
          '',
          'const other = { name: "Ada" };',
          'console.log(other && other.name); // "Ada"',
        ].join('\n'),
        output: ['threw: TypeError', 'null', 'Ada'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This pattern is common in older code. Modern JavaScript has optional chaining (`user?.name`) which expresses the same idea more directly, and you will meet it in the next lesson. Recognising the `&&` form matters because it is everywhere in existing codebases.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Pattern 2: default values, and its trap',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: '`||` supplies a fallback — for any falsy value, which is usually too many.',
        code: [
          'function greet(name) {',
          '  const displayName = name || "friend";',
          '  return "Hello, " + displayName;',
          '}',
          '',
          'console.log(greet("Ada"));        // Hello, Ada',
          'console.log(greet());             // Hello, friend',
          'console.log(greet(""));           // Hello, friend  ← maybe wrong',
          '',
          'function setVolume(level) {',
          '  return level || 50;',
          '}',
          '',
          'console.log(setVolume(80));       // 80',
          'console.log(setVolume(0));        // 50  ← definitely wrong',
        ].join('\n'),
        output: ['Hello, Ada', 'Hello, friend', 'Hello, friend', '80', '50'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'The `||` default trap',
        body: [
          '`setVolume(0)` should mute the audio. Instead it returns 50, because `0` is falsy and `||` cannot distinguish "the caller passed zero" from "the caller passed nothing".',
          'The same bug affects `""`, `false` and `NaN`. Whenever a falsy value is legitimate data, use `??` instead — it falls back only on `null` and `undefined`. The next lesson covers it properly.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Pattern 3: conditional execution',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Using the operators as compact control flow.',
        code: [
          'const isLoggedIn = true;',
          'const errors = [];',
          '',
          '// Run only if the condition holds',
          'isLoggedIn && console.log("Welcome back");',
          '',
          '// Run only if the condition fails',
          'errors.length || console.log("No errors to report");',
          '',
          '// This is how conditional rendering works in JSX:',
          '// {items.length > 0 && <List items={items} />}',
        ].join('\n'),
        output: ['Welcome back', 'No errors to report'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'The React rendering trap this creates',
        body: [
          '`{items.length && <List />}` looks reasonable but renders a literal `0` on screen when the array is empty — because `&&` returns the falsy left operand, which is the number `0`, and React renders numbers.',
          'Write `{items.length > 0 && <List />}` so the left operand is a boolean. `false` is not rendered, but `0` is. This is one of the most commonly reported React bugs, and it comes straight from the fact that `&&` returns an operand rather than a boolean.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const a = 0 || "" || null || "found" || "later";',
          'const b = "one" && 0 && "three";',
          '',
          'console.log(a, b);',
        ].join('\n'),
        options: [
          '"found" 0',
          '"found" "three"',
          'null 0',
          '"later" 0',
        ],
        correct: 0,
        explanation:
          'For `a`, `||` returns the first **truthy** operand and stops there: `0`, `""` and `null` are all falsy, so it continues to `"found"`, which is truthy — so it returns `"found"` and never evaluates `"later"`. For `b`, `&&` returns the first **falsy** operand: `"one"` is truthy so it continues, `0` is falsy so it returns `0` immediately and never evaluates `"three"`.',
      },
    ],
    exercises: [
      {
        id: 'ex-m04-04-a',
        title: 'Predict what && returns',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['operators'],
        instructions: 'What does this print?',
        code: ['console.log("hello" && 0);'].join('\n'),
        options: ['true', 'false', '0', '"hello"'],
        correct: 2,
        hints: [
          '`&&` returns an operand, not a boolean.',
          'It returns the first falsy value it finds.',
        ],
        solution: '0',
        solutionExplanation:
          '`&&` returns the first falsy operand, or the last operand if all are truthy. `"hello"` is truthy so evaluation continues; `0` is falsy so it is returned immediately. The result is the number `0`, not the boolean `false` — a distinction that matters enormously when the result is assigned or rendered rather than used in a condition.',
      },
      {
        id: 'ex-m04-04-b',
        title: 'Fix the zero-default bug',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['operators', 'coercion'],
        instructions:
          'This function should return the supplied quantity, falling back to 1 only when nothing was supplied. At the moment a deliberate quantity of 0 is replaced by 1. Fix it so `0` is respected while `null` and `undefined` still fall back.',
        starterCode: [
          'function resolveQuantity(quantity) {',
          '  return quantity || 1;',
          '}',
        ].join('\n'),
        tests: [
          { name: 'returns a supplied quantity', body: 'expect(resolveQuantity(5)).toBe(5);' },
          { name: 'respects a quantity of zero', body: 'expect(resolveQuantity(0)).toBe(0);' },
          { name: 'falls back for undefined', body: 'expect(resolveQuantity(undefined)).toBe(1);' },
          { name: 'falls back for null', body: 'expect(resolveQuantity(null)).toBe(1);' },
          { name: 'falls back when nothing is passed', body: 'expect(resolveQuantity()).toBe(1);' },
          { name: 'respects negative quantities', body: 'expect(resolveQuantity(-3)).toBe(-3);', hidden: true },
        ],
        hints: [
          '`||` falls back on every falsy value, including `0`.',
          'You want to fall back only on `null` and `undefined`.',
          'The nullish coalescing operator `??` does exactly that.',
        ],
        solution: [
          'function resolveQuantity(quantity) {',
          '  return quantity ?? 1;',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`??` returns its right-hand side only when the left is `null` or `undefined`, so a deliberate `0` passes through untouched while a missing value still gets the default. `||` cannot make that distinction, because it treats every falsy value the same. An equivalent explicit form is `quantity == null ? 1 : quantity`, which some codebases prefer for clarity, but `??` is the modern idiom and is what you will see in current code.',
      },
      {
        id: 'ex-m04-04-c',
        title: 'Write a short-circuit safe getter',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 20,
        topicIds: ['operators', 'objects'],
        instructions:
          'Write `getCity(user)` that returns the value of `user.address.city`, or the string `"Unknown"` if `user`, `user.address`, or `user.address.city` is missing. Do not use optional chaining — use `&&` and `||`, so you practise the short-circuit patterns directly.',
        starterCode: [
          'function getCity(user) {',
          '  // Guard each level before reading the next.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'returns the city when present', body: 'expect(getCity({ address: { city: "London" } })).toBe("London");' },
          { name: 'handles a missing address', body: 'expect(getCity({})).toBe("Unknown");' },
          { name: 'handles a missing city', body: 'expect(getCity({ address: {} })).toBe("Unknown");' },
          { name: 'handles null user', body: 'expect(getCity(null)).toBe("Unknown");' },
          { name: 'handles undefined user', body: 'expect(getCity(undefined)).toBe("Unknown");' },
          { name: 'handles a missing argument', body: 'expect(getCity()).toBe("Unknown");' },
          { name: 'treats an empty city as missing', body: 'expect(getCity({ address: { city: "" } })).toBe("Unknown");', hidden: true },
        ],
        hints: [
          '`user && user.address` stops safely if `user` is null.',
          'Chain a third `&&` to reach `city`.',
          'Finish with `|| "Unknown"` to supply the fallback.',
        ],
        solution: [
          'function getCity(user) {',
          '  return (user && user.address && user.address.city) || "Unknown";',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Each `&&` acts as a gate: if `user` is null the chain returns `null` immediately and `user.address` is never evaluated, avoiding the TypeError. The final `||` converts any falsy result — `null`, `undefined`, or an empty string — into `"Unknown"`. The hidden test is worth noting: because `||` falls back on *any* falsy value, an empty city string also becomes `"Unknown"`. That is the desired behaviour here, but if you needed to preserve an empty string you would use `?? "Unknown"` instead. Modern code writes this as `user?.address?.city || "Unknown"`, which is the subject of the next lesson.',
      },
    ],
    quiz: {
      id: 'qz-m04-04',
      questions: [
        {
          id: 'q-m04-04-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['operators'],
          prompt: 'What does `a || b` return?',
          options: [
            'Always `true` or `false`',
            '`a` if `a` is truthy, otherwise `b`',
            '`b` if `b` is truthy, otherwise `a`',
            'Both values as an array',
          ],
          correct: 1,
          optionExplanations: [
            'Only `!` always returns a boolean.',
            'Correct — it returns the first truthy operand, or the last one.',
            'The left operand is evaluated first and wins if truthy.',
            'It returns a single value.',
          ],
          explanation:
            '`||` returns the first truthy operand it finds, or the final operand if none are truthy. It returns the operand itself, not a boolean — which is why `0 || 50` returns `50` and breaks defaults where zero is valid.',
        },
        {
          id: 'q-m04-04-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['operators'],
          prompt: 'Does the function on the right-hand side run?',
          code: [
            'function log() { console.log("ran"); return true; }',
            'false && log();',
            'console.log("done");',
          ].join('\n'),
          options: ['ran, then done', 'done only', 'ran only', 'Nothing prints'],
          correct: 1,
          optionExplanations: [
            'Short-circuiting prevents the call entirely.',
            'Correct — `&&` stops as soon as its left operand is falsy.',
            'The final log always runs.',
            'The last line is unconditional.',
          ],
          explanation:
            '`&&` short-circuits: once the left operand is falsy the result is determined, so the right operand is never evaluated and `log()` is never called. This is observable behaviour you can rely on, not just an optimisation.',
        },
        {
          id: 'q-m04-04-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['operators'],
          prompt: 'Why does `{items.length && <List />}` render a literal `0` in React when the array is empty?',
          options: [
            'Because React cannot render arrays',
            'Because `&&` returns the falsy left operand — the number `0` — and React renders numbers',
            'Because `length` is undefined on empty arrays',
            'Because the component throws an error',
          ],
          correct: 1,
          optionExplanations: [
            'Arrays render fine in React.',
            'Correct — use `items.length > 0 &&` so the operand is a boolean.',
            '`length` is `0`, which is defined.',
            'Nothing throws; it simply renders the wrong thing.',
          ],
          explanation:
            '`&&` returns the operand rather than a boolean, so an empty array yields the number `0`. React renders `0` but ignores `false`, so the fix is to make the left operand a real boolean with `items.length > 0`.',
        },
        {
          id: 'q-m04-04-4',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['operators'],
          prompt: 'Which statements about logical operators are true? (Select all that apply.)',
          options: [
            '`&&` returns the first falsy operand, or the last operand',
            '`!` always returns a boolean',
            '`||` evaluates its right operand only when the left is falsy',
            '`&&` always returns a boolean',
          ],
          correct: [0, 1, 2],
          explanation:
            '`&&` returns an operand — the first falsy one, or the last if all are truthy — so it does not always return a boolean. `!` is the only one of the three that always does, because it explicitly converts and negates. And `||` short-circuits: it skips the right operand entirely when the left is already truthy.',
        },
      ],
    },
    summary:
      '`&&` and `||` return one of their operands rather than a boolean: `&&` gives the first falsy operand or the last one, and `||` gives the first truthy operand or the last one. Only `!` always returns an actual boolean. Both short-circuit, genuinely skipping evaluation of the right-hand side when the result is already determined — which enables the `user && user.name` guard pattern and the `condition && doThing()` conditional-execution idiom. The `||` default pattern has a well-known trap: because it falls back on every falsy value, a legitimate `0`, `""` or `false` is replaced by the default. Use `??` when falsy values are valid data. The same operand-returning behaviour is why `{items.length && <List/>}` renders a stray `0` in React.',
    keyTakeaways: [
      '`&&` returns the first falsy operand or the last; `||` returns the first truthy or the last',
      'Only `!` always returns a boolean',
      'Short-circuiting means the right operand genuinely does not run',
      '`||` for defaults breaks on valid `0`, `""` and `false` — use `??`',
      'Make the left operand a boolean in JSX: `items.length > 0 && ...`',
    ],
    relatedLessons: ['l-m03-03', 'l-m04-05'],
    interviewConnections: [
      'What does the && operator return?',
      'What is short-circuit evaluation?',
      'Why does `0 || "default"` return "default"?',
      'Why does `{count && <X/>}` render 0 in React?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m04-05',
    slug: 'nullish-and-optional-chaining',
    moduleId: M,
    order: 5,
    title: 'Nullish Coalescing and Optional Chaining',
    description: 'The two modern operators that replace whole categories of defensive code — and the rules that govern them.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 15,
    xp: 30,
    topicIds: ['operators', 'modern-js', 'objects'],
    prerequisites: ['l-m04-04'],
    learningObjectives: [
      'Use `??` and explain exactly how it differs from `||`',
      'Use `?.` for properties, array indexes and function calls',
      'Explain what "short-circuiting the whole chain" means',
      'Recognise when optional chaining hides a bug rather than preventing one',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'These two operators arrived together in ES2020, and they solve the two problems the previous lesson left open: `||` falls back too eagerly, and `&&` chains become unreadable.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Nullish coalescing: `??`',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`a ?? b` returns `b` **only when `a` is `null` or `undefined`**. Every other value — including `0`, `""`, `false` and `NaN` — is returned as-is.',
          'The term "nullish" means exactly "null or undefined", and it is the same notion that governs default parameters.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The comparison that matters.',
        code: [
          'console.log(0 || "default");        // "default"  ← 0 was discarded',
          'console.log(0 ?? "default");        // 0          ← 0 was kept',
          '',
          'console.log("" || "default");       // "default"',
          'console.log("" ?? "default");       // ""',
          '',
          'console.log(false || "default");    // "default"',
          'console.log(false ?? "default");    // false',
          '',
          'console.log(null ?? "default");     // "default"',
          'console.log(undefined ?? "default"); // "default"',
          '',
          'console.log(NaN ?? "default");      // NaN  ← NaN is not nullish',
        ].join('\n'),
        output: ['default', '0', 'default', '', 'default', 'false', 'default', 'default', 'NaN'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'A rule you can apply without thinking',
        body: [
          'Use `??` for **default values**, and `||` only when you genuinely want "any falsy value should be replaced".',
          'The second case is rarer than people assume. It is right for something like `const displayName = name.trim() || "Anonymous"`, where an empty string really should become the stand-in text. It is wrong for almost every numeric or boolean setting.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: '`??` cannot be mixed with `&&` or `||`',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Brackets are required, by design.',
        code: [
          '// a || b ?? c   is a SyntaxError',
          '// a && b ?? c   is a SyntaxError',
          '',
          '// Add brackets to state your intent:',
          'const a = null;',
          'const b = 0;',
          'const c = "fallback";',
          '',
          'console.log((a || b) ?? c);   // 0',
          'console.log(a || (b ?? c));   // 0',
          '',
          '// Chaining ?? with itself is fine:',
          'console.log(null ?? undefined ?? "third");',
        ].join('\n'),
        output: ['0', '0', 'third'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Mixing them without brackets is a deliberate SyntaxError. The precedence would be genuinely ambiguous to readers, so the language requires you to say which grouping you meant — the same reasoning behind `-2 ** 2` being an error.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Optional chaining: `?.`',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`a?.b` returns `undefined` if `a` is `null` or `undefined`, instead of throwing. If `a` has any other value, it reads `b` normally.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Three forms: property, index and call.',
        code: [
          'const user = { name: "Ada", address: { city: "London" }, tags: ["a"] };',
          'const empty = null;',
          '',
          '// Property access',
          'console.log(user?.name);            // "Ada"',
          'console.log(empty?.name);           // undefined — no throw',
          '',
          '// Nested',
          'console.log(user?.address?.city);   // "London"',
          'console.log(empty?.address?.city);  // undefined',
          '',
          '// Array index — note the dot before the bracket',
          'console.log(user?.tags?.[0]);       // "a"',
          'console.log(empty?.tags?.[0]);      // undefined',
          '',
          '// Function call — note the dot before the parens',
          'console.log(user.greet?.());        // undefined — greet does not exist',
        ].join('\n'),
        output: ['Ada', 'undefined', 'London', 'undefined', 'a', 'undefined', 'undefined'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'The syntax for indexes and calls',
        body: [
          'For array indexes it is `?.[index]`, not `?[index]`. For function calls it is `?.()`, not `?()`. The dot is always present — think of `?.` as a single unit that replaces the `.` you would otherwise write.',
          '`fn?.()` is especially useful for optional callbacks: it calls `fn` if it exists and does nothing otherwise, replacing `if (typeof fn === "function") fn();`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The whole chain short-circuits',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This is the part people miss. When `?.` short-circuits, it abandons **the entire rest of the chain**, not just the next step — including function calls and index lookups further along.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Everything after the short-circuit is skipped.',
        code: [
          'const data = null;',
          '',
          'function sideEffect() {',
          '  console.log("this never runs");',
          '  return 0;',
          '}',
          '',
          '// The index expression is not even evaluated',
          'console.log(data?.items[sideEffect()]);   // undefined',
          '',
          'console.log("finished");',
        ].join('\n'),
        output: ['undefined', 'finished'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Note that only **one** `?.` was needed at the start. Once it short-circuits, `.items`, the index lookup, and the call to `sideEffect()` are all abandoned. You do not need `?.` at every step of a chain — only where a value might genuinely be nullish.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Do not scatter `?.` everywhere',
        body: [
          'Writing `user?.address?.city?.trim?.()` when only `address` is genuinely optional does two harmful things: it adds noise, and — more seriously — it silences errors you would want to see. If `user` should always exist, a TypeError is *useful* information telling you something upstream is broken.',
          'Use `?.` where a value is legitimately sometimes absent. Let it throw where absence indicates a bug.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The two together',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The idiomatic modern pattern.',
        code: [
          'function getFontSize(settings) {',
          '  return settings?.display?.fontSize ?? 14;',
          '}',
          '',
          'console.log(getFontSize({ display: { fontSize: 18 } }));  // 18',
          'console.log(getFontSize({ display: { fontSize: 0 } }));   // 0  ← preserved',
          'console.log(getFontSize({ display: {} }));                // 14',
          'console.log(getFontSize({}));                             // 14',
          'console.log(getFontSize(null));                           // 14',
          'console.log(getFontSize());                               // 14',
        ].join('\n'),
        output: ['18', '0', '14', '14', '14', '14'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'One line replaces roughly eight lines of defensive checks, and it gets the `0` case right — which a `||` version would not. This combination of `?.` for safe traversal and `??` for the default is the standard way to read optional nested configuration in modern JavaScript.',
        ],
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Before ES2020',
          code: [
            'function getFontSize(settings) {',
            '  if (!settings) return 14;',
            '  if (!settings.display) return 14;',
            '  const size =',
            '    settings.display.fontSize;',
            '  if (size === null ||',
            '      size === undefined) {',
            '    return 14;',
            '  }',
            '  return size;',
            '}',
          ].join('\n'),
        },
        right: {
          title: 'Modern',
          code: [
            'function getFontSize(settings) {',
            '  return settings?.display?.fontSize',
            '    ?? 14;',
            '}',
          ].join('\n'),
        },
        note: 'Identical behaviour, including preserving a deliberate `0`. The older `settings && settings.display && settings.display.fontSize || 14` form is shorter but wrong, because `||` would discard the zero.',
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const config = { volume: 0, label: "" };',
          '',
          'console.log(config?.volume ?? 50);',
          'console.log(config?.volume || 50);',
          'console.log(config?.missing ?? 50);',
        ].join('\n'),
        options: [
          '0, 50, 50',
          '0, 0, 50',
          '50, 50, 50',
          '0, 50, undefined',
        ],
        correct: 0,
        explanation:
          'The first line reads `0`, which is not nullish, so `??` keeps it. The second reads the same `0`, but `0` is falsy so `||` discards it and returns `50` — the exact bug `??` was introduced to fix. The third reads a property that does not exist, giving `undefined`, which *is* nullish, so `??` supplies `50`. Note the optional chaining never short-circuits here, because `config` is a real object.',
      },
    ],
    exercises: [
      {
        id: 'ex-m04-05-a',
        title: 'Predict the nullish difference',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['operators'],
        instructions: 'What does this print?',
        code: ['console.log(0 ?? "fallback");'].join('\n'),
        options: ['"fallback"', '0', 'null', 'undefined'],
        correct: 1,
        hints: [
          '`??` only falls back for two specific values. Is `0` one of them?',
        ],
        solution: '0',
        solutionExplanation:
          '`??` returns its right-hand side only when the left is `null` or `undefined`. `0` is neither — it is merely falsy — so it is returned unchanged. `0 || "fallback"` would give `"fallback"` instead, and that difference is precisely why `??` was added to the language.',
      },
      {
        id: 'ex-m04-05-b',
        title: 'Choose the right operator',
        kind: EXERCISE_KIND.CHOOSE_IMPLEMENTATION,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['operators'],
        instructions:
          'A settings object may omit `retryCount`. When omitted, the default should be 3. When explicitly set to 0, retries should be disabled. Which expression is correct?',
        options: [
          'settings.retryCount || 3',
          'settings.retryCount ?? 3',
          'settings.retryCount && 3',
          'settings.retryCount ? settings.retryCount : 3',
        ],
        correct: 1,
        hints: [
          'Which operators treat `0` as "missing"?',
        ],
        solution: 'settings.retryCount ?? 3',
        solutionExplanation:
          '`??` falls back only for `null` and `undefined`, so an explicit `0` is preserved and retries are correctly disabled. Both `||` and the ternary test truthiness, so both would replace `0` with `3` and silently re-enable retries — the exact bug being guarded against. `&&` does something different again, returning `0` when the value is falsy and `3` when it is truthy, which is backwards.',
      },
      {
        id: 'ex-m04-05-c',
        title: 'Read nested configuration safely',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 20,
        topicIds: ['operators', 'objects'],
        instructions:
          'Write `getTimeout(config)` that returns `config.network.timeout.ms`, defaulting to `5000` if any part of that path is missing. A timeout of `0` must be preserved. Use optional chaining and nullish coalescing.',
        starterCode: [
          'function getTimeout(config) {',
          '  // One expression is enough.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'reads a nested value', body: 'expect(getTimeout({ network: { timeout: { ms: 250 } } })).toBe(250);' },
          { name: 'preserves a timeout of zero', body: 'expect(getTimeout({ network: { timeout: { ms: 0 } } })).toBe(0);' },
          { name: 'defaults when ms is missing', body: 'expect(getTimeout({ network: { timeout: {} } })).toBe(5000);' },
          { name: 'defaults when timeout is missing', body: 'expect(getTimeout({ network: {} })).toBe(5000);' },
          { name: 'defaults when network is missing', body: 'expect(getTimeout({})).toBe(5000);' },
          { name: 'defaults for null', body: 'expect(getTimeout(null)).toBe(5000);' },
          { name: 'defaults when nothing is passed', body: 'expect(getTimeout()).toBe(5000);' },
          { name: 'preserves a null-ish-looking but valid value', body: 'expect(getTimeout({ network: { timeout: { ms: false } } })).toBe(false);', hidden: true },
        ],
        hints: [
          'Chain `?.` at each level that might be absent.',
          'Finish with `?? 5000`, not `|| 5000`.',
          'The whole thing fits on one line.',
        ],
        solution: [
          'function getTimeout(config) {',
          '  return config?.network?.timeout?.ms ?? 5000;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Each `?.` guards one level: if any of them is `null` or `undefined` the entire chain short-circuits to `undefined`, which `??` then converts to the default. Using `||` instead would pass every test except the two important ones — a timeout of `0` would become `5000`, and the hidden test with `false` would fail too. That is the whole reason these two operators are paired: `?.` handles the traversal, `??` handles the default, and neither one discards a legitimate falsy value.',
      },
      {
        id: 'ex-m04-05-d',
        title: 'Call an optional callback',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 20,
        topicIds: ['operators', 'functions'],
        instructions:
          'Write `notify(handlers, message)` that calls `handlers.onMessage(message)` if that function exists, and returns whatever it returns. If `handlers` is missing, or has no `onMessage`, return the string `"no handler"`. Use optional chaining for the call.',
        starterCode: [
          'function notify(handlers, message) {',
          '  // fn?.() calls fn only if it exists.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'calls the handler and returns its result', body: 'expect(notify({ onMessage: (m) => "got " + m }, "hi")).toBe("got hi");' },
          { name: 'passes the message through', body: 'let seen = null; notify({ onMessage: (m) => { seen = m; } }, "test"); expect(seen).toBe("test");' },
          { name: 'returns the fallback when onMessage is missing', body: 'expect(notify({}, "hi")).toBe("no handler");' },
          { name: 'returns the fallback when handlers is null', body: 'expect(notify(null, "hi")).toBe("no handler");' },
          { name: 'returns the fallback when nothing is passed', body: 'expect(notify()).toBe("no handler");' },
          { name: 'preserves a handler that returns 0', body: 'expect(notify({ onMessage: () => 0 }, "x")).toBe(0);', hidden: true },
        ],
        hints: [
          '`handlers?.onMessage?.(message)` calls it safely and gives `undefined` otherwise.',
          'A handler that returns nothing also gives `undefined`, so use `??` for the fallback.',
          'The hidden test means you must not use `||`.',
        ],
        solution: [
          'function notify(handlers, message) {',
          '  return handlers?.onMessage?.(message) ?? "no handler";',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The `?.()` call form is the neat part: it invokes `onMessage` only if it exists, replacing the older `if (typeof handlers.onMessage === "function")` guard. The first `?.` protects against a null `handlers`, and the second against a missing `onMessage`. `??` then supplies the fallback for the `undefined` that short-circuiting produces. The hidden test rules out `||`, which would replace a legitimate return value of `0` with the fallback string. Note one honest limitation of this design: a handler that genuinely returns `undefined` is indistinguishable from a missing one, so a real API would usually check for the handler separately rather than inferring from the return value.',
      },
    ],
    quiz: {
      id: 'qz-m04-05',
      questions: [
        {
          id: 'q-m04-05-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['operators'],
          prompt: 'When does `a ?? b` return `b`?',
          options: [
            'Whenever `a` is falsy',
            'Only when `a` is `null` or `undefined`',
            'Only when `a` is `undefined`',
            'Whenever `a` is truthy',
          ],
          correct: 1,
          optionExplanations: [
            'That is `||`, which also discards `0`, `""` and `false`.',
            'Correct — "nullish" means exactly null or undefined.',
            '`null` also triggers the fallback.',
            'That would be backwards.',
          ],
          explanation:
            '`??` falls back only for the two nullish values. Every other value, including `0`, `""`, `false` and `NaN`, is returned unchanged — which is exactly what makes it correct for default values where `||` is not.',
        },
        {
          id: 'q-m04-05-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['operators'],
          prompt: 'Does this throw, and what does it produce?',
          code: [
            'const data = null;',
            'console.log(data?.a.b.c);',
          ].join('\n'),
          options: ['TypeError', 'undefined', 'null', 'A SyntaxError'],
          correct: 1,
          optionExplanations: [
            'Short-circuiting prevents the access entirely.',
            'Correct — the whole chain is abandoned, not just the next step.',
            'Short-circuiting produces `undefined`, not `null`.',
            'The syntax is valid.',
          ],
          explanation:
            'When `?.` short-circuits it abandons the entire remaining chain, so `.a`, `.b` and `.c` are never evaluated and the expression is `undefined`. This is why a single `?.` at the point of genuine uncertainty is often enough — you do not need one at every step.',
        },
        {
          id: 'q-m04-05-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['operators'],
          prompt: 'Why is `a || b ?? c` a SyntaxError?',
          options: [
            'Because `??` cannot be used with strings',
            'Because the precedence would be ambiguous to readers, so brackets are required',
            'Because `||` is deprecated',
            'Because `??` must always be the first operator in an expression',
          ],
          correct: 1,
          optionExplanations: [
            'Types are irrelevant to the parse.',
            'Correct — write `(a || b) ?? c` or `a || (b ?? c)`.',
            '`||` is perfectly current.',
            '`??` can appear anywhere, just not unbracketed beside `||` or `&&`.',
          ],
          explanation:
            'Mixing `??` with `&&` or `||` without brackets is a deliberate SyntaxError, because either grouping would be a plausible reading. The language requires you to state which you meant — the same principle as `-2 ** 2`.',
        },
        {
          id: 'q-m04-05-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['operators'],
          prompt: 'Adding `?.` at every step of a property chain is good defensive practice.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'It adds noise and silences errors that would tell you something is genuinely broken.',
            'Correct — use it only where a value is legitimately optional.',
          ],
          explanation:
            'Optional chaining should mark the places where absence is expected. Applying it everywhere suppresses TypeErrors that would otherwise reveal a real upstream bug, turning a loud failure into a silent `undefined` that surfaces somewhere far less convenient.',
        },
      ],
    },
    summary:
      '`??` returns its right-hand side only when the left is `null` or `undefined`, so it preserves legitimate `0`, `""` and `false` values that `||` would discard — making it the correct operator for default values. It cannot be mixed with `&&` or `||` without brackets, by design. `?.` returns `undefined` instead of throwing when the value to its left is nullish, and comes in three forms: `?.prop`, `?.[index]` and `?.()`. When it short-circuits it abandons the entire remaining chain, including function calls further along, so one `?.` at the genuinely uncertain point is usually enough. Together, `value?.a?.b ?? fallback` replaces a page of defensive checks while getting the falsy cases right — but scatter `?.` too freely and you silence the errors that would have told you something upstream is broken.',
    keyTakeaways: [
      '`??` falls back only for `null` and `undefined`; `||` falls back for any falsy value',
      'Use `??` for defaults; use `||` only when every falsy value really should be replaced',
      '`a || b ?? c` is a SyntaxError — brackets are mandatory',
      'Optional chaining forms: `?.prop`, `?.[index]`, `?.()`',
      'A short-circuiting `?.` abandons the whole rest of the chain',
      'Use `?.` where absence is expected, not everywhere — it hides real bugs',
    ],
    relatedLessons: ['l-m04-04', 'l-m04-06'],
    interviewConnections: [
      'What is the difference between || and ??',
      'What does optional chaining do when it short-circuits?',
      'Why is `a || b ?? c` a SyntaxError?',
      'When should you avoid optional chaining?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m04-06',
    slug: 'ternary-precedence-and-more',
    moduleId: M,
    order: 6,
    title: 'The Ternary, Precedence and the Remaining Operators',
    description: 'Conditional expressions, the precedence rules worth memorising, and the operators nobody teaches you.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 16,
    xp: 35,
    topicIds: ['operators', 'clean-code'],
    prerequisites: ['l-m04-05'],
    learningObjectives: [
      'Use the ternary operator where an expression is required',
      'Reason about operator precedence instead of guessing',
      'Explain associativity and where it changes the result',
      'Use `typeof`, `delete`, `void` and the comma operator correctly',
    ],
    sections: [
      {
        kind: SECTION.HEADING,
        text: 'The conditional (ternary) operator',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`condition ? valueIfTrue : valueIfFalse` is the only operator taking three operands, which is why it is often just called "the ternary".',
          'Its importance is not brevity — it is that it is an **expression**. As you saw in the orientation module, `if` is a statement and therefore cannot appear where a value is expected. The ternary can.',
        ],
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: '`if` — a statement',
          code: [
            'let label;',
            '',
            'if (score > 50) {',
            '  label = "pass";',
            '} else {',
            '  label = "fail";',
            '}',
            '',
            '// `label` must be `let`,',
            '// and is briefly undefined.',
          ].join('\n'),
        },
        right: {
          title: 'Ternary — an expression',
          code: [
            'const label =',
            '  score > 50 ? "pass" : "fail";',
            '',
            '',
            '',
            '',
            '',
            '// Bound once, can be `const`,',
            '// never undefined.',
          ].join('\n'),
        },
        note: 'The ternary version can use `const` because the value is known at the moment of binding. That is a real advantage, not just a stylistic one.',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Common uses — including inside a template literal, where a statement could not go.',
        code: [
          'const count = 1;',
          'console.log(`You have ${count} item${count === 1 ? "" : "s"}`);',
          '',
          'const user = { name: "Ada", admin: false };',
          'console.log(user.admin ? "Administrator" : "Standard user");',
          '',
          '// Nested ternaries — readable only when formatted as a ladder',
          'const score = 72;',
          'const grade =',
          '  score >= 90 ? "A"',
          '  : score >= 80 ? "B"',
          '  : score >= 70 ? "C"',
          '  : "F";',
          'console.log(grade);',
        ].join('\n'),
        output: ['You have 1 item', 'Standard user', 'C'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Nested ternaries: acceptable, with discipline',
        body: [
          'A ternary ladder formatted one condition per line, as above, is readable and widely used. A nested ternary crammed onto one line is not.',
          'The moment you need a branch to do more than produce a value — log something, throw, call two functions — you have outgrown the ternary and should use `if`. Trying to force side effects into an expression is where ternaries earn their bad reputation.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Operator precedence',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Precedence decides which operator binds more tightly when you do not use brackets. There are around twenty levels; you do not need to memorise the table, but a handful of relationships prevent almost all real bugs.',
        ],
      },
      {
        kind: SECTION.TABLE,
        headers: ['Tightness', 'Operators', 'Associativity'],
        rows: [
          ['Highest', '`()` grouping, `.` `[]` member access, `?.`', 'Left'],
          ['', '`new`, function call', 'Left'],
          ['', '`++` `--` (postfix)', ''],
          ['', '`!` `typeof` `delete` `void`, unary `+` `-`, prefix `++` `--`', 'Right'],
          ['', '`**`', '**Right**'],
          ['', '`*` `/` `%`', 'Left'],
          ['', '`+` `-`', 'Left'],
          ['', '`<` `>` `<=` `>=` `instanceof` `in`', 'Left'],
          ['', '`==` `!=` `===` `!==`', 'Left'],
          ['', '`&&`', 'Left'],
          ['', '`||`, `??`', 'Left'],
          ['', '`? :` ternary', '**Right**'],
          ['Lowest', '`=` and compound assignment, `,`', '**Right** (`,` is left)'],
        ],
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The five relationships worth internalising: **member access beats everything**; **unary beats arithmetic**; **arithmetic beats comparison**; **comparison beats logical**; **logical beats assignment**.',
          'That ordering explains most expressions you will read.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Precedence in action — the implicit grouping shown in comments.',
        code: [
          'console.log(2 + 3 * 4);',
          '// * beats +  →  2 + (3 * 4)  →  14',
          '',
          'console.log(1 + 2 > 2);',
          '// + beats >  →  (1 + 2) > 2  →  true',
          '',
          'console.log(true || false && false);',
          '// && beats ||  →  true || (false && false)  →  true',
          '',
          'console.log(!true === false);',
          '// ! beats ===  →  (!true) === false  →  true',
          '',
          'console.log(typeof 1 + 1);',
          '// typeof beats +  →  (typeof 1) + 1  →  "number1"',
        ].join('\n'),
        output: ['14', 'true', 'true', 'true', 'number1'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: '`&&` binds tighter than `||`',
        body: [
          'This catches people constantly. `a || b && c` means `a || (b && c)`, not `(a || b) && c`.',
          'The two readings genuinely differ. With `a = true, b = true, c = false`: the correct grouping gives `true`, the mistaken one gives `false`. When you mix them, add brackets — not because the parser needs them, but because the next person reading it does.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Associativity',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Associativity settles ties between operators of *equal* precedence. Almost everything is left-associative, which matches how you read. Three things are right-associative: `**`, the ternary, and assignment.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Where the direction changes the answer.',
        code: [
          'console.log(10 - 3 - 2);',
          '// left  →  (10 - 3) - 2  →  5',
          '',
          'console.log(2 ** 3 ** 2);',
          '// right →  2 ** (3 ** 2)  →  512',
          '',
          'let a, b;',
          'a = b = 5;',
          '// right →  a = (b = 5)',
          'console.log(a, b);',
        ].join('\n'),
        output: ['5', '512', '5 5'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'The remaining operators',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: '`delete` removes a property and reports success.',
        code: [
          'const user = { name: "Ada", temp: 1 };',
          '',
          'console.log(delete user.temp);   // true',
          'console.log(user.temp);          // undefined',
          'console.log("temp" in user);     // false — genuinely removed',
          '',
          'console.log(delete user.missing); // true — nothing to do, still succeeds',
        ].join('\n'),
        output: ['true', 'undefined', 'false', 'true'].join('\n'),
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        caption: '`delete` applies to properties, not variables. This snippet is deliberately not runnable.',
        code: [
          'let x = 1;',
          'delete x;',
          '// Strict mode:     SyntaxError: Delete of an unqualified identifier',
          '// Non-strict mode: returns false and does nothing',
          '',
          '// On arrays it leaves a hole rather than shortening:',
          'const list = ["a", "b", "c"];',
          'delete list[1];',
          '// list is now ["a", <empty>, "c"] with length still 3',
          '// Use list.splice(1, 1) to actually remove the element.',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`delete` removes a property entirely, which is different from setting it to `undefined` — after `delete`, the key no longer exists, so `in` and `Object.keys` no longer see it.',
          'On arrays, `delete` leaves a hole rather than shortening the array: `delete arr[0]` makes `arr[0]` undefined but `arr.length` unchanged. Use `splice` or `filter` instead.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: '`void` and the comma operator — rare, but you will meet them.',
        code: [
          '// void evaluates its operand and returns undefined',
          'console.log(void 0);          // undefined',
          'console.log(void "anything"); // undefined',
          '',
          '// The comma operator evaluates every expression, returns the last',
          'const result = (1, 2, 3);',
          'console.log(result);          // 3',
          '',
          '// Its one common use — multiple loop counters',
          'for (let i = 0, j = 10; i < j; i++, j--) {',
          '  if (i === 4) console.log("met at", i, j);',
          '}',
        ].join('\n'),
        output: ['undefined', 'undefined', '3', 'met at 4 6'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Why `void 0` exists',
        body: [
          'Before ES5, `undefined` was a writable global — you could assign to it and break code that compared against it. `void 0` always produces the real `undefined`, so it was used defensively, and minifiers still emit it because it is shorter.',
          'Modern `undefined` cannot be reassigned in any scope you would care about, so you should write `undefined`. You need to *recognise* `void 0` when reading minified or older code, not write it.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'console.log(typeof 1 + 1, true || false && false, 2 + 3 * 4);',
        ].join('\n'),
        options: [
          '"number1" true 14',
          '2 true 20',
          '"number1" false 14',
          '2 false 20',
        ],
        correct: 0,
        explanation:
          'Three precedence rules in one line. `typeof` is a unary operator and binds tighter than `+`, so `typeof 1 + 1` is `(typeof 1) + 1` — the string `"number"` concatenated with `1`, giving `"number1"`. `&&` binds tighter than `||`, so the second is `true || (false && false)`, which short-circuits to `true`. And `*` binds tighter than `+`, so the third is `2 + (3 * 4)` = `14`.',
      },
    ],
    exercises: [
      {
        id: 'ex-m04-06-a',
        title: 'Predict the logical precedence',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['operators'],
        instructions: 'What does this print?',
        code: [
          'const a = true;',
          'const b = true;',
          'const c = false;',
          '',
          'console.log(a || b && c);',
        ].join('\n'),
        options: ['true', 'false', 'undefined', 'SyntaxError'],
        correct: 0,
        hints: [
          'Which of `&&` and `||` binds more tightly?',
          'Group the expression explicitly, then evaluate.',
        ],
        solution: 'true',
        solutionExplanation:
          '`&&` has higher precedence than `||`, so this groups as `a || (b && c)`. The right side evaluates to `false`, but `a` is `true`, so `||` short-circuits and returns `true` without even needing it. Had the grouping been `(a || b) && c` — which is what many people read at a glance — the answer would be `false`. This is why mixing `&&` and `||` without brackets is worth avoiding even though the parser handles it fine.',
      },
      {
        id: 'ex-m04-06-b',
        title: 'Replace the if with a ternary',
        kind: EXERCISE_KIND.REFACTOR,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['operators', 'clean-code'],
        instructions:
          'Rewrite this function using a ternary so the result can be bound with `const` in a single expression. Behaviour must not change.',
        starterCode: [
          'function accessLevel(user) {',
          '  let level;',
          '  if (user.isAdmin) {',
          '    level = "full";',
          '  } else {',
          '    level = "limited";',
          '  }',
          '  return level;',
          '}',
        ].join('\n'),
        tests: [
          { name: 'admins get full access', body: 'expect(accessLevel({ isAdmin: true })).toBe("full");' },
          { name: 'others get limited access', body: 'expect(accessLevel({ isAdmin: false })).toBe("limited");' },
          { name: 'a missing flag means limited', body: 'expect(accessLevel({})).toBe("limited");' },
          { name: 'uses const rather than let', body: 'expect(/\\blet\\b/.test(accessLevel.toString())).toBe(false);', hidden: true },
        ],
        hints: [
          'The shape is `condition ? valueIfTrue : valueIfFalse`.',
          'Because the value is known at binding time, `const` now works.',
          'You can return the ternary directly without naming it at all.',
        ],
        solution: [
          'function accessLevel(user) {',
          '  const level = user.isAdmin ? "full" : "limited";',
          '  return level;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The ternary is an expression, so its result can be bound immediately — which means `const` works and the variable is never briefly `undefined`. That is the substantive gain over the `if` version, beyond the shorter code. Returning the ternary directly with `return user.isAdmin ? "full" : "limited";` is equally correct and arguably better here, since naming a value used only once on the next line adds little. The hidden test checks that `let` really was eliminated rather than the ternary being bolted onto the old structure.',
      },
      {
        id: 'ex-m04-06-c',
        title: 'Write a grade classifier',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 20,
        topicIds: ['operators'],
        instructions:
          'Write `classify(score)` returning `"A"` for 90 and above, `"B"` for 80–89, `"C"` for 70–79, `"D"` for 60–69, and `"F"` below 60. Return `"Invalid"` for anything that is not a finite number. Use a ternary ladder rather than `if`/`else if`.',
        starterCode: [
          'function classify(score) {',
          '  // A ternary ladder, one condition per line.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'classifies an A', body: 'expect(classify(95)).toBe("A"); expect(classify(90)).toBe("A");' },
          { name: 'classifies a B', body: 'expect(classify(85)).toBe("B"); expect(classify(80)).toBe("B");' },
          { name: 'classifies a C', body: 'expect(classify(75)).toBe("C");' },
          { name: 'classifies a D', body: 'expect(classify(65)).toBe("D");' },
          { name: 'classifies an F', body: 'expect(classify(30)).toBe("F"); expect(classify(0)).toBe("F");' },
          { name: 'rejects non-numbers', body: 'expect(classify("90")).toBe("Invalid"); expect(classify(null)).toBe("Invalid");' },
          { name: 'rejects NaN', body: 'expect(classify(NaN)).toBe("Invalid");' },
          { name: 'handles scores above 100', body: 'expect(classify(120)).toBe("A");', hidden: true },
        ],
        hints: [
          'Check validity first with `Number.isFinite`, before any comparison.',
          'Order the boundaries from highest to lowest so each `>=` only needs one condition.',
          'Format it as a ladder: `cond ? val : cond ? val : ... : fallback`.',
        ],
        solution: [
          'function classify(score) {',
          '  return !Number.isFinite(score) ? "Invalid"',
          '    : score >= 90 ? "A"',
          '    : score >= 80 ? "B"',
          '    : score >= 70 ? "C"',
          '    : score >= 60 ? "D"',
          '    : "F";',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Checking validity first matters more than it looks: every comparison involving `NaN` is false, so without that guard `NaN` would fall all the way through the ladder and be classified as `"F"` rather than `"Invalid"`. Ordering the boundaries from highest to lowest means each branch needs only a lower bound — once `score >= 90` has failed, a later `score >= 80` implicitly means 80–89. The ladder formatting, with the `:` starting each line, is what keeps a nested ternary readable; the same code on one line would be genuinely hard to follow.',
      },
    ],
    quiz: {
      id: 'qz-m04-06',
      questions: [
        {
          id: 'q-m04-06-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['operators'],
          prompt: 'Why can a ternary be used where an `if` cannot?',
          options: [
            'Because it is shorter',
            'Because it is an expression that produces a value, while `if` is a statement',
            'Because it runs faster',
            'Because it does not need brackets',
          ],
          correct: 1,
          optionExplanations: [
            'Brevity is a side benefit, not the reason.',
            'Correct — only expressions can appear where a value is expected.',
            'Performance is not the distinction.',
            'Bracket usage is irrelevant.',
          ],
          explanation:
            'Only expressions can appear where a value is required — on the right of `=`, inside a template literal, as a function argument. `if` is a statement and produces no value, so it cannot go in those positions. The practical benefit is that a ternary lets you bind with `const` in one step.',
        },
        {
          id: 'q-m04-06-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['operators'],
          prompt: 'What does this precedence puzzle produce?',
          code: ['console.log(typeof 1 + 1);'].join('\n'),
          options: ['2', '"number1"', '"number"', 'NaN'],
          correct: 1,
          optionExplanations: [
            'That would require `typeof (1 + 1)`.',
            'Correct — `typeof` binds tighter, then the result is concatenated.',
            'The `+ 1` is not discarded.',
            'Concatenation succeeds, so nothing is NaN.',
          ],
          explanation:
            '`typeof` is a unary operator and binds more tightly than `+`, so this is `(typeof 1) + 1`. That is the string `"number"` concatenated with `1`, giving `"number1"`. `typeof (1 + 1)` would give `"number"`.',
        },
        {
          id: 'q-m04-06-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['operators', 'objects'],
          prompt: 'What is the difference between `delete obj.x` and `obj.x = undefined`?',
          options: [
            'They are identical',
            '`delete` removes the key entirely, so `in` and `Object.keys` no longer see it; assigning `undefined` leaves the key present',
            '`delete` only works on arrays',
            'Assigning `undefined` throws in strict mode',
          ],
          correct: 1,
          optionExplanations: [
            'They differ in whether the key survives.',
            'Correct — one removes the property, the other blanks its value.',
            '`delete` works on any object property.',
            'Assigning `undefined` is perfectly legal.',
          ],
          explanation:
            '`delete` removes the property, so `"x" in obj` becomes false and `Object.keys` no longer lists it. Assigning `undefined` keeps the key with an undefined value, so both still report it. On arrays, `delete` leaves a hole without changing `length` — use `splice` or `filter` instead.',
        },
        {
          id: 'q-m04-06-4',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['operators'],
          prompt: 'Which of these are right-associative? (Select all that apply.)',
          options: ['`**`', '`-`', 'the ternary `? :`', '`=` assignment'],
          correct: [0, 2, 3],
          explanation:
            'Almost every operator is left-associative, matching reading order. The three exceptions are exponentiation (`2 ** 3 ** 2` is `2 ** (3 ** 2)`), the ternary (which is why ladders chain correctly to the right), and assignment (which is why `a = b = 5` works). Subtraction is left-associative: `10 - 3 - 2` is `(10 - 3) - 2`.',
        },
      ],
    },
    summary:
      'The ternary is the expression form of a conditional, which is why it can appear where `if` cannot and why it lets you bind a result with `const` in one step; formatted as a one-condition-per-line ladder it stays readable, but the moment a branch needs side effects you have outgrown it. Precedence decides binding without brackets, and five relationships cover most cases: member access beats everything, unary beats arithmetic, arithmetic beats comparison, comparison beats logical, logical beats assignment. The trap worth remembering is that `&&` binds tighter than `||`. Associativity resolves ties and is left for almost everything, with `**`, the ternary and assignment as the right-associative exceptions. `delete` removes a key entirely rather than blanking it, `void 0` is a legacy way to produce `undefined`, and the comma operator returns its last expression.',
    keyTakeaways: [
      'The ternary is an expression, so it works where `if` cannot and enables `const`',
      'Format nested ternaries as a ladder; switch to `if` when branches need side effects',
      '`&&` binds tighter than `||` — bracket them when mixed',
      'Unary operators such as `typeof` bind tighter than arithmetic',
      '`**`, the ternary and assignment are the right-associative exceptions',
      '`delete` removes the key; assigning `undefined` merely blanks the value',
    ],
    relatedLessons: ['l-m04-04', 'l-m04-05'],
    interviewConnections: [
      'What is the difference between an expression and a statement?',
      'Does && or || have higher precedence?',
      'What does typeof 1 + 1 evaluate to?',
      'What is the difference between delete obj.x and obj.x = undefined?',
    ],
  },
];
