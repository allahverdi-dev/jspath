import { SECTION, CALLOUT_TONE, DIFFICULTY, EXERCISE_KIND, QUIZ_KIND } from '../../schema/types.js';

const M = 'm08';

export default [
  /* ================================================================== */
  {
    id: 'l-m08-01',
    slug: 'why-functions-exist',
    moduleId: M,
    order: 1,
    title: 'Why Functions Exist',
    description: 'The problem functions solve, how to declare and call one, and what actually happens when you do.',
    difficulty: DIFFICULTY.BEGINNER,
    estimatedMinutes: 14,
    xp: 25,
    topicIds: ['functions'],
    prerequisites: ['l-m07-03'],
    learningObjectives: [
      'Explain what problem functions solve',
      'Declare a function and call it',
      'Describe what happens when a function is invoked',
      'Recognise that declaring and calling are separate events',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Everything you have written so far runs once, top to bottom. That is fine for a script that does one job — but real programs need to do the same job many times, with different data, from different places.',
          'A **function** is a named, reusable block of instructions. You describe the work once, then run it whenever you need it.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The problem, without functions',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The same three lines of logic, written out three times.',
        code: [
          'const priceA = 20;',
          'const taxA = priceA * 0.2;',
          'const totalA = priceA + taxA;',
          'console.log("Total A:", totalA);',
          '',
          'const priceB = 35;',
          'const taxB = priceB * 0.2;',
          'const totalB = priceB + taxB;',
          'console.log("Total B:", totalB);',
          '',
          'const priceC = 8;',
          'const taxC = priceC * 0.2;',
          'const totalC = priceC + taxC;',
          'console.log("Total C:", totalC);',
        ].join('\n'),
        output: ['Total A: 24', 'Total B: 42', 'Total C: 9.6'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This works, and it is bad code — not because it is long, but because the tax rule now lives in **three places**. When the rate changes to 22%, you must find and edit every copy. Miss one and you have a bug that only appears for certain products.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The same thing with a function',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The rule is written once and used three times.',
        code: [
          'function calculateTotal(price) {',
          '  const tax = price * 0.2;',
          '  return price + tax;',
          '}',
          '',
          'console.log("Total A:", calculateTotal(20));',
          'console.log("Total B:", calculateTotal(35));',
          'console.log("Total C:", calculateTotal(8));',
        ].join('\n'),
        output: ['Total A: 24', 'Total B: 42', 'Total C: 9.6'].join('\n'),
      },
      {
        kind: SECTION.LIST,
        items: [
          '**One place to change.** Update the rate once and every caller gets the new behaviour.',
          '**A name that explains intent.** `calculateTotal(20)` says what is happening; three lines of arithmetic make the reader work it out.',
          '**Testable in isolation.** You can check `calculateTotal` on its own, which you cannot do with inline arithmetic.',
          '**Usable from anywhere.** Other code can call it without copying it.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Anatomy of a declaration',
      },
      {
        kind: SECTION.ANNOTATED_CODE,
        language: 'javascript',
        code: [
          'function calculateTotal(price) {',
          '  const tax = price * 0.2;',
          '  return price + tax;',
          '}',
        ].join('\n'),
        annotations: [
          { line: 1, text: 'The `function` keyword begins a **function declaration**.' },
          { line: 1, text: '`calculateTotal` is the name. Same identifier rules as a variable — camelCase, and conventionally a verb phrase because a function *does* something.' },
          { line: 1, text: '`(price)` lists the **parameters** — the names this function will use for the values it receives.' },
          { line: 1, text: 'The `{` opens the **body**: the instructions that run each time the function is called.' },
          { line: 3, text: '`return` sends a value back to whoever called the function, and immediately stops the function.' },
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Declaring is not running',
        body: [
          'A declaration only *describes* the work. Nothing inside the body executes until the function is **called**, which you do by writing its name followed by parentheses: `calculateTotal(20)`.',
          'The parentheses are the call operator. Without them you are referring to the function itself rather than running it — a distinction that matters enormously with callbacks, and which lesson 4 covers properly.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Declaring produces no output. Only calling does.',
        code: [
          'function announce() {',
          '  console.log("The function ran");',
          '}',
          '',
          'console.log("Declared, but not yet called");',
          '',
          'announce();',
          'announce();',
        ].join('\n'),
        output: ['Declared, but not yet called', 'The function ran', 'The function ran'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'What happens during a call',
      },
      {
        kind: SECTION.STEPS,
        steps: [
          {
            title: '1. Execution jumps into the function',
            body: 'The line that called it pauses. Control transfers to the first line of the function body.',
          },
          {
            title: '2. Parameters receive the arguments',
            body: 'The values you passed are bound to the parameter names, as if they had been declared inside the function.',
          },
          {
            title: '3. The body runs, top to bottom',
            body: 'Exactly like any other code — the same rules of order, scope and control flow you already know.',
          },
          {
            title: '4. `return` sends a value back and exits',
            body: 'Execution resumes at the exact point that called the function, and the call expression is replaced by the returned value.',
          },
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Watch the order: the caller pauses, the function runs, the caller resumes.',
        code: [
          'function double(n) {',
          '  console.log("  inside double, n is", n);',
          '  return n * 2;',
          '}',
          '',
          'console.log("before the call");',
          'const result = double(5);',
          'console.log("after the call, result is", result);',
        ].join('\n'),
        output: [
          'before the call',
          '  inside double, n is 5',
          'after the call, result is 10',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Name functions after what they do',
        body: [
          'A function performs an action, so its name should usually be a verb phrase: `calculateTotal`, `fetchUser`, `validateEmail`, `formatDate`.',
          'The exception is a function that answers a yes-or-no question, which reads better with the boolean naming from Module 01: `isValid`, `hasPermission`, `canEdit`. Then `if (isValid(input))` reads as a sentence.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'function greet() {',
          '  console.log("hello");',
          '}',
          '',
          'greet;',
          'greet();',
        ].join('\n'),
        options: [
          'Prints "hello" once',
          'Prints "hello" twice',
          'Prints nothing',
          'Throws a TypeError',
        ],
        correct: 0,
        explanation:
          'Line 5 is `greet` with no parentheses. That evaluates to the function value, does nothing with it, and discards it — no call happens, so nothing prints. Line 6 has the call operator `()`, so the body runs and prints once. This one-character difference is behind a great many bugs with event handlers and callbacks, which lesson 4 covers in detail.',
      },
    ],
    exercises: [
      {
        id: 'ex-m08-01-a',
        title: 'Declaring versus calling',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['functions'],
        instructions: 'What does this print?',
        code: [
          'function shout() {',
          '  console.log("HEY");',
          '}',
        ].join('\n'),
        options: ['HEY', 'Nothing', 'undefined', 'shout'],
        correct: 1,
        hints: [
          'A declaration describes the work. What actually triggers it?',
        ],
        solution: 'Nothing',
        solutionExplanation:
          'The function is declared but never called, so its body never runs and nothing is printed. A declaration only creates the function; the parentheses `shout()` are what execute it. Beginners frequently write a perfectly correct function, see no output, and assume the function is broken — when in fact it was simply never invoked.',
      },
      {
        id: 'ex-m08-01-b',
        title: 'Write your first function',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 15,
        topicIds: ['functions'],
        instructions:
          'Write a function `greet(name)` that returns a greeting of exactly the form `"Hello, Ada!"`. It must return the string, not log it.',
        starterCode: [
          'function greet(name) {',
          '  // Build and return the greeting.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'greets by name', body: 'expect(greet("Ada")).toBe("Hello, Ada!");' },
          { name: 'works with a different name', body: 'expect(greet("Grace")).toBe("Hello, Grace!");' },
          { name: 'handles an empty name', body: 'expect(greet("")).toBe("Hello, !");' },
          { name: 'returns rather than logs', body: 'expect(typeof greet("X")).toBe("string");', hidden: true },
        ],
        hints: [
          'A template literal makes the spacing visible: `` `Hello, ${name}!` ``.',
          'Use `return`, not `console.log` — the tests inspect the returned value.',
        ],
        solution: [
          'function greet(name) {',
          '  return `Hello, ${name}!`;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The template literal keeps the comma, space and exclamation mark visible exactly where they will appear, which is why it is preferred over `"Hello, " + name + "!"` where a missing space is easy to overlook. The hidden test checks the function returns a string: a version using `console.log` would print the right text but return `undefined`, and every test would fail. Lesson 3 covers that distinction in full.',
      },
      {
        id: 'ex-m08-01-c',
        title: 'Remove the repetition',
        kind: EXERCISE_KIND.REFACTOR,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 20,
        topicIds: ['functions', 'clean-code'],
        instructions:
          'This code repeats the same calculation three times. Write a function `areaOf(width, height)` that returns the area, so the rule lives in one place. The tests call your function directly.',
        starterCode: [
          '// const area1 = 3 * 4;',
          '// const area2 = 5 * 6;',
          '// const area3 = 2 * 8;',
          '',
          'function areaOf(width, height) {',
          '  // Return the area.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'computes a simple area', body: 'expect(areaOf(3, 4)).toBe(12);' },
          { name: 'computes another area', body: 'expect(areaOf(5, 6)).toBe(30);' },
          { name: 'handles a zero dimension', body: 'expect(areaOf(0, 8)).toBe(0);' },
          { name: 'handles decimals', body: 'expect(areaOf(2.5, 4)).toBe(10);' },
          { name: 'handles a single unit', body: 'expect(areaOf(1, 1)).toBe(1);', hidden: true },
        ],
        hints: [
          'The body is a single `return` statement.',
          'Multiply the two parameters together.',
        ],
        solution: [
          'function areaOf(width, height) {',
          '  return width * height;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'One line replaces three repeated expressions, and now the rule has a name that says what it computes. The real gain is future change: if areas ever need rounding, or a unit suffix, or validation, there is exactly one place to add it. This is the core argument for functions — not that they save typing, but that they give a behaviour a single home.',
      },
    ],
    quiz: {
      id: 'qz-m08-01',
      questions: [
        {
          id: 'q-m08-01-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['functions'],
          prompt: 'What is the main reason to extract repeated logic into a function?',
          options: [
            'It makes the file shorter',
            'The behaviour gets one definition, so a change happens in one place instead of several',
            'Functions run faster than inline code',
            'It is required by the language',
          ],
          correct: 1,
          optionExplanations: [
            'Shorter is a side effect; a one-line function used once is still often worthwhile.',
            'Correct — a single home for a rule is what prevents inconsistent copies.',
            'There is no performance benefit; a call has a small cost if anything.',
            'Nothing in JavaScript requires functions.',
          ],
          explanation:
            'Duplicated logic means a rule lives in several places, and changing it means finding every copy. A function gives the rule one definition, one name, and one place to edit or test.',
        },
        {
          id: 'q-m08-01-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['functions'],
          prompt: 'How many times does this print?',
          code: [
            'function run() {',
            '  console.log("x");',
            '}',
            'run();',
            'run();',
            'run;',
          ].join('\n'),
          options: ['Once', 'Twice', 'Three times', 'Not at all'],
          correct: 1,
          optionExplanations: [
            'There are two calls with parentheses.',
            'Correct — the third line has no call operator.',
            '`run;` without parentheses does not invoke anything.',
            'The two calls definitely execute.',
          ],
          explanation:
            'Only `run()` invokes the function. The bare `run;` evaluates to the function value and discards it, running nothing. The parentheses are the call operator.',
        },
        {
          id: 'q-m08-01-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['functions'],
          prompt: 'What happens to the calling code while a function runs?',
          options: [
            'It continues in parallel',
            'It pauses at the call, and resumes with the returned value once the function finishes',
            'It restarts from the top',
            'It is discarded',
          ],
          correct: 1,
          optionExplanations: [
            'Parallel execution is not how a normal function call works.',
            'Correct — control transfers in, then returns to the exact same point.',
            'Nothing restarts.',
            'The caller resumes normally.',
          ],
          explanation:
            'A call transfers control into the function body. When the function returns, execution resumes at exactly the point of the call, and the call expression is replaced by the returned value.',
        },
      ],
    },
    summary:
      'A function is a named, reusable block of instructions. Its value is not that it saves typing but that it gives a rule a single definition — one place to change, one name that explains intent, and something testable in isolation. A declaration only describes the work; nothing runs until the function is **called** with the parentheses operator, and a bare reference without parentheses runs nothing at all. During a call, execution transfers into the body, parameters are bound to the arguments passed, the body runs top to bottom, and `return` sends a value back and resumes the caller at exactly the point it paused.',
    keyTakeaways: [
      'Functions give a rule one definition, one name and one place to change',
      'Declaring describes the work; calling with `()` runs it',
      '`greet` refers to the function; `greet()` invokes it',
      'A call pauses the caller, runs the body, then resumes with the returned value',
      'Name functions as verb phrases, or as questions when they return a boolean',
    ],
    relatedLessons: ['l-m07-03', 'l-m08-02'],
    interviewConnections: [
      'Why use functions instead of repeating code?',
      'What is the difference between referring to a function and calling it?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m08-02',
    slug: 'parameters-and-arguments',
    moduleId: M,
    order: 2,
    title: 'Parameters and Arguments',
    description: 'The distinction interviewers ask about, plus what happens when you pass too few or too many values.',
    difficulty: DIFFICULTY.BEGINNER,
    estimatedMinutes: 15,
    xp: 30,
    topicIds: ['functions'],
    prerequisites: ['l-m08-01'],
    learningObjectives: [
      'State the difference between a parameter and an argument',
      'Predict what happens when an argument is missing',
      'Explain why extra arguments are silently ignored',
      'Explain that parameters are local to the function',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'These two words are used interchangeably in conversation and mean genuinely different things. The distinction is a common interview warm-up, and getting it right also makes error messages easier to read.',
        ],
      },
      {
        kind: SECTION.TERMS,
        terms: [
          { term: 'Parameter', definition: 'A name listed in the function **declaration**. It is an empty slot — it has no value until the function is called.' },
          { term: 'Argument', definition: 'An actual value passed in at the **call site**. Arguments fill the parameter slots.' },
        ],
      },
      {
        kind: SECTION.ANNOTATED_CODE,
        language: 'javascript',
        code: [
          'function add(a, b) {',
          '  return a + b;',
          '}',
          '',
          'const total = add(3, 7);',
        ].join('\n'),
        annotations: [
          { line: 1, text: '`a` and `b` are **parameters** — names declared as part of the function.' },
          { line: 5, text: '`3` and `7` are **arguments** — the concrete values supplied when calling.' },
          { line: 5, text: 'A useful memory aid: **P**arameters are in the **P**rototype/declaration; **A**rguments are **A**ctual values.' },
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Parameters are local variables',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'A parameter behaves exactly like a variable declared at the top of the function body. It exists only inside the function, and reassigning it has no effect on the caller.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The parameter is a separate binding from the caller\'s variable.',
        code: [
          'function increase(n) {',
          '  n = n + 100;',
          '  return n;',
          '}',
          '',
          'const value = 5;',
          'const result = increase(value);',
          '',
          'console.log("returned:", result);',
          'console.log("original:", value);',
          '',
          '// The parameter name does not exist outside the function',
          'try {',
          '  console.log(n);',
          '} catch (error) {',
          '  console.log(error.name + ":", error.message);',
          '}',
        ].join('\n'),
        output: [
          'returned: 105',
          'original: 5',
          'ReferenceError: n is not defined',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Objects are the exception worth remembering',
        body: [
          'Reassigning a parameter never affects the caller, whatever its type. But if the argument was an **object**, the parameter refers to the same object, so mutating a property *is* visible to the caller.',
          'This is the pass-by-value-of-a-reference behaviour from Module 02. Lesson 6 of this module returns to it, and Module 14 covers it in depth.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Too few arguments',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A missing argument is `undefined` — not an error.',
        code: [
          'function describe(name, role) {',
          '  return `${name} is a ${role}`;',
          '}',
          '',
          'console.log(describe("Ada", "engineer"));',
          'console.log(describe("Ada"));',
          'console.log(describe());',
        ].join('\n'),
        output: [
          'Ada is a engineer',
          'Ada is a undefined',
          'undefined is a undefined',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'JavaScript does not check argument counts. A parameter with no corresponding argument is simply `undefined`, and the function runs on regardless — producing text like `"Ada is a undefined"` rather than complaining.',
          'This is a deliberate design choice with a real cost: mistakes surface far from their source. The defences are default parameters, which lesson 5 covers, and validating inputs at the top of the function with the guard clauses from Module 07.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Failing loudly instead of producing nonsense.',
        code: [
          'function describe(name, role) {',
          '  if (name === undefined) throw new Error("name is required");',
          '  if (role === undefined) throw new Error("role is required");',
          '',
          '  return `${name} is a ${role}`;',
          '}',
          '',
          'console.log(describe("Ada", "engineer"));',
          '',
          'try {',
          '  describe("Ada");',
          '} catch (error) {',
          '  console.log("Error:", error.message);',
          '}',
        ].join('\n'),
        output: ['Ada is a engineer', 'Error: role is required'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Too many arguments',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Extra arguments are accepted and ignored.',
        code: [
          'function add(a, b) {',
          '  return a + b;',
          '}',
          '',
          'console.log(add(1, 2));',
          'console.log(add(1, 2, 3, 4, 5));',
          '',
          '// The function reports how many parameters it declares',
          'console.log("declared parameters:", add.length);',
        ].join('\n'),
        output: ['3', '3', 'declared parameters: 2'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Silently ignored extras cause the `map(parseInt)` bug',
        body: [
          'You met this in Module 03: `["1","2","3"].map(parseInt)` gives `[1, NaN, NaN]`, because `map` passes three arguments — element, index and array — and `parseInt` quietly accepts the index as its radix.',
          'The general lesson: when passing a function as a callback, check how many arguments the caller will supply. A function that accepts more parameters than you expected will receive them.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Order matters, names do not',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Arguments are matched by position only.',
        code: [
          'function divide(numerator, denominator) {',
          '  return numerator / denominator;',
          '}',
          '',
          'console.log(divide(10, 2));',
          'console.log(divide(2, 10));',
          '',
          '// The caller\'s variable names are irrelevant',
          'const denominator = 4;',
          'const numerator = 20;',
          'console.log(divide(denominator, numerator));',
        ].join('\n'),
        output: ['5', '0.2', '0.2'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'That last call passes `4` then `20`, so it computes `4 / 20` — despite the caller\'s variables being named to suggest otherwise. JavaScript matches arguments to parameters strictly by **position**.',
          'This is why functions with many parameters become error-prone: nothing stops you swapping two of the same type. A common professional response is to accept a single object instead, which makes each value self-labelling at the call site — a pattern Module 14 covers once you have met destructuring.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'function sub(a, b) {',
          '  return a - b;',
          '}',
          '',
          'console.log(sub(10, 3), sub(3, 10), sub(10));',
        ].join('\n'),
        options: [
          '7 -7 NaN',
          '7 -7 10',
          '7 7 NaN',
          '7 -7 undefined',
        ],
        correct: 0,
        explanation:
          '`sub(10, 3)` is `10 - 3`, giving 7. `sub(3, 10)` is `3 - 10`, giving `-7` — position determines everything. `sub(10)` supplies no second argument, so `b` is `undefined`; `10 - undefined` converts `undefined` to `NaN` and any arithmetic with `NaN` gives `NaN`. Note it does not throw — the missing argument produces a wrong value rather than an error, which is exactly what makes it hard to trace.',
      },
    ],
    exercises: [
      {
        id: 'ex-m08-02-a',
        title: 'Parameter or argument?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['functions'],
        instructions:
          'In `function scale(factor) { ... }` called as `scale(3)`, which statement is correct?',
        options: [
          '`factor` is the argument and `3` is the parameter',
          '`factor` is the parameter and `3` is the argument',
          'Both are parameters',
          'Both are arguments',
        ],
        correct: 1,
        hints: [
          'One appears in the declaration, one at the call site.',
        ],
        solution: '`factor` is the parameter and `3` is the argument',
        solutionExplanation:
          'Parameters are the names declared as part of the function — they are slots with no value until a call happens. Arguments are the actual values supplied at the call site to fill those slots. A memory aid: **P**arameter appears in the **P**rototype (declaration), **A**rgument is the **A**ctual value.',
      },
      {
        id: 'ex-m08-02-b',
        title: 'Predict the missing argument',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['functions'],
        instructions: 'What does this print?',
        code: [
          'function multiply(a, b) {',
          '  return a * b;',
          '}',
          '',
          'console.log(multiply(5));',
        ].join('\n'),
        options: ['5', 'NaN', 'undefined', 'A TypeError'],
        correct: 1,
        hints: [
          'What value does `b` hold when no second argument is supplied?',
          'What is `5 * undefined`?',
        ],
        solution: 'NaN',
        solutionExplanation:
          'JavaScript never checks argument counts, so `b` is `undefined`. Multiplication converts both operands to numbers, and `Number(undefined)` is `NaN`, so the result is `NaN`. No error is thrown — the function happily returns a meaningless value, which then propagates into whatever uses it. Default parameters or an explicit guard at the top of the function prevent this.',
      },
      {
        id: 'ex-m08-02-c',
        title: 'Convert temperatures',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 20,
        topicIds: ['functions'],
        instructions:
          'Write `toFahrenheit(celsius)` returning the Fahrenheit equivalent, using the formula `F = C × 9/5 + 32`. Return a number.',
        starterCode: [
          'function toFahrenheit(celsius) {',
          '  // F = C * 9/5 + 32',
          '}',
        ].join('\n'),
        tests: [
          { name: 'freezing point', body: 'expect(toFahrenheit(0)).toBe(32);' },
          { name: 'boiling point', body: 'expect(toFahrenheit(100)).toBe(212);' },
          { name: 'body temperature', body: 'expect(toFahrenheit(37)).toBe(98.6);' },
          { name: 'handles negatives', body: 'expect(toFahrenheit(-40)).toBe(-40);' },
          { name: 'returns a number', body: 'expect(typeof toFahrenheit(20)).toBe("number");', hidden: true },
        ],
        hints: [
          'Multiplication and division bind tighter than addition, so the brackets are optional.',
          'Write `celsius * 9 / 5 + 32`.',
          '`-40` is the point where both scales agree — a useful sanity check.',
        ],
        solution: [
          'function toFahrenheit(celsius) {',
          '  return celsius * 9 / 5 + 32;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Operator precedence from Module 04 does the grouping for you: `*` and `/` bind tighter than `+`, so the expression evaluates as `((celsius * 9) / 5) + 32` without needing brackets. The body-temperature test is the interesting one — `37 * 9 / 5` is exactly `66.6`, and adding 32 gives exactly `98.6`, so no floating-point drift appears here. Writing `celsius * (9 / 5)` would compute `1.8` first and could introduce a rounding difference, which is a small reminder that operation order matters with floats.',
      },
      {
        id: 'ex-m08-02-d',
        title: 'Validate login values',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 25,
        topicIds: ['functions', 'control-flow'],
        instructions:
          'Write `validateLogin(username, password)` returning an error string for the first problem found, or `null` if both are acceptable. Rules in order: username missing (`"Username required"`), password missing (`"Password required"`), username shorter than 3 characters (`"Username too short"`), password shorter than 8 characters (`"Password too short"`). A missing argument counts as missing.',
        starterCode: [
          'function validateLogin(username, password) {',
          '  // Guard clauses, in the order the rules are listed.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'accepts valid credentials', body: 'expect(validateLogin("ada", "lovelace123")).toBe(null);' },
          { name: 'rejects a missing username', body: 'expect(validateLogin(undefined, "lovelace123")).toBe("Username required"); expect(validateLogin("", "lovelace123")).toBe("Username required");' },
          { name: 'rejects a missing password', body: 'expect(validateLogin("ada")).toBe("Password required");' },
          { name: 'rejects a short username', body: 'expect(validateLogin("ad", "lovelace123")).toBe("Username too short");' },
          { name: 'rejects a short password', body: 'expect(validateLogin("ada", "short")).toBe("Password too short");' },
          { name: 'reports only the first problem', body: 'expect(validateLogin("ad", "s")).toBe("Username too short");' },
          { name: 'accepts exact boundary lengths', body: 'expect(validateLogin("abc", "12345678")).toBe(null);', hidden: true },
        ],
        hints: [
          '`!username` catches `undefined` and `""` together, since both are falsy.',
          'Order the guards exactly as the rules are listed.',
          'Return `null` only after every guard has passed.',
        ],
        solution: [
          'function validateLogin(username, password) {',
          '  if (!username) return "Username required";',
          '  if (!password) return "Password required";',
          '  if (username.length < 3) return "Username too short";',
          '  if (password.length < 8) return "Password too short";',
          '',
          '  return null;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The guard-clause structure from Module 07 handles this cleanly, and the ordering is what makes the "first problem only" test pass — `validateLogin("ad", "s")` breaks two rules but reports the username one because that guard runs first. Using `!username` deliberately catches `undefined` (missing argument) and `""` (empty input) with one condition, since both are falsy and both mean "nothing supplied". The boundary test confirms the comparisons use `<` rather than `<=`, so exactly 3 and exactly 8 characters are accepted.',
      },
    ],
    quiz: {
      id: 'qz-m08-02',
      questions: [
        {
          id: 'q-m08-02-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['functions'],
          prompt: 'What is the difference between a parameter and an argument?',
          options: [
            'They are two words for the same thing',
            'A parameter is a name in the declaration; an argument is a value passed at the call site',
            'A parameter is a value; an argument is a name',
            'Parameters are optional; arguments are required',
          ],
          correct: 1,
          optionExplanations: [
            'They are used interchangeably in speech but denote different things.',
            'Correct — declaration versus call site.',
            'This has the two reversed.',
            'Both can be omitted, since JavaScript checks no counts.',
          ],
          explanation:
            'Parameters are the named slots declared as part of the function. Arguments are the concrete values supplied when calling it. Arguments fill parameter slots, matched strictly by position.',
        },
        {
          id: 'q-m08-02-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['functions'],
          prompt: 'What is the result when an argument is omitted?',
          code: [
            'function f(a, b) {',
            '  return b;',
            '}',
            'console.log(f(1));',
          ].join('\n'),
          options: ['null', 'undefined', '0', 'A TypeError'],
          correct: 1,
          optionExplanations: [
            '`null` is never produced automatically.',
            'Correct — an unfilled parameter holds `undefined`.',
            'No numeric default exists.',
            'JavaScript does not check argument counts.',
          ],
          explanation:
            'A parameter with no corresponding argument is `undefined`. Nothing is thrown, so the mistake propagates as a wrong value rather than an immediate error — which is why default parameters and input guards matter.',
        },
        {
          id: 'q-m08-02-3',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['functions'],
          prompt: 'What happens to extra arguments?',
          code: [
            'function f(a) {',
            '  return a;',
            '}',
            'console.log(f(1, 2, 3));',
          ].join('\n'),
          options: ['1', '[1, 2, 3]', '6', 'A TypeError'],
          correct: 0,
          optionExplanations: [
            'Correct — extras are accepted and ignored.',
            'Parameters do not collect extras unless declared with rest syntax.',
            'No summing occurs.',
            'Extra arguments are legal.',
          ],
          explanation:
            'Extra arguments are silently accepted and ignored by named parameters. This is exactly why `map(parseInt)` misbehaves — `map` supplies three arguments and `parseInt` quietly uses the second as a radix. Rest parameters, in lesson 5, are how you capture extras deliberately.',
        },
        {
          id: 'q-m08-02-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['functions'],
          prompt: 'Reassigning a parameter inside a function changes the caller’s variable.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'The parameter is a separate binding local to the function.',
            'Correct — reassignment is purely local.',
          ],
          explanation:
            'A parameter behaves like a local variable, so reassigning it re-points only that local name. The caller is unaffected. The nuance is that if the argument was an object, the parameter refers to the same object, so *mutating a property* is visible to the caller — but that is mutation, not reassignment.',
        },
      ],
    },
    summary:
      'Parameters are the names declared as part of a function; arguments are the values supplied at the call site, matched strictly by **position** — the caller\'s variable names are irrelevant. A parameter behaves as a local variable, so reassigning it never affects the caller, although mutating an object passed in does. JavaScript checks no argument counts: a missing argument is `undefined` and the function runs on to produce a wrong value rather than an error, while extra arguments are silently ignored — the mechanism behind the `map(parseInt)` bug. The defences are default parameters and guard clauses that validate inputs at the top of the function.',
    keyTakeaways: [
      'Parameters are in the declaration; arguments are the actual values passed',
      'Arguments match parameters by position, never by name',
      'A missing argument is `undefined` — no error is thrown',
      'Extra arguments are silently ignored',
      'Reassigning a parameter is local; mutating a passed object is not',
    ],
    relatedLessons: ['l-m08-01', 'l-m08-03'],
    interviewConnections: [
      'What is the difference between a parameter and an argument?',
      'What happens if you call a function with too few arguments?',
      'Why does map(parseInt) misbehave?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m08-03',
    slug: 'return-values',
    moduleId: M,
    order: 3,
    title: 'return, and Why It Is Not console.log',
    description: 'The single most common beginner confusion, plus what happens to code written after a return.',
    difficulty: DIFFICULTY.BEGINNER,
    estimatedMinutes: 15,
    xp: 30,
    topicIds: ['functions'],
    prerequisites: ['l-m08-02'],
    learningObjectives: [
      'Explain the difference between returning and logging',
      'Explain what a function with no `return` evaluates to',
      'Recognise that `return` exits the function immediately',
      'Store and reuse a returned value',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'If there is one confusion that costs beginners the most time, it is this one. `console.log` and `return` both seem to "produce" the result — but only one of them gives the value back to your program.',
        ],
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'console.log — shows a human',
          code: [
            'function addLog(a, b) {',
            '  console.log(a + b);',
            '}',
            '',
            'const x = addLog(2, 3);',
            '// Prints 5 to the console',
            '// x is undefined',
            '// The value is gone.',
          ].join('\n'),
        },
        right: {
          title: 'return — gives the program',
          code: [
            'function addReturn(a, b) {',
            '  return a + b;',
            '}',
            '',
            'const y = addReturn(2, 3);',
            '// Prints nothing',
            '// y is 5',
            '// The value can be used.',
          ].join('\n'),
        },
        note: '`console.log` writes text to a developer tool. It has no effect on your program\'s data. `return` hands a value back to the calling code, where it can be stored, passed on, or combined.',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The difference made concrete.',
        code: [
          'function addLog(a, b) {',
          '  console.log(a + b);',
          '}',
          '',
          'function addReturn(a, b) {',
          '  return a + b;',
          '}',
          '',
          'const x = addLog(2, 3);',
          'const y = addReturn(2, 3);',
          '',
          'console.log("x is", x);',
          'console.log("y is", y);',
          '',
          '// Only the returned value can be used in further work',
          'console.log("doubled:", y * 2);',
        ].join('\n'),
        output: ['5', 'x is undefined', 'y is 5', 'doubled: 10'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'The symptom to recognise',
        body: [
          'You write a function, run it, see the right answer printed, and conclude it works. Then you try to use the result and get `undefined` — or, worse, `NaN` once it enters some arithmetic.',
          'If a function\'s output appears in the console but every attempt to use it gives `undefined`, the function is logging when it should be returning. Swap `console.log(...)` for `return ...` and log at the **call site** instead if you still want to see it.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'A function with no return gives `undefined`',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Three ways to end up with `undefined`.',
        code: [
          'function noReturn() {',
          '  const value = 42;',
          '}',
          '',
          'function bareReturn() {',
          '  return;',
          '}',
          '',
          'function conditionalReturn(flag) {',
          '  if (flag) {',
          '    return "yes";',
          '  }',
          '}',
          '',
          'console.log(noReturn());',
          'console.log(bareReturn());',
          'console.log(conditionalReturn(true));',
          'console.log(conditionalReturn(false));',
        ].join('\n'),
        output: ['undefined', 'undefined', 'yes', 'undefined'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The third case is the one that reaches production. `conditionalReturn` returns a useful value on one path and `undefined` on the other — so it works during testing with the happy path and fails later with data that takes the other branch. Make every path return something, or make the absence explicit by returning `null`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: '`return` exits immediately',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Nothing after a return in the same block can run.',
        code: [
          'function demo() {',
          '  console.log("this runs");',
          '  return "done";',
          '  console.log("this never runs");',
          '}',
          '',
          'console.log(demo());',
          '',
          '// Which is exactly what makes guard clauses work',
          'function classify(n) {',
          '  if (n < 0) return "negative";',
          '  if (n === 0) return "zero";',
          '  return "positive";',
          '}',
          '',
          'console.log(classify(-5), classify(0), classify(5));',
        ].join('\n'),
        output: ['this runs', 'done', 'negative zero positive'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Code after a `return` in the same block is **unreachable**. It is not an error — it simply never executes, which is worse, because it looks like it does something. Most linters flag it, and it is usually a sign of an editing mistake.',
          'This same immediacy is what makes guard clauses work: each `return` ends the function, so the code below it only runs when every earlier guard passed.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Never put a newline after `return`',
        body: [
          'Automatic Semicolon Insertion from Module 00 has one genuinely dangerous case, and this is it. A `return` followed by a line break gets a semicolon inserted immediately — so `return` on its own line returns `undefined`, and the expression below becomes unreachable.',
          'This is silent. Keep the returned value on the same line as `return`, and if it is long, wrap it in brackets starting on the `return` line.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The ASI trap, and the fix.',
        code: [
          'function broken() {',
          '  return',
          '    "never reached";',
          '}',
          '',
          'function fixed() {',
          '  return (',
          '    "this works"',
          '  );',
          '}',
          '',
          'console.log(broken());',
          'console.log(fixed());',
        ].join('\n'),
        output: ['undefined', 'this works'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Returning different kinds of value',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A function can return any value — including an object or another function.',
        code: [
          'function getUser() {',
          '  return { name: "Ada", role: "engineer" };',
          '}',
          '',
          'function getScores() {',
          '  return [90, 85, 77];',
          '}',
          '',
          'function isAdult(age) {',
          '  return age >= 18;',
          '}',
          '',
          'const user = getUser();',
          'console.log(user.name);',
          'console.log(getScores().length);',
          'console.log(isAdult(20), isAdult(10));',
        ].join('\n'),
        output: ['Ada', '3', 'true false'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Returning an object is how a function gives back several values at once, since `return` sends exactly one thing. Notice also `getScores().length` — because the call expression is replaced by its returned value, you can use a property or method on it directly without storing it first.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'function check(n) {',
          '  if (n > 10) {',
          '    return "big";',
          '  }',
          '  console.log("small");',
          '}',
          '',
          'const a = check(20);',
          'const b = check(5);',
          '',
          'console.log(a, b);',
        ].join('\n'),
        options: [
          'Prints "small", then "big undefined"',
          'Prints "big undefined"',
          'Prints "small", then "big small"',
          'Prints "small small"',
        ],
        correct: 0,
        explanation:
          '`check(20)` takes the `if` branch and returns `"big"` immediately, so the `console.log("small")` below is never reached — `a` is `"big"`. `check(5)` skips the `if`, reaches the log which prints `"small"`, then falls off the end of the function with no `return`, so `b` is `undefined`. The final line then prints `big undefined`. The ordering matters: `"small"` is printed during the second call, before the last line runs.',
      },
    ],
    exercises: [
      {
        id: 'ex-m08-03-a',
        title: 'Predict the logged function result',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['functions'],
        instructions: 'What is the value of `result`?',
        code: [
          'function sum(a, b) {',
          '  console.log(a + b);',
          '}',
          '',
          'const result = sum(2, 3);',
          'console.log("result:", result);',
        ].join('\n'),
        options: ['5', 'undefined', 'null', 'NaN'],
        correct: 1,
        hints: [
          'Does the function contain a `return` statement?',
          'What does a function without a return evaluate to?',
        ],
        solution: 'undefined',
        solutionExplanation:
          'The function prints `5` to the console but never returns anything, so the call evaluates to `undefined` and that is what `result` holds. Output appears — which is exactly why this is so confusing — but the value never reaches the program. The fix is `return a + b;`, and logging at the call site if you still want to see it.',
      },
      {
        id: 'ex-m08-03-b',
        title: 'Fix the logging function',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 20,
        topicIds: ['functions'],
        instructions:
          'This function prints the correct discount but callers always receive `undefined`. Fix it so the value is returned.',
        starterCode: [
          'function applyDiscount(price, percent) {',
          '  const discount = price * (percent / 100);',
          '  console.log(price - discount);',
          '}',
        ].join('\n'),
        tests: [
          { name: 'applies a 10% discount', body: 'expect(applyDiscount(100, 10)).toBe(90);' },
          { name: 'applies a 25% discount', body: 'expect(applyDiscount(200, 25)).toBe(150);' },
          { name: 'handles a zero discount', body: 'expect(applyDiscount(50, 0)).toBe(50);' },
          { name: 'handles a full discount', body: 'expect(applyDiscount(80, 100)).toBe(0);' },
          { name: 'returns a number', body: 'expect(typeof applyDiscount(10, 5)).toBe("number");', hidden: true },
        ],
        hints: [
          'The calculation is already correct — only the final statement is wrong.',
          'Replace `console.log(...)` with `return ...`.',
        ],
        solution: [
          'function applyDiscount(price, percent) {',
          '  const discount = price * (percent / 100);',
          '  return price - discount;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'One keyword changes everything. The arithmetic was right all along, which is what makes this bug so persistent — the console shows the correct number, so the function looks like it works. Only when a caller tries to use the result does the `undefined` appear, often several steps downstream where the cause is no longer obvious. If you still want to see the value while developing, log it at the call site: `console.log(applyDiscount(100, 10))`.',
      },
      {
        id: 'ex-m08-03-c',
        title: 'Return a status message',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['functions', 'control-flow'],
        instructions:
          'Write `stockStatus(quantity)` returning `"Out of stock"` for 0, `"Low stock"` for 1 to 5, and `"In stock"` for more than 5. Every path must return a string — no path may fall off the end.',
        starterCode: [
          'function stockStatus(quantity) {',
          '  // Every branch must return.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'reports out of stock', body: 'expect(stockStatus(0)).toBe("Out of stock");' },
          { name: 'reports low stock at the lower bound', body: 'expect(stockStatus(1)).toBe("Low stock");' },
          { name: 'reports low stock at the upper bound', body: 'expect(stockStatus(5)).toBe("Low stock");' },
          { name: 'reports in stock', body: 'expect(stockStatus(6)).toBe("In stock"); expect(stockStatus(100)).toBe("In stock");' },
          { name: 'never returns undefined', body: 'for (let i = 0; i <= 20; i++) expect(typeof stockStatus(i)).toBe("string");', hidden: true },
        ],
        hints: [
          'Handle the most specific case first, as in Module 07.',
          'Three branches, each with its own `return`.',
          'The final `return` needs no condition — it catches everything left.',
        ],
        solution: [
          'function stockStatus(quantity) {',
          '  if (quantity === 0) return "Out of stock";',
          '  if (quantity <= 5) return "Low stock";',
          '  return "In stock";',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Because each `return` exits immediately, the second condition only ever sees quantities of 1 or more — so `quantity <= 5` implicitly means "between 1 and 5" without needing to state the lower bound. That implicit narrowing is the benefit of ordered guards. The final `return` has no condition because everything reaching it has already failed both earlier checks. The hidden test sweeps the whole range to prove no input falls through to an implicit `undefined`, which is the failure mode this exercise is really about.',
      },
      {
        id: 'ex-m08-03-d',
        title: 'Calculate a cart total',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 25,
        topicIds: ['functions'],
        instructions:
          'Write `cartTotal(price, quantity, taxRate)` returning the final total: subtotal plus tax. `taxRate` is a decimal such as `0.2` for 20%. Round the result to two decimal places and return it as a **number**, not a string.',
        starterCode: [
          'function cartTotal(price, quantity, taxRate) {',
          '  // Math.round(value * 100) / 100 rounds to 2 places and keeps a number.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'computes a simple total', body: 'expect(cartTotal(10, 2, 0.2)).toBe(24);' },
          { name: 'handles a zero tax rate', body: 'expect(cartTotal(10, 3, 0)).toBe(30);' },
          { name: 'handles a zero quantity', body: 'expect(cartTotal(10, 0, 0.2)).toBe(0);' },
          { name: 'rounds to two decimal places', body: 'expect(cartTotal(9.99, 3, 0.2)).toBe(35.96);' },
          { name: 'returns a number, not a string', body: 'expect(typeof cartTotal(5, 1, 0.1)).toBe("number");' },
          { name: 'handles a decimal-heavy case', body: 'expect(cartTotal(19.99, 7, 0.175)).toBe(164.42);', hidden: true },
        ],
        hints: [
          'Subtotal is `price * quantity`.',
          'Tax is the subtotal multiplied by the rate — not the unit price.',
          '`toFixed(2)` returns a string, so it would fail the type test. Use `Math.round(x * 100) / 100`.',
        ],
        solution: [
          'function cartTotal(price, quantity, taxRate) {',
          '  const subtotal = price * quantity;',
          '  const total = subtotal * (1 + taxRate);',
          '  return Math.round(total * 100) / 100;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Multiplying by `(1 + taxRate)` computes the subtotal and its tax in one step, which avoids a second rounding opportunity. The rounding idiom from Module 06 — multiply by 100, round, divide by 100 — is used rather than `toFixed(2)` because `toFixed` returns a *string* and would fail the type test. Rounding once at the end rather than at each step keeps the error minimal; the hidden test with an awkward rate exists to catch an implementation that rounds too early or computes tax from the unit price instead of the subtotal.',
      },
    ],
    quiz: {
      id: 'qz-m08-03',
      questions: [
        {
          id: 'q-m08-03-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['functions'],
          prompt: 'What is the essential difference between `return` and `console.log`?',
          options: [
            'They are the same; `return` is just newer',
            '`return` hands a value back to the calling code; `console.log` writes text to a developer tool and affects no data',
            '`console.log` is faster',
            '`return` only works with strings',
          ],
          correct: 1,
          optionExplanations: [
            'They serve entirely different purposes.',
            'Correct — one produces a value, the other produces output for a human.',
            'Speed is irrelevant to the distinction.',
            '`return` works with any value.',
          ],
          explanation:
            '`console.log` is a debugging tool that displays text; it has no effect on your program\'s data. `return` gives a value back to the caller, where it can be stored and used. A function that logs instead of returning appears to work while giving every caller `undefined`.',
        },
        {
          id: 'q-m08-03-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['functions'],
          prompt: 'Why does this return undefined?',
          code: [
            'function f() {',
            '  return',
            '    42;',
            '}',
            'console.log(f());',
          ].join('\n'),
          options: ['42', 'undefined', 'A SyntaxError', 'null'],
          correct: 1,
          optionExplanations: [
            'The 42 is never reached.',
            'Correct — a semicolon is inserted right after `return`.',
            'The code parses fine, which is what makes it dangerous.',
            '`null` is never produced automatically.',
          ],
          explanation:
            'Automatic Semicolon Insertion adds a semicolon immediately after a `return` followed by a line break, so the function returns `undefined` and `42;` becomes unreachable. Always keep the returned value on the same line, or open a bracket on the `return` line.',
        },
        {
          id: 'q-m08-03-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['functions'],
          prompt: 'What happens to code written after a `return` in the same block?',
          options: [
            'It runs before the return',
            'It never executes — it is unreachable',
            'It causes a SyntaxError',
            'It runs after the caller finishes',
          ],
          correct: 1,
          optionExplanations: [
            'Execution reaches the `return` first and leaves.',
            'Correct — and it is not an error, merely dead code.',
            'It parses perfectly well.',
            'Nothing resumes inside the function.',
          ],
          explanation:
            '`return` exits the function immediately, so anything below it in the same block is unreachable. It is not an error, which is why linters flag it — dead code that looks active is misleading. This same immediacy is what makes guard clauses work.',
        },
        {
          id: 'q-m08-03-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['functions'],
          prompt: 'A function can return more than one value using several `return` statements.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'The first `return` reached ends the function.',
            'Correct — a function returns exactly one value per call.',
          ],
          explanation:
            'A function may *contain* many `return` statements, but only one executes per call and it ends the function immediately. To hand back several values, return a single object or array containing them.',
        },
      ],
    },
    summary:
      '`console.log` displays text for a human and has no effect on your data; `return` hands a value back to the calling code where it can be stored and used. A function that logs instead of returning appears to work while giving every caller `undefined` — the single most common beginner confusion. A function with no `return`, a bare `return`, or a conditional `return` that misses a path all evaluate to `undefined`, and the third is the one that reaches production. `return` exits immediately, making code below it unreachable and making guard clauses work. Beware the ASI trap: a line break directly after `return` silently returns `undefined`.',
    keyTakeaways: [
      '`return` gives a value to the program; `console.log` shows text to a person',
      'No return, bare return, or an unhandled branch all give `undefined`',
      '`return` exits the function immediately — later code is unreachable',
      'Never put a line break directly after `return`',
      'Return an object to hand back several values at once',
    ],
    relatedLessons: ['l-m08-02', 'l-m08-04'],
    interviewConnections: [
      'What is the difference between return and console.log?',
      'What does a function without a return statement evaluate to?',
      'Why does `return` followed by a newline return undefined?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m08-04',
    slug: 'function-expressions',
    moduleId: M,
    order: 4,
    title: 'Function Expressions and Functions as Values',
    description: 'Declarations versus expressions, anonymous functions, and the idea that unlocks callbacks.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 16,
    xp: 35,
    topicIds: ['functions'],
    prerequisites: ['l-m08-03'],
    learningObjectives: [
      'Write a function expression and explain how it differs from a declaration',
      'Explain why a declaration can be called before its definition and an expression cannot',
      'Store functions in variables, arrays and object properties',
      'Distinguish passing a function from passing its result',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'You met in Module 02 that functions are values. This lesson makes that practical, because it is the idea everything from array methods to event handlers to promises is built on.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Two ways to create a function',
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Function declaration',
          code: [
            'function double(n) {',
            '  return n * 2;',
            '}',
            '',
            '// A statement.',
            '// Creates a binding named',
            '// `double` in the scope.',
          ].join('\n'),
        },
        right: {
          title: 'Function expression',
          code: [
            'const double = function (n) {',
            '  return n * 2;',
            '};',
            '',
            '// An expression assigned',
            '// to a variable. Note the',
            '// semicolon — it is a',
            '// variable declaration.',
          ].join('\n'),
        },
        note: 'Both produce a function you call the same way: `double(5)`. The difference is *when* the function becomes usable, which the next section covers.',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The function in the right-hand column has no name after the `function` keyword — it is an **anonymous function**. It does not need a name, because the variable already provides one for calling it.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The timing difference',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A declaration can be called before it appears; an expression cannot.',
        code: [
          '// Declarations are usable throughout their scope',
          'console.log(declared(5));',
          '',
          'function declared(n) {',
          '  return n * 2;',
          '}',
          '',
          '// Expressions are not usable until the assignment runs',
          'try {',
          '  console.log(expressed(5));',
          '} catch (error) {',
          '  console.log(error.name + ":", error.message);',
          '}',
          '',
          'const expressed = function (n) {',
          '  return n * 2;',
          '};',
          '',
          'console.log(expressed(5));',
        ].join('\n'),
        output: [
          '10',
          "ReferenceError: Cannot access 'expressed' before initialization",
          '10',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'A **function declaration** is fully created before any code in its scope runs, so it can be called from anywhere in that scope — including lines above it.',
          'A **function expression** assigned to `const` follows the ordinary rules for `const`. The binding exists from the start of the scope but is uninitialized until the assignment line runs, so calling it earlier throws — the Temporal Dead Zone you met in Module 01. Module 10 covers the mechanism in full.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INTERVIEW,
        title: 'Declaration vs expression — a complete answer',
        body: [
          '"A declaration creates its function before the surrounding code runs, so it can be called from anywhere in the scope, including above its definition. An expression is just a value assigned to a variable, so it follows that variable\'s rules — with `const` or `let` it sits in the Temporal Dead Zone until the assignment executes, so calling it earlier throws a ReferenceError. In practice I prefer expressions, because relying on being able to call something defined further down makes code harder to follow."',
          'Note the error wording differs and is a giveaway: an uninitialized `const` says "Cannot access before initialization", whereas a name that does not exist at all says "is not defined".',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Functions stored like any other value',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A function can go anywhere a value can go.',
        code: [
          'function double(n) { return n * 2; }',
          '',
          '// In a variable',
          'const alias = double;',
          'console.log(alias(4));',
          '',
          '// In an array',
          'const operations = [double, function (n) { return n + 1; }];',
          'console.log(operations[0](4), operations[1](4));',
          '',
          '// As an object property',
          'const math = {',
          '  double: double,',
          '  triple(n) { return n * 3; },',
          '};',
          'console.log(math.double(4), math.triple(4));',
          '',
          '// Functions are objects, so they carry properties',
          'console.log(double.name, double.length);',
        ].join('\n'),
        output: ['8', '8 5', '8 12', 'double 1'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The `triple(n) { ... }` form inside the object is **method shorthand** — a concise way to write a function-valued property. Module 14 covers object methods properly.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Passing a function versus passing its result',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This is the distinction that causes the most confusion when learners first meet callbacks, and it is worth stopping on.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'One character changes everything.',
        code: [
          'function greet() {',
          '  return "hello";',
          '}',
          '',
          'function inspect(value) {',
          '  console.log("received a", typeof value, "→", value);',
          '}',
          '',
          'inspect(greet);',
          'inspect(greet());',
        ].join('\n'),
        output: [
          'received a function → ƒ greet()',
          'received a string → hello',
        ].join('\n'),
      },
      {
        kind: SECTION.LIST,
        items: [
          '`greet` — the **function itself**. Nothing has run. The receiver decides when, or whether, to call it.',
          '`greet()` — the function is called **immediately**, and its return value is what gets passed.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'The classic event-handler bug',
        body: [
          '`button.addEventListener("click", handleClick())` runs `handleClick` immediately, at the moment the listener is registered, and passes its return value — usually `undefined` — as the handler. So the code fires once on page load and never again on a click.',
          'The fix is to drop the parentheses: `addEventListener("click", handleClick)`. Whenever a callback "runs at the wrong time" or "does nothing", check the parentheses first.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The same mistake, made observable.',
        code: [
          'function handler() {',
          '  console.log("handler ran");',
          '  return "result";',
          '}',
          '',
          'function register(callback) {',
          '  console.log("registering...");',
          '  console.log("stored:", typeof callback);',
          '}',
          '',
          'console.log("--- correct: passing the function ---");',
          'register(handler);',
          '',
          'console.log("--- wrong: passing the result ---");',
          'register(handler());',
        ].join('\n'),
        output: [
          '--- correct: passing the function ---',
          'registering...',
          'stored: function',
          '--- wrong: passing the result ---',
          'handler ran',
          'registering...',
          'stored: string',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Look at the ordering in the wrong case: `"handler ran"` appears **before** `"registering..."`. The function executed while the arguments were being evaluated, long before `register` did anything. That inverted ordering is the tell-tale sign of this bug.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const fns = [];',
          '',
          'function make() {',
          '  return "made";',
          '}',
          '',
          'fns.push(make);',
          'fns.push(make());',
          '',
          'console.log(typeof fns[0], typeof fns[1], fns.length);',
        ].join('\n'),
        options: [
          'function string 2',
          'function function 2',
          'string string 2',
          'function string 1',
        ],
        correct: 0,
        explanation:
          '`fns.push(make)` stores the function value itself, so `fns[0]` is a function. `fns.push(make())` calls `make` first and stores its return value, so `fns[1]` is the string `"made"`. Both pushes succeed, so the length is 2. The two lines look nearly identical and do completely different things — which is precisely why the callback bug is so easy to write.',
      },
    ],
    exercises: [
      {
        id: 'ex-m08-04-a',
        title: 'Predict the hoisting difference',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['functions', 'hoisting'],
        instructions: 'What happens when this runs?',
        code: [
          'console.log(go());',
          '',
          'const go = function () {',
          '  return "gone";',
          '};',
        ].join('\n'),
        options: [
          'Prints "gone"',
          'Prints undefined',
          'Throws a ReferenceError',
          'Throws a TypeError',
        ],
        correct: 2,
        hints: [
          'This is a function expression assigned to a `const`.',
          'What rules does a `const` binding follow before its assignment line?',
        ],
        solution: 'Throws a ReferenceError',
        solutionExplanation:
          'A function expression follows the rules of the variable it is assigned to. The `const go` binding exists from the start of the scope but is uninitialized until the assignment runs, so accessing it earlier throws `ReferenceError: Cannot access \'go\' before initialization` — the Temporal Dead Zone. Had this been a function *declaration* (`function go() {...}`), it would have printed `"gone"` successfully, because declarations are fully created before any code runs.',
      },
      {
        id: 'ex-m08-04-b',
        title: 'Passing a function correctly',
        kind: EXERCISE_KIND.CHOOSE_IMPLEMENTATION,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['functions', 'higher-order'],
        instructions:
          'You want `runLater` to call `notify` at some point in the future. Which call is correct?',
        options: [
          'runLater(notify())',
          'runLater(notify)',
          'runLater("notify")',
          'runLater(function notify)',
        ],
        correct: 1,
        hints: [
          'Which form runs the function immediately?',
          'You want to hand over the function itself, not its result.',
        ],
        solution: 'runLater(notify)',
        solutionExplanation:
          'Without parentheses you pass the function value, leaving `runLater` free to call it whenever it chooses. With parentheses, `notify` runs immediately and its return value — often `undefined` — is passed instead, so nothing useful happens later. Passing the string `"notify"` passes text, not a function. The fourth option is not valid syntax. This exact mistake is the most common cause of event handlers that fire once on load and never again.',
      },
      {
        id: 'ex-m08-04-c',
        title: 'Build an operation dispatcher',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['functions', 'higher-order'],
        instructions:
          'Write `makeCalculator()` that returns an object with three function properties — `add`, `subtract` and `multiply` — each taking two numbers and returning the result. The returned object must be usable as `calc.add(2, 3)`.',
        starterCode: [
          'function makeCalculator() {',
          '  // Return an object whose properties are functions.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'add works', body: 'expect(makeCalculator().add(2, 3)).toBe(5);' },
          { name: 'subtract works', body: 'expect(makeCalculator().subtract(10, 4)).toBe(6);' },
          { name: 'multiply works', body: 'expect(makeCalculator().multiply(3, 4)).toBe(12);' },
          { name: 'returns an object', body: 'expect(typeof makeCalculator()).toBe("object");' },
          { name: 'the properties are functions', body: 'const c = makeCalculator(); expect(typeof c.add).toBe("function"); expect(typeof c.multiply).toBe("function");' },
          { name: 'handles negatives', body: 'expect(makeCalculator().add(-5, 2)).toBe(-3);', hidden: true },
        ],
        hints: [
          'Object properties can hold functions, just like any other value.',
          'Method shorthand is concise: `{ add(a, b) { return a + b; } }`.',
          'Remember to `return` the object itself.',
        ],
        solution: [
          'function makeCalculator() {',
          '  return {',
          '    add(a, b) { return a + b; },',
          '    subtract(a, b) { return a - b; },',
          '    multiply(a, b) { return a * b; },',
          '  };',
          '}',
        ].join('\n'),
        solutionExplanation:
          'This works only because functions are values that can be stored in object properties. Method shorthand — `add(a, b) { ... }` — is equivalent to `add: function (a, b) { ... }` but shorter and now conventional. Note that `makeCalculator` returns a brand-new object on every call, so two calculators are independent; that becomes meaningful once objects hold state, which Module 14 explores. This grouping of related functions under one object is a small example of the module pattern you will meet in Module 41.',
      },
      {
        id: 'ex-m08-04-d',
        title: 'Fix the immediate call',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['functions', 'higher-order'],
        instructions:
          'This function should store a callback and return a function that runs it when invoked. At the moment the callback runs immediately and the stored value is wrong. Fix `defer` so the test showing deferred execution passes.',
        starterCode: [
          'function defer(callback) {',
          '  const stored = callback();',
          '  return function () {',
          '    return stored;',
          '  };',
          '}',
        ].join('\n'),
        tests: [
          { name: 'does not run the callback while deferring', body: 'let ran = false; defer(() => { ran = true; return 1; }); expect(ran).toBe(false);' },
          { name: 'runs the callback when invoked', body: 'let ran = false; const go = defer(() => { ran = true; return 1; }); go(); expect(ran).toBe(true);' },
          { name: 'returns the callback result', body: 'const go = defer(() => 42); expect(go()).toBe(42);' },
          { name: 'can be invoked more than once', body: 'let count = 0; const go = defer(() => { count += 1; return count; }); go(); go(); expect(count).toBe(2);' },
          { name: 'returns a function', body: 'expect(typeof defer(() => 1)).toBe("function");', hidden: true },
        ],
        hints: [
          '`callback()` with parentheses runs it straight away — that is the bug.',
          'Move the call inside the returned function.',
          'The returned function should call and return in one step.',
        ],
        solution: [
          'function defer(callback) {',
          '  return function () {',
          '    return callback();',
          '  };',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The original called `callback()` at defer time and cached the result, which defeats the entire purpose — the first test catches this by checking that nothing has run yet. Moving the call inside the returned function means it happens only when that function is invoked, and the fourth test confirms it runs afresh each time rather than replaying a cached value. The returned function keeps access to `callback` even after `defer` has finished, which is a closure — the subject of Module 32.',
      },
    ],
    quiz: {
      id: 'qz-m08-04',
      questions: [
        {
          id: 'q-m08-04-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['functions', 'hoisting'],
          prompt: 'Why can a function declaration be called before its definition, but not a function expression assigned to `const`?',
          options: [
            'Declarations are faster to create',
            'A declaration is fully created before the surrounding code runs; an expression follows its variable’s rules and sits in the TDZ until assigned',
            'Expressions are not real functions',
            'Declarations are automatically global',
          ],
          correct: 1,
          optionExplanations: [
            'Creation speed is not the mechanism.',
            'Correct — the difference is when the binding becomes usable.',
            'Both produce identical function values.',
            'A declaration is scoped like anything else.',
          ],
          explanation:
            'Function declarations are created and initialized before any code in their scope executes, so they are callable from anywhere in that scope. A function expression is a value assigned to a variable, so with `const` or `let` the binding is uninitialized until the assignment runs — accessing it earlier throws a ReferenceError.',
        },
        {
          id: 'q-m08-04-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['functions'],
          prompt: 'What are these two values?',
          code: [
            'function f() { return 1; }',
            'const a = f;',
            'const b = f();',
            'console.log(typeof a, typeof b);',
          ].join('\n'),
          options: [
            'function number',
            'function function',
            'number number',
            'undefined number',
          ],
          correct: 0,
          optionExplanations: [
            'Correct — `a` holds the function, `b` holds its result.',
            '`f()` invokes and yields a number.',
            '`f` without parentheses is not called.',
            '`a` is definitely the function value.',
          ],
          explanation:
            'Without parentheses you refer to the function itself; with them you call it and get the return value. This one-character difference is the root of the classic callback bug.',
        },
        {
          id: 'q-m08-04-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['functions'],
          prompt: 'What is an anonymous function?',
          options: [
            'A function that takes no parameters',
            'A function created without a name after the `function` keyword, typically assigned to a variable or passed as an argument',
            'A function that returns nothing',
            'A function that cannot be called',
          ],
          correct: 1,
          optionExplanations: [
            'Parameter count is unrelated.',
            'Correct — the variable or the receiving function supplies the way to call it.',
            'Return behaviour is unrelated.',
            'It is called like any other function.',
          ],
          explanation:
            'An anonymous function has no name of its own. It does not need one, because it is either assigned to a variable that provides a name for calling it, or passed directly as an argument to be called by the receiver.',
        },
        {
          id: 'q-m08-04-4',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['functions'],
          prompt: 'Which of these are valid things to do with a function in JavaScript? (Select all that apply.)',
          options: [
            'Store it in an array',
            'Assign it to an object property',
            'Pass it as an argument to another function',
            'Return it from another function',
          ],
          correct: [0, 1, 2, 3],
          explanation:
            'All four. Functions are ordinary values — "first-class functions" — so anything you can do with a number or a string you can do with a function. This is what makes callbacks, array methods, event handlers and promises possible.',
        },
      ],
    },
    summary:
      'A function declaration is a statement that creates its function before the surrounding code runs, so it can be called from anywhere in its scope. A function expression is a value assigned to a variable, so it follows that variable\'s rules — with `const` it sits in the Temporal Dead Zone until the assignment executes, and calling it earlier throws "Cannot access before initialization". An expression\'s function usually has no name of its own, making it anonymous. Because functions are ordinary values, they can be stored in variables, arrays and object properties, passed as arguments and returned. The critical distinction is `fn` versus `fn()`: the first hands over the function for the receiver to call later, the second runs it immediately and hands over its result — the source of the classic event-handler bug.',
    keyTakeaways: [
      'Declarations are usable throughout their scope; expressions are not until assigned',
      'An expression assigned to `const` throws "Cannot access before initialization" if called early',
      'Anonymous functions get their callable name from the variable or the receiver',
      'Functions can live in variables, arrays, object properties, arguments and return values',
      '`fn` passes the function; `fn()` runs it and passes the result',
    ],
    relatedLessons: ['l-m08-03', 'l-m08-05'],
    interviewConnections: [
      'What is the difference between a function declaration and a function expression?',
      'What does first-class functions mean?',
      'Why does addEventListener("click", handler()) fire immediately?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m08-05',
    slug: 'default-and-rest-parameters',
    moduleId: M,
    order: 5,
    title: 'Default and Rest Parameters',
    description: 'Supplying sensible fallbacks, and accepting any number of arguments.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 15,
    xp: 30,
    topicIds: ['functions'],
    prerequisites: ['l-m08-04'],
    learningObjectives: [
      'Give parameters default values and explain exactly when they apply',
      'Explain why defaults trigger on `undefined` but not on `null`',
      'Collect extra arguments with a rest parameter',
      'Distinguish rest parameters from the spread operator',
    ],
    sections: [
      {
        kind: SECTION.HEADING,
        text: 'Default parameters',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'A default gives a parameter a value to use when no argument was supplied, turning the `undefined` problem from the previous lessons into sensible behaviour.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A default is written directly in the parameter list.',
        code: [
          'function greet(name = "friend", greeting = "Hello") {',
          '  return `${greeting}, ${name}!`;',
          '}',
          '',
          'console.log(greet("Ada", "Welcome"));',
          'console.log(greet("Ada"));',
          'console.log(greet());',
        ].join('\n'),
        output: ['Welcome, Ada!', 'Hello, Ada!', 'Hello, friend!'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Defaults trigger on `undefined` only',
        body: [
          'A default applies when the argument is `undefined` — whether omitted entirely or passed explicitly as `undefined`. It does **not** apply for `null`, `0`, `""` or `false`, all of which count as deliberately supplied values.',
          'This bites regularly with API data, where `null` is the conventional "empty" value. Your carefully written defaults will never fire for it.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Only one of these five uses the default.',
        code: [
          'function show(value = "DEFAULT") {',
          '  return value;',
          '}',
          '',
          'console.log(show());',
          'console.log(show(undefined));',
          'console.log(show(null));',
          'console.log(show(0));',
          'console.log(show(""));',
        ].join('\n'),
        output: ['DEFAULT', 'DEFAULT', 'null', '0', ''].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'If you need `null` to fall back too, handle it in the body with the nullish coalescing operator from Module 04: `const safe = value ?? "DEFAULT";`. That treats `null` and `undefined` alike while still preserving `0` and `""`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Defaults are evaluated at call time',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A default expression runs fresh on every call that needs it.',
        code: [
          'let counter = 0;',
          '',
          'function nextId(id = ++counter) {',
          '  return id;',
          '}',
          '',
          'console.log(nextId(), nextId(), nextId());',
          'console.log(nextId(99));',
          'console.log(nextId());',
          '',
          '// A later parameter may use an earlier one',
          'function makeRange(start, end = start + 10) {',
          '  return `${start} to ${end}`;',
          '}',
          '',
          'console.log(makeRange(5));',
          'console.log(makeRange(5, 7));',
        ].join('\n'),
        output: ['1 2 3', '99', '4', '5 to 15', '5 to 7'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Two things worth noticing. The default expression is evaluated **each time it is needed**, not once when the function is defined — so `++counter` produces a new value per call, and passing an explicit `99` skips the increment entirely. And a later parameter can reference an earlier one, because parameters are initialized left to right.',
          'The reverse does not work: `function f(a = b, b = 1)` throws, because `b` is still in its Temporal Dead Zone when `a`\'s default is evaluated.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Put parameters with defaults last',
        body: [
          'A default is only useful if the argument can be omitted, and you can only omit trailing arguments. In `function f(a = 1, b)` you can never skip `a` without also passing `undefined` explicitly, which defeats the point.',
          'Order parameters so required ones come first and optional ones follow.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Rest parameters',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'A **rest parameter** collects every remaining argument into a real array, which is how a function accepts a variable number of values.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Three dots before the last parameter name.',
        code: [
          'function sum(...numbers) {',
          '  let total = 0;',
          '  for (const n of numbers) {',
          '    total += n;',
          '  }',
          '  return total;',
          '}',
          '',
          'console.log(sum(1, 2, 3));',
          'console.log(sum(1, 2, 3, 4, 5));',
          'console.log(sum());',
          '',
          '// It is a genuine array',
          'function inspect(...args) {',
          '  return `${Array.isArray(args)} / length ${args.length}`;',
          '}',
          'console.log(inspect("a", "b"));',
        ].join('\n'),
        output: ['6', '15', '0', 'true / length 2'].join('\n'),
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Rest can follow named parameters — and must come last.',
        code: [
          'function describe(action, ...items) {',
          '  if (items.length === 0) return `${action}: nothing`;',
          '  return `${action}: ${items.join(", ")}`;',
          '}',
          '',
          'console.log(describe("Buying", "milk", "bread", "eggs"));',
          'console.log(describe("Buying"));',
          '',
          '// Rest is excluded from the reported parameter count',
          'console.log(describe.length);',
        ].join('\n'),
        output: [
          'Buying: milk, bread, eggs',
          'Buying: nothing',
          '1',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Rest must be the final parameter',
        body: [
          '`function f(...rest, last)` is a SyntaxError. A rest parameter consumes everything remaining, so nothing could ever fill a parameter after it.',
          'For the same reason there can be only one rest parameter, and it cannot have a default value.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Rest versus spread',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The `...` symbol appears in two roles that look identical and do opposite things. The distinction is *where* it appears.',
        ],
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Rest — collects, in a parameter list',
          code: [
            'function sum(...numbers) {',
            '  // many arguments →',
            '  // one array',
            '  return numbers.length;',
            '}',
            '',
            'sum(1, 2, 3); // 3',
          ].join('\n'),
        },
        right: {
          title: 'Spread — expands, at a call site',
          code: [
            'const values = [1, 2, 3];',
            '',
            '// one array →',
            '// many arguments',
            'Math.max(...values); // 3',
            '',
            'sum(...values); // 3',
          ].join('\n'),
        },
        note: 'Rest appears in a **declaration** and gathers. Spread appears in a **call or literal** and scatters. Same three dots, opposite directions.',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Both in one example.',
        code: [
          'function sum(...numbers) {',
          '  let total = 0;',
          '  for (const n of numbers) total += n;',
          '  return total;',
          '}',
          '',
          'const values = [1, 2, 3, 4];',
          '',
          'console.log(sum(1, 2, 3));',
          'console.log(sum(...values));',
          'console.log(sum(...values, 10));',
        ].join('\n'),
        output: ['6', '10', '20'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Rest replaced the old `arguments` object',
        body: [
          'Older code uses a special `arguments` object available inside any non-arrow function. It holds every argument but is not a real array, so it lacks `map`, `filter` and the rest — you had to convert it first.',
          'A rest parameter gives you a genuine array directly, works in arrow functions where `arguments` does not exist, and names what it collects. Use rest in new code; recognise `arguments` when reading old code.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'function f(a = 10, ...rest) {',
          '  return `${a} / ${rest.length}`;',
          '}',
          '',
          'console.log(f(), f(1), f(1, 2, 3), f(undefined, 5));',
        ].join('\n'),
        options: [
          '10 / 0, 1 / 0, 1 / 2, 10 / 1',
          '10 / 0, 1 / 0, 1 / 3, 10 / 1',
          '10 / 0, 1 / 1, 1 / 2, undefined / 1',
          '10 / 0, 1 / 0, 1 / 2, undefined / 1',
        ],
        correct: 0,
        explanation:
          '`f()` supplies nothing, so `a` takes its default 10 and `rest` is empty. `f(1)` fills `a` with 1 and leaves `rest` empty. `f(1, 2, 3)` fills `a` with 1 and collects the remaining two arguments, so `rest.length` is 2 — not 3, because the first argument went to the named parameter. `f(undefined, 5)` passes `undefined` explicitly, which triggers the default of 10, and collects one remaining argument.',
      },
    ],
    exercises: [
      {
        id: 'ex-m08-05-a',
        title: 'Predict the null default',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['functions'],
        instructions: 'What does this print?',
        code: [
          'function label(text = "none") {',
          '  return text;',
          '}',
          '',
          'console.log(label(null));',
        ].join('\n'),
        options: ['"none"', 'null', 'undefined', 'An error'],
        correct: 1,
        hints: [
          'Defaults apply for exactly one value. Which?',
          'Is `null` that value?',
        ],
        solution: 'null',
        solutionExplanation:
          'A default parameter applies only when the argument is `undefined`. Passing `null` counts as deliberately supplying a value, so the default is skipped and `null` is returned unchanged. This matters constantly with API data, where `null` is the conventional representation of an empty field — defaults you wrote will simply never fire for it. Use `text ?? "none"` inside the body if you want `null` to fall back too.',
      },
      {
        id: 'ex-m08-05-b',
        title: 'Add defaults to a function',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 20,
        topicIds: ['functions'],
        instructions:
          'Write `formatPrice(amount, currency = "£", decimals = 2)` returning a string such as `"£9.99"`. `amount` is required; the other two have the defaults shown.',
        starterCode: [
          'function formatPrice(amount, currency = "£", decimals = 2) {',
          '  // toFixed(decimals) formats the number to that many places.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'uses both defaults', body: 'expect(formatPrice(9.99)).toBe("£9.99");' },
          { name: 'pads to two decimals', body: 'expect(formatPrice(5)).toBe("£5.00");' },
          { name: 'accepts a different currency', body: 'expect(formatPrice(9.99, "$")).toBe("$9.99");' },
          { name: 'accepts different decimals', body: 'expect(formatPrice(9.5, "$", 1)).toBe("$9.5");' },
          { name: 'handles zero decimals', body: 'expect(formatPrice(10, "£", 0)).toBe("£10");' },
          { name: 'handles zero amount', body: 'expect(formatPrice(0)).toBe("£0.00");', hidden: true },
        ],
        hints: [
          '`amount.toFixed(decimals)` produces the numeric part as a string.',
          'Join the currency symbol to the front with a template literal.',
        ],
        solution: [
          'function formatPrice(amount, currency = "£", decimals = 2) {',
          '  return `${currency}${amount.toFixed(decimals)}`;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The defaults sit on the two optional parameters, and both come after the required `amount` — which is what makes them omittable. `toFixed` returns a string with exactly the requested number of decimal places, padding with zeros where needed, which is why `5` becomes `"5.00"`. The hidden zero test matters because `0` is falsy: an implementation that checked truthiness somewhere would break on it, whereas default parameters correctly leave a supplied `0` alone.',
      },
      {
        id: 'ex-m08-05-c',
        title: 'Accept any number of arguments',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['functions'],
        instructions:
          'Write `average(...numbers)` returning the mean of however many numbers are passed. Return `0` when called with no arguments.',
        starterCode: [
          'function average(...numbers) {',
          '  // Guard the empty case before dividing.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'averages three numbers', body: 'expect(average(1, 2, 3)).toBe(2);' },
          { name: 'averages two numbers', body: 'expect(average(10, 20)).toBe(15);' },
          { name: 'handles a single number', body: 'expect(average(7)).toBe(7);' },
          { name: 'returns 0 with no arguments', body: 'expect(average()).toBe(0);' },
          { name: 'handles negatives', body: 'expect(average(-10, 10)).toBe(0);' },
          { name: 'handles decimals', body: 'expect(average(1, 2)).toBe(1.5);' },
          { name: 'works with spread input', body: 'const vals = [2, 4, 6]; expect(average(...vals)).toBe(4);', hidden: true },
        ],
        hints: [
          '`...numbers` collects the arguments into a real array.',
          'Return `0` immediately if `numbers.length` is 0 — dividing by zero would give NaN.',
          'Sum with a loop, then divide by the length.',
        ],
        solution: [
          'function average(...numbers) {',
          '  if (numbers.length === 0) return 0;',
          '',
          '  let total = 0;',
          '  for (const n of numbers) {',
          '    total += n;',
          '  }',
          '',
          '  return total / numbers.length;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The empty guard is essential: without it, `0 / 0` gives `NaN` rather than the required `0`. The rest parameter produces a genuine array, so `for...of` and `.length` both work directly — with the old `arguments` object you would have had to convert it first. The hidden test shows rest and spread as complements: the caller spreads an array into separate arguments, and the rest parameter gathers them straight back up. Module 13 will let you replace the loop with `reduce`.',
      },
      {
        id: 'ex-m08-05-d',
        title: 'Fix the parameter order',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['functions'],
        instructions:
          'This function is meant to let the caller omit the separator, but `join(["a","b"])` currently fails. Reorder the parameters so the optional one comes last, keeping the same default.',
        starterCode: [
          'function join(separator = "-", items) {',
          '  return items.join(separator);',
          '}',
        ].join('\n'),
        tests: [
          { name: 'uses the default separator', body: 'expect(join(["a", "b"])).toBe("a-b");' },
          { name: 'accepts a custom separator', body: 'expect(join(["a", "b"], "+")).toBe("a+b");' },
          { name: 'handles a single item', body: 'expect(join(["only"])).toBe("only");' },
          { name: 'handles an empty array', body: 'expect(join([])).toBe("");' },
          { name: 'handles a space separator', body: 'expect(join(["x", "y", "z"], " ")).toBe("x y z");', hidden: true },
        ],
        hints: [
          'You can only omit trailing arguments, so a default on the first parameter is unusable.',
          'Swap the two parameters, keeping the default attached to `separator`.',
        ],
        solution: [
          'function join(items, separator = "-") {',
          '  return items.join(separator);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'With the original order, calling `join(["a","b"])` bound the array to `separator` and left `items` as `undefined`, so `items.join` threw a TypeError. Because arguments are matched by position and only trailing ones can be omitted, a parameter with a default is only genuinely optional if nothing required follows it. The rule is simple: required parameters first, optional ones last. This is why almost every well-designed API puts the data first and the options object last.',
      },
    ],
    quiz: {
      id: 'qz-m08-05',
      questions: [
        {
          id: 'q-m08-05-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['functions'],
          prompt: 'When does a default parameter apply?',
          options: [
            'Whenever the argument is falsy',
            'Only when the argument is `undefined`, whether omitted or passed explicitly',
            'Only when the argument is omitted entirely',
            'Whenever the argument is `null` or `undefined`',
          ],
          correct: 1,
          optionExplanations: [
            '`0`, `""` and `false` are supplied values and do not trigger the default.',
            'Correct — including an explicit `undefined`.',
            'Passing `undefined` explicitly triggers it too.',
            '`null` counts as a supplied value and does not trigger it.',
          ],
          explanation:
            'The default fires for `undefined` only. `null`, `0`, `""` and `false` are all treated as deliberately supplied values. To make `null` fall back as well, use `??` inside the function body.',
        },
        {
          id: 'q-m08-05-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['functions'],
          prompt: 'How many arguments does the rest parameter collect?',
          code: [
            'function f(a, ...rest) {',
            '  return rest.length;',
            '}',
            'console.log(f(1, 2, 3, 4));',
          ].join('\n'),
          options: ['4', '3', '2', '1'],
          correct: 1,
          optionExplanations: [
            'The first argument fills the named parameter.',
            'Correct — `a` takes 1, and `rest` collects the remaining three.',
            'Only one named parameter precedes the rest.',
            'Three arguments remain after `a`.',
          ],
          explanation:
            'A rest parameter collects everything left over after the named parameters are filled. `a` takes the first argument and `rest` gathers the other three into an array.',
        },
        {
          id: 'q-m08-05-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['functions'],
          prompt: 'What is the difference between rest and spread?',
          options: [
            'They are the same operator used in different places',
            'Rest gathers many values into an array in a parameter list; spread expands an array into separate values at a call site',
            'Rest works on arrays; spread works on objects',
            'Spread is deprecated in favour of rest',
          ],
          correct: 1,
          optionExplanations: [
            'They perform opposite operations, distinguished by position.',
            'Correct — gathering versus scattering.',
            'Both work with arrays, and spread also works with objects.',
            'Both are current and complementary.',
          ],
          explanation:
            'The same `...` syntax does opposite things depending on where it appears. In a parameter list it is rest and gathers arguments into an array. In a call or literal it is spread and expands an iterable into separate values.',
        },
        {
          id: 'q-m08-05-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['functions'],
          prompt: '`function f(...rest, last) {}` is valid JavaScript.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'A rest parameter consumes all remaining arguments, so nothing could fill `last`.',
            'Correct — it is a SyntaxError.',
          ],
          explanation:
            'A rest parameter must be last, because it absorbs every remaining argument and no later parameter could ever receive one. For the same reason there can be only one rest parameter, and it cannot have a default.',
        },
      ],
    },
    summary:
      'A default parameter supplies a value when the argument is `undefined` — omitted or explicitly passed — but never for `null`, `0`, `""` or `false`, which count as deliberate values; use `??` in the body if `null` should fall back too. Default expressions are evaluated fresh on each call that needs them, and a later parameter may reference an earlier one. Because only trailing arguments can be omitted, parameters with defaults must come last. A rest parameter gathers every remaining argument into a genuine array, must be the final parameter, and replaces the old array-like `arguments` object. Rest and spread share the `...` syntax but do opposite things: rest gathers in a declaration, spread scatters at a call site.',
    keyTakeaways: [
      'Defaults fire on `undefined` only — never on `null`, `0`, `""` or `false`',
      'Default expressions are evaluated per call, and can reference earlier parameters',
      'Put parameters with defaults last, since only trailing arguments can be omitted',
      '`...rest` gathers remaining arguments into a real array and must come last',
      'Rest gathers in a declaration; spread scatters at a call site',
    ],
    relatedLessons: ['l-m08-04', 'l-m08-06'],
    interviewConnections: [
      'When does a default parameter actually apply?',
      'What is the difference between rest parameters and the spread operator?',
      'Why was rest introduced when `arguments` already existed?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m08-06',
    slug: 'scope-reuse-and-composition',
    moduleId: M,
    order: 6,
    title: 'Local Variables, Reuse and Composition',
    description: 'Why what happens in a function stays there, and how small functions combine into larger behaviour.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 16,
    xp: 35,
    topicIds: ['functions', 'scope', 'clean-code'],
    prerequisites: ['l-m08-05'],
    learningObjectives: [
      'Explain that variables declared inside a function are local to it',
      'Explain why locality makes functions safe to reuse',
      'Build larger behaviour by having functions call other functions',
      'Recognise a pure function and why it is easier to reason about',
    ],
    sections: [
      {
        kind: SECTION.HEADING,
        text: 'Variables inside a function are local',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A local variable does not exist outside its function.',
        code: [
          'function calculate() {',
          '  const secret = 42;',
          '  return secret * 2;',
          '}',
          '',
          'console.log(calculate());',
          '',
          'try {',
          '  console.log(secret);',
          '} catch (error) {',
          '  console.log(error.name + ":", error.message);',
          '}',
        ].join('\n'),
        output: ['84', 'ReferenceError: secret is not defined'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Each call creates a **fresh set** of local variables that disappear when the function returns. Two calls never share them, which is what makes a function safe to call from anywhere without worrying about interference.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Each call gets its own counter.',
        code: [
          'function countTo(limit) {',
          '  let count = 0;',
          '  while (count < limit) {',
          '    count += 1;',
          '  }',
          '  return count;',
          '}',
          '',
          'console.log(countTo(3));',
          'console.log(countTo(5));',
          'console.log(countTo(3));',
        ].join('\n'),
        output: ['3', '5', '3'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The third call gives 3 again, not 8 or 11. Its `count` started fresh at 0, entirely unaffected by the two calls before it. If locals persisted between calls, functions would be almost impossible to reason about.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Functions can read outward, but not inward',
        body: [
          'A function can read variables from the scope that surrounds it, but the surrounding scope cannot see inside the function. Access runs one way — outward.',
          'Module 10 covers this properly as the **scope chain**, along with what happens when an inner and outer variable share a name.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Reading outward works; reading inward does not.',
        code: [
          'const taxRate = 0.2;',
          '',
          'function addTax(price) {',
          '  const tax = price * taxRate;',
          '  return price + tax;',
          '}',
          '',
          'console.log(addTax(100));',
          '',
          'try {',
          '  console.log(tax);',
          '} catch (error) {',
          '  console.log(error.name);',
          '}',
        ].join('\n'),
        output: ['120', 'ReferenceError'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Functions calling functions',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'A function body is ordinary code, so it can call other functions. This is how a large behaviour is built from small, individually understandable pieces.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Four small functions, each doing one job.',
        code: [
          'function subtotal(price, quantity) {',
          '  return price * quantity;',
          '}',
          '',
          'function applyDiscount(amount, percent) {',
          '  return amount * (1 - percent / 100);',
          '}',
          '',
          'function addTax(amount, rate) {',
          '  return amount * (1 + rate);',
          '}',
          '',
          'function roundMoney(amount) {',
          '  return Math.round(amount * 100) / 100;',
          '}',
          '',
          'function orderTotal(price, quantity, discountPercent, taxRate) {',
          '  const base = subtotal(price, quantity);',
          '  const discounted = applyDiscount(base, discountPercent);',
          '  const taxed = addTax(discounted, taxRate);',
          '  return roundMoney(taxed);',
          '}',
          '',
          'console.log(orderTotal(20, 3, 10, 0.2));',
          'console.log(orderTotal(9.99, 2, 0, 0.2));',
        ].join('\n'),
        output: ['64.8', '23.98'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Compare this with the same arithmetic crammed into a single expression. Each step now has a name, each can be tested alone, and changing how discounts work means editing one small function rather than hunting through a long formula.',
          'Reading `orderTotal` top to bottom describes the business process in order — which is exactly what a colleague needs when they arrive at this code six months later.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'One function, one job',
        body: [
          'A useful test: can you describe what the function does in one sentence without using the word "and"? If not, it is probably doing two things and would be clearer split.',
          'This is not a rule to apply mechanically — a function split into pieces too small becomes its own kind of hard to follow. But when a function grows past a screenful, the "and" test usually reveals a natural seam.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Pure functions',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Every function above shares two properties that make them unusually easy to work with.',
        ],
      },
      {
        kind: SECTION.LIST,
        ordered: true,
        items: [
          '**The same inputs always produce the same output.** `subtotal(20, 3)` is 60 today, tomorrow, and on the thousandth call.',
          '**Nothing outside the function changes.** No variable elsewhere is modified, nothing is printed, no file or network is touched.',
        ],
      },
      {
        kind: SECTION.PROSE,
        body: [
          'A function with both properties is called **pure**. Pure functions are trivial to test — call with inputs, check the output — and trivial to reason about, because you never have to ask what else might have happened.',
          'An **impure** function does something beyond returning a value: modifying an outer variable, logging, writing to storage, sending a request. Those actions are called **side effects**.',
        ],
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Pure',
          code: [
            'function addItem(cart, item) {',
            '  return [...cart, item];',
            '}',
            '',
            '// Returns a new array.',
            '// The original cart is',
            '// untouched. Same input,',
            '// same output, always.',
          ].join('\n'),
        },
        right: {
          title: 'Impure — mutates its input',
          code: [
            'function addItem(cart, item) {',
            '  cart.push(item);',
            '  return cart;',
            '}',
            '',
            '// Changes the caller\'s array.',
            '// Anything else holding a',
            '// reference to it sees the',
            '// change too.',
          ].join('\n'),
        },
        note: 'Neither is wrong. The impure version is sometimes exactly what you want. But the pure version cannot surprise a caller, which is why it is the safer default — and why Module 13\'s array methods are built around returning new arrays.',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The difference a caller actually feels.',
        code: [
          'function pureAdd(cart, item) {',
          '  return [...cart, item];',
          '}',
          '',
          'function impureAdd(cart, item) {',
          '  cart.push(item);',
          '  return cart;',
          '}',
          '',
          'const original = ["apple"];',
          '',
          'const copy = pureAdd(original, "bread");',
          'console.log("after pure  →", original, copy);',
          '',
          'impureAdd(original, "milk");',
          'console.log("after impure →", original);',
        ].join('\n'),
        output: [
          'after pure  → ["apple"] ["apple", "bread"]',
          'after impure → ["apple", "milk"]',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Side effects are necessary — just deliberate',
        body: [
          'A program with no side effects would do nothing observable: no display, no saving, no requests. The goal is not to eliminate them but to keep them **separate** from calculation.',
          'A common structure is a core of pure functions that compute, surrounded by a thin layer that performs the effects. That core stays easy to test even when the surrounding application is complicated. Module 37 explores this properly.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'let total = 0;',
          '',
          'function addPure(a, b) {',
          '  return a + b;',
          '}',
          '',
          'function addImpure(a, b) {',
          '  total = a + b;',
          '  return total;',
          '}',
          '',
          'addPure(2, 3);',
          'console.log(total);',
          '',
          'addImpure(2, 3);',
          'console.log(total);',
        ].join('\n'),
        options: [
          '0 then 5',
          '5 then 5',
          '0 then 0',
          '5 then 10',
        ],
        correct: 0,
        explanation:
          '`addPure` computes and returns a value without touching anything outside itself, so `total` is still `0` after it runs — and its return value was discarded. `addImpure` assigns to the outer `total` as a side effect, so the second log shows 5. The purity difference is invisible from the call site: both look like `add(2, 3)`, but only one changes the world around it.',
      },
    ],
    exercises: [
      {
        id: 'ex-m08-06-a',
        title: 'Predict the local variable error',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['functions', 'scope'],
        instructions: 'What happens on the final line?',
        code: [
          'function setup() {',
          '  const config = "ready";',
          '  return true;',
          '}',
          '',
          'setup();',
          'console.log(config);',
        ].join('\n'),
        options: [
          'Prints "ready"',
          'Prints undefined',
          'Throws a ReferenceError',
          'Prints true',
        ],
        correct: 2,
        hints: [
          'Where does `config` exist?',
          'Does calling the function make its locals available outside?',
        ],
        solution: 'Throws a ReferenceError',
        solutionExplanation:
          '`config` is declared inside `setup`, so it exists only during that call and is gone once the function returns. Calling the function does not export its locals. The outer scope has no binding named `config`, so reading it throws `ReferenceError: config is not defined`. Note this is different from `undefined`: the name does not exist at all rather than existing without a value.',
      },
      {
        id: 'ex-m08-06-b',
        title: 'Identify the pure function',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['functions'],
        instructions: 'Which of these functions is pure?',
        options: [
          'function f(a) { console.log(a); return a; }',
          'function f(a) { return a * 2; }',
          'function f(arr) { arr.push(1); return arr; }',
          'function f() { return Math.random(); }',
        ],
        correct: 1,
        hints: [
          'A pure function returns the same output for the same input and changes nothing outside itself.',
          'Check each option against both conditions.',
        ],
        solution: 'function f(a) { return a * 2; }',
        solutionExplanation:
          'Only the second satisfies both conditions: the same input always gives the same output, and nothing outside the function is touched. The first logs, which is a side effect. The third mutates the caller\'s array, which is also a side effect and visible to anyone else holding that array. The fourth returns a different value on every call, so it fails the first condition even though it changes nothing externally.',
      },
      {
        id: 'ex-m08-06-c',
        title: 'Compose small functions',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['functions', 'clean-code'],
        instructions:
          'Write three small functions and one that composes them. `cleanName(name)` trims whitespace; `capitalise(name)` makes the first letter uppercase and the rest lowercase; `formatName(name)` uses both to turn `"  aDA  "` into `"Ada"`. An empty or whitespace-only name must produce `""`.',
        starterCode: [
          'function cleanName(name) {',
          '  // Trim surrounding whitespace.',
          '}',
          '',
          'function capitalise(name) {',
          '  // First letter uppercase, rest lowercase. Handle an empty string.',
          '}',
          '',
          'function formatName(name) {',
          '  // Use the two functions above.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'cleanName trims', body: 'expect(cleanName("  ada  ")).toBe("ada");' },
          { name: 'capitalise fixes casing', body: 'expect(capitalise("aDA")).toBe("Ada");' },
          { name: 'capitalise handles an empty string', body: 'expect(capitalise("")).toBe("");' },
          { name: 'formatName combines both', body: 'expect(formatName("  aDA  ")).toBe("Ada");' },
          { name: 'formatName handles a clean name', body: 'expect(formatName("Grace")).toBe("Grace");' },
          { name: 'formatName handles whitespace only', body: 'expect(formatName("   ")).toBe("");' },
          { name: 'formatName handles a single letter', body: 'expect(formatName(" b ")).toBe("B");', hidden: true },
        ],
        hints: [
          '`cleanName` is a one-line `return name.trim();`.',
          '`capitalise` must guard the empty case before reading `name[0]`.',
          '`formatName` calls `capitalise(cleanName(name))` — inner first.',
        ],
        solution: [
          'function cleanName(name) {',
          '  return name.trim();',
          '}',
          '',
          'function capitalise(name) {',
          '  if (name.length === 0) return "";',
          '  return name[0].toUpperCase() + name.slice(1).toLowerCase();',
          '}',
          '',
          'function formatName(name) {',
          '  return capitalise(cleanName(name));',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Each function does exactly one job and can be tested alone, which is why the tests can check them independently before checking the combination. `formatName` reads inside-out: `cleanName` runs first, and its result becomes the argument to `capitalise`. The empty guard inside `capitalise` is what makes the whitespace-only case work — trimming `"   "` gives `""`, and `""[0]` is `undefined`, so `undefined.toUpperCase()` would throw without it. All three are pure, so none can surprise a caller.',
      },
      {
        id: 'ex-m08-06-d',
        title: 'Make an impure function pure',
        kind: EXERCISE_KIND.REFACTOR,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['functions', 'clean-code'],
        instructions:
          'This function mutates the array it receives, so the caller\'s data changes unexpectedly. Rewrite it to return a new array instead, leaving the input untouched.',
        starterCode: [
          'function withItem(list, item) {',
          '  list.push(item);',
          '  return list;',
          '}',
        ].join('\n'),
        tests: [
          { name: 'returns a list containing the new item', body: 'expect(withItem(["a"], "b")).toEqual(["a", "b"]);' },
          { name: 'does not modify the original', body: 'const original = ["a"]; withItem(original, "b"); expect(original).toEqual(["a"]);' },
          { name: 'returns a different array object', body: 'const original = ["a"]; expect(withItem(original, "b")).not.toBe(original);' },
          { name: 'works on an empty list', body: 'expect(withItem([], "x")).toEqual(["x"]);' },
          { name: 'can be called repeatedly without accumulating', body: 'const original = ["a"]; withItem(original, "b"); withItem(original, "c"); expect(original.length).toBe(1);', hidden: true },
        ],
        hints: [
          '`push` mutates. You need to build a new array.',
          'Spread the original into a new array literal, then add the item.',
          '`[...list, item]` does both in one expression.',
        ],
        solution: [
          'function withItem(list, item) {',
          '  return [...list, item];',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Spreading copies the existing elements into a brand-new array and the trailing `item` is appended, so the original is never touched — which the second and third tests verify from different angles: one checks the contents are unchanged, the other checks a genuinely different object was returned. The hidden test is the one that shows why this matters in practice: with the mutating version, repeated calls silently accumulate into the caller\'s array. The name `withItem` follows a common convention for pure transformations, signalling "a version of this list with the item" rather than "add the item to this list".',
      },
    ],
    quiz: {
      id: 'qz-m08-06',
      questions: [
        {
          id: 'q-m08-06-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['functions', 'scope'],
          prompt: 'What happens to variables declared inside a function when it returns?',
          options: [
            'They become global',
            'They cease to be accessible, and each new call creates a fresh set',
            'They keep their values for the next call',
            'They are copied to the caller',
          ],
          correct: 1,
          optionExplanations: [
            'Locals never leak into the global scope.',
            'Correct — this is what makes repeated calls independent.',
            'Persisting locals would make functions very hard to reason about.',
            'Only the returned value reaches the caller.',
          ],
          explanation:
            'Local variables exist for the duration of a call and are unreachable afterwards, and each call creates its own fresh set. That independence is exactly what makes a function safe to call from anywhere. The one exception is a variable captured by an inner function — a closure, covered in Module 32.',
        },
        {
          id: 'q-m08-06-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['functions'],
          prompt: 'What makes a function pure?',
          options: [
            'It has no parameters',
            'The same inputs always give the same output, and nothing outside the function is changed',
            'It contains no `if` statements',
            'It returns a primitive value',
          ],
          correct: 1,
          optionExplanations: [
            'Parameter count is irrelevant.',
            'Correct — both conditions must hold.',
            'Control flow has nothing to do with purity.',
            'A pure function can return an object.',
          ],
          explanation:
            'Purity requires both determinism and the absence of side effects. A function that logs, mutates its argument, reads the clock or calls `Math.random` fails one of the two. Pure functions are trivially testable because you never have to ask what else happened.',
        },
        {
          id: 'q-m08-06-3',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['functions', 'scope'],
          prompt: 'Why does the second call return 3 rather than 6?',
          code: [
            'function tally(n) {',
            '  let sum = 0;',
            '  for (let i = 0; i < n; i++) sum += 1;',
            '  return sum;',
            '}',
            'tally(3);',
            'console.log(tally(3));',
          ].join('\n'),
          options: ['6', '3', '0', 'undefined'],
          correct: 1,
          optionExplanations: [
            'That would require `sum` to persist between calls.',
            'Correct — each call starts with a fresh `sum` of 0.',
            'The loop does run and increments the sum.',
            'The function returns a number.',
          ],
          explanation:
            'Each call creates its own `sum`, initialised to 0. Nothing carries over from the previous call, so the result is the same every time for the same argument. This independence is what makes functions predictable.',
        },
        {
          id: 'q-m08-06-4',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['functions'],
          prompt: 'Which of these are side effects? (Select all that apply.)',
          options: [
            'Calling `console.log`',
            'Pushing to an array passed in as an argument',
            'Returning a computed value',
            'Assigning to a variable declared outside the function',
          ],
          correct: [0, 1, 3],
          explanation:
            'Logging, mutating an argument, and assigning to an outer variable all change something beyond the function\'s return value. Returning a computed value is the function\'s normal job and is not a side effect. Side effects are necessary in real programs — the aim is to keep them separate from calculation, not to eliminate them.',
        },
      ],
    },
    summary:
      'Variables declared inside a function are local to it: they vanish when the call ends, and every call creates a fresh set, which is what makes repeated calls independent and predictable. A function can read variables from the surrounding scope but the surrounding scope cannot see inside it — access runs outward only. Larger behaviour is built by having functions call other functions, so each step has a name, can be tested alone, and can be changed in one place. A **pure** function returns the same output for the same input and changes nothing outside itself, making it trivially testable; an **impure** one has side effects such as logging, mutating an argument, or assigning to an outer variable. Side effects are necessary, but keeping them separate from calculation is what keeps a codebase tractable.',
    keyTakeaways: [
      'Locals exist only during a call; each call gets a fresh set',
      'Functions read outward into enclosing scopes, never inward',
      'Compose small single-purpose functions rather than writing one large one',
      'A pure function is deterministic and changes nothing outside itself',
      'Mutating an argument is a side effect the caller can feel',
    ],
    relatedLessons: ['l-m08-05', 'l-m08-01'],
    interviewConnections: [
      'What is a pure function?',
      'What is a side effect?',
      'Why are local variables important for reusable functions?',
    ],
  },
];
