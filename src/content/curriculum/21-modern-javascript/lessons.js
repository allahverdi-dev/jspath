import { SECTION, CALLOUT_TONE, DIFFICULTY, EXERCISE_KIND, QUIZ_KIND } from '../../schema/types.js';

const M = 'm21';

export default [
  /* ================================================================== */
  {
    id: 'l-m21-01',
    slug: 'what-modern-javascript-means',
    moduleId: M,
    order: 1,
    title: 'What "Modern JavaScript" Actually Means',
    description: 'ECMAScript, the yearly release cycle, and why "ES6" is a misleading name for current JavaScript.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 14,
    xp: 30,
    topicIds: ['modern-js'],
    prerequisites: ['l-m20-06'],
    learningObjectives: [
      'Distinguish JavaScript from ECMAScript',
      'Explain the yearly release cycle and the TC39 stage process',
      'Explain why "ES6" is not a synonym for modern JavaScript',
      'Judge when a newer feature is genuinely the better choice',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Module 00 introduced the distinction briefly. It is worth stating precisely now, because the vocabulary appears constantly in documentation, job adverts and code review.',
          '**ECMAScript** is the specification — the formal document defining the language: syntax, semantics, built-in objects. **JavaScript** is the name of the language as implemented, which is what you actually write. Practically, people use the terms interchangeably, and that is usually fine; the distinction matters when reading the spec or discussing which version supports what.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'What is *not* ECMAScript',
        body: [
          'The DOM, `fetch`, timers, `localStorage`, `console` — none of these are in the ECMAScript specification. They come from the **host environment**: the browser (via the WHATWG and W3C specifications) or Node.',
          'This is the distinction Modules 17–20 kept returning to, and it is what lets you reason about where code will run. `Array.prototype.map` works anywhere JavaScript runs; `document.querySelector` only where a DOM exists.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The yearly release cycle',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'ES6 — properly **ES2015** — was a huge release after six years of stagnation. It brought `let`/`const`, arrow functions, classes, template literals, destructuring, spread, promises, modules and much more. It was such a leap that "ES6" became shorthand for "modern JavaScript".',
          'That shorthand is now badly out of date. Since 2015, ECMAScript has shipped **annually**, each release named for its year.',
        ],
      },
      {
        kind: SECTION.TABLE,
        headers: ['Release', 'Notable additions you have already used'],
        rows: [
          ['ES2015 (ES6)', '`let`/`const`, arrow functions, template literals, destructuring, spread/rest, default parameters, classes, `Promise`, `Map`/`Set`'],
          ['ES2016', '`Array.prototype.includes`, the `**` exponent operator'],
          ['ES2017', '`async`/`await`, `Object.entries`, `Object.values`, `String.padStart`'],
          ['ES2018', 'Object spread/rest, `Promise.finally`, named regex capture groups'],
          ['ES2019', '`Array.flat`, `Array.flatMap`, `Object.fromEntries`, optional `catch` binding'],
          ['ES2020', 'Optional chaining `?.`, nullish coalescing `??`, `BigInt`, `Promise.allSettled`'],
          ['ES2021', 'Logical assignment `||=` `&&=` `??=`, `String.replaceAll`, numeric separators'],
          ['ES2022', '`Array.at`, `Object.hasOwn`, class fields, top-level `await`'],
          ['ES2023', '`findLast`, `findLastIndex`, `toSorted`, `toReversed`'],
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: '"ES6" is not a synonym for "current JavaScript"',
        body: [
          'Optional chaining is ES2020. `Object.fromEntries` is ES2019. `async`/`await` is ES2017. Calling any of these "ES6 features" is simply wrong, and it obscures that JavaScript has kept evolving for a decade since.',
          'The useful modern phrasing is either the specific year (`?.` is ES2020) or simply "modern JavaScript" without a version number. Reserve "ES6" for things genuinely introduced in 2015.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'How a feature becomes part of the language',
      },
      {
        kind: SECTION.STEPS,
        title: 'The TC39 stage process',
        steps: [
          { title: 'Stage 0 — Strawperson', detail: 'An idea has been raised. No commitment of any kind.' },
          { title: 'Stage 1 — Proposal', detail: 'A champion is assigned and the problem is described. The shape may change completely.' },
          { title: 'Stage 2 — Draft', detail: 'Formal specification text exists. The syntax is roughly settled but not guaranteed.' },
          { title: 'Stage 3 — Candidate', detail: 'The specification is complete and browsers begin shipping implementations. Still not final — features have been changed or withdrawn at this stage.' },
          { title: 'Stage 4 — Finished', detail: 'Two independent implementations ship, tests pass, and it is included in the next yearly release. Only now is it genuinely part of the language.' },
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Stage 3 is not a promise',
        body: [
          'It is tempting to adopt an exciting Stage 3 proposal because a browser already supports it and a build tool can transform it. Occasionally that is the right call. But Stage 3 features have been substantially redesigned or dropped — the decorators proposal has been reworked repeatedly across many years.',
          'For production code, Stage 4 (or something long-shipped across every target browser) is the safe line.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Newer is not automatically better',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This module consolidates syntax that genuinely improves most code. But "modern" is not an argument on its own — each feature earns its place by making a specific thing clearer, and each can be misapplied.',
        ],
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Modern, and clearer',
          code: [
            'const { name, email } =',
            '  user;',
            '',
            'const merged = {',
            '  ...defaults,',
            '  ...overrides,',
            '};',
            '',
            '// Says what it does,',
            '// in fewer moving parts.',
          ].join('\n'),
        },
        right: {
          title: 'Modern, and worse',
          code: [
            'const {',
            '  a: { b: { c: [, d] = [] }',
            '    = {} } = {},',
            '} = input ?? {};',
            '',
            '// Every feature here is',
            '// current. Nobody can',
            '// read it, including the',
            '// person who wrote it.',
          ].join('\n'),
        },
        note: 'Both use only modern syntax. The question is never "is this modern?" but "does a reader understand this faster than the alternative?" Lesson 2 returns to this with destructuring specifically.',
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'What to actually check before using a feature',
        body: [
          'Does it run in every environment you support? For browsers, MDN\'s compatibility tables and caniuse.com answer this directly; for Node, the version matters. If a build step transforms your code, check what it targets.',
          'And separately: does it make *this* code clearer? A feature that is universally supported and still makes the line harder to read is not an improvement.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          '// Which release introduced optional chaining (?.) ?',
        ].join('\n'),
        options: [
          'ES2020',
          'ES2015 (ES6)',
          'ES2017',
          'It is not yet standard',
        ],
        correct: 0,
        explanation:
          'Optional chaining shipped in **ES2020**, five years after ES2015. Calling it an "ES6 feature" — as a great deal of writing does — is inaccurate, and it obscures the fact that JavaScript has had a yearly release every year since 2015. ES6/ES2015 introduced `let`/`const`, arrow functions, template literals, destructuring, spread and classes; `?.` and `??` both arrived in ES2020, `async`/`await` in ES2017, and `Object.fromEntries` in ES2019.',
      },
    ],
    exercises: [
      {
        id: 'ex-m21-01-a',
        title: 'ECMAScript or host API?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 15,
        topicIds: ['modern-js'],
        instructions: 'Which of these is defined by the ECMAScript specification?',
        options: [
          'document.querySelector',
          'Object.fromEntries',
          'localStorage.setItem',
          'fetch',
        ],
        correct: 1,
        hints: [
          'Which one works in Node, with no browser at all?',
        ],
        solution: 'Object.fromEntries',
        solutionExplanation:
          '`Object.fromEntries` is part of the language (ES2019) and works anywhere JavaScript runs. `document.querySelector`, `localStorage` and `fetch` are all **host APIs** supplied by the browser environment, defined by WHATWG/W3C specifications rather than ECMAScript. This is the same language-versus-host distinction Modules 17–20 relied on, and it is what tells you where a given piece of code can run.',
      },
      {
        id: 'ex-m21-01-b',
        title: 'Which release?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['modern-js'],
        instructions: 'A colleague calls `async`/`await` "an ES6 feature". Are they right?',
        options: [
          'Yes — ES6 introduced all modern syntax',
          'No — async/await is ES2017; ES6 (ES2015) introduced Promises but not async/await',
          'No — async/await is still a Stage 3 proposal',
          'Yes, but only in browsers',
        ],
        correct: 1,
        hints: [
          'ES2015 brought Promises. When did the syntax sugar over them arrive?',
        ],
        solution: 'No — async/await is ES2017; ES6 (ES2015) introduced Promises but not async/await',
        solutionExplanation:
          'ES2015 introduced `Promise` itself, but the `async`/`await` syntax built on top of it arrived two releases later in **ES2017**. "ES6" has become loose shorthand for "modern JavaScript", which obscures a decade of yearly releases since — optional chaining and nullish coalescing are ES2020, `Object.fromEntries` is ES2019, logical assignment is ES2021.',
      },
      {
        id: 'ex-m21-01-c',
        title: 'Is Stage 3 safe for production?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['modern-js'],
        instructions:
          'A proposal is at Stage 3 and your target browser already supports it. Is it safe to depend on in production code?',
        options: [
          'Yes — Stage 3 means the design is final',
          'Not necessarily — Stage 3 features have been redesigned or withdrawn; Stage 4 is the safe line',
          'No — Stage 3 features never ship',
          'Yes, because browser support is the only thing that matters',
        ],
        correct: 1,
        hints: [
          'What does Stage 4 add that Stage 3 does not guarantee?',
        ],
        solution: 'Not necessarily — Stage 3 features have been redesigned or withdrawn; Stage 4 is the safe line',
        solutionExplanation:
          'Stage 3 means the specification is complete enough for implementers to try it and gather feedback — which is exactly when problems surface. Features have been substantially reworked at this stage; decorators went through several incompatible redesigns over many years. Stage 4 requires two independent shipping implementations and passing tests, and is when the feature genuinely enters the language. Current browser support tells you a feature *runs today*, not that its design is settled.',
      },
      {
        id: 'ex-m21-01-d',
        title: 'Is modern always better?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['modern-js'],
        instructions:
          'A pull request replaces a clear three-line function with one deeply-nested destructuring expression. The reviewer objects. Who is right?',
        options: [
          'The author — the new version uses modern syntax',
          'The reviewer — modernity is not the goal; a reader understanding it faster is',
          'The author, because fewer lines is always better',
          'Neither — style is entirely subjective',
        ],
        correct: 1,
        hints: [
          'What is the actual purpose of the syntax being used?',
        ],
        solution: 'The reviewer — modernity is not the goal; a reader understanding it faster is',
        solutionExplanation:
          'Modern syntax earns its place by making a specific thing clearer, not by being recent. Deeply-nested destructuring with defaults at several levels uses entirely current features and is still frequently harder to read than the explicit version — including for the person who wrote it, three months later. "Fewer lines" is not the same as "clearer", and the two often diverge exactly where the syntax gets clever.',
      },
    ],
    quiz: {
      id: 'qz-m21-01',
      questions: [
        {
          id: 'q-m21-01-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['modern-js'],
          prompt: 'What is the relationship between JavaScript and ECMAScript?',
          options: [
            'They are competing languages',
            'ECMAScript is the specification; JavaScript is the language as implemented',
            'ECMAScript is a JavaScript framework',
            'JavaScript is a newer version of ECMAScript',
          ],
          correct: 1,
          optionExplanations: [
            'They describe the same language from different angles.',
            'Correct — spec versus implementation.',
            'It is a standards document, not a library.',
            'They are not sequential versions of each other.',
          ],
          explanation:
            'ECMAScript is the formal specification defining the language; JavaScript is that language as implemented and used. The terms are used interchangeably in practice, but the distinction matters when discussing versions or reading the spec.',
        },
        {
          id: 'q-m21-01-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['modern-js'],
          prompt: 'How often does a new ECMAScript version ship?',
          options: ['Every few years, unpredictably', 'Annually', 'Only when browsers agree', 'It stopped after ES6'],
          correct: 1,
          optionExplanations: [
            'That was the pre-2015 pattern.',
            'Correct — one release per year, named for the year.',
            'The release cadence is fixed by TC39.',
            'Releases have continued every year since.',
          ],
          explanation:
            'Since ES2015 the language has shipped annually, each release named for its year. This is why "ES6" is misleading shorthand — it names one release from a decade ago, not modern JavaScript as a whole.',
        },
        {
          id: 'q-m21-01-3',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['modern-js'],
          prompt: 'Which of these are NOT part of the ECMAScript specification? (Select all that apply.)',
          options: ['The DOM', 'fetch', 'Array.prototype.map', 'localStorage'],
          correct: [0, 1, 3],
          explanation:
            'The DOM, `fetch` and `localStorage` are all host APIs provided by the browser, defined outside ECMAScript. `Array.prototype.map` is part of the language and works in any JavaScript environment — which is exactly why the distinction is worth keeping straight.',
        },
        {
          id: 'q-m21-01-4',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['modern-js'],
          prompt: 'What does Stage 4 in the TC39 process mean?',
          options: [
            'The proposal is being discussed',
            'The feature is finished — two implementations ship, tests pass, and it enters the next yearly release',
            'Browsers have begun experimenting',
            'The proposal was rejected',
          ],
          correct: 1,
          optionExplanations: [
            'That is Stage 1.',
            'Correct — Stage 4 is the finished line.',
            'That is Stage 3.',
            'Rejected proposals leave the process entirely.',
          ],
          explanation:
            'Stage 4 requires two independent shipping implementations and passing conformance tests, after which the feature is included in the next annual release. Stage 3 means browsers are trying it out, and features have been redesigned or withdrawn at that point.',
        },
      ],
    },
    summary:
      '**ECMAScript** is the specification; **JavaScript** is the language as implemented — used interchangeably in practice, but the distinction matters for versions and for knowing what is *not* in the spec: the DOM, `fetch`, timers and `localStorage` are all **host APIs** from the browser, which is why `Array.prototype.map` runs anywhere and `document.querySelector` does not. ES6 — properly **ES2015** — was a landmark release after six years of stagnation, which is why its name became shorthand for "modern JavaScript". That shorthand is now a decade out of date: releases ship **annually**, and optional chaining and nullish coalescing are ES2020, `Object.fromEntries` is ES2019, `async`/`await` is ES2017, logical assignment is ES2021. Features enter the language through the **TC39 stage process**, and only **Stage 4** — two shipping implementations, passing tests — means finished; Stage 3 features have been redesigned or withdrawn. Finally, "modern" is never an argument on its own: each feature earns its place by making a specific thing clearer, and every one of them can be misapplied into something harder to read.',
    keyTakeaways: [
      'ECMAScript is the specification; JavaScript is the implemented language',
      'The DOM, fetch, timers and storage are host APIs, not ECMAScript',
      'Releases are annual and named by year — ES6 means ES2015 specifically',
      '?. and ?? are ES2020, not ES6',
      'Stage 4 means finished; Stage 3 can still change',
      'Newer is not automatically clearer — readability decides',
    ],
    relatedLessons: ['l-m00-03', 'l-m21-02'],
    interviewConnections: [
      'What is the difference between JavaScript and ECMAScript?',
      'Is optional chaining an ES6 feature?',
      'What does it mean for a proposal to reach Stage 4?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m21-02',
    slug: 'destructuring-in-depth',
    moduleId: M,
    order: 2,
    title: 'Destructuring in Depth',
    description: 'Array holes, nested patterns, parameter destructuring — and the point where it stops helping.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 18,
    xp: 40,
    topicIds: ['destructuring', 'modern-js'],
    prerequisites: ['l-m21-01'],
    learningObjectives: [
      'Skip elements and collect a rest in array destructuring',
      'Combine renaming, defaults and nesting deliberately',
      'Destructure function parameters, including nested options',
      'Judge when destructuring has stopped improving readability',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Modules 12 and 14 covered array and object destructuring separately. This lesson goes further: the combinations, the parameter forms, and — just as important — the point at which a pattern becomes less readable than the plain version.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Array destructuring: skipping and rest',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Holes skip positions; rest collects everything remaining.',
        code: [
          'const scores = [90, 75, 60, 45, 30];',
          '',
          'const [first] = scores;',
          'console.log("first     :", first);',
          '',
          '// A hole skips a position — note the leading comma',
          'const [, second] = scores;',
          'console.log("second    :", second);',
          '',
          'const [, , third] = scores;',
          'console.log("third     :", third);',
          '',
          '// Rest collects the remainder into a real array',
          'const [top, ...others] = scores;',
          'console.log("top       :", top);',
          'console.log("others    :", others);',
          '',
          '// Combined',
          'const [, runnerUp, ...rest] = scores;',
          'console.log("runner-up :", runnerUp);',
          'console.log("rest      :", rest);',
        ].join('\n'),
        output: [
          'first     : 90',
          'second    : 75',
          'third     : 60',
          'top       : 90',
          'others    : [75, 60, 45, 30]',
          'runner-up : 75',
          'rest      : [60, 45, 30]',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Holes are easy to miscount',
        body: [
          '`const [, , third]` needs exactly two commas to reach index 2. One comma too few or too many silently binds the wrong element — there is no error, just wrong data.',
          'Beyond one hole, `scores[2]` is almost always clearer. Skipping is at its best for a single leading skip, or with a well-known shape such as a returned pair.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Swapping and returning pairs',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Two idioms worth recognising instantly.',
        code: [
          'let a = 1;',
          'let b = 2;',
          '',
          '// Swap with no temporary variable',
          '[a, b] = [b, a];',
          'console.log("swapped:", a, b);',
          '',
          '// Returning several values as an array, destructured at the call site',
          'function minMax(values) {',
          '  return [Math.min(...values), Math.max(...values)];',
          '}',
          '',
          'const [lowest, highest] = minMax([4, 9, 1, 7]);',
          'console.log("min/max:", lowest, highest);',
        ].join('\n'),
        output: [
          'swapped: 2 1',
          'min/max: 1 9',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Array return values are positional — name them carefully',
        body: [
          'Returning `[min, max]` works because the caller knows the order. That is fine for a genuinely ordered pair, and it is why React\'s `useState` returns an array — the caller chooses both names.',
          'For three or more values, or where order carries no meaning, return an **object** instead. `const { width, height } = getSize()` cannot be silently mis-ordered; `const [height, width] = getSize()` can, and will be.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Combining rename, default and nesting',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'All three at once — and the order they are written in.',
        code: [
          'const response = {',
          '  data: {',
          '    user: { id: 7, displayName: "Ada" },',
          '  },',
          '  meta: { page: 2 },',
          '};',
          '',
          '// property: newName = default',
          'const {',
          '  data: {',
          '    user: { id: userId, displayName: name, role: role = "member" },',
          '  },',
          '  meta: { page, total: total = 0 },',
          '} = response;',
          '',
          'console.log("userId:", userId);',
          'console.log("name  :", name);',
          'console.log("role  :", role);',
          'console.log("page  :", page);',
          'console.log("total :", total);',
          '',
          '// Intermediate names are NOT created as variables',
          'console.log("typeof data:", typeof data);',
        ].join('\n'),
        output: [
          'userId: 7',
          'name  : Ada',
          'role  : member',
          'page  : 2',
          'total : 0',
          'typeof data: undefined',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The order within each entry is always `property: newName = default`. And note the last line: destructuring through `data` and `user` does **not** create variables called `data` or `user` — only the leaf names are bound. That surprises people who then try to use the intermediate object.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Parameter destructuring',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Named arguments, with defaults at two levels.',
        code: [
          'function createChart({',
          '  title = "Untitled",',
          '  size: { width = 400, height = 300 } = {},',
          '  labels = [],',
          '} = {}) {',
          '  return `${title}: ${width}x${height}, ${labels.length} labels`;',
          '}',
          '',
          'console.log(createChart());',
          'console.log(createChart({ title: "Sales" }));',
          'console.log(createChart({ title: "Sales", size: { width: 800 } }));',
          'console.log(createChart({ labels: ["a", "b"] }));',
        ].join('\n'),
        output: [
          'Untitled: 400x300, 0 labels',
          'Sales: 400x300, 0 labels',
          'Sales: 800x300, 0 labels',
          'Untitled: 400x300, 2 labels',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Every nesting level needs its own `= {}`',
        body: [
          'There are two defaults doing different jobs here. The trailing `= {}` on the whole parameter makes `createChart()` with **no argument** legal. The `= {}` after `size:` makes `createChart({ title: "x" })` legal when `size` is **absent**.',
          'Omit the inner one and `createChart({ title: "x" })` throws `Cannot read properties of undefined`, because the pattern tries to destructure `width` out of a `size` that does not exist. Each level you destructure through needs its own fallback.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The failure, made concrete.',
        code: [
          'function broken({ size: { width } }) {',
          '  return width;',
          '}',
          '',
          'console.log("with size:", broken({ size: { width: 10 } }));',
          '',
          'try {',
          '  broken({ title: "no size here" });',
          '} catch (error) {',
          '  console.log(error.name + ":", error.message);',
          '}',
        ].join('\n'),
        output: [
          'with size: 10',
          "TypeError: Cannot read properties of undefined (reading 'width')",
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Destructuring in loops and callbacks',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Where parameter destructuring pays off most.',
        code: [
          'const users = [',
          '  { id: 1, name: "Ada", role: "admin" },',
          '  { id: 2, name: "Grace", role: "user" },',
          '];',
          '',
          '// In a callback parameter',
          'console.log(users.map(({ name }) => name));',
          '',
          '// In a for...of',
          'for (const { id, name } of users) {',
          '  console.log(`${id}: ${name}`);',
          '}',
          '',
          '// Over Object.entries — the double-bracket shape from Module 15',
          'const counts = { js: 3, css: 1 };',
          'for (const [language, count] of Object.entries(counts)) {',
          '  console.log(`${language} = ${count}`);',
          '}',
        ].join('\n'),
        output: [
          '["Ada", "Grace"]',
          '1: Ada',
          '2: Grace',
          'js = 3',
          'css = 1',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'When to stop',
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Too clever',
          code: [
            'const {',
            '  a: { b: { c: [, d]',
            '    = [] } = {} } = {},',
            '} = input ?? {};',
            '',
            '// What is d? Which',
            '// default applied?',
            '// Is a hole skipping',
            '// one element or two?',
          ].join('\n'),
        },
        right: {
          title: 'Boring and readable',
          code: [
            'const items =',
            '  input?.a?.b?.c ?? [];',
            'const d = items[1];',
            '',
            '// Two lines. Obvious.',
            '// Optional chaining does',
            '// the guarding; the index',
            '// is explicit.',
          ].join('\n'),
        },
        note: 'Both are valid modern JavaScript. The right-hand version is easier to read, easier to debug, and easier to change. Destructuring is a tool for flattening one level of ceremony — not a competition.',
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'A practical rule',
        body: [
          'Destructuring earns its place at **one level of nesting**, with a handful of names. At two levels it is borderline. At three, or with defaults at multiple levels, the plain version with optional chaining is almost always clearer.',
          'The test is not "does this work?" — it is "can a colleague read this once and say what it binds?"',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const values = [10, 20, 30, 40];',
          'const [, x, ...y] = values;',
          '',
          'console.log(x, y);',
        ].join('\n'),
        options: [
          '20 [30, 40]',
          '10 [20, 30, 40]',
          '20 [30]',
          '30 [40]',
        ],
        correct: 0,
        explanation:
          'The leading comma is a **hole** that skips index 0, so `x` binds index 1 — the value `20`. The rest element then collects every remaining element into a new array, giving `[30, 40]`. Rest must come last in the pattern and always produces a real array, never a `NodeList`-style object. Miscounting holes is silent: one comma fewer would have bound `10` with no error at all, which is why more than a single skip is usually clearer written as an index.',
      },
    ],
    exercises: [
      {
        id: 'ex-m21-02-a',
        title: 'Predict the hole',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['destructuring'],
        instructions: 'What does this print?',
        code: [
          'const [, , c] = ["a", "b", "c", "d"];',
          'console.log(c);',
        ].join('\n'),
        options: ['"c"', '"b"', '"d"', '"a"'],
        correct: 0,
        hints: [
          'How many positions do two leading commas skip?',
        ],
        solution: '"c"',
        solutionExplanation:
          'Two leading commas create two holes, skipping indexes 0 and 1, so the binding lands on index 2 — `"c"`. Miscounting is silent: one comma fewer binds `"b"` with no error. This is why more than a single skip is usually clearer written as `arr[2]`.',
      },
      {
        id: 'ex-m21-02-b',
        title: 'Destructure a nested response',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['destructuring'],
        instructions:
          'Write `summarise(response)` returning `"<name> (<role>) — page <page>"`, reading `response.data.user.name`, `response.data.user.role` (defaulting to `"member"`) and `response.meta.page` (defaulting to `1`). Use destructuring.',
        starterCode: [
          'function summarise(response) {',
          '  // property: newName = default',
          '}',
        ].join('\n'),
        tests: [
          { name: 'reads all three values', body: 'expect(summarise({ data: { user: { name: "Ada", role: "admin" } }, meta: { page: 2 } })).toBe("Ada (admin) — page 2");' },
          { name: 'defaults the role', body: 'expect(summarise({ data: { user: { name: "Ada" } }, meta: { page: 3 } })).toBe("Ada (member) — page 3");' },
          { name: 'defaults the page', body: 'expect(summarise({ data: { user: { name: "Ada", role: "admin" } }, meta: {} })).toBe("Ada (admin) — page 1");' },
          { name: 'defaults both', body: 'expect(summarise({ data: { user: { name: "Grace" } }, meta: {} })).toBe("Grace (member) — page 1");' },
          { name: 'does not create intermediate variables', body: 'expect(summarise({ data: { user: { name: "A", role: "r" } }, meta: { page: 9 } })).toBe("A (r) — page 9");', hidden: true },
        ],
        hints: [
          'Nest the pattern to mirror the data shape.',
          '`role: role = "member"` renames-and-defaults; `role = "member"` alone also works when the names match.',
        ],
        solution: [
          'function summarise(response) {',
          '  const {',
          '    data: { user: { name, role = "member" } },',
          '    meta: { page = 1 },',
          '  } = response;',
          '',
          '  return `${name} (${role}) — page ${page}`;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The pattern mirrors the shape of the data, which is what makes nested destructuring readable when it stays at this depth. Because the variable names match the property names, `role = "member"` suffices — the explicit `role: role = "member"` rename form is only needed when the names differ. Note that `data`, `user` and `meta` are **not** bound as variables; destructuring *through* a level only binds the leaves.',
      },
      {
        id: 'ex-m21-02-c',
        title: 'Options object with nested defaults',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.HARD,
        xp: 30,
        topicIds: ['destructuring'],
        instructions:
          'Write `describeBox({ label, size })` returning `"<label>: <width>x<height>"`. Default `label` to `"box"`, `width` to `100`, `height` to `50`, and make calling `describeBox()` with no argument at all work.',
        starterCode: [
          'function describeBox() {',
          '  // Every level you destructure through needs its own fallback.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'uses all supplied values', body: 'expect(describeBox({ label: "card", size: { width: 200, height: 80 } })).toBe("card: 200x80");' },
          { name: 'defaults the whole size object', body: 'expect(describeBox({ label: "card" })).toBe("card: 100x50");' },
          { name: 'defaults one dimension', body: 'expect(describeBox({ label: "card", size: { width: 300 } })).toBe("card: 300x50");' },
          { name: 'defaults the label', body: 'expect(describeBox({ size: { width: 10, height: 20 } })).toBe("box: 10x20");' },
          { name: 'works with an empty object', body: 'expect(describeBox({})).toBe("box: 100x50");' },
          { name: 'works with no argument at all', body: 'expect(describeBox()).toBe("box: 100x50");' },
          { name: 'keeps an explicit zero', body: 'expect(describeBox({ size: { width: 0 } })).toBe("box: 0x50");', hidden: true },
        ],
        hints: [
          'The trailing `= {}` on the parameter handles a missing argument.',
          'A separate `= {}` after `size:` handles a missing size object.',
          'Defaults apply only to `undefined`, so an explicit `0` survives.',
        ],
        solution: [
          'function describeBox({',
          '  label = "box",',
          '  size: { width = 100, height = 50 } = {},',
          '} = {}) {',
          '  return `${label}: ${width}x${height}`;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Two separate `= {}` defaults do two different jobs. The trailing one makes `describeBox()` with no argument legal — without it, the pattern would try to destructure `undefined` and throw. The one after `size:` makes a missing `size` legal — without it, `describeBox({ label: "card" })` would throw when reaching for `width` inside a `size` that does not exist. The hidden test confirms the Module 14 rule that defaults apply only to `undefined`: an explicit `0` is kept, where a `||` fallback would have wrongly replaced it with 100.',
      },
      {
        id: 'ex-m21-02-d',
        title: 'Has this gone too far?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['destructuring'],
        instructions:
          'Which of these is the better way to read a possibly-missing deeply nested value?',
        options: [
          'const { a: { b: { c } = {} } = {} } = input ?? {};',
          'const c = input?.a?.b?.c;',
          'Both are equally readable',
          'Neither works',
        ],
        correct: 1,
        hints: [
          'Which one can a colleague read once and immediately explain?',
        ],
        solution: 'const c = input?.a?.b?.c;',
        solutionExplanation:
          'Both produce `undefined` when the path is missing, but optional chaining says exactly that in one obvious line, while the destructuring version needs a `= {}` at every level and forces the reader to track which default applies where. Destructuring earns its place at one level of nesting with a handful of names; past that, optional chaining is clearer, easier to debug and easier to change.',
      },
    ],
    quiz: {
      id: 'qz-m21-02',
      questions: [
        {
          id: 'q-m21-02-1',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['destructuring'],
          prompt: 'What does this bind?',
          code: [
            'const [a, ...b] = [1, 2, 3];',
            'console.log(a, b);',
          ].join('\n'),
          options: ['1 [2, 3]', '1 2', '[1] [2, 3]', '1 [3]'],
          correct: 0,
          optionExplanations: [
            'Correct — rest collects every remaining element into an array.',
            'Rest always produces an array, not a single value.',
            'The first binding is the element itself.',
            'Rest starts from the next index and takes everything.',
          ],
          explanation:
            'Rest collects all remaining elements into a real array, so `b` is `[2, 3]`. It must be the last element of the pattern.',
        },
        {
          id: 'q-m21-02-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['destructuring'],
          prompt: 'Why does function f({ size: { width } }) {} throw when called as f({})?',
          options: [
            'Because width has no default',
            'Because size is undefined, and the pattern tries to read width from it',
            'Because the parameter needs a name',
            'It does not throw',
          ],
          correct: 1,
          optionExplanations: [
            'A default on width would not help; the level above is missing.',
            'Correct — each level destructured through needs its own fallback.',
            'Destructured parameters need no separate name.',
            'It throws a TypeError.',
          ],
          explanation:
            'The pattern reaches into `size` to bind `width`, but `size` is `undefined`, so it throws `Cannot read properties of undefined`. Adding `= {}` after `size:` gives that level a fallback.',
        },
        {
          id: 'q-m21-02-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['destructuring'],
          prompt: 'In const { a: { b } } = obj, which variables are created?',
          options: ['Both a and b', 'Only b', 'Only a', 'Neither — it is a type annotation'],
          correct: 1,
          optionExplanations: [
            'Intermediate names are paths, not bindings.',
            'Correct — only the leaf names are bound.',
            'a is the path being traversed, not a variable.',
            'It is a real destructuring pattern.',
          ],
          explanation:
            'Destructuring *through* a level does not bind that level. Only the leaf names become variables, which surprises people who then try to use the intermediate object.',
        },
        {
          id: 'q-m21-02-4',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['destructuring'],
          prompt: 'When should a function return an object rather than an array?',
          options: [
            'Always — arrays are outdated',
            'When there are several values or the order carries no meaning, since object destructuring cannot be silently mis-ordered',
            'Only when returning more than ten values',
            'Never — arrays destructure more concisely',
          ],
          correct: 1,
          optionExplanations: [
            'Arrays are right for genuinely ordered pairs.',
            'Correct — naming beats position once order is arbitrary.',
            'The threshold is about meaning, not count.',
            'Conciseness is not worth a silent mis-ordering bug.',
          ],
          explanation:
            'An ordered pair like `[min, max]` is fine as an array — that is why `useState` returns one. For three or more values, or where order is arbitrary, an object makes mis-ordering impossible: `const { width, height }` cannot be silently swapped, while `const [height, width]` can.',
        },
      ],
    },
    summary:
      'Array destructuring supports **holes** (`const [, second]`) to skip positions and a trailing **rest** to collect the remainder into a real array. Holes are silent when miscounted — one comma fewer binds the wrong element with no error — so beyond a single skip, an index is clearer. Swapping with `[a, b] = [b, a]` and destructuring a returned pair are both worth recognising instantly; but return an **object** rather than an array once there are several values or the order is arbitrary, since `const { width, height }` cannot be mis-ordered and `const [height, width]` can. Within an object pattern the order is always `property: newName = default`, and destructuring **through** a level does not bind that level — only leaf names become variables. Parameter destructuring gives named arguments, and **every level you destructure through needs its own `= {}`**: the trailing one makes a missing argument legal, the inner ones make missing nested objects legal. Destructuring earns its place at one level of nesting with a handful of names; at three levels, or with defaults at several, optional chaining and an explicit index are clearer, more debuggable and easier to change.',
    keyTakeaways: [
      'Holes skip positions; rest collects the remainder as a real array',
      'Miscounted holes fail silently — prefer an index beyond one skip',
      'Return an object, not an array, once order carries no meaning',
      'The order within a pattern is property: newName = default',
      'Destructuring through a level does not bind that level',
      'Each nesting level needs its own = {} fallback',
    ],
    relatedLessons: ['l-m14-06', 'l-m21-03'],
    interviewConnections: [
      'How do you skip elements when destructuring an array?',
      'Why does destructuring a nested parameter throw when the outer object is missing?',
      'When should a function return an object instead of an array?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m21-03',
    slug: 'spread-versus-rest',
    moduleId: M,
    order: 3,
    title: 'Spread versus Rest',
    description: 'The same three dots doing opposite jobs — and the shallowness that catches everyone.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 17,
    xp: 40,
    topicIds: ['destructuring', 'modern-js'],
    prerequisites: ['l-m21-02'],
    learningObjectives: [
      'Tell spread and rest apart by their position',
      'Spread any iterable into an array or arguments',
      'Merge objects and predict which value wins',
      'Explain precisely what "shallow" means for both',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          '`...` means two opposite things depending on where it appears. Once you know which position signals which, the ambiguity disappears entirely.',
        ],
      },
      {
        kind: SECTION.TABLE,
        headers: ['', 'Spread', 'Rest'],
        rows: [
          ['Direction', '**Expands** one value into many', '**Collects** many values into one'],
          ['Where it appears', 'In a call, array literal or object literal', 'In a parameter list or destructuring pattern'],
          ['Position', 'Anywhere in the literal or call', '**Must be last**'],
          ['Example', '`f(...args)`, `[...a, ...b]`, `{ ...o }`', '`function f(...args)`, `const [a, ...rest] =`'],
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The same syntax, side by side.',
        code: [
          'const numbers = [1, 2, 3];',
          '',
          '// SPREAD — expands the array into separate arguments',
          'console.log("spread into call:", Math.max(...numbers));',
          '',
          '// REST — collects separate arguments into one array',
          'function collect(...args) {',
          '  return args;',
          '}',
          'console.log("rest in params  :", collect(1, 2, 3));',
          '',
          '// Both at once: rest collects, spread re-expands',
          'function forward(...args) {',
          '  return Math.max(...args);',
          '}',
          'console.log("collect + expand:", forward(4, 9, 2));',
        ].join('\n'),
        output: [
          'spread into call: 3',
          'rest in params  : [1, 2, 3]',
          'collect + expand: 9',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'The position tells you which it is',
        body: [
          'On the **left** of an `=`, or in a **parameter list**, it is rest — it is receiving. On the **right**, in a literal or a call, it is spread — it is supplying.',
          '`const [a, ...rest] = arr` collects. `const arr2 = [...arr]` expands. Same three dots, opposite direction.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Spread works on any iterable',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Arrays, strings, Sets and Maps all spread.',
        code: [
          'console.log("array  :", [...[1, 2]]);',
          'console.log("string :", [..."abc"]);',
          'console.log("Set    :", [...new Set([1, 1, 2])]);',
          'console.log("Map    :", [...new Map([["a", 1]])]);',
          '',
          '// Combining and inserting — spread can appear anywhere',
          'const a = [1, 2];',
          'const b = [5, 6];',
          'console.log("combined:", [...a, 3, 4, ...b]);',
          '',
          '// A plain object is NOT iterable — this throws',
          'try {',
          '  console.log([...{ a: 1 }]);',
          '} catch (error) {',
          '  console.log(error.name + ":", error.message);',
          '}',
        ].join('\n'),
        output: [
          'array  : [1, 2]',
          'string : ["a", "b", "c"]',
          'Set    : [1, 2]',
          'Map    : [["a", 1]]',
          'combined: [1, 2, 3, 4, 5, 6]',
          'TypeError: {(intermediate value)} is not iterable',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Array spread and object spread are different features',
        body: [
          'Spreading into an **array literal** (`[...x]`) requires `x` to be **iterable** — arrays, strings, `Set`, `Map`, `NodeList`. A plain object is not iterable, so `[...obj]` throws.',
          'Spreading into an **object literal** (`{ ...x }`) copies own enumerable **properties** and works on any object. They arrived in different releases too: array spread in ES2015, object spread not until ES2018.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Object spread and merge order',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Later properties win — which is the whole game.',
        code: [
          'const defaults = { theme: "light", size: 14, locale: "en" };',
          'const userSettings = { theme: "dark" };',
          '',
          'console.log("user last    :", { ...defaults, ...userSettings });',
          'console.log("defaults last:", { ...defaults, ...userSettings, ...defaults });',
          '',
          '// An explicit property also obeys position',
          'console.log("override     :", { ...defaults, size: 20 });',
          'console.log("overridden   :", { size: 20, ...defaults });',
        ].join('\n'),
        output: [
          'user last    : {theme: "dark", size: 14, locale: "en"}',
          'defaults last: {theme: "light", size: 14, locale: "en"}',
          'override     : {theme: "light", size: 20, locale: "en"}',
          'overridden   : {size: 14, theme: "light", locale: "en"}',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Defaults first, overrides last',
        body: [
          'Putting the defaults **last** silently overwrites everything the user chose — the second and fourth lines above show it happening. This is the most common spread bug in configuration code, and it produces no error, just an application that ignores its settings.',
          'Note also the fourth line: a repeated key keeps its **original position** in the object but takes the later value, which is why `size` appears first there but holds `14`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Both are shallow',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The top level is copied; nested references are shared.',
        code: [
          'const original = {',
          '  name: "Ada",',
          '  address: { city: "London" },',
          '  tags: ["admin"],',
          '};',
          '',
          'const copy = { ...original };',
          '',
          '// Top-level change: independent',
          'copy.name = "Grace";',
          '',
          '// Nested change: SHARED',
          'copy.address.city = "Paris";',
          'copy.tags.push("staff");',
          '',
          'console.log("original name :", original.name);',
          'console.log("original city :", original.address.city);',
          'console.log("original tags :", original.tags);',
          'console.log("same nested   :", original.address === copy.address);',
          '',
          '// Arrays behave identically',
          'const nested = [[1, 2], [3, 4]];',
          'const arrayCopy = [...nested];',
          'arrayCopy[0].push(99);',
          'console.log("nested array  :", nested[0]);',
        ].join('\n'),
        output: [
          'original name : Ada',
          'original city : Paris',
          'original tags : ["admin", "staff"]',
          'same nested   : true',
          'nested array  : [1, 2, 99]',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Spread never produces a deep copy',
        body: [
          'This is the reference rule from Module 14 again. A shallow copy copies the top-level **values** — and the value of a nested object is a **reference**, so the copy points at the same nested object.',
          'The symptom is the classic "I copied it, but changing the copy changed the original". The top level *was* copied; the nesting was not. Use a targeted nested spread for the path you change, or `structuredClone` when you genuinely need a full deep copy.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Copying only the path you actually change.',
        code: [
          'const state = {',
          '  user: { name: "Ada", address: { city: "London" } },',
          '  count: 1,',
          '};',
          '',
          'const moved = {',
          '  ...state,',
          '  user: {',
          '    ...state.user,',
          '    address: { ...state.user.address, city: "Paris" },',
          '  },',
          '};',
          '',
          'console.log("original city:", state.user.address.city);',
          'console.log("new city     :", moved.user.address.city);',
          'console.log("name kept    :", moved.user.name);',
          'console.log("count kept   :", moved.count);',
          'console.log("new objects  :", state.user !== moved.user, state.user.address !== moved.user.address);',
        ].join('\n'),
        output: [
          'original city: London',
          'new city     : Paris',
          'name kept    : Ada',
          'count kept   : 1',
          'new objects  : true true',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Each level along the changed path gets its own spread, so each becomes a new object; everything off that path is shared by reference, which is exactly what you want — it is both cheaper and correct. This is the standard immutable-update pattern in React and in state management generally.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const a = { x: 1, nested: { y: 2 } };',
          'const b = { ...a };',
          '',
          'b.x = 99;',
          'b.nested.y = 99;',
          '',
          'console.log(a.x, a.nested.y);',
        ].join('\n'),
        options: [
          '1 99',
          '99 99',
          '1 2',
          '99 2',
        ],
        correct: 0,
        explanation:
          'Spread copies the **top-level values**. `x` is a primitive, so `b` got its own independent copy and changing it leaves `a.x` at `1`. The value of `nested` is a **reference**, so `b.nested` points at the very same object as `a.nested` — mutating through either is visible through both, making `a.nested.y` become `99`. That split is precisely what "shallow" means, and it is the reference rule from Module 14 in its most commonly-encountered form.',
      },
    ],
    exercises: [
      {
        id: 'ex-m21-03-a',
        title: 'Predict the merge order',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['destructuring'],
        instructions: 'What does this print?',
        code: [
          'const d = { mode: "light" };',
          'const u = { mode: "dark" };',
          'console.log({ ...u, ...d }.mode);',
        ].join('\n'),
        options: ['"dark"', '"light"', 'undefined', 'Both'],
        correct: 1,
        hints: [
          'Which spread appears last?',
        ],
        solution: '"light"',
        solutionExplanation:
          'Properties are applied left to right and a later one overwrites an earlier one with the same key. Here `d` is spread **last**, so its `"light"` wins and the user\'s `"dark"` is discarded — the classic configuration bug. Defaults must come first and overrides last: `{ ...d, ...u }` would correctly give `"dark"`.',
      },
      {
        id: 'ex-m21-03-b',
        title: 'Spread or rest?',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['destructuring'],
        instructions:
          'Write `average(...numbers)` returning the mean of any number of arguments, and `0` when called with none.',
        starterCode: [
          'function average() {',
          '  // Collect the arguments, then reduce.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'averages several numbers', body: 'expect(average(2, 4, 6)).toBe(4);' },
          { name: 'handles a single number', body: 'expect(average(5)).toBe(5);' },
          { name: 'returns 0 for no arguments', body: 'expect(average()).toBe(0);' },
          { name: 'handles decimals', body: 'expect(average(1, 2)).toBe(1.5);' },
          { name: 'works with a spread array', body: 'expect(average(...[3, 5, 10])).toBe(6);' },
          { name: 'handles negatives', body: 'expect(average(-4, 4)).toBe(0);', hidden: true },
        ],
        hints: [
          '`function average(...numbers)` collects every argument into a real array.',
          'Guard the empty case before dividing, or you get NaN.',
        ],
        solution: [
          'function average(...numbers) {',
          '  if (numbers.length === 0) return 0;',
          '',
          '  const total = numbers.reduce((sum, n) => sum + n, 0);',
          '  return total / numbers.length;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Rest in the parameter list collects every argument into a **real array**, so `reduce` and `length` work directly — unlike the old `arguments` object, which is array-like but not an array. The empty guard matters: `0 / 0` is `NaN`, not `0`. The fifth test shows the symmetry — the caller uses **spread** to expand an array into arguments, which the function\'s **rest** then collects back into an array.',
      },
      {
        id: 'ex-m21-03-c',
        title: 'Update nested state immutably',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.HARD,
        xp: 30,
        topicIds: ['destructuring'],
        instructions:
          'Write `withCity(state, city)` returning a new state with `state.user.address.city` changed. Neither the original state, nor its `user`, nor its `address` may be modified, and all other properties must be preserved.',
        starterCode: [
          'function withCity(state, city) {',
          '  // Spread each level along the path you change.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'changes the city', body: 'expect(withCity({ user: { name: "A", address: { city: "London", zip: "E1" } }, count: 2 }, "Paris").user.address.city).toBe("Paris");' },
          { name: 'preserves sibling properties', body: 'const r = withCity({ user: { name: "A", address: { city: "L", zip: "E1" } }, count: 2 }, "P"); expect(r.user.name).toBe("A"); expect(r.user.address.zip).toBe("E1"); expect(r.count).toBe(2);' },
          { name: 'does not modify the original', body: 'const s = { user: { name: "A", address: { city: "London" } } }; withCity(s, "Paris"); expect(s.user.address.city).toBe("London");' },
          { name: 'creates a new address object', body: 'const s = { user: { address: { city: "L" } } }; expect(withCity(s, "P").user.address === s.user.address).toBe(false);' },
          { name: 'creates a new user object', body: 'const s = { user: { address: { city: "L" } } }; expect(withCity(s, "P").user === s.user).toBe(false);' },
          { name: 'creates a new state object', body: 'const s = { user: { address: { city: "L" } } }; expect(withCity(s, "P") === s).toBe(false);', hidden: true },
        ],
        hints: [
          'Spread at each of the three levels: state, user, address.',
          'Override `city` inside the innermost spread.',
        ],
        solution: [
          'function withCity(state, city) {',
          '  return {',
          '    ...state,',
          '    user: {',
          '      ...state.user,',
          '      address: { ...state.user.address, city },',
          '    },',
          '  };',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Each of the three tests checking object identity fails without a spread at that specific level — a single `{ ...state }` would leave `user` and `address` as shared references, so mutating them would be visible through the original. Spreading each level along the changed path creates a new object at each, while everything **off** that path stays shared by reference, which is both cheaper and correct. The shorthand `city` works because the parameter already has the right name. This is the standard immutable-update pattern in React state.',
      },
      {
        id: 'ex-m21-03-d',
        title: 'Why did the original change?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['destructuring'],
        instructions:
          'A developer writes `const copy = { ...original }`, then `copy.settings.theme = "dark"`, and finds `original.settings.theme` also changed. Why?',
        options: [
          'Spread is broken for objects',
          'Spread is shallow — the value of `settings` is a reference, so both objects point at the same nested object',
          'They forgot to use Object.assign',
          'The original was frozen',
        ],
        correct: 1,
        hints: [
          'What exactly gets copied when the property value is an object?',
        ],
        solution: 'Spread is shallow — the value of `settings` is a reference, so both objects point at the same nested object',
        solutionExplanation:
          'A shallow copy copies the top-level **values**, and the value of an object-typed property is a **reference**. So `copy.settings` and `original.settings` are the very same object, and mutating through either is visible through both. `Object.assign` behaves identically — it is shallow too. The fix is a targeted nested spread for the path being changed, or `structuredClone` when a genuine deep copy is needed.',
      },
    ],
    quiz: {
      id: 'qz-m21-03',
      questions: [
        {
          id: 'q-m21-03-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['destructuring'],
          prompt: 'How do you tell spread from rest?',
          options: [
            'They use different syntax',
            'By position — rest appears in a parameter list or on the left of a destructuring pattern; spread appears in a call or literal',
            'Rest only works with arrays',
            'Spread must always be last',
          ],
          correct: 1,
          optionExplanations: [
            'Both are three dots.',
            'Correct — receiving versus supplying.',
            'Rest works for objects too.',
            'It is rest that must be last.',
          ],
          explanation:
            'Position disambiguates them. Receiving — a parameter list, or the left of a `=` — is rest, and must come last. Supplying — a call, an array literal, an object literal — is spread, and may appear anywhere in it.',
        },
        {
          id: 'q-m21-03-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['destructuring'],
          prompt: 'Why does [...someObject] throw for a plain object?',
          options: [
            'Objects cannot be copied',
            'Array spread requires an iterable, and a plain object is not iterable',
            'You must use Object.assign instead',
            'It only fails in strict mode',
          ],
          correct: 1,
          optionExplanations: [
            'Objects copy fine with object spread.',
            'Correct — array spread and object spread are different features.',
            'Object spread works; the array form does not.',
            'It throws in any mode.',
          ],
          explanation:
            'Spreading into an **array** literal requires an iterable — arrays, strings, `Set`, `Map`, `NodeList`. Spreading into an **object** literal copies own enumerable properties and works on any object. They are separate features from separate releases.',
        },
        {
          id: 'q-m21-03-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['destructuring'],
          prompt: 'In { ...defaults, ...userSettings }, which values win on a key collision?',
          options: ['defaults', 'userSettings', 'Whichever is non-null', 'It throws on collision'],
          correct: 1,
          optionExplanations: [
            'Earlier properties are overwritten.',
            'Correct — later properties win.',
            'Position decides, not the value.',
            'Collisions are normal and silent.',
          ],
          explanation:
            'Properties are applied left to right, so the later spread overwrites the earlier one. Defaults must come first and overrides last — reversing them silently discards user choices.',
        },
        {
          id: 'q-m21-03-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['destructuring'],
          prompt: 'const copy = { ...original } produces a fully independent deep copy.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'Nested objects remain shared references.',
            'Correct — spread is always shallow.',
          ],
          explanation:
            'Spread copies top-level values only. Because the value of a nested object is a reference, the copy shares every nested object with the original. Use a targeted nested spread, or `structuredClone` for a genuine deep copy.',
        },
      ],
    },
    summary:
      '`...` is **spread** when supplying — in a call, array literal or object literal — and **rest** when receiving, in a parameter list or on the left of a destructuring pattern, where it must come **last**. Position is what disambiguates them. Array spread (`[...x]`) requires an **iterable** — arrays, strings, `Set`, `Map`, `NodeList` — so a plain object throws; object spread (`{ ...x }`) copies own enumerable properties from any object, and the two arrived in different releases (ES2015 and ES2018). In an object merge, properties apply left to right and **later ones win**, so defaults must come first and overrides last — reversing them silently ignores user settings, with a repeated key keeping its original position but the later value. Most importantly, **both forms are shallow**: the top-level values are copied, but the value of a nested object is a **reference**, so the copy shares every nested object. That is the source of "I copied it but changing the copy changed the original". Spread each level along the path you change — everything off that path stays shared, which is both cheaper and correct — or use `structuredClone` for a genuine deep copy.',
    keyTakeaways: [
      'Spread supplies and expands; rest receives and collects, and must be last',
      'Array spread needs an iterable; object spread works on any object',
      'Later properties win in a merge — defaults first, overrides last',
      'Both spread forms are shallow — nested objects stay shared',
      'Spread each level along the changed path for immutable updates',
      'Rest parameters give a real array, unlike the old arguments object',
    ],
    relatedLessons: ['l-m21-02', 'l-m21-04'],
    interviewConnections: [
      'What is the difference between spread and rest?',
      'Is object spread a deep copy?',
      'Which value wins when two spreads share a key?',
      'Why does spreading a plain object into an array throw?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m21-04',
    slug: 'optional-chaining-and-nullish',
    moduleId: M,
    order: 4,
    title: 'Optional Chaining, Nullish Coalescing and Logical Assignment',
    description: 'Three ES2020-era operators that react to null and undefined specifically — and nothing else.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 18,
    xp: 40,
    topicIds: ['modern-js'],
    prerequisites: ['l-m21-03'],
    learningObjectives: [
      'Use ?. for properties, calls and computed access',
      'State precisely what triggers short-circuiting',
      'Choose between || and ?? correctly for every falsy value',
      'Use the logical assignment operators appropriately',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Modules 14 and 15 introduced `?.` and `??`. This lesson pins down their exact semantics, because the difference between "falsy" and "nullish" is where most misuse comes from — and adds the logical assignment operators built on the same distinction.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The three forms of optional chaining',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Property, computed and call — each has its own syntax.',
        code: [
          'const user = {',
          '  name: "Ada",',
          '  greet() { return "hi"; },',
          '  tags: ["admin"],',
          '};',
          'const empty = {};',
          '',
          '// 1. Property access',
          'console.log("property:", user?.name, empty?.name);',
          '',
          '// 2. Computed access — note ?.[ not ?[',
          'const key = "name";',
          'console.log("computed:", user?.[key], empty?.[key]);',
          'console.log("index   :", user.tags?.[0], empty.tags?.[0]);',
          '',
          '// 3. Call — note ?.( not ?(',
          'console.log("call    :", user.greet?.(), empty.greet?.());',
        ].join('\n'),
        output: [
          'property: Ada undefined',
          'computed: Ada undefined',
          'index   : admin undefined',
          'call    : hi undefined',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'The optional call form is genuinely useful',
        body: [
          '`callback?.()` calls the function only if it exists — replacing the very common `if (callback) callback()`. It is ideal for optional handlers passed into a function.',
          'Note the syntax carefully: `?.(` and `?.[`, never `?(` or `?[`. The dot is always present.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'It reacts to null and undefined only',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Every other falsy value proceeds normally.',
        code: [
          'const cases = [null, undefined, 0, "", false, NaN];',
          '',
          'for (const value of cases) {',
          '  const wrapper = { value };',
          '  // toString exists on 0, "" and false — but not on null/undefined',
          '  const result = wrapper.value?.toString?.();',
          '  console.log(String(value).padEnd(9), "->", JSON.stringify(result));',
          '}',
        ].join('\n'),
        output: [
          'null      -> undefined',
          'undefined -> undefined',
          '0         -> "0"',
          '          -> ""',
          'false     -> "false"',
          'NaN       -> "NaN"',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Only `null` and `undefined` short-circuit. `0`, `""`, `false` and `NaN` are all falsy but perfectly real values, so the chain continues and their `toString` runs normally.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Optional chaining is not a general error guard',
        body: [
          'It protects against exactly one thing: reading a property of `null` or `undefined`. It does **not** catch a wrong type — `user.age?.toUpperCase()` still throws when `age` is a number, because `age` is not nullish so the call proceeds and numbers have no `toUpperCase`.',
          'It also cannot rescue an **undeclared identifier**: `notDeclared?.x` throws `ReferenceError`, because the variable itself does not exist. And it cannot appear on the left of an assignment.',
          'Sprinkling `?.` across a chain where only one level is genuinely optional hides real bugs — a missing `a.b` that *should* always exist becomes a silent `undefined` surfacing three functions later.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Nullish coalescing versus logical OR',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The complete comparison across every falsy value.',
        code: [
          'const values = [null, undefined, 0, "", false, NaN, "text", 42];',
          '',
          'console.log("value".padEnd(11) + "|| \'fallback\'".padEnd(16) + "?? \'fallback\'");',
          'console.log("-".repeat(45));',
          '',
          'for (const v of values) {',
          '  const withOr = v || "fallback";',
          '  const withNullish = v ?? "fallback";',
          '  console.log(',
          '    String(v).padEnd(11) + String(withOr).padEnd(16) + String(withNullish),',
          '  );',
          '}',
        ].join('\n'),
        output: [
          'value      || \'fallback\'   ?? \'fallback\'',
          '---------------------------------------------',
          'null       fallback        fallback',
          'undefined  fallback        fallback',
          '0          fallback        0',
          '           fallback        ',
          'false      fallback        false',
          'NaN        fallback        NaN',
          'text       text            text',
          '42         42              42',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'The four rows in the middle are the whole point',
        body: [
          '`||` replaces **every falsy value**, so a legitimate `0`, `""`, `false` or `NaN` is silently discarded. `??` replaces only `null` and `undefined`, preserving all four.',
          'This matters constantly in real code: a quantity of `0`, an empty search string the user deliberately cleared, an explicit `false` for a setting. `const qty = input.qty || 1` turns a genuine zero into one; `?? 1` does not.',
          'Use `||` only when you genuinely want "any falsy value should become the fallback" — which is rarer than it looks.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A realistic bug, and its fix.',
        code: [
          'function renderSettings(settings) {',
          '  const withOr = {',
          '    fontSize: settings.fontSize || 14,',
          '    showTips: settings.showTips || true,',
          '    label: settings.label || "Untitled",',
          '  };',
          '',
          '  const withNullish = {',
          '    fontSize: settings.fontSize ?? 14,',
          '    showTips: settings.showTips ?? true,',
          '    label: settings.label ?? "Untitled",',
          '  };',
          '',
          '  return { withOr, withNullish };',
          '}',
          '',
          '// A user who set the size to 0, disabled tips, and cleared the label',
          'const { withOr, withNullish } = renderSettings({',
          '  fontSize: 0,',
          '  showTips: false,',
          '  label: "",',
          '});',
          '',
          'console.log("with || :", withOr);',
          'console.log("with ?? :", withNullish);',
        ].join('\n'),
        output: [
          'with || : {fontSize: 14, showTips: true, label: "Untitled"}',
          'with ?? : {fontSize: 0, showTips: false, label: ""}',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The `||` version ignores **every choice the user made** — and note `showTips: false || true` is always `true`, so that setting can never be turned off at all. The `??` version respects all three. This is not a contrived example; it is the single most common `||` bug in configuration handling.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INTERVIEW,
        title: 'A very frequent interview question',
        body: [
          '"What is the difference between `||` and `??`?" — the expected answer names the trigger precisely: `||` falls back on any **falsy** value, `??` only on **nullish** (`null` or `undefined`).',
          'A strong answer gives the concrete consequence: `0 || 10` is `10` while `0 ?? 10` is `0`, and explains why that matters for a legitimate zero, an empty string, or an explicit `false`.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: '`??` cannot be mixed with `&&` or `||` unparenthesised',
        body: [
          'Writing `a || b ?? c` is a **SyntaxError**. This is deliberate: the precedence would be ambiguous to readers, so the language requires explicit parentheses — `(a || b) ?? c` or `a || (b ?? c)`.',
          'It is the rare case where JavaScript refuses to guess, and it prevents a genuinely confusing class of bug.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Logical assignment operators',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Three ES2021 operators, each following its own trigger rule.',
        code: [
          'const config = { size: 0, name: "", enabled: false, missing: undefined };',
          '',
          '// ??= assigns only when nullish',
          'config.size ??= 10;',
          'config.missing ??= "filled";',
          '',
          '// ||= assigns when falsy',
          'config.name ||= "default";',
          '',
          '// &&= assigns only when the existing value is truthy',
          'config.enabled &&= "was truthy";',
          '',
          'console.log(config);',
        ].join('\n'),
        output: [
          '{size: 0, name: "default", enabled: false, missing: "filled"}',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Each mirrors its non-assigning counterpart exactly. `??=` left `size: 0` alone because `0` is not nullish, but filled `missing`. `||=` overwrote the empty `name` because `""` is falsy — the same trap as `||`. And `&&=` left `enabled: false` alone because it only assigns when the existing value is already truthy.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Where each genuinely helps',
        body: [
          '`??=` is the useful one day to day: `options.retries ??= 3` sets a default without clobbering a deliberate `0`, and `cache[key] ??= expensive()` is a compact memoisation — note the right side is only evaluated when the assignment actually happens.',
          '`&&=` is niche; it reads as "update this only if it already has a value". `||=` carries the same falsy trap as `||`, so prefer `??=` unless you specifically want every falsy value replaced.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const settings = { count: 0, label: "" };',
          '',
          'const a = settings.count || 5;',
          'const b = settings.count ?? 5;',
          'const c = settings.label || "none";',
          'const d = settings.label ?? "none";',
          '',
          'console.log(a, b, JSON.stringify(c), JSON.stringify(d));',
        ].join('\n'),
        options: [
          '5 0 "none" ""',
          '0 0 "" ""',
          '5 5 "none" "none"',
          '0 5 "" "none"',
        ],
        correct: 0,
        explanation:
          '`||` falls back on any **falsy** value, and both `0` and `""` are falsy — so `a` becomes `5` and `c` becomes `"none"`, silently discarding two deliberate user values. `??` falls back only on **nullish** values, and neither `0` nor `""` is `null` or `undefined` — so `b` stays `0` and `d` stays `""`. This is the practical difference in one line: `||` asks "is this falsy?", `??` asks "is this missing?", and those are very different questions.',
      },
    ],
    exercises: [
      {
        id: 'ex-m21-04-a',
        title: 'Predict || versus ??',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['modern-js'],
        instructions: 'What does this print?',
        code: ['console.log(0 || "x", 0 ?? "x");'].join('\n'),
        options: ['"x" 0', '0 "x"', '"x" "x"', '0 0'],
        correct: 0,
        hints: [
          'Is 0 falsy? Is 0 nullish?',
        ],
        solution: '"x" 0',
        solutionExplanation:
          '`0` is falsy, so `||` replaces it with the fallback `"x"`. But `0` is **not** nullish — it is neither `null` nor `undefined` — so `??` keeps it. This is the entire practical difference: `||` asks "is this falsy?" while `??` asks "is this missing?", and a legitimate zero answers those two questions differently.',
      },
      {
        id: 'ex-m21-04-b',
        title: 'Fix the settings fallback',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['modern-js'],
        instructions:
          'This ignores a user who sets `volume` to 0 or `subtitles` to false. Fix it so only genuinely missing values get a default.',
        starterCode: [
          'function applyDefaults(settings) {',
          '  return {',
          '    volume: settings.volume || 50,',
          '    subtitles: settings.subtitles || true,',
          '    language: settings.language || "en",',
          '  };',
          '}',
        ].join('\n'),
        tests: [
          { name: 'keeps a volume of zero', body: 'expect(applyDefaults({ volume: 0 }).volume).toBe(0);' },
          { name: 'keeps subtitles set to false', body: 'expect(applyDefaults({ subtitles: false }).subtitles).toBe(false);' },
          { name: 'defaults a missing volume', body: 'expect(applyDefaults({}).volume).toBe(50);' },
          { name: 'defaults missing subtitles', body: 'expect(applyDefaults({}).subtitles).toBe(true);' },
          { name: 'defaults a missing language', body: 'expect(applyDefaults({}).language).toBe("en");' },
          { name: 'keeps a supplied language', body: 'expect(applyDefaults({ language: "fr" }).language).toBe("fr");' },
          { name: 'keeps an explicitly empty language', body: 'expect(applyDefaults({ language: "" }).language).toBe("");', hidden: true },
          { name: 'defaults an explicitly null volume', body: 'expect(applyDefaults({ volume: null }).volume).toBe(50);', hidden: true },
        ],
        hints: [
          '`||` replaces every falsy value, including 0, "" and false.',
          '`??` replaces only null and undefined.',
        ],
        solution: [
          'function applyDefaults(settings) {',
          '  return {',
          '    volume: settings.volume ?? 50,',
          '    subtitles: settings.subtitles ?? true,',
          '    language: settings.language ?? "en",',
          '  };',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Switching all three to `??` means only genuinely absent values get a default. The `subtitles` case is the most striking: with `||`, `false || true` is always `true`, so the setting could **never** be turned off. The two hidden tests pin down the boundary precisely — an explicitly empty string is a real value and is kept, while an explicit `null` is nullish and correctly falls back.',
      },
      {
        id: 'ex-m21-04-c',
        title: 'Safe optional callback',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['modern-js'],
        instructions:
          'Write `notify(handlers, message)` that calls `handlers.onMessage(message)` if it exists and returns its result, or returns `"no handler"` if it does not. Do not use `if`.',
        starterCode: [
          'function notify(handlers, message) {',
          '  // The optional call form, paired with a nullish fallback.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'calls the handler', body: 'expect(notify({ onMessage: (m) => `got ${m}` }, "hi")).toBe("got hi");' },
          { name: 'handles a missing handler', body: 'expect(notify({}, "hi")).toBe("no handler");' },
          { name: 'handles an undefined handlers object', body: 'expect(notify(undefined, "hi")).toBe("no handler");' },
          { name: 'passes the message through', body: 'expect(notify({ onMessage: (m) => m.toUpperCase() }, "abc")).toBe("ABC");' },
          { name: 'keeps a handler that returns an empty string', body: 'expect(notify({ onMessage: () => "" }, "x")).toBe("");', hidden: true },
        ],
        hints: [
          '`handlers?.onMessage?.(message)` guards both the object and the function.',
          'Use `??` rather than `||` for the fallback.',
        ],
        solution: [
          'function notify(handlers, message) {',
          '  return handlers?.onMessage?.(message) ?? "no handler";',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Two optional-chaining guards handle two different risks: `handlers?.` covers a missing object, and `?.(` covers a missing function on an object that does exist. Note the syntax is `?.(`, never `?(`. The `??` fallback is what makes the hidden test pass — a handler legitimately returning an empty string is kept, where `||` would have wrongly replaced it with `"no handler"`.',
      },
      {
        id: 'ex-m21-04-d',
        title: 'What optional chaining does not catch',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['modern-js'],
        instructions:
          'Which of these still throws, despite the optional chaining?',
        options: [
          'user?.address?.city where address is undefined',
          'user.age?.toUpperCase() where age is the number 30',
          'user?.name where user is null',
          'items?.[0] where items is undefined',
        ],
        correct: 1,
        hints: [
          'Is a number nullish? What happens after the chain does not short-circuit?',
        ],
        solution: 'user.age?.toUpperCase() where age is the number 30',
        solutionExplanation:
          '`30` is not `null` or `undefined`, so `?.` does **not** short-circuit — the call proceeds, and numbers have no `toUpperCase` method, so it throws `TypeError`. Optional chaining guards exactly one thing: reading through a nullish value. It is not a general error guard, does not protect against a wrong type, and cannot rescue an undeclared identifier (`notDeclared?.x` throws `ReferenceError`). The other three all short-circuit correctly to `undefined`.',
      },
    ],
    quiz: {
      id: 'qz-m21-04',
      questions: [
        {
          id: 'q-m21-04-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['modern-js'],
          prompt: 'What exactly causes ?. to short-circuit?',
          options: [
            'Any falsy value',
            'Only null or undefined',
            'Only undefined',
            'Any value that is not an object',
          ],
          correct: 1,
          optionExplanations: [
            '0, "" and false are falsy but do not short-circuit.',
            'Correct — nullish means null or undefined.',
            'null triggers it too.',
            'Primitives like strings have properties and proceed normally.',
          ],
          explanation:
            'Only `null` and `undefined` short-circuit a chain. `0`, `""`, `false` and `NaN` are falsy but real values, so the chain continues — which is why `user.age?.toUpperCase()` still throws for a numeric age.',
        },
        {
          id: 'q-m21-04-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['modern-js'],
          prompt: 'What does this print?',
          code: ['console.log("" || "a", "" ?? "a");'].join('\n'),
          options: ['"a" ""', '"" "a"', '"a" "a"', '"" ""'],
          correct: 0,
          optionExplanations: [
            'Correct — "" is falsy but not nullish.',
            'This has the two operators reversed.',
            '?? keeps the empty string.',
            '|| replaces the empty string.',
          ],
          explanation:
            'An empty string is falsy, so `||` replaces it. It is not nullish, so `??` keeps it — which matters whenever an empty value is a deliberate user choice, such as a cleared search field.',
        },
        {
          id: 'q-m21-04-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['modern-js'],
          prompt: 'Why is a || b ?? c a SyntaxError?',
          options: [
            '?? cannot be used with strings',
            'The language requires explicit parentheses because the precedence would be ambiguous to readers',
            '?? must come first',
            'It is valid syntax',
          ],
          correct: 1,
          optionExplanations: [
            'Operand types are irrelevant.',
            'Correct — JavaScript refuses to guess here.',
            'Order is not the issue; mixing unparenthesised is.',
            'It genuinely throws a SyntaxError.',
          ],
          explanation:
            'Mixing `??` with `&&` or `||` without parentheses is deliberately forbidden, because the intended precedence would not be obvious to a reader. Write `(a || b) ?? c` or `a || (b ?? c)` explicitly.',
        },
        {
          id: 'q-m21-04-4',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['modern-js'],
          prompt: 'What does options.retries ??= 3 do?',
          options: [
            'Always sets retries to 3',
            'Sets retries to 3 only if it is currently null or undefined — leaving a deliberate 0 alone',
            'Sets retries to 3 if it is falsy',
            'Compares retries with 3',
          ],
          correct: 1,
          optionExplanations: [
            'It assigns conditionally.',
            'Correct — it mirrors ?? exactly.',
            'That is ||=, which would clobber a zero.',
            'It assigns, not compares.',
          ],
          explanation:
            '`??=` assigns only when the current value is nullish, so a deliberate `0` survives. `||=` would replace it, carrying the same falsy trap as `||`. The right-hand side is only evaluated when the assignment actually happens, which makes `cache[key] ??= expensive()` a compact memoisation.',
        },
      ],
    },
    summary:
      'Optional chaining has three forms — `?.` for properties, **`?.[`** for computed access, and **`?.(`** for calls (never `?[` or `?(`) — and `callback?.()` neatly replaces `if (callback) callback()`. It short-circuits on **`null` or `undefined` only**: `0`, `""`, `false` and `NaN` are falsy but real, so the chain continues. It is therefore **not a general error guard** — `user.age?.toUpperCase()` still throws for a numeric age, `notDeclared?.x` still throws `ReferenceError`, and it cannot appear on the left of an assignment. Sprinkling it where only one level is genuinely optional hides real bugs. `??` follows the same nullish rule, which is the whole difference from `||`: `||` replaces **every falsy value**, so a legitimate `0`, `""` or `false` is silently discarded — `settings.subtitles || true` can never be turned off — while `??` preserves all of them. Mixing `??` with `&&`/`||` unparenthesised is a deliberate **SyntaxError**. The logical assignment operators mirror their counterparts: prefer **`??=`** (`options.retries ??= 3` keeps a deliberate `0`, and its right side evaluates only when assigning), treat `||=` with the same caution as `||`, and reach for `&&=` rarely.',
    keyTakeaways: [
      'Syntax is ?., ?.[ and ?.( — the dot is always present',
      '?. and ?? trigger on null and undefined only, never other falsy values',
      '?. is not a general error guard — wrong types still throw',
      '|| replaces every falsy value; ?? replaces only missing ones',
      'a || b ?? c is a SyntaxError — parenthesise explicitly',
      'Prefer ??= over ||= so a deliberate 0 or "" survives',
    ],
    relatedLessons: ['l-m14-03', 'l-m21-05'],
    interviewConnections: [
      'What is the difference between || and ??',
      'What exactly makes optional chaining short-circuit?',
      'Does optional chaining protect against all errors?',
      'What does ??= do, and how does it differ from ||=?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m21-05',
    slug: 'modern-patterns-and-judgment',
    moduleId: M,
    order: 5,
    title: 'Modern Patterns and Judgment',
    description: 'Combining the features in realistic code, previewing modules and classes, and knowing when to stop.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 17,
    xp: 40,
    topicIds: ['modern-js'],
    prerequisites: ['l-m21-04'],
    learningObjectives: [
      'Combine modern features in realistic code',
      'Recognise import/export and class syntax when reading',
      'Refactor older code where it genuinely improves clarity',
      'Explain when a modern feature makes code worse',
    ],
    sections: [
      {
        kind: SECTION.HEADING,
        text: 'Everything together',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'One realistic function using most of this module.',
        code: [
          'function summariseOrders(orders = [], { currency = "GBP", limit } = {}) {',
          '  const paid = orders.filter(({ status }) => status === "paid");',
          '',
          '  const rows = paid',
          '    .slice(0, limit ?? paid.length)',
          '    .map(({ id, total, customer }) => ({',
          '      id,',
          '      total,',
          '      name: customer?.name ?? "Guest",',
          '    }));',
          '',
          '  const revenue = rows.reduce((sum, { total }) => sum + total, 0);',
          '',
          '  return { currency, count: rows.length, revenue, rows };',
          '}',
          '',
          'const orders = [',
          '  { id: 1, total: 50, status: "paid", customer: { name: "Ada" } },',
          '  { id: 2, total: 30, status: "pending", customer: { name: "Bob" } },',
          '  { id: 3, total: 90, status: "paid" },',
          '];',
          '',
          'const all = summariseOrders(orders);',
          'console.log("count  :", all.count);',
          'console.log("revenue:", all.revenue);',
          'console.log("names  :", all.rows.map(({ name }) => name));',
          '',
          'const limited = summariseOrders(orders, { limit: 1, currency: "USD" });',
          'console.log("limited:", limited.count, limited.currency);',
          '',
          'console.log("no args:", summariseOrders().count);',
        ].join('\n'),
        output: [
          'count  : 2',
          'revenue: 140',
          'names  : ["Ada", "Guest"]',
          'limited: 1 USD',
          'no args: 0',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Count the features: default parameters, a destructured options object with its own `= {}`, destructuring in callback parameters, shorthand properties, optional chaining, nullish coalescing, and shorthand object returns. Each does one small job, and no single line is hard to read — which is the standard to aim for.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Modules — a preview',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        caption: 'Recognising import/export syntax. Module 28 covers this properly.',
        code: [
          '// Named exports — a file can have many',
          'export function formatPrice(value) { /* … */ }',
          'export const TAX_RATE = 0.2;',
          '',
          '// Default export — at most one per file',
          'export default function Button() { /* … */ }',
          '',
          '// Importing',
          'import Button from "./Button.js";',
          'import { formatPrice, TAX_RATE } from "./money.js";',
          'import { formatPrice as format } from "./money.js";',
          'import * as money from "./money.js";',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Two things worth knowing now',
        body: [
          'Named imports use **braces** and must match the exported name (or be renamed with `as`); a default import has **no braces** and you choose the name freely. Getting the braces wrong is the most common early module error.',
          'Every ES module is **strict mode automatically** — which is why Modules 05 and 14 kept noting that sloppy-mode behaviour does not apply. Module 28 covers the rest, including why imports are hoisted and live.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Classes — a preview',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Enough to read class syntax. Module 31 covers the semantics.',
        code: [
          'class Account {',
          '  #balance = 0;              // private field',
          '  static currency = "GBP";   // static field',
          '',
          '  constructor(owner) {',
          '    this.owner = owner;',
          '  }',
          '',
          '  deposit(amount) {',
          '    this.#balance += amount;',
          '    return this;',
          '  }',
          '',
          '  get balance() {            // getter, as in Module 15',
          '    return this.#balance;',
          '  }',
          '}',
          '',
          'const account = new Account("Ada").deposit(50).deposit(25);',
          '',
          'console.log("owner   :", account.owner);',
          'console.log("balance :", account.balance);',
          'console.log("currency:", Account.currency);',
          '',
          '// The private field is genuinely hidden — it is not an own property',
          'console.log("own keys:", Object.keys(account));',
          'console.log("in check:", "balance" in account, Object.hasOwn(account, "#balance"));',
        ].join('\n'),
        output: [
          'owner   : Ada',
          'balance : 75',
          'currency: GBP',
          'own keys: ["owner"]',
          'in check: true false',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Private access fails at parse time, not run time',
        body: [
          'Writing `account.#balance` outside the class is a **SyntaxError**, not a catchable runtime error — the file fails to parse, so a `try`/`catch` around it cannot help. That is stronger enforcement than most languages offer, and stronger than the `_balance` convention from Module 15, which anyone can simply ignore.',
          '`Object.keys` above shows only `owner`, because `#balance` is not a property at all — it is a separate, genuinely inaccessible slot. The `balance` reported by `in` is the **getter**, which is a public member of the class.',
        ],
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Three things to recognise: `#balance` is a genuinely **private** field — unlike the `_balance` naming convention from Module 15, this is enforced by the language. `static` members belong to the class rather than to instances. And returning `this` from a method enables the chaining you see on the `new Account(...)` line.',
          'Module 31 covers inheritance, `super`, and how classes relate to the prototype system underneath.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Refactoring with judgment',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A genuine improvement.',
        code: [
          '// Older style',
          'function oldWay(users, options) {',
          '  options = options || {};',
          '  var role = options.role;',
          '  if (role === undefined) role = "member";',
          '',
          '  var names = [];',
          '  for (var i = 0; i < users.length; i++) {',
          '    if (users[i].role === role) {',
          '      names.push(users[i].name);',
          '    }',
          '  }',
          '  return names;',
          '}',
          '',
          '// Modern equivalent',
          'function newWay(users, { role = "member" } = {}) {',
          '  return users',
          '    .filter((user) => user.role === role)',
          '    .map(({ name }) => name);',
          '}',
          '',
          'const users = [',
          '  { name: "Ada", role: "admin" },',
          '  { name: "Grace", role: "member" },',
          '  { name: "Alan", role: "member" },',
          '];',
          '',
          'console.log("old:", oldWay(users));',
          'console.log("new:", newWay(users));',
          'console.log("old admin:", oldWay(users, { role: "admin" }));',
          'console.log("new admin:", newWay(users, { role: "admin" }));',
        ].join('\n'),
        output: [
          'old: ["Grace", "Alan"]',
          'new: ["Grace", "Alan"]',
          'old admin: ["Ada"]',
          'new admin: ["Ada"]',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Eleven lines become three, and every removed line was ceremony rather than logic: manual option defaulting, an index variable, a manual accumulator. The modern version states the transformation directly.',
          'Note one genuine behavioural difference: the old version\'s `options || {}` would also replace a deliberately-passed falsy value, while the parameter default triggers only on `undefined`. That is a fix, not just a shortening — but it is worth noticing rather than assuming a refactor is always behaviour-preserving.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Refactors that make things worse',
        body: [
          '**Chaining past readability.** Six chained array methods over one long expression is harder to debug than three named intermediate variables — you cannot inspect a step that has no name.',
          '**Arrow functions as object methods.** Module 14 covered this: an arrow has no `this` of its own, so it silently reads the enclosing scope instead of the object.',
          '**Replacing a clear loop with a contrived `reduce`.** If `reduce` needs a comment to explain the accumulator, a loop was clearer.',
          '**`||` to `??` without thinking.** They differ. Changing one is a behaviour change, not a modernisation — check whether a falsy value is actually possible there.',
        ],
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Too much in one expression',
          code: [
            'return data.filter(x => x.a)',
            '  .map(x => x.b)',
            '  .filter(Boolean)',
            '  .map(x => x.trim())',
            '  .filter(x => x.length > 2)',
            '  .sort();',
            '',
            '// Which step produced',
            '// the wrong output?',
          ].join('\n'),
        },
        right: {
          title: 'Named stages',
          code: [
            'const active =',
            '  data.filter(x => x.a);',
            'const labels = active',
            '  .map(x => x.b)',
            '  .filter(Boolean);',
            '',
            'return labels',
            '  .map(x => x.trim())',
            '  .filter(x => x.length > 2)',
            '  .sort();',
          ].join('\n'),
        },
        note: 'Both use identical features. The right-hand version can be inspected at each stage in a debugger and read one idea at a time. Naming intermediate results is not old-fashioned — it is how you make a pipeline debuggable.',
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const obj = {',
          '  name: "Ada",',
          '  regular() { return this?.name; },',
          '  arrow: () => this?.name,',
          '};',
          '',
          'console.log(obj.regular(), obj.arrow());',
        ].join('\n'),
        options: [
          'Ada undefined',
          'Ada Ada',
          'undefined undefined',
          'undefined Ada',
        ],
        correct: 0,
        explanation:
          '`obj.regular()` has `obj` to the left of the dot, so `this` is `obj` and it returns `"Ada"`. The arrow function has **no `this` of its own** — it inherits from the enclosing scope, and an object literal is not a scope, so it looks past it to the module top level where `this` is `undefined`. Calling it as `obj.arrow()` cannot change that, because an arrow\'s `this` is fixed lexically rather than at call time. This is why "modernising" methods into arrows is a genuine behaviour change, not a cosmetic one.',
      },
    ],
    exercises: [
      {
        id: 'ex-m21-05-a',
        title: 'Modernise a verbose function',
        kind: EXERCISE_KIND.REFACTOR,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 30,
        topicIds: ['modern-js'],
        instructions:
          'Rewrite this using modern syntax — a destructured options parameter with a default, and array methods instead of the manual loop. Behaviour must be identical for the tested cases.',
        starterCode: [
          'function activeNames(users, options) {',
          '  options = options || {};',
          '  var minAge = options.minAge;',
          '  if (minAge === undefined) minAge = 18;',
          '',
          '  var result = [];',
          '  for (var i = 0; i < users.length; i++) {',
          '    if (users[i].active && users[i].age >= minAge) {',
          '      result.push(users[i].name);',
          '    }',
          '  }',
          '  return result;',
          '}',
        ].join('\n'),
        tests: [
          { name: 'filters by active and default age', body: 'expect(activeNames([{ name: "A", active: true, age: 20 }, { name: "B", active: false, age: 30 }, { name: "C", active: true, age: 15 }])).toEqual(["A"]);' },
          { name: 'honours a supplied minAge', body: 'expect(activeNames([{ name: "A", active: true, age: 20 }, { name: "B", active: true, age: 30 }], { minAge: 25 })).toEqual(["B"]);' },
          { name: 'works with no options', body: 'expect(activeNames([{ name: "A", active: true, age: 18 }])).toEqual(["A"]);' },
          { name: 'works with an empty options object', body: 'expect(activeNames([{ name: "A", active: true, age: 18 }], {})).toEqual(["A"]);' },
          { name: 'returns an empty array when nothing matches', body: 'expect(activeNames([{ name: "A", active: false, age: 40 }])).toEqual([]);' },
          { name: 'handles an empty user list', body: 'expect(activeNames([])).toEqual([]);' },
          { name: 'honours a minAge of zero', body: 'expect(activeNames([{ name: "A", active: true, age: 0 }], { minAge: 0 })).toEqual(["A"]);', hidden: true },
        ],
        hints: [
          'Destructure the options parameter with `{ minAge = 18 } = {}`.',
          '`filter` then `map` replaces the loop and the accumulator.',
        ],
        solution: [
          'function activeNames(users, { minAge = 18 } = {}) {',
          '  return users',
          '    .filter((user) => user.active && user.age >= minAge)',
          '    .map(({ name }) => name);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The destructured parameter with `{ minAge = 18 } = {}` replaces four lines of manual defaulting, and `filter` plus `map` replaces the index variable and accumulator. The hidden test is the interesting one: a `minAge` of `0` survives here because parameter defaults trigger only on `undefined` — the original\'s `options || {}` combined with an explicit `undefined` check happened to handle this too, but a naive `options.minAge || 18` refactor would have broken it. Refactors are behaviour changes until proven otherwise.',
      },
      {
        id: 'ex-m21-05-b',
        title: 'Read the import',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['modern-js', 'modules'],
        instructions:
          'A file has `export default function Button() {}` and `export const SIZES = [];`. Which import is correct?',
        options: [
          'import { Button, SIZES } from "./Button.js";',
          'import Button, { SIZES } from "./Button.js";',
          'import { default as Button }, SIZES from "./Button.js";',
          'import Button, SIZES from "./Button.js";',
        ],
        correct: 1,
        hints: [
          'Which one needs braces — the default export or the named one?',
        ],
        solution: 'import Button, { SIZES } from "./Button.js";',
        solutionExplanation:
          'A **default** export is imported without braces and you may name it anything. A **named** export must be imported inside braces, matching its exported name (or renamed with `as`). Combining both puts the default first, then the braced named imports. The first option wrongly braces the default, and the last wrongly omits braces from the named export — both are the most common early module errors.',
      },
      {
        id: 'ex-m21-05-c',
        title: 'Spot the bad refactor',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 20,
        topicIds: ['modern-js'],
        instructions:
          'A pull request changes `greet: function () { return this.name; }` to `greet: () => this.name` inside an object literal. Is this a safe modernisation?',
        options: [
          'Yes — arrow functions are equivalent and shorter',
          'No — an arrow has no this of its own, so it no longer refers to the object',
          'Yes, provided the object is frozen',
          'No, because arrows cannot return values',
        ],
        correct: 1,
        hints: [
          'Does an arrow function get its own `this`?',
        ],
        solution: 'No — an arrow has no this of its own, so it no longer refers to the object',
        solutionExplanation:
          'An arrow function inherits `this` from the enclosing **scope**, and an object literal is not a scope — so it looks past the object to the surrounding module, where `this` is `undefined`. `greet()` would return `undefined` instead of the name. This is a behaviour change disguised as a style change, and it is why Module 14 recommends shorthand methods for anything touching `this`. Arrows are wrong as methods and right inside them.',
      },
      {
        id: 'ex-m21-05-d',
        title: 'Combine the features',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.HARD,
        xp: 35,
        topicIds: ['modern-js'],
        instructions:
          'Write `report(items = [], { top } = {})` returning `{ count, total, names }` for items with `status === "done"`, limited to the first `top` items when supplied. Each name is `item.owner.name`, defaulting to `"Unassigned"` when the owner or name is missing.',
        starterCode: [
          'function report() {',
          '  // Defaults, destructuring, filter/map/reduce, ?. and ??',
          '}',
        ].join('\n'),
        tests: [
          { name: 'summarises done items', body: 'expect(report([{ status: "done", value: 5, owner: { name: "A" } }, { status: "open", value: 9, owner: { name: "B" } }])).toEqual({ count: 1, total: 5, names: ["A"] });' },
          { name: 'defaults a missing owner', body: 'expect(report([{ status: "done", value: 2 }]).names).toEqual(["Unassigned"]);' },
          { name: 'defaults a missing owner name', body: 'expect(report([{ status: "done", value: 2, owner: {} }]).names).toEqual(["Unassigned"]);' },
          { name: 'honours the top limit', body: 'expect(report([{ status: "done", value: 1, owner: { name: "A" } }, { status: "done", value: 2, owner: { name: "B" } }], { top: 1 })).toEqual({ count: 1, total: 1, names: ["A"] });' },
          { name: 'works with no arguments', body: 'expect(report()).toEqual({ count: 0, total: 0, names: [] });' },
          { name: 'works with no options', body: 'expect(report([{ status: "done", value: 4, owner: { name: "Z" } }])).toEqual({ count: 1, total: 4, names: ["Z"] });' },
          { name: 'keeps an empty owner name as Unassigned', body: 'expect(report([{ status: "done", value: 1, owner: { name: "" } }]).names).toEqual([""]);', hidden: true },
        ],
        hints: [
          '`filter` on status, then `slice(0, top ?? done.length)`.',
          '`item.owner?.name ?? "Unassigned"` handles both missing levels.',
          '`reduce` with an initial value of `0` for the total.',
        ],
        solution: [
          'function report(items = [], { top } = {}) {',
          '  const done = items.filter(({ status }) => status === "done");',
          '  const selected = done.slice(0, top ?? done.length);',
          '',
          '  return {',
          '    count: selected.length,',
          '    total: selected.reduce((sum, { value }) => sum + value, 0),',
          '    names: selected.map((item) => item.owner?.name ?? "Unassigned"),',
          '  };',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`top ?? done.length` is the key line — using `||` there would break a legitimate `top` of `0`, replacing it with the full length. `item.owner?.name` guards a missing owner object, and `?? "Unassigned"` then covers both that case and a present owner with no name, which the second and third tests separate. The hidden test pins the boundary: an owner whose name is genuinely `""` keeps the empty string, because `??` only replaces nullish values — a `||` fallback would have wrongly labelled them "Unassigned".',
      },
    ],
    quiz: {
      id: 'qz-m21-05',
      questions: [
        {
          id: 'q-m21-05-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['modern-js', 'modules'],
          prompt: 'How does importing a default export differ from a named export?',
          options: [
            'There is no difference',
            'A default import uses no braces and can be named freely; a named import uses braces and must match the exported name',
            'A default import uses braces',
            'Named exports cannot be renamed',
          ],
          correct: 1,
          optionExplanations: [
            'The brace syntax differs meaningfully.',
            'Correct — braces distinguish the two.',
            'It is the named import that uses braces.',
            'Named imports can be renamed with `as`.',
          ],
          explanation:
            'A default export is imported without braces under any name you choose. A named export requires braces and must match the exported name, unless renamed with `as`. Getting this backwards is the most common early module error.',
        },
        {
          id: 'q-m21-05-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['modern-js'],
          prompt: 'What does the # prefix mean on a class field?',
          options: [
            'It is a naming convention meaning "internal"',
            'It makes the field genuinely private, enforced by the language',
            'It marks the field as static',
            'It creates a getter',
          ],
          correct: 1,
          optionExplanations: [
            'That is the older _underscore convention.',
            'Correct — real, enforced privacy.',
            'That is the static keyword.',
            'Getters use the get keyword.',
          ],
          explanation:
            '`#field` is genuinely private and inaccessible from outside the class — unlike the `_field` naming convention from Module 15, which is only a hint that anyone can ignore.',
        },
        {
          id: 'q-m21-05-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['modern-js'],
          prompt: 'When is breaking a long method chain into named variables preferable?',
          options: [
            'Never — chaining is always more modern',
            'When the chain is long enough that a reader cannot follow it, or a debugger has no named step to inspect',
            'Only when the chain exceeds ten methods',
            'Only in older codebases',
          ],
          correct: 1,
          optionExplanations: [
            'Modernity is not the criterion.',
            'Correct — readability and debuggability decide.',
            'There is no fixed threshold.',
            'It applies to any codebase.',
          ],
          explanation:
            'Naming intermediate stages lets a reader take one idea at a time and gives a debugger something to inspect. A step with no name cannot be examined, which is exactly what makes a long chain hard to debug.',
        },
        {
          id: 'q-m21-05-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['modern-js'],
          prompt: 'Changing || to ?? is a safe cosmetic modernisation.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'They fall back on different values.',
            'Correct — it is a behaviour change that must be checked case by case.',
          ],
          explanation:
            '`||` falls back on every falsy value; `??` only on nullish ones. Swapping them changes what happens for `0`, `""` and `false` — usually a fix, but always a behaviour change to verify rather than assume.',
        },
      ],
    },
    summary:
      'Modern features combine well when each does one small job and no individual line is hard to read — default parameters, a destructured options object with its own `= {}`, destructuring in callback parameters, shorthand properties, `?.` and `??` all fit comfortably in one short function. **Modules** (Module 28) use braces for named imports, which must match the exported name or be renamed with `as`, and no braces for a default import, which you may name freely; every ES module is strict mode automatically. **Classes** (Module 31) add `#private` fields that are genuinely enforced — unlike the `_underscore` convention — plus `static` members and methods that return `this` for chaining. Refactoring older code to modern syntax is usually a real improvement, replacing ceremony with intent, but it is a **behaviour change until proven otherwise**: `options || {}` is not the same as a parameter default, `||` is not `??`, and an arrow function is not a drop-in replacement for a method because it has no `this` of its own. Chaining past the point a reader can follow, or a debugger can inspect, makes code worse regardless of how modern the syntax is — naming intermediate stages is not old-fashioned, it is what makes a pipeline debuggable.',
    keyTakeaways: [
      'Good modern code layers small features, each doing one clear job',
      'Named imports need braces; a default import does not',
      'ES modules are always strict mode',
      '#private fields are enforced; _underscore is only a convention',
      'Every refactor is a behaviour change until verified — especially || to ??',
      'Name intermediate stages when a chain outgrows readability',
    ],
    relatedLessons: ['l-m21-04', 'l-m21-01'],
    interviewConnections: [
      'What is the difference between a default and a named export?',
      'What does the # prefix do on a class field?',
      'Why is replacing a method with an arrow function risky?',
      'When would you break up a method chain?',
    ],
  },
];
