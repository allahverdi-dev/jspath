import { SECTION, CALLOUT_TONE, DIFFICULTY, EXERCISE_KIND, QUIZ_KIND } from '../../schema/types.js';

const M = 'm03';

export default [
  /* ================================================================== */
  {
    id: 'l-m03-01',
    slug: 'explicit-conversion',
    moduleId: M,
    order: 1,
    title: 'Explicit Conversion: String, Number and Boolean',
    description: 'Converting deliberately with the three conversion functions, and the conversion table you should actually know.',
    difficulty: DIFFICULTY.BEGINNER,
    estimatedMinutes: 15,
    xp: 25,
    topicIds: ['coercion', 'types'],
    prerequisites: ['l-m02-06'],
    learningObjectives: [
      'Convert between strings, numbers and booleans deliberately',
      'Predict what `Number()` returns for unusual inputs',
      'Explain why `String(value)` is safer than `value.toString()`',
      'Choose explicit conversion over relying on implicit coercion',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Because JavaScript is dynamically typed, values frequently need converting from one type to another — form inputs arrive as strings, APIs return numbers as text, and conditions need booleans.',
          'There are two ways this happens. **Explicit conversion** is you asking for it, clearly and visibly. **Implicit coercion** is the language doing it silently as a side effect of some other operation. This lesson covers the explicit form, which is the one you should reach for by default.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The three conversion functions',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Called without `new`, these convert. Called with `new`, they create wrapper objects — never do that.',
        code: [
          'console.log(String(42));        // "42"',
          'console.log(String(true));      // "true"',
          'console.log(String(null));      // "null"',
          '',
          'console.log(Number("42"));      // 42',
          'console.log(Number("3.14"));    // 3.14',
          'console.log(Number(true));      // 1',
          '',
          'console.log(Boolean(1));        // true',
          'console.log(Boolean(0));        // false',
          'console.log(Boolean("hello"));  // true',
        ].join('\n'),
        output: ['42', 'true', 'null', '42', '3.14', '1', 'true', 'false', 'true'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Converting to string',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'There are three common ways, and one of them is safer than the others.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'String() handles every value. toString() does not.',
        code: [
          'const value = 42;',
          '',
          'console.log(String(value));      // "42"  — always works',
          'console.log(value.toString());   // "42"  — works here',
          'console.log(`${value}`);         // "42"  — template literal',
          'console.log(value + "");         // "42"  — implicit, avoid',
          '',
          '// The difference appears with null and undefined:',
          'console.log(String(null));       // "null"',
          'console.log(String(undefined));  // "undefined"',
          '',
          'try {',
          '  null.toString();',
          '} catch (error) {',
          '  console.log(error.name + ":", error.message);',
          '}',
        ].join('\n'),
        output: [
          '42', '42', '42', '42', 'null', 'undefined',
          'TypeError: Cannot read properties of null (reading \'toString\')',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Prefer `String(value)`',
        body: [
          '`value.toString()` calls a method *on* the value, so it throws for `null` and `undefined` — the two values most likely to turn up unexpectedly in real data. `String(value)` is a function that accepts anything and never throws.',
          'Template literals are equally safe and often more readable when you are building a larger string. Reserve `value + ""` for code golf; it relies on implicit coercion and communicates nothing about your intent.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Converting to number',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`Number()` is stricter than most people expect, and its behaviour with non-obvious inputs is worth learning properly rather than guessing.',
        ],
      },
      {
        kind: SECTION.TABLE,
        headers: ['Input', '`Number(input)`', 'Why'],
        rows: [
          ['`"42"`', '`42`', 'Parses cleanly'],
          ['`"3.14"`', '`3.14`', 'Decimals are fine'],
          ['`"  42  "`', '`42`', 'Surrounding whitespace is trimmed'],
          ['`"42px"`', '`NaN`', 'The whole string must be numeric'],
          ['`""`', '`0`', 'An empty string converts to zero — a common surprise'],
          ['`"   "`', '`0`', 'Whitespace-only is treated as empty'],
          ['`true` / `false`', '`1` / `0`', 'Booleans map to one and zero'],
          ['`null`', '`0`', 'Specified as zero'],
          ['`undefined`', '`NaN`', 'No sensible numeric value exists'],
          ['`[]`', '`0`', 'Becomes `""`, which becomes `0`'],
          ['`[5]`', '`5`', 'Becomes `"5"`, which becomes `5`'],
          ['`[1, 2]`', '`NaN`', 'Becomes `"1,2"`, which is not numeric'],
          ['`{}`', '`NaN`', 'Becomes `"[object Object]"`'],
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Run the surprising rows and confirm them for yourself.',
        code: [
          'console.log(Number(""));          // 0',
          'console.log(Number("   "));       // 0',
          'console.log(Number(null));        // 0',
          'console.log(Number(undefined));   // NaN',
          'console.log(Number([]));          // 0',
          'console.log(Number([5]));         // 5',
          'console.log(Number([1, 2]));      // NaN',
          'console.log(Number({}));          // NaN',
          'console.log(Number("42px"));      // NaN',
        ].join('\n'),
        output: ['0', '0', '0', 'NaN', '0', '5', 'NaN', 'NaN', 'NaN'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: '`Number("")` is `0`, and this causes real bugs',
        body: [
          'An empty form field gives you `""`. Converting it with `Number()` produces `0`, not `NaN` — so a validation check like `if (Number.isNaN(Number(input)))` will happily accept an empty field as the number zero.',
          'When validating user input, check for an empty string *before* converting, or use a check that rejects it explicitly.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Converting to boolean',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`Boolean()` is the simplest of the three, because there are exactly eight values that convert to `false`. Everything else — without exception — converts to `true`. The next lesson but one covers this list in detail; for now, note that the conversion itself is entirely predictable.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Watch the last two carefully.',
        code: [
          'console.log(Boolean(0));          // false',
          'console.log(Boolean(""));         // false',
          'console.log(Boolean(null));       // false',
          'console.log(Boolean(NaN));        // false',
          '',
          'console.log(Boolean("0"));        // true  ← a non-empty string',
          'console.log(Boolean("false"));    // true  ← still a non-empty string',
          'console.log(Boolean([]));         // true  ← an empty array is an object',
          'console.log(Boolean({}));         // true',
        ].join('\n'),
        output: ['false', 'false', 'false', 'false', 'true', 'true', 'true', 'true'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The last four are where people go wrong. `"0"` and `"false"` are non-empty strings, so they are truthy despite their contents suggesting otherwise. And **every** object is truthy, including empty arrays and empty objects — a fact that catches out anyone expecting `[]` to behave like "nothing".',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'The `!!` shorthand',
        body: [
          'You will often see `!!value` used to convert to boolean. It is not a special operator — it is the logical NOT applied twice. The first `!` converts to boolean and negates; the second negates back. `!!value` and `Boolean(value)` are exactly equivalent.',
          '`Boolean(value)` is clearer for readers who have not met the idiom, but `!!` is extremely common and you must be able to read it.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const inputs = ["", "0", [], null];',
          '',
          'const numbers = inputs.map(Number);',
          'console.log(numbers.join(", "));',
        ].join('\n'),
        options: [
          '0, 0, 0, 0',
          'NaN, 0, NaN, 0',
          '0, 0, NaN, NaN',
          'NaN, NaN, NaN, NaN',
        ],
        correct: 0,
        explanation:
          'Every one of these converts to `0`. An empty string is specified as `0`. The string `"0"` parses as the number zero. An empty array converts to the empty string first, and then to `0`. And `null` is specified as `0`. Only `undefined` among the "empty-looking" values gives `NaN`. This is exactly why `Number()` is a poor validity check for user input — four very different empty-ish inputs all silently become a valid-looking zero.',
      },
    ],
    exercises: [
      {
        id: 'ex-m03-01-a',
        title: 'Predict Number on an empty string',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['coercion'],
        instructions: 'What does this print?',
        code: ['console.log(Number(""));'].join('\n'),
        options: ['NaN', '0', 'undefined', '""'],
        correct: 1,
        hints: [
          'An empty string is a special case in the conversion rules.',
        ],
        solution: '0',
        solutionExplanation:
          'The specification defines an empty string — and a whitespace-only string — as converting to `0` rather than `NaN`. This is a frequent source of validation bugs, because an untouched form field gives `""`, which then passes a "is this a number?" check as the value zero. Check for an empty string explicitly before converting.',
      },
      {
        id: 'ex-m03-01-b',
        title: 'Choose the safe string conversion',
        kind: EXERCISE_KIND.CHOOSE_IMPLEMENTATION,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['coercion'],
        instructions:
          'You need to convert a value to a string. The value might be `null` or `undefined`. Which approach is safe?',
        options: [
          'value.toString()',
          'String(value)',
          'value.toFixed()',
          'value.valueOf()',
        ],
        correct: 1,
        hints: [
          'Three of these call a method on the value. What happens if the value is null?',
        ],
        solution: 'String(value)',
        solutionExplanation:
          '`String(value)` is a function that accepts any value, including `null` and `undefined`, returning `"null"` and `"undefined"` respectively. The other three are method calls on the value itself, so all of them throw `TypeError: Cannot read properties of null` when the value is null or undefined. A template literal `` `${value}` `` is equally safe and often more readable in context.',
      },
      {
        id: 'ex-m03-01-c',
        title: 'Write a strict number converter',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['coercion'],
        instructions:
          'Write `toNumberOrNull(value)` that converts a value to a number, but returns `null` for anything that is not a genuine number. Empty strings, whitespace-only strings, `null`, `undefined`, arrays, objects and non-numeric strings must all return `null`. `"42"` should return `42`, and `"3.14"` should return `3.14`.',
        starterCode: [
          'function toNumberOrNull(value) {',
          '  // Reject the empty-ish cases before converting.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'converts a numeric string', body: 'expect(toNumberOrNull("42")).toBe(42);' },
          { name: 'converts a decimal string', body: 'expect(toNumberOrNull("3.14")).toBe(3.14);' },
          { name: 'passes a real number through', body: 'expect(toNumberOrNull(7)).toBe(7);' },
          { name: 'keeps zero as zero', body: 'expect(toNumberOrNull("0")).toBe(0);' },
          { name: 'rejects an empty string', body: 'expect(toNumberOrNull("")).toBe(null);' },
          { name: 'rejects a whitespace string', body: 'expect(toNumberOrNull("   ")).toBe(null);' },
          { name: 'rejects null and undefined', body: 'expect(toNumberOrNull(null)).toBe(null); expect(toNumberOrNull(undefined)).toBe(null);' },
          { name: 'rejects a non-numeric string', body: 'expect(toNumberOrNull("42px")).toBe(null);' },
          { name: 'rejects arrays and objects', body: 'expect(toNumberOrNull([])).toBe(null); expect(toNumberOrNull([5])).toBe(null); expect(toNumberOrNull({})).toBe(null);', hidden: true },
        ],
        hints: [
          'Accept only numbers and strings to begin with — that removes arrays, objects, null and undefined in one step.',
          'For strings, trim first and reject the result if it is empty.',
          'After converting, use `Number.isFinite` to reject `NaN` and the infinities.',
        ],
        solution: [
          'function toNumberOrNull(value) {',
          '  if (typeof value === "number") {',
          '    return Number.isFinite(value) ? value : null;',
          '  }',
          '',
          '  if (typeof value !== "string") return null;',
          '',
          '  const trimmed = value.trim();',
          '  if (trimmed === "") return null;',
          '',
          '  const parsed = Number(trimmed);',
          '  return Number.isFinite(parsed) ? parsed : null;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The guard clauses do the work in order of specificity. Restricting to numbers and strings immediately eliminates the awkward cases — `[5]` would otherwise convert to `5` and sneak through, which the hidden test checks for. Trimming and testing for an empty string handles the `Number("") === 0` trap explicitly, which is the whole point of the exercise. `Number.isFinite` at the end catches both `NaN` from unparseable text and the infinities. Returning `null` rather than `NaN` for failure is a deliberate design choice: `null` means "deliberately no value", and unlike `NaN` it can be tested with a straightforward `=== null`.',
      },
    ],
    quiz: {
      id: 'qz-m03-01',
      questions: [
        {
          id: 'q-m03-01-1',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['coercion'],
          prompt: 'What is printed by these three conversions?',
          code: ['console.log(Number(""), Number(null), Number(undefined));'].join('\n'),
          options: ['0 0 NaN', '0 0 0', 'NaN NaN NaN', 'NaN 0 NaN'],
          correct: 0,
          optionExplanations: [
            'Correct — only `undefined` has no sensible numeric value.',
            '`undefined` converts to `NaN`, not zero.',
            'Empty strings and `null` are both specified as zero.',
            'An empty string converts to zero, not NaN.',
          ],
          explanation:
            'The specification defines `Number("")` and `Number(null)` as `0`, while `Number(undefined)` is `NaN`. This asymmetry between `null` and `undefined` shows up repeatedly and is worth committing to memory.',
        },
        {
          id: 'q-m03-01-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['coercion'],
          prompt: 'Why is `String(value)` preferred over `value.toString()`?',
          options: [
            '`toString()` is deprecated',
            '`String()` accepts any value including null and undefined, while `toString()` throws on those',
            '`String()` is faster',
            '`toString()` only works on numbers',
          ],
          correct: 1,
          optionExplanations: [
            '`toString` is a normal, current method.',
            'Correct — a method call requires a value to call it on.',
            'Performance is not the consideration.',
            '`toString` exists on most types, including arrays and objects.',
          ],
          explanation:
            '`value.toString()` is a method call, so it throws a TypeError when `value` is `null` or `undefined` — precisely the values most likely to appear unexpectedly. `String(value)` is a function that handles every input and returns `"null"` or `"undefined"` for those cases.',
        },
        {
          id: 'q-m03-01-3',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['coercion'],
          prompt: 'Which of these convert to `true` with `Boolean()`? (Select all that apply.)',
          options: ['"0"', '[]', '0', '"false"'],
          correct: [0, 1, 3],
          explanation:
            '`"0"` and `"false"` are non-empty strings, and every non-empty string is truthy regardless of its contents. `[]` is an object, and every object is truthy including empty ones. Only the number `0` is falsy. This is why you cannot test whether an array has items with `if (arr)` — you must check `arr.length`.',
        },
      ],
    },
    summary:
      'Explicit conversion uses `String()`, `Number()` and `Boolean()` called without `new`. Prefer `String(value)` over `value.toString()`, because the latter throws on `null` and `undefined`. `Number()` requires the *entire* string to be numeric — `"42px"` gives `NaN` — but it defines several empty-looking values as `0`, including `""`, whitespace-only strings, `null` and `[]`, which makes it a poor validity check on its own. `Boolean()` is fully predictable: exactly eight values are falsy and everything else is true, including `"0"`, `"false"`, `[]` and `{}`. Reaching for explicit conversion rather than relying on implicit coercion makes your intent visible and your bugs shallower.',
    keyTakeaways: [
      '`String()`, `Number()` and `Boolean()` convert; never call them with `new`',
      '`String(value)` is safe for null and undefined; `value.toString()` throws',
      '`Number("")`, `Number("  ")`, `Number(null)` and `Number([])` are all `0`',
      '`Number(undefined)` is `NaN` — unlike `null`',
      '`Number()` needs the whole string to be numeric',
      'Every object is truthy, including `[]` and `{}`; `"0"` and `"false"` are truthy too',
    ],
    relatedLessons: ['l-m02-04', 'l-m03-02'],
    interviewConnections: [
      'What does Number("") return, and why?',
      'What is the difference between String(x) and x.toString()?',
      'Is an empty array truthy or falsy?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m03-02',
    slug: 'parseint-parsefloat',
    moduleId: M,
    order: 2,
    title: 'parseInt, parseFloat and Parsing Real Input',
    description: 'Lenient parsing for messy real-world strings, the radix argument that prevents a classic bug, and when parsing is the wrong tool.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 14,
    xp: 25,
    topicIds: ['coercion', 'numbers'],
    prerequisites: ['l-m03-01'],
    learningObjectives: [
      'Explain how `parseInt` differs from `Number`',
      'Always supply a radix and explain why it matters',
      'Choose between `parseInt`, `parseFloat` and `Number` for a given task',
      'Recognise when lenient parsing hides a data problem',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          '`Number()` is strict: the entire string must be a valid number or you get `NaN`. `parseInt` and `parseFloat` are lenient: they read from the start of the string, take as much as looks numeric, and ignore the rest.',
          'That leniency is occasionally exactly what you need, and frequently a way to turn a data problem into a silent wrong answer. Knowing which situation you are in is the skill.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Strict versus lenient, side by side.',
        code: [
          'console.log(Number("42px"));       // NaN — the whole string must be numeric',
          'console.log(parseInt("42px", 10)); // 42  — reads until it cannot',
          '',
          'console.log(Number("3.9"));        // 3.9',
          'console.log(parseInt("3.9", 10));  // 3   — stops at the decimal point',
          'console.log(parseFloat("3.9"));    // 3.9',
          '',
          'console.log(parseInt("px42", 10)); // NaN — nothing numeric at the start',
          'console.log(parseInt("  42  ", 10)); // 42 — leading whitespace is skipped',
        ].join('\n'),
        output: ['NaN', '42', '3.9', '3', '3.9', 'NaN', '42'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Note that `parseInt` **truncates rather than rounds**. `parseInt("3.9", 10)` is `3`, not `4`. It is not doing arithmetic — it simply stops reading when it meets a character that cannot be part of an integer.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Always pass the radix',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`parseInt` takes a second argument, the **radix** — the number base to interpret the digits in. Base 10 is what you almost always want, but it is not always the default.',
          'Historically, a leading `0` meant octal, so `parseInt("08")` returned `0` in older engines. Modern engines default to base 10 for such strings, but the `0x` prefix still means hexadecimal, and code may run in environments you did not anticipate.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The radix is not decoration.',
        code: [
          'console.log(parseInt("0x1F"));      // 31 — read as hexadecimal',
          'console.log(parseInt("0x1F", 10));  // 0  — stops at the "x"',
          'console.log(parseInt("0x1F", 16));  // 31 — deliberately hexadecimal',
          '',
          'console.log(parseInt("11", 2));     // 3  — binary',
          'console.log(parseInt("777", 8));    // 511 — octal',
          'console.log(parseInt("ff", 16));    // 255',
        ].join('\n'),
        output: ['31', '0', '31', '3', '511', '255'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'The classic `map(parseInt)` bug',
        body: [
          'This is one of the best-known JavaScript puzzles, and it exists entirely because `parseInt` takes a second argument.',
          '`["1", "2", "3"].map(parseInt)` returns `[1, NaN, NaN]` rather than `[1, 2, 3]`. `map` passes three arguments to its callback — the element, the index and the array — so `parseInt` receives the index as its radix. The calls become `parseInt("1", 0)`, `parseInt("2", 1)` and `parseInt("3", 2)`. Radix 0 is treated as 10, giving 1; radix 1 is invalid, giving `NaN`; and `"3"` is not a valid digit in base 2, giving `NaN`.',
          'The fix is to pass an explicit arrow function: `["1", "2", "3"].map((s) => parseInt(s, 10))`, or simply `.map(Number)`.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Confirm the puzzle, then the fix.',
        code: [
          'console.log(["1", "2", "3"].map(parseInt));',
          'console.log(["1", "2", "3"].map((s) => parseInt(s, 10)));',
          'console.log(["1", "2", "3"].map(Number));',
        ].join('\n'),
        output: ['[1, NaN, NaN]', '[1, 2, 3]', '[1, 2, 3]'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Choosing the right tool',
      },
      {
        kind: SECTION.TABLE,
        headers: ['Situation', 'Use', 'Why'],
        rows: [
          ['A form field that must contain a number', '`Number()` plus validation', 'Strictness catches bad input instead of guessing'],
          ['A CSS value such as `"14px"`', '`parseInt(v, 10)`', 'The unit suffix is expected and should be dropped'],
          ['A decimal from text such as `"3.5rem"`', '`parseFloat(v)`', 'Keeps the fractional part'],
          ['A JSON field already typed as a number', 'Use it directly', 'No conversion is needed'],
          ['A hexadecimal colour component', '`parseInt(v, 16)`', 'The radix is the point'],
          ['An `id` from a URL such as `"42"`', '`Number()`', 'Should be strictly numeric; leniency would hide bugs'],
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A legitimate use of lenient parsing.',
        code: [
          'const styles = { paddingTop: "24px", lineHeight: "1.5", width: "50%" };',
          '',
          'const paddingTop = parseInt(styles.paddingTop, 10);',
          'const lineHeight = parseFloat(styles.lineHeight);',
          'const widthPercent = parseFloat(styles.width);',
          '',
          'console.log(paddingTop + 8);',
          'console.log(lineHeight * 2);',
          'console.log(widthPercent / 100);',
        ].join('\n'),
        output: ['32', '3', '0.5'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Leniency can hide a real problem',
        body: [
          'If a user types `"12 apples"` into a quantity field and you use `parseInt`, you silently get `12` and the nonsense goes unnoticed. If you use `Number()`, you get `NaN` and can show a helpful error.',
          'The rule of thumb: parse leniently when the extra characters are **expected and meaningless** — a CSS unit, a currency symbol you control. Convert strictly when the extra characters would indicate the data is **wrong**.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const values = ["10", "10.9", "10px", "px10"];',
          '',
          'const parsed = values.map((v) => parseInt(v, 10));',
          'console.log(parsed.join(", "));',
        ].join('\n'),
        options: [
          '10, 10.9, 10, NaN',
          '10, 10, 10, NaN',
          '10, 11, 10, 0',
          '10, 10, NaN, NaN',
        ],
        correct: 1,
        explanation:
          '`parseInt` reads integer digits from the start and stops at the first character it cannot use. `"10"` gives 10. `"10.9"` gives 10 — it stops at the decimal point and truncates rather than rounding, so 11 is wrong. `"10px"` gives 10, stopping at `p`. `"px10"` gives `NaN` because there is nothing numeric at the *start*. Note that wrapping `parseInt` in an arrow function here also avoids the `map(parseInt)` radix bug.',
      },
    ],
    exercises: [
      {
        id: 'ex-m03-02-a',
        title: 'Predict the map(parseInt) result',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['coercion', 'array-methods'],
        instructions: 'What does this print?',
        code: ['console.log(["1", "2", "3"].map(parseInt));'].join('\n'),
        options: [
          '[1, 2, 3]',
          '[1, NaN, NaN]',
          '[NaN, NaN, NaN]',
          '[1, 2, NaN]',
        ],
        correct: 1,
        hints: [
          'How many arguments does `map` pass to its callback?',
          'What is `parseInt`\'s second parameter?',
        ],
        solution: '[1, NaN, NaN]',
        solutionExplanation:
          '`map` passes three arguments — element, index and array — so `parseInt` receives the index as its radix. The calls become `parseInt("1", 0)`, `parseInt("2", 1)` and `parseInt("3", 2)`. A radix of 0 is treated as 10, giving 1. Radix 1 is invalid, giving `NaN`. And in base 2 the only valid digits are 0 and 1, so `"3"` gives `NaN`. Use `.map((s) => parseInt(s, 10))` or `.map(Number)` instead. This is a favourite interview question precisely because it tests whether you know how many arguments `map` supplies.',
      },
      {
        id: 'ex-m03-02-b',
        title: 'Choose the parser',
        kind: EXERCISE_KIND.CHOOSE_IMPLEMENTATION,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['coercion'],
        instructions:
          'A user types a quantity into a form field. You want to reject anything that is not purely a number. Which approach is correct?',
        options: [
          'parseInt(input, 10)',
          'parseFloat(input)',
          'Number(input) with an explicit empty-string check',
          'input * 1',
        ],
        correct: 2,
        hints: [
          'Which of these would silently accept `"12 apples"` as 12?',
          'Which one is strict about the whole string?',
        ],
        solution: 'Number(input) with an explicit empty-string check',
        solutionExplanation:
          '`Number()` requires the entire string to be numeric, so `"12 apples"` gives `NaN` and you can show a useful error. Both `parseInt` and `parseFloat` would silently return 12 and let the nonsense through. `input * 1` uses implicit coercion and behaves like `Number()`, but communicates nothing about intent. The explicit empty-string check is still needed because `Number("")` is `0`, which would otherwise pass validation as a legitimate zero.',
      },
      {
        id: 'ex-m03-02-c',
        title: 'Extract a CSS pixel value',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['coercion'],
        instructions:
          'Write `pixelsToNumber(value)` that turns a CSS length string such as `"24px"` into the number `24`. It should handle decimals (`"1.5px"` gives `1.5`), plain numeric strings without a unit, and leading whitespace. Return `null` if there is no number at the start.',
        starterCode: [
          'function pixelsToNumber(value) {',
          '  // Decimals must survive, so parseInt is not the right tool.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'handles a whole pixel value', body: 'expect(pixelsToNumber("24px")).toBe(24);' },
          { name: 'keeps decimals', body: 'expect(pixelsToNumber("1.5px")).toBe(1.5);' },
          { name: 'handles a value with no unit', body: 'expect(pixelsToNumber("42")).toBe(42);' },
          { name: 'handles leading whitespace', body: 'expect(pixelsToNumber("  8px  ")).toBe(8);' },
          { name: 'handles zero', body: 'expect(pixelsToNumber("0px")).toBe(0);' },
          { name: 'returns null when there is no leading number', body: 'expect(pixelsToNumber("auto")).toBe(null);' },
          { name: 'returns null for an empty string', body: 'expect(pixelsToNumber("")).toBe(null);' },
          { name: 'handles negative values', body: 'expect(pixelsToNumber("-10px")).toBe(-10);', hidden: true },
        ],
        hints: [
          '`parseFloat` keeps the fractional part; `parseInt` would truncate `"1.5px"` to 1.',
          '`parseFloat` skips leading whitespace for you.',
          'It returns `NaN` when there is nothing numeric at the start — convert that to `null`.',
        ],
        solution: [
          'function pixelsToNumber(value) {',
          '  const parsed = parseFloat(value);',
          '  return Number.isNaN(parsed) ? null : parsed;',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`parseFloat` is the right tool because the extra characters — the `px` unit — are expected and meaningless, which is exactly the case leniency is for. It skips leading whitespace, keeps the decimal part, and handles the minus sign. The only work left is translating its `NaN` failure signal into `null`, which is easier to test against. Note that `parseFloat` takes no radix argument, so the `map(parseInt)` trap does not apply to it.',
      },
    ],
    quiz: {
      id: 'qz-m03-02',
      questions: [
        {
          id: 'q-m03-02-1',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['coercion'],
          prompt: 'What do these two conversions give?',
          code: ['console.log(Number("42px"), parseInt("42px", 10));'].join('\n'),
          options: ['42 42', 'NaN 42', 'NaN NaN', '42 NaN'],
          correct: 1,
          optionExplanations: [
            '`Number` requires the whole string to be numeric.',
            'Correct — strict versus lenient.',
            '`parseInt` reads what it can from the start.',
            'This has the two the wrong way round.',
          ],
          explanation:
            '`Number()` is strict: any non-numeric character anywhere makes the whole conversion fail with `NaN`. `parseInt` is lenient: it reads digits from the start and stops at the first character it cannot use, giving 42.',
        },
        {
          id: 'q-m03-02-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['coercion'],
          prompt: 'Why should you always pass a radix to `parseInt`?',
          options: [
            'It is required; omitting it is a SyntaxError',
            'Because strings with certain prefixes are interpreted in another base, and the default is not guaranteed across environments',
            'Because it makes parsing faster',
            'Because without it, `parseInt` returns a string',
          ],
          correct: 1,
          optionExplanations: [
            'The argument is optional; the problem is what happens when you omit it.',
            'Correct — `"0x1F"` is read as hexadecimal without an explicit radix.',
            'Performance is not the issue.',
            '`parseInt` always returns a number or NaN.',
          ],
          explanation:
            'Without an explicit radix, a `0x` prefix causes hexadecimal interpretation, and older engines treated a leading `0` as octal. Passing `10` makes the intent explicit and the behaviour identical everywhere. Most linter configurations enforce this.',
        },
        {
          id: 'q-m03-02-3',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['coercion'],
          prompt: 'Does parseInt round or truncate?',
          code: ['console.log(parseInt("3.9", 10));'].join('\n'),
          options: ['4', '3', '3.9', 'NaN'],
          correct: 1,
          optionExplanations: [
            '`parseInt` does no arithmetic, so it never rounds.',
            'Correct — it stops reading at the decimal point.',
            'That is what `parseFloat` would give.',
            'There is a valid integer at the start of the string.',
          ],
          explanation:
            '`parseInt` reads integer digits from the start and stops at the first character that cannot be part of an integer — here, the decimal point. It truncates rather than rounding, because it is parsing text rather than performing arithmetic. Use `parseFloat` to keep the fraction, or `Math.round` if you actually want rounding.',
        },
      ],
    },
    summary:
      '`Number()` is strict — the whole string must be numeric — while `parseInt` and `parseFloat` are lenient, reading from the start and ignoring the rest. `parseInt` truncates rather than rounds, and takes a radix argument that you should always supply, because prefixes such as `0x` otherwise change the base. Forgetting that second parameter causes the famous `["1","2","3"].map(parseInt)` result of `[1, NaN, NaN]`, since `map` passes the index as the radix. Choose leniency when the extra characters are expected and meaningless, such as a CSS unit; choose strictness when extra characters mean the data is wrong, such as a user-entered quantity.',
    keyTakeaways: [
      '`Number()` is strict; `parseInt`/`parseFloat` are lenient',
      '`parseInt` truncates — `parseInt("3.9", 10)` is 3, not 4',
      'Always pass a radix: `parseInt(value, 10)`',
      '`map(parseInt)` gives `[1, NaN, NaN]` because the index becomes the radix',
      'Parse leniently for expected units; convert strictly for user input',
    ],
    relatedLessons: ['l-m03-01', 'l-m03-03'],
    interviewConnections: [
      'What is the difference between Number() and parseInt()?',
      'Why does ["1","2","3"].map(parseInt) give [1, NaN, NaN]?',
      'Why should parseInt always be given a radix?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m03-03',
    slug: 'truthy-and-falsy',
    moduleId: M,
    order: 3,
    title: 'Truthy and Falsy',
    description: 'The eight falsy values, why everything else is true, and the guard-clause bug that catches everyone.',
    difficulty: DIFFICULTY.BEGINNER,
    estimatedMinutes: 14,
    xp: 25,
    topicIds: ['coercion', 'booleans'],
    prerequisites: ['l-m03-02'],
    learningObjectives: [
      'List all eight falsy values from memory',
      'Predict how any value behaves in a condition',
      'Explain why `if (count)` is a bug when zero is valid',
      'Choose between a truthiness check and an explicit comparison',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Whenever a value appears where a boolean is expected — in an `if`, a `while`, a ternary, or with `&&`, `||` and `!` — JavaScript converts it to a boolean. A value that converts to `true` is called **truthy**; one that converts to `false` is **falsy**.',
          'There are exactly eight falsy values. Everything else in the language is truthy. Because the falsy list is short and closed, memorising it is genuinely worthwhile — it lets you predict any condition instantly.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The eight falsy values',
      },
      {
        kind: SECTION.TABLE,
        headers: ['Value', 'Notes'],
        rows: [
          ['`false`', 'The boolean itself'],
          ['`0`', 'The number zero'],
          ['`-0`', 'Negative zero — a distinct value that behaves identically here'],
          ['`0n`', 'BigInt zero'],
          ['`""`', 'The empty string, in any quote style'],
          ['`null`', ''],
          ['`undefined`', ''],
          ['`NaN`', ''],
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'A way to remember them',
        body: [
          'Three "nothing" values — `null`, `undefined`, `NaN`. Three "zero" values — `0`, `-0`, `0n`. One empty string — `""`. And `false` itself.',
          'Anything not on that list is truthy. That includes `"0"`, `"false"`, `[]`, `{}`, and every function.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Every falsy value, and the truthy values people expect to be falsy.',
        code: [
          'const falsy = [false, 0, -0, 0n, "", null, undefined, NaN];',
          'console.log("all falsy:", falsy.every((v) => !v));',
          '',
          'const surprisinglyTruthy = ["0", "false", " ", [], {}, function () {}];',
          'console.log("all truthy:", surprisinglyTruthy.every((v) => Boolean(v)));',
        ].join('\n'),
        output: ['all falsy: true', 'all truthy: true'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'That second list is where the bugs live. `"0"` is a non-empty string, so it is truthy. `"false"` is a non-empty string, so it is truthy. A single space `" "` is a non-empty string, so it is truthy. And every object is truthy, including the empty array and the empty object.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The zero bug',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This is the most common truthiness mistake in real code, and it is worth studying closely because it is so easy to write by accident.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A guard clause that rejects a perfectly valid value.',
        code: [
          'function describeStock(count) {',
          '  if (!count) {',
          '    return "Stock level unknown";',
          '  }',
          '  return "In stock: " + count;',
          '}',
          '',
          'console.log(describeStock(5));',
          'console.log(describeStock(0));         // wrong — 0 is a real stock level',
          'console.log(describeStock(undefined)); // correct',
        ].join('\n'),
        output: ['In stock: 5', 'Stock level unknown', 'Stock level unknown'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`describeStock(0)` should report that the item is out of stock — zero is a genuine, meaningful count. But `0` is falsy, so `!count` is `true` and the guard fires. The function cannot distinguish "we have none" from "we do not know".',
          'The fix is to check for the thing you actually mean.',
        ],
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Truthiness — wrong when 0 is valid',
          code: [
            'function describeStock(count) {',
            '  if (!count) {',
            '    return "unknown";',
            '  }',
            '  return "In stock: " + count;',
            '}',
            '',
            '// describeStock(0)',
            '// → "unknown"  ✗',
          ].join('\n'),
        },
        right: {
          title: 'Explicit — correct',
          code: [
            'function describeStock(count) {',
            '  if (count == null) {',
            '    return "unknown";',
            '  }',
            '  return "In stock: " + count;',
            '}',
            '',
            '// describeStock(0)',
            '// → "In stock: 0"  ✓',
          ].join('\n'),
        },
        note: 'The same bug appears with empty strings — `if (!name)` rejects a legitimately empty name — and with `false` in a settings object, where `if (!options.enabled)` cannot tell "disabled" from "not specified".',
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'The three values that cause almost all truthiness bugs',
        body: [
          '`0`, `""` and `false` are all falsy, yet all three are frequently *valid data*. A quantity of zero, an empty search box, a disabled setting — each is meaningful, and each is silently swallowed by a truthiness check.',
          'Ask yourself: could this value legitimately be `0`, `""` or `false`? If so, do not use a bare truthiness test.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'When truthiness is the right tool',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'None of this means truthiness checks are bad. They are concise and idiomatic when the falsy cases genuinely all mean the same thing.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Legitimate uses.',
        code: [
          'const users = [];',
          'const name = "";',
          'const config = { retries: 0 };',
          '',
          '// Good: an empty array and a missing array both mean "nothing to show"',
          'if (users.length) {',
          '  console.log("has users");',
          '} else {',
          '  console.log("no users to display");',
          '}',
          '',
          '// Good: an empty name and a missing name both mean "nothing to greet"',
          'console.log(name ? "Hi " + name : "Hi there");',
          '',
          '// Bad: 0 retries is a deliberate setting, not a missing one',
          'console.log(config.retries || 3);        // 3  ← wrong',
          'console.log(config.retries ?? 3);        // 0  ← right',
        ].join('\n'),
        output: ['no users to display', 'Hi there', '3', '0'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'That last pair previews the nullish coalescing operator `??`, which you will meet properly in the operators module. It exists precisely because of this problem: `||` falls back on any falsy value, while `??` falls back only on `null` and `undefined`. For default values, `??` is almost always what you meant.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'You cannot check an array for emptiness with truthiness',
        body: [
          '`if ([])` is `true`, because every object is truthy. An empty array is still an array, so it is still an object, so it is still truthy.',
          'Always check `array.length` instead. The same applies to objects — use `Object.keys(obj).length` to test for an object with no properties.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const values = [0, "", "0", [], null, "false"];',
          '',
          'const truthyCount = values.filter(Boolean).length;',
          'console.log(truthyCount);',
        ].join('\n'),
        options: ['2', '3', '4', '1'],
        correct: 1,
        explanation:
          'Three values survive the filter. `"0"` is a non-empty string and therefore truthy. `[]` is an object and every object is truthy, even an empty one. `"false"` is a non-empty string, so its contents are irrelevant — it is truthy too. The falsy ones are `0`, `""` and `null`. The idiom `filter(Boolean)` is a compact way to strip falsy values from an array, and this result shows exactly why you must be sure that is what you want.',
      },
    ],
    exercises: [
      {
        id: 'ex-m03-03-a',
        title: 'Which value is truthy?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['coercion', 'booleans'],
        instructions: 'Which of these is truthy?',
        options: ['0', '""', '"0"', 'NaN'],
        correct: 2,
        hints: [
          'Look carefully at the quotation marks.',
        ],
        solution: '"0"',
        solutionExplanation:
          '`"0"` is a string containing one character, and every non-empty string is truthy regardless of what it contains. Its contents look falsy, but the conversion rule only cares whether the string is empty. The other three — the number `0`, the empty string, and `NaN` — are all on the list of eight falsy values.',
      },
      {
        id: 'ex-m03-03-b',
        title: 'Fix the zero guard',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['coercion', 'booleans'],
        instructions:
          'This function should return `"none"` only when the score is missing (`null` or `undefined`). At the moment it also returns `"none"` for a score of 0, which is a legitimate score. Fix it.',
        starterCode: [
          'function describeScore(score) {',
          '  if (!score) {',
          '    return "none";',
          '  }',
          '  return "Score: " + score;',
          '}',
        ].join('\n'),
        tests: [
          { name: 'a positive score is described', body: 'expect(describeScore(10)).toBe("Score: 10");' },
          { name: 'a score of zero is described, not rejected', body: 'expect(describeScore(0)).toBe("Score: 0");' },
          { name: 'null returns none', body: 'expect(describeScore(null)).toBe("none");' },
          { name: 'undefined returns none', body: 'expect(describeScore(undefined)).toBe("none");' },
          { name: 'a missing argument returns none', body: 'expect(describeScore()).toBe("none");' },
          { name: 'a negative score is described', body: 'expect(describeScore(-5)).toBe("Score: -5");', hidden: true },
        ],
        hints: [
          '`!score` is true for `0`, which is the bug — `0` is falsy but valid.',
          'You want to test specifically for null and undefined, not for falsiness.',
          '`score == null` is true for exactly those two values.',
        ],
        solution: [
          'function describeScore(score) {',
          '  if (score == null) {',
          '    return "none";',
          '  }',
          '  return "Score: " + score;',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`score == null` is the idiomatic check for "null or undefined and nothing else", which is exactly the condition intended. The original `!score` was too broad: it caught `0`, and would also have caught `""` and `NaN`. The explicit alternative `score === null || score === undefined` is equivalent and some teams prefer it. The hidden test with a negative score is a reminder that `-5` is truthy, so the original code happened to handle it correctly — the bug was specifically about zero.',
      },
      {
        id: 'ex-m03-03-c',
        title: 'Write a falsy-value counter',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['coercion', 'booleans'],
        instructions:
          'Write `countFalsy(values)` that takes an array and returns how many of its elements are falsy. Remember that `"0"`, `[]` and `{}` are all truthy.',
        starterCode: [
          'function countFalsy(values) {',
          '  // Count elements that convert to false.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'counts the classic falsy values', body: 'expect(countFalsy([0, "", null, undefined, NaN, false])).toBe(6);' },
          { name: 'does not count truthy lookalikes', body: 'expect(countFalsy(["0", "false", [], {}, " "])).toBe(0);' },
          { name: 'handles a mixed array', body: 'expect(countFalsy([1, 0, "a", "", null])).toBe(3);' },
          { name: 'returns 0 for an empty array', body: 'expect(countFalsy([])).toBe(0);' },
          { name: 'counts negative zero as falsy', body: 'expect(countFalsy([-0])).toBe(1);' },
          { name: 'does not count negative numbers', body: 'expect(countFalsy([-1, -5])).toBe(0);', hidden: true },
        ],
        hints: [
          '`filter` keeps elements for which the callback returns true.',
          '`!value` is true exactly when the value is falsy.',
          'The result of `filter` is an array — you need its `length`.',
        ],
        solution: [
          'function countFalsy(values) {',
          '  return values.filter((value) => !value).length;',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`!value` performs the boolean conversion and negates it in one step, so the filter keeps exactly the falsy elements. The second test is the important one: `"0"`, `"false"` and `" "` are all non-empty strings, and `[]` and `{}` are objects, so none of them count — a solution that checked contents rather than truthiness would fail here. The hidden test guards against confusing "falsy" with "negative or zero"; `-1` is truthy, and only `-0` is falsy among negative numbers.',
      },
    ],
    quiz: {
      id: 'qz-m03-03',
      questions: [
        {
          id: 'q-m03-03-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['coercion', 'booleans'],
          prompt: 'How many falsy values are there in JavaScript?',
          options: ['Five', 'Six', 'Eight', 'It varies by environment'],
          correct: 2,
          optionExplanations: [
            'Older material often says five or six, before BigInt and negative zero were commonly listed.',
            'This misses `-0` and `0n`.',
            'Correct — false, 0, -0, 0n, "", null, undefined, NaN.',
            'The list is fixed by the specification.',
          ],
          explanation:
            'The eight are `false`, `0`, `-0`, `0n`, `""`, `null`, `undefined` and `NaN`. Everything else in the language is truthy, without exception.',
        },
        {
          id: 'q-m03-03-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['coercion', 'booleans'],
          prompt: 'What does this print?',
          code: [
            'if ([]) {',
            '  console.log("truthy");',
            '} else {',
            '  console.log("falsy");',
            '}',
          ].join('\n'),
          options: ['truthy', 'falsy', 'Nothing', 'TypeError'],
          correct: 0,
          optionExplanations: [
            'Correct — every object is truthy, and an empty array is still an object.',
            'Emptiness is irrelevant; only the eight listed values are falsy.',
            'One branch always runs.',
            'There is nothing to throw here.',
          ],
          explanation:
            'Every object is truthy, including empty arrays and empty objects. To test whether an array has elements you must check `array.length`. This is a common surprise for people coming from Python, where an empty list is falsy.',
        },
        {
          id: 'q-m03-03-3',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['coercion'],
          prompt: 'Why do these two produce different results?',
          code: [
            'const settings = { retries: 0 };',
            'console.log(settings.retries || 3);',
            'console.log(settings.retries ?? 3);',
          ].join('\n'),
          options: ['3 and 3', '0 and 0', '3 and 0', '0 and 3'],
          correct: 2,
          optionExplanations: [
            '`??` only falls back on null or undefined, and 0 is neither.',
            '`||` falls back on any falsy value, and 0 is falsy.',
            'Correct — `||` is fooled by the falsy 0; `??` is not.',
            'This has the two operators the wrong way round.',
          ],
          explanation:
            '`||` returns the right-hand side whenever the left is *falsy*, so a deliberate setting of `0` is replaced by the default. `??` returns the right-hand side only when the left is `null` or `undefined`, so it correctly preserves `0`. For default values, `??` is almost always the operator you meant.',
        },
        {
          id: 'q-m03-03-4',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['coercion', 'booleans'],
          prompt: 'Which values would incorrectly trigger a `if (!value) return;` guard even though they may be valid data? (Select all that apply.)',
          options: ['0', '""', 'false', '"none"'],
          correct: [0, 1, 2],
          explanation:
            '`0`, `""` and `false` are all falsy, yet all three are frequently legitimate data — a quantity of zero, an empty search box, a deliberately disabled setting. A bare truthiness guard cannot tell them apart from "missing". `"none"` is a non-empty string and therefore truthy, so it passes the guard without incident.',
        },
      ],
    },
    summary:
      'Exactly eight values are falsy — `false`, `0`, `-0`, `0n`, `""`, `null`, `undefined` and `NaN` — and everything else is truthy, including `"0"`, `"false"`, `" "`, `[]` and `{}`. Truthiness checks are concise and idiomatic when all the falsy cases genuinely mean the same thing, such as `if (users.length)`. They are a bug when a falsy value is valid data: `0` as a quantity, `""` as a name, `false` as a setting. In those cases test for what you actually mean, usually `value == null`, and prefer `??` over `||` when supplying defaults. Arrays can never be tested for emptiness with truthiness, because every object is truthy.',
    keyTakeaways: [
      'Eight falsy values: false, 0, -0, 0n, "", null, undefined, NaN',
      'Everything else is truthy — including "0", "false", [] and {}',
      '`if (!count)` is a bug whenever 0 is a valid count',
      'Test arrays with `array.length`, never with `if (array)`',
      'Use `??` rather than `||` for defaults, so 0 and "" survive',
    ],
    relatedLessons: ['l-m03-01', 'l-m03-04'],
    interviewConnections: [
      'What are the falsy values in JavaScript?',
      'Is an empty array truthy or falsy?',
      'What is the difference between || and ?? for default values?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m03-04',
    slug: 'implicit-coercion',
    moduleId: M,
    order: 4,
    title: 'Implicit Coercion and the Overloaded Plus',
    description: 'What happens when you combine mismatched types, why `+` is the odd one out, and how to stop it surprising you.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 16,
    xp: 30,
    topicIds: ['coercion', 'operators'],
    prerequisites: ['l-m03-03'],
    learningObjectives: [
      'Explain why `+` behaves differently from the other arithmetic operators',
      'Predict the result of expressions mixing strings and numbers',
      'Explain how objects are converted to primitives',
      'Avoid implicit coercion in code you write',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Implicit coercion is JavaScript converting types on your behalf, without being asked, as a side effect of some other operation. It is the source of the language\'s worst reputation — and most of it follows from a small number of rules that are entirely learnable.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: '`+` is overloaded; the others are not',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`+` does two completely different jobs: numeric addition and string concatenation. Its rule is simple — **if either operand is a string, it concatenates; otherwise it adds numerically.**',
          'Every other arithmetic operator (`-`, `*`, `/`, `%`, `**`) has only one job, so it always converts both operands to numbers.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The single most important asymmetry in the language.',
        code: [
          'console.log("5" + 3);   // "53"  — concatenation',
          'console.log("5" - 3);   // 2     — numeric',
          'console.log("5" * 3);   // 15    — numeric',
          'console.log("5" / 3);   // 1.666… — numeric',
          '',
          'console.log(5 + "3");   // "53"  — order does not matter',
          'console.log(1 + 2 + "3"); // "33" — left to right: 3 then "33"',
          'console.log("1" + 2 + 3); // "123" — left to right: "12" then "123"',
        ].join('\n'),
        output: ['53', '2', '15', '1.6666666666666667', '53', '33', '123'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Those last two lines are worth reading twice. `+` is left-associative, so it evaluates in pairs from the left. In `1 + 2 + "3"`, the first `+` sees two numbers and produces `3`; the second sees a number and a string and produces `"33"`. In `"1" + 2 + 3`, the very first `+` already involves a string, so everything from then on concatenates.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'The form-input bug',
        body: [
          'Values read from an HTML input are **always strings**, even from `<input type="number">`. So `input.value + 1` where the field contains `5` gives `"51"`, not `6`.',
          'Convert explicitly at the boundary — the moment the value enters your code — rather than hoping the arithmetic works out. `const quantity = Number(input.value);` and then use `quantity` everywhere below.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Booleans, null and undefined in arithmetic',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Everything becomes a number, using the table from lesson 1.',
        code: [
          'console.log(true + 1);        // 2    — true is 1',
          'console.log(false + 1);       // 1    — false is 0',
          'console.log(true + true);     // 2',
          '',
          'console.log(null + 1);        // 1    — null is 0',
          'console.log(undefined + 1);   // NaN  — undefined is NaN',
          '',
          'console.log("5" - null);      // 5',
          'console.log("5" - undefined); // NaN',
        ].join('\n'),
        output: ['2', '1', '2', '1', 'NaN', '5', 'NaN'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'None of these are new rules — they are the same conversions `Number()` performs, applied automatically. `true` becomes `1`, `false` and `null` become `0`, and `undefined` becomes `NaN`. Once you know the conversion table, the arithmetic is entirely predictable.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'How objects become primitives',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'When an object appears in an arithmetic expression, JavaScript first converts it to a primitive by calling `valueOf()` and then `toString()` — for `+`, it tries `valueOf` first and falls back to `toString`.',
          'For plain objects, `valueOf` returns the object itself (no help), so `toString` runs and returns the famously unhelpful `"[object Object]"`. For arrays, `toString` joins the elements with commas.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Where "[object Object]" comes from.',
        code: [
          'console.log([] + []);          // ""   — two empty strings joined',
          'console.log([1, 2] + [3]);     // "1,23"',
          'console.log([] + {});          // "[object Object]"',
          '',
          'console.log({} + "");          // "[object Object]"',
          'console.log([1] + 1);          // "11" — "1" then concatenated',
          'console.log([1] - 1);          // 0    — "1" → 1, then 1 - 1',
          '',
          'console.log(String({}));       // "[object Object]"',
          'console.log(String([1, 2]));   // "1,2"',
        ].join('\n'),
        output: ['', '1,23', '[object Object]', '[object Object]', '11', '0', '[object Object]', '1,2'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'That `[1] - 1` result is instructive. `-` has no string behaviour, so both operands must become numbers: the array becomes the string `"1"`, which becomes the number `1`, and `1 - 1` is `0`. Meanwhile `[1] + 1` sees that one operand converted to a string and concatenates instead.',
          'Seeing `"[object Object]"` on screen is always the same diagnosis: an object was converted to a string somewhere it should not have been. Usually you meant to read a property from it, or to call `JSON.stringify`.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'The `{} + []` party trick',
        body: [
          'You may see the claim that `{} + []` is `0` while `[] + {}` is `"[object Object]"`. The difference is not about coercion at all — it is about parsing.',
          'When `{}` appears at the *start of a statement*, JavaScript reads it as an empty block, not an object literal. The line then becomes `+[]`, a unary plus applied to an empty array, which is `0`. Inside an expression — such as `console.log({} + [])` — it is an object literal and you get `"[object Object]"`. It is a parsing curiosity, not a coercion rule.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Writing code that avoids all of this',
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Relying on coercion',
          code: [
            'const qty = input.value;',
            'const total = qty * price;',
            'const label = "Total: " + total;',
            '',
            '// Works by accident.',
            '// Breaks silently when',
            '// qty is "" or "3 items".',
          ].join('\n'),
        },
        right: {
          title: 'Explicit at the boundary',
          code: [
            'const qty = Number(input.value);',
            '',
            'if (!Number.isFinite(qty)) {',
            '  return showError("Enter a number");',
            '}',
            '',
            'const total = qty * price;',
            'const label = `Total: ${total}`;',
          ].join('\n'),
        },
        note: 'Convert once, at the point data enters your code, and validate immediately. Everything downstream can then assume it has a real number, and template literals make the string-building explicit.',
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'console.log(1 + 2 + "3" + 4 + 5);',
        ].join('\n'),
        options: ['"3345"', '"12345"', '15', '"33445"'],
        correct: 0,
        explanation:
          '`+` is left-associative, so this evaluates in pairs from the left. `1 + 2` is numeric addition giving `3`. Then `3 + "3"` involves a string, so it concatenates to `"33"`. From that point every operand joins a string: `"33" + 4` gives `"334"`, and `"334" + 5` gives `"3345"`. The rule is that once a string enters the chain, everything after it concatenates.',
      },
    ],
    exercises: [
      {
        id: 'ex-m03-04-a',
        title: 'Predict the plus and minus difference',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['coercion', 'operators'],
        instructions: 'What does this print?',
        code: ['console.log("10" + 5, "10" - 5);'].join('\n'),
        options: ['15 5', '"105" 5', '"105" "105"', '15 "105"'],
        correct: 1,
        hints: [
          '`+` has two jobs. Which one wins when a string is involved?',
          '`-` has only one job.',
        ],
        solution: '"105" 5',
        solutionExplanation:
          '`+` concatenates whenever either operand is a string, so `"10" + 5` produces the string `"105"`. `-` has no string meaning at all, so it converts both operands to numbers and computes `10 - 5 = 5`. This asymmetry is the single most common source of arithmetic surprises in JavaScript, and it is why form input must be converted before any arithmetic.',
      },
      {
        id: 'ex-m03-04-b',
        title: 'Fix the form arithmetic bug',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['coercion'],
        instructions:
          'This function receives quantity and price as strings, exactly as they arrive from form inputs, and should return the numeric total. `calculateTotal("3", "10")` should return `30`, but at the moment `addShipping` produces the wrong result. Fix the function so both operations are numeric.',
        starterCode: [
          'function calculateTotal(quantity, price) {',
          '  const subtotal = quantity * price;',
          '  const shipping = "5";',
          '  return subtotal + shipping;',
          '}',
        ].join('\n'),
        tests: [
          { name: 'calculateTotal("3", "10") returns 35', body: 'expect(calculateTotal("3", "10")).toBe(35);' },
          { name: 'returns a number, not a string', body: 'expect(typeof calculateTotal("3", "10")).toBe("number");' },
          { name: 'works with numeric arguments too', body: 'expect(calculateTotal(2, 20)).toBe(45);' },
          { name: 'handles a quantity of zero', body: 'expect(calculateTotal("0", "10")).toBe(5);' },
          { name: 'handles decimals', body: 'expect(calculateTotal("2", "2.5")).toBe(10);', hidden: true },
        ],
        hints: [
          '`quantity * price` already works — `*` converts both operands to numbers.',
          'The bug is `subtotal + shipping`, where `shipping` is a string, so `+` concatenates.',
          'Convert `shipping` to a number with `Number()` before adding.',
        ],
        solution: [
          'function calculateTotal(quantity, price) {',
          '  const subtotal = Number(quantity) * Number(price);',
          '  const shipping = Number("5");',
          '  return subtotal + shipping;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The multiplication happened to work because `*` has only one meaning and converts both operands automatically. The addition did not, because `+` sees a string and switches to concatenation — `30 + "5"` gives `"305"`, not `35`. Converting `shipping` alone would fix the tests, but converting all three inputs explicitly is better practice: it makes the types obvious to a reader and stops the function depending on `*`\'s automatic conversion, which is coercion working by accident rather than by design.',
      },
      {
        id: 'ex-m03-04-c',
        title: 'Write a safe adder',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 20,
        topicIds: ['coercion'],
        instructions:
          'Write `safeAdd(a, b)` that adds two values numerically, never concatenating. If either value cannot be converted to a finite number, return `null`. Note that `safeAdd("", 5)` must return `null`, even though `Number("")` is `0`.',
        starterCode: [
          'function safeAdd(a, b) {',
          '  // Convert both, reject anything that is not a real number.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'adds two numbers', body: 'expect(safeAdd(2, 3)).toBe(5);' },
          { name: 'adds two numeric strings numerically', body: 'expect(safeAdd("2", "3")).toBe(5);' },
          { name: 'adds a mix', body: 'expect(safeAdd("10", 5)).toBe(15);' },
          { name: 'handles zero', body: 'expect(safeAdd(0, 0)).toBe(0);' },
          { name: 'rejects an empty string', body: 'expect(safeAdd("", 5)).toBe(null);' },
          { name: 'rejects non-numeric text', body: 'expect(safeAdd("abc", 5)).toBe(null);' },
          { name: 'rejects null and undefined', body: 'expect(safeAdd(null, 5)).toBe(null); expect(safeAdd(undefined, 5)).toBe(null);' },
          { name: 'rejects arrays', body: 'expect(safeAdd([], 5)).toBe(null); expect(safeAdd([2], 5)).toBe(null);', hidden: true },
        ],
        hints: [
          'Write a small helper that converts one value and returns `null` on failure — then use it twice.',
          'Accept only numbers and non-empty strings; that rejects arrays, null and undefined in one step.',
          '`Number.isFinite` catches `NaN` and the infinities after conversion.',
        ],
        solution: [
          'function toFiniteNumber(value) {',
          '  if (typeof value === "number") {',
          '    return Number.isFinite(value) ? value : null;',
          '  }',
          '  if (typeof value !== "string") return null;',
          '',
          '  const trimmed = value.trim();',
          '  if (trimmed === "") return null;',
          '',
          '  const parsed = Number(trimmed);',
          '  return Number.isFinite(parsed) ? parsed : null;',
          '}',
          '',
          'function safeAdd(a, b) {',
          '  const left = toFiniteNumber(a);',
          '  const right = toFiniteNumber(b);',
          '',
          '  if (left === null || right === null) return null;',
          '',
          '  return left + right;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Extracting `toFiniteNumber` avoids duplicating the validation, which is worth doing the moment you need the same logic twice. Restricting to numbers and strings rejects arrays before `Number([2])` can quietly turn into `2` — the hidden test checks exactly that. The empty-string guard matters because `Number("")` is `0`, which would otherwise make `safeAdd("", 5)` return a plausible-looking `5`. Once both values are confirmed finite numbers, the final `+` cannot possibly concatenate, because neither operand is a string.',
      },
    ],
    quiz: {
      id: 'qz-m03-04',
      questions: [
        {
          id: 'q-m03-04-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['coercion', 'operators'],
          prompt: 'What is the rule for the `+` operator?',
          options: [
            'It always adds numerically',
            'It always concatenates',
            'If either operand is a string it concatenates; otherwise it adds numerically',
            'It concatenates only when both operands are strings',
          ],
          correct: 2,
          optionExplanations: [
            '`"5" + 3` gives `"53"`, so this cannot be right.',
            '`2 + 3` gives `5`, so it does not always concatenate.',
            'Correct — one string is enough to switch modes.',
            'Only one operand needs to be a string.',
          ],
          explanation:
            '`+` is overloaded. A single string operand is enough to switch it into concatenation mode. Every other arithmetic operator has only one meaning and therefore always converts both operands to numbers.',
        },
        {
          id: 'q-m03-04-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['coercion'],
          prompt: 'What is the result of this chain?',
          code: ['console.log("1" + 2 + 3);'].join('\n'),
          options: ['"123"', '"15"', '6', '"33"'],
          correct: 0,
          optionExplanations: [
            'Correct — once a string enters the chain, everything after concatenates.',
            'The 2 and 3 are never added to each other.',
            'The leading string prevents numeric addition entirely.',
            'That would be the result of `1 + 2 + "3"`.',
          ],
          explanation:
            '`+` is left-associative. `"1" + 2` concatenates to `"12"`, then `"12" + 3` concatenates to `"123"`. Contrast with `1 + 2 + "3"`, where the first pair are both numbers and add to 3, giving `"33"`.',
        },
        {
          id: 'q-m03-04-3',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['coercion'],
          prompt: 'Why do these two array expressions differ?',
          code: ['console.log([1] + 1, [1] - 1);'].join('\n'),
          options: ['"11" 0', '2 0', '"11" "11"', '2 2'],
          correct: 0,
          optionExplanations: [
            'Correct — `+` concatenates after the array becomes `"1"`; `-` converts to a number.',
            '`+` never adds numerically when one side became a string.',
            '`-` has no string behaviour.',
            'The first expression produces a string.',
          ],
          explanation:
            'Both convert the array to the primitive string `"1"` first. Then `+` sees a string operand and concatenates, giving `"11"`. `-` has no string meaning, so it converts `"1"` to the number 1 and computes `1 - 1 = 0`.',
        },
        {
          id: 'q-m03-04-4',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['coercion'],
          prompt: 'You see `"[object Object]"` rendered on a page. What has happened?',
          options: [
            'A network request failed',
            'An object was converted to a string, usually by string concatenation',
            'The object is empty',
            'A property name was misspelled',
          ],
          correct: 1,
          optionExplanations: [
            'This has nothing to do with networking.',
            'Correct — it is the default `toString` result for a plain object.',
            'Emptiness makes no difference; every plain object stringifies the same way.',
            'A misspelled property gives `undefined`, not this.',
          ],
          explanation:
            '`"[object Object]"` is what `Object.prototype.toString` returns, so seeing it means an object was coerced to a string — typically by concatenation or by being placed in a template literal. You almost certainly meant to read a specific property, or to use `JSON.stringify`.',
        },
      ],
    },
    summary:
      '`+` is overloaded: if either operand is a string it concatenates, otherwise it adds numerically. Every other arithmetic operator has one meaning and always converts to numbers, which is why `"5" + 3` is `"53"` while `"5" - 3` is `2`. Because `+` is left-associative, once a string enters a chain everything after it concatenates. Booleans, `null` and `undefined` follow the same conversion table as `Number()`. Objects are converted to primitives via `valueOf` then `toString`, which is where `"[object Object]"` comes from — always a sign that an object was stringified by accident. The defence is to convert explicitly at the boundary where data enters your code, validate immediately, and use template literals for string building.',
    keyTakeaways: [
      '`+` concatenates if either operand is a string; every other operator converts to number',
      '`+` is left-associative — once a string appears, the rest concatenates',
      'Form inputs are always strings, even `type="number"`',
      '`true` is 1, `false` and `null` are 0, `undefined` is NaN',
      '`"[object Object]"` means an object was stringified by accident',
      'Convert explicitly at the boundary; never rely on implicit coercion',
    ],
    relatedLessons: ['l-m03-01', 'l-m03-05'],
    interviewConnections: [
      'Why does "5" + 3 give "53" but "5" - 3 give 2?',
      'What does 1 + 2 + "3" evaluate to?',
      'Where does "[object Object]" come from?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m03-05',
    slug: 'equality-coercion',
    moduleId: M,
    order: 5,
    title: '== versus ===',
    description: 'The loose equality algorithm, why it is not transitive, and the one case where `==` is genuinely the right choice.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 16,
    xp: 30,
    topicIds: ['coercion', 'operators'],
    prerequisites: ['l-m03-04'],
    learningObjectives: [
      'Explain precisely what `==` and `===` each do',
      'Work through the loose equality algorithm on any pair of values',
      'Explain why `==` is not transitive',
      'Justify `== null` as the one acceptable use of loose equality',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'JavaScript has two equality operators. `===` is called strict equality; `==` is loose equality, or "abstract" equality in the specification.',
          '`===` compares types first: if the types differ, the answer is `false` and no conversion happens. `==` converts operands to a common type first, then compares.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The same comparisons with both operators.',
        code: [
          'console.log(5 === "5");   // false — different types, done',
          'console.log(5 == "5");    // true  — "5" converts to 5',
          '',
          'console.log(0 === false); // false',
          'console.log(0 == false);  // true  — false converts to 0',
          '',
          'console.log(null === undefined); // false',
          'console.log(null == undefined);  // true  — special rule',
        ].join('\n'),
        output: ['false', 'true', 'false', 'true', 'false', 'true'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'The loose equality algorithm',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The rules look intimidating listed out, but there are only five, and applying them in order lets you work out any comparison.',
        ],
      },
      {
        kind: SECTION.STEPS,
        steps: [
          {
            title: '1. Same type? Compare like `===`',
            body: 'If both operands have the same type, no conversion happens at all and the result matches strict equality.',
          },
          {
            title: '2. `null` and `undefined` are equal to each other, and to nothing else',
            body: '`null == undefined` is `true`. Neither is loosely equal to `0`, `""`, `false` or anything else. This is a hard-coded special case.',
          },
          {
            title: '3. Number and string? Convert the string to a number',
            body: '`5 == "5"` becomes `5 == 5`, which is `true`.',
          },
          {
            title: '4. Boolean involved? Convert the boolean to a number first',
            body: '`true` becomes `1` and `false` becomes `0`, then the comparison restarts. This is why `"1" == true` is `true` — it becomes `"1" == 1`, then `1 == 1`.',
          },
          {
            title: '5. Object versus primitive? Convert the object to a primitive',
            body: 'Using `valueOf` then `toString`, exactly as in arithmetic. Then compare again with these same rules.',
          },
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Work through each of these against the five rules.',
        code: [
          'console.log("1" == true);    // true  — rule 4, then rule 3',
          'console.log("" == 0);        // true  — rule 3: "" → 0',
          'console.log("0" == 0);       // true  — rule 3: "0" → 0',
          'console.log("" == "0");      // false — rule 1: two different strings',
          '',
          'console.log([] == 0);        // true  — [] → "" → 0',
          'console.log([] == false);    // true  — false → 0, [] → "" → 0',
          'console.log([1] == 1);       // true  — [1] → "1" → 1',
          '',
          'console.log(null == 0);      // false — rule 2 forbids it',
          'console.log(NaN == NaN);     // false — NaN equals nothing',
        ].join('\n'),
        output: ['true', 'true', 'true', 'false', 'true', 'true', 'true', 'false', 'false'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: '`==` is not transitive',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Look at three of those results together:',
        ],
      },
      {
        kind: SECTION.ANNOTATED_CODE,
        language: 'javascript',
        code: [
          '"" == 0     // true',
          '"0" == 0    // true',
          '"" == "0"   // false',
        ].join('\n'),
        annotations: [
          { line: 1, text: 'Rule 3 applies: the string `""` converts to the number `0`, and `0 == 0`.' },
          { line: 2, text: 'Rule 3 applies again: `"0"` converts to `0`, and `0 == 0`.' },
          { line: 3, text: 'Rule 1 applies: both are strings, so no conversion happens and they are compared directly. `""` and `"0"` are different strings.' },
        ],
      },
      {
        kind: SECTION.PROSE,
        body: [
          'In ordinary mathematics, if `a = b` and `b = c` then `a = c`. Loose equality breaks that guarantee, because *which conversion happens depends on the pair being compared*. This is the strongest single argument against using `==`: it is not an equivalence relation, so you cannot reason about it the way you reason about equality.',
          '`===` is transitive, because it never converts anything.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INTERVIEW,
        title: 'A complete answer to "== vs ==="',
        body: [
          '"`===` compares type and value with no conversion. `==` converts to a common type first, following a specific algorithm — null and undefined are equal only to each other, strings convert to numbers when compared with numbers, booleans convert to numbers first, and objects convert to primitives. The problem is that `==` is not transitive: `"" == 0` and `"0" == 0` are both true, but `"" == "0"` is false. So I use `===` everywhere, with one exception — `x == null` as a concise check for null or undefined."',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The one legitimate use of `==`',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: '`== null` is the idiomatic "is this null or undefined?" check.',
        code: [
          'function hasValue(v) { return v != null; }',
          '',
          'console.log(hasValue(null));       // false',
          'console.log(hasValue(undefined));  // false',
          'console.log(hasValue(0));          // true  — a real value',
          'console.log(hasValue(""));         // true  — a real value',
          'console.log(hasValue(false));      // true  — a real value',
          'console.log(hasValue(NaN));        // true  — a real value',
        ].join('\n'),
        output: ['false', 'false', 'true', 'true', 'true', 'true'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Because rule 2 makes `null` and `undefined` loosely equal to each other **and to nothing else**, `v == null` is exactly "is this null or undefined?". It is shorter than `v === null || v === undefined` and universally recognised.',
          'Every other use of `==` should be `===`. Most linter configurations enforce exactly this, with an explicit exemption for `== null`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: '`Object.is` — a third kind of equality',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Two edge cases where `===` gives arguably the wrong answer.',
        code: [
          'console.log(NaN === NaN);          // false',
          'console.log(Object.is(NaN, NaN));  // true',
          '',
          'console.log(0 === -0);             // true',
          'console.log(Object.is(0, -0));     // false',
          '',
          '// Otherwise identical to ===',
          'console.log(Object.is("a", "a"));  // true',
          'console.log(Object.is({}, {}));    // false — different objects',
        ].join('\n'),
        output: ['false', 'true', 'true', 'false', 'true', 'false'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`Object.is` behaves like `===` except for those two cases: it considers `NaN` equal to itself, and distinguishes `0` from `-0`. It is occasionally useful — `Object.is(v, NaN)` is a readable NaN check — but `===` remains the default.',
          'Remember that none of these three compares object *contents*. All of them ask whether two objects are the same object, as you saw in the previous module.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'console.log([] == false, [] == 0, [] === false);',
        ].join('\n'),
        options: [
          'true true true',
          'true true false',
          'false false false',
          'true false false',
        ],
        correct: 1,
        explanation:
          'For `[] == false`: rule 4 converts `false` to `0`, then rule 5 converts `[]` to the primitive `""`, then rule 3 converts `""` to `0` — and `0 == 0` is true. For `[] == 0`: rule 5 and rule 3 give the same chain, so it is also true. For `[] === false`: the types differ — object versus boolean — so strict equality returns false immediately with no conversion. Note the oddity this creates: `[]` is *truthy* in an `if`, yet it is loosely equal to `false`. Those are two different mechanisms, which is exactly why mixing them causes confusion.',
      },
    ],
    exercises: [
      {
        id: 'ex-m03-05-a',
        title: 'Predict the non-transitive trio',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['coercion', 'operators'],
        instructions: 'What does this print?',
        code: ['console.log("" == 0, "0" == 0, "" == "0");'].join('\n'),
        options: [
          'true true true',
          'true true false',
          'false false false',
          'false true false',
        ],
        correct: 1,
        hints: [
          'For the first two, one side is a number — what does rule 3 do?',
          'For the third, both sides are the same type. Does any conversion happen?',
        ],
        solution: 'true true false',
        solutionExplanation:
          'The first two mix a string with a number, so rule 3 converts the string to a number: `""` becomes `0` and `"0"` becomes `0`, and both comparisons are `0 == 0`. The third compares two strings — same type, so rule 1 applies and no conversion happens at all. `""` and `"0"` are simply different strings. This is the clearest demonstration that `==` is not transitive, and it is the single best argument for always using `===`.',
      },
      {
        id: 'ex-m03-05-b',
        title: 'Choose the null check',
        kind: EXERCISE_KIND.CHOOSE_IMPLEMENTATION,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['coercion'],
        instructions:
          'You want to run some code only when `value` is neither `null` nor `undefined`. Values of `0`, `""` and `false` must be allowed through. Which check is correct and idiomatic?',
        options: [
          'if (value)',
          'if (value !== null)',
          'if (value != null)',
          'if (typeof value !== "undefined")',
        ],
        correct: 2,
        hints: [
          'The first rejects 0, "" and false, which must be allowed.',
          'The second and fourth each catch only one of the two values you want to exclude.',
        ],
        solution: 'if (value != null)',
        solutionExplanation:
          'Loose inequality with `null` is true for everything except `null` and `undefined`, because rule 2 of the loose equality algorithm makes those two equal to each other and to nothing else. That is precisely the check required. `if (value)` is a truthiness test and wrongly rejects `0`, `""` and `false`. `value !== null` lets `undefined` through, and the `typeof` check lets `null` through. This is the one place where `==`/`!=` is the recommended choice, and most linters exempt it explicitly.',
      },
      {
        id: 'ex-m03-05-c',
        title: 'Implement strict equality by hand',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['coercion', 'types'],
        instructions:
          'Write `sameValue(a, b)` that behaves like `Object.is`: identical to `===` except that it treats `NaN` as equal to itself and treats `0` and `-0` as different. Do not call `Object.is` — implement the logic yourself.',
        starterCode: [
          'function sameValue(a, b) {',
          '  // Handle the two special cases, then fall back to ===.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'equal numbers are equal', body: 'expect(sameValue(1, 1)).toBe(true);' },
          { name: 'different numbers are not', body: 'expect(sameValue(1, 2)).toBe(false);' },
          { name: 'NaN equals NaN', body: 'expect(sameValue(NaN, NaN)).toBe(true);' },
          { name: 'NaN does not equal a number', body: 'expect(sameValue(NaN, 1)).toBe(false);' },
          { name: 'zero and negative zero differ', body: 'expect(sameValue(0, -0)).toBe(false);' },
          { name: 'zero equals zero', body: 'expect(sameValue(0, 0)).toBe(true);' },
          { name: 'no type coercion happens', body: 'expect(sameValue(1, "1")).toBe(false);' },
          { name: 'strings compare by value', body: 'expect(sameValue("a", "a")).toBe(true);' },
          { name: 'objects compare by identity', body: 'const o = {}; expect(sameValue(o, o)).toBe(true); expect(sameValue({}, {})).toBe(false);', hidden: true },
        ],
        hints: [
          'Detect NaN without `===` — `Number.isNaN` works, or the fact that `a !== a` is only true for NaN.',
          'To tell `0` from `-0`, note that `1 / 0` is `Infinity` while `1 / -0` is `-Infinity`.',
          'Everything else can simply return `a === b`.',
        ],
        solution: [
          'function sameValue(a, b) {',
          '  // Both NaN: the only values not equal to themselves.',
          '  if (a !== a && b !== b) return true;',
          '',
          '  // Distinguish 0 from -0 by their reciprocals.',
          '  if (a === 0 && b === 0) {',
          '    return 1 / a === 1 / b;',
          '  }',
          '',
          '  return a === b;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The `a !== a` trick is the classic way to detect NaN without a helper — it is true only for NaN, since every other value equals itself. For the zero case, `===` already reports `0 === -0` as true, so the two must be told apart another way: dividing by them gives `Infinity` and `-Infinity` respectively, which do differ. Everything else falls through to plain `===`, which is the correct behaviour for all remaining values including objects, where identity is compared rather than contents. `Number.isNaN(a) && Number.isNaN(b)` is an equally valid first condition and arguably more readable.',
      },
    ],
    quiz: {
      id: 'qz-m03-05',
      questions: [
        {
          id: 'q-m03-05-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['coercion', 'operators'],
          prompt: 'What is the essential difference between `==` and `===`?',
          options: [
            '`==` compares values and `===` compares references',
            '`===` compares type and value with no conversion; `==` converts to a common type first',
            'They are identical; `===` is just newer',
            '`==` only works with primitives',
          ],
          correct: 1,
          optionExplanations: [
            'Both compare objects by reference; the difference is about conversion.',
            'Correct.',
            'They give different answers for many inputs.',
            '`==` works with objects too, converting them to primitives.',
          ],
          explanation:
            '`===` checks that the types match and then compares values, performing no conversion. `==` applies a defined algorithm to convert the operands to a common type before comparing, which is why it produces surprising results and is not transitive.',
        },
        {
          id: 'q-m03-05-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['coercion'],
          prompt: 'Why is `==` described as not transitive?',
          options: [
            'Because it is slower than `===`',
            'Because `"" == 0` and `"0" == 0` are both true, yet `"" == "0"` is false',
            'Because it cannot compare objects',
            'Because it returns undefined for some comparisons',
          ],
          correct: 1,
          optionExplanations: [
            'Speed has nothing to do with transitivity.',
            'Correct — the conversion applied depends on which pair is being compared.',
            'It can compare objects, by converting them to primitives.',
            'It always returns a boolean.',
          ],
          explanation:
            'Transitivity means that if `a == b` and `b == c`, then `a == c` should hold. Loose equality breaks it because which conversion happens depends on the pair: comparing a string with a number converts the string, but comparing two strings converts nothing. This makes `==` impossible to reason about reliably.',
        },
        {
          id: 'q-m03-05-3',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['coercion'],
          prompt: 'What does this print?',
          code: ['console.log(null == 0, null >= 0);'].join('\n'),
          options: ['true true', 'false false', 'false true', 'true false'],
          correct: 2,
          optionExplanations: [
            'Rule 2 forbids `null` from being loosely equal to a number.',
            'Relational operators do convert `null`, so the second is true.',
            'Correct — equality and relational operators follow different rules.',
            'This has the two the wrong way round.',
          ],
          explanation:
            'Loose equality has a hard-coded rule that `null` is equal only to `undefined`, so it never converts to a number and `null == 0` is false. Relational operators such as `>=` have no such rule, so they convert `null` to `0`, making `0 >= 0` true. The lesson is not to memorise this but to avoid comparing `null` with numbers at all.',
        },
        {
          id: 'q-m03-05-4',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['coercion'],
          prompt: 'How does `Object.is` differ from `===`? (Select all that apply.)',
          options: [
            '`Object.is(NaN, NaN)` is true',
            '`Object.is(0, -0)` is false',
            '`Object.is` compares object contents',
            '`Object.is("1", 1)` is true',
          ],
          correct: [0, 1],
          explanation:
            '`Object.is` matches `===` in every case except two: it treats `NaN` as equal to itself, and it distinguishes `0` from `-0`. It performs no type coercion, so `Object.is("1", 1)` is false, and like every equality operator it compares objects by identity rather than by contents.',
        },
      ],
    },
    summary:
      '`===` compares type and value with no conversion; `==` converts to a common type first, following five rules — same types compare directly, `null` and `undefined` are equal only to each other, strings convert to numbers when compared with numbers, booleans convert to numbers first, and objects convert to primitives. The decisive problem with `==` is that it is not transitive: `"" == 0` and `"0" == 0` are both true while `"" == "0"` is false, because the conversion applied depends on the pair. Use `===` everywhere, with the single exception of `x == null`, which is the idiomatic and linter-approved way to test for null-or-undefined. `Object.is` is a third variant matching `===` except that it treats `NaN` as equal to itself and distinguishes `0` from `-0`.',
    keyTakeaways: [
      '`===` compares type and value; `==` converts first',
      'The five loose-equality rules are learnable and worth knowing',
      '`==` is not transitive — the strongest argument against using it',
      '`x == null` is the one legitimate use, meaning "null or undefined"',
      '`null == 0` is false but `null >= 0` is true — different rule sets',
      '`Object.is` differs from `===` only for `NaN` and `-0`',
    ],
    relatedLessons: ['l-m03-04', 'l-m03-06'],
    interviewConnections: [
      'What is the difference between == and ===?',
      'Why is == not transitive?',
      'When is it acceptable to use ==?',
      'Why is null == 0 false but null >= 0 true?',
      'How does Object.is differ from ===?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m03-06',
    slug: 'coercion-traps-and-practices',
    moduleId: M,
    order: 6,
    title: 'Coercion Traps and Safe Practices',
    description: 'The classic puzzles collected in one place, plus the small set of habits that make coercion stop mattering.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 16,
    xp: 35,
    topicIds: ['coercion', 'clean-code'],
    prerequisites: ['l-m03-05'],
    learningObjectives: [
      'Explain the well-known coercion puzzles rather than merely recognising them',
      'Apply the boundary-conversion pattern to real input',
      'Validate numeric and text input robustly',
      'State the habits that make coercion bugs rare',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'You now know every rule involved. This lesson applies them to the puzzles you will actually be shown, and then sets out the handful of practices that make the whole topic stop being a problem in code you write.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The classic puzzles, explained',
      },
      {
        kind: SECTION.TABLE,
        headers: ['Expression', 'Result', 'Why'],
        rows: [
          ['`"5" + 3`', '`"53"`', '`+` concatenates when either side is a string'],
          ['`"5" - 3`', '`2`', '`-` has no string meaning, so both convert to numbers'],
          ['`[] + []`', '`""`', 'Both become `""`, joined'],
          ['`[] + {}`', '`"[object Object]"`', '`""` joined with the default object string'],
          ['`[] == false`', '`true`', '`false` → `0`; `[]` → `""` → `0`'],
          ['`Boolean([])`', '`true`', 'Every object is truthy — a different mechanism'],
          ['`null == 0`', '`false`', 'Loose equality never converts `null` to a number'],
          ['`null >= 0`', '`true`', 'Relational operators do convert it'],
          ['`NaN == NaN`', '`false`', '`NaN` is equal to nothing, itself included'],
          ['`0.1 + 0.2 == 0.3`', '`false`', 'Floating-point representation, not coercion'],
          ['`"b" + "a" + +"a" + "a"`', '`"baNaNa"`', '`+"a"` is `NaN`, which stringifies'],
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Confirm the two that look most contradictory.',
        code: [
          '// [] is truthy, yet loosely equal to false',
          'console.log(Boolean([]));   // true',
          'console.log([] == false);   // true',
          '',
          '// These use different mechanisms:',
          '//   Boolean() asks "is it one of the eight falsy values?" — no.',
          '//   ==       converts both sides to numbers — both become 0.',
          '',
          '// The famous one:',
          'console.log("b" + "a" + +"a" + "a");',
        ].join('\n'),
        output: ['true', 'true', 'baNaNa'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The `[]` pair is the one worth understanding properly, because it looks like a genuine contradiction and is not. Truthiness asks a single question — is this value one of the eight falsy values? An array is not, so it is truthy. Loose equality asks a different question entirely, converting both operands to a common type; `[]` becomes `""` becomes `0`, and `false` becomes `0`.',
          'Two different questions, two different mechanisms, two answers that only appear to conflict.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Practice 1: convert at the boundary',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The single most effective habit. Data enters your program from forms, URLs, APIs and storage — and it usually arrives as text. Convert it once, at the point of entry, validate immediately, and let everything downstream assume correct types.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'One conversion point, one validation point.',
        code: [
          'function parseOrderInput(raw) {',
          '  const quantity = Number(String(raw.quantity).trim());',
          '  const price = Number(String(raw.price).trim());',
          '',
          '  if (!Number.isFinite(quantity) || quantity < 0) {',
          '    return { ok: false, error: "Quantity must be a positive number" };',
          '  }',
          '  if (!Number.isFinite(price) || price < 0) {',
          '    return { ok: false, error: "Price must be a positive number" };',
          '  }',
          '',
          '  return { ok: true, quantity, price };',
          '}',
          '',
          'console.log(parseOrderInput({ quantity: "3", price: "9.99" }));',
          'console.log(parseOrderInput({ quantity: "3 items", price: "9.99" }));',
        ].join('\n'),
        output: [
          '{ok: true, quantity: 3, price: 9.99}',
          '{ok: false, error: "Quantity must be a positive number"}',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Watch the empty-string hole',
        body: [
          'The function above still has one gap: `Number("")` is `0`, and `0` passes `Number.isFinite` and the `>= 0` check. An untouched quantity field would be accepted as zero.',
          'Whether that is a bug depends on your requirements. If an empty field should be rejected, test the trimmed string for emptiness *before* converting. This is exactly the check the exercise below asks you to add.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Practice 2: use the right comparison',
      },
      {
        kind: SECTION.TABLE,
        headers: ['You mean', 'Write'],
        rows: [
          ['These are the same value', '`a === b`'],
          ['This is null or undefined', '`a == null`'],
          ['This has a real value', '`a != null`'],
          ['This is NaN', '`Number.isNaN(a)`'],
          ['This is a usable number', '`Number.isFinite(a)`'],
          ['This array has items', '`arr.length > 0`'],
          ['This object has properties', '`Object.keys(obj).length > 0`'],
          ['Use this default if missing', '`a ?? fallback`'],
          ['Use this default if falsy', '`a || fallback`'],
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Practice 3: build strings with template literals',
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Concatenation — coercion by accident',
          code: [
            'const msg = "Total: " + total',
            '  + " (" + count + " items)";',
            '',
            '// If total is undefined:',
            '// "Total: undefined (3 items)"',
            '// and no warning at all.',
          ].join('\n'),
        },
        right: {
          title: 'Template literal — coercion on purpose',
          code: [
            'const msg =',
            '  `Total: ${total} (${count} items)`;',
            '',
            '// Same conversion, but the',
            '// intent to build a string is',
            '// unmistakable to a reader.',
          ].join('\n'),
        },
        note: 'Template literals still coerce — that is their job — but they make string-building explicit rather than a side effect of arithmetic syntax, and they cannot accidentally perform addition.',
      },
      {
        kind: SECTION.HEADING,
        text: 'Practice 4: never test a type by coercing it',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Validation that looks reasonable and is not.',
        code: [
          '// Wrong: accepts "", null, [] and "0" in confusing ways',
          'function badIsNumber(v) { return !isNaN(v); }',
          '',
          'console.log(badIsNumber(""));    // true  ← wrong',
          'console.log(badIsNumber(null));  // true  ← wrong',
          'console.log(badIsNumber([]));    // true  ← wrong',
          'console.log(badIsNumber("42"));  // true  ← a string, not a number',
          '',
          '// Right: no coercion at all',
          'function isNumber(v) { return typeof v === "number" && Number.isFinite(v); }',
          '',
          'console.log(isNumber(""));       // false',
          'console.log(isNumber(null));     // false',
          'console.log(isNumber("42"));     // false',
          'console.log(isNumber(42));       // true',
        ].join('\n'),
        output: ['true', 'true', 'true', 'true', 'false', 'false', 'false', 'true'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The global `isNaN` converts its argument before testing, so `badIsNumber` is really asking "would this convert to a number?" — and `""`, `null` and `[]` all convert to `0`. A type check should never involve a conversion, because the conversion is what destroys the information you were trying to inspect.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'The whole module in five habits',
        body: [
          'Convert explicitly at the boundary, and validate there. Use `===` everywhere except `== null`. Use `??` rather than `||` for defaults. Build strings with template literals. Never test a type by coercing it.',
          'Follow those and coercion becomes something you need to *read* — in other people\'s code and in interviews — rather than something that bites you in your own.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const raw = "";',
          '',
          'console.log(Number(raw));',
          'console.log(Number.isFinite(Number(raw)));',
          'console.log(raw === "");',
        ].join('\n'),
        options: [
          'NaN, false, true',
          '0, true, true',
          '0, false, true',
          'NaN, true, false',
        ],
        correct: 1,
        explanation:
          '`Number("")` is `0`, not `NaN` — the empty string is a special case in the conversion rules. Because `0` is a perfectly finite number, `Number.isFinite` returns `true`, which means a validation built only on that check would accept an empty field as the value zero. The third line is the guard you actually needed: testing the raw string for emptiness *before* converting. This is the single most practical trap in the whole module.',
      },
    ],
    exercises: [
      {
        id: 'ex-m03-06-a',
        title: 'Explain the array contradiction',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['coercion'],
        instructions:
          'Why is `Boolean([])` true while `[] == false` is also true? These look contradictory.',
        options: [
          'Because `==` has a bug with arrays',
          'Because they use different mechanisms: truthiness checks the falsy list, while `==` converts both sides to numbers',
          'Because empty arrays are sometimes objects and sometimes not',
          'Because `Boolean()` and `==` disagree about the type of an array',
        ],
        correct: 1,
        hints: [
          'What question does truthiness actually ask?',
          'What conversions does loose equality perform on each side?',
        ],
        solution: 'Because they use different mechanisms: truthiness checks the falsy list, while `==` converts both sides to numbers',
        solutionExplanation:
          'Truthiness asks one question only: is this value one of the eight falsy values? An array is an object and is not on that list, so it is truthy. Loose equality asks something entirely different — it converts `false` to `0`, converts `[]` to its primitive `""`, then converts `""` to `0`, and finds `0 == 0`. There is no contradiction because the two operations are answering different questions. This is a favourite interview question precisely because reciting the results is easy while explaining them requires understanding both mechanisms.',
      },
      {
        id: 'ex-m03-06-b',
        title: 'Predict the validation hole',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['coercion'],
        instructions: 'A form field is left untouched, so its value is `""`. What does this validation print?',
        code: [
          'const value = "";',
          '',
          'if (Number.isFinite(Number(value))) {',
          '  console.log("valid number:", Number(value));',
          '} else {',
          '  console.log("invalid");',
          '}',
        ].join('\n'),
        options: [
          'invalid',
          'valid number: 0',
          'valid number: NaN',
          'Nothing is printed',
        ],
        correct: 1,
        hints: [
          'What is `Number("")`?',
          'Is that result finite?',
        ],
        solution: 'valid number: 0',
        solutionExplanation:
          '`Number("")` is `0`, which is a perfectly finite number, so the validation passes and treats an untouched field as a deliberate zero. This is a real bug pattern that reaches production regularly — an empty quantity field silently becomes an order for zero items. The fix is to test the trimmed raw string for emptiness before converting, rather than relying on the conversion to signal failure.',
      },
      {
        id: 'ex-m03-06-c',
        title: 'Write a robust input validator',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['coercion'],
        instructions:
          'Write `validateQuantity(raw)` that returns `{ ok: true, value: n }` for a valid quantity or `{ ok: false, error: "..." }` otherwise. A valid quantity is a whole number of at least 1. Reject empty and whitespace-only input with the error `"Required"`, non-numeric input with `"Must be a number"`, and anything below 1 or fractional with `"Must be a whole number of at least 1"`.',
        starterCode: [
          'function validateQuantity(raw) {',
          '  // Guard in order: empty, then non-numeric, then out of range.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'accepts a valid quantity', body: 'expect(validateQuantity("3")).toEqual({ ok: true, value: 3 });' },
          { name: 'accepts a numeric value', body: 'expect(validateQuantity(5)).toEqual({ ok: true, value: 5 });' },
          { name: 'trims surrounding whitespace', body: 'expect(validateQuantity("  7  ")).toEqual({ ok: true, value: 7 });' },
          { name: 'rejects an empty string', body: 'expect(validateQuantity("")).toEqual({ ok: false, error: "Required" });' },
          { name: 'rejects whitespace only', body: 'expect(validateQuantity("   ")).toEqual({ ok: false, error: "Required" });' },
          { name: 'rejects null and undefined', body: 'expect(validateQuantity(null).error).toBe("Required"); expect(validateQuantity(undefined).error).toBe("Required");' },
          { name: 'rejects non-numeric text', body: 'expect(validateQuantity("3 items")).toEqual({ ok: false, error: "Must be a number" });' },
          { name: 'rejects zero and negatives', body: 'expect(validateQuantity("0").error).toBe("Must be a whole number of at least 1"); expect(validateQuantity("-2").error).toBe("Must be a whole number of at least 1");' },
          { name: 'rejects fractions', body: 'expect(validateQuantity("2.5").error).toBe("Must be a whole number of at least 1");', hidden: true },
        ],
        hints: [
          'Convert to a string and trim first, so `null` and `undefined` become `"null"`/`"undefined"` — or handle them before that.',
          'Test the trimmed string for `""` before converting, to close the `Number("") === 0` hole.',
          '`Number.isInteger(n)` checks for a whole number; combine it with `n >= 1`.',
        ],
        solution: [
          'function validateQuantity(raw) {',
          '  if (raw == null) {',
          '    return { ok: false, error: "Required" };',
          '  }',
          '',
          '  const trimmed = String(raw).trim();',
          '  if (trimmed === "") {',
          '    return { ok: false, error: "Required" };',
          '  }',
          '',
          '  const value = Number(trimmed);',
          '  if (!Number.isFinite(value)) {',
          '    return { ok: false, error: "Must be a number" };',
          '  }',
          '',
          '  if (!Number.isInteger(value) || value < 1) {',
          '    return { ok: false, error: "Must be a whole number of at least 1" };',
          '  }',
          '',
          '  return { ok: true, value };',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The guards run from most general to most specific, and the order matters. Handling `null` and `undefined` first prevents `String(null)` producing the string `"null"`, which would otherwise fall through to the "Must be a number" branch with a misleading message. Trimming and testing for `""` next closes the `Number("") === 0` hole — without it, an empty field would reach the range check and be reported as "Must be a whole number of at least 1", which is confusing feedback for a field the user simply has not filled in. Returning a result object rather than throwing lets the caller decide how to present the error, and gives a place to put the converted value so the caller never has to convert again.',
      },
      {
        id: 'ex-m03-06-d',
        title: 'Refactor away the coercion',
        kind: EXERCISE_KIND.REFACTOR,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['coercion', 'clean-code'],
        instructions:
          'Rewrite this function to remove every reliance on implicit coercion. It should behave identically for valid input but must use `===`, explicit conversion, and a correct default for `discount`. A `discount` of `0` must be respected rather than replaced by the default of `10`.',
        starterCode: [
          'function priceLabel(price, discount) {',
          '  const rate = discount || 10;',
          '  const final = price - (price * rate) / 100;',
          '  if (final == 0) {',
          '    return "Free";',
          '  }',
          '  return "Price: " + final;',
          '}',
        ].join('\n'),
        tests: [
          { name: 'applies the default discount', body: 'expect(priceLabel(100)).toBe("Price: 90");' },
          { name: 'applies an explicit discount', body: 'expect(priceLabel(100, 20)).toBe("Price: 80");' },
          { name: 'respects a discount of zero', body: 'expect(priceLabel(100, 0)).toBe("Price: 100");' },
          { name: 'reports a free item', body: 'expect(priceLabel(100, 100)).toBe("Free");' },
          { name: 'handles a price of zero', body: 'expect(priceLabel(0, 50)).toBe("Free");' },
          { name: 'handles decimals', body: 'expect(priceLabel(50, 10)).toBe("Price: 45");' },
          { name: 'null discount still uses the default', body: 'expect(priceLabel(100, null)).toBe("Price: 90");', hidden: true },
        ],
        hints: [
          '`discount || 10` replaces a deliberate 0 with 10 — `??` falls back only on null and undefined.',
          '`final == 0` uses loose equality; `===` is correct and behaves identically here.',
          'Build the returned string with a template literal rather than `+`.',
        ],
        solution: [
          'function priceLabel(price, discount) {',
          '  const rate = discount ?? 10;',
          '  const final = price - (price * rate) / 100;',
          '',
          '  if (final === 0) {',
          '    return "Free";',
          '  }',
          '',
          '  return `Price: ${final}`;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Three changes, each removing a distinct reliance on coercion. `??` replaces `||` so that a deliberate discount of `0` survives instead of being swallowed as falsy — that is the behavioural bug the third test catches. `===` replaces `==`; here both give the same answer since `final` is always a number, but using strict equality by default means you never have to stop and check whether it matters. The template literal replaces `+` concatenation, making it explicit that a string is being built rather than relying on the overloaded plus. The hidden test confirms `??` still falls back for `null`, which is the intended behaviour.',
      },
    ],
    quiz: {
      id: 'qz-m03-06',
      questions: [
        {
          id: 'q-m03-06-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['coercion'],
          prompt: 'What is the single most effective habit for avoiding coercion bugs?',
          options: [
            'Memorising the loose equality table',
            'Converting and validating input explicitly at the boundary where it enters your code',
            'Using `var` instead of `let`',
            'Avoiding all arithmetic on numbers',
          ],
          correct: 1,
          optionExplanations: [
            'Useful for reading code and interviews, but it does not prevent bugs in your own.',
            'Correct — everything downstream can then assume correct types.',
            'Declaration keywords have nothing to do with coercion.',
            'Arithmetic is fine once the types are known to be correct.',
          ],
          explanation:
            'Data arrives as text from forms, URLs and APIs. Converting once at the entry point and validating immediately means the rest of the program can assume correct types, so coercion never gets a chance to happen implicitly.',
        },
        {
          id: 'q-m03-06-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['coercion'],
          prompt: 'Why does this validation accept an empty string?',
          code: [
            'function isValid(v) { return !isNaN(v); }',
            'console.log(isValid(""), isValid(null), isValid("42"));',
          ].join('\n'),
          options: [
            'false false true',
            'true true true',
            'true false true',
            'false true false',
          ],
          correct: 1,
          optionExplanations: [
            'The global `isNaN` coerces, so all three convert successfully.',
            'Correct — `""` and `null` both convert to 0, and `"42"` converts to 42.',
            '`null` converts to 0, which is not NaN.',
            'All three pass this flawed check.',
          ],
          explanation:
            'The global `isNaN` converts its argument before testing, so it really asks "would this convert to a number?". `""` and `null` both convert to `0`, and `"42"` converts to `42`, so all three return `true`. A type check must never coerce — use `typeof v === "number" && Number.isFinite(v)`.',
        },
        {
          id: 'q-m03-06-3',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['coercion', 'clean-code'],
          prompt: 'Which of these are recommended practices? (Select all that apply.)',
          options: [
            'Use `===` everywhere except `== null`',
            'Use `??` rather than `||` when supplying defaults',
            'Build strings with template literals rather than `+`',
            'Use `!isNaN(v)` to check whether a value is a number',
          ],
          correct: [0, 1, 2],
          explanation:
            'The first three are the core habits. Strict equality avoids the non-transitivity of `==`, `??` preserves valid falsy values such as `0` and `""`, and template literals make string-building explicit rather than a side effect of the overloaded `+`. The fourth is the anti-pattern: the global `isNaN` coerces first, so it accepts `""`, `null` and `[]` as numbers.',
        },
      ],
    },
    summary:
      'The famous coercion puzzles all follow from rules you now know: `+` is overloaded, other operators convert to numbers, objects stringify via `toString`, and loose equality converts before comparing. The `Boolean([]) === true` and `[] == false` pair is not a contradiction — truthiness checks the eight-value falsy list while `==` converts both sides to numbers. In your own code, five habits make the topic largely irrelevant: convert and validate explicitly at the boundary where data enters, use `===` everywhere except `== null`, use `??` rather than `||` for defaults, build strings with template literals, and never test a type by coercing it. The `Number("") === 0` hole is the trap most likely to reach production, so check raw input for emptiness before converting.',
    keyTakeaways: [
      '`Boolean([])` and `[] == false` are both true — different mechanisms, not a contradiction',
      'Convert and validate at the boundary; everything downstream can then trust its types',
      '`Number("")` is `0`, so check for an empty string before converting',
      'A type check must never coerce — `!isNaN(v)` is not a number check',
      'Five habits: boundary conversion, `===`, `??`, template literals, no coercive type tests',
    ],
    relatedLessons: ['l-m03-05', 'l-m03-03'],
    interviewConnections: [
      'Why is [] truthy but [] == false also true?',
      'What does "b" + "a" + +"a" + "a" evaluate to, and why?',
      'How do you safely validate numeric input from a form?',
      'What is wrong with using !isNaN() as a number check?',
    ],
  },
];
