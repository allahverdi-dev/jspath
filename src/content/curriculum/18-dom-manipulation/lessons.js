import { SECTION, CALLOUT_TONE, DIFFICULTY, EXERCISE_KIND, QUIZ_KIND } from '../../schema/types.js';

const M = 'm18';

export default [
  /* ================================================================== */
  {
    id: 'l-m18-01',
    slug: 'creating-elements',
    moduleId: M,
    order: 1,
    title: 'Creating Elements',
    description: 'Building real elements from JavaScript — text, classes and attributes — without concatenating markup strings.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 17,
    xp: 35,
    topicIds: ['dom-manipulation', 'dom'],
    prerequisites: ['l-m17-06'],
    learningObjectives: [
      'Create elements with document.createElement',
      'Set text, classes and attributes on a created element',
      'Build a nested structure entirely from JavaScript',
      'Explain the trade-off between createElement and innerHTML',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Module 17 was about **finding and changing** elements that were already in the page. This module is about **building** them — the shift from "scripting a static page" to "rendering a UI from data".',
          '`document.createElement(tagName)` makes a brand-new element. Critically, that element exists only in memory until you explicitly insert it into the document, which lesson 2 covers.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<div id="host"></div>',
        caption: 'A created element is real, but not yet in the page.',
        code: [
          'const item = document.createElement("li");',
          '',
          'console.log("tag      :", item.tagName);',
          'console.log("is an element:", item instanceof HTMLElement);',
          '',
          '// It has no parent, because it is not in the document yet',
          'console.log("parent   :", item.parentElement);',
          'console.log("in page  :", document.body.contains(item));',
        ].join('\n'),
        output: [
          'tag      : LI',
          'is an element: true',
          'parent   : null',
          'in page  : false',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Created, but detached',
        body: [
          'A freshly created element is **detached** — a valid element object with no place in the document tree. `parentElement` is `null` and nothing renders. That is deliberate: it lets you fully build and configure an element before it ever touches the page.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Configuring a created element',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<div id="host"></div>',
        caption: 'Everything from Module 17 applies to elements you create yourself.',
        code: [
          'const user = { id: 42, name: "Ada Lovelace", role: "admin" };',
          '',
          'const item = document.createElement("li");',
          '',
          '// Text — safe for any value, including user-provided data',
          'item.textContent = user.name;',
          '',
          '// Classes',
          'item.classList.add("user-item");',
          'item.classList.toggle("is-admin", user.role === "admin");',
          '',
          '// Attributes and data-*',
          'item.setAttribute("aria-label", `User ${user.name}`);',
          'item.dataset.userId = String(user.id);',
          '',
          'console.log("text   :", item.textContent);',
          'console.log("class  :", item.className);',
          'console.log("aria   :", item.getAttribute("aria-label"));',
          'console.log("data id:", item.dataset.userId);',
        ].join('\n'),
        output: [
          'text   : Ada Lovelace',
          'class  : user-item is-admin',
          'aria   : User Ada Lovelace',
          'data id: 42',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Building a nested structure',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Real UI is not one element — it is a small tree. You build it by creating each piece and appending children to parents. `parent.append(child)` is the modern way to attach one node inside another; lesson 2 covers the full family of insertion methods.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<div id="host"></div>',
        caption: 'A product card, assembled entirely in JavaScript.',
        code: [
          'const product = { name: "Desk lamp", price: 24.99, inStock: true };',
          '',
          'const card = document.createElement("article");',
          'card.className = "product-card";',
          '',
          'const title = document.createElement("h3");',
          'title.textContent = product.name;',
          '',
          'const price = document.createElement("p");',
          'price.className = "price";',
          'price.textContent = `£${product.price.toFixed(2)}`;',
          '',
          'const badge = document.createElement("span");',
          'badge.className = product.inStock ? "badge in-stock" : "badge sold-out";',
          'badge.textContent = product.inStock ? "In stock" : "Sold out";',
          '',
          '// append() accepts several nodes at once',
          'card.append(title, price, badge);',
          '',
          'document.getElementById("host").append(card);',
          '',
          'console.log("children   :", card.children.length);',
          'console.log("structure  :", [...card.children].map((c) => c.tagName).join(","));',
          'console.log("title text :", card.querySelector("h3").textContent);',
          'console.log("badge class:", badge.className);',
        ].join('\n'),
        output: [
          'children   : 3',
          'structure  : H3,P,SPAN',
          'title text : Desk lamp',
          'badge class: badge in-stock',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Build first, insert last',
        body: [
          'Notice the order: the card was fully assembled — title, price and badge all appended — **before** it was put into the document. Only the final `append` touches the live page.',
          'This is a good habit for readability, and it also means the browser does not see a half-built card at any point. Lesson 4 shows `DocumentFragment`, which extends the same idea to inserting many top-level elements at once.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'createElement versus innerHTML',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The same card could be written as a single template string assigned to `innerHTML`. Both approaches are legitimate; the trade-off is worth understanding precisely rather than following a slogan.',
        ],
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'createElement',
          code: [
            'const li = document.',
            '  createElement("li");',
            'li.textContent = user.name;',
            '',
            '// Data can never be',
            '// parsed as markup.',
            '// Verbose for big trees.',
            '// Keeps references to',
            '// each element you made.',
          ].join('\n'),
        },
        right: {
          title: 'innerHTML template',
          code: [
            'list.innerHTML = `',
            '  <li>${user.name}</li>`;',
            '',
            '// Concise, reads like',
            '// the markup it makes.',
            '// Interpolated data IS',
            '// parsed as markup —',
            '// unsafe if untrusted.',
          ].join('\n'),
        },
        note: 'The deciding question is the same one from Module 17: does any part of the string come from a user or an external system? If yes, that value must never be interpolated into parsed markup. `createElement` with `textContent` makes that mistake structurally impossible.',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="safe"></ul><ul id="unsafe"></ul>',
        caption: 'The same hostile input, through both routes.',
        code: [
          'const userName = \'<img src=x onerror="window.__hit = true">\';',
          '',
          '// createElement + textContent: inert, always',
          'const li = document.createElement("li");',
          'li.textContent = userName;',
          'document.getElementById("safe").append(li);',
          '',
          'console.log("safe — child elements:", li.children.length);',
          'console.log("safe — text preserved:", li.textContent === userName);',
          '',
          '// innerHTML: the string is PARSED into real elements',
          'document.getElementById("unsafe").innerHTML = `<li>${userName}</li>`;',
          'const parsed = document.querySelector("#unsafe li");',
          '',
          'console.log("unsafe — child elements:", parsed.children.length);',
          'console.log("unsafe — created tag  :", parsed.children[0].tagName);',
        ].join('\n'),
        output: [
          'safe — child elements: 0',
          'safe — text preserved: true',
          'unsafe — child elements: 1',
          'unsafe — created tag  : IMG',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The `textContent` route produced **zero** child elements — the hostile string is nothing but visible text. The `innerHTML` route parsed it into a real `<img>` element, exactly as the attacker intended. That is the whole difference, demonstrated rather than asserted.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'The honest rule, not a blanket ban',
        body: [
          '`innerHTML` is not forbidden. A template string containing **only markup you wrote**, with no interpolated external data, is perfectly safe and often clearer than a dozen `createElement` calls.',
          'The rule is about the **data**, not the property: never interpolate a value you did not author into a string that gets parsed as HTML. When a structure mixes fixed markup with user data, the common professional pattern is to build the skeleton with `innerHTML` and then set the variable text with `textContent` afterwards.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'createTextNode',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<p id="para"></p>',
        caption: 'Text nodes can be created directly, though textContent is usually simpler.',
        code: [
          'const para = document.getElementById("para");',
          '',
          'const text = document.createTextNode("Hello ");',
          'const strong = document.createElement("strong");',
          'strong.textContent = "world";',
          '',
          'para.append(text, strong, "!");',
          '',
          'console.log("text     :", para.textContent);',
          'console.log("childNodes:", para.childNodes.length);',
          'console.log("children  :", para.children.length);',
        ].join('\n'),
        output: [
          'text     : Hello world!',
          'childNodes: 3',
          'children  : 1',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This is the node-versus-element distinction from Module 17 made concrete. The paragraph has **three child nodes** — a text node, the `<strong>` element, and another text node from the `"!"` string — but only **one child element**, since `children` counts elements alone.',
          'You will rarely call `createTextNode` explicitly: `append()` converts any plain string argument into a text node for you, and `textContent` handles the common case. It is worth recognising, because it explains what `append("!")` actually did.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const div = document.createElement("div");',
          'div.textContent = "<b>hi</b>";',
          '',
          'console.log(div.children.length, div.textContent);',
        ].join('\n'),
        options: [
          '0 "<b>hi</b>"',
          '1 "hi"',
          '1 "<b>hi</b>"',
          '0 "hi"',
        ],
        correct: 0,
        explanation:
          '`textContent` never parses its input as markup, whether the element is already in the document or freshly created and still detached — the property behaves identically either way. The string `"<b>hi</b>"` becomes literal visible text: no `<b>` element is created, so `children.length` is `0`, and reading `textContent` back returns the exact same string that was assigned. This is precisely the safety guarantee that makes `createElement` + `textContent` the structurally safe way to render untrusted data.',
      },
    ],
    exercises: [
      {
        id: 'ex-m18-01-a',
        title: 'Predict the detached element',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['dom-manipulation'],
        instructions: 'What does this print?',
        code: [
          'const el = document.createElement("p");',
          'console.log(el.parentElement);',
        ].join('\n'),
        options: ['null', 'undefined', 'The body element', 'It throws'],
        correct: 0,
        hints: [
          'Has the element been inserted into the document yet?',
        ],
        solution: 'null',
        solutionExplanation:
          '`createElement` builds a real, fully functional element, but it is **detached** — it has no place in the document tree until you explicitly insert it. `parentElement` is `null` for any detached node. This is deliberate: it lets you configure an element completely before it ever appears in the page.',
      },
      {
        id: 'ex-m18-01-b',
        title: 'Create a configured element',
        kind: EXERCISE_KIND.DOM_TASK,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['dom-manipulation'],
        instructions:
          'Write `makeBadge(label, tone)` returning a new `<span>` element whose text is `label`, with the class `badge` plus a second class equal to `tone`, and a `data-tone` attribute also set to `tone`. Do not insert it into the document.',
        html: '<div id="host"></div>',
        starterCode: [
          'function makeBadge(label, tone) {',
          '  // createElement, then configure it.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'returns a span element', body: 'expect(makeBadge("New", "info").tagName).toBe("SPAN");' },
          { name: 'sets the text', body: 'expect(makeBadge("New", "info").textContent).toBe("New");' },
          { name: 'has both classes', body: 'const b = makeBadge("New", "info"); expect(b.classList.contains("badge")).toBe(true); expect(b.classList.contains("info")).toBe(true);' },
          { name: 'sets the data-tone attribute', body: 'expect(makeBadge("New", "warn").dataset.tone).toBe("warn");' },
          { name: 'is detached from the document', body: 'expect(makeBadge("New", "info").parentElement).toBe(null);' },
          { name: 'treats markup-like labels as text', body: 'const b = makeBadge("<b>x</b>", "info"); expect(b.children.length).toBe(0); expect(b.textContent).toBe("<b>x</b>");', hidden: true },
        ],
        hints: [
          '`document.createElement("span")` creates the element.',
          '`classList.add("badge", tone)` adds both classes in one call.',
          'Use `textContent`, not `innerHTML`, so any label is safe.',
        ],
        solution: [
          'function makeBadge(label, tone) {',
          '  const span = document.createElement("span");',
          '  span.textContent = label;',
          '  span.classList.add("badge", tone);',
          '  span.dataset.tone = tone;',
          '',
          '  return span;',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`classList.add` accepts multiple class names in a single call, which is tidier than two separate calls. `dataset.tone` writes the `data-tone` attribute using the camelCase conversion from Module 17. The hidden test is the important one: using `textContent` rather than `innerHTML` means a label containing markup is rendered as literal text and creates no child elements — the badge is safe for any label, including one that came from a user.',
      },
      {
        id: 'ex-m18-01-c',
        title: 'Build a nested card',
        kind: EXERCISE_KIND.DOM_TASK,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 30,
        topicIds: ['dom-manipulation'],
        instructions:
          'Write `makeUserCard(user)` taking `{ name, email }` and returning a detached `<div class="user-card">` containing an `<h3>` with the name and a `<p class="email">` with the email, in that order.',
        html: '<div id="host"></div>',
        starterCode: [
          'function makeUserCard(user) {',
          '  // Create the wrapper, create the children, append them.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'returns a div with the right class', body: 'const c = makeUserCard({ name: "Ada", email: "a@b.com" }); expect(c.tagName).toBe("DIV"); expect(c.classList.contains("user-card")).toBe(true);' },
          { name: 'has exactly two child elements', body: 'expect(makeUserCard({ name: "Ada", email: "a@b.com" }).children.length).toBe(2);' },
          { name: 'puts the name in an h3 first', body: 'const c = makeUserCard({ name: "Ada", email: "a@b.com" }); expect(c.children[0].tagName).toBe("H3"); expect(c.children[0].textContent).toBe("Ada");' },
          { name: 'puts the email in a p with the email class', body: 'const c = makeUserCard({ name: "Ada", email: "a@b.com" }); expect(c.children[1].tagName).toBe("P"); expect(c.children[1].className).toBe("email"); expect(c.children[1].textContent).toBe("a@b.com");' },
          { name: 'is detached', body: 'expect(makeUserCard({ name: "Ada", email: "a@b.com" }).parentElement).toBe(null);' },
          { name: 'renders a markup-like name as text', body: 'const c = makeUserCard({ name: "<script>x</script>", email: "e" }); expect(c.querySelector("h3").children.length).toBe(0);', hidden: true },
        ],
        hints: [
          'Create three elements: the wrapper div, an h3 and a p.',
          '`wrapper.append(heading, para)` attaches both in one call, in order.',
          'Set text with `textContent` on each child.',
        ],
        solution: [
          'function makeUserCard(user) {',
          '  const card = document.createElement("div");',
          '  card.className = "user-card";',
          '',
          '  const heading = document.createElement("h3");',
          '  heading.textContent = user.name;',
          '',
          '  const email = document.createElement("p");',
          '  email.className = "email";',
          '  email.textContent = user.email;',
          '',
          '  card.append(heading, email);',
          '',
          '  return card;',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`append` accepts multiple nodes and inserts them in argument order, which is what guarantees the h3 comes before the p. The card is fully assembled before being returned and is still detached, so the caller decides where and when it enters the document. The hidden test confirms the security property: because the name is set with `textContent`, even a `<script>` string becomes inert visible text rather than a parsed element.',
      },
      {
        id: 'ex-m18-01-d',
        title: 'createElement or innerHTML?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['dom-manipulation'],
        instructions:
          'You are rendering a list of comments. Each comment has fixed surrounding markup you wrote, plus body text typed by users. What is the correct approach?',
        options: [
          'Build the whole thing as one innerHTML template string with the comment body interpolated',
          'Build the structure however you like, but set the user-supplied body with textContent rather than interpolating it into parsed markup',
          'Never use innerHTML anywhere in the application',
          'Use innerHTML but remove any angle brackets from the body first',
        ],
        correct: 1,
        hints: [
          'Is the risk in the property itself, or in what gets interpolated into it?',
        ],
        solution: 'Build the structure however you like, but set the user-supplied body with textContent rather than interpolating it into parsed markup',
        solutionExplanation:
          'The risk lives in the **data**, not the property. Fixed markup you authored is safe to use with `innerHTML`; the user-supplied body is not, because interpolating it means it gets parsed. Setting that one value with `textContent` afterwards removes the risk entirely while keeping the template concise. Banning `innerHTML` outright is unnecessary, and hand-stripping angle brackets is a fragile denylist that misses other attack vectors — the safe approach is to never parse untrusted data in the first place.',
      },
    ],
    quiz: {
      id: 'qz-m18-01',
      questions: [
        {
          id: 'q-m18-01-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'What is true of an element returned by document.createElement?',
          options: [
            'It is automatically added to the end of the body',
            'It is a real element but detached — it has no parent until you insert it',
            'It is only a template and must be converted before use',
            'It cannot have classes or attributes set until it is in the document',
          ],
          correct: 1,
          optionExplanations: [
            'Nothing is inserted automatically; you choose where it goes.',
            'Correct — fully functional, but not yet in the tree.',
            'It is a genuine element object, not a template.',
            'It can be fully configured while detached — that is the point.',
          ],
          explanation:
            '`createElement` returns a real, fully usable element that simply has no place in the document yet. `parentElement` is `null` and nothing renders until you insert it, which lets you build and configure it completely first.',
        },
        {
          id: 'q-m18-01-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'Why is createElement with textContent structurally safer than an innerHTML template for user data?',
          options: [
            'It is faster',
            'textContent can never parse its value as markup, so hostile input becomes inert text rather than real elements',
            'innerHTML is deprecated',
            'createElement automatically escapes HTML entities in the DOM tree',
          ],
          correct: 1,
          optionExplanations: [
            'Speed is not the security argument.',
            'Correct — the value is never parsed, so it cannot become an element.',
            'innerHTML is current and legitimate for trusted markup.',
            'It is not "escaping" — the value is simply never parsed as markup at all.',
          ],
          explanation:
            'An `innerHTML` template parses everything interpolated into it, so hostile input becomes real elements. `textContent` treats its value as literal text under all circumstances, making the mistake structurally impossible rather than merely avoided by discipline.',
        },
        {
          id: 'q-m18-01-3',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['dom-manipulation'],
          prompt: 'What does this print?',
          code: [
            'const p = document.createElement("p");',
            'p.append("a", document.createElement("b"), "c");',
            'console.log(p.childNodes.length, p.children.length);',
          ].join('\n'),
          options: ['3 1', '3 3', '1 1', '2 1'],
          correct: 0,
          optionExplanations: [
            'Correct — three nodes total, but only one of them is an element.',
            '`children` counts elements only, and there is just one.',
            '`childNodes` includes the two text nodes.',
            'Both strings become their own text nodes.',
          ],
          explanation:
            '`append` converts each plain string argument into a text node, so there are three child **nodes**: a text node, the `<b>` element, and another text node. `children` counts only elements, giving `1` — the node-versus-element distinction from Module 17 in practice.',
        },
        {
          id: 'q-m18-01-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['dom-manipulation'],
          prompt: 'innerHTML should never be used anywhere in professional JavaScript.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'The risk is specific to untrusted interpolated data, not the property itself.',
            'Correct — it is safe and often clearer for markup you authored.',
          ],
          explanation:
            'A template containing only markup you wrote, with no interpolated external data, is perfectly safe. The rule concerns the data being interpolated, not the property — which is why the common professional pattern is fixed markup via `innerHTML` plus variable text via `textContent`.',
        },
      ],
    },
    summary:
      '`document.createElement(tag)` returns a real but **detached** element — `parentElement` is `null` and nothing renders until you insert it, which lets you configure it fully first. Everything from Module 17 applies to elements you create: `textContent` for text, `classList` for classes, `setAttribute` and `dataset` for attributes. Real UI is a small tree, assembled by appending children to parents — `append()` takes several nodes at once and converts any plain string argument into a text node, which is why a paragraph can have three child **nodes** but one child **element**. The `createElement`-versus-`innerHTML` choice is decided by the **data**, not by the property: a template containing only markup you authored is safe and often clearer, but any value from a user or external system must never be interpolated into a string that gets parsed — `createElement` with `textContent` makes that mistake structurally impossible, producing inert text where `innerHTML` would produce a real element.',
    keyTakeaways: [
      'createElement returns a real element that is detached until inserted',
      'Configure text, classes and attributes before inserting — build first, insert last',
      'append() takes multiple nodes and turns plain strings into text nodes',
      'childNodes counts all nodes; children counts elements only',
      'The innerHTML risk is about interpolated untrusted data, not the property itself',
      'Fixed markup via innerHTML + variable text via textContent is a safe common pattern',
    ],
    relatedLessons: ['l-m17-03', 'l-m18-02'],
    interviewConnections: [
      'What does document.createElement return, and is it in the page?',
      'Why is createElement with textContent safer than an innerHTML template?',
      'What is the difference between childNodes and children?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m18-02',
    slug: 'inserting-and-removing',
    moduleId: M,
    order: 2,
    title: 'Inserting and Removing Nodes',
    description: 'append, prepend, before, after, replaceWith and remove — placing a node exactly where you want it.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 17,
    xp: 35,
    topicIds: ['dom-manipulation'],
    prerequisites: ['l-m18-01'],
    learningObjectives: [
      'Insert nodes as children with append and prepend',
      'Insert nodes as siblings with before and after',
      'Replace and remove existing nodes',
      'Explain why appending an existing node moves rather than copies it',
    ],
    sections: [
      {
        kind: SECTION.HEADING,
        text: 'Inserting as a child',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="list"><li>Bread</li></ul>',
        caption: 'append adds to the end; prepend adds to the start.',
        code: [
          'const list = document.getElementById("list");',
          '',
          'const last = document.createElement("li");',
          'last.textContent = "Milk";',
          'list.append(last);',
          '',
          'const first = document.createElement("li");',
          'first.textContent = "Apples";',
          'list.prepend(first);',
          '',
          'console.log([...list.children].map((li) => li.textContent));',
        ].join('\n'),
        output: ['["Apples", "Bread", "Milk"]'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'append versus appendChild',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="a"></ul><ul id="b"></ul>',
        caption: 'Three real differences worth knowing.',
        code: [
          'const a = document.getElementById("a");',
          'const b = document.getElementById("b");',
          '',
          '// 1. append takes MULTIPLE nodes; appendChild takes exactly one',
          'const x = document.createElement("li");',
          'const y = document.createElement("li");',
          'a.append(x, y);',
          'console.log("append, two at once:", a.children.length);',
          '',
          '// 2. append accepts plain strings; appendChild requires a node',
          'a.append("plain text");',
          'console.log("string appended    :", a.childNodes.length);',
          '',
          '// 3. appendChild RETURNS the appended node; append returns undefined',
          'const node = document.createElement("li");',
          'const returned = b.appendChild(node);',
          'console.log("appendChild returns node:", returned === node);',
          'console.log("append returns          :", a.append(document.createElement("li")));',
        ].join('\n'),
        output: [
          'append, two at once: 2',
          'string appended    : 3',
          'appendChild returns node: true',
          'append returns          : undefined',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Prefer `append` in new code',
        body: [
          '`append` is more flexible — multiple arguments, plain strings accepted — and reads better. `appendChild` is the older API you will meet constantly in existing code; its one advantage is returning the appended node, which occasionally allows a compact `const el = parent.appendChild(document.createElement("li"))`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Inserting as a sibling',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="list"><li id="middle">Bread</li></ul>',
        caption: 'before and after are called on the reference node, not the parent.',
        code: [
          'const middle = document.getElementById("middle");',
          '',
          'const above = document.createElement("li");',
          'above.textContent = "Apples";',
          'middle.before(above);',
          '',
          'const below = document.createElement("li");',
          'below.textContent = "Milk";',
          'middle.after(below);',
          '',
          'const list = document.getElementById("list");',
          'console.log([...list.children].map((li) => li.textContent));',
        ].join('\n'),
        output: ['["Apples", "Bread", "Milk"]'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Note who you call these on',
        body: [
          '`append`/`prepend` are called on the **parent** — "put this inside me". `before`/`after` are called on the **sibling** — "put this next to me". Mixing them up is a common early slip: `list.after(item)` puts the item after the whole list, not inside it.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Replacing and removing',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="list"><li id="old">Old</li><li id="doomed">Doomed</li><li>Keep</li></ul>',
        caption: 'replaceWith swaps a node; remove takes it out entirely.',
        code: [
          'const list = document.getElementById("list");',
          '',
          'const fresh = document.createElement("li");',
          'fresh.textContent = "New";',
          'document.getElementById("old").replaceWith(fresh);',
          '',
          'document.getElementById("doomed").remove();',
          '',
          'console.log([...list.children].map((li) => li.textContent));',
          'console.log("doomed still in page:", document.getElementById("doomed") !== null);',
        ].join('\n'),
        output: [
          '["New", "Keep"]',
          'doomed still in page: false',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`remove()` is called on the node itself — no parent reference needed. The older equivalent, `parent.removeChild(node)`, is still common in existing code and requires you to have the parent to hand, which is exactly the awkwardness `remove()` removes.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Inserting an existing node moves it',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This surprises almost everyone once. A node can only exist in **one place** in the document tree. Appending a node that is already in the page does not copy it — it **moves** it.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="from"><li id="item">Movable</li></ul><ul id="to"></ul>',
        caption: 'One node, one place — appending relocates it.',
        code: [
          'const item = document.getElementById("item");',
          'const from = document.getElementById("from");',
          'const to = document.getElementById("to");',
          '',
          'console.log("before — from:", from.children.length, "to:", to.children.length);',
          '',
          'to.append(item);',
          '',
          'console.log("after  — from:", from.children.length, "to:", to.children.length);',
          'console.log("new parent id:", item.parentElement.id);',
        ].join('\n'),
        output: [
          'before — from: 1 to: 0',
          'after  — from: 0 to: 1',
          'new parent id: to',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'This is useful behaviour, and a real bug source',
        body: [
          'Moving is often exactly what you want — reordering a list, promoting an item into a different container — and it happens in a single call with no explicit removal step.',
          'It becomes a bug when you meant to duplicate. Appending the same node into ten places leaves it in the tenth only. To genuinely duplicate a node, use `cloneNode`, covered in lesson 4.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Clearing a container',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="list"><li>A</li><li>B</li><li>C</li></ul>',
        caption: 'Two idiomatic ways to empty an element.',
        code: [
          'const list = document.getElementById("list");',
          'console.log("start:", list.children.length);',
          '',
          '// Simple and clear for a container you fully own',
          'list.replaceChildren();',
          'console.log("after replaceChildren:", list.children.length);',
          '',
          'list.append(document.createElement("li"), document.createElement("li"));',
          'console.log("refilled:", list.children.length);',
          '',
          '// Also common; equally valid for a container of plain generated content',
          'list.innerHTML = "";',
          'console.log("after innerHTML = \\"\\":", list.children.length);',
        ].join('\n'),
        output: [
          'start: 3',
          'after replaceChildren: 0',
          'refilled: 2',
          'after innerHTML = "": 0',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`replaceChildren()` with no arguments removes every child. Called **with** arguments, it replaces all children in one step — which lesson 3 uses as the core of a re-render.',
          '`innerHTML = ""` achieves the same clearing effect. It carries none of the security concern discussed in lesson 1, because an empty string contains no data to parse — the risk only ever came from interpolating untrusted values.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          '// <ul id="a"><li id="x">X</li></ul><ul id="b"></ul>',
          'const x = document.getElementById("x");',
          'document.getElementById("b").append(x);',
          '',
          'const a = document.getElementById("a");',
          'console.log(a.children.length, document.getElementById("b").children.length);',
        ].join('\n'),
        options: [
          '0 1',
          '1 1',
          '1 0',
          '0 0',
        ],
        correct: 0,
        explanation:
          'A node can exist in only one place in the document tree, so appending an already-attached node **moves** it rather than copying it. The element is removed from `#a` automatically — no explicit `remove()` call needed — and inserted into `#b`, leaving `#a` empty and `#b` with one child. This is frequently the behaviour you want for reordering, but it is a genuine bug when you intended to duplicate the node, which requires `cloneNode`.',
      },
    ],
    exercises: [
      {
        id: 'ex-m18-02-a',
        title: 'Predict the move',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['dom-manipulation'],
        instructions:
          'Given `<div id="a"><p id="p">Hi</p></div><div id="b"></div>`, what does this print?',
        code: [
          'const p = document.getElementById("p");',
          'document.getElementById("b").append(p);',
          'document.getElementById("b").append(p);',
          'console.log(document.getElementById("b").children.length);',
        ].join('\n'),
        options: ['2', '1', '0', 'It throws'],
        correct: 1,
        hints: [
          'Can the same node be in two places at once?',
        ],
        solution: '1',
        solutionExplanation:
          'Appending the same node twice does not duplicate it. The first `append` moves it into `#b`; the second moves it again — from `#b` to `#b`, which simply re-positions it at the end and leaves exactly one child. A node exists in only one place in the tree, so duplication requires `cloneNode`, not repeated appending.',
      },
      {
        id: 'ex-m18-02-b',
        title: 'Insert in the right position',
        kind: EXERCISE_KIND.DOM_TASK,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['dom-manipulation'],
        instructions:
          'Write `insertAfterTarget(text)` that creates a `<li>` with the given text and inserts it immediately **after** the element with id `"target"`, inside the same list.',
        html: '<ul id="list"><li>First</li><li id="target">Target</li><li>Last</li></ul>',
        starterCode: [
          'function insertAfterTarget(text) {',
          '  // Which method is called on the sibling rather than the parent?',
          '}',
        ].join('\n'),
        tests: [
          { name: 'inserts a new list item', body: 'insertAfterTarget("New"); expect(document.querySelectorAll("#list li").length).toBe(4);' },
          { name: 'places it directly after the target', body: 'insertAfterTarget("New"); expect(document.getElementById("target").nextElementSibling.textContent).toBe("New");' },
          { name: 'keeps it inside the list', body: 'insertAfterTarget("New"); expect(document.querySelector("#list li:nth-child(3)").textContent).toBe("New");' },
          { name: 'sets the text safely', body: 'insertAfterTarget("<b>x</b>"); expect(document.getElementById("target").nextElementSibling.children.length).toBe(0);', hidden: true },
        ],
        hints: [
          '`after()` is called on the reference node, not the parent.',
          '`target.after(newItem)` places the new item as the next sibling.',
        ],
        solution: [
          'function insertAfterTarget(text) {',
          '  const item = document.createElement("li");',
          '  item.textContent = text;',
          '  document.getElementById("target").after(item);',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`after()` is called on the **sibling** you are positioning relative to, not on the parent — `target.after(item)` reads naturally as "put this after the target". Using `list.append(item)` would have placed it at the very end instead, failing the position tests. The hidden test confirms `textContent` keeps markup-like input inert.',
      },
      {
        id: 'ex-m18-02-c',
        title: 'Replace and remove',
        kind: EXERCISE_KIND.DOM_TASK,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['dom-manipulation'],
        instructions:
          'Write `dismissNotification(id)` that removes the element with the given id from the document, and returns `true` if it existed or `false` if there was no such element.',
        html: '<div id="notifications"><div id="n1" class="note">One</div><div id="n2" class="note">Two</div></div>',
        starterCode: [
          'function dismissNotification(id) {',
          '  // Guard first — getElementById returns null when nothing matches.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'removes an existing notification', body: 'expect(dismissNotification("n1")).toBe(true); expect(document.getElementById("n1")).toBe(null);' },
          { name: 'leaves the others alone', body: 'dismissNotification("n1"); expect(document.querySelectorAll(".note").length).toBe(1);' },
          { name: 'returns false for a missing id', body: 'expect(dismissNotification("nope")).toBe(false);' },
          { name: 'does not throw for a missing id', body: 'expect(() => dismissNotification("nope")).not.toThrow();' },
          { name: 'returns a real boolean', body: 'expect(typeof dismissNotification("nope")).toBe("boolean");', hidden: true },
        ],
        hints: [
          '`document.getElementById(id)` returns `null` when nothing matches.',
          '`element.remove()` needs no parent reference.',
          'Return `false` before attempting to remove when the element is absent.',
        ],
        solution: [
          'function dismissNotification(id) {',
          '  const el = document.getElementById(id);',
          '  if (el === null) return false;',
          '',
          '  el.remove();',
          '  return true;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The `null` guard is essential — calling `.remove()` on the `null` returned by a failed lookup would throw `Cannot read properties of null`, the exact hazard Module 17 established for every single-element selection. `remove()` is called on the node itself and needs no parent reference, unlike the older `parent.removeChild(node)`. Returning early with `false` keeps the success path clean and gives the caller a real boolean.',
      },
      {
        id: 'ex-m18-02-d',
        title: 'Which insertion method?',
        kind: EXERCISE_KIND.CHOOSE_IMPLEMENTATION,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['dom-manipulation'],
        instructions:
          'You have a reference to `item` (an `<li>` inside a list) and want to insert `newItem` directly before it. Which is correct?',
        options: [
          'item.parentElement.prepend(newItem)',
          'item.before(newItem)',
          'item.append(newItem)',
          'item.parentElement.append(newItem)',
        ],
        correct: 1,
        hints: [
          'Which method positions a node relative to a sibling rather than inside a parent?',
        ],
        solution: 'item.before(newItem)',
        solutionExplanation:
          '`before()` positions the new node as the immediate previous sibling of `item`, which is exactly the requirement. `prepend` on the parent would put it at the very start of the list regardless of where `item` sits; `item.append(newItem)` would put it *inside* the `<li>`; and `parentElement.append` would put it at the very end. Only `before` is relative to the specific reference node.',
      },
    ],
    quiz: {
      id: 'qz-m18-02',
      questions: [
        {
          id: 'q-m18-02-1',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['dom-manipulation'],
          prompt: 'How does append() differ from appendChild()? (Select all that apply.)',
          options: [
            'append accepts multiple nodes in one call',
            'append accepts plain strings, converting them to text nodes',
            'appendChild returns the appended node; append returns undefined',
            'appendChild can insert into a detached element but append cannot',
          ],
          correct: [0, 1, 2],
          explanation:
            '`append` takes any number of arguments and converts plain strings into text nodes, but returns `undefined`. `appendChild` takes exactly one node and returns it, which occasionally enables a compact one-liner. Both work identically on detached and attached elements.',
        },
        {
          id: 'q-m18-02-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'What happens when you append a node that is already somewhere else in the document?',
          options: [
            'It is copied, leaving the original in place',
            'It is moved — removed from its old position and inserted at the new one',
            'It throws an error',
            'Nothing happens',
          ],
          correct: 1,
          optionExplanations: [
            'Duplication requires cloneNode; appending never copies.',
            'Correct — a node exists in exactly one place in the tree.',
            'Moving is legal and common.',
            'The node genuinely relocates.',
          ],
          explanation:
            'A node can occupy only one position in the document tree, so inserting an already-attached node relocates it automatically, with no explicit removal step. This is convenient for reordering but is a bug when duplication was intended — use `cloneNode` for that.',
        },
        {
          id: 'q-m18-02-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'You have a reference to an element and want to remove it. Which is simplest?',
          options: [
            'element.remove()',
            'element.parentElement.removeChild(element)',
            'element.innerHTML = ""',
            'element.replaceWith()',
          ],
          correct: 0,
          optionExplanations: [
            'Correct — called on the node itself, no parent reference needed.',
            'This works but needlessly requires the parent.',
            'This empties the element but leaves the element itself in place.',
            'This technically works but reads as a replacement, not a removal.',
          ],
          explanation:
            '`remove()` is called directly on the node and needs no parent reference. `removeChild` is the older API still common in existing code, and `innerHTML = ""` clears an element\'s **contents** rather than removing the element itself — a different operation entirely.',
        },
        {
          id: 'q-m18-02-4',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'What is the difference between calling append and calling after?',
          options: [
            'They are identical',
            'append is called on the parent and inserts inside it; after is called on a sibling and inserts next to it',
            'after is called on the parent and inserts inside it',
            'append only works with text',
          ],
          correct: 1,
          optionExplanations: [
            'They insert at completely different positions.',
            'Correct — "inside me" versus "next to me".',
            'This has the two reversed.',
            'append accepts elements and strings alike.',
          ],
          explanation:
            '`append`/`prepend` are parent-relative — "put this inside me, at the end/start". `before`/`after` are sibling-relative — "put this next to me". Calling `list.after(item)` places the item outside the list entirely, which is a common early mistake.',
        },
      ],
    },
    summary:
      'Nodes are inserted as **children** with `append` (end) and `prepend` (start), called on the parent, or as **siblings** with `before` and `after`, called on the reference node — confusing the two puts content outside the container you meant. `append` supersedes `appendChild` in new code: it takes multiple arguments and converts plain strings into text nodes, though `appendChild` returns the appended node where `append` returns `undefined`. `replaceWith` swaps a node for another, and `remove()` takes a node out with no parent reference needed, replacing the older `parent.removeChild(node)`. Inserting a node that is already in the document **moves** it rather than copying it, since a node occupies exactly one position in the tree — excellent for reordering, and a genuine bug when duplication was intended, which needs `cloneNode`. To empty a container, `replaceChildren()` with no arguments or `innerHTML = ""` both work; the latter carries no security concern here because an empty string contains no data to parse.',
    keyTakeaways: [
      'append/prepend are parent-relative; before/after are sibling-relative',
      'append takes multiple nodes and strings; appendChild takes one node and returns it',
      'remove() needs no parent reference, unlike removeChild',
      'Inserting an attached node moves it — a node lives in exactly one place',
      'Duplication requires cloneNode, never repeated appending',
      'replaceChildren() or innerHTML = "" both empty a container',
    ],
    relatedLessons: ['l-m18-01', 'l-m18-03'],
    interviewConnections: [
      'What is the difference between append and appendChild?',
      'What happens if you append an element that is already in the DOM?',
      'How do you remove an element from the page?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m18-03',
    slug: 'rendering-data',
    moduleId: M,
    order: 3,
    title: 'Rendering Data to the Page',
    description: 'Turning an array into a list, re-rendering without duplicates, empty states and conditional rendering.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 20,
    xp: 45,
    topicIds: ['dom-manipulation'],
    prerequisites: ['l-m18-02'],
    learningObjectives: [
      'Render an array of data as DOM elements',
      'Re-render without accidentally duplicating content',
      'Render an empty state when there is no data',
      'Apply conditional rendering in plain JavaScript',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'This is the core skill of the module, and the shape of nearly every real interface: **data in, DOM out**. Everything here combines Module 13\'s array methods with the creation and insertion APIs from the last two lessons.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Rendering an array',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="tasks"></ul>',
        caption: 'map to elements, then insert them all at once.',
        code: [
          'const tasks = [',
          '  { id: 1, title: "Write tests", done: true },',
          '  { id: 2, title: "Review PR", done: false },',
          '  { id: 3, title: "Deploy", done: false },',
          '];',
          '',
          'function taskElement(task) {',
          '  const li = document.createElement("li");',
          '  li.textContent = task.title;',
          '  li.dataset.taskId = String(task.id);',
          '  li.classList.toggle("done", task.done);',
          '  return li;',
          '}',
          '',
          'const list = document.getElementById("tasks");',
          'list.append(...tasks.map(taskElement));',
          '',
          'console.log("rendered :", list.children.length);',
          'console.log("titles   :", [...list.children].map((li) => li.textContent));',
          'console.log("done ones:", list.querySelectorAll(".done").length);',
        ].join('\n'),
        output: [
          'rendered : 3',
          'titles   : ["Write tests", "Review PR", "Deploy"]',
          'done ones: 1',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Two things make this read well. A **named function per item** (`taskElement`) keeps the mapping logic in one obvious place. And spreading the mapped array into `append(...)` inserts every element in a single call, rather than appending inside a loop.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The duplicate-render bug',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Rendering once is easy. The bug appears the second time — when data changes and you render again.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="list"></ul>',
        caption: 'Appending on every render accumulates instead of replacing.',
        code: [
          'const items = ["A", "B"];',
          'const list = document.getElementById("list");',
          '',
          'function renderBroken() {',
          '  for (const text of items) {',
          '    const li = document.createElement("li");',
          '    li.textContent = text;',
          '    list.append(li);',
          '  }',
          '}',
          '',
          'renderBroken();',
          'console.log("after first render :", list.children.length);',
          '',
          'renderBroken();',
          'console.log("after second render:", list.children.length);',
          'console.log("contents:", [...list.children].map((li) => li.textContent));',
        ].join('\n'),
        output: [
          'after first render : 2',
          'after second render: 4',
          'contents: ["A", "B", "A", "B"]',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'The single most common rendering bug in vanilla JavaScript',
        body: [
          'Every list item appears twice, then three times, then four. The cause is that `append` **adds** — it has no opinion about what was already there. A render function that appends without clearing first accumulates on every call.',
          'The fix is to make the render function **idempotent**: calling it twice with the same data must produce the same result as calling it once.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="list"></ul>',
        caption: 'replaceChildren makes the render idempotent in one call.',
        code: [
          'const items = ["A", "B"];',
          'const list = document.getElementById("list");',
          '',
          'function render() {',
          '  const elements = items.map((text) => {',
          '    const li = document.createElement("li");',
          '    li.textContent = text;',
          '    return li;',
          '  });',
          '',
          '  list.replaceChildren(...elements);',
          '}',
          '',
          'render();',
          'console.log("after first render :", list.children.length);',
          '',
          'render();',
          'console.log("after second render:", list.children.length);',
          '',
          'render();',
          'console.log("after third render :", list.children.length);',
          'console.log("contents:", [...list.children].map((li) => li.textContent));',
        ].join('\n'),
        output: [
          'after first render : 2',
          'after second render: 2',
          'after third render : 2',
          'contents: ["A", "B"]',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`replaceChildren(...elements)` removes every existing child and inserts the new ones in a single operation. The render function now produces the same result no matter how many times it runs — which is exactly the property that makes UI code predictable.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Empty states',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<div id="results"></div>',
        caption: 'An empty array should render a message, not nothing at all.',
        code: [
          'const container = document.getElementById("results");',
          '',
          'function renderResults(results) {',
          '  if (results.length === 0) {',
          '    const empty = document.createElement("p");',
          '    empty.className = "empty-state";',
          '    empty.textContent = "No results found. Try a different search.";',
          '    container.replaceChildren(empty);',
          '    return;',
          '  }',
          '',
          '  const items = results.map((r) => {',
          '    const div = document.createElement("div");',
          '    div.className = "result";',
          '    div.textContent = r;',
          '    return div;',
          '  });',
          '',
          '  container.replaceChildren(...items);',
          '}',
          '',
          'renderResults(["Lamp", "Desk"]);',
          'console.log("with data — results:", container.querySelectorAll(".result").length);',
          'console.log("with data — empty  :", container.querySelector(".empty-state") !== null);',
          '',
          'renderResults([]);',
          'console.log("no data   — results:", container.querySelectorAll(".result").length);',
          'console.log("no data   — message:", container.querySelector(".empty-state").textContent);',
        ].join('\n'),
        output: [
          'with data — results: 2',
          'with data — empty  : false',
          'no data   — results: 0',
          'no data   — message: No results found. Try a different search.',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'An empty container is not an empty state',
        body: [
          'Rendering nothing leaves the user staring at blank space, unable to tell whether the app is loading, broken, or genuinely has no data. An explicit empty state answers that question and usually suggests a next action.',
          'Because `replaceChildren` is used in both branches, switching between the two states is automatic — the empty message replaces the results, and the results replace the empty message, with no leftover elements from the previous render.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Conditional rendering',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="users"></ul>',
        caption: 'Conditionally including an element, using filter and a plain if.',
        code: [
          'const users = [',
          '  { name: "Ada", role: "admin", active: true },',
          '  { name: "Grace", role: "user", active: true },',
          '  { name: "Alan", role: "user", active: false },',
          '];',
          '',
          'function userElement(user) {',
          '  const li = document.createElement("li");',
          '',
          '  const name = document.createElement("span");',
          '  name.textContent = user.name;',
          '  li.append(name);',
          '',
          '  // Only admins get a badge — conditional child',
          '  if (user.role === "admin") {',
          '    const badge = document.createElement("span");',
          '    badge.className = "badge";',
          '    badge.textContent = "Admin";',
          '    li.append(badge);',
          '  }',
          '',
          '  return li;',
          '}',
          '',
          '// Conditional inclusion of whole rows — filter before mapping',
          'const list = document.getElementById("users");',
          'list.replaceChildren(...users.filter((u) => u.active).map(userElement));',
          '',
          'console.log("rows    :", list.children.length);',
          'console.log("badges  :", list.querySelectorAll(".badge").length);',
          'console.log("names   :", [...list.children].map((li) => li.firstElementChild.textContent));',
        ].join('\n'),
        output: [
          'rows    : 2',
          'badges  : 1',
          'names   : ["Ada", "Grace"]',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Vanilla JavaScript has no special conditional-rendering syntax — you use the language you already have. **Which rows appear** is decided by `filter` before mapping; **what goes inside a row** is decided by an ordinary `if` inside the element-building function. That is the whole technique.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          '// <ul id="l"></ul>',
          'const l = document.getElementById("l");',
          'const data = ["x"];',
          '',
          'function render() {',
          '  l.append(...data.map(() => document.createElement("li")));',
          '}',
          '',
          'render();',
          'render();',
          '',
          'console.log(l.children.length);',
        ].join('\n'),
        options: ['2', '1', '0', '4'],
        correct: 0,
        explanation:
          '`append` adds to whatever is already present — it never clears first. The array has one item, so each `render()` call appends one new `<li>`, and calling it twice leaves two children even though the data never changed. This is the classic duplicate-render bug. Swapping `append` for `replaceChildren` makes the function idempotent, so any number of renders with the same data produces exactly one item.',
      },
    ],
    exercises: [
      {
        id: 'ex-m18-03-a',
        title: 'Render a user list',
        kind: EXERCISE_KIND.DOM_TASK,
        difficulty: DIFFICULTY.EASY,
        xp: 25,
        topicIds: ['dom-manipulation'],
        instructions:
          'Write `renderUsers(users)` that renders an array of `{ name }` objects as `<li>` elements inside `#list`, one per user, replacing any previous content so repeated calls never duplicate.',
        html: '<ul id="list"></ul>',
        starterCode: [
          'function renderUsers(users) {',
          '  // Build the elements, then replace all children at once.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'renders one item per user', body: 'renderUsers([{ name: "Ada" }, { name: "Grace" }]); expect(document.querySelectorAll("#list li").length).toBe(2);' },
          { name: 'renders the names in order', body: 'renderUsers([{ name: "Ada" }, { name: "Grace" }]); expect([...document.querySelectorAll("#list li")].map((li) => li.textContent)).toEqual(["Ada", "Grace"]);' },
          { name: 'does not duplicate on re-render', body: 'renderUsers([{ name: "Ada" }]); renderUsers([{ name: "Ada" }]); expect(document.querySelectorAll("#list li").length).toBe(1);' },
          { name: 'replaces previous content', body: 'renderUsers([{ name: "Ada" }, { name: "Grace" }]); renderUsers([{ name: "Alan" }]); expect([...document.querySelectorAll("#list li")].map((li) => li.textContent)).toEqual(["Alan"]);' },
          { name: 'handles an empty array', body: 'renderUsers([{ name: "Ada" }]); renderUsers([]); expect(document.querySelectorAll("#list li").length).toBe(0);' },
          { name: 'renders a markup-like name as text', body: 'renderUsers([{ name: "<b>x</b>" }]); expect(document.querySelector("#list li").children.length).toBe(0);', hidden: true },
        ],
        hints: [
          '`users.map(...)` builds the array of elements.',
          '`list.replaceChildren(...elements)` inserts them and clears old content in one step.',
          'Set text with `textContent`.',
        ],
        solution: [
          'function renderUsers(users) {',
          '  const items = users.map((user) => {',
          '    const li = document.createElement("li");',
          '    li.textContent = user.name;',
          '    return li;',
          '  });',
          '',
          '  document.getElementById("list").replaceChildren(...items);',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`replaceChildren` is what makes this idempotent — it clears and inserts in one operation, so the third and fourth tests (re-render, and render with different data) both pass without any manual clearing step. An `append`-based version would have accumulated items on every call. Spreading the mapped array passes each element as a separate argument. The empty-array case needs no special handling: `replaceChildren()` with no arguments simply clears.',
      },
      {
        id: 'ex-m18-03-b',
        title: 'Render with an empty state',
        kind: EXERCISE_KIND.DOM_TASK,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 30,
        topicIds: ['dom-manipulation'],
        instructions:
          'Write `renderProducts(products)` rendering `{ name, price }` objects into `#grid` as `<div class="card">` elements whose text is `"name — £price"` (price formatted to two decimals). When the array is empty, render a single `<p class="empty">` with the text `"No products"` instead.',
        html: '<div id="grid"></div>',
        starterCode: [
          'function renderProducts(products) {',
          '  // Handle the empty case first, then the normal case.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'renders one card per product', body: 'renderProducts([{ name: "Lamp", price: 25 }, { name: "Desk", price: 150 }]); expect(document.querySelectorAll("#grid .card").length).toBe(2);' },
          { name: 'formats the card text', body: 'renderProducts([{ name: "Lamp", price: 25 }]); expect(document.querySelector("#grid .card").textContent).toBe("Lamp — £25.00");' },
          { name: 'shows the empty state for an empty array', body: 'renderProducts([]); expect(document.querySelector("#grid .empty").textContent).toBe("No products");' },
          { name: 'renders no cards in the empty state', body: 'renderProducts([]); expect(document.querySelectorAll("#grid .card").length).toBe(0);' },
          { name: 'switches from empty back to data', body: 'renderProducts([]); renderProducts([{ name: "Lamp", price: 25 }]); expect(document.querySelectorAll("#grid .card").length).toBe(1); expect(document.querySelector("#grid .empty")).toBe(null);' },
          { name: 'switches from data back to empty', body: 'renderProducts([{ name: "Lamp", price: 25 }]); renderProducts([]); expect(document.querySelectorAll("#grid .card").length).toBe(0); expect(document.querySelector("#grid .empty")).not.toBe(null);' },
          { name: 'does not duplicate on re-render', body: 'renderProducts([{ name: "Lamp", price: 25 }]); renderProducts([{ name: "Lamp", price: 25 }]); expect(document.querySelectorAll("#grid .card").length).toBe(1);', hidden: true },
        ],
        hints: [
          'Return early after rendering the empty state.',
          '`price.toFixed(2)` gives the two-decimal format.',
          'Use `replaceChildren` in both branches so the states swap cleanly.',
        ],
        solution: [
          'function renderProducts(products) {',
          '  const grid = document.getElementById("grid");',
          '',
          '  if (products.length === 0) {',
          '    const empty = document.createElement("p");',
          '    empty.className = "empty";',
          '    empty.textContent = "No products";',
          '    grid.replaceChildren(empty);',
          '    return;',
          '  }',
          '',
          '  const cards = products.map((product) => {',
          '    const card = document.createElement("div");',
          '    card.className = "card";',
          '    card.textContent = `${product.name} — £${product.price.toFixed(2)}`;',
          '    return card;',
          '  });',
          '',
          '  grid.replaceChildren(...cards);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Using `replaceChildren` in **both** branches is what makes the two state-switching tests pass: the empty message replaces the cards and the cards replace the empty message, with nothing left over from the previous render. An early `return` after the empty branch keeps the main path unindented. `toFixed(2)` produces the required `"25.00"` formatting, and building the text as one `textContent` assignment keeps it safe for any product name.',
      },
      {
        id: 'ex-m18-03-c',
        title: 'Fix the duplicate render',
        kind: EXERCISE_KIND.DOM_TASK,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['dom-manipulation'],
        instructions:
          'This render function duplicates its output every time it is called. Fix it so repeated calls with the same data always produce the same result.',
        html: '<ul id="notifications"></ul>',
        starterCode: [
          'function renderNotifications(notes) {',
          '  const list = document.getElementById("notifications");',
          '',
          '  for (const note of notes) {',
          '    const li = document.createElement("li");',
          '    li.textContent = note;',
          '    list.append(li);',
          '  }',
          '}',
        ].join('\n'),
        tests: [
          { name: 'renders the notifications', body: 'renderNotifications(["A", "B"]); expect(document.querySelectorAll("#notifications li").length).toBe(2);' },
          { name: 'does not duplicate on the second call', body: 'renderNotifications(["A", "B"]); renderNotifications(["A", "B"]); expect(document.querySelectorAll("#notifications li").length).toBe(2);' },
          { name: 'stays stable across many calls', body: 'renderNotifications(["A"]); renderNotifications(["A"]); renderNotifications(["A"]); expect(document.querySelectorAll("#notifications li").length).toBe(1);' },
          { name: 'reflects new data rather than adding to old', body: 'renderNotifications(["A", "B"]); renderNotifications(["C"]); expect([...document.querySelectorAll("#notifications li")].map((li) => li.textContent)).toEqual(["C"]);' },
          { name: 'clears fully for an empty array', body: 'renderNotifications(["A"]); renderNotifications([]); expect(document.querySelectorAll("#notifications li").length).toBe(0);', hidden: true },
        ],
        hints: [
          '`append` adds to what is already there — it never clears.',
          'Build all the elements first, then use `replaceChildren`.',
        ],
        solution: [
          'function renderNotifications(notes) {',
          '  const list = document.getElementById("notifications");',
          '',
          '  const items = notes.map((note) => {',
          '    const li = document.createElement("li");',
          '    li.textContent = note;',
          '    return li;',
          '  });',
          '',
          '  list.replaceChildren(...items);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The original appended into whatever was already in the list, so every call added another full copy. Building the elements with `map` and then calling `replaceChildren` makes the function **idempotent**: its result depends only on the data passed in, never on how many times it has run before. The final hidden test confirms this holds for the empty case too — `replaceChildren()` with no arguments clears the list rather than leaving stale items behind.',
      },
      {
        id: 'ex-m18-03-d',
        title: 'Why did the list duplicate?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['dom-manipulation'],
        instructions:
          'A render function calls `container.append(...)` each time data changes, and the list grows every render. What is the underlying cause?',
        options: [
          'append is asynchronous and runs twice',
          'append adds to existing content — the function never clears what was rendered previously',
          'The data array is being mutated',
          'createElement caches elements between calls',
        ],
        correct: 1,
        hints: [
          'Does append have any opinion about what is already in the container?',
        ],
        solution: 'append adds to existing content — the function never clears what was rendered previously',
        solutionExplanation:
          '`append` is purely additive: it inserts at the end and does nothing about existing children. A render function that appends without clearing therefore accumulates a fresh copy on every call. The fix is to make the function idempotent, most simply with `replaceChildren(...elements)`, which clears and inserts in one operation so the outcome depends only on the current data.',
      },
    ],
    quiz: {
      id: 'qz-m18-03',
      questions: [
        {
          id: 'q-m18-03-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'What makes a render function idempotent?',
          options: [
            'It runs only once',
            'Calling it repeatedly with the same data produces the same result as calling it once',
            'It uses createElement rather than innerHTML',
            'It returns the elements it created',
          ],
          correct: 1,
          optionExplanations: [
            'Idempotence is about repeatable results, not a single execution.',
            'Correct — the outcome depends only on the data, not the call count.',
            'The construction method is unrelated to idempotence.',
            'The return value is irrelevant to this property.',
          ],
          explanation:
            'An idempotent render produces output determined solely by its input data. `replaceChildren(...elements)` achieves this by clearing and inserting in one step, whereas `append` accumulates and makes the result depend on how many times the function has run.',
        },
        {
          id: 'q-m18-03-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'Why render an explicit empty state rather than simply rendering nothing?',
          options: [
            'It is required by the DOM specification',
            'A blank container leaves the user unable to tell whether the app is loading, broken, or genuinely has no data',
            'It makes rendering faster',
            'Empty containers cause layout errors',
          ],
          correct: 1,
          optionExplanations: [
            'Nothing in the specification requires it; it is a UX decision.',
            'Correct — an empty state communicates and usually suggests a next action.',
            'Performance is unrelated.',
            'An empty container is perfectly valid markup.',
          ],
          explanation:
            'Rendering nothing is ambiguous to the user. An explicit empty state says "there genuinely is no data here" and typically offers a next step, such as adjusting a search.',
        },
        {
          id: 'q-m18-03-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'How is conditional rendering done in vanilla JavaScript?',
          options: [
            'With a special DOM conditional API',
            'With ordinary language features — filter to decide which items render, and if statements to decide what goes inside each one',
            'By setting elements to display: none only',
            'It is not possible without a framework',
          ],
          correct: 1,
          optionExplanations: [
            'No such dedicated API exists.',
            'Correct — you use the JavaScript you already know.',
            'Hiding with CSS is one technique but not the general answer.',
            'It is entirely possible with plain DOM APIs.',
          ],
          explanation:
            'There is no dedicated conditional-rendering syntax. `filter` before mapping decides which rows exist at all, and a plain `if` inside the element-building function decides what each row contains.',
        },
        {
          id: 'q-m18-03-4',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['dom-manipulation'],
          prompt: 'Given an empty `<ul id="l">`, what is the final child count?',
          code: [
            'const l = document.getElementById("l");',
            'const render = () => l.replaceChildren(document.createElement("li"));',
            'render();',
            'render();',
            'render();',
            'console.log(l.children.length);',
          ].join('\n'),
          options: ['3', '1', '0', '2'],
          correct: 1,
          optionExplanations: [
            'That would be the result with append, not replaceChildren.',
            'Correct — each call replaces all children, so one remains.',
            'Each call inserts one element.',
            'The count does not accumulate at all.',
          ],
          explanation:
            '`replaceChildren` removes every existing child before inserting the new one, so the result is identical regardless of how many times the function runs — exactly the idempotence that prevents the duplicate-render bug.',
        },
      ],
    },
    summary:
      'Rendering data is **data in, DOM out**: a named element-building function per item, `map` to turn the array into elements, and a single insertion call. The defining bug is the **duplicate render** — `append` is purely additive and has no opinion about existing children, so a render function that appends accumulates a fresh copy on every call. The fix is to make the function **idempotent**, most directly with `replaceChildren(...elements)`, which clears and inserts in one operation so the output depends only on the current data and never on the call count. An empty array should produce an explicit **empty state** rather than a blank container, since blankness cannot tell the user whether the app is loading, broken, or genuinely has no data — and using `replaceChildren` in both branches makes the two states swap cleanly with nothing left over. Conditional rendering needs no special API: `filter` before mapping decides which rows exist, and an ordinary `if` inside the builder decides what each row contains.',
    keyTakeaways: [
      'A named element-builder per item plus map keeps rendering readable',
      'append accumulates — the classic duplicate-render bug on re-render',
      'replaceChildren(...elements) makes a render idempotent in one call',
      'Render an explicit empty state, never a blank container',
      'Use replaceChildren in every branch so states swap cleanly',
      'Conditional rendering is just filter and if — no special API',
    ],
    relatedLessons: ['l-m18-02', 'l-m18-04'],
    interviewConnections: [
      'How would you render an array of data to the page?',
      'Why does a list duplicate when the render function runs twice?',
      'What does it mean for a render function to be idempotent?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m18-04',
    slug: 'fragments-and-cloning',
    moduleId: M,
    order: 4,
    title: 'DocumentFragment and Cloning',
    description: 'Assembling nodes off-document, and duplicating an existing structure with cloneNode.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 16,
    xp: 35,
    topicIds: ['dom-manipulation'],
    prerequisites: ['l-m18-03'],
    learningObjectives: [
      'Build a group of nodes in a DocumentFragment before inserting',
      'Explain that a fragment\'s children move into the document, leaving no wrapper',
      'Duplicate a node with cloneNode, shallow or deep',
      'Judge when a fragment genuinely helps rather than assuming it always does',
    ],
    sections: [
      {
        kind: SECTION.HEADING,
        text: 'DocumentFragment',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'A `DocumentFragment` is a lightweight, **document-less** container. You can append nodes into it exactly as you would into an element, but it is not part of the page and renders nothing itself.',
          'Its defining behaviour: when you insert a fragment into the document, its **children move in and the fragment itself does not**. It leaves no wrapper element behind.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="list"></ul>',
        caption: 'The fragment vanishes; only its children arrive.',
        code: [
          'const fragment = document.createDocumentFragment();',
          '',
          'for (const text of ["A", "B", "C"]) {',
          '  const li = document.createElement("li");',
          '  li.textContent = text;',
          '  fragment.append(li);',
          '}',
          '',
          'console.log("fragment children before:", fragment.children.length);',
          '',
          'const list = document.getElementById("list");',
          'list.append(fragment);',
          '',
          'console.log("list children  :", list.children.length);',
          'console.log("list contents  :", [...list.children].map((li) => li.textContent));',
          'console.log("fragment after :", fragment.children.length);',
          '',
          '// No wrapper element was added',
          'console.log("first child tag:", list.firstElementChild.tagName);',
        ].join('\n'),
        output: [
          'fragment children before: 3',
          'list children  : 3',
          'list contents  : ["A", "B", "C"]',
          'fragment after : 0',
          'first child tag: LI',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'The fragment is emptied, not inserted',
        body: [
          'After the insertion the fragment has **zero** children — they were moved out, following the same "a node lives in one place" rule from lesson 2. And the list\'s first child is an `<li>`, not a fragment or a wrapper `<div>`.',
          'That "no wrapper" property is what makes fragments useful for grouping: you can hand around a collection of sibling nodes as one value without polluting the final markup with a container element you did not want.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'When a fragment actually helps',
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Do not repeat the old "dramatically faster" claim',
        body: [
          'You will find many older articles claiming a `DocumentFragment` is a dramatic performance optimisation, because each insertion into a live document used to risk triggering layout work. Modern browsers batch DOM changes far more aggressively, and the difference for typical list sizes is usually negligible.',
          'The honest framing: a fragment is a **construction and grouping tool**. It lets you build a set of siblings before they are in the document, and pass them around as a single value. Any performance benefit is a bonus you should measure, not assume.',
        ],
      },
      {
        kind: SECTION.PROSE,
        body: [
          'In practice, `replaceChildren(...elements)` from lesson 3 already inserts an array of elements in one operation, so it covers most rendering needs without a fragment at all. Fragments earn their place when a function needs to **return** a group of siblings, since a plain array is not a node and cannot be inserted directly by every API.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<div id="host"></div>',
        caption: 'Returning several sibling nodes as one value.',
        code: [
          'function buildRows(items) {',
          '  const fragment = document.createDocumentFragment();',
          '',
          '  for (const item of items) {',
          '    const row = document.createElement("p");',
          '    row.textContent = item;',
          '    fragment.append(row);',
          '  }',
          '',
          '  return fragment;',
          '}',
          '',
          'const app = document.getElementById("host");',
          'app.append(buildRows(["one", "two"]));',
          '',
          'console.log("children:", app.children.length);',
          'console.log("tags    :", [...app.children].map((el) => el.tagName));',
        ].join('\n'),
        output: [
          'children: 2',
          'tags    : ["P", "P"]',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'cloneNode',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Lesson 2 established that inserting an existing node **moves** it. `cloneNode` is how you genuinely duplicate one. It takes a boolean: `false` for a **shallow** clone (the element alone), `true` for a **deep** clone (the element and all its descendants).',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<div id="card" class="card" data-id="7"><h3>Title</h3><p>Body</p></div>',
        caption: 'Shallow versus deep, and what carries across.',
        code: [
          'const card = document.getElementById("card");',
          '',
          'const shallow = card.cloneNode(false);',
          'const deep = card.cloneNode(true);',
          '',
          'console.log("original children:", card.children.length);',
          'console.log("shallow children :", shallow.children.length);',
          'console.log("deep children    :", deep.children.length);',
          '',
          '// Attributes and classes are copied by both',
          'console.log("deep class :", deep.className);',
          'console.log("deep data  :", deep.dataset.id);',
          '',
          '// The clone is a separate, detached element',
          'console.log("same object:", deep === card);',
          'console.log("detached   :", deep.parentElement === null);',
        ].join('\n'),
        output: [
          'original children: 2',
          'shallow children : 0',
          'deep children    : 2',
          'deep class : card',
          'deep data  : 7',
          'same object: false',
          'detached   : true',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'A clone duplicates markup, not behaviour',
        body: [
          '`cloneNode(true)` copies the element, its attributes, its classes and all its descendants. It does **not** copy event listeners added with `addEventListener`, and it does not copy JavaScript state you attached to the original.',
          'That surprises people who clone a working button and find the copy does nothing. Module 19 covers event delegation, which handles this cleanly by listening on a stable parent rather than on each cloned child.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<div id="host"><div id="template" class="card"><h3>Name</h3><p class="role">Role</p></div></div>',
        caption: 'A practical pattern: clone a structure, then fill in the data.',
        code: [
          'const template = document.getElementById("template");',
          'const app = document.getElementById("host");',
          '',
          'const people = [',
          '  { name: "Ada", role: "Engineer" },',
          '  { name: "Grace", role: "Admiral" },',
          '];',
          '',
          'for (const person of people) {',
          '  const copy = template.cloneNode(true);',
          '  copy.removeAttribute("id");',
          '  copy.querySelector("h3").textContent = person.name;',
          '  copy.querySelector(".role").textContent = person.role;',
          '  app.append(copy);',
          '}',
          '',
          'console.log("cards in app:", app.querySelectorAll(".card").length);',
          'console.log("names       :", [...app.querySelectorAll(".card h3")].map((h) => h.textContent));',
        ].join('\n'),
        output: [
          'cards in app: 3',
          'names       : ["Name", "Ada", "Grace"]',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Note `removeAttribute("id")` on each copy. Cloning copies the `id` too, and duplicate ids are invalid — `getElementById` would only ever find the first. Stripping the id from clones is essential whenever the source carries one.',
          'The original template is still in the page, which is why three cards are counted rather than two. A real implementation would either hide the template with CSS or use the `<template>` element, whose contents are not rendered at all.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          '// <div id="box"><span>hi</span></div>',
          'const box = document.getElementById("box");',
          'const copy = box.cloneNode(false);',
          '',
          'console.log(copy.children.length, copy.id, copy === box);',
        ].join('\n'),
        options: [
          '0 "box" false',
          '1 "box" false',
          '0 "" false',
          '1 "box" true',
        ],
        correct: 0,
        explanation:
          '`cloneNode(false)` is a **shallow** clone: it copies the element itself along with its attributes — including the `id`, which is why `copy.id` is still `"box"` — but none of its descendants, so `children.length` is `0`. The clone is a genuinely separate object, so `copy === box` is `false`. The duplicated `id` is exactly why cloned elements usually need `removeAttribute("id")` before insertion, since duplicate ids are invalid and break `getElementById`.',
      },
    ],
    exercises: [
      {
        id: 'ex-m18-04-a',
        title: 'Predict the fragment insertion',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['dom-manipulation'],
        instructions:
          'Given an empty `<ul id="l">`, what does this print?',
        code: [
          'const f = document.createDocumentFragment();',
          'f.append(document.createElement("li"), document.createElement("li"));',
          'document.getElementById("l").append(f);',
          'console.log(document.getElementById("l").children.length, f.children.length);',
        ].join('\n'),
        options: ['2 0', '2 2', '1 2', '0 2'],
        correct: 0,
        hints: [
          'Does the fragment itself get inserted, or only its children?',
          'What happens to a node when it is moved somewhere else?',
        ],
        solution: '2 0',
        solutionExplanation:
          'Inserting a fragment moves its **children** into the target and leaves the fragment itself out of the document entirely — no wrapper element is added. Because the children were moved rather than copied (the same one-node-one-place rule as any other insertion), the fragment is left empty afterwards, so its child count drops to `0` while the list gains both items.',
      },
      {
        id: 'ex-m18-04-b',
        title: 'Build rows with a fragment',
        kind: EXERCISE_KIND.DOM_TASK,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['dom-manipulation'],
        instructions:
          'Write `buildOptions(values)` returning a `DocumentFragment` containing one `<option>` per value, where each option\'s text and `value` attribute are both set to that value. Do not insert it anywhere.',
        html: '<select id="picker"></select>',
        starterCode: [
          'function buildOptions(values) {',
          '  // createDocumentFragment, append into it, return it.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'returns a fragment with one option per value', body: 'expect(buildOptions(["a", "b"]).children.length).toBe(2);' },
          { name: 'creates option elements', body: 'expect(buildOptions(["a"]).children[0].tagName).toBe("OPTION");' },
          { name: 'sets the text', body: 'expect(buildOptions(["Apples"]).children[0].textContent).toBe("Apples");' },
          { name: 'sets the value attribute', body: 'expect(buildOptions(["Apples"]).children[0].value).toBe("Apples");' },
          { name: 'handles an empty array', body: 'expect(buildOptions([]).children.length).toBe(0);' },
          { name: 'inserts its children when appended', body: 'const p = document.getElementById("picker"); p.append(buildOptions(["x", "y"])); expect(p.querySelectorAll("option").length).toBe(2);', hidden: true },
        ],
        hints: [
          '`document.createDocumentFragment()` creates it.',
          'Set both `textContent` and `value` on each option.',
          'Return the fragment itself, not an array.',
        ],
        solution: [
          'function buildOptions(values) {',
          '  const fragment = document.createDocumentFragment();',
          '',
          '  for (const value of values) {',
          '    const option = document.createElement("option");',
          '    option.textContent = value;',
          '    option.value = value;',
          '    fragment.append(option);',
          '  }',
          '',
          '  return fragment;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'This is the case where a fragment genuinely earns its place: the function returns a **group of siblings as a single node**, which the caller can insert with one `append` — a plain array could not be passed to every insertion API directly. Setting `option.value` uses the property rather than `setAttribute`, which is the idiomatic choice for form controls per Module 17. The hidden test confirms the fragment\'s children move into the select on insertion, with no wrapper element added.',
      },
      {
        id: 'ex-m18-04-c',
        title: 'Clone a card template',
        kind: EXERCISE_KIND.DOM_TASK,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 30,
        topicIds: ['dom-manipulation'],
        instructions:
          'Write `makeCardFrom(name)` that deep-clones `#template`, removes the clone\'s `id`, sets its `<h3>` text to `name`, and returns the clone without inserting it.',
        html: '<div id="host"><div id="template" class="card"><h3>Sample heading</h3><p class="meta">Member</p></div></div>',
        starterCode: [
          'function makeCardFrom(name) {',
          '  // Deep clone, strip the id, fill in the name.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'returns a clone with the card class', body: 'expect(makeCardFrom("Ada").classList.contains("card")).toBe(true);' },
          { name: 'copies the descendants', body: 'expect(makeCardFrom("Ada").children.length).toBe(2);' },
          { name: 'sets the heading text', body: 'expect(makeCardFrom("Ada").querySelector("h3").textContent).toBe("Ada");' },
          { name: 'removes the id from the clone', body: 'expect(makeCardFrom("Ada").id).toBe("");' },
          { name: 'leaves the original template untouched', body: 'makeCardFrom("Ada"); expect(document.getElementById("template").querySelector("h3").textContent).toBe("Sample heading");' },
          { name: 'is a different element from the template', body: 'expect(makeCardFrom("Ada") === document.getElementById("template")).toBe(false);' },
          { name: 'preserves other descendants', body: 'expect(makeCardFrom("Ada").querySelector(".meta").textContent).toBe("Member");', hidden: true },
        ],
        hints: [
          '`cloneNode(true)` copies descendants; `cloneNode(false)` does not.',
          '`removeAttribute("id")` prevents a duplicate id in the document.',
          'Query inside the clone, not the document, when setting the heading.',
        ],
        solution: [
          'function makeCardFrom(name) {',
          '  const clone = document.getElementById("template").cloneNode(true);',
          '  clone.removeAttribute("id");',
          '  clone.querySelector("h3").textContent = name;',
          '',
          '  return clone;',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`cloneNode(true)` is required — a shallow clone would copy the card but none of its children, failing the descendants test. Removing the `id` matters because cloning copies attributes including `id`, and duplicate ids are invalid: `getElementById` would only ever find the first. Querying with `clone.querySelector("h3")` rather than `document.querySelector` is what keeps the original template untouched, which the fifth test verifies directly.',
      },
      {
        id: 'ex-m18-04-d',
        title: 'Does a clone keep its listeners?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['dom-manipulation'],
        instructions:
          'You add a click listener to a button with `addEventListener`, then deep-clone the button and insert the copy. Does clicking the copy trigger the handler?',
        options: [
          'Yes — cloneNode copies everything about the element',
          'No — cloneNode copies markup, attributes and descendants, but not listeners added with addEventListener',
          'Only if you pass true to cloneNode',
          'Only in older browsers',
        ],
        correct: 1,
        hints: [
          'Is a listener part of the element\'s markup, or attached separately in JavaScript?',
        ],
        solution: 'No — cloneNode copies markup, attributes and descendants, but not listeners added with addEventListener',
        solutionExplanation:
          'A listener registered with `addEventListener` is not part of the element\'s markup — it is a separate JavaScript registration — so it is not duplicated by cloning. `cloneNode(true)` faithfully copies the element, its attributes, classes and all descendants, and the copy still does nothing when clicked. This is a common surprise when duplicating a working control. Module 19\'s event delegation solves it cleanly by attaching one listener to a stable parent, so cloned and dynamically-created children are handled automatically.',
      },
    ],
    quiz: {
      id: 'qz-m18-04',
      questions: [
        {
          id: 'q-m18-04-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'What happens to a DocumentFragment when you insert it into the document?',
          options: [
            'The fragment is inserted as a wrapper element containing its children',
            'Its children move into the document and the fragment itself is not inserted, leaving no wrapper',
            'The fragment is copied, keeping its children too',
            'It throws unless the fragment is empty',
          ],
          correct: 1,
          optionExplanations: [
            'No wrapper element appears in the resulting markup.',
            'Correct — children move in, fragment stays out and is left empty.',
            'The children are moved, not copied, so the fragment ends up empty.',
            'Inserting a populated fragment is the normal use.',
          ],
          explanation:
            'Inserting a fragment moves its children into the target and discards the fragment itself, adding no wrapper to the markup. Because the children move rather than copy, the fragment is empty afterwards.',
        },
        {
          id: 'q-m18-04-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'What is the difference between cloneNode(true) and cloneNode(false)?',
          options: [
            'true copies event listeners as well',
            'true copies the element and all its descendants; false copies only the element itself',
            'true inserts the clone into the document automatically',
            'false creates a reference rather than a copy',
          ],
          correct: 1,
          optionExplanations: [
            'Neither copies listeners added with addEventListener.',
            'Correct — deep versus shallow.',
            'Both produce a detached clone you must insert yourself.',
            'Both produce genuine, separate copies.',
          ],
          explanation:
            '`cloneNode(true)` is a deep clone including all descendants; `cloneNode(false)` is shallow, copying only the element and its attributes. Neither copies listeners registered with `addEventListener`, and both return a detached element.',
        },
        {
          id: 'q-m18-04-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'Why must you usually remove the id from a cloned element?',
          options: [
            'Cloning corrupts the id',
            'Because cloning copies the id, and duplicate ids are invalid — getElementById would only find the first',
            'Because clones cannot have attributes',
            'You do not — ids are never copied',
          ],
          correct: 1,
          optionExplanations: [
            'The id is copied intact, not corrupted.',
            'Correct — duplicate ids break lookups and are invalid markup.',
            'Clones keep all attributes, including id.',
            'Ids are copied along with every other attribute.',
          ],
          explanation:
            'Cloning copies every attribute, including `id`. Inserting the clone would then create a duplicate id, which is invalid and means `getElementById` can only ever return the first match — so `removeAttribute("id")` on the clone is standard practice.',
        },
        {
          id: 'q-m18-04-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['dom-manipulation'],
          prompt: 'Using a DocumentFragment is always dramatically faster than inserting elements individually.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'This is an outdated claim repeated from older articles.',
            'Correct — treat it as a construction and grouping tool, and measure before claiming a speed benefit.',
          ],
          explanation:
            'Modern browsers batch DOM changes aggressively, so for typical list sizes the difference is usually negligible. A fragment is best understood as a construction and grouping tool — particularly for returning a set of siblings as one value — with any performance benefit something to measure rather than assume.',
        },
      ],
    },
    summary:
      'A `DocumentFragment` is a document-less container you can append into like an element. Inserting it moves its **children** into the target and leaves the fragment itself out, adding **no wrapper** to the markup — and because the children move rather than copy, the fragment is empty afterwards. Its genuine value is construction and grouping, especially for a function that must **return a set of siblings as one node**; the old claim that it is always dramatically faster is outdated, since modern browsers batch DOM changes aggressively, and `replaceChildren(...elements)` already covers most rendering needs. `cloneNode(false)` copies an element and its attributes alone; `cloneNode(true)` copies all descendants too. Both return a **detached** copy and neither duplicates listeners added with `addEventListener` — a clone reproduces markup, not behaviour, which is a common surprise when duplicating a working control. Cloning also copies the `id`, so `removeAttribute("id")` is standard before inserting a clone, since duplicate ids are invalid and break `getElementById`.',
    keyTakeaways: [
      'A fragment\'s children move into the document; the fragment leaves no wrapper',
      'After insertion the fragment is empty — its children were moved, not copied',
      'Fragments are a construction/grouping tool; do not assume a big speed win',
      'cloneNode(true) is deep, cloneNode(false) is shallow — both return detached copies',
      'Clones do not carry addEventListener listeners — markup is copied, behaviour is not',
      'Strip the id from a clone before inserting it, or you create a duplicate id',
    ],
    relatedLessons: ['l-m18-03', 'l-m18-05'],
    interviewConnections: [
      'What is a DocumentFragment and what happens when you insert one?',
      'What is the difference between a shallow and a deep cloneNode?',
      'Does cloneNode copy event listeners?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m18-05',
    slug: 'traversing-the-dom',
    moduleId: M,
    order: 5,
    title: 'Traversing the DOM',
    description: 'Moving between parents, children and siblings — and why the Element-oriented APIs are usually the right ones.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 17,
    xp: 40,
    topicIds: ['dom-manipulation', 'dom'],
    prerequisites: ['l-m18-04'],
    learningObjectives: [
      'Move to a parent, children and siblings with the Element APIs',
      'Explain why the Node APIs return unexpected results for UI work',
      'Choose the right traversal property for a given task',
      'Combine traversal with the selection methods from Module 17',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Selection finds an element from anywhere. **Traversal** moves from an element you already have to one nearby — which is what event handlers do constantly: "given the button that was clicked, find the row it belongs to."',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The Element-oriented properties',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="list"><li id="first">A</li><li id="middle">B</li><li id="last">C</li></ul>',
        caption: 'Parent, children and siblings.',
        code: [
          'const middle = document.getElementById("middle");',
          'const list = document.getElementById("list");',
          '',
          'console.log("parent        :", middle.parentElement.id);',
          'console.log("previous      :", middle.previousElementSibling.textContent);',
          'console.log("next          :", middle.nextElementSibling.textContent);',
          '',
          'console.log("children count:", list.children.length);',
          'console.log("first child   :", list.firstElementChild.textContent);',
          'console.log("last child    :", list.lastElementChild.textContent);',
          '',
          '// At the ends, sibling properties are null',
          'console.log("before first  :", document.getElementById("first").previousElementSibling);',
          'console.log("after last    :", document.getElementById("last").nextElementSibling);',
        ].join('\n'),
        output: [
          'parent        : list',
          'previous      : A',
          'next          : C',
          'children count: 3',
          'first child   : A',
          'last child    : C',
          'before first  : null',
          'after last    : null',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Traversal at an edge returns null',
        body: [
          'There is nothing before the first child or after the last, so those properties are `null` — the same "not found" convention as a failed `querySelector`. Chaining off one without a guard throws, so `item.nextElementSibling?.textContent` is the safe form when you are not certain an adjacent element exists.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Why the Node APIs surprise people',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Every Element property above has an older **Node** counterpart: `parentNode`, `childNodes`, `firstChild`, `nextSibling`. These see *every* node, including the text nodes created by ordinary whitespace and line breaks in your HTML.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="list">\n  <li>A</li>\n  <li>B</li>\n</ul>',
        caption: 'The same list, counted two ways — the markup here is indented across lines.',
        code: [
          'const list = document.getElementById("list");',
          '',
          'console.log("children (elements):", list.children.length);',
          'console.log("childNodes (all)   :", list.childNodes.length);',
          '',
          'console.log("firstElementChild  :", list.firstElementChild.tagName);',
          'console.log("firstChild type    :", list.firstChild.nodeType);',
          'console.log("firstChild is text :", list.firstChild.nodeType === Node.TEXT_NODE);',
        ].join('\n'),
        output: [
          'children (elements): 2',
          'childNodes (all)   : 5',
          'firstElementChild  : LI',
          'firstChild type    : 3',
          'firstChild is text : true',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'This is the whole reason to prefer the Element APIs',
        body: [
          'The list has two `<li>` elements but **five** child nodes: the whitespace before the first `<li>`, the two elements, and the whitespace between and after them. `firstChild` is a text node containing a newline and spaces — not the `<li>` you almost certainly wanted.',
          'Reformatting your HTML would change these counts, which makes any UI logic built on `childNodes` or `firstChild` fragile in a way that has nothing to do with your actual data. Use `children`, `firstElementChild` and the rest unless you specifically need to work with text nodes.',
        ],
      },
      {
        kind: SECTION.TABLE,
        headers: ['Element API (prefer)', 'Node API', 'Difference'],
        rows: [
          ['`parentElement`', '`parentNode`', 'Nearly always identical; `parentNode` can return a non-element such as the document'],
          ['`children`', '`childNodes`', '`childNodes` includes text and comment nodes'],
          ['`firstElementChild`', '`firstChild`', '`firstChild` is often a whitespace text node'],
          ['`lastElementChild`', '`lastChild`', 'Same whitespace problem at the other end'],
          ['`nextElementSibling`', '`nextSibling`', '`nextSibling` is often whitespace between elements'],
          ['`previousElementSibling`', '`previousSibling`', 'Same whitespace problem'],
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'closest — searching upward',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="list"><li class="row" data-id="7"><div class="content"><button class="delete"><span class="icon">x</span></button></div></li></ul>',
        caption: 'closest walks up the ancestors until a selector matches.',
        code: [
          'const icon = document.querySelector(".icon");',
          '',
          '// Walk upward until something matches the selector',
          'console.log("nearest button:", icon.closest("button").className);',
          'console.log("nearest row   :", icon.closest(".row").dataset.id);',
          'console.log("nearest list  :", icon.closest("ul").id);',
          '',
          '// closest checks the element ITSELF first',
          'console.log("self match    :", icon.closest(".icon") === icon);',
          '',
          '// No matching ancestor gives null',
          'console.log("no match      :", icon.closest(".nonexistent"));',
        ].join('\n'),
        output: [
          'nearest button: delete',
          'nearest row   : 7',
          'nearest list  : list',
          'self match    : true',
          'no match      : null',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`closest` is the single most useful traversal method in real code, because it is robust to nesting. Given a click on the tiny `<span class="icon">` inside a button inside a row, one call gets you the row — without knowing or caring how many wrapper elements sit in between. Module 19 uses it constantly for event delegation.',
          'Note that `closest` tests the element itself before walking upward, which is why `icon.closest(".icon")` returns the icon.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'matches and contains',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<div id="outer"><button id="btn" class="danger" data-action="delete">Go</button></div><div id="elsewhere"></div>',
        caption: 'Testing an element against a selector, and testing ancestry.',
        code: [
          'const btn = document.getElementById("btn");',
          'const outer = document.getElementById("outer");',
          'const elsewhere = document.getElementById("elsewhere");',
          '',
          '// matches: does THIS element match the selector?',
          'console.log("matches .danger      :", btn.matches(".danger"));',
          'console.log("matches [data-action]:", btn.matches("[data-action=\'delete\']"));',
          'console.log("matches .safe        :", btn.matches(".safe"));',
          '',
          '// contains: is the argument inside this element?',
          'console.log("outer contains btn   :", outer.contains(btn));',
          'console.log("elsewhere contains   :", elsewhere.contains(btn));',
          '',
          '// contains counts the element itself as contained',
          'console.log("outer contains outer :", outer.contains(outer));',
        ].join('\n'),
        output: [
          'matches .danger      : true',
          'matches [data-action]: true',
          'matches .safe        : false',
          'outer contains btn   : true',
          'elsewhere contains   : false',
          'outer contains outer : true',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'The three work together',
        body: [
          '`matches` answers "is this the thing?", `closest` answers "which ancestor is the thing?", and `contains` answers "is this inside that?". In an event handler you typically use `closest` to find the meaningful element, then `contains` to confirm it belongs to the container you are responsible for — the exact guard pattern Module 19 builds on.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          '// <ul id="l">\\n  <li>A</li>\\n</ul>  (indented markup)',
          'const l = document.getElementById("l");',
          '',
          'console.log(l.children.length, l.childNodes.length);',
        ].join('\n'),
        options: [
          '1 3',
          '1 1',
          '3 3',
          '1 2',
        ],
        correct: 0,
        explanation:
          'The markup is indented across lines, so the whitespace before the `<li>` and the whitespace after it each become a **text node**. `children` counts elements only and reports `1`. `childNodes` counts every node — whitespace text node, the `<li>` element, whitespace text node — and reports `3`. Reformatting the HTML would change the `childNodes` count without changing the data, which is exactly why UI logic should use the Element-oriented properties.',
      },
    ],
    exercises: [
      {
        id: 'ex-m18-05-a',
        title: 'Predict the node count',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['dom-manipulation'],
        instructions:
          'For `<div id="d"><span>x</span></div>` written with no whitespace between the tags, what does this print?',
        code: [
          'const d = document.getElementById("d");',
          'console.log(d.children.length, d.childNodes.length);',
        ].join('\n'),
        options: ['1 1', '1 3', '1 2', '2 2'],
        correct: 0,
        hints: [
          'Is there any whitespace between the div and span tags here?',
        ],
        solution: '1 1',
        solutionExplanation:
          'With the tags written adjacent and no whitespace between them, no text nodes are created — so `children` and `childNodes` agree at `1`. That agreement is precisely what makes the whitespace problem so easy to miss: the two properties give identical answers for compact markup and diverge the moment someone reformats the HTML with indentation, which is why UI code should not depend on `childNodes`.',
      },
      {
        id: 'ex-m18-05-b',
        title: 'Find the containing row',
        kind: EXERCISE_KIND.DOM_TASK,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['dom-manipulation'],
        instructions:
          'Write `rowIdFor(element)` returning the `data-id` of the nearest ancestor (or the element itself) with the class `row`, or `null` if there is no such ancestor.',
        html: '<ul><li class="row" data-id="7"><div><button><span id="icon">x</span></button></div></li><li class="row" data-id="9"><span id="plain">y</span></li></ul><div id="outside"></div>',
        starterCode: [
          'function rowIdFor(element) {',
          '  // One method walks up the ancestors for you.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'finds the row from a deeply nested element', body: 'expect(rowIdFor(document.getElementById("icon"))).toBe("7");' },
          { name: 'finds the row from a direct child', body: 'expect(rowIdFor(document.getElementById("plain"))).toBe("9");' },
          { name: 'returns null when there is no row ancestor', body: 'expect(rowIdFor(document.getElementById("outside"))).toBe(null);' },
          { name: 'works when the element itself is the row', body: 'expect(rowIdFor(document.querySelector(".row"))).toBe("7");' },
          { name: 'does not throw for an element outside any row', body: 'expect(() => rowIdFor(document.getElementById("outside"))).not.toThrow();', hidden: true },
        ],
        hints: [
          '`closest(".row")` walks up the ancestors, checking the element itself first.',
          'It returns `null` when nothing matches — guard before reading `dataset`.',
        ],
        solution: [
          'function rowIdFor(element) {',
          '  const row = element.closest(".row");',
          '  return row === null ? null : row.dataset.id;',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`closest` is what makes this robust to nesting depth — the icon is three levels inside its row, but one call finds it without any knowledge of the intermediate wrappers. Because `closest` checks the element itself before walking upward, the fourth test (where the element *is* the row) passes with no special case. The `null` guard is essential: `closest` returns `null` when no ancestor matches, and reading `.dataset` from that would throw, which the hidden test verifies.',
      },
      {
        id: 'ex-m18-05-c',
        title: 'Move an item up the list',
        kind: EXERCISE_KIND.DOM_TASK,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 30,
        topicIds: ['dom-manipulation'],
        instructions:
          'Write `moveUp(id)` that swaps the element with the given id with its previous sibling. If it is already first (or does not exist), leave the list unchanged. Return `true` if a move happened, `false` otherwise.',
        html: '<ul id="list"><li id="a">A</li><li id="b">B</li><li id="c">C</li></ul>',
        starterCode: [
          'function moveUp(id) {',
          '  // Which insertion method places a node before another?',
          '}',
        ].join('\n'),
        tests: [
          { name: 'moves a middle item up', body: 'document.getElementById("list").innerHTML = `<li id="a">A</li><li id="b">B</li><li id="c">C</li>`; expect(moveUp("b")).toBe(true); expect([...document.querySelectorAll("#list li")].map((li) => li.id)).toEqual(["b", "a", "c"]);' },
          { name: 'moves the last item up', body: 'document.getElementById("list").innerHTML = `<li id="a">A</li><li id="b">B</li><li id="c">C</li>`; expect(moveUp("c")).toBe(true); expect([...document.querySelectorAll("#list li")].map((li) => li.id)).toEqual(["a", "c", "b"]);' },
          { name: 'does nothing for the first item', body: 'document.getElementById("list").innerHTML = `<li id="a">A</li><li id="b">B</li><li id="c">C</li>`; expect(moveUp("a")).toBe(false); expect([...document.querySelectorAll("#list li")].map((li) => li.id)).toEqual(["a", "b", "c"]);' },
          { name: 'returns false for a missing id', body: 'document.getElementById("list").innerHTML = `<li id="a">A</li><li id="b">B</li>`; expect(moveUp("nope")).toBe(false);' },
          { name: 'keeps the list length unchanged', body: 'document.getElementById("list").innerHTML = `<li id="a">A</li><li id="b">B</li><li id="c">C</li>`; moveUp("b"); expect(document.querySelectorAll("#list li").length).toBe(3);' },
          { name: 'does not throw for a missing id', body: 'expect(() => moveUp("nope")).not.toThrow();', hidden: true },
        ],
        hints: [
          '`previousElementSibling` is `null` for the first element.',
          '`previous.before(item)` moves the item ahead of it — inserting an attached node relocates it.',
          'Guard both the missing element and the no-previous-sibling case.',
        ],
        solution: [
          'function moveUp(id) {',
          '  const item = document.getElementById(id);',
          '  if (item === null) return false;',
          '',
          '  const previous = item.previousElementSibling;',
          '  if (previous === null) return false;',
          '',
          '  previous.before(item);',
          '  return true;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'This combines traversal with lesson 2\'s move behaviour: `previous.before(item)` inserts an already-attached node, which **relocates** it rather than copying — so the swap happens in one call with no explicit removal, and the list length never changes. Two guards are needed, and they fail for different reasons: a missing element (bad id) and an element that is already first (`previousElementSibling` is `null` at the edge). Using `previousElementSibling` rather than `previousSibling` is what makes this reliable regardless of whitespace in the markup.',
      },
      {
        id: 'ex-m18-05-d',
        title: 'Which traversal property?',
        kind: EXERCISE_KIND.CHOOSE_IMPLEMENTATION,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['dom-manipulation'],
        instructions:
          'You need the first `<li>` inside a `<ul>` whose HTML is written with normal indentation. Which is reliable?',
        options: [
          'list.firstChild',
          'list.firstElementChild',
          'list.childNodes[0]',
          'list.parentElement',
        ],
        correct: 1,
        hints: [
          'Which one skips whitespace text nodes?',
        ],
        solution: 'list.firstElementChild',
        solutionExplanation:
          'With indented markup, the first child **node** is a whitespace text node from the newline and spaces, so `firstChild` and `childNodes[0]` both return that text node rather than the `<li>`. `firstElementChild` skips non-element nodes and reliably returns the first `<li>` regardless of formatting. `parentElement` goes the wrong direction entirely — upward, not into the children.',
      },
    ],
    quiz: {
      id: 'qz-m18-05',
      questions: [
        {
          id: 'q-m18-05-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'Why do children and childNodes often report different counts?',
          options: [
            'childNodes is buggy in some browsers',
            'childNodes includes text and comment nodes — including whitespace from indented markup — while children counts elements only',
            'children only counts direct children',
            'They never differ',
          ],
          correct: 1,
          optionExplanations: [
            'Both are correct and consistent; they simply count different things.',
            'Correct — whitespace between tags becomes text nodes.',
            'Both count only direct children.',
            'They differ whenever there is whitespace or comments between tags.',
          ],
          explanation:
            'Whitespace and newlines between tags become text nodes, which `childNodes` counts and `children` ignores. This makes `childNodes`-based logic sensitive to HTML formatting, which is why the Element-oriented properties are preferred for UI work.',
        },
        {
          id: 'q-m18-05-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'What does element.closest(selector) do?',
          options: [
            'Finds the nearest matching descendant',
            'Walks up from the element (checking the element itself first) until an ancestor matches the selector, or returns null',
            'Finds the nearest sibling that matches',
            'Returns the closest element by pixel distance',
          ],
          correct: 1,
          optionExplanations: [
            'It searches upward, not downward — querySelector searches descendants.',
            'Correct — self, then ancestors, returning null if nothing matches.',
            'Siblings are not searched.',
            'It is structural, not geometric.',
          ],
          explanation:
            '`closest` tests the element itself, then each ancestor in turn, returning the first match or `null`. It is the key to robust event handling: given a click on any nested element, one call finds the meaningful container regardless of nesting depth.',
        },
        {
          id: 'q-m18-05-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'What does nextElementSibling return for the last element in a container?',
          options: ['undefined', 'null', 'The parent element', 'The first sibling, wrapping around'],
          correct: 1,
          optionExplanations: [
            'DOM traversal uses null, not undefined, for "nothing there".',
            'Correct — the same null convention as a failed lookup.',
            'It never returns the parent.',
            'There is no wrapping behaviour.',
          ],
          explanation:
            'There is no element after the last one, so the property is `null` — matching the convention used by `querySelector` and `closest`. Chaining off it without a guard throws, so optional chaining is the safe form when an adjacent element is not certain.',
        },
        {
          id: 'q-m18-05-4',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'What is the difference between matches and contains?',
          options: [
            'They are aliases',
            'matches tests whether this element matches a selector; contains tests whether a given node is inside this element',
            'contains takes a selector; matches takes a node',
            'matches searches descendants',
          ],
          correct: 1,
          optionExplanations: [
            'They answer completely different questions.',
            'Correct — "is this the thing?" versus "is that inside this?".',
            'This has the argument types reversed.',
            'matches tests only the element it is called on.',
          ],
          explanation:
            '`matches(selector)` returns a boolean for the element it is called on. `contains(node)` returns whether the passed node is a descendant — and counts the element itself as contained. In event handling, `closest` finds the meaningful element and `contains` confirms it belongs to your container.',
        },
      ],
    },
    summary:
      'Traversal moves from an element you already have to one nearby. The **Element-oriented** properties — `parentElement`, `children`, `firstElementChild`, `lastElementChild`, `nextElementSibling`, `previousElementSibling` — are the right default for UI work, and return `null` at an edge, following the same convention as a failed lookup. Their older **Node** counterparts (`parentNode`, `childNodes`, `firstChild`, `nextSibling`) see every node, including the text nodes that ordinary indentation creates — so a two-item list can have five child nodes, and `firstChild` is usually whitespace rather than the element you wanted. That makes `childNodes`-based logic sensitive to HTML formatting rather than to your data. `closest(selector)` walks upward from the element (testing itself first) until an ancestor matches, returning `null` if none does — the most valuable traversal method in real code, because one call finds a meaningful container regardless of nesting depth. `matches(selector)` asks "is this the thing?" and `contains(node)` asks "is that inside this?" — together with `closest` they form the guard pattern that event delegation depends on.',
    keyTakeaways: [
      'Prefer the Element APIs — childNodes and firstChild see whitespace text nodes',
      'Traversal at an edge returns null; use optional chaining when unsure',
      'closest walks up from the element itself and is robust to nesting depth',
      'matches tests the element; contains tests ancestry, counting itself',
      'Traversal plus insertion moves nodes — previous.before(item) swaps in one call',
      'childNodes counts change when HTML is reformatted; children counts do not',
    ],
    relatedLessons: ['l-m18-04', 'l-m18-06'],
    interviewConnections: [
      'What is the difference between children and childNodes?',
      'What does closest() do and why is it useful in event handling?',
      'Why does firstChild often not return the element you expect?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m18-06',
    slug: 'live-and-static-collections',
    moduleId: M,
    order: 6,
    title: 'Live and Static Collections',
    description: 'Why children updates itself but querySelectorAll does not, and how to iterate either one.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 15,
    xp: 35,
    topicIds: ['dom-manipulation', 'dom'],
    prerequisites: ['l-m18-05'],
    learningObjectives: [
      'Explain the difference between a live and a static collection',
      'Predict which DOM APIs return which kind',
      'Iterate a NodeList and an HTMLCollection correctly',
      'Convert a DOM collection to a real array when array methods are needed',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Module 17 introduced this briefly. It matters enough for dynamic UI — where elements are constantly created and removed — to demonstrate properly.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Static versus live, demonstrated',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="list"><li class="item">A</li><li class="item">B</li></ul>',
        caption: 'The same list, captured two ways, then modified.',
        code: [
          'const list = document.getElementById("list");',
          '',
          'const staticList = document.querySelectorAll(".item"); // NodeList, static',
          'const liveByClass = document.getElementsByClassName("item"); // HTMLCollection, live',
          'const liveChildren = list.children; // HTMLCollection, live',
          '',
          'console.log("before — static:", staticList.length, "byClass:", liveByClass.length, "children:", liveChildren.length);',
          '',
          'const extra = document.createElement("li");',
          'extra.className = "item";',
          'list.append(extra);',
          '',
          'console.log("after add — static:", staticList.length, "byClass:", liveByClass.length, "children:", liveChildren.length);',
          '',
          'list.firstElementChild.remove();',
          '',
          'console.log("after remove — static:", staticList.length, "byClass:", liveByClass.length, "children:", liveChildren.length);',
        ].join('\n'),
        output: [
          'before — static: 2 byClass: 2 children: 2',
          'after add — static: 2 byClass: 3 children: 3',
          'after remove — static: 2 byClass: 2 children: 2',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The static `NodeList` reports `2` throughout — it is a **snapshot** taken when `querySelectorAll` ran, and nothing that happens afterwards changes it. The two live `HTMLCollection`s track the document continuously, growing when an element is added and shrinking when one is removed.',
        ],
      },
      {
        kind: SECTION.TABLE,
        headers: ['API', 'Returns', 'Live or static'],
        rows: [
          ['`querySelectorAll(sel)`', '`NodeList`', '**Static** snapshot'],
          ['`getElementsByClassName(cls)`', '`HTMLCollection`', '**Live**'],
          ['`getElementsByTagName(tag)`', '`HTMLCollection`', '**Live**'],
          ['`element.children`', '`HTMLCollection`', '**Live**'],
          ['`element.childNodes`', '`NodeList`', '**Live**'],
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Do not assume "NodeList means static"',
        body: [
          'The last row is the one that catches people out. `childNodes` returns a `NodeList` that **is live**, while `querySelectorAll` returns a `NodeList` that is **static**. The liveness is a property of the specific API, not of the collection type.',
          'The reliable rule is per-method, not per-type: `querySelectorAll` is the static one; `children`, `childNodes` and the `getElementsBy*` family are live.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The classic live-collection bug',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="list"><li class="item">A</li><li class="item">B</li><li class="item">C</li><li class="item">D</li></ul>',
        caption: 'Removing elements while looping over a live collection skips half of them.',
        code: [
          'const list = document.getElementById("list");',
          'const live = list.getElementsByClassName("item");',
          '',
          '// Looping forward over a LIVE collection while removing',
          'for (let i = 0; i < live.length; i++) {',
          '  live[i].remove();',
          '}',
          '',
          'console.log("remaining after live loop:", list.children.length);',
          '',
          '// Reset',
          'list.replaceChildren();',
          'for (const text of ["A", "B", "C", "D"]) {',
          '  const li = document.createElement("li");',
          '  li.className = "item";',
          '  li.textContent = text;',
          '  list.append(li);',
          '}',
          '',
          '// A static snapshot is unaffected by the removals',
          'document.querySelectorAll("#list .item").forEach((el) => el.remove());',
          'console.log("remaining after static loop:", list.children.length);',
        ].join('\n'),
        output: [
          'remaining after live loop: 2',
          'remaining after static loop: 0',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Why exactly two survived',
        body: [
          'Removing `live[0]` shifts every remaining element down one index while `i` moves up — so the loop reads index 1 next, which is now the *third* original element, skipping the second entirely. Half the list is skipped, and `length` shrinks as you go.',
          'The static `querySelectorAll` snapshot has no such problem: removing an element from the document does not change the snapshot, so every element is visited exactly once. **When you intend to modify the collection you are iterating, take a static snapshot first.**',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Iterating DOM collections',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="list"><li class="item">A</li><li class="item">B</li></ul>',
        caption: 'What works on which.',
        code: [
          'const nodeList = document.querySelectorAll(".item");',
          'const collection = document.getElementById("list").children;',
          '',
          '// forEach: NodeList has it, HTMLCollection does NOT',
          'console.log("NodeList forEach     :", typeof nodeList.forEach);',
          'console.log("HTMLCollection forEach:", typeof collection.forEach);',
          '',
          '// for...of works on both — they are iterable',
          'const a = [];',
          'for (const el of nodeList) a.push(el.textContent);',
          'for (const el of collection) a.push(el.textContent);',
          'console.log("for...of both        :", a);',
          '',
          '// Neither has map/filter/reduce',
          'console.log("NodeList map         :", typeof nodeList.map);',
          '',
          '// Array.from or spread gives a real array',
          'console.log("Array.from is array  :", Array.isArray(Array.from(collection)));',
          'console.log("spread is array      :", Array.isArray([...nodeList]));',
          'console.log("mapped               :", [...nodeList].map((el) => el.textContent));',
        ].join('\n'),
        output: [
          'NodeList forEach     : function',
          'HTMLCollection forEach: undefined',
          'for...of both        : ["A", "B", "A", "B"]',
          'NodeList map         : undefined',
          'Array.from is array  : true',
          'spread is array      : true',
          'mapped               : ["A", "B"]',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'The practical summary',
        body: [
          'A `NodeList` has `forEach` and `length`; an `HTMLCollection` has only `length` and indexing. **Both are iterable**, so `for...of` works on either. Neither is an `Array`, so neither has `map`, `filter` or `reduce`.',
          'When you want the full array toolkit from Module 13, convert with `Array.from(collection)` or `[...collection]` — which is also a convenient way to take a static snapshot of a live collection, since the resulting array does not update.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        needsDom: true,
        html: '<ul id="list"><li class="item" data-price="10">A</li><li class="item" data-price="25">B</li><li class="item" data-price="5">C</li></ul>',
        caption: 'Converting unlocks everything from Module 13.',
        code: [
          'const items = [...document.querySelectorAll(".item")];',
          '',
          'const prices = items.map((el) => Number(el.dataset.price));',
          'console.log("prices  :", prices);',
          'console.log("total   :", prices.reduce((sum, p) => sum + p, 0));',
          'console.log("cheap   :", items.filter((el) => Number(el.dataset.price) < 20).length);',
          'console.log("any big :", items.some((el) => Number(el.dataset.price) > 20));',
          'console.log("texts   :", items.map((el) => el.textContent).join(", "));',
        ].join('\n'),
        output: [
          'prices  : [10, 25, 5]',
          'total   : 40',
          'cheap   : 2',
          'any big : true',
          'texts   : A, B, C',
        ].join('\n'),
      },
      {
        kind: SECTION.PREDICT,
        code: [
          '// <ul id="l"><li class="i">A</li></ul>',
          'const l = document.getElementById("l");',
          'const snap = document.querySelectorAll(".i");',
          'const live = l.children;',
          '',
          'const extra = document.createElement("li");',
          'extra.className = "i";',
          'l.append(extra);',
          '',
          'console.log(snap.length, live.length);',
        ].join('\n'),
        options: [
          '1 2',
          '2 2',
          '1 1',
          '2 1',
        ],
        correct: 0,
        explanation:
          '`querySelectorAll` returns a **static** `NodeList` — a snapshot frozen at the moment it was called — so appending a new matching element afterwards does not change it, and `snap.length` stays `1`. `element.children` returns a **live** `HTMLCollection` that tracks the document continuously, so it immediately reflects the new child and reports `2`. Re-running `querySelectorAll` would give `2` as well, but the already-captured `snap` variable never updates.',
      },
    ],
    exercises: [
      {
        id: 'ex-m18-06-a',
        title: 'Predict the live collection',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['dom-manipulation'],
        instructions:
          'Given `<ul id="l"><li>A</li><li>B</li></ul>`, what does this print?',
        code: [
          'const kids = document.getElementById("l").children;',
          'document.getElementById("l").firstElementChild.remove();',
          'console.log(kids.length);',
        ].join('\n'),
        options: ['2', '1', '0', 'It throws'],
        correct: 1,
        hints: [
          'Is `children` a live collection or a static snapshot?',
        ],
        solution: '1',
        solutionExplanation:
          '`element.children` returns a **live** `HTMLCollection` that tracks the document continuously. Removing a child updates the collection immediately, even though `kids` was captured before the removal — so the count drops to `1`. A `querySelectorAll` snapshot taken at the same moment would still report `2`, because it never updates after being created.',
      },
      {
        id: 'ex-m18-06-b',
        title: 'Remove every matching element safely',
        kind: EXERCISE_KIND.DOM_TASK,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['dom-manipulation'],
        instructions:
          'Write `removeAll(selector)` that removes every element in the document matching the selector, and returns how many it removed. It must remove *all* of them, not skip any.',
        html: '<div id="host"><p class="temp">1</p><p class="temp">2</p><p class="temp">3</p><p class="temp">4</p><p class="keep">keep</p></div>',
        starterCode: [
          'function removeAll(selector) {',
          '  // Take a static snapshot before removing anything.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'removes every matching element', body: 'document.getElementById("host").innerHTML = `<p class="temp">1</p><p class="temp">2</p><p class="temp">3</p><p class="temp">4</p><p class="keep">keep</p>`; removeAll(".temp"); expect(document.querySelectorAll(".temp").length).toBe(0);' },
          { name: 'returns the number removed', body: 'document.getElementById("host").innerHTML = `<p class="temp">1</p><p class="temp">2</p><p class="temp">3</p><p class="temp">4</p><p class="keep">keep</p>`; expect(removeAll(".temp")).toBe(4);' },
          { name: 'leaves non-matching elements alone', body: 'document.getElementById("host").innerHTML = `<p class="temp">1</p><p class="keep">keep</p>`; removeAll(".temp"); expect(document.querySelectorAll(".keep").length).toBe(1);' },
          { name: 'returns 0 when nothing matches', body: 'expect(removeAll(".nothing")).toBe(0);' },
          { name: 'does not throw when nothing matches', body: 'expect(() => removeAll(".nothing")).not.toThrow();' },
          { name: 'is safe to call twice', body: 'document.getElementById("host").innerHTML = `<p class="temp">1</p><p class="temp">2</p>`; removeAll(".temp"); expect(removeAll(".temp")).toBe(0);', hidden: true },
        ],
        hints: [
          '`querySelectorAll` gives a static snapshot, unaffected by the removals.',
          'Capture the count before removing, or use the NodeList\'s length.',
          'A live collection would skip half the elements as indices shift.',
        ],
        solution: [
          'function removeAll(selector) {',
          '  const matches = document.querySelectorAll(selector);',
          '  matches.forEach((el) => el.remove());',
          '',
          '  return matches.length;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Using `querySelectorAll` is what makes this correct: its static snapshot is unaffected by the removals, so every matched element is visited exactly once and `matches.length` still reports the original count afterwards. A live collection from `getElementsByClassName` would have skipped every other element as the indices shifted beneath the loop — the classic bug from this lesson. `NodeList` has `forEach`, so no array conversion is needed here.',
      },
      {
        id: 'ex-m18-06-c',
        title: 'Sum data from the DOM',
        kind: EXERCISE_KIND.DOM_TASK,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['dom-manipulation'],
        instructions:
          'Write `totalPrice()` returning the sum of the `data-price` attributes of every `.item` element, as a number. Return `0` when there are none.',
        html: '<ul id="list"><li class="item" data-price="10">A</li><li class="item" data-price="25">B</li><li class="item" data-price="5.5">C</li></ul>',
        starterCode: [
          'function totalPrice() {',
          '  // A NodeList has no reduce — convert it first.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'sums the prices', body: 'document.getElementById("list").innerHTML = `<li class="item" data-price="10">A</li><li class="item" data-price="25">B</li><li class="item" data-price="5.5">C</li>`; expect(totalPrice()).toBe(40.5);' },
          { name: 'returns a number', body: 'expect(typeof totalPrice()).toBe("number");' },
          { name: 'returns 0 when there are no items', body: 'document.getElementById("list").innerHTML = ``; expect(totalPrice()).toBe(0);' },
          { name: 'reflects removed items', body: 'document.getElementById("list").innerHTML = `<li class="item" data-price="10">A</li><li class="item" data-price="25">B</li><li class="item" data-price="5.5">C</li>`; document.querySelector(".item").remove(); expect(totalPrice()).toBe(30.5);', hidden: true },
        ],
        hints: [
          '`[...nodeList]` or `Array.from(nodeList)` gives a real array.',
          '`dataset.price` is a string — convert with `Number(...)`.',
          '`reduce` with an initial value of `0` handles the empty case.',
        ],
        solution: [
          'function totalPrice() {',
          '  return [...document.querySelectorAll(".item")]',
          '    .reduce((sum, el) => sum + Number(el.dataset.price), 0);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'A `NodeList` has `forEach` but not `reduce`, so spreading it into a real array unlocks the full Module 13 toolkit. `dataset.price` is always a **string** — the Module 17 rule — so `Number(...)` is required or the values would concatenate as text instead of summing. The `reduce` initial value of `0` handles the empty case without throwing, satisfying the third test. Because `querySelectorAll` runs fresh on each call, the function always reflects the current document, which the hidden test verifies.',
      },
      {
        id: 'ex-m18-06-d',
        title: 'Which collection for the job?',
        kind: EXERCISE_KIND.CHOOSE_IMPLEMENTATION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['dom-manipulation'],
        instructions:
          'You need to loop over every `.row` element and remove each one. Which approach removes all of them reliably?',
        options: [
          'Loop forward over document.getElementsByClassName("row") by index',
          'Use document.querySelectorAll(".row") and remove each',
          'Loop forward over parent.children by index',
          'Any of these work identically',
        ],
        correct: 1,
        hints: [
          'Which of these gives a snapshot that will not shift beneath the loop?',
        ],
        solution: 'Use document.querySelectorAll(".row") and remove each',
        solutionExplanation:
          '`querySelectorAll` returns a static snapshot that is unaffected by removals, so every element is visited exactly once. Both live-collection options shrink as you remove, shifting the remaining elements down while the index moves up — skipping every other element and leaving roughly half the list behind. Whenever you intend to modify the collection you are iterating, take a static snapshot first.',
      },
    ],
    quiz: {
      id: 'qz-m18-06',
      questions: [
        {
          id: 'q-m18-06-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'Which statement about DOM collection liveness is accurate?',
          options: [
            'All NodeLists are static and all HTMLCollections are live',
            'Liveness depends on the specific API — querySelectorAll is static, but childNodes returns a live NodeList',
            'All DOM collections are live',
            'All DOM collections are static',
          ],
          correct: 1,
          optionExplanations: [
            'childNodes returns a NodeList that is live, breaking this rule.',
            'Correct — it is a per-method property, not a per-type one.',
            'querySelectorAll returns a static snapshot.',
            'children and the getElementsBy* family are live.',
          ],
          explanation:
            'Liveness is determined by the method, not the collection type. `querySelectorAll` gives a static `NodeList`; `childNodes` gives a **live** `NodeList`; `children` and the `getElementsBy*` family give live `HTMLCollection`s.',
        },
        {
          id: 'q-m18-06-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'Why does removing elements in a forward index loop over a live collection skip some?',
          options: [
            'The browser caches the collection incorrectly',
            'Removing an element shifts the remaining ones down an index while the loop counter moves up, so every other element is skipped',
            'Live collections cannot be indexed',
            'remove() is asynchronous',
          ],
          correct: 1,
          optionExplanations: [
            'The collection updates correctly; the loop logic is the problem.',
            'Correct — indices shift beneath the loop.',
            'They can be indexed normally.',
            'remove() is synchronous.',
          ],
          explanation:
            'A live collection updates immediately on removal, so element 1 becomes element 0 while `i` advances to 1 — landing on what was originally element 2. Taking a static snapshot with `querySelectorAll` avoids the problem entirely.',
        },
        {
          id: 'q-m18-06-3',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['dom-manipulation'],
          prompt: 'Which are true of a NodeList from querySelectorAll? (Select all that apply.)',
          options: [
            'It has forEach',
            'It has length',
            'It is iterable with for...of',
            'It has map and filter',
          ],
          correct: [0, 1, 2],
          explanation:
            'A `NodeList` has `length`, `forEach`, and is iterable so `for...of` works. It is **not** an `Array`, so `map`, `filter` and `reduce` are absent — convert with `Array.from(list)` or `[...list]` when you need them.',
        },
        {
          id: 'q-m18-06-4',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dom-manipulation'],
          prompt: 'What does an HTMLCollection lack that a NodeList has?',
          options: ['length', 'Indexing with []', 'forEach', 'Iterability with for...of'],
          correct: 2,
          optionExplanations: [
            'Both have length.',
            'Both support index access.',
            'Correct — HTMLCollection has no forEach method.',
            'Both are iterable.',
          ],
          explanation:
            'An `HTMLCollection` supports `length`, index access and `for...of`, but has no `forEach`. Use `for...of`, or convert with `Array.from`/spread to get the full array toolkit.',
        },
      ],
    },
    summary:
      'A **static** collection is a snapshot frozen at the moment it was created; a **live** collection tracks the document continuously. Liveness is a property of the specific **API**, not of the collection type — `querySelectorAll` returns a static `NodeList`, but `childNodes` returns a **live** `NodeList`, while `children` and the `getElementsBy*` family return live `HTMLCollection`s. The classic bug is removing elements in a forward index loop over a live collection: each removal shifts the remaining elements down while the counter moves up, silently skipping every other one — take a static snapshot with `querySelectorAll` whenever you intend to modify what you are iterating. For iteration, a `NodeList` has `length` and `forEach`; an `HTMLCollection` has `length` and indexing but **no `forEach`**; both are iterable, so `for...of` works on either. Neither is an `Array`, so `map`, `filter` and `reduce` require converting with `Array.from(collection)` or `[...collection]` — which also produces a static snapshot of a live collection as a useful side effect.',
    keyTakeaways: [
      'Liveness depends on the method, not the collection type',
      'querySelectorAll is static; children, childNodes and getElementsBy* are live',
      'Removing in a forward index loop over a live collection skips every other element',
      'Take a static snapshot when modifying the collection you iterate',
      'NodeList has forEach; HTMLCollection does not; both work with for...of',
      'Array.from or spread converts either into a real array and freezes it',
    ],
    relatedLessons: ['l-m18-05', 'l-m17-02'],
    interviewConnections: [
      'What is the difference between a live and a static DOM collection?',
      'Why can removing elements while looping over getElementsByClassName skip some?',
      'Is a NodeList an array, and how do you convert one?',
      'Which DOM methods return live collections?',
    ],
  },
];
