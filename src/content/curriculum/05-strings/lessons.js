import { SECTION, CALLOUT_TONE, DIFFICULTY, EXERCISE_KIND, QUIZ_KIND } from '../../schema/types.js';

const M = 'm05';

export default [
  /* ================================================================== */
  {
    id: 'l-m05-01',
    slug: 'creating-strings',
    moduleId: M,
    order: 1,
    title: 'Creating Strings: Quotes and Escaping',
    description: 'The three quote styles, when each is right, and how to put a quote inside a quote.',
    difficulty: DIFFICULTY.BEGINNER,
    estimatedMinutes: 12,
    xp: 25,
    topicIds: ['strings'],
    prerequisites: ['l-m04-06'],
    learningObjectives: [
      'Choose between single, double and backtick quotes',
      'Escape characters that would otherwise end or break a string',
      'Use the common escape sequences including `\\n` and `\\t`',
      'Explain why escaping matters when handling file paths and user text',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'A string is a sequence of characters. You have been using them since the first module; this module makes them precise and gives you the working vocabulary of methods you will use every day.',
          'JavaScript offers three ways to write a string literal, and unlike some languages they are not interchangeable in every respect.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'All three produce ordinary string values.',
        code: [
          "const single = 'Hello';",
          'const double = "Hello";',
          'const backtick = `Hello`;',
          '',
          'console.log(single === double);',
          'console.log(double === backtick);',
          'console.log(typeof backtick);',
        ].join('\n'),
        output: ['true', 'true', 'string'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Single and double quotes are genuinely identical in behaviour — the choice between them is pure convention, and most teams pick one and enforce it with a formatter. Backticks are different: they create **template literals**, which support interpolation and span multiple lines. The next lesson is devoted to them.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Putting a quote inside a string',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'A string ends at the first matching quote character, so a quote inside the text needs handling. There are two approaches.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Switch the outer quote, or escape the inner one.',
        code: [
          '// 1. Use a different outer quote — usually the cleaner option',
          'const a = "It\'s working";',
          "const b = 'She said \"yes\"';",
          'const c = `Both \' and " are fine here`;',
          '',
          'console.log(a);',
          'console.log(b);',
          'console.log(c);',
          '',
          '// 2. Escape with a backslash',
          "const d = 'It\\'s working';",
          'const e = "She said \\"yes\\"";',
          '',
          'console.log(d);',
          'console.log(e);',
        ].join('\n'),
        output: [
          "It's working",
          'She said "yes"',
          'Both \' and " are fine here',
          "It's working",
          'She said "yes"',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Prefer switching the quote over escaping',
        body: [
          'Escaping works, but backslashes make text harder to read and easy to get wrong. If your text contains apostrophes, wrap it in double quotes. If it contains double quotes, wrap it in single quotes or backticks.',
          'Backticks are the escape hatch when text contains both — which is common in anything quoting human speech.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Escape sequences',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'A backslash gives the next character a special meaning. These are the sequences you will actually encounter.',
        ],
      },
      {
        kind: SECTION.TABLE,
        headers: ['Sequence', 'Produces'],
        rows: [
          ['`\\n`', 'Newline'],
          ['`\\t`', 'Tab'],
          ['`\\\\`', 'A single literal backslash'],
          ["`\\'`", 'A single quote'],
          ['`\\"`', 'A double quote'],
          ['`` \\` ``', 'A backtick'],
          ['`\\u00e9`', 'A character by its four-digit code — é'],
          ['`\\u{1F600}`', 'A character by its full code point — an emoji'],
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Escape sequences in use.',
        code: [
          'console.log("Line one\\nLine two");',
          'console.log("Name:\\tAda");',
          'console.log("A backslash: \\\\");',
          'console.log("Caf\\u00e9");',
          'console.log("\\u{1F680} launch");',
        ].join('\n'),
        output: [
          'Line one\nLine two',
          'Name:\tAda',
          'A backslash: \\',
          'Café',
          '🚀 launch',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Backslashes in Windows paths and regular expressions',
        body: [
          'A Windows path such as `C:\\Users\\Ada` contains backslashes, and in a string literal each one must be doubled: `"C:\\\\Users\\\\Ada"`. Forgetting this produces silently wrong text — `\\U` is not a recognised escape, and `\\A` is not either, so the backslashes simply vanish.',
          'The same applies when building a regular expression from a string. This is one reason regex literals (`/pattern/`) are preferred over `new RegExp("pattern")` where possible: they avoid a second layer of escaping.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'What happens when you forget to double a backslash.',
        code: [
          'const wrong = "C:\\Users\\Ada";',
          'const right = "C:\\\\Users\\\\Ada";',
          '',
          'console.log(wrong);',
          'console.log(right);',
          'console.log(wrong.length, right.length);',
        ].join('\n'),
        output: ['C:UsersAda', 'C:\\Users\\Ada', '10 12'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The backslashes did not produce an error — they were consumed as escape characters for `U` and `A`, which have no special meaning, so they were dropped. The result is a plausible-looking string that is quietly wrong, which is exactly the sort of bug that survives a code review.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const path = "a\\nb";',
          'console.log(path.length);',
        ].join('\n'),
        options: ['4', '3', '2', '5'],
        correct: 1,
        explanation:
          '`\\n` is a single escape sequence producing one character — a newline — not two characters. So the string is `a`, newline, `b`: three characters in total. The backslash exists only in the *source code*; it is not part of the resulting string value. This is why counting characters in a literal requires reading escapes as single units.',
      },
    ],
    exercises: [
      {
        id: 'ex-m05-01-a',
        title: 'Count the escaped characters',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['strings'],
        instructions: 'What does this print?',
        code: [
          'console.log("a\\tb".length);',
        ].join('\n'),
        options: ['4', '3', '2', '5'],
        correct: 1,
        hints: [
          '`\\t` is an escape sequence. How many characters does it produce?',
        ],
        solution: '3',
        solutionExplanation:
          '`\\t` is one escape sequence producing a single tab character. The string therefore contains `a`, a tab, and `b` — three characters. The backslash is part of the source syntax, not part of the string value. Every escape sequence in the table produces exactly one character, with the exception of `\\u{...}` for characters outside the basic range, which you will meet in the final lesson of this module.',
      },
      {
        id: 'ex-m05-01-b',
        title: 'Choose the right quoting',
        kind: EXERCISE_KIND.CHOOSE_IMPLEMENTATION,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['strings'],
        instructions:
          'You need a string containing exactly: `He said "it\'s fine"`. Which literal is both correct and easiest to read?',
        options: [
          '"He said \\"it\'s fine\\""',
          '\'He said "it\\\'s fine"\'',
          '`He said "it\'s fine"`',
          '"He said "it\'s fine""',
        ],
        correct: 2,
        hints: [
          'The text contains both a double quote and an apostrophe.',
          'Which quote style needs no escaping at all here?',
        ],
        solution: '`He said "it\'s fine"`',
        solutionExplanation:
          'Backticks let both `"` and `\'` appear unescaped, so the literal reads exactly like the text it produces. The first two options are correct but need escaping, which makes them harder to read and easier to get wrong. The fourth is a SyntaxError: the string ends at the second `"`, and what follows is not valid syntax. When text contains both quote characters, backticks are the natural choice.',
      },
      {
        id: 'ex-m05-01-c',
        title: 'Build a tab-separated row',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 15,
        topicIds: ['strings'],
        instructions:
          'Write `toRow(values)` that joins an array of values into a single tab-separated line ending with a newline. `toRow(["a", "b"])` should produce the string `"a\\tb\\n"`.',
        starterCode: [
          'function toRow(values) {',
          '  // Join with a tab, then append a newline.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'joins two values', body: 'expect(toRow(["a", "b"])).toBe("a\\tb\\n");' },
          { name: 'joins three values', body: 'expect(toRow(["x", "y", "z"])).toBe("x\\ty\\tz\\n");' },
          { name: 'handles a single value', body: 'expect(toRow(["only"])).toBe("only\\n");' },
          { name: 'handles an empty array', body: 'expect(toRow([])).toBe("\\n");' },
          { name: 'converts numbers to text', body: 'expect(toRow([1, 2])).toBe("1\\t2\\n");' },
          { name: 'the result ends with a newline', body: 'expect(toRow(["a"]).endsWith("\\n")).toBe(true);', hidden: true },
        ],
        hints: [
          '`array.join(separator)` combines elements into a string.',
          'The separator you want is the escape sequence for a tab.',
          'Append the newline escape sequence to the joined result.',
        ],
        solution: [
          'function toRow(values) {',
          '  return values.join("\\t") + "\\n";',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`join` handles the separator placement, including the edge cases: a single element gets no separator, and an empty array produces an empty string — which is why the empty case correctly yields just the newline. `join` also converts each element to a string automatically, so the numeric test passes without explicit conversion. Note that the escape sequences produce single characters, so the resulting string for `["a","b"]` is four characters long, not six.',
      },
    ],
    quiz: {
      id: 'qz-m05-01',
      questions: [
        {
          id: 'q-m05-01-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['strings'],
          prompt: 'What is the difference between single and double quotes in JavaScript?',
          options: [
            'Double quotes allow interpolation, single quotes do not',
            'There is no behavioural difference; the choice is convention',
            'Single quotes are faster',
            'Double quotes allow multiline strings',
          ],
          correct: 1,
          optionExplanations: [
            'Interpolation requires backticks; neither quote style supports it.',
            'Correct — they produce identical strings.',
            'There is no performance difference.',
            'Multiline strings require backticks.',
          ],
          explanation:
            'Single and double quotes behave identically and produce identical string values. Backticks are the one that differs, supporting interpolation and multiline text. Teams simply pick one of the two quote styles and enforce it with a formatter.',
        },
        {
          id: 'q-m05-01-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['strings'],
          prompt: 'What does this Windows path print?',
          code: ['console.log("C:\\Users");'].join('\n'),
          options: ['C:\\Users', 'C:Users', 'C:\\\\Users', 'A SyntaxError'],
          correct: 1,
          optionExplanations: [
            'That requires the backslash to be doubled in the literal.',
            'Correct — `\\U` is not a recognised escape, so the backslash is consumed.',
            'The literal backslash would need to be escaped to appear twice.',
            'Unrecognised escapes do not throw.',
          ],
          explanation:
            'The backslash begins an escape sequence. Since `\\U` has no special meaning, the backslash is simply dropped and `U` remains. The result is a plausible-looking but silently wrong path. Write `"C:\\\\Users"` to get a literal backslash.',
        },
        {
          id: 'q-m05-01-3',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['strings'],
          prompt: 'Which of these produce a string containing exactly one character? (Select all that apply.)',
          options: ['`"\\n"`', '`"\\\\"`', '`"\\t"`', '`"ab"`'],
          correct: [0, 1, 2],
          explanation:
            'Each escape sequence produces exactly one character: `\\n` is a newline, `\\\\` is a single literal backslash, and `\\t` is a tab. The backslashes exist only in the source syntax. `"ab"` is the only two-character string in the list.',
        },
      ],
    },
    summary:
      'Single and double quotes behave identically; backticks additionally support interpolation and multiline text. When text contains a quote character, prefer switching the outer quote style over escaping, and reach for backticks when the text contains both `\'` and `"`. A backslash gives the next character special meaning — `\\n`, `\\t`, `\\\\`, `\\u00e9` and friends each produce exactly one character in the resulting string, since the backslash is source syntax rather than data. Forgetting to double a backslash in a Windows path or a regex string does not throw: the backslash is silently consumed, producing text that looks nearly right and is wrong.',
    keyTakeaways: [
      'Single and double quotes are interchangeable; backticks are different',
      'Prefer switching the outer quote to escaping the inner one',
      'Escape sequences produce one character — the backslash is not part of the value',
      '`\\\\` is needed for a literal backslash, as in Windows paths',
      'An unrecognised escape silently drops the backslash rather than erroring',
    ],
    relatedLessons: ['l-m04-06', 'l-m05-02'],
    interviewConnections: [
      'What is the difference between single, double and backtick quotes?',
      'How do you include a quote character inside a string?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m05-02',
    slug: 'template-literals',
    moduleId: M,
    order: 2,
    title: 'Template Literals',
    description: 'Interpolation, multiline text, and why template literals replaced string concatenation.',
    difficulty: DIFFICULTY.BEGINNER,
    estimatedMinutes: 13,
    xp: 25,
    topicIds: ['strings', 'modern-js'],
    prerequisites: ['l-m05-01'],
    learningObjectives: [
      'Interpolate values and expressions into strings',
      'Write multiline strings without escape sequences',
      'Explain why template literals are safer than `+` concatenation',
      'Recognise tagged template literals when you see them',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Template literals use backticks and add two capabilities that ordinary quotes lack: **interpolation** and **multiline text**. Together they have made string concatenation with `+` largely obsolete in modern code.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Interpolation',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: '`${...}` inserts the value of any expression.',
        code: [
          'const name = "Ada";',
          'const age = 36;',
          '',
          'console.log(`Hello, ${name}!`);',
          'console.log(`Next year you will be ${age + 1}.`);',
          'console.log(`Name has ${name.length} letters.`);',
          '',
          '// Any expression works, including a ternary',
          'const items = 1;',
          'console.log(`You have ${items} item${items === 1 ? "" : "s"}.`);',
        ].join('\n'),
        output: [
          'Hello, Ada!',
          'Next year you will be 37.',
          'Name has 3 letters.',
          'You have 1 item.',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Anything inside `${}` is an **expression**, so it must produce a value. That is why a ternary works there but an `if` statement does not — the distinction you met in the orientation module and again with operators.',
        ],
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Concatenation',
          code: [
            'const msg = "Hello, " + name',
            '  + "! You are " + age',
            '  + " years old.";',
            '',
            '// Easy to lose a space,',
            '// hard to see the shape',
            '// of the final string.',
          ].join('\n'),
        },
        right: {
          title: 'Template literal',
          code: [
            'const msg =',
            '  `Hello, ${name}! You are ' +
            '${age} years old.`;',
            '',
            '// The spacing is visible',
            '// exactly as it will appear.',
          ].join('\n'),
        },
        note: 'The readability gain is not cosmetic: missing or doubled spaces inside concatenated quotes are one of the most common small bugs in string building, and template literals make them visible.',
      },
      {
        kind: SECTION.HEADING,
        text: 'Multiline strings',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Line breaks in the source become line breaks in the string.',
        code: [
          'const message = `Dear Ada,',
          '',
          'Thank you for your note.',
          '',
          'Regards,',
          'The Team`;',
          '',
          'console.log(message);',
          'console.log("---");',
          'console.log("lines:", message.split("\\n").length);',
        ].join('\n'),
        output: [
          'Dear Ada,',
          '',
          'Thank you for your note.',
          '',
          'Regards,',
          'The Team',
          '---',
          'lines: 6',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Indentation becomes part of the string',
        body: [
          'A multiline template literal preserves everything between the backticks — including the leading whitespace you added to line it up with the surrounding code. A template indented inside a function body will carry those spaces into every line of the output.',
          'Either write the lines flush against the left margin, which looks odd but is correct, or strip the indentation afterwards. This surprises people the first time they generate HTML or an email body.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Interpolation converts to string',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The same conversion rules you met in the coercion module.',
        code: [
          'console.log(`${42}`);            // "42"',
          'console.log(`${true}`);          // "true"',
          'console.log(`${null}`);          // "null"',
          'console.log(`${undefined}`);     // "undefined"',
          'console.log(`${[1, 2, 3]}`);     // "1,2,3"',
          'console.log(`${{ a: 1 }}`);      // "[object Object]"',
        ].join('\n'),
        output: ['42', 'true', 'null', 'undefined', '1,2,3', '[object Object]'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Interpolation applies the same string conversion as `String()`, so an object produces the unhelpful `"[object Object]"`. If you want to see an object\'s contents, use `JSON.stringify(obj)` inside the interpolation, or log the object separately rather than embedding it.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Template literals are not safe for HTML',
        body: [
          'Building HTML with a template literal and inserting it via `innerHTML` is a cross-site scripting vulnerability if any interpolated value came from a user. Interpolation performs no escaping whatsoever — it inserts the text exactly as-is, including any tags.',
          'You will cover this properly in the DOM and Security modules. For now: template literals make strings, and a string containing user text is not automatically safe to treat as markup.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Tagged templates',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'A function name placed immediately before a template literal receives the pieces rather than the finished string. You will rarely write one, but you will see them — `styled.div` in styled-components and `gql` in GraphQL clients both use this syntax.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The tag function receives the literal parts and the interpolated values separately.',
        code: [
          'function highlight(strings, ...values) {',
          '  return strings.reduce((result, part, i) => {',
          '    const value = i < values.length ? `[${values[i]}]` : "";',
          '    return result + part + value;',
          '  }, "");',
          '}',
          '',
          'const name = "Ada";',
          'const role = "engineer";',
          '',
          'console.log(highlight`Hello ${name}, the ${role}.`);',
        ].join('\n'),
        output: ['Hello [Ada], the [engineer].'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Because the tag function sees the values separately from the fixed text, it can process them — escaping them for HTML, or turning them into SQL parameters. That separation is precisely what makes tagged templates a genuine safety tool rather than just syntax.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const a = 1;',
          'const b = 2;',
          '',
          'console.log(`${a + b}` === "3");',
          'console.log(`${a}${b}` === "12");',
        ].join('\n'),
        options: [
          'true true',
          'false true',
          'true false',
          'false false',
        ],
        correct: 0,
        explanation:
          'The first interpolation evaluates the expression `a + b`, which is numeric addition giving `3`, then converts that to the string `"3"`. The second has two separate interpolations, each converting its own value, producing `"1"` followed by `"2"` — the string `"12"`. Both comparisons are therefore true. Note the difference from concatenation: inside `${}` the `+` is ordinary addition because both operands are numbers, whereas `"" + a + b` would concatenate.',
      },
    ],
    exercises: [
      {
        id: 'ex-m05-02-a',
        title: 'Predict the interpolated object',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['strings', 'coercion'],
        instructions: 'What does this print?',
        code: [
          'const user = { name: "Ada" };',
          'console.log(`User: ${user}`);',
        ].join('\n'),
        options: [
          'User: { name: "Ada" }',
          'User: [object Object]',
          'User: Ada',
          'User: undefined',
        ],
        correct: 1,
        hints: [
          'Interpolation applies the same conversion as `String()`.',
          'What does `String({})` produce?',
        ],
        solution: 'User: [object Object]',
        solutionExplanation:
          'Interpolation converts each value to a string using the same rules as `String()`, and a plain object stringifies to `"[object Object]"` via its default `toString`. To see the contents you would interpolate `JSON.stringify(user)` instead, or interpolate the specific property with `${user.name}`. Seeing `"[object Object]"` on screen always means an object was stringified where a specific value was intended.',
      },
      {
        id: 'ex-m05-02-b',
        title: 'Refactor concatenation into a template',
        kind: EXERCISE_KIND.REFACTOR,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 15,
        topicIds: ['strings', 'clean-code'],
        instructions:
          'Rewrite this function using a single template literal instead of concatenation. The output must be identical.',
        starterCode: [
          'function describe(product) {',
          '  return "The " + product.name + " costs $" + product.price + " (" + product.stock + " left)";',
          '}',
        ].join('\n'),
        tests: [
          { name: 'formats a product', body: 'expect(describe({ name: "Lamp", price: 25, stock: 3 })).toBe("The Lamp costs $25 (3 left)");' },
          { name: 'handles zero stock', body: 'expect(describe({ name: "Pen", price: 2, stock: 0 })).toBe("The Pen costs $2 (0 left)");' },
          { name: 'handles decimal prices', body: 'expect(describe({ name: "Book", price: 9.99, stock: 12 })).toBe("The Book costs $9.99 (12 left)");' },
          { name: 'uses a template literal', body: 'expect(describe.toString()).toContain("`");', hidden: true },
        ],
        hints: [
          'Wrap the whole string in backticks.',
          'Replace each `" + value + "` with `${value}`.',
          'The `$` before the price is literal text and stays as it is — only `${` starts an interpolation.',
        ],
        solution: [
          'function describe(product) {',
          '  return `The ${product.name} costs $${product.price} (${product.stock} left)`;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The `$${product.price}` sequence looks odd but is correct: the first `$` is literal currency text, and `${` begins the interpolation. A lone `$` not followed by `{` has no special meaning inside a template literal. The refactored version makes the spacing visible — in the original, the space before `(` lives inside a quoted fragment where it is easy to lose. Note the zero-stock test passes either way here, but concatenation would have been a problem if any value were falsy and a `||` default had been involved.',
      },
      {
        id: 'ex-m05-02-c',
        title: 'Build a receipt line',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['strings'],
        instructions:
          'Write `receiptLine(item, quantity, unitPrice)` returning a line of the form `"3 x Lamp @ $25.00 = $75.00"`. Prices must always show exactly two decimal places, and the quantity is a whole number. Use a template literal.',
        starterCode: [
          'function receiptLine(item, quantity, unitPrice) {',
          '  // toFixed(2) formats a number to two decimal places.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'formats a whole-pound price', body: 'expect(receiptLine("Lamp", 3, 25)).toBe("3 x Lamp @ $25.00 = $75.00");' },
          { name: 'formats a decimal price', body: 'expect(receiptLine("Book", 2, 9.99)).toBe("2 x Book @ $9.99 = $19.98");' },
          { name: 'handles a quantity of one', body: 'expect(receiptLine("Pen", 1, 1.5)).toBe("1 x Pen @ $1.50 = $1.50");' },
          { name: 'handles a quantity of zero', body: 'expect(receiptLine("Mug", 0, 4)).toBe("0 x Mug @ $4.00 = $0.00");' },
          { name: 'pads a whole total to two decimals', body: 'expect(receiptLine("Box", 4, 2.5)).toBe("4 x Box @ $2.50 = $10.00");', hidden: true },
        ],
        hints: [
          '`(25).toFixed(2)` gives the string `"25.00"`.',
          'Compute the total as `quantity * unitPrice`, then format it.',
          'Remember the literal `$` before each interpolation.',
        ],
        solution: [
          'function receiptLine(item, quantity, unitPrice) {',
          '  const total = quantity * unitPrice;',
          '  return `${quantity} x ${item} @ $${unitPrice.toFixed(2)} = $${total.toFixed(2)}`;',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`toFixed(2)` returns a *string* with exactly two decimal places, padding with zeros where needed — which is why `25` becomes `"25.00"` and the total `10` becomes `"10.00"`. Computing `total` before the template keeps the interpolation readable; embedding `(quantity * unitPrice).toFixed(2)` directly would also work but is denser. Worth noting for real applications: `toFixed` is a display-formatting tool built on floating-point arithmetic, so for actual money you would follow the advice from the Data Types module and work in integer cents.',
      },
    ],
    quiz: {
      id: 'qz-m05-02',
      questions: [
        {
          id: 'q-m05-02-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['strings'],
          prompt: 'What can go inside `${...}` in a template literal?',
          options: [
            'Only variable names',
            'Any expression that produces a value',
            'Any statement, including `if`',
            'Only strings and numbers',
          ],
          correct: 1,
          optionExplanations: [
            'Function calls, arithmetic and ternaries all work.',
            'Correct — it must be an expression, not a statement.',
            '`if` is a statement and produces no value, so it cannot go there.',
            'Any value works; it is converted to a string.',
          ],
          explanation:
            '`${}` takes an expression — anything that evaluates to a value. That includes arithmetic, method calls and ternaries, but not statements such as `if` or `for`, which produce no value. The result is converted to a string using the same rules as `String()`.',
        },
        {
          id: 'q-m05-02-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['strings'],
          prompt: 'How many lines does this string contain?',
          code: [
            'const s = `one',
            'two`;',
            'console.log(s.split("\\n").length);',
          ].join('\n'),
          options: ['1', '2', '3', '0'],
          correct: 1,
          optionExplanations: [
            'The line break in the source is preserved in the value.',
            'Correct — the newline between `one` and `two` is part of the string.',
            'There is only one line break, producing two pieces.',
            'Splitting always produces at least one piece.',
          ],
          explanation:
            'A template literal preserves line breaks exactly as written, so this string contains a newline between `one` and `two`. Splitting on newlines gives two pieces. This also means the indentation you add to line a template up with surrounding code becomes part of the string.',
        },
        {
          id: 'q-m05-02-3',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['strings', 'security'],
          prompt: 'Template literals escape interpolated values, making them safe to insert into HTML.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'No escaping of any kind is performed.',
            'Correct — interpolation inserts the text exactly as-is, tags included.',
          ],
          explanation:
            'Interpolation performs a plain string conversion and no escaping. Building HTML from user input with a template literal and assigning it to `innerHTML` is a cross-site scripting vulnerability. A *tagged* template can perform escaping, because the tag function receives the values separately from the fixed text.',
        },
      ],
    },
    summary:
      'Template literals use backticks and add interpolation via `${expression}` plus genuine multiline support. Anything inside `${}` must be an expression, and its result is converted to a string with the same rules as `String()` — so an object becomes `"[object Object]"` unless you stringify it deliberately. Multiline templates preserve every character between the backticks, including the indentation you added for alignment. Template literals have largely replaced `+` concatenation because the final spacing is visible in the source. They perform no escaping, so a template containing user text is not safe to assign to `innerHTML`. A tag function placed before a template receives the fixed parts and the values separately, which is how libraries such as styled-components work.',
    keyTakeaways: [
      '`${}` takes an expression, not a statement',
      'Multiline templates preserve line breaks and leading indentation',
      'Interpolation converts with `String()` rules — objects become `[object Object]`',
      'Template literals do no escaping and are not HTML-safe',
      'A tag function receives the literal parts and values separately',
    ],
    relatedLessons: ['l-m05-01', 'l-m05-03'],
    interviewConnections: [
      'What are template literals and what do they add over quotes?',
      'Can you put a statement inside ${}?',
      'Are template literals safe for building HTML?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m05-03',
    slug: 'indexes-length-immutability',
    moduleId: M,
    order: 3,
    title: 'Indexes, Length and Immutability',
    description: 'Reading individual characters, why `at(-1)` exists, and the consequence of strings never changing.',
    difficulty: DIFFICULTY.BEGINNER,
    estimatedMinutes: 13,
    xp: 25,
    topicIds: ['strings'],
    prerequisites: ['l-m05-02'],
    learningObjectives: [
      'Access characters by index with `[]`, `charAt` and `at`',
      'Use `at(-1)` to read from the end',
      'Explain what happens when you index out of range',
      'Explain why every string method returns a new string',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'A string is an ordered sequence, so each character has a position. Positions are **zero-based**: the first character is at index 0.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Three ways to read a character, and one that reads from the end.',
        code: [
          'const word = "JavaScript";',
          '',
          'console.log(word.length);      // 10',
          'console.log(word[0]);          // "J"',
          'console.log(word.charAt(0));   // "J"',
          'console.log(word.at(0));       // "J"',
          '',
          '// The last character',
          'console.log(word[word.length - 1]);  // "t"',
          'console.log(word.at(-1));            // "t"  — much clearer',
          'console.log(word.at(-2));            // "p"',
        ].join('\n'),
        output: ['10', 'J', 'J', 'J', 't', 't', 'p'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`at()` was added in ES2022 specifically to support negative indexes, which count backwards from the end. Before it, reading the last character required the awkward `word[word.length - 1]`, and reading the second-to-last was worse. `at(-1)` is a genuine readability improvement and works on arrays too.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Out-of-range access differs',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Three methods, three different answers for an invalid index.',
        code: [
          'const word = "abc";',
          '',
          'console.log(word[10]);        // undefined',
          'console.log(word.at(10));     // undefined',
          'console.log(word.charAt(10)); // ""  — an empty string',
          '',
          'console.log(word[-1]);        // undefined — brackets do not do negatives',
          'console.log(word.at(-1));     // "c"',
          'console.log(word.charAt(-1)); // ""',
        ].join('\n'),
        output: ['undefined', 'undefined', '', 'undefined', 'c', ''].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: '`charAt` returning `""` hides mistakes',
        body: [
          '`charAt` returns an empty string for an out-of-range index rather than `undefined`. An empty string is falsy, so a check like `if (!word.charAt(i))` cannot distinguish "past the end" from a legitimate character — and concatenating it silently produces nothing at all.',
          'Prefer `[]` or `at()`, both of which give `undefined`, which is much harder to mistake for real data. `charAt` exists mainly for compatibility with very old code.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Iterating over characters',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Three ways to walk a string.',
        code: [
          'const word = "code";',
          '',
          '// Index-based — gives you the position',
          'for (let i = 0; i < word.length; i++) {',
          '  console.log(i, word[i]);',
          '}',
          '',
          '// for...of — cleaner when you only need the character',
          'const letters = [];',
          'for (const letter of word) {',
          '  letters.push(letter.toUpperCase());',
          '}',
          'console.log(letters.join(""));',
          '',
          '// Spread into an array to use array methods',
          'console.log([...word].reverse().join(""));',
        ].join('\n'),
        output: ['0 c', '1 o', '2 d', '3 e', 'CODE', 'edoc'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Spreading a string into an array is a common idiom — it gives you the full set of array methods, which strings do not have. `[...word].reverse().join("")` is the standard way to reverse a string, since there is no `String.prototype.reverse`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Immutability, revisited',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'You met this in the Data Types module: strings cannot be modified. It is worth restating here because it governs how every method in the rest of this module behaves.',
          '**Every string method returns a new string. None of them modifies the original.** If you do not capture the return value, nothing has happened.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The single most common string mistake.',
        code: [
          'let text = "  hello  ";',
          '',
          'text.trim();               // return value discarded',
          'console.log(`[${text}]`);  // unchanged',
          '',
          'text = text.trim();        // capture the result',
          'console.log(`[${text}]`);',
          '',
          '// Methods chain, because each returns a new string',
          'const messy = "  Hello World  ";',
          'const clean = messy.trim().toLowerCase().replace(" ", "-");',
          'console.log(clean);',
          'console.log(`[${messy}]`); // the original is untouched',
        ].join('\n'),
        output: ['[  hello  ]', '[hello]', 'hello-world', '[  Hello World  ]'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Chaining is the natural consequence',
        body: [
          'Because each method returns a new string, the next method can be called straight onto that result. `text.trim().toLowerCase().split(" ")` reads as a pipeline of transformations, and no intermediate variable is needed.',
          'This is the same reason the non-mutating array methods chain so well, which you will see in the array module.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const s = "hello";',
          's.toUpperCase();',
          'const t = s.toUpperCase();',
          '',
          'console.log(s, t, s.at(-1));',
        ].join('\n'),
        options: [
          'HELLO HELLO O',
          'hello HELLO o',
          'hello hello o',
          'HELLO HELLO o',
        ],
        correct: 1,
        explanation:
          'The second line calls `toUpperCase()` and throws the result away, so `s` is untouched — strings are immutable and no method can change one in place. The third line captures the result into `t`, which is `"HELLO"`. And `s.at(-1)` reads the last character of the unchanged `s`, which is the lowercase `"o"`. Forgetting to capture a method\'s return value is the most common string mistake there is.',
      },
    ],
    exercises: [
      {
        id: 'ex-m05-03-a',
        title: 'Predict the discarded result',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['strings'],
        instructions: 'What does this print?',
        code: [
          'let name = "ada";',
          'name.toUpperCase();',
          'console.log(name);',
        ].join('\n'),
        options: ['ADA', 'ada', 'undefined', 'TypeError'],
        correct: 1,
        hints: [
          'Does any string method modify the string it is called on?',
          'What happened to the value `toUpperCase()` returned?',
        ],
        solution: 'ada',
        solutionExplanation:
          'Strings are immutable, so `toUpperCase()` cannot modify `name`. It builds and returns a new string `"ADA"`, and because nothing captures that return value it is immediately discarded. `name` still refers to the original. You need `name = name.toUpperCase();`. This is the single most common mistake beginners make with strings, and it applies to every string method without exception.',
      },
      {
        id: 'ex-m05-03-b',
        title: 'Read the last character',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 15,
        topicIds: ['strings'],
        instructions:
          'Write `lastChar(text)` that returns the final character of a string, or `undefined` if the string is empty. Use `at()`.',
        starterCode: [
          'function lastChar(text) {',
          '  // at() accepts negative indexes.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'returns the last character', body: 'expect(lastChar("hello")).toBe("o");' },
          { name: 'works on a single character', body: 'expect(lastChar("x")).toBe("x");' },
          { name: 'returns undefined for an empty string', body: 'expect(lastChar("")).toBe(undefined);' },
          { name: 'handles trailing spaces', body: 'expect(lastChar("hi ")).toBe(" ");' },
          { name: 'handles digits', body: 'expect(lastChar("abc123")).toBe("3");', hidden: true },
        ],
        hints: [
          '`at(-1)` reads the last character.',
          'For an empty string, `at(-1)` already returns `undefined`.',
          'The whole function is one line.',
        ],
        solution: [
          'function lastChar(text) {',
          '  return text.at(-1);',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`at(-1)` counts backwards from the end, and returns `undefined` when the index is out of range — which handles the empty-string case for free. The older equivalent `text[text.length - 1]` also returns `undefined` for an empty string, but reads far less clearly. `text.charAt(text.length - 1)` would fail the empty test, since `charAt` returns `""` rather than `undefined` for an invalid index.',
      },
      {
        id: 'ex-m05-03-c',
        title: 'Check for a palindrome',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['strings'],
        instructions:
          'Write `isPalindrome(text)` that returns `true` if the text reads the same forwards and backwards, ignoring case. Assume the input contains only letters and spaces, and treat spaces as significant.',
        starterCode: [
          'function isPalindrome(text) {',
          '  // Normalise the case, then compare against the reversed string.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'detects a simple palindrome', body: 'expect(isPalindrome("racecar")).toBe(true);' },
          { name: 'ignores case', body: 'expect(isPalindrome("RaceCar")).toBe(true);' },
          { name: 'rejects a non-palindrome', body: 'expect(isPalindrome("hello")).toBe(false);' },
          { name: 'handles a single character', body: 'expect(isPalindrome("a")).toBe(true);' },
          { name: 'handles an empty string', body: 'expect(isPalindrome("")).toBe(true);' },
          { name: 'handles an even-length palindrome', body: 'expect(isPalindrome("abba")).toBe(true);' },
          { name: 'treats spaces as significant', body: 'expect(isPalindrome("ab ba")).toBe(true); expect(isPalindrome("a b")).toBe(false);', hidden: true },
        ],
        hints: [
          '`toLowerCase()` normalises the case.',
          'There is no `String.prototype.reverse` — spread into an array first.',
          '`[...text].reverse().join("")` produces the reversed string.',
        ],
        solution: [
          'function isPalindrome(text) {',
          '  const normalised = text.toLowerCase();',
          '  const reversed = [...normalised].reverse().join("");',
          '  return normalised === reversed;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Normalising the case first means the comparison only has to check character order. Since strings have no `reverse` method, the standard idiom is to spread into an array, reverse that array, and join it back — the spread is what makes each character an element. Note that `reverse` mutates the array it is called on, which is harmless here because the spread created a fresh one. An alternative approach compares characters from both ends inward with a loop, which avoids building a second string and is more efficient for very long input, though less readable.',
      },
    ],
    quiz: {
      id: 'qz-m05-03',
      questions: [
        {
          id: 'q-m05-03-1',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['strings'],
          prompt: 'What do these two out-of-range accesses return?',
          code: [
            'const s = "abc";',
            'console.log(s[10], s.charAt(10));',
          ].join('\n'),
          options: [
            'undefined undefined',
            'undefined ""',
            '"" ""',
            'Both throw',
          ],
          correct: 1,
          optionExplanations: [
            '`charAt` differs deliberately.',
            'Correct — bracket access gives `undefined`, `charAt` gives an empty string.',
            'Bracket access does not return an empty string.',
            'Neither throws for an out-of-range index.',
          ],
          explanation:
            'Bracket access and `at()` both return `undefined` for an index past the end. `charAt` returns an empty string instead, which is falsy and easily mistaken for real data — one reason to prefer `[]` or `at()`.',
        },
        {
          id: 'q-m05-03-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['strings'],
          prompt: 'Why was `at()` added when bracket access already existed?',
          options: [
            'Because bracket access is deprecated',
            'Because `at()` supports negative indexes, so `at(-1)` reads the last character',
            'Because `at()` is faster',
            'Because bracket access does not work on strings',
          ],
          correct: 1,
          optionExplanations: [
            'Bracket access is entirely current.',
            'Correct — it replaces the awkward `s[s.length - 1]`.',
            'Performance was not the motivation.',
            'Bracket access works fine on strings.',
          ],
          explanation:
            '`s[-1]` gives `undefined`, because bracket access treats `-1` as a property name rather than a position from the end. `at(-1)` counts backwards, making "the last character" readable. It works on arrays too.',
        },
        {
          id: 'q-m05-03-3',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['strings'],
          prompt: 'Calling `text.trim()` removes the surrounding whitespace from `text`.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'Strings are immutable; no method can modify one in place.',
            'Correct — it returns a new trimmed string, leaving the original alone.',
          ],
          explanation:
            'Every string method returns a new string and leaves the original untouched. `text.trim()` on its own has no visible effect; you must write `text = text.trim()` or use the returned value. This is the most common string mistake there is.',
        },
      ],
    },
    summary:
      'Characters are read by zero-based index using `[]`, `charAt()` or `at()`. `at()` accepts negative indexes, so `at(-1)` gives the last character and replaces the awkward `s[s.length - 1]`. Out-of-range behaviour differs: `[]` and `at()` return `undefined` while `charAt` returns an empty string, which is falsy and easy to mistake for data — prefer the first two. Strings can be walked with an index loop, `for...of`, or by spreading into an array to gain array methods, which is how you reverse a string since there is no `reverse` on strings. Above all: strings are immutable, so every method returns a new string and modifies nothing. Failing to capture that return value is the most common string mistake there is.',
    keyTakeaways: [
      'Indexes are zero-based; `length` counts characters',
      '`at(-1)` reads the last character; `[-1]` does not',
      '`charAt` returns `""` out of range; `[]` and `at()` return `undefined`',
      'Spread into an array to reverse or use array methods',
      'Every string method returns a new string — capture the result',
    ],
    relatedLessons: ['l-m02-01', 'l-m05-04'],
    interviewConnections: [
      'Are strings mutable in JavaScript?',
      'How do you get the last character of a string?',
      'How would you reverse a string?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m05-04',
    slug: 'searching-strings',
    moduleId: M,
    order: 4,
    title: 'Searching Within Strings',
    description: 'includes, indexOf, startsWith, endsWith — which to reach for, and the -1 trap.',
    difficulty: DIFFICULTY.BEGINNER,
    estimatedMinutes: 13,
    xp: 25,
    topicIds: ['strings'],
    prerequisites: ['l-m05-03'],
    learningObjectives: [
      'Test for a substring with `includes`, `startsWith` and `endsWith`',
      'Find a position with `indexOf` and `lastIndexOf`',
      'Explain why `indexOf` returning `0` breaks truthiness checks',
      'Choose the clearest search method for a given task',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'There are two families of search method: those that answer **"is it there?"** with a boolean, and those that answer **"where is it?"** with a position. Reach for the boolean family unless you actually need the position.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The boolean family',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'All three return true or false, and all are case sensitive.',
        code: [
          'const email = "ada@example.com";',
          '',
          'console.log(email.includes("@"));            // true',
          'console.log(email.includes("example"));      // true',
          'console.log(email.startsWith("ada"));        // true',
          'console.log(email.endsWith(".com"));         // true',
          '',
          '// Case sensitive',
          'console.log(email.includes("EXAMPLE"));      // false',
          '',
          '// Optional position argument',
          'console.log(email.includes("ada", 1));       // false — search starts at index 1',
          'console.log(email.startsWith("example", 4)); // true  — treat index 4 as the start',
        ].join('\n'),
        output: ['true', 'true', 'true', 'true', 'false', 'false', 'true'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Case-insensitive searching',
        body: [
          'None of these methods has a case-insensitive option. Normalise both sides first: `text.toLowerCase().includes(term.toLowerCase())`.',
          'That is correct for English and most Latin-script text. For a genuinely international application, `toLocaleLowerCase()` handles locale-specific rules — notably Turkish, where the uppercase of `i` is `İ` rather than `I`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The position family',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: '`indexOf` returns the position, or `-1` when not found.',
        code: [
          'const text = "the cat sat on the mat";',
          '',
          'console.log(text.indexOf("cat"));       // 4',
          'console.log(text.indexOf("the"));       // 0   — the first occurrence',
          'console.log(text.lastIndexOf("the"));   // 15  — the last occurrence',
          'console.log(text.indexOf("dog"));       // -1  — not found',
          '',
          '// Search from a position',
          'console.log(text.indexOf("the", 1));    // 15 — skips the first one',
        ].join('\n'),
        output: ['4', '0', '15', '-1', '15'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'The `-1` trap',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`indexOf` signals "not found" with `-1`, which is a **truthy** number. And when the match is at the very start, it returns `0`, which is **falsy**. Both facts break the obvious truthiness check, and they break it in opposite directions.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The wrong check gets both cases backwards.',
        code: [
          'const text = "hello world";',
          '',
          '// Wrong: -1 is truthy, so "not found" reads as "found"',
          'if (text.indexOf("xyz")) {',
          '  console.log("WRONG: reports found, but it is not there");',
          '}',
          '',
          '// Wrong: 0 is falsy, so a match at the start reads as "not found"',
          'if (text.indexOf("hello")) {',
          '  console.log("this does not run");',
          '} else {',
          '  console.log("WRONG: reports missing, but it is at index 0");',
          '}',
          '',
          '// Right:',
          'console.log(text.indexOf("hello") !== -1);',
          'console.log(text.includes("hello"));',
        ].join('\n'),
        output: [
          'WRONG: reports found, but it is not there',
          'WRONG: reports missing, but it is at index 0',
          'true',
          'true',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'This is why `includes` was added',
        body: [
          '`includes` arrived in ES2015 precisely because `indexOf(x) !== -1` is easy to get wrong and awkward to read. If you only need to know whether something is present, `includes` is unambiguous and cannot be misused this way.',
          'The same trap and the same fix apply to `Array.prototype.indexOf` and `Array.prototype.includes`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Choosing a method',
      },
      {
        kind: SECTION.TABLE,
        headers: ['Question', 'Use', 'Returns'],
        rows: [
          ['Does it contain this?', '`includes`', 'boolean'],
          ['Does it begin with this?', '`startsWith`', 'boolean'],
          ['Does it end with this?', '`endsWith`', 'boolean'],
          ['Where is the first occurrence?', '`indexOf`', 'index, or `-1`'],
          ['Where is the last occurrence?', '`lastIndexOf`', 'index, or `-1`'],
          ['Does it match a pattern?', '`RegExp.test`', 'boolean'],
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A practical example: extracting a file extension.',
        code: [
          'function getExtension(filename) {',
          '  const dot = filename.lastIndexOf(".");',
          '',
          '  // No dot, or the name starts with one (a hidden file)',
          '  if (dot <= 0) return "";',
          '',
          '  return filename.slice(dot + 1).toLowerCase();',
          '}',
          '',
          'console.log(getExtension("photo.JPG"));',
          'console.log(getExtension("archive.tar.gz"));',
          'console.log(getExtension("README"));',
          'console.log(getExtension(".gitignore"));',
        ].join('\n'),
        output: ['jpg', 'gz', '', ''].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`lastIndexOf` is the right choice here because a filename may contain several dots and only the final one separates the extension. The `dot <= 0` guard handles two cases at once: `-1` means there is no dot at all, and `0` means the name begins with a dot, which is a hidden file rather than a bare extension.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const s = "hello";',
          '',
          'console.log(s.indexOf("h"), s.includes("h"));',
          'console.log(Boolean(s.indexOf("h")), Boolean(s.indexOf("z")));',
        ].join('\n'),
        options: [
          '0 true / false true',
          '0 true / true false',
          '1 true / true false',
          '0 true / true true',
        ],
        correct: 0,
        explanation:
          '`indexOf("h")` returns `0`, since `h` is the first character, and `includes` returns `true`. The second line shows the trap: `Boolean(0)` is `false` even though the character was found, and `Boolean(-1)` is `true` even though `z` was not found. The truthiness of an `indexOf` result is exactly backwards from what you want in both cases, which is why `includes` exists.',
      },
    ],
    exercises: [
      {
        id: 'ex-m05-04-a',
        title: 'Predict the indexOf result',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['strings'],
        instructions: 'What does this print?',
        code: ['console.log("hello".indexOf("z"));'].join('\n'),
        options: ['0', '-1', 'undefined', 'null'],
        correct: 1,
        hints: [
          '`indexOf` returns a position. What position means "not present"?',
        ],
        solution: '-1',
        solutionExplanation:
          '`indexOf` returns `-1` when the search value is not found, because every valid position is zero or greater. The awkward part is that `-1` is truthy, so `if (str.indexOf(x))` reports "found" for a missing value — and `0`, a genuine match at the start, is falsy and reports "not found". Always compare explicitly with `!== -1`, or use `includes` instead.',
      },
      {
        id: 'ex-m05-04-b',
        title: 'Fix the search check',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['strings'],
        instructions:
          'This function should report whether the text contains the term. It currently returns the wrong answer when the term is missing, and also when the term is at the very start. Fix it.',
        starterCode: [
          'function contains(text, term) {',
          '  if (text.indexOf(term)) {',
          '    return true;',
          '  }',
          '  return false;',
          '}',
        ].join('\n'),
        tests: [
          { name: 'finds a term in the middle', body: 'expect(contains("hello world", "world")).toBe(true);' },
          { name: 'finds a term at the start', body: 'expect(contains("hello world", "hello")).toBe(true);' },
          { name: 'reports a missing term as false', body: 'expect(contains("hello world", "xyz")).toBe(false);' },
          { name: 'finds a single character', body: 'expect(contains("abc", "a")).toBe(true);' },
          { name: 'is case sensitive', body: 'expect(contains("abc", "A")).toBe(false);' },
          { name: 'an empty term is always found', body: 'expect(contains("abc", "")).toBe(true);', hidden: true },
        ],
        hints: [
          '`indexOf` returns `0` for a match at the start, and `0` is falsy.',
          'It returns `-1` when missing, and `-1` is truthy.',
          '`includes` answers this question directly and cannot be misused.',
        ],
        solution: [
          'function contains(text, term) {',
          '  return text.includes(term);',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`includes` returns a boolean directly, so there is no numeric result to misinterpret. The original was wrong in both directions: a match at index `0` is falsy and reported `false`, while a missing term returned `-1` which is truthy and reported `true`. Writing `return text.indexOf(term) !== -1;` would also be correct and is what you would use in code that must support very old environments — but `includes` says what it means. The hidden test reflects a small specification detail: every string contains the empty string, so both approaches return `true` there.',
      },
      {
        id: 'ex-m05-04-c',
        title: 'Write a case-insensitive search',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['strings'],
        instructions:
          'Write `search(items, term)` that returns the array items whose text contains the term, ignoring case. Surrounding whitespace in the term should be ignored. An empty or whitespace-only term should return all items.',
        starterCode: [
          'function search(items, term) {',
          '  // Normalise both sides before comparing.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'matches case-insensitively', body: 'expect(search(["Apple", "Banana"], "apple")).toEqual(["Apple"]);' },
          { name: 'matches a partial term', body: 'expect(search(["Apple", "Pineapple"], "APP")).toEqual(["Apple", "Pineapple"]);' },
          { name: 'trims the search term', body: 'expect(search(["Apple"], "  apple  ")).toEqual(["Apple"]);' },
          { name: 'returns everything for an empty term', body: 'expect(search(["a", "b"], "")).toEqual(["a", "b"]);' },
          { name: 'returns everything for a whitespace term', body: 'expect(search(["a", "b"], "   ")).toEqual(["a", "b"]);' },
          { name: 'returns an empty array when nothing matches', body: 'expect(search(["Apple"], "zzz")).toEqual([]);' },
          { name: 'handles an empty item list', body: 'expect(search([], "a")).toEqual([]);', hidden: true },
        ],
        hints: [
          'Trim and lowercase the term once, outside the loop.',
          '`filter` keeps the items whose callback returns true.',
          'Lowercase each item before calling `includes`.',
        ],
        solution: [
          'function search(items, term) {',
          '  const needle = term.trim().toLowerCase();',
          '  if (needle === "") return [...items];',
          '',
          '  return items.filter((item) => item.toLowerCase().includes(needle));',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Normalising the term once before the loop rather than inside it avoids repeating the same work for every item — a habit worth forming even when the cost is small. The explicit empty check returns a copy of the array rather than the array itself, so the caller cannot accidentally mutate the original through the returned reference. Strictly the check is not required, since every string contains the empty string and `filter` would keep everything anyway, but stating the intent makes the behaviour obvious to a reader rather than something they must deduce.',
      },
    ],
    quiz: {
      id: 'qz-m05-04',
      questions: [
        {
          id: 'q-m05-04-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['strings'],
          prompt: 'Why is `if (text.indexOf(term))` a bug?',
          options: [
            'Because `indexOf` is deprecated',
            'Because `-1` for "not found" is truthy, and `0` for a match at the start is falsy',
            'Because `indexOf` returns a string',
            'Because `indexOf` is case insensitive',
          ],
          correct: 1,
          optionExplanations: [
            '`indexOf` is entirely current and useful when you need a position.',
            'Correct — the truthiness is backwards in both directions.',
            'It returns a number.',
            'It is case sensitive.',
          ],
          explanation:
            'A missing term gives `-1`, which is truthy, so the check reports "found". A match at index `0` gives `0`, which is falsy, so the check reports "missing". Compare with `!== -1`, or use `includes`, which returns a boolean.',
        },
        {
          id: 'q-m05-04-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['strings'],
          prompt: 'What does this print?',
          code: [
            'const t = "the cat sat on the mat";',
            'console.log(t.indexOf("the"), t.lastIndexOf("the"));',
          ].join('\n'),
          options: ['0 0', '0 14', '14 0', '0 -1'],
          correct: 1,
          optionExplanations: [
            '`lastIndexOf` searches from the end.',
            'Correct — the first occurrence is at 0 and the last at 14.',
            'This has the two methods the wrong way round.',
            'Both occurrences exist, so neither returns -1.',
          ],
          explanation:
            '`indexOf` returns the position of the first match, `lastIndexOf` the position of the last. Both return `-1` when there is no match at all. `lastIndexOf` is the right choice for things like finding a file extension, where only the final separator matters.',
        },
        {
          id: 'q-m05-04-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['strings'],
          prompt: 'How do you perform a case-insensitive `includes` check?',
          options: [
            'Pass a second argument: `text.includes(term, false)`',
            'Normalise both sides: `text.toLowerCase().includes(term.toLowerCase())`',
            'Use `text.includesIgnoreCase(term)`',
            'It is case insensitive by default',
          ],
          correct: 1,
          optionExplanations: [
            'The second argument is a starting position, not a case flag.',
            'Correct — normalise both sides before comparing.',
            'No such method exists.',
            'All the string search methods are case sensitive.',
          ],
          explanation:
            'None of the search methods offer a case-insensitive mode, so you lowercase both the text and the term before comparing. For international text, `toLocaleLowerCase()` respects locale rules — notably Turkish, where `i` uppercases to `İ`.',
        },
      ],
    },
    summary:
      'Search methods split into two families. `includes`, `startsWith` and `endsWith` return booleans and are what you want when the question is "is it there?". `indexOf` and `lastIndexOf` return a position, or `-1` when absent — and that sentinel is the trap, because `-1` is truthy while a legitimate match at index `0` is falsy, so a bare truthiness check is wrong in both directions. Compare explicitly with `!== -1`, or prefer `includes`, which is exactly why it was added. All these methods are case sensitive, so normalise both sides with `toLowerCase()` for a case-insensitive search. `lastIndexOf` is the right tool when only the final occurrence matters, such as separating a file extension.',
    keyTakeaways: [
      'Use `includes` / `startsWith` / `endsWith` for yes-or-no questions',
      'Use `indexOf` / `lastIndexOf` only when you need the position',
      '`indexOf` returns `-1` when missing — truthy — and `0` at the start — falsy',
      'Compare with `!== -1`, or use `includes`',
      'All search methods are case sensitive; normalise both sides',
    ],
    relatedLessons: ['l-m05-03', 'l-m05-05'],
    interviewConnections: [
      'What is the difference between includes and indexOf?',
      'Why is `if (str.indexOf(x))` a bug?',
      'How do you search a string case-insensitively?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m05-05',
    slug: 'extracting-and-transforming',
    moduleId: M,
    order: 5,
    title: 'Extracting and Transforming Strings',
    description: 'slice versus substring, case conversion, trimming, padding and repeating.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 15,
    xp: 30,
    topicIds: ['strings'],
    prerequisites: ['l-m05-04'],
    learningObjectives: [
      'Extract substrings with `slice`, including negative indexes',
      'Explain how `substring` differs from `slice` and why `slice` is preferred',
      'Trim and pad strings for display',
      'Combine methods into readable transformation chains',
    ],
    sections: [
      {
        kind: SECTION.HEADING,
        text: 'slice',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`slice(start, end)` returns the characters from `start` up to **but not including** `end`. Omitting `end` takes everything to the finish. Negative values count from the end.',
          'The "up to but not including" rule means `end - start` gives the length of the result, which is a useful check.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'slice, including the negative forms.',
        code: [
          'const text = "JavaScript";',
          '',
          'console.log(text.slice(0, 4));    // "Java"',
          'console.log(text.slice(4));       // "Script"',
          'console.log(text.slice(-6));      // "Script" — last six characters',
          'console.log(text.slice(-6, -3));  // "Scr"',
          'console.log(text.slice(0, -6));   // "Java"  — all but the last six',
          '',
          '// An impossible range gives an empty string rather than an error',
          'console.log(`[${text.slice(5, 2)}]`);',
        ].join('\n'),
        output: ['Java', 'Script', 'Script', 'Scr', 'Java', '[]'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'substring, and why to avoid it',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The same call, two very different results.',
        code: [
          'const text = "JavaScript";',
          '',
          '// substring silently swaps arguments that are the wrong way round',
          'console.log(text.slice(5, 2));      // ""     — honest',
          'console.log(text.substring(5, 2));  // "vaS"  — swapped to (2, 5)',
          '',
          '// substring treats negatives as 0',
          'console.log(text.slice(-6));        // "Script"',
          'console.log(text.substring(-6));    // "JavaScript"',
        ].join('\n'),
        output: ['', 'vaS', 'Script', 'JavaScript'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Prefer `slice`',
        body: [
          '`substring` silently swaps its arguments when `start > end`, and silently converts negative indexes to `0`. Both behaviours hide bugs: a calculation that produced a reversed range or a negative index should give you an obviously wrong empty result, not a plausible-looking substring from somewhere else entirely.',
          '`slice` is also consistent with `Array.prototype.slice`, so one mental model covers both. There is no situation where `substring` is the better choice.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Case conversion',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Case conversion, and capitalising a word.',
        code: [
          'console.log("Hello".toUpperCase());',
          'console.log("Hello".toLowerCase());',
          '',
          '// There is no capitalise method — build it from slice',
          'function capitalise(word) {',
          '  if (word.length === 0) return word;',
          '  return word[0].toUpperCase() + word.slice(1).toLowerCase();',
          '}',
          '',
          'console.log(capitalise("hello"));',
          'console.log(capitalise("hELLO"));',
          'console.log(`[${capitalise("")}]`);',
        ].join('\n'),
        output: ['HELLO', 'hello', 'Hello', 'Hello', '[]'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Trimming',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Removing whitespace from the ends.',
        code: [
          'const messy = "   hello   ";',
          '',
          'console.log(`[${messy.trim()}]`);',
          'console.log(`[${messy.trimStart()}]`);',
          'console.log(`[${messy.trimEnd()}]`);',
          '',
          '// Tabs and newlines count as whitespace too',
          'console.log(`[${"\\n\\t hi \\t\\n".trim()}]`);',
          '',
          '// Inner whitespace is untouched',
          'console.log(`[${"  a  b  ".trim()}]`);',
        ].join('\n'),
        output: ['[hello]', '[hello   ]', '[   hello]', '[hi]', '[a  b]'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'Always trim user input',
        body: [
          'Trailing spaces from a copy-paste are invisible and cause real problems: a username comparison fails, a lookup misses, a validation check passes when it should not. Trimming at the point where input enters your program — the same boundary principle from the coercion module — removes an entire class of confusing bugs.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Padding and repeating',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Padding for alignment, repeating for separators.',
        code: [
          '// padStart is ideal for numbers and time',
          'console.log("5".padStart(2, "0"));',
          'console.log("7".padStart(3, "0"));',
          'console.log(String(42).padStart(5, "0"));',
          '',
          '// padEnd aligns a column',
          'console.log("Name".padEnd(10, ".") + "Ada");',
          'console.log("Role".padEnd(10, ".") + "Engineer");',
          '',
          '// Padding never truncates',
          'console.log("toolong".padStart(3, "0"));',
          '',
          'console.log("-".repeat(20));',
        ].join('\n'),
        output: [
          '05', '007', '00042',
          'Name......Ada',
          'Role......Engineer',
          'toolong',
          '--------------------',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Note that padding never truncates: if the string is already at or beyond the target length it is returned unchanged. That makes `padStart` safe for formatting values of unpredictable size, but it also means you cannot rely on it to enforce a maximum width — combine it with `slice` if you need that.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Chaining transformations',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A URL slug, built as a pipeline.',
        code: [
          'function toSlug(title) {',
          '  return title',
          '    .trim()',
          '    .toLowerCase()',
          '    .replaceAll(" ", "-");',
          '}',
          '',
          'console.log(toSlug("  Hello World  "));',
          'console.log(toSlug("Learning JavaScript Strings"));',
        ].join('\n'),
        output: ['hello-world', 'learning-javascript-strings'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Each method returns a new string, so the next call operates on that result. Reading top to bottom describes the transformation in order, which is far clearer than nesting the calls inside one another.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const t = "JavaScript";',
          '',
          'console.log(t.slice(4, 2), t.substring(4, 2));',
        ].join('\n'),
        options: [
          '"" "aS"',
          '"aS" "aS"',
          '"" ""',
          '"Sc" "Sc"',
        ],
        correct: 0,
        explanation:
          '`slice(4, 2)` has a start after its end, which describes an impossible range, so it honestly returns an empty string. `substring(4, 2)` silently swaps the arguments to `(2, 4)` and returns the characters at indexes 2 and 3 — `"aS"`. That silent swap is exactly why `slice` is preferred: a reversed range almost always means a calculation went wrong upstream, and an empty result makes that visible.',
      },
    ],
    exercises: [
      {
        id: 'ex-m05-05-a',
        title: 'Predict the negative slice',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['strings'],
        instructions: 'What does this print?',
        code: ['console.log("JavaScript".slice(-6));'].join('\n'),
        options: ['"Script"', '"JavaSc"', '"Java"', '""'],
        correct: 0,
        hints: [
          'A negative start counts backwards from the end.',
          'How many characters are taken when the end is omitted?',
        ],
        solution: '"Script"',
        solutionExplanation:
          'A negative start index counts from the end of the string, so `-6` means "six characters from the end". With no end argument, `slice` takes everything from there to the finish — the last six characters, `"Script"`. This is the idiomatic way to take the final N characters, and it is far clearer than `text.slice(text.length - 6)`. Note that `substring(-6)` would give the whole string instead, since it converts negatives to `0`.',
      },
      {
        id: 'ex-m05-05-b',
        title: 'Truncate with an ellipsis',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['strings'],
        instructions:
          'Write `truncate(text, maxLength)` that shortens text to at most `maxLength` characters, appending `"..."` when it had to shorten. The returned string must never exceed `maxLength`, so the ellipsis counts towards the limit. If the text already fits, return it unchanged.',
        starterCode: [
          'function truncate(text, maxLength) {',
          '  // The ellipsis is three characters and counts towards the limit.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'leaves short text alone', body: 'expect(truncate("hello", 10)).toBe("hello");' },
          { name: 'leaves exact-length text alone', body: 'expect(truncate("hello", 5)).toBe("hello");' },
          { name: 'truncates and appends an ellipsis', body: 'expect(truncate("hello world", 8)).toBe("hello...");' },
          { name: 'never exceeds the limit', body: 'expect(truncate("abcdefghij", 6).length).toBe(6);' },
          { name: 'handles an empty string', body: 'expect(truncate("", 5)).toBe("");' },
          { name: 'handles a very small limit', body: 'expect(truncate("abcdef", 4)).toBe("a...");', hidden: true },
        ],
        hints: [
          'First check whether truncation is needed at all.',
          'Keep `maxLength - 3` characters, then append the ellipsis.',
          '`slice(0, n)` takes the first n characters.',
        ],
        solution: [
          'function truncate(text, maxLength) {',
          '  if (text.length <= maxLength) return text;',
          '  return text.slice(0, maxLength - 3) + "...";',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The guard clause handles the common case first and returns the original untouched, which also covers the empty-string test. When truncation is needed, keeping `maxLength - 3` characters leaves exactly enough room for the ellipsis, so the result is always exactly `maxLength` long — which the fourth test verifies. A subtlety worth noticing: this implementation assumes `maxLength` is at least 3. For a smaller limit, `maxLength - 3` goes negative and `slice` would count from the end, producing something unexpected; a production version would clamp that.',
      },
      {
        id: 'ex-m05-05-c',
        title: 'Build a title-case converter',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 20,
        topicIds: ['strings'],
        instructions:
          'Write `toTitleCase(text)` that capitalises the first letter of each word and lowercases the rest. Words are separated by single spaces. `toTitleCase("hELLO wORLD")` should give `"Hello World"`.',
        starterCode: [
          'function toTitleCase(text) {',
          '  // split into words, transform each, join back together.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'capitalises each word', body: 'expect(toTitleCase("hello world")).toBe("Hello World");' },
          { name: 'lowercases the rest of each word', body: 'expect(toTitleCase("hELLO wORLD")).toBe("Hello World");' },
          { name: 'handles a single word', body: 'expect(toTitleCase("javascript")).toBe("Javascript");' },
          { name: 'handles an empty string', body: 'expect(toTitleCase("")).toBe("");' },
          { name: 'handles three words', body: 'expect(toTitleCase("the quick fox")).toBe("The Quick Fox");' },
          { name: 'leaves already-correct text correct', body: 'expect(toTitleCase("Hello World")).toBe("Hello World");' },
          { name: 'handles words containing digits', body: 'expect(toTitleCase("es6 modules")).toBe("Es6 Modules");', hidden: true },
        ],
        hints: [
          '`text.split(" ")` gives an array of words.',
          'Use `map` to transform each word.',
          'For each word: `word[0].toUpperCase() + word.slice(1).toLowerCase()`.',
          'Guard against empty words so `word[0]` does not fail.',
        ],
        solution: [
          'function toTitleCase(text) {',
          '  return text',
          '    .split(" ")',
          '    .map((word) => {',
          '      if (word.length === 0) return word;',
          '      return word[0].toUpperCase() + word.slice(1).toLowerCase();',
          '    })',
          '    .join(" ");',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The split-map-join pipeline is the standard shape for per-word string transformations, and you will use it constantly. The empty-word guard matters: splitting `""` produces `[""]`, and `word[0]` on an empty string is `undefined`, so `undefined.toUpperCase()` would throw a TypeError — the empty-string test would fail without it. Lowercasing the remainder with `slice(1).toLowerCase()` is what makes `"hELLO"` come out as `"Hello"` rather than `"HELLO"`. Note this simple version treats consecutive spaces as producing empty words, which the guard handles gracefully.',
      },
    ],
    quiz: {
      id: 'qz-m05-05',
      questions: [
        {
          id: 'q-m05-05-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['strings'],
          prompt: 'Why is `slice` preferred over `substring`?',
          options: [
            '`substring` is deprecated',
            '`slice` supports negative indexes and does not silently swap reversed arguments',
            '`slice` is faster',
            '`substring` only works on arrays',
          ],
          correct: 1,
          optionExplanations: [
            '`substring` is not deprecated, merely worse.',
            'Correct — its behaviour is honest where `substring` hides mistakes.',
            'Performance is not the distinction.',
            '`substring` is a string method; `slice` works on both strings and arrays.',
          ],
          explanation:
            '`substring` silently swaps `start` and `end` when they are the wrong way round, and converts negative indexes to `0`. Both behaviours turn a calculation bug into a plausible-looking wrong result. `slice` returns an empty string for an impossible range and supports negatives, and it matches `Array.prototype.slice`.',
        },
        {
          id: 'q-m05-05-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['strings'],
          prompt: 'What does this padding produce?',
          code: ['console.log("toolong".padStart(3, "0"));'].join('\n'),
          options: ['"000"', '"too"', '"toolong"', '"0toolong"'],
          correct: 2,
          optionExplanations: [
            'Padding does not replace the original content.',
            'Padding never truncates.',
            'Correct — the string already exceeds the target length, so it is returned unchanged.',
            'No padding is added when the string is already long enough.',
          ],
          explanation:
            '`padStart` and `padEnd` only add characters when the string is shorter than the target length. They never truncate, so a longer string is returned unchanged. Combine with `slice` if you need to enforce a maximum width as well.',
        },
        {
          id: 'q-m05-05-3',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['strings'],
          prompt: 'Which of these return `"Script"` from `"JavaScript"`? (Select all that apply.)',
          options: [
            '`"JavaScript".slice(4)`',
            '`"JavaScript".slice(-6)`',
            '`"JavaScript".substring(-6)`',
            '`"JavaScript".slice(4, 10)`',
          ],
          correct: [0, 1, 3],
          explanation:
            'Slicing from index 4 to the end gives `"Script"`, as does taking the last six characters with `-6`, as does the explicit range 4 to 10. `substring(-6)` is the odd one out: it converts the negative index to `0` and returns the entire string, which is exactly the silent behaviour that makes `slice` the better default.',
        },
      ],
    },
    summary:
      '`slice(start, end)` extracts up to but not including `end`, supports negative indexes counting from the end, and returns an empty string for an impossible range. `substring` should be avoided: it silently swaps reversed arguments and converts negatives to `0`, turning calculation bugs into plausible-looking wrong results. Case conversion has no built-in capitalise, so build one from `slice`. Trim user input at the boundary — invisible trailing whitespace causes an entire class of confusing bugs. `padStart` and `padEnd` align output and never truncate, and `repeat` builds separators. Because every method returns a new string, transformations chain naturally into a readable top-to-bottom pipeline.',
    keyTakeaways: [
      '`slice(start, end)` excludes `end` and accepts negative indexes',
      'Avoid `substring` — it swaps reversed arguments and ignores negatives',
      '`slice(-n)` takes the last n characters',
      'Trim user input at the boundary; invisible whitespace causes real bugs',
      'Padding never truncates',
      'Methods chain because each returns a new string',
    ],
    relatedLessons: ['l-m05-04', 'l-m05-06'],
    interviewConnections: [
      'What is the difference between slice and substring?',
      'How would you capitalise the first letter of a word?',
      'How do you take the last N characters of a string?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m05-06',
    slug: 'splitting-replacing-unicode',
    moduleId: M,
    order: 6,
    title: 'Splitting, Replacing and Unicode Reality',
    description: 'Turning strings into arrays and back, replacing every occurrence, and why `length` sometimes lies.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 16,
    xp: 35,
    topicIds: ['strings'],
    prerequisites: ['l-m05-05'],
    learningObjectives: [
      'Split strings into arrays and join them back',
      'Explain why `replace` only replaces the first match',
      'Use `replaceAll` and a global regex correctly',
      'Explain why `"👋".length` is 2 and when it matters',
    ],
    sections: [
      {
        kind: SECTION.HEADING,
        text: 'split and join',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Strings to arrays and back.',
        code: [
          'const csv = "name,age,city";',
          'console.log(csv.split(","));',
          '',
          'const sentence = "the quick brown fox";',
          'console.log(sentence.split(" ").length);',
          '',
          '// An empty separator splits into characters',
          'console.log("abc".split(""));',
          '',
          '// No separator gives a single-element array',
          'console.log("abc".split());',
          '',
          '// A limit caps the number of pieces',
          'console.log("a,b,c,d".split(",", 2));',
          '',
          '// join is the inverse',
          'console.log(["a", "b", "c"].join("-"));',
        ].join('\n'),
        output: [
          '["name", "age", "city"]',
          '4',
          '["a", "b", "c"]',
          '["abc"]',
          '["a", "b"]',
          'a-b-c',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Do not parse real CSV with `split(",")`',
        body: [
          'It works for simple data and breaks the moment a field contains a comma inside quotes — `"Smith, Ada",36` splits into three pieces rather than two. Real CSV also allows escaped quotes and embedded newlines.',
          'For genuine CSV use a parser. `split` is for data whose format you control.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'replace and replaceAll',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This catches almost everyone once: **`replace` with a string pattern replaces only the first occurrence.**',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Three ways to replace, two of which handle every occurrence.',
        code: [
          'const text = "cat bat cat";',
          '',
          'console.log(text.replace("cat", "dog"));      // only the first',
          'console.log(text.replaceAll("cat", "dog"));   // every one',
          'console.log(text.replace(/cat/g, "dog"));     // every one, via a global regex',
          '',
          '// The original is unchanged, as always',
          'console.log(text);',
        ].join('\n'),
        output: ['dog bat cat', 'dog bat dog', 'dog bat dog', 'cat bat cat'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`replaceAll` arrived in ES2021 and is the clearest option. Before it, the standard approach was a regular expression with the `g` (global) flag — which you will still see everywhere and should be able to read.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: '`replaceAll` requires a global regex if you pass one',
        body: [
          'Passing a non-global regex to `replaceAll` throws a `TypeError` rather than silently replacing once. `text.replaceAll(/cat/, "dog")` is an error; `text.replaceAll(/cat/g, "dog")` is fine.',
          'This is deliberate — the method name promises "all", so a pattern that can only match once is treated as a mistake rather than quietly obeyed.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A replacement function receives each match.',
        code: [
          'const text = "I have 3 cats and 12 dogs";',
          '',
          '// A function receives each match and returns its replacement',
          'const doubled = text.replace(/\\d+/g, (match) => Number(match) * 2);',
          'console.log(doubled);',
          '',
          '// $& in a replacement string means "the matched text"',
          'console.log("price: 30".replace(/\\d+/, "[$&]"));',
        ].join('\n'),
        output: ['I have 6 cats and 24 dogs', 'price: [30]'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Unicode: why `length` sometimes lies',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'JavaScript strings are sequences of **UTF-16 code units**, not of characters as humans understand them. For English text the two coincide. For anything else they may not.',
          'Characters outside the basic range — most emoji, many CJK characters, mathematical symbols — need two code units, called a surrogate pair. `length` counts code units, so such a character counts as 2.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'One visible character, two code units.',
        code: [
          'console.log("abc".length);      // 3',
          'console.log("👋".length);        // 2  — one emoji, two code units',
          'console.log("é".length);         // 1',
          '',
          '// Indexing splits the pair and produces garbage',
          'console.log("👋"[0] === "👋");    // false',
          '',
          '// Spreading and for...of iterate by code point, not code unit',
          'console.log([..."👋"].length);    // 1',
          'console.log([..."a👋b"].length);  // 3',
          'console.log("a👋b".length);       // 4',
        ].join('\n'),
        output: ['3', '2', '1', 'false', '1', '3', '4'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The practical rule: **spreading and `for...of` iterate by code point; `length` and `[]` work in code units.** If you are handling text that may contain emoji or non-Latin scripts, prefer `[...text]` over an index loop.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Even code points are not the whole story',
        body: [
          'Some visible characters are built from several code points combined — a flag emoji, or a family emoji, or an accented letter written as a base letter plus a combining mark. For those, even `[...text].length` will not match what a human counts.',
          'When you genuinely need human-perceived character counts, `Intl.Segmenter` is the correct tool. Reaching for it is rare, but knowing the limitation exists prevents you from confidently shipping a character counter that is wrong for half your users.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The same text measured three ways.',
        code: [
          'const text = "héllo 👋";',
          '',
          'console.log("code units:", text.length);',
          'console.log("code points:", [...text].length);',
          '',
          '// Reversing with spread keeps the emoji intact',
          'console.log([...text].reverse().join(""));',
          '',
          '// Reversing by index would break it',
          'let broken = "";',
          'for (let i = text.length - 1; i >= 0; i--) broken += text[i];',
          'console.log("length of broken result:", broken.length);',
        ].join('\n'),
        output: [
          'code units: 8',
          'code points: 7',
          '👋 olléh',
          'length of broken result: 8',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Comparing strings',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: '`localeCompare` for human-facing sorting.',
        code: [
          '// Raw comparison uses code units',
          'console.log("apple" < "Banana");   // false — uppercase sorts first',
          '',
          '// localeCompare returns -1, 0 or 1',
          'console.log("apple".localeCompare("Banana"));',
          '',
          'const names = ["Émile", "Adam", "Zoe", "ada"];',
          'console.log([...names].sort());',
          'console.log([...names].sort((a, b) => a.localeCompare(b)));',
        ].join('\n'),
        output: [
          'false',
          '-1',
          '["Adam", "Zoe", "ada", "Émile"]',
          '["ada", "Adam", "Émile", "Zoe"]',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The default sort groups by code unit, which produces an order no reader would expect: every uppercase letter comes before every lowercase one, so `"Zoe"` beats `"ada"` and `"Adam"` is separated from `"ada"` entirely. `"Émile"` lands last, because `É` sits well above the plain Latin letters in the code-unit table.',
          '`localeCompare` sorts the way a person would: case-insensitively for ordering purposes, and with accented letters filed next to their unaccented counterparts. Use it for anything users will read.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const s = "a👋b";',
          '',
          'console.log(s.length, [...s].length, s.split("").length);',
        ].join('\n'),
        options: [
          '4 3 4',
          '3 3 3',
          '4 4 4',
          '4 3 3',
        ],
        correct: 0,
        explanation:
          '`length` counts UTF-16 code units, and the emoji needs two, so the total is 4. Spreading iterates by code point, correctly seeing three characters. And `split("")` splits by code unit like `length` does, giving 4 pieces — with the emoji broken into two useless halves. This is why `[...text]` is the safe way to get characters when text may contain emoji, and `split("")` is not.',
      },
    ],
    exercises: [
      {
        id: 'ex-m05-06-a',
        title: 'Predict the partial replace',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['strings'],
        instructions: 'What does this print?',
        code: ['console.log("a-b-c".replace("-", "+"));'].join('\n'),
        options: ['"a+b+c"', '"a+b-c"', '"a-b-c"', '"abc"'],
        correct: 1,
        hints: [
          'How many occurrences does `replace` handle when given a string pattern?',
        ],
        solution: '"a+b-c"',
        solutionExplanation:
          '`replace` with a string pattern replaces only the **first** occurrence, so just the first hyphen changes. To replace every occurrence use `replaceAll("-", "+")`, or a global regular expression `replace(/-/g, "+")`. This catches nearly everyone at least once, because the method name gives no hint that it stops after one.',
      },
      {
        id: 'ex-m05-06-b',
        title: 'Count words safely',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['strings'],
        instructions:
          'Write `countWords(text)` returning the number of words. Words are separated by any amount of whitespace, and leading or trailing whitespace must not count. An empty or whitespace-only string has zero words.',
        starterCode: [
          'function countWords(text) {',
          '  // Trim first, then split on runs of whitespace.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'counts simple words', body: 'expect(countWords("the quick fox")).toBe(3);' },
          { name: 'ignores surrounding whitespace', body: 'expect(countWords("  hello world  ")).toBe(2);' },
          { name: 'handles multiple spaces between words', body: 'expect(countWords("a    b")).toBe(2);' },
          { name: 'returns 0 for an empty string', body: 'expect(countWords("")).toBe(0);' },
          { name: 'returns 0 for whitespace only', body: 'expect(countWords("    ")).toBe(0);' },
          { name: 'counts a single word', body: 'expect(countWords("hello")).toBe(1);' },
          { name: 'handles tabs and newlines', body: 'expect(countWords("a\\tb\\nc")).toBe(3);', hidden: true },
        ],
        hints: [
          'Trim the text before splitting, so leading whitespace does not create an empty first piece.',
          'Splitting on the literal `" "` fails for multiple spaces — split on the regex `/\\s+/`.',
          'After trimming, an empty string still splits into `[""]`, so check for it explicitly.',
        ],
        solution: [
          'function countWords(text) {',
          '  const trimmed = text.trim();',
          '  if (trimmed === "") return 0;',
          '  return trimmed.split(/\\s+/).length;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Three details make this correct. Trimming first prevents leading whitespace from producing an empty first element. Splitting on the regex `/\\s+/` rather than a literal space treats any run of whitespace — spaces, tabs, newlines — as a single separator, which is what the multiple-spaces and hidden tests check. And the explicit empty check is genuinely necessary: `"".split(/\\s+/)` returns `[""]`, an array of length 1, so without the guard an empty string would report one word. That last case is the one people miss.',
      },
      {
        id: 'ex-m05-06-c',
        title: 'Count characters correctly',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 20,
        topicIds: ['strings'],
        instructions:
          'Write `countCharacters(text)` that counts code points rather than UTF-16 code units, so that an emoji counts as one character rather than two.',
        starterCode: [
          'function countCharacters(text) {',
          '  // `length` counts code units. Something else iterates by code point.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'counts plain text', body: 'expect(countCharacters("hello")).toBe(5);' },
          { name: 'counts an emoji as one', body: 'expect(countCharacters("👋")).toBe(1);' },
          { name: 'counts mixed content', body: 'expect(countCharacters("a👋b")).toBe(3);' },
          { name: 'differs from length for emoji', body: 'expect(countCharacters("👋")).not.toBe("👋".length);' },
          { name: 'handles an empty string', body: 'expect(countCharacters("")).toBe(0);' },
          { name: 'handles accented characters', body: 'expect(countCharacters("héllo")).toBe(5);' },
          { name: 'counts two emoji as two', body: 'expect(countCharacters("👋🚀")).toBe(2);', hidden: true },
        ],
        hints: [
          'Spreading a string iterates by code point rather than code unit.',
          '`[...text]` produces an array of characters.',
          '`Array.from(text)` does the same thing.',
        ],
        solution: [
          'function countCharacters(text) {',
          '  return [...text].length;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Spread uses the string iterator, which is defined to walk code points rather than code units, so a surrogate pair is delivered as one element. `Array.from(text)` is exactly equivalent. `text.length` and `text.split("")` both work in code units and would count each emoji as 2. Worth being honest about the limit of this solution: code points are not the same as what a human perceives as a character. A flag emoji is two code points, and a family emoji can be seven, so this function would report those as several characters. `Intl.Segmenter` with `granularity: "grapheme"` is the tool for genuine human character counts.',
      },
      {
        id: 'ex-m05-06-d',
        title: 'Build a slug generator',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['strings'],
        instructions:
          'Write `slugify(title)` that turns a title into a URL slug: lowercase, words joined by single hyphens, with surrounding whitespace removed and runs of whitespace collapsed. `slugify("  Hello   World  ")` should give `"hello-world"`.',
        starterCode: [
          'function slugify(title) {',
          '  // trim, lowercase, then collapse whitespace runs into hyphens.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'creates a basic slug', body: 'expect(slugify("Hello World")).toBe("hello-world");' },
          { name: 'trims and collapses whitespace', body: 'expect(slugify("  Hello   World  ")).toBe("hello-world");' },
          { name: 'handles a single word', body: 'expect(slugify("JavaScript")).toBe("javascript");' },
          { name: 'handles three words', body: 'expect(slugify("The Quick Fox")).toBe("the-quick-fox");' },
          { name: 'returns an empty string for empty input', body: 'expect(slugify("")).toBe("");' },
          { name: 'returns an empty string for whitespace only', body: 'expect(slugify("   ")).toBe("");' },
          { name: 'handles tabs and newlines as separators', body: 'expect(slugify("a\\tb\\nc")).toBe("a-b-c");', hidden: true },
        ],
        hints: [
          'Trim first so the ends do not produce stray hyphens.',
          'Lowercase before or after splitting — either works.',
          'Replace runs of whitespace using the global regex `/\\s+/g`.',
          'Guard the empty case, or an empty input produces an unexpected result.',
        ],
        solution: [
          'function slugify(title) {',
          '  const trimmed = title.trim().toLowerCase();',
          '  if (trimmed === "") return "";',
          '  return trimmed.replace(/\\s+/g, "-");',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Trimming before replacing is what prevents leading and trailing hyphens — without it, `"  hi  "` would become `"-hi-"`. Using `/\\s+/g` with the `+` quantifier collapses each *run* of whitespace into a single hyphen, so multiple spaces do not produce multiple hyphens; the `g` flag makes it apply to every run rather than just the first. The empty guard is needed because `"".replace(...)` returns `""` correctly but the trimmed-whitespace case would otherwise depend on subtle behaviour — stating it explicitly is clearer. A production slugifier would go further and strip punctuation and accents, but this covers the core transformation.',
      },
    ],
    quiz: {
      id: 'qz-m05-06',
      questions: [
        {
          id: 'q-m05-06-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['strings'],
          prompt: 'How many occurrences does `replace` handle when given a string pattern?',
          options: [
            'All of them',
            'Only the first',
            'Only the last',
            'It depends on the string length',
          ],
          correct: 1,
          optionExplanations: [
            'That requires `replaceAll` or a global regex.',
            'Correct — a surprising default that catches almost everyone once.',
            'It searches from the start.',
            'The behaviour is always the same.',
          ],
          explanation:
            '`replace` with a string pattern replaces only the first match. Use `replaceAll`, or a regular expression with the `g` flag. Passing a non-global regex to `replaceAll` throws a TypeError, deliberately.',
        },
        {
          id: 'q-m05-06-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['strings'],
          prompt: 'Why is this not 1?',
          code: ['console.log("👋".length);'].join('\n'),
          options: ['1', '2', '4', '0'],
          correct: 1,
          optionExplanations: [
            'That would be true if `length` counted characters.',
            'Correct — `length` counts UTF-16 code units, and this emoji needs two.',
            'A surrogate pair is two code units, not four.',
            'The string is not empty.',
          ],
          explanation:
            'JavaScript strings are sequences of UTF-16 code units. Characters outside the basic range are stored as a surrogate pair of two code units, so `length` reports 2. `[..."👋"].length` gives 1, because spreading iterates by code point.',
        },
        {
          id: 'q-m05-06-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['strings'],
          prompt: 'What does `"".split(/\\s+/)` return?',
          options: [
            'An empty array',
            'An array containing one empty string',
            'null',
            'undefined',
          ],
          correct: 1,
          optionExplanations: [
            'Splitting never produces an empty array from a string input.',
            'Correct — `[""]`, which has length 1.',
            '`split` always returns an array.',
            '`split` always returns an array.',
          ],
          explanation:
            'Splitting an empty string produces `[""]`, an array of length 1. This is why a word counter must check for an empty string explicitly rather than relying on the split result — otherwise empty input reports one word.',
        },
        {
          id: 'q-m05-06-4',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['strings'],
          prompt: 'Which of these iterate a string by code point rather than code unit? (Select all that apply.)',
          options: ['`[...text]`', '`for (const c of text)`', '`text.split("")`', '`Array.from(text)`'],
          correct: [0, 1, 3],
          explanation:
            'Spread, `for...of` and `Array.from` all use the string iterator, which walks code points and keeps surrogate pairs intact. `split("")` works in code units, so it breaks an emoji into two useless halves — as does an index-based loop with `text[i]`.',
        },
      ],
    },
    summary:
      '`split` turns a string into an array and `join` reverses it, but neither is a substitute for a real parser when the format allows quoting. `replace` with a string pattern changes only the first occurrence — use `replaceAll`, or a regex with the `g` flag, and note that `replaceAll` throws on a non-global regex by design. A replacement can also be a function, which receives each match. Finally, JavaScript strings are sequences of UTF-16 code units, so `length`, `[]` and `split("")` count and index by code unit, meaning an emoji counts as 2 and indexing splits it into garbage. Spread, `for...of` and `Array.from` iterate by code point and are the safe choice for international text — though even code points do not match human-perceived characters for flags and combined emoji, where `Intl.Segmenter` is the correct tool.',
    keyTakeaways: [
      '`replace` handles only the first match; use `replaceAll` or a `/g` regex',
      '`replaceAll` throws if given a non-global regex',
      '`"".split(sep)` returns `[""]`, not `[]` — guard empty input',
      '`length` counts UTF-16 code units, so `"👋".length` is 2',
      'Spread, `for...of` and `Array.from` iterate by code point',
      'Use `localeCompare` for sorting text users will read',
    ],
    relatedLessons: ['l-m05-05', 'l-m04-03'],
    interviewConnections: [
      'Why does replace only replace the first occurrence?',
      'Why is "👋".length equal to 2?',
      'How do you correctly count the characters in a string containing emoji?',
      'How would you reverse a string containing emoji?',
    ],
  },
];
