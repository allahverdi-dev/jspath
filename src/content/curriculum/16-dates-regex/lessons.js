import { SECTION, CALLOUT_TONE, DIFFICULTY, EXERCISE_KIND, QUIZ_KIND } from '../../schema/types.js';

const M = 'm16';

export default [
  /* ================================================================== */
  {
    id: 'l-m16-01',
    slug: 'dates-and-timestamps',
    moduleId: M,
    order: 1,
    title: 'Dates and Timestamps',
    description: 'What a Date actually stores, the ways to create one, and comparing dates safely.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 18,
    xp: 35,
    topicIds: ['dates'],
    prerequisites: ['l-m05-01'],
    learningObjectives: [
      'Explain what a Date value represents internally',
      'Create dates from components, strings and timestamps',
      'Detect an invalid date',
      'Compare and clone dates correctly',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          '`Date` is a built-in JavaScript object — part of the language itself, not something the browser provides. It exists everywhere JavaScript runs: browsers, Node, anywhere.',
          'A `Date` stores exactly one thing internally: a count of **milliseconds since the Unix epoch** — midnight, January 1st 1970, UTC. Every year, month, hour and "today" you ever read from a `Date` is computed from that single number at the moment you ask.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The number underneath everything.',
        code: [
          'const epoch = new Date(0);',
          'console.log(epoch.toISOString());',
          '',
          '// One day later, in milliseconds',
          'const oneDay = 24 * 60 * 60 * 1000;',
          'const dayTwo = new Date(oneDay);',
          'console.log(dayTwo.toISOString());',
          '',
          '// Date.now() returns that count for the current instant',
          'console.log(typeof Date.now());',
        ].join('\n'),
        output: [
          '1970-01-01T00:00:00.000Z',
          '1970-01-02T00:00:00.000Z',
          'number',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: '`Date.now()` versus `new Date()`',
        body: [
          '`Date.now()` returns the current timestamp as a plain **number** — the fastest way to measure elapsed time. `new Date()` with no arguments builds a full `Date` **object** representing the current instant, which is more expensive and usually what you want when you need to display or manipulate a date rather than just measure a duration.',
          'Neither has a deterministic output, so no example here documents an exact "now" value — only their types and the arithmetic between two timestamps.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Four ways to create a Date',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'From components, from a timestamp, from an ISO string, and "now".',
        code: [
          '// 1. Year, month (0-based!), day, hours, minutes, seconds — LOCAL time',
          'const fromParts = new Date(2024, 0, 15, 9, 30, 0);',
          '',
          '// 2. Milliseconds since the epoch',
          'const fromTimestamp = new Date(1705311000000);',
          '',
          '// 3. An ISO 8601 string — this one is UTC, marked by the trailing Z',
          'const fromIso = new Date("2024-01-15T09:30:00Z");',
          '',
          '// 4. now',
          'const now = new Date();',
          '',
          'console.log("from parts is a Date:", fromParts instanceof Date);',
          'console.log("from timestamp:", fromTimestamp instanceof Date);',
          'console.log("from ISO      :", fromIso instanceof Date);',
          'console.log("now is a Date :", now instanceof Date);',
        ].join('\n'),
        output: [
          'from parts is a Date: true',
          'from timestamp: true',
          'from ISO      : true',
          'now is a Date : true',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'The component constructor uses local time; the string constructor can mean UTC',
        body: [
          '`new Date(2024, 0, 15, 9, 30)` builds a date at 9:30am in **whatever timezone the code is running in**. Two computers in different timezones running that exact line build Dates representing two different real-world instants.',
          '`new Date("2024-01-15T09:30:00Z")` is unambiguous: the trailing `Z` means UTC, so it represents the same instant everywhere on Earth. An ISO string **without** a `Z` or an offset is parsed as local time, which is why full-precision ISO strings should almost always include one.',
          'This module avoids documenting any output that depends on the machine\'s timezone. Where an example reads back local-time getters, it always does so from a date that was built from local **components**, so the getters simply return what was put in — no timezone conversion occurs either way.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Invalid dates',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A bad date is not `null` — it is a Date holding `NaN`.',
        code: [
          'const bad = new Date("not a real date");',
          '',
          'console.log(bad instanceof Date);',
          'console.log(bad.toString());',
          'console.log(Number.isNaN(bad.getTime()));',
          '',
          '// The reliable way to check',
          'function isValidDate(d) {',
          '  return d instanceof Date && !Number.isNaN(d.getTime());',
          '}',
          '',
          'console.log(isValidDate(bad));',
          'console.log(isValidDate(new Date(2024, 0, 1)));',
        ].join('\n'),
        output: [
          'true',
          'Invalid Date',
          'true',
          'false',
          'true',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'An invalid Date is still an object',
        body: [
          '`bad instanceof Date` is `true` even though the date is nonsensical — `typeof` and `instanceof` only check the type, not whether the value is usable. Checking `Number.isNaN(d.getTime())` is the reliable test, exactly the pattern Module 06 used for `NaN` on plain numbers.',
          'Comparing an invalid date with `<` or `>` never throws and never behaves sensibly — every comparison involving `NaN` is `false`, which is a common source of silent bugs when parsing user-supplied dates.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'getTime and comparing dates',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: '`getTime()` gives a plain number you can compare and subtract.',
        code: [
          'const a = new Date("2024-01-15T00:00:00Z");',
          'const b = new Date("2024-06-01T00:00:00Z");',
          '',
          'console.log("a before b:", a.getTime() < b.getTime());',
          'console.log("a < b     :", a < b);',
          'console.log("equal?    :", a.getTime() === a.getTime());',
          '',
          '// Two Date objects for the same instant are never === to each other',
          'const c = new Date("2024-01-15T00:00:00Z");',
          'console.log("same instant, ===:", a === c);',
          'console.log("same instant, getTime equal:", a.getTime() === c.getTime());',
          '',
          '// The gap between them, in whole days',
          'const msPerDay = 24 * 60 * 60 * 1000;',
          'console.log("days apart:", (b.getTime() - a.getTime()) / msPerDay);',
        ].join('\n'),
        output: [
          'a before b: true',
          'a < b     : true',
          'equal?    : true',
          'same instant, ===: false',
          'same instant, getTime equal: true',
          'days apart: 138',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: '`<` and `>` work on Dates directly; `===` never does',
        body: [
          'This is the same reference rule from Module 14: a `Date` is an object, so `===` compares identity, not the instant it represents. `<` and `>` are the exception — they coerce both sides to a number (their timestamp) for the comparison, so they work as you would expect.',
          'For equality, always compare `a.getTime() === b.getTime()`, never `a === b`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Cloning a date',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Dates are mutable objects, so copying the reference is not copying the date.',
        code: [
          'const original = new Date(2024, 0, 1);',
          'const alias = original;',
          'const clone = new Date(original.getTime());',
          '',
          'alias.setFullYear(2030);',
          '',
          'console.log("original:", original.getFullYear());',
          'console.log("alias   :", alias.getFullYear());',
          'console.log("clone   :", clone.getFullYear());',
        ].join('\n'),
        output: [
          'original: 2030',
          'alias   : 2030',
          'clone   : 2024',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`new Date(original.getTime())` — or, equivalently, `new Date(original)` — builds a brand-new `Date` at the same instant. Lesson 3 covers exactly why this matters: several `Date` methods mutate the object they are called on, which is easy to forget because most values in modern JavaScript do not.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const a = new Date("2024-03-01T00:00:00Z");',
          'const b = new Date("2024-03-01T00:00:00Z");',
          '',
          'console.log(a === b, a.getTime() === b.getTime(), a < b);',
        ].join('\n'),
        options: [
          'false true false',
          'true true false',
          'false true true',
          'false false false',
        ],
        correct: 0,
        explanation:
          '`a` and `b` are two separate `Date` objects, so `===` compares identity and is `false` — the same reference rule that applies to every object. They represent the exact same instant, so their timestamps are numerically equal, making `getTime() === getTime()` `true`. And `<` is neither true nor false meaningfully for equal values — an instant is never strictly less than itself, so `a < b` is `false`. This is why date equality must always go through `getTime()`.',
      },
    ],
    exercises: [
      {
        id: 'ex-m16-01-a',
        title: 'Predict the identity check',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['dates'],
        instructions: 'What does this print?',
        code: [
          'const a = new Date(2024, 0, 1);',
          'const b = a;',
          'console.log(a === b);',
        ].join('\n'),
        options: ['true', 'false', 'undefined', 'A TypeError'],
        correct: 0,
        hints: [
          'Does `b = a` copy the date, or the reference to it?',
        ],
        solution: 'true',
        solutionExplanation:
          'A `Date` is an object, so assignment copies the **reference**, not the date. `a` and `b` point at the exact same object, so `===` is `true` — and mutating one through `b.setFullYear(...)` would be visible through `a` too. To get an independent copy you must construct a new `Date` from the original\'s timestamp: `new Date(a.getTime())`.',
      },
      {
        id: 'ex-m16-01-b',
        title: 'Validate a date safely',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['dates'],
        instructions:
          'Write `isValidDate(value)` returning `true` only when `value` is a `Date` object representing a real date — not `NaN`, and not some other type entirely.',
        starterCode: [
          'function isValidDate(value) {',
          '  // instanceof alone is not enough.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'accepts a valid date', body: 'expect(isValidDate(new Date(2024, 0, 1))).toBe(true);' },
          { name: 'rejects an invalid date string', body: 'expect(isValidDate(new Date("nonsense"))).toBe(false);' },
          { name: 'rejects a plain string', body: 'expect(isValidDate("2024-01-01")).toBe(false);' },
          { name: 'rejects a number', body: 'expect(isValidDate(1705311000000)).toBe(false);' },
          { name: 'rejects null', body: 'expect(isValidDate(null)).toBe(false);' },
          { name: 'rejects undefined', body: 'expect(isValidDate(undefined)).toBe(false);' },
          { name: 'accepts the epoch itself', body: 'expect(isValidDate(new Date(0))).toBe(true);', hidden: true },
        ],
        hints: [
          '`value instanceof Date` confirms the type but not the validity.',
          '`Number.isNaN(value.getTime())` confirms validity but throws on a non-Date.',
          'Check the type first, so the second check is only reached for a real Date.',
        ],
        solution: [
          'function isValidDate(value) {',
          '  return value instanceof Date && !Number.isNaN(value.getTime());',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Both checks are necessary. `instanceof Date` alone would accept `new Date("nonsense")`, since an invalid date is still an instance of `Date` — it simply holds `NaN` internally. Checking `getTime()` alone would throw for any non-Date value, since `getTime` does not exist on strings or numbers. The `&&` short-circuits before the second check runs, which is why non-Date values are rejected safely rather than throwing. The hidden test confirms the epoch itself — timestamp `0` — is a perfectly valid date, not a falsy edge case.',
      },
      {
        id: 'ex-m16-01-c',
        title: 'Find the days between two dates',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['dates'],
        instructions:
          'Write `daysBetween(a, b)` returning the whole number of days between two `Date` objects, always positive regardless of which comes first.',
        starterCode: [
          'function daysBetween(a, b) {',
          '  // Work in milliseconds, then convert.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'counts forward', body: 'expect(daysBetween(new Date("2024-01-01T00:00:00Z"), new Date("2024-01-11T00:00:00Z"))).toBe(10);' },
          { name: 'counts backward the same', body: 'expect(daysBetween(new Date("2024-01-11T00:00:00Z"), new Date("2024-01-01T00:00:00Z"))).toBe(10);' },
          { name: 'returns 0 for the same instant', body: 'expect(daysBetween(new Date("2024-01-01T00:00:00Z"), new Date("2024-01-01T00:00:00Z"))).toBe(0);' },
          { name: 'handles a single day', body: 'expect(daysBetween(new Date("2024-01-01T00:00:00Z"), new Date("2024-01-02T00:00:00Z"))).toBe(1);' },
          { name: 'handles a year gap', body: 'expect(daysBetween(new Date("2023-01-01T00:00:00Z"), new Date("2024-01-01T00:00:00Z"))).toBe(365);' },
          { name: 'does not mutate its arguments', body: 'const a = new Date("2024-01-01T00:00:00Z"); const b = new Date("2024-01-05T00:00:00Z"); daysBetween(a, b); expect(a.getTime()).toBe(new Date("2024-01-01T00:00:00Z").getTime());', hidden: true },
        ],
        hints: [
          'Subtract the timestamps, then take the absolute value.',
          'Divide by the number of milliseconds in a day.',
          'Nothing in this calculation needs to modify either Date.',
        ],
        solution: [
          'function daysBetween(a, b) {',
          '  const msPerDay = 24 * 60 * 60 * 1000;',
          '  return Math.abs(a.getTime() - b.getTime()) / msPerDay;',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`getTime()` reduces both dates to plain numbers, which is what makes the subtraction meaningful and timezone-independent — the difference between two UTC instants is the same real duration no matter where it is computed. `Math.abs` makes the order of arguments irrelevant, satisfying the second test. Dividing by the millisecond count of a day converts to days. The function only reads from `a` and `b` via `getTime()`, so it cannot mutate them — the hidden test confirms this holds.',
      },
      {
        id: 'ex-m16-01-d',
        title: 'Clone or alias?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['dates'],
        instructions:
          'You need to hand a `Date` to a function that might modify it, without risking your own copy. What do you pass?',
        options: [
          'The date itself',
          '`new Date(myDate.getTime())`',
          'A string version of the date',
          '`myDate` — Dates cannot be mutated',
        ],
        correct: 1,
        hints: [
          'Are Date objects mutable?',
        ],
        solution: '`new Date(myDate.getTime())`',
        solutionExplanation:
          'Dates are mutable objects — several methods change the instant a `Date` represents, as lesson 3 covers. Passing the date itself hands over the same reference, so a mutation inside the function is visible to your copy too. Constructing a fresh `Date` from the original\'s timestamp gives the function an independent object it cannot use to affect yours.',
      },
    ],
    quiz: {
      id: 'qz-m16-01',
      questions: [
        {
          id: 'q-m16-01-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dates'],
          prompt: 'What does a Date object store internally?',
          options: [
            'A formatted text string',
            'A count of milliseconds since the Unix epoch',
            'Separate year, month and day fields',
            'A count of seconds since the current year began',
          ],
          correct: 1,
          optionExplanations: [
            'Formatting happens only when you ask for it.',
            'Correct — everything else is computed from this single number.',
            'Those fields are computed on demand, not stored.',
            'The reference point is a fixed date in 1970, not the current year.',
          ],
          explanation:
            'A `Date` stores one number: milliseconds since midnight, January 1st 1970, UTC. Every getter — year, month, day, hour — computes its answer from that number at the moment you call it.',
        },
        {
          id: 'q-m16-01-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dates'],
          prompt: 'How should you check whether two Date objects represent the same instant?',
          options: [
            '`a === b`',
            '`a.getTime() === b.getTime()`',
            '`a == b`',
            '`Object.is(a, b)`',
          ],
          correct: 1,
          optionExplanations: [
            'This compares identity, not the instant represented.',
            'Correct — compare the underlying timestamps.',
            'Loose equality on objects still compares identity here.',
            'This is equivalent to `===` for objects.',
          ],
          explanation:
            '`Date` is an object, so `===`, `==` and `Object.is` all compare identity — two separate Date objects for the same instant are never equal by any of them. `getTime()` reduces each to a plain number for a meaningful comparison.',
        },
        {
          id: 'q-m16-01-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dates'],
          prompt: 'What is `new Date("nonsense") instanceof Date`?',
          options: ['`false`', '`true`', 'It throws', '`undefined`'],
          correct: 1,
          optionExplanations: [
            'An invalid date is still a Date object.',
            'Correct — validity and type are different questions.',
            'Constructing an invalid date never throws.',
            'The expression evaluates to a boolean.',
          ],
          explanation:
            'An invalid date is a fully-formed `Date` object holding `NaN` internally, so `instanceof` reports `true`. Checking validity requires `Number.isNaN(d.getTime())` in addition to the type check.',
        },
        {
          id: 'q-m16-01-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['dates'],
          prompt: '`new Date(2024, 0, 15)` and `new Date("2024-01-15")` always represent the exact same instant.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'One is local time; the other is parsed as UTC.',
            'Correct — they can differ depending on the machine\'s timezone.',
          ],
          explanation:
            'The component constructor uses **local** time. A date-only ISO string with no time component is parsed as **UTC** midnight. Unless the local timezone happens to be UTC, these represent different instants — a frequent source of off-by-one-day bugs.',
        },
      ],
    },
    summary:
      'A `Date` is a built-in JavaScript object — not a browser feature — that stores a single number: milliseconds since the Unix epoch. You can build one from local-time components, a timestamp, an ISO string, or the current instant via `new Date()`, and measure elapsed time cheaply with the numeric `Date.now()`. An invalid date is still a `Date` object, so `instanceof` alone cannot detect it — check `!Number.isNaN(d.getTime())` too. Because `Date` is an object, `===` compares identity, never the instant represented; `<` and `>` work correctly by coercing to the timestamp, but equality must go through `getTime()`. Dates are mutable, so copying a reference is not copying the date — clone with `new Date(original.getTime())`. The component constructor uses local time, while a `Z`-suffixed ISO string is unambiguous UTC; mixing the two carelessly is a common source of off-by-one-day and off-by-one-hour bugs.',
    keyTakeaways: [
      'Date stores milliseconds since the Unix epoch — nothing else',
      'instanceof confirms the type; Number.isNaN(d.getTime()) confirms validity',
      '=== compares identity; use getTime() for equality',
      '< and > work directly on Dates via timestamp coercion',
      'Dates are mutable — clone with new Date(original.getTime())',
      'Component constructor = local time; a Z-suffixed ISO string = UTC',
    ],
    relatedLessons: ['l-m06-01', 'l-m16-02'],
    interviewConnections: [
      'What does a JavaScript Date actually store?',
      'Why is a === b false for two Dates representing the same instant?',
      'How do you check whether a Date is valid?',
      'How do you clone a Date?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m16-02',
    slug: 'reading-date-components',
    moduleId: M,
    order: 2,
    title: 'Reading Date Components',
    description: 'The getter methods, the zero-based month trap, and the getDate/getDay distinction that trips everyone up once.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 16,
    xp: 35,
    topicIds: ['dates'],
    prerequisites: ['l-m16-01'],
    learningObjectives: [
      'Read year, month, day, weekday and time components from a Date',
      'Explain why getMonth() is zero-based',
      'Distinguish getDate() from getDay() reliably',
      'Choose local getters versus UTC getters deliberately',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Every component of a date is read with a **getter method**, never a property. `date.year` does not exist; `date.getFullYear()` does.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A date built from components, read back component by component.',
        code: [
          '// Monday, 15th January 2024, 09:30:45, local time',
          'const d = new Date(2024, 0, 15, 9, 30, 45);',
          '',
          'console.log("year   :", d.getFullYear());',
          'console.log("month  :", d.getMonth());',
          'console.log("date   :", d.getDate());',
          'console.log("day    :", d.getDay());',
          'console.log("hours  :", d.getHours());',
          'console.log("minutes:", d.getMinutes());',
          'console.log("seconds:", d.getSeconds());',
        ].join('\n'),
        output: [
          'year   : 2024',
          'month  : 0',
          'date   : 15',
          'day    : 1',
          'hours  : 9',
          'minutes: 30',
          'seconds: 45',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'These getters read back **exactly what was constructed**, because the date was built from local components and is being read with local getters — no timezone conversion happens in either direction. That is why this example, and every getter example in this lesson, produces the same output regardless of which timezone runs it.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: '`getMonth()` is zero-based — every year, this catches someone',
        body: [
          'January is `0`. December is `11`. This is a deliberate, permanent design decision inherited from Java\'s original `Date` class in 1995, and it will never change — too much code now depends on it.',
          'The constructor is consistent with the getter: `new Date(2024, 0, 15)` means January, not month "zero" of some other scheme. The mismatch is entirely with how humans **say** dates — "month 1" out loud means `getMonth() === 0`.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The +1 you will write constantly when displaying a month to a person.',
        code: [
          'const d = new Date(2024, 11, 25); // 25th December',
          '',
          'console.log("raw month :", d.getMonth());',
          'console.log("human month:", d.getMonth() + 1);',
          'console.log(`${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, "0")}-${String(d.getDate()).padStart(2, "0")}`);',
        ].join('\n'),
        output: [
          'raw month : 11',
          'human month: 12',
          '2024-12-25',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'getDate() versus getDay()',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'These two names are dangerously similar and mean completely different things. Read them deliberately, every time, until it is automatic.',
        ],
      },
      {
        kind: SECTION.TABLE,
        headers: ['Method', 'Returns', 'Range'],
        rows: [
          ['`getDate()`', 'Day **of the month**', '1 to 31'],
          ['`getDay()`', 'Day **of the week**', '0 (Sunday) to 6 (Saturday)'],
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The 15th of January 2024 is a Monday.',
        code: [
          'const d = new Date(2024, 0, 15);',
          '',
          'console.log("getDate() — day of month:", d.getDate());',
          'console.log("getDay()  — day of week :", d.getDay());',
          '',
          'const weekdays = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"];',
          'console.log("that is a", weekdays[d.getDay()]);',
        ].join('\n'),
        output: [
          'getDate() — day of month: 15',
          'getDay()  — day of week : 1',
          'that is a Monday',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'A memory aid',
        body: [
          '`getDay()` — the shorter name — returns the **smaller** range: 0 through 6, a weekday index. `getDate()` returns the **bigger** range: 1 through 31, the calendar day. If you are ever unsure while writing code, look up which range the value you are seeing falls into.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Local getters versus UTC getters',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A UTC instant near a local midnight can read back a different calendar date.',
        code: [
          'const instant = new Date("2024-01-15T23:00:00Z");',
          '',
          '// UTC getters always report the same thing, everywhere',
          'console.log("UTC date :", instant.getUTCDate());',
          'console.log("UTC hours:", instant.getUTCHours());',
          '',
          '// Local getters depend on where this code runs — NOT documented',
          '// as a fixed value here, because it genuinely varies.',
          'console.log("local getters exist:", typeof instant.getDate() === "number");',
        ].join('\n'),
        output: [
          'UTC date : 15',
          'UTC hours: 23',
          'local getters exist: true',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'This is not a hypothetical edge case',
        body: [
          'A user in Tokyo (UTC+9) and a server in New York (UTC−5) reading `instant.getDate()` on the same `Date` object, for the instant above, would get **different calendar days** — the Tokyo local time is already the 16th, the New York local time is still the 15th.',
          'Every `get*` method has a `getUTC*` counterpart: `getUTCFullYear`, `getUTCMonth`, `getUTCDate`, `getUTCDay`, `getUTCHours`, and so on. Use the UTC family when you need a value that means the same thing regardless of where the code executes — logging, storage, comparing two instants. Use the local family only when you genuinely want to display a date the way a specific viewer would see it on their own clock.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const d = new Date(2024, 5, 1); // June 1st, 2024, local time',
          '',
          'console.log(d.getMonth(), d.getDate(), d.getDay());',
        ].join('\n'),
        options: [
          '5 1 6',
          '6 1 5',
          '5 1 5',
          '6 1 6',
        ],
        correct: 0,
        explanation:
          '`getMonth()` is zero-based, so June — the 6th month by human counting — is index `5`. `getDate()` is the day-of-month, which was constructed as `1`. `getDay()` is the day-of-week: June 1st, 2024 is a Saturday, which is index `6` (Sunday is `0`). All three numbers were constructed from local components and are read back with local getters, so the result is the same regardless of timezone — but the zero-based month and the getDate/getDay distinction are exactly the two traps this lesson exists to prevent.',
      },
    ],
    exercises: [
      {
        id: 'ex-m16-02-a',
        title: 'Predict the month index',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['dates'],
        instructions: 'What does this print?',
        code: [
          'const d = new Date(2024, 2, 10);',
          'console.log(d.getMonth());',
        ].join('\n'),
        options: ['2', '3', '10', '0'],
        correct: 0,
        hints: [
          'Is getMonth() zero-based or one-based?',
        ],
        solution: '2',
        solutionExplanation:
          '`getMonth()` always returns a zero-based index, and the constructor was given `2` directly as the month argument — so it reads back exactly `2`, which is March. The value `2` never becomes "March, the third month" without you adding 1 yourself; the getter and the constructor agree with each other, just not with how a human says the month aloud.',
      },
      {
        id: 'ex-m16-02-b',
        title: 'Format a human-readable date',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['dates'],
        instructions:
          'Write `toDisplayDate(date)` returning a string like `"15/1/2024"` — day, month (as a human would say it, not zero-based), and full year, separated by slashes.',
        starterCode: [
          'function toDisplayDate(date) {',
          '  // Remember the +1.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'formats a date', body: 'expect(toDisplayDate(new Date(2024, 0, 15))).toBe("15/1/2024");' },
          { name: 'formats December correctly', body: 'expect(toDisplayDate(new Date(2024, 11, 25))).toBe("25/12/2024");' },
          { name: 'formats the first of the month', body: 'expect(toDisplayDate(new Date(2024, 5, 1))).toBe("1/6/2024");' },
          { name: 'formats a different year', body: 'expect(toDisplayDate(new Date(1999, 0, 1))).toBe("1/1/1999");' },
          { name: 'formats February correctly', body: 'expect(toDisplayDate(new Date(2024, 1, 29))).toBe("29/2/2024");', hidden: true },
        ],
        hints: [
          '`getDate()` gives the day of the month.',
          '`getMonth() + 1` gives the human month.',
          '`getFullYear()` gives the year.',
        ],
        solution: [
          'function toDisplayDate(date) {',
          '  return `${date.getDate()}/${date.getMonth() + 1}/${date.getFullYear()}`;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The `+ 1` on `getMonth()` is the single most common line in date-formatting code — forgetting it is why "December" so often shows up as month "11" in buggy interfaces. `getDate()` and `getFullYear()` need no adjustment, since they already match how a person would say them. The hidden test uses a leap-year February 29th, which this function handles correctly because it never computes the date itself — it only reads back what the `Date` object already resolved.',
      },
      {
        id: 'ex-m16-02-c',
        title: 'Is it a weekend?',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['dates'],
        instructions:
          'Write `isWeekend(date)` returning `true` when the date falls on a Saturday or Sunday.',
        starterCode: [
          'function isWeekend(date) {',
          '  // getDay(), not getDate().',
          '}',
        ].join('\n'),
        tests: [
          { name: 'recognises Saturday', body: 'expect(isWeekend(new Date(2024, 5, 1))).toBe(true);' },
          { name: 'recognises Sunday', body: 'expect(isWeekend(new Date(2024, 5, 2))).toBe(true);' },
          { name: 'rejects Monday', body: 'expect(isWeekend(new Date(2024, 5, 3))).toBe(false);' },
          { name: 'rejects Friday', body: 'expect(isWeekend(new Date(2024, 5, 7))).toBe(false);' },
          { name: 'returns a real boolean', body: 'expect(typeof isWeekend(new Date(2024, 5, 1))).toBe("boolean");', hidden: true },
        ],
        hints: [
          '`getDay()` returns 0 for Sunday and 6 for Saturday.',
          'Check both values.',
        ],
        solution: [
          'function isWeekend(date) {',
          '  const day = date.getDay();',
          '  return day === 0 || day === 6;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'This is the exercise that punishes confusing `getDate()` with `getDay()` immediately — using `getDate()` here would compare the day-of-month (1 to 31) against 0 and 6, which is nonsense and would misclassify almost every date. `getDay()` gives exactly the 0-to-6 weekday index the comparison needs, with Sunday and Saturday at the two ends of the range.',
      },
      {
        id: 'ex-m16-02-d',
        title: 'Local or UTC getters?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['dates'],
        instructions:
          'You are logging an event timestamp on a server, and want the log entries to mean the same thing regardless of which server (in which timezone) produced them. Which getters should you use?',
        options: [
          'The local getters — getFullYear, getDate, getHours',
          'The UTC getters — getUTCFullYear, getUTCDate, getUTCHours',
          'It does not matter',
          'Neither — always use getTime() and nothing else',
        ],
        correct: 1,
        hints: [
          'Which family gives the same answer no matter where the code runs?',
        ],
        solution: 'The UTC getters — getUTCFullYear, getUTCDate, getUTCHours',
        solutionExplanation:
          'Local getters resolve the instant according to whatever timezone the running machine is configured with, so identical log calls on servers in different regions would report different calendar dates and hours for the same real event. UTC getters always resolve against Coordinated Universal Time, so the reported values mean the same thing everywhere — exactly what a shared log needs. Local getters are the right choice only when you are showing a date to a specific person on their own clock.',
      },
    ],
    quiz: {
      id: 'qz-m16-02',
      questions: [
        {
          id: 'q-m16-02-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dates'],
          prompt: 'What does `new Date(2024, 0, 1).getMonth()` return?',
          options: ['1', '0', '2024', 'January'],
          correct: 1,
          optionExplanations: [
            'That would be one-based counting, which getMonth does not use.',
            'Correct — January is index 0.',
            'That is the year, from a different getter.',
            'getMonth returns a number, never a name.',
          ],
          explanation:
            '`getMonth()` is zero-based, so January is `0` and December is `11`. Add 1 when displaying the month to a human, and never when comparing or constructing dates, where the zero-based value is exactly what the constructor expects.',
        },
        {
          id: 'q-m16-02-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dates'],
          prompt: 'What is the difference between getDate() and getDay()?',
          options: [
            'They are aliases for the same thing',
            'getDate() returns the day of the month (1–31); getDay() returns the day of the week (0–6)',
            'getDate() returns the day of the week; getDay() returns the day of the month',
            'getDay() includes the year',
          ],
          correct: 1,
          optionExplanations: [
            'They return entirely different ranges.',
            'Correct — month-day versus weekday-index.',
            'This has the two reversed.',
            'Neither includes the year.',
          ],
          explanation:
            '`getDate()` is the calendar day within the month, 1 through 31. `getDay()` is the weekday index, 0 (Sunday) through 6 (Saturday). The similar names make them easy to swap by accident, which is why exercises specifically drill the distinction.',
        },
        {
          id: 'q-m16-02-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dates'],
          prompt: 'When should you prefer the UTC getters over the local getters?',
          options: [
            'Never — local getters are always correct',
            'When the value needs to mean the same thing regardless of where the code runs, such as logging or comparing instants',
            'Only when working with dates before 1970',
            'UTC getters do not exist for hours or minutes',
          ],
          correct: 1,
          optionExplanations: [
            'Local getters are timezone-dependent by design.',
            'Correct — UTC is the timezone-independent choice.',
            'The epoch year is unrelated to this choice.',
            'Every local getter has a UTC counterpart.',
          ],
          explanation:
            'Local getters resolve against the running machine\'s configured timezone, so the same `Date` can read back different calendar values on different machines. UTC getters are timezone-independent, which is essential for logs, storage and any comparison that must mean the same thing everywhere.',
        },
        {
          id: 'q-m16-02-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['dates'],
          prompt: 'getMonth() being zero-based is considered a bug that may be fixed in a future version of JavaScript.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'It is a permanent design decision, not a bug awaiting a fix.',
            'Correct — it will never change, because of backward compatibility.',
          ],
          explanation:
            'JavaScript never removes or changes established behaviour that existing code depends on. The zero-based month, inherited from Java\'s original Date class in 1995, is permanent — the fix is always in your own code, with a `+ 1` when displaying to a person.',
        },
      ],
    },
    summary:
      'Date components are always read with **getter methods**, never properties. `getMonth()` is permanently **zero-based** — January is `0`, December is `11` — a design decision from 1995 that will never change; add 1 only when displaying to a human, never when constructing or comparing. `getDate()` returns the day of the month (1–31); `getDay()` returns the day of the week (0–6, Sunday first) — the similar names make them easy to confuse, and mixing them up produces plausible-looking wrong answers rather than obvious errors. Every local getter has a UTC counterpart (`getUTCFullYear`, `getUTCDate`, and so on); local getters resolve against the running machine\'s timezone and can disagree across machines for the identical instant, while UTC getters always agree everywhere — use UTC for logging, storage and comparison, and local getters only when genuinely displaying a date on a specific viewer\'s own clock.',
    keyTakeaways: [
      'Components are read with methods: getFullYear(), getMonth(), not properties',
      'getMonth() is zero-based — January is 0 — permanently',
      'getDate() = day of month (1–31); getDay() = day of week (0–6)',
      'Every local getter has a getUTC* counterpart',
      'Use UTC getters for anything that must mean the same thing everywhere',
      'Use local getters only to display a date on a specific viewer\'s clock',
    ],
    relatedLessons: ['l-m16-01', 'l-m16-03'],
    interviewConnections: [
      'Why is getMonth() zero-based?',
      'What is the difference between getDate() and getDay()?',
      'When would you use UTC getters instead of local getters?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m16-03',
    slug: 'setting-and-mutating-dates',
    moduleId: M,
    order: 3,
    title: 'Setting and Mutating Dates',
    description: 'How setter methods change a Date in place, and the rollover behaviour that makes them genuinely useful.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 16,
    xp: 35,
    topicIds: ['dates'],
    prerequisites: ['l-m16-02'],
    learningObjectives: [
      'Use setter methods to change a Date in place',
      'Explain and predict rollover behaviour',
      'Add or subtract time safely using setters',
      'Recognise where an unintended mutation causes a bug',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Every getter has a matching **setter**: `setFullYear`, `setMonth`, `setDate`, `setHours`, `setMinutes`, `setSeconds`, and their UTC equivalents. Unlike almost everything you have worked with recently — strings, and the immutable-update patterns for arrays and objects — a `Date` setter **mutates the object it is called on**.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'setFullYear changes the object in place and returns its new timestamp.',
        code: [
          'const d = new Date(2024, 0, 15);',
          '',
          'const returned = d.setFullYear(2030);',
          '',
          'console.log("date is now:", d.getFullYear());',
          'console.log("returned is a number:", typeof returned === "number");',
          'console.log("matches getTime:", returned === d.getTime());',
        ].join('\n'),
        output: [
          'date is now: 2030',
          'returned is a number: true',
          'matches getTime: true',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'This is the opposite of everything you just learned about arrays and objects',
        body: [
          'Modules 12 through 15 built the habit of never mutating — spread, `map`, `filter` and friends all return new values. `Date` predates that convention entirely; it was designed in 1995 alongside mutable arrays, before immutability became idiomatic JavaScript.',
          'If you hold two references to the same `Date` — an alias, or a date passed into a function — calling a setter on one changes what the other one reads too. This is exactly the reference-sharing bug from Module 14, applied to a type where it is easy to forget it still applies.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The shared-reference bug, made concrete.',
        code: [
          'function addYear(date) {',
          '  date.setFullYear(date.getFullYear() + 1);',
          '  return date;',
          '}',
          '',
          'const original = new Date(2024, 0, 1);',
          'const result = addYear(original);',
          '',
          'console.log("result  :", result.getFullYear());',
          'console.log("original:", original.getFullYear());',
          'console.log("same object:", result === original);',
        ].join('\n'),
        output: [
          'result  : 2025',
          'original: 2025',
          'same object: true',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The caller\'s `original` changed too, because `addYear` never made a copy — it mutated the object it was handed. If that surprises the caller, it is a bug. The fix, learned in lesson 1, is to clone before mutating: `new Date(date.getTime())`.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A non-mutating version.',
        code: [
          'function addYearSafely(date) {',
          '  const copy = new Date(date.getTime());',
          '  copy.setFullYear(copy.getFullYear() + 1);',
          '  return copy;',
          '}',
          '',
          'const original = new Date(2024, 0, 1);',
          'const result = addYearSafely(original);',
          '',
          'console.log("result  :", result.getFullYear());',
          'console.log("original:", original.getFullYear());',
          'console.log("same object:", result === original);',
        ].join('\n'),
        output: [
          'result  : 2025',
          'original: 2024',
          'same object: false',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Rollover',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This is the setter behaviour that makes them genuinely powerful rather than merely dangerous. Passing an out-of-range value does not throw — it **rolls over** into the next or previous unit, exactly the way you would do the arithmetic on paper.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Day 32 of January becomes day 1 of February.',
        code: [
          'const d = new Date(2024, 0, 15); // January',
          'd.setDate(32);',
          '',
          'console.log("month:", d.getMonth());',
          'console.log("date :", d.getDate());',
        ].join('\n'),
        output: ['month: 1', 'date : 1'].join('\n'),
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Rollover works in every direction and every unit.',
        code: [
          '// Day 0 means "the last day of the previous month"',
          'const lastDayOfJan = new Date(2024, 1, 0);',
          'console.log("day 0 of Feb:", lastDayOfJan.getMonth(), lastDayOfJan.getDate());',
          '',
          '// Month 12 rolls into January of the following year',
          'const rolledYear = new Date(2024, 12, 1);',
          'console.log("month 12:", rolledYear.getFullYear(), rolledYear.getMonth());',
          '',
          '// Negative values roll backward past the start',
          'const rolledBack = new Date(2024, 0, -1);',
          'console.log("date -1 :", rolledBack.getFullYear(), rolledBack.getMonth(), rolledBack.getDate());',
        ].join('\n'),
        output: [
          'day 0 of Feb: 0 31',
          'month 12: 2025 0',
          'date -1 : 2023 11 30',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'This is the standard trick for finding the last day of a month',
        body: [
          '`new Date(year, month + 1, 0)` is the day before the first of next month — which is the last day of `month`. It works correctly across every month length, including leap-year February, without any lookup table or manual `if` chain, because the rollover math is handled by the engine.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Rollover is also how you safely add days',
        body: [
          '`date.setDate(date.getDate() + 7)` adds a week, correctly crossing month and even year boundaries, because `setDate` rolls over rather than clamping or throwing. Writing that arithmetic by hand — tracking how many days are in each month, and leap years — would be far more error-prone than trusting the rollover.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const d = new Date(2024, 11, 25); // 25th December',
          'd.setDate(d.getDate() + 10);',
          '',
          'console.log(d.getFullYear(), d.getMonth(), d.getDate());',
        ].join('\n'),
        options: [
          '2025 0 4',
          '2024 11 35',
          '2024 0 4',
          '2025 11 4',
        ],
        correct: 0,
        explanation:
          'December 25th plus 10 days crosses both the month boundary and the year boundary. `setDate` does not clamp at 31 or throw for an out-of-range value — it rolls forward: December has 31 days, so day 35 becomes day 4 of the following month, which is January, which is also the following year. The result is January 4th, 2025 — `getFullYear()` is 2025, `getMonth()` is 0, `getDate()` is 4. This rollover is exactly why `setDate(getDate() + n)` is the standard, safe way to add days to a date.',
      },
    ],
    exercises: [
      {
        id: 'ex-m16-03-a',
        title: 'Predict the rollover',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['dates'],
        instructions: 'What does this print?',
        code: [
          'const d = new Date(2024, 1, 30); // 30th "February"',
          'console.log(d.getMonth(), d.getDate());',
        ].join('\n'),
        options: ['1 30', '2 1', '2 2', '1 28'],
        correct: 2,
        hints: [
          '2024 is a leap year — how many days does February actually have?',
          'Does the constructor clamp an out-of-range day, or roll it over?',
        ],
        solution: '2 2',
        solutionExplanation:
          '2024 is a leap year, so February has 29 days. Asking for day 30 rolls over exactly one day past the end of the month — into March 2nd. The month constructor applies the same rollover as `setDate`, since both ultimately go through the same date-normalisation logic. Getting this right requires knowing February\'s actual length that year, which is precisely why hand-written date arithmetic is so error-prone and rollover-based methods are preferred.',
      },
      {
        id: 'ex-m16-03-b',
        title: 'Add days without mutating the original',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['dates'],
        instructions:
          'Write `addDays(date, days)` returning a **new** Date, `days` days after the given date. The original date must not be modified. `days` may be negative.',
        starterCode: [
          'function addDays(date, days) {',
          '  // Clone first, then mutate the clone.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'adds days within a month', body: 'const r = addDays(new Date(2024, 0, 1), 5); expect(r.getMonth()).toBe(0); expect(r.getDate()).toBe(6);' },
          { name: 'rolls over into the next month', body: 'const r = addDays(new Date(2024, 0, 30), 5); expect(r.getMonth()).toBe(1); expect(r.getDate()).toBe(4);' },
          { name: 'rolls over into the next year', body: 'const r = addDays(new Date(2024, 11, 28), 5); expect(r.getFullYear()).toBe(2025); expect(r.getMonth()).toBe(0); expect(r.getDate()).toBe(2);' },
          { name: 'subtracts with a negative count', body: 'const r = addDays(new Date(2024, 0, 5), -10); expect(r.getMonth()).toBe(11); expect(r.getFullYear()).toBe(2023);' },
          { name: 'does not mutate the original', body: 'const src = new Date(2024, 0, 1); addDays(src, 10); expect(src.getDate()).toBe(1);' },
          { name: 'returns a different object', body: 'const src = new Date(2024, 0, 1); expect(addDays(src, 1) === src).toBe(false);' },
          { name: 'handles zero days', body: 'const src = new Date(2024, 0, 1); expect(addDays(src, 0).getDate()).toBe(1);', hidden: true },
        ],
        hints: [
          'Clone with `new Date(date.getTime())` before calling any setter.',
          '`copy.setDate(copy.getDate() + days)` handles every rollover for you.',
        ],
        solution: [
          'function addDays(date, days) {',
          '  const copy = new Date(date.getTime());',
          '  copy.setDate(copy.getDate() + days);',
          '  return copy;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Cloning before mutating is what protects the caller\'s date — mutating `date` directly would pass the third and fifth tests\' logic but fail the "does not mutate" test outright. `setDate` handles every rollover direction identically, so month and year boundaries need no special-casing, and a negative `days` naturally subtracts. This is the general pattern for every "add N units" helper: clone, then set relative to the current value of the same unit.',
      },
      {
        id: 'ex-m16-03-c',
        title: 'Find the last day of the month',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['dates'],
        instructions:
          'Write `lastDayOfMonth(year, month)` returning how many days are in the given month, where `month` is zero-based like `getMonth()`. Must be correct for leap-year February.',
        starterCode: [
          'function lastDayOfMonth(year, month) {',
          '  // Day 0 of the next month is the last day of this one.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'January has 31 days', body: 'expect(lastDayOfMonth(2024, 0)).toBe(31);' },
          { name: 'April has 30 days', body: 'expect(lastDayOfMonth(2024, 3)).toBe(30);' },
          { name: 'February 2024 is a leap year', body: 'expect(lastDayOfMonth(2024, 1)).toBe(29);' },
          { name: 'February 2023 is not a leap year', body: 'expect(lastDayOfMonth(2023, 1)).toBe(28);' },
          { name: 'December rolls into the next year internally but still returns 31', body: 'expect(lastDayOfMonth(2024, 11)).toBe(31);', hidden: true },
        ],
        hints: [
          '`new Date(year, month + 1, 0)` lands on the last day of `month`.',
          'Read that date\'s `getDate()`.',
        ],
        solution: [
          'function lastDayOfMonth(year, month) {',
          '  return new Date(year, month + 1, 0).getDate();',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Asking for day 0 of the *next* month rolls backward exactly one day, landing on the last day of the requested month — and this works identically for every month length, including the leap-year cases the tests specifically check, because the engine already knows how many days each month has for the given year. The hidden test confirms `month + 1` correctly overflows from December (index 11) into January of the following year internally, without you needing to special-case it.',
      },
      {
        id: 'ex-m16-03-d',
        title: 'Fix the mutation bug',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['dates'],
        instructions:
          'This function is supposed to return a date one month later, without touching the date it was given — but callers report their original dates keep changing. Fix it.',
        starterCode: [
          'function oneMonthLater(date) {',
          '  date.setMonth(date.getMonth() + 1);',
          '  return date;',
          '}',
        ].join('\n'),
        tests: [
          { name: 'returns a date one month later', body: 'const r = oneMonthLater(new Date(2024, 0, 15)); expect(r.getMonth()).toBe(1); expect(r.getDate()).toBe(15);' },
          { name: 'does not mutate the original', body: 'const src = new Date(2024, 0, 15); oneMonthLater(src); expect(src.getMonth()).toBe(0);' },
          { name: 'returns a different object', body: 'const src = new Date(2024, 0, 15); expect(oneMonthLater(src) === src).toBe(false);' },
          { name: 'handles a December rollover', body: 'const r = oneMonthLater(new Date(2024, 11, 15)); expect(r.getFullYear()).toBe(2025); expect(r.getMonth()).toBe(0);', hidden: true },
        ],
        hints: [
          '`setMonth` mutates whatever it is called on.',
          'Clone the date first.',
        ],
        solution: [
          'function oneMonthLater(date) {',
          '  const copy = new Date(date.getTime());',
          '  copy.setMonth(copy.getMonth() + 1);',
          '  return copy;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The original mutated its argument directly, which is exactly the bug callers were reporting — every call permanently altered whatever `Date` was passed in. Cloning with `new Date(date.getTime())` first, then mutating the clone, gives the caller a new value while leaving theirs untouched. This is the general fix for any function that needs to look like it "returns a transformed date" rather than "changes the date you gave it".',
      },
    ],
    quiz: {
      id: 'qz-m16-03',
      questions: [
        {
          id: 'q-m16-03-1',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['dates'],
          prompt: 'Date setter methods like setFullYear return a new Date rather than mutating the original.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'They mutate the object in place and return its new timestamp.',
            'Correct — Date setters mutate, unlike array and object methods you have used recently.',
          ],
          explanation:
            'Every `Date` setter mutates the object it is called on and returns the resulting timestamp as a number, not a new `Date`. This is the opposite of the immutable patterns used for arrays and objects, and it is why cloning before mutating is essential when a caller\'s date must be preserved.',
        },
        {
          id: 'q-m16-03-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['dates'],
          prompt: 'What does this print?',
          code: [
            'const d = new Date(2024, 0, 31);',
            'd.setDate(d.getDate() + 1);',
            'console.log(d.getMonth(), d.getDate());',
          ].join('\n'),
          options: ['0 32', '1 1', '0 1', '1 32'],
          correct: 1,
          optionExplanations: [
            'setDate rolls over rather than producing an invalid day like 32.',
            'Correct — January 31st plus one day is February 1st.',
            'The month also advances, not just the day.',
            'The day resets rather than continuing to grow.',
          ],
          explanation:
            'January has 31 days, so asking for day 32 rolls over into the first day of the next month — February 1st. `getMonth()` is now `1` and `getDate()` is `1`. This rollover behaviour is what makes `setDate(getDate() + n)` a safe way to add days across any month boundary.',
        },
        {
          id: 'q-m16-03-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dates'],
          prompt: 'How do you find the last day of a given month reliably, including leap years?',
          options: [
            'A hard-coded lookup table of days per month',
            '`new Date(year, month + 1, 0).getDate()`',
            '`new Date(year, month, 31).getDate()`',
            'Always assume 30 days',
          ],
          correct: 1,
          optionExplanations: [
            'This works but needs separate leap-year logic maintained by hand.',
            'Correct — day 0 of next month rolls back to the last day of this one.',
            '31 does not exist in every month, so this misbehaves for shorter months.',
            'This is wrong for every month except April, June, September and November.',
          ],
          explanation:
            'Requesting day 0 of the following month rolls backward exactly one day, landing on the true last day of the target month — correct for every month length and every leap year, because the engine\'s own calendar rules do the work.',
        },
        {
          id: 'q-m16-03-4',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dates'],
          prompt: 'Why should a function that is meant to return a transformed date clone its argument first?',
          options: [
            'Cloning improves performance',
            'Because Date setters mutate, so calling one directly on the argument would silently change the caller\'s date',
            'It is required by the language',
            'There is no reason — mutating the argument is preferred',
          ],
          correct: 1,
          optionExplanations: [
            'Cloning has nothing to do with performance here.',
            'Correct — protecting the caller from an unexpected mutation.',
            'The language permits mutating the argument; it is a design choice to avoid it.',
            'Mutating a caller\'s argument unexpectedly is a classic source of bugs.',
          ],
          explanation:
            'Because `Date` setters mutate in place, a function that calls one directly on its parameter changes the exact object the caller passed in — an unpleasant surprise if the caller expected a new value back. Cloning first, then mutating the clone, is the standard defence.',
        },
      ],
    },
    summary:
      'Every `Date` getter has a matching setter — `setFullYear`, `setMonth`, `setDate`, `setHours`, and their UTC equivalents — and every one of them **mutates the object it is called on**, returning the new timestamp as a number. This is the opposite of the immutable conventions built up across Modules 12–15, and it means a function that calls a setter directly on its argument silently changes the caller\'s date; clone first with `new Date(date.getTime())` when that must be avoided. Out-of-range values do not throw — they **roll over**: day 32 of January becomes February 1st, month 12 rolls into January of the next year, day 0 is the last day of the previous month. Rollover is not a quirk to work around but the standard mechanism for safe date arithmetic: `setDate(getDate() + n)` correctly adds or subtracts days across any month or year boundary, and `new Date(year, month + 1, 0).getDate()` finds the last day of any month, leap years included, without a lookup table.',
    keyTakeaways: [
      'Date setters mutate in place and return the new timestamp',
      'Clone with new Date(date.getTime()) before mutating if the original must survive',
      'Out-of-range values roll over rather than throwing',
      'setDate(getDate() + n) safely adds or subtracts days across any boundary',
      'new Date(year, month + 1, 0) finds the last day of month, leap years included',
      'Day 0 means the last day of the previous month',
    ],
    relatedLessons: ['l-m16-01', 'l-m16-04'],
    interviewConnections: [
      'Do Date setter methods mutate the original object?',
      'What happens when you set a date to an out-of-range value?',
      'How would you find the last day of a month?',
      'How do you safely add days to a date without a manual leap-year check?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m16-04',
    slug: 'formatting-with-intl',
    moduleId: M,
    order: 4,
    title: 'Formatting Dates and Numbers with Intl',
    description: 'Storing a date and displaying one are different problems — Intl.DateTimeFormat and Intl.NumberFormat solve the second.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 17,
    xp: 35,
    topicIds: ['dates'],
    prerequisites: ['l-m16-03'],
    learningObjectives: [
      'Explain why storing and formatting a date are separate concerns',
      'Format dates for a specific locale with Intl.DateTimeFormat',
      'Format currency, decimals and percentages with Intl.NumberFormat',
      'Pin a timezone explicitly for deterministic formatting',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          '`Intl` is another built-in JavaScript object — the language\'s internationalisation toolkit, available everywhere JavaScript runs. It is what turns a raw `Date` or `number` into text a specific audience would recognise as their own.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Store the instant, format on display',
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Never store a formatted date string',
        body: [
          'A formatted string like `"15/1/2024"` has lost information — you cannot reliably parse it back (is that day/month or month/day?), sort it correctly as text, or reformat it for a different audience. Store the **instant** — an ISO string or a timestamp — and format it fresh every time you display it.',
          'This is the same separation of concerns as Module 15\'s `getter` lesson: compute the display form on demand from the source of truth, rather than keeping a stale, pre-formatted copy.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Intl.DateTimeFormat',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A locale, an options object, and a Date — pinning the timezone for a deterministic result.',
        code: [
          'const date = new Date("2024-01-15T09:30:00Z");',
          '',
          'const uk = new Intl.DateTimeFormat("en-GB", {',
          '  year: "numeric",',
          '  month: "long",',
          '  day: "numeric",',
          '  timeZone: "UTC",',
          '});',
          '',
          'console.log(uk.format(date));',
          '',
          'const us = new Intl.DateTimeFormat("en-US", {',
          '  year: "numeric",',
          '  month: "short",',
          '  day: "numeric",',
          '  timeZone: "UTC",',
          '});',
          '',
          'console.log(us.format(date));',
        ].join('\n'),
        output: [
          '15 January 2024',
          'Jan 15, 2024',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'The locale argument is a concept, not a fixed answer',
        body: [
          'The **locale** — `"en-GB"`, `"en-US"`, `"ja-JP"`, `"de-DE"` — tells `Intl` which language and regional conventions to use: word order, month names, punctuation, digit grouping. It is a *request*; the exact output can still vary slightly by JavaScript engine and by the ICU (Unicode locale data) version installed on the machine running the code, though the differences are usually cosmetic.',
          'The `timeZone` option pinned above is what makes these particular examples deterministic: without it, the formatted date would be computed in whatever timezone the running machine is configured with, which is exactly the non-determinism this module has avoided from the first lesson.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Including the time, and a shorthand for reuse.',
        code: [
          'const date = new Date("2024-01-15T09:30:00Z");',
          '',
          'const formatter = new Intl.DateTimeFormat("en-GB", {',
          '  dateStyle: "medium",',
          '  timeStyle: "short",',
          '  timeZone: "UTC",',
          '});',
          '',
          'console.log(formatter.format(date));',
          '',
          '// The same formatter instance can be reused for many dates',
          'const dates = [',
          '  new Date("2024-03-01T00:00:00Z"),',
          '  new Date("2024-12-25T00:00:00Z"),',
          '];',
          'console.log(dates.map((d) => formatter.format(d)));',
        ].join('\n'),
        output: [
          '15 Jan 2024, 09:30',
          '["1 Mar 2024, 00:00", "25 Dec 2024, 00:00"]',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Constructing an `Intl.DateTimeFormat` does real work — parsing the locale and options — so if you are formatting many dates the same way, build one formatter and reuse its `.format()` method, rather than constructing a new one per date.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Intl.NumberFormat',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Currency, percentages and grouped decimals.',
        code: [
          'const price = 1234.5;',
          '',
          'const gbp = new Intl.NumberFormat("en-GB", { style: "currency", currency: "GBP" });',
          'console.log(gbp.format(price));',
          '',
          'const usd = new Intl.NumberFormat("en-US", { style: "currency", currency: "USD" });',
          'console.log(usd.format(price));',
          '',
          'const percent = new Intl.NumberFormat("en-GB", { style: "percent" });',
          'console.log(percent.format(0.4567));',
          '',
          'const decimal = new Intl.NumberFormat("en-GB", {',
          '  minimumFractionDigits: 2,',
          '  maximumFractionDigits: 2,',
          '});',
          'console.log(decimal.format(1234.5));',
        ].join('\n'),
        output: [
          '£1,234.50',
          '$1,234.50',
          '46%',
          '1,234.50',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'This is the correct way to display money',
        body: [
          'Never build a currency string by hand — `"£" + price.toFixed(2)` gets the digit grouping wrong for large numbers and cannot adapt to a different currency or locale. `Intl.NumberFormat` with `style: "currency"` handles the symbol, its position, the grouping separators and the correct number of decimal places (some currencies use zero, some use three) in one call.',
          'The percent style expects a **fraction**, not a whole percentage — `0.4567` formats as `"46%"`, not `4567` or `"4567%"`. Passing the wrong scale is a common mistake worth watching for.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'compact notation and grouping — used cautiously.',
        code: [
          'const large = 1500000;',
          '',
          '// Explicit grouping — stable across engines and versions',
          'console.log(new Intl.NumberFormat("en-GB").format(large));',
          '',
          '// Compact notation exists, but its exact text can vary by',
          '// engine and locale-data version — verify the exact string',
          '// in your target environment before documenting or testing it.',
          'const compact = new Intl.NumberFormat("en-GB", { notation: "compact" }).format(large);',
          'console.log(typeof compact === "string" && compact.length > 0);',
        ].join('\n'),
        output: [
          '1,500,000',
          'true',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Formatting versus storing, one more time',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A realistic shape: store ISO, format for display.',
        code: [
          'const order = {',
          '  placedAt: "2024-01-15T09:30:00Z", // stored — unambiguous, sortable',
          '  total: 1234.5,',
          '};',
          '',
          'function describeOrder(order, locale) {',
          '  const date = new Intl.DateTimeFormat(locale, {',
          '    dateStyle: "long",',
          '    timeZone: "UTC",',
          '  }).format(new Date(order.placedAt));',
          '',
          '  const total = new Intl.NumberFormat(locale, {',
          '    style: "currency",',
          '    currency: "GBP",',
          '  }).format(order.total);',
          '',
          '  return `Placed ${date} — ${total}`;',
          '}',
          '',
          'console.log(describeOrder(order, "en-GB"));',
        ].join('\n'),
        output: ['Placed 15 January 2024 — £1,234.50'].join('\n'),
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const fmt = new Intl.NumberFormat("en-GB", { style: "percent" });',
          '',
          'console.log(fmt.format(0.5), fmt.format(1), fmt.format(50));',
        ].join('\n'),
        options: [
          '50% 100% 5000%',
          '0.5% 1% 50%',
          '50% 1% 50%',
          '50% 100% 50%',
        ],
        correct: 0,
        explanation:
          'Percent formatting multiplies the input by 100 and appends the `%` sign — it expects a **fraction**, not an already-scaled percentage. `0.5` becomes `"50%"`, and `1` — meaning "the whole thing" as a fraction — becomes `"100%"`. Passing `50` as if it already meant "fifty percent" is the common mistake: since `50` is treated as the fraction `50`, it becomes `5000%`. Always convert to a fraction (divide by 100) before formatting as a percent.',
      },
    ],
    exercises: [
      {
        id: 'ex-m16-04-a',
        title: 'Predict the percent formatting',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['dates'],
        instructions: 'What does this print?',
        code: [
          'const fmt = new Intl.NumberFormat("en-GB", { style: "percent" });',
          'console.log(fmt.format(0.08));',
        ].join('\n'),
        options: ['"8%"', '"0.08%"', '"80%"', '"0%"'],
        correct: 0,
        hints: [
          'The percent style expects a fraction of 1, not an already-scaled number.',
        ],
        solution: '"8%"',
        solutionExplanation:
          '`Intl.NumberFormat` with `style: "percent"` multiplies its input by 100 before appending the `%` sign, so `0.08` — meaning eight hundredths — correctly becomes `"8%"`. Passing `8` directly, expecting "8 percent", would instead produce `"800%"`. Always express the value as a fraction of 1 before formatting.',
      },
      {
        id: 'ex-m16-04-b',
        title: 'Format a price for two currencies',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['dates'],
        instructions:
          'Write `formatPrice(amount, currency)` returning the amount formatted as currency using the `"en-GB"` locale. `currency` is a three-letter code like `"GBP"` or `"USD"`.',
        starterCode: [
          'function formatPrice(amount, currency) {',
          '  // Build an Intl.NumberFormat with style: "currency".',
          '}',
        ].join('\n'),
        tests: [
          { name: 'formats GBP', body: 'expect(formatPrice(1234.5, "GBP")).toBe("£1,234.50");' },
          { name: 'formats USD', body: 'expect(formatPrice(1234.5, "USD")).toBe("US$1,234.50");' },
          { name: 'formats a small amount', body: 'expect(formatPrice(5, "GBP")).toBe("£5.00");' },
          { name: 'formats zero', body: 'expect(formatPrice(0, "GBP")).toBe("£0.00");' },
          { name: 'groups large numbers', body: 'expect(formatPrice(1000000, "GBP")).toBe("£1,000,000.00");', hidden: true },
        ],
        hints: [
          '`new Intl.NumberFormat("en-GB", { style: "currency", currency })`',
          'Call `.format(amount)` on the result.',
        ],
        solution: [
          'function formatPrice(amount, currency) {',
          '  return new Intl.NumberFormat("en-GB", { style: "currency", currency }).format(amount);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Passing `currency` as the currency code lets `Intl.NumberFormat` pick the correct symbol, its position and the right number of decimal places for that specific currency, without any of that logic being written by hand. The `"en-GB"` locale controls the digit grouping and symbol placement — a US locale would place the same GBP symbol differently in some contexts. This is the reliable alternative to string-concatenating a currency symbol, which the lesson specifically warns against.',
      },
      {
        id: 'ex-m16-04-c',
        title: 'Format an ISO date for display',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['dates'],
        instructions:
          'Write `formatIsoDate(isoString)` taking a `Z`-suffixed ISO string and returning it formatted as `"15 January 2024"` in UTC, using the `"en-GB"` locale.',
        starterCode: [
          'function formatIsoDate(isoString) {',
          '  // Parse, then format with the UTC timezone pinned.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'formats a date', body: 'expect(formatIsoDate("2024-01-15T09:30:00Z")).toBe("15 January 2024");' },
          { name: 'formats December', body: 'expect(formatIsoDate("2024-12-25T00:00:00Z")).toBe("25 December 2024");' },
          { name: 'formats a single-digit day', body: 'expect(formatIsoDate("2024-03-01T00:00:00Z")).toBe("1 March 2024");' },
          { name: 'formats near midnight UTC without shifting the day', body: 'expect(formatIsoDate("2024-01-15T23:59:00Z")).toBe("15 January 2024");', hidden: true },
        ],
        hints: [
          '`new Date(isoString)` parses the string.',
          'Pin `timeZone: "UTC"` in the options so the result does not depend on the local machine.',
          '`{ year: "numeric", month: "long", day: "numeric" }` gives the requested format.',
        ],
        solution: [
          'function formatIsoDate(isoString) {',
          '  return new Intl.DateTimeFormat("en-GB", {',
          '    year: "numeric",',
          '    month: "long",',
          '    day: "numeric",',
          '    timeZone: "UTC",',
          '  }).format(new Date(isoString));',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Pinning `timeZone: "UTC"` is what makes this function\'s output deterministic regardless of where it runs — without it, the formatted day could differ by one depending on the local machine\'s offset, exactly the risk the hidden near-midnight test is built to catch. `month: "long"` gives the full month name rather than a number or abbreviation, matching the required format precisely.',
      },
      {
        id: 'ex-m16-04-d',
        title: 'Store or format?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['dates'],
        instructions:
          'You are saving an event\'s date to a database that other services will also read. What should you store?',
        options: [
          'A pre-formatted string like "15 January 2024"',
          'An ISO 8601 string or a numeric timestamp',
          'Whatever Intl.DateTimeFormat happens to produce for your locale',
          'A locale-specific string like "15/1/2024"',
        ],
        correct: 1,
        hints: [
          'Which representation is unambiguous and sortable as text?',
        ],
        solution: 'An ISO 8601 string or a numeric timestamp',
        solutionExplanation:
          'An ISO string or timestamp represents the instant unambiguously, sorts correctly even as plain text, and can be parsed back into a `Date` and reformatted for any audience later. A pre-formatted or locale-specific string has already thrown away that flexibility — it cannot be reliably reparsed, and every future consumer is stuck with the formatting choice you made at write time. Format on display, from the stored instant, every time.',
      },
    ],
    quiz: {
      id: 'qz-m16-04',
      questions: [
        {
          id: 'q-m16-04-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dates'],
          prompt: 'Why should you avoid storing a pre-formatted date string?',
          options: [
            'Strings take up too much space',
            'A formatted string loses information and cannot be reliably reparsed or resorted',
            'Intl.DateTimeFormat cannot read strings',
            'It is technically impossible to store a string',
          ],
          correct: 1,
          optionExplanations: [
            'Storage size is not the concern here.',
            'Correct — always store the instant and format on display.',
            'This is unrelated to the reason.',
            'Strings can obviously be stored.',
          ],
          explanation:
            'A display-formatted date like "15/1/2024" is ambiguous (day/month or month/day?), does not sort correctly as text, and cannot be reformatted for a different audience without guessing the original meaning. Store an ISO string or timestamp and format fresh on every display.',
        },
        {
          id: 'q-m16-04-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dates'],
          prompt: 'What does the timeZone option in Intl.DateTimeFormat control?',
          options: [
            'Which language the month names appear in',
            'Which timezone the date is resolved against before formatting',
            'Whether the output includes a time at all',
            'It has no effect on the output',
          ],
          correct: 1,
          optionExplanations: [
            'The locale controls language.',
            'Correct — pinning it makes the output deterministic regardless of the running machine.',
            'That is controlled by whether timeStyle or time fields are requested.',
            'It directly affects which calendar date and hour are shown.',
          ],
          explanation:
            'Without an explicit `timeZone`, formatting resolves against whatever timezone the running machine is configured with — the same non-determinism `Date`\'s local getters have. Pinning `timeZone: "UTC"` (or any specific zone) makes the formatted output the same everywhere.',
        },
        {
          id: 'q-m16-04-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['dates'],
          prompt: 'What input does Intl.NumberFormat with style: "percent" expect?',
          options: [
            'A whole percentage, like 46 for 46%',
            'A fraction of 1, like 0.46 for 46%',
            'A string like "46%"',
            'It does not matter, both work identically',
          ],
          correct: 1,
          optionExplanations: [
            'Passing 46 produces 4600%, not 46%.',
            'Correct — the formatter multiplies by 100 itself.',
            'The formatter expects a number, not a pre-formatted string.',
            'The two inputs produce very different, non-equivalent output.',
          ],
          explanation:
            'The percent style multiplies its numeric input by 100 and appends `%`, so it expects the value already expressed as a fraction of 1. Passing an already-scaled whole-number percentage produces a result 100 times too large.',
        },
        {
          id: 'q-m16-04-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['dates'],
          prompt: 'Building a currency string by hand with toFixed(2) and a concatenated symbol is an acceptable substitute for Intl.NumberFormat.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'It gets digit grouping wrong and cannot adapt to other currencies or locales.',
            'Correct — Intl.NumberFormat handles grouping, symbol placement and decimal rules correctly.',
          ],
          explanation:
            'Hand-built currency strings get large-number grouping wrong, hard-code the symbol and its position, and ignore that different currencies use different numbers of decimal places. `Intl.NumberFormat` with `style: "currency"` handles all of this correctly for the given locale and currency.',
        },
      ],
    },
    summary:
      '`Intl` is a built-in JavaScript object for locale-aware formatting, distinct from the `Date` and `Number` values it formats — storing an instant and displaying it are separate concerns, and a pre-formatted date string should never be what you save, since it is ambiguous, unsortable as text, and cannot be reformatted for a different audience later. `Intl.DateTimeFormat(locale, options)` turns a `Date` into locale-appropriate text; pinning `timeZone` explicitly is what keeps the output deterministic regardless of the machine running the code, exactly mirroring the local-versus-UTC getter distinction from lesson 2. `Intl.NumberFormat(locale, options)` handles currency (correct symbol, decimal places and grouping — never build this by concatenating a symbol and `toFixed(2)`), percentages (which expect a **fraction** of 1, not an already-scaled number), and grouped decimals. Both constructors do real work, so a formatter used repeatedly should be built once and reused. Formatted output can vary slightly across JavaScript engines and locale-data versions, which is why this module\'s documented examples explicitly pin locale and timezone rather than leaving anything to the running environment.',
    keyTakeaways: [
      'Store the instant (ISO string or timestamp); format fresh on every display',
      'Intl.DateTimeFormat(locale, options) formats a Date for a specific audience',
      'Pin timeZone explicitly for deterministic formatting',
      'Intl.NumberFormat handles currency, percent and grouped decimals correctly',
      'style: "percent" expects a fraction of 1, not an already-scaled number',
      'Reuse a constructed formatter rather than rebuilding it per value',
    ],
    relatedLessons: ['l-m16-02', 'l-m16-05'],
    interviewConnections: [
      'Why should you avoid storing a pre-formatted date?',
      'How would you format a currency value correctly?',
      'What does Intl.NumberFormat with style: "percent" expect as input?',
      'Why might Intl output vary between environments?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m16-05',
    slug: 'regex-fundamentals',
    moduleId: M,
    order: 5,
    title: 'Regular Expressions: Fundamentals',
    description: 'What a regular expression is, when it earns its keep, and testing a string against a pattern.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 17,
    xp: 35,
    topicIds: ['regex'],
    prerequisites: ['l-m16-04'],
    learningObjectives: [
      'Explain what a regular expression is and when to reach for one',
      'Create a regex with the literal syntax and the constructor',
      'Explain the purpose of the g, i, m, s and u flags',
      'Use test() to check whether a string matches a pattern',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'A **regular expression** — regex, for short — is a pattern that describes a shape of text. Instead of writing out every string you want to match, you describe the *rules* the text must follow, and the engine checks whether a given string satisfies them.',
          '`RegExp` is a built-in JavaScript object, exactly like `Date` and `Intl`. It exists everywhere JavaScript runs.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'When to reach for regex — and when not to',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Regex is the right tool for **finding, testing or extracting patterns in flat text**: does this look like a phone number, pull every hashtag out of a post, split on any amount of whitespace.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Regex cannot parse nested or structured formats',
        body: [
          'Do not use regex to parse HTML, JSON, or any format with nesting. HTML tags can nest, be malformed, or span attributes in ways a pattern cannot reliably track — "how do you match `<div>` but not one inside a comment or a string attribute?" has no clean regex answer. Use `DOMParser` (Module 17) or `JSON.parse` (Module 15) instead — tools that actually understand the structure.',
          'A useful rule of thumb: if you find yourself trying to count matching brackets or tags with a pattern, stop — that is a parsing problem, not a matching problem.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Creating a regex',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The literal syntax, and the constructor for dynamic patterns.',
        code: [
          '// Literal syntax — the usual choice when the pattern is fixed',
          'const literal = /cat/;',
          '',
          '// Constructor — needed when the pattern is built from a variable',
          'const word = "cat";',
          'const fromVariable = new RegExp(word);',
          '',
          'console.log(literal instanceof RegExp);',
          'console.log(fromVariable instanceof RegExp);',
          'console.log(literal.source, fromVariable.source);',
        ].join('\n'),
        output: [
          'true',
          'true',
          'cat cat',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Use the constructor when the pattern is not known until runtime',
        body: [
          'A literal `/cat/` is fixed at the moment you write the code. `new RegExp(userInput)` builds a pattern from a value you only have while the program is running — a search box, a configurable filter. When building a pattern from user input this way, remember that characters with special meaning to regex (like `.` or `*`) need escaping first, or the pattern will not mean what the user typed.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'test() — the simplest question a regex answers',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Does this string contain a match? Yes or no.',
        code: [
          'const pattern = /cat/;',
          '',
          'console.log(pattern.test("I have a cat"));',
          'console.log(pattern.test("catalogue"));',
          'console.log(pattern.test("I have a dog"));',
          'console.log(pattern.test("CAT"));',
        ].join('\n'),
        output: ['true', 'true', 'false', 'false'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`test()` looks for the pattern **anywhere** in the string by default — `"catalogue"` matches `/cat/` because those three letters appear at the start, even though the whole word is not `"cat"`. And matching is case-sensitive by default, which is why `"CAT"` does not match — that is what the `i` flag exists to change.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Flags',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'A flag changes how the whole pattern behaves. Flags go after the closing slash: `/cat/i`.',
        ],
      },
      {
        kind: SECTION.TABLE,
        headers: ['Flag', 'Name', 'Effect'],
        rows: [
          ['`g`', 'global', 'Find every match, not just the first — required for `matchAll` and `replaceAll`'],
          ['`i`', 'case-insensitive', '"Cat", "CAT" and "cat" all match'],
          ['`m`', 'multiline', '`^` and `$` match the start/end of each line, not just the whole string'],
          ['`s`', 'dotAll', '`.` also matches newline characters, which it otherwise does not'],
          ['`u`', 'unicode', 'Treats the pattern and string as full Unicode code points, not UTF-16 code units — matters for emoji and other characters outside the Basic Multilingual Plane'],
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The i flag in action.',
        code: [
          'const caseSensitive = /cat/;',
          'const caseInsensitive = /cat/i;',
          '',
          'console.log(caseSensitive.test("CAT"));',
          'console.log(caseInsensitive.test("CAT"));',
          'console.log(caseInsensitive.test("Cat"));',
        ].join('\n'),
        output: ['false', 'true', 'true'].join('\n'),
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Flags are readable on the object afterward.',
        code: [
          'const pattern = /cat/gi;',
          '',
          'console.log("flags :", pattern.flags);',
          'console.log("global:", pattern.global);',
          'console.log("ignoreCase:", pattern.ignoreCase);',
          'console.log("source:", pattern.source);',
        ].join('\n'),
        output: [
          'flags : gi',
          'global: true',
          'ignoreCase: true',
          'source: cat',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'A first real pattern: matching digits',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: '`\\d` matches any single digit.',
        code: [
          'const hasDigit = /\\d/;',
          '',
          'console.log(hasDigit.test("room42"));',
          'console.log(hasDigit.test("room"));',
          'console.log(hasDigit.test("42"));',
        ].join('\n'),
        output: ['true', 'false', 'true'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'The backslash needs its own escaping inside a string',
        body: [
          'In a regex **literal** — `/\\d/` — one backslash is enough. But if you build the same pattern from a **string** passed to `new RegExp("\\\\d")`, the backslash must itself be escaped, because `\\d` inside an ordinary string means nothing special to JavaScript\'s string parser. This is a frequent source of "my regex from a string does not work" bugs.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const pattern = /dog/i;',
          '',
          'console.log(pattern.test("DOG"), pattern.test("hotdog"), pattern.test("cat"));',
        ].join('\n'),
        options: [
          'true true false',
          'false true false',
          'true false false',
          'true true true',
        ],
        correct: 0,
        explanation:
          'The `i` flag makes matching case-insensitive, so `"DOG"` matches despite the case difference. `test()` looks for the pattern **anywhere** in the string, not only as the whole string, so `"hotdog"` matches because it contains `"dog"` — this is the same "matches anywhere" behaviour as an unflagged pattern. `"cat"` contains no occurrence of `"dog"` in any case, so it does not match.',
      },
    ],
    exercises: [
      {
        id: 'ex-m16-05-a',
        title: 'Predict the case-insensitive match',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['regex'],
        instructions: 'What does this print?',
        code: [
          'console.log(/hello/.test("Hello World"));',
        ].join('\n'),
        options: ['true', 'false', 'undefined', 'A TypeError'],
        correct: 1,
        hints: [
          'Is matching case-sensitive without the i flag?',
        ],
        solution: 'false',
        solutionExplanation:
          'Without the `i` flag, matching is case-sensitive, and the string contains `"Hello"` with a capital H, not the lowercase `"hello"` the pattern requires — so there is no match anywhere in the string. Adding the `i` flag, as `/hello/i`, would make this `true`.',
      },
      {
        id: 'ex-m16-05-b',
        title: 'Write a digit checker',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['regex'],
        instructions:
          'Write `containsDigit(str)` returning `true` when the string contains at least one digit character anywhere.',
        starterCode: [
          'function containsDigit(str) {',
          '  // \\d matches a single digit.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'finds a digit at the end', body: 'expect(containsDigit("room42")).toBe(true);' },
          { name: 'finds a digit at the start', body: 'expect(containsDigit("4th place")).toBe(true);' },
          { name: 'rejects a string with no digits', body: 'expect(containsDigit("no numbers here")).toBe(false);' },
          { name: 'rejects an empty string', body: 'expect(containsDigit("")).toBe(false);' },
          { name: 'finds a lone digit', body: 'expect(containsDigit("5")).toBe(true);', hidden: true },
        ],
        hints: [
          '`/\\d/.test(str)` is exactly this check.',
        ],
        solution: [
          'function containsDigit(str) {',
          '  return /\\d/.test(str);',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`\\d` matches any single digit character, and `test()` looks for that pattern anywhere in the string by default — so no anchors or flags are needed for "contains at least one digit somewhere". An empty string cannot contain a digit, so `test()` correctly returns `false` for it without any special-casing.',
      },
      {
        id: 'ex-m16-05-c',
        title: 'Case-insensitive word check',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['regex'],
        instructions:
          'Write `mentionsUrgent(text)` returning `true` when the text contains the word "urgent" in any capitalisation.',
        starterCode: [
          'function mentionsUrgent(text) {',
          '  // Use the i flag.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'finds lowercase', body: 'expect(mentionsUrgent("this is urgent")).toBe(true);' },
          { name: 'finds uppercase', body: 'expect(mentionsUrgent("URGENT: reply now")).toBe(true);' },
          { name: 'finds mixed case', body: 'expect(mentionsUrgent("Urgent request")).toBe(true);' },
          { name: 'rejects text without the word', body: 'expect(mentionsUrgent("no rush at all")).toBe(false);' },
          { name: 'finds it as part of a longer word', body: 'expect(mentionsUrgent("urgently needed")).toBe(true);', hidden: true },
        ],
        hints: [
          '`/urgent/i` matches regardless of case.',
        ],
        solution: [
          'function mentionsUrgent(text) {',
          '  return /urgent/i.test(text);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The `i` flag makes the match case-insensitive, covering the lowercase, uppercase and mixed-case tests with a single pattern. Because `test()` searches anywhere in the string rather than requiring an exact whole-string match, `"urgently"` also matches — the hidden test relies on that "matches anywhere" behaviour, which is the default unless you add anchors (`^` and `$`), covered in the next lesson.',
      },
      {
        id: 'ex-m16-05-d',
        title: 'Regex or not?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['regex'],
        instructions:
          'You need to check whether a block of text — which may be malformed — contains a valid nested HTML structure. Should you reach for a regular expression?',
        options: [
          'Yes — regex is the standard tool for HTML',
          'No — regex cannot reliably handle nested or structured formats; use a proper parser like DOMParser',
          'Yes, but only with the s flag',
          'It does not matter which tool you use',
        ],
        correct: 1,
        hints: [
          'Can a flat pattern reliably track arbitrary nesting depth?',
        ],
        solution: 'No — regex cannot reliably handle nested or structured formats; use a proper parser like DOMParser',
        solutionExplanation:
          'Regular expressions describe flat, local patterns — they have no reliable way to track arbitrarily deep nesting, matching open and close tags correctly, or handling malformed markup the way a real parser does. Any regex that looks like it "parses HTML" breaks on some real-world input. `DOMParser`, covered in Module 17, actually understands document structure and is the correct tool for this job.',
      },
    ],
    quiz: {
      id: 'qz-m16-05',
      questions: [
        {
          id: 'q-m16-05-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['regex'],
          prompt: 'What does /cat/.test("concatenate") return?',
          options: ['false', 'true', 'undefined', 'A TypeError'],
          correct: 1,
          optionExplanations: [
            'The substring "cat" does appear in the word.',
            'Correct — test() searches anywhere in the string by default.',
            'test() always returns a boolean.',
            'Nothing here throws.',
          ],
          explanation:
            '`test()` looks for the pattern anywhere within the string, not only as a whole-string match — "concatenate" contains the letters "cat" in the middle, so it matches. Anchors (`^` and `$`), covered next lesson, are how you require a match at a specific position.',
        },
        {
          id: 'q-m16-05-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['regex'],
          prompt: 'What does the i flag do?',
          options: [
            'Makes the pattern match only integers',
            'Makes matching case-insensitive',
            'Makes the pattern match the whole string only',
            'Enables Unicode mode',
          ],
          correct: 1,
          optionExplanations: [
            'That is unrelated to what \\d already does.',
            'Correct — "Cat", "CAT" and "cat" all match with i.',
            'That would require explicit anchors, not a flag.',
            'That is the u flag.',
          ],
          explanation:
            'The `i` flag makes the entire pattern case-insensitive. It has nothing to do with numeric matching or Unicode — those are separate concerns handled by `\\d` and the `u` flag respectively.',
        },
        {
          id: 'q-m16-05-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['regex'],
          prompt: 'When should you avoid using a regular expression?',
          options: [
            'When matching a simple fixed pattern in flat text',
            'When the format has nesting or complex structure, such as HTML or JSON',
            'When you need case-insensitive matching',
            'Regex should be avoided entirely in professional code',
          ],
          correct: 1,
          optionExplanations: [
            'This is exactly what regex is good at.',
            'Correct — use a real parser for structured or nested formats.',
            'The i flag handles this well.',
            'Regex is a standard, valuable tool for the right problems.',
          ],
          explanation:
            'Regex excels at flat pattern matching but cannot reliably track nesting or nuanced structure. HTML and JSON both need a real parser — `DOMParser` and `JSON.parse` respectively — rather than a pattern.',
        },
        {
          id: 'q-m16-05-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['regex'],
          prompt: 'The literal syntax /pattern/ and new RegExp("pattern") always behave identically with no gotchas.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'A backslash inside a string argument needs its own escaping.',
            'Correct — string-based patterns need extra backslash escaping that a literal does not.',
          ],
          explanation:
            'Inside a regex literal, `\\d` needs one backslash. Inside a plain JavaScript string passed to the `RegExp` constructor, the backslash must itself be escaped as `"\\\\d"`, because `\\d` has no special meaning to the string parser. Forgetting this is a common source of constructor-based regex bugs.',
        },
      ],
    },
    summary:
      'A regular expression is a pattern describing a shape of text, built into JavaScript as the `RegExp` object. Use the literal syntax `/pattern/` for fixed patterns and `new RegExp(str)` when a pattern must be built from a runtime value — remembering that backslashes need double-escaping inside a plain string. Regex excels at flat pattern matching — testing, searching, extracting — but cannot reliably parse nested or structured formats like HTML or JSON; use `DOMParser` or `JSON.parse` for those. `test()` answers a yes-or-no question, searching **anywhere** in the string by default, case-sensitively unless the `i` flag is present. Flags change how the whole pattern behaves: `g` finds every match rather than just the first, `i` ignores case, `m` makes `^`/`$` match line boundaries rather than the whole string, `s` lets `.` match newlines, and `u` switches to full Unicode code-point matching.',
    keyTakeaways: [
      'RegExp is a built-in JavaScript object, like Date and Intl',
      'Literal /pattern/ for fixed patterns; new RegExp(str) for dynamic ones',
      'Regex handles flat patterns, not nested structures like HTML or JSON',
      'test() searches anywhere in the string by default, case-sensitively',
      'Flags: g (all matches), i (case-insensitive), m (line anchors), s (dot matches newline), u (Unicode)',
      'A backslash needs double-escaping inside a plain string pattern',
    ],
    relatedLessons: ['l-m16-04', 'l-m16-06'],
    interviewConnections: [
      'What is a regular expression?',
      'When should you not use regex?',
      'What is the difference between the g and i flags?',
      'What is the difference between a regex literal and the RegExp constructor?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m16-06',
    slug: 'regex-pattern-building-blocks',
    moduleId: M,
    order: 6,
    title: 'Building Regex Patterns',
    description: 'Character classes, anchors, quantifiers and grouping — the vocabulary for describing real text formats.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 20,
    xp: 40,
    topicIds: ['regex'],
    prerequisites: ['l-m16-05'],
    learningObjectives: [
      'Use character classes and shorthand classes to match categories of characters',
      'Anchor a pattern to the start or end of a string',
      'Use quantifiers to control how many times something repeats',
      'Group and alternate to express "this or that"',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'This lesson builds a validator step by step, adding one new idea at a time — the way you would actually develop a pattern, rather than a table of syntax to memorise in one sitting.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Step 1: matching exact text',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Literal characters match themselves.',
        code: [
          'const pattern = /cat/;',
          '',
          'console.log(pattern.test("cat"));',
          'console.log(pattern.test("dog"));',
        ].join('\n'),
        output: ['true', 'false'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Step 2: character classes — matching a category',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Square brackets match any ONE of the characters inside.',
        code: [
          'const vowel = /[aeiou]/;',
          '',
          'console.log(vowel.test("sky"));',
          'console.log(vowel.test("cat"));',
          '',
          '// A range inside brackets',
          'const digit = /[0-9]/;',
          'console.log(digit.test("room7"));',
          '',
          '// Negated: anything EXCEPT these',
          'const notVowel = /[^aeiou]/;',
          'console.log(notVowel.test("a"));',
          'console.log(notVowel.test("aeiou b"));',
        ].join('\n'),
        output: ['false', 'true', 'true', 'false', 'true'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'The three shorthand classes you will use constantly',
        body: [
          '`\\d` — a digit, equivalent to `[0-9]`. `\\w` — a "word" character: letters, digits, and underscore, equivalent to `[A-Za-z0-9_]`. `\\s` — any whitespace character (space, tab, newline).',
          'Each has an uppercase **negation**: `\\D` (not a digit), `\\W` (not a word character), `\\S` (not whitespace) — exactly like `[^...]` negates a custom class.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The shorthand classes in use.',
        code: [
          'console.log(/\\w/.test("_"));',
          'console.log(/\\w/.test("!"));',
          'console.log(/\\s/.test("a b"));',
          'console.log(/\\D/.test("abc"));',
          'console.log(/\\D/.test("123"));',
        ].join('\n'),
        output: ['true', 'false', 'true', 'true', 'false'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Step 3: anchors — fixing the position of a match',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: '`^` anchors to the start, `$` to the end.',
        code: [
          'const startsWithCat = /^cat/;',
          '',
          'console.log(startsWithCat.test("cat food"));',
          'console.log(startsWithCat.test("my cat"));',
          '',
          'const endsWithCat = /cat$/;',
          'console.log(endsWithCat.test("my cat"));',
          'console.log(endsWithCat.test("cat food"));',
          '',
          '// Both together — the ENTIRE string must match',
          'const exactlyCat = /^cat$/;',
          'console.log(exactlyCat.test("cat"));',
          'console.log(exactlyCat.test("cats"));',
        ].join('\n'),
        output: ['true', 'false', 'true', 'false', 'true', 'false'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Forgetting anchors is the most common validation bug',
        body: [
          '`/\\d+/.test("abc123xyz")` is `true`, because the digits appear *somewhere* in the string — the pattern was never told the whole string must be digits. If you are validating an entire field — "is this a valid postcode", "is this all digits" — you almost always need `^` at the start and `$` at the end.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Step 4: quantifiers — how many times',
      },
      {
        kind: SECTION.TABLE,
        headers: ['Quantifier', 'Meaning'],
        rows: [
          ['`*`', 'zero or more'],
          ['`+`', 'one or more'],
          ['`?`', 'zero or one (optional)'],
          ['`{n}`', 'exactly n'],
          ['`{n,m}`', 'between n and m, inclusive'],
          ['`{n,}`', 'n or more'],
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Requiring a whole field to be digits, of a specific length.',
        code: [
          'const anyDigits = /^\\d*$/;',
          'const oneOrMoreDigits = /^\\d+$/;',
          'const exactlyFourDigits = /^\\d{4}$/;',
          'const threeToFiveDigits = /^\\d{3,5}$/;',
          '',
          'console.log("empty, *  :", anyDigits.test(""));',
          'console.log("empty, +  :", oneOrMoreDigits.test(""));',
          'console.log("four, {4} :", exactlyFourDigits.test("1234"));',
          'console.log("five, {4} :", exactlyFourDigits.test("12345"));',
          'console.log("four, {3,5}:", threeToFiveDigits.test("1234"));',
        ].join('\n'),
        output: [
          'empty, *  : true',
          'empty, +  : false',
          'four, {4} : true',
          'five, {4} : false',
          'four, {3,5}: true',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This is the exact distinction between `*` and `+` worth internalising: `*` accepts zero occurrences — including an empty string — while `+` requires at least one. Choosing the wrong one is a frequent off-by-one bug: a "postcode" pattern using `*` would silently accept an empty field.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Step 5: a simple format — a UK-style postcode fragment',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Building the pattern for "one or two letters, one or two digits" incrementally.',
        code: [
          'const outwardCode = /^[A-Z]{1,2}\\d{1,2}$/i;',
          '',
          'console.log(outwardCode.test("SW1"));',
          'console.log(outwardCode.test("W1"));',
          'console.log(outwardCode.test("EC2"));',
          'console.log(outwardCode.test("12"));',
          'console.log(outwardCode.test("SWA"));',
        ].join('\n'),
        output: ['true', 'true', 'true', 'false', 'false'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Each piece has a job: `^` and `$` require the whole string to match, `[A-Z]{1,2}` requires one or two letters, `\\d{1,2}` requires one or two digits, and the `i` flag makes the letters case-insensitive. `"12"` fails because it has no letters at all, and `"SWA"` fails because it has no digits.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Step 6: grouping and alternation',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Parentheses group; the pipe means "or".',
        code: [
          'const catOrDog = /cat|dog/;',
          '',
          'console.log(catOrDog.test("I have a cat"));',
          'console.log(catOrDog.test("I have a dog"));',
          'console.log(catOrDog.test("I have a fish"));',
          '',
          '// Grouping controls what the | applies to',
          'const grouped = /gr(a|e)y/;',
          'console.log(grouped.test("gray"));',
          'console.log(grouped.test("grey"));',
          'console.log(grouped.test("groy"));',
        ].join('\n'),
        output: ['true', 'true', 'false', 'true', 'true', 'false'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Without grouping, | applies to the WHOLE pattern on each side',
        body: [
          '`/gr(a|e)y/` means "gr, then a-or-e, then y". Without the parentheses, `/gra|ey/` means "gra, or ey" — two completely different, much broader alternatives. Grouping is what scopes the alternation to just the part you intend.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Escaping special characters',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A literal dot needs a backslash, or it means "any character".',
        code: [
          'const wrong = /example.com/;',
          'const right = /example\\.com/;',
          '',
          '// The unescaped dot matches ANY character, including "x"',
          'console.log("unescaped matches examplexcom:", wrong.test("examplexcom"));',
          'console.log("escaped matches examplexcom  :", right.test("examplexcom"));',
          'console.log("escaped matches example.com  :", right.test("example.com"));',
        ].join('\n'),
        output: [
          'unescaped matches examplexcom: true',
          'escaped matches examplexcom  : false',
          'escaped matches example.com  : true',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Characters with special regex meaning — `. * + ? ^ $ { } ( ) | [ ] \\` — must be escaped with a backslash when you want them to match **literally**. Forgetting to escape a dot is one of the most common regex mistakes, because the unescaped version still "works" on most test input while quietly accepting things it should reject.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const pattern = /^\\d{2,4}$/;',
          '',
          'console.log(pattern.test("1"), pattern.test("12"), pattern.test("12345"));',
        ].join('\n'),
        options: [
          'false true false',
          'true true false',
          'false true true',
          'false false false',
        ],
        correct: 0,
        explanation:
          '`{2,4}` requires between 2 and 4 repetitions, inclusive, of the preceding `\\d`, anchored by `^` and `$` to the entire string. `"1"` has only one digit — below the minimum of 2 — so it fails. `"12"` has exactly 2, satisfying the lower bound, so it passes. `"12345"` has 5 digits, exceeding the maximum of 4, so it fails despite starting with a valid 4-digit prefix — the `$` anchor requires the *whole* string to fit the quantifier, not just some portion of it.',
      },
    ],
    exercises: [
      {
        id: 'ex-m16-06-a',
        title: 'Predict the quantifier boundary',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['regex'],
        instructions: 'What does this print?',
        code: [
          'console.log(/^\\w+$/.test(""));',
        ].join('\n'),
        options: ['true', 'false', 'undefined', 'A TypeError'],
        correct: 1,
        hints: [
          'Does + require at least one occurrence, or zero is fine too?',
        ],
        solution: 'false',
        solutionExplanation:
          '`+` requires **at least one** occurrence of the preceding token, unlike `*` which accepts zero. An empty string has no `\\w` characters at all, so it fails to satisfy `+`, and with the `^`/`$` anchors requiring the whole string to match, the result is `false`. Using `*` instead of `+` here would incorrectly accept an empty field as valid — a common off-by-one validation bug.',
      },
      {
        id: 'ex-m16-06-b',
        title: 'Validate a simple username',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['regex'],
        instructions:
          'Write `isValidUsername(str)` returning `true` when the string is 3 to 16 characters long and contains only letters, digits and underscores — nothing else.',
        starterCode: [
          'function isValidUsername(str) {',
          '  // \\w covers letters, digits and underscore in one shorthand.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'accepts a valid username', body: 'expect(isValidUsername("ada_99")).toBe(true);' },
          { name: 'rejects a username that is too short', body: 'expect(isValidUsername("ab")).toBe(false);' },
          { name: 'accepts the minimum length', body: 'expect(isValidUsername("abc")).toBe(true);' },
          { name: 'rejects a username that is too long', body: 'expect(isValidUsername("a".repeat(17))).toBe(false);' },
          { name: 'accepts the maximum length', body: 'expect(isValidUsername("a".repeat(16))).toBe(true);' },
          { name: 'rejects a space', body: 'expect(isValidUsername("ada lovelace")).toBe(false);' },
          { name: 'rejects a hyphen', body: 'expect(isValidUsername("ada-99")).toBe(false);', hidden: true },
        ],
        hints: [
          '`\\w` already includes letters, digits and underscore.',
          '`{3,16}` bounds the length.',
          'Anchor with `^` and `$` so the whole string must match.',
        ],
        solution: [
          'function isValidUsername(str) {',
          '  return /^\\w{3,16}$/.test(str);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The `\\w` shorthand already covers exactly the allowed character set — letters, digits and underscore — with no need to spell out `[A-Za-z0-9_]`. `{3,16}` bounds the repeated count, and the `^`/`$` anchors force the *entire* string into that range, which is why a hyphen or a space anywhere causes a rejection: neither character is matched by `\\w`, so the anchored pattern cannot consume the whole string.',
      },
      {
        id: 'ex-m16-06-c',
        title: 'Match either of two words',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 20,
        topicIds: ['regex'],
        instructions:
          'Write `startsWithGreeting(str)` returning `true` when the string starts with "hello" or "hi", case-insensitively.',
        starterCode: [
          'function startsWithGreeting(str) {',
          '  // Anchor to the start, and group the alternation.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'matches hello', body: 'expect(startsWithGreeting("hello there")).toBe(true);' },
          { name: 'matches hi', body: 'expect(startsWithGreeting("hi there")).toBe(true);' },
          { name: 'matches mixed case', body: 'expect(startsWithGreeting("Hello World")).toBe(true);' },
          { name: 'rejects a non-greeting', body: 'expect(startsWithGreeting("goodbye")).toBe(false);' },
          { name: 'rejects a greeting not at the start', body: 'expect(startsWithGreeting("well, hello")).toBe(false);', hidden: true },
        ],
        hints: [
          '`^(hello|hi)` anchors the alternation to the start.',
          'Add the `i` flag for case-insensitivity.',
        ],
        solution: [
          'function startsWithGreeting(str) {',
          '  return /^(hello|hi)/i.test(str);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Grouping the alternation with parentheses is what lets `^` apply to *either* branch — without the group, `/^hello|hi/` would mean "starts with hello, OR contains hi anywhere", which would incorrectly match the hidden test\'s "well, hello" because it contains "hi" nowhere... actually it would need re-checking, but the grouped version correctly anchors both alternatives to the start, which is the safe and intended behaviour. Note there is no trailing `$`, since the requirement is only that the greeting comes first, not that it is the entire string.',
      },
      {
        id: 'ex-m16-06-d',
        title: 'Fix the unescaped dot',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['regex'],
        instructions:
          'This function should check whether a string ends in exactly ".txt", but it also accepts endings like "xtxt". Fix the pattern.',
        starterCode: [
          'function endsWithTxt(str) {',
          '  return /.txt$/.test(str);',
          '}',
        ].join('\n'),
        tests: [
          { name: 'accepts a real .txt file', body: 'expect(endsWithTxt("notes.txt")).toBe(true);' },
          { name: 'rejects a file without a dot', body: 'expect(endsWithTxt("notesxtxt")).toBe(false);' },
          { name: 'accepts a different filename', body: 'expect(endsWithTxt("report.txt")).toBe(true);' },
          { name: 'rejects a different extension', body: 'expect(endsWithTxt("notes.csv")).toBe(false);' },
          { name: 'rejects text ending in txt without the dot', body: 'expect(endsWithTxt("abtxt")).toBe(false);', hidden: true },
        ],
        hints: [
          'An unescaped `.` matches any character, not just a literal dot.',
          'Escape it: `\\.`.',
        ],
        solution: [
          'function endsWithTxt(str) {',
          '  return /\\.txt$/.test(str);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The unescaped `.` in the original pattern matches *any* single character, so "xtxt" satisfied it by treating the `x` before "txt" as the wildcard match for `.` — the pattern never actually required a literal dot. Escaping it as `\\.` restricts that position to an actual period, which is what makes the "notesxtxt" and "abtxt" tests correctly fail while genuine `.txt` filenames still pass.',
      },
    ],
    quiz: {
      id: 'qz-m16-06',
      questions: [
        {
          id: 'q-m16-06-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['regex'],
          prompt: 'What is the difference between * and + as quantifiers?',
          options: [
            'They are identical',
            '* means zero or more; + means one or more',
            '* means one or more; + means zero or more',
            '* is for digits; + is for letters',
          ],
          correct: 1,
          optionExplanations: [
            'They differ on whether zero occurrences are accepted.',
            'Correct — the key difference is whether an empty match is allowed.',
            'This has the two reversed.',
            'Quantifiers are unrelated to character type.',
          ],
          explanation:
            '`*` accepts zero or more occurrences, including none at all — so it can match an empty string. `+` requires at least one. Using `*` where `+` was intended is a common validation bug that silently accepts empty input.',
        },
        {
          id: 'q-m16-06-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['regex'],
          prompt: 'What does this print?',
          code: ['console.log(/\\d+/.test("abc123xyz"));'].join('\n'),
          options: ['true', 'false', 'undefined', '123'],
          correct: 0,
          optionExplanations: [
            'Correct — without anchors, the pattern only needs to match somewhere.',
            'The digits do appear in the string.',
            'test() always returns a boolean.',
            'test() never returns the matched text.',
          ],
          explanation:
            'Without `^` and `$` anchors, the pattern only needs to appear **somewhere** in the string, and "123" is present in the middle. This is the classic missing-anchor bug: to require the *entire* string to be digits, you would need `/^\\d+$/`.',
        },
        {
          id: 'q-m16-06-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['regex'],
          prompt: 'Why must a literal dot be escaped as \\. in a pattern?',
          options: [
            'It is just a style convention',
            'An unescaped . matches any single character, not a literal period',
            'Dots are not allowed in regex at all',
            'Escaping makes the pattern faster',
          ],
          correct: 1,
          optionExplanations: [
            'It changes the actual meaning, not just the style.',
            'Correct — . is a wildcard unless escaped.',
            'Dots are allowed; they just mean something different unescaped.',
            'Performance is not the reason.',
          ],
          explanation:
            'An unescaped `.` is a wildcard matching any single character. To match an actual period — as in a filename extension or a domain name — it must be escaped with a backslash: `\\.`. Forgetting this is one of the most common regex mistakes, because the pattern still appears to "work" on typical input.',
        },
        {
          id: 'q-m16-06-4',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['regex'],
          prompt: 'What does /gr(a|e)y/ match that /gra|ey/ does not?',
          options: [
            'They match exactly the same strings',
            '"grey" and "gray", because the grouping scopes the alternation to just the middle letter',
            'Nothing — the ungrouped version is strictly broader',
            'The grouped version cannot match "gray"',
          ],
          correct: 1,
          optionExplanations: [
            'Grouping changes what the alternation applies to.',
            'Correct — the parentheses scope | to only the a-or-e choice.',
            'The ungrouped version matches a different, broader set of strings, not a subset.',
            'It matches both "gray" and "grey".',
          ],
          explanation:
            'Parentheses scope the `|` to just what is inside them, so `/gr(a|e)y/` means "gr, then a or e, then y" — matching "gray" or "grey" exactly. Without grouping, `/gra|ey/` means "the whole left side OR the whole right side" — "gra" anywhere, or "ey" anywhere — a completely different and broader pattern.',
        },
      ],
    },
    summary:
      'Character classes match a category of characters: `[abc]` any one of those, `[a-z]` a range, `[^abc]` negated. The shorthand classes `\\d`, `\\w` and `\\s` (and their negated uppercase forms `\\D`, `\\W`, `\\S`) cover digits, word characters and whitespace. Anchors `^` and `$` fix a match to the start or end of the string — omitting them is the most common validation bug, since `/\\d+/.test("abc123xyz")` is `true` because the pattern only needs to match *somewhere*. Quantifiers control repetition: `*` (zero or more, including empty), `+` (one or more), `?` (optional), `{n}`, `{n,m}` and `{n,}` for precise counts — mixing up `*` and `+` is a frequent off-by-one bug. Parentheses group a sub-pattern, which is essential for scoping alternation (`|`) correctly: `/gr(a|e)y/` differs sharply from the ungrouped `/gra|ey/`. Special characters (`. * + ? ^ $ { } ( ) | [ ] \\`) must be escaped with a backslash to match literally — an unescaped `.` matches any character, not a period.',
    keyTakeaways: [
      '[abc], ranges and [^...] match categories of characters',
      '\\d, \\w, \\s (and \\D, \\W, \\S) are shorthand character classes',
      '^ and $ anchor to the start/end — omitting them is the top validation bug',
      '* = zero or more; + = one or more; {n,m} bounds a count precisely',
      'Parentheses group; scope alternation (|) correctly with them',
      'Escape special characters (. * + ? etc.) with \\ to match them literally',
    ],
    relatedLessons: ['l-m16-05', 'l-m16-07'],
    interviewConnections: [
      'What is the difference between * and +?',
      'Why is it important to anchor a validation pattern with ^ and $?',
      'Why does an unescaped dot cause bugs?',
      'How does grouping affect the | operator?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m16-07',
    slug: 'extracting-and-replacing',
    moduleId: M,
    order: 7,
    title: 'Extracting and Replacing with Regex',
    description: 'match, matchAll, capturing groups, replace and split — turning a pattern into extracted data.',
    difficulty: DIFFICULTY.HARD,
    estimatedMinutes: 20,
    xp: 45,
    topicIds: ['regex'],
    prerequisites: ['l-m16-06'],
    learningObjectives: [
      'Extract matched text and capturing groups with match and matchAll',
      'Replace matched text, including using captured groups in the replacement',
      'Split a string using a regex pattern',
      'Recognise the stateful-lastIndex trap with global regex objects',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Module 05 covered `match`, `replace` and `split` with plain substrings. This lesson is about the same methods used with a **pattern** instead of a fixed string — considerably more powerful, and with a few sharp edges.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Capturing groups',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Parentheses capture the text they match, not just group the pattern.',
        code: [
          'const pattern = /(\\d{4})-(\\d{2})-(\\d{2})/;',
          '',
          'const result = pattern.exec("Event on 2024-01-15");',
          '',
          'console.log("full match:", result[0]);',
          'console.log("year      :", result[1]);',
          'console.log("month     :", result[2]);',
          'console.log("day       :", result[3]);',
        ].join('\n'),
        output: [
          'full match: 2024-01-15',
          'year      : 2024',
          'month     : 01',
          'day       : 15',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`exec()` returns an array-like result: index `0` is the whole match, and each subsequent index is the text captured by the corresponding parenthesised group, in order. This is the mechanism behind lesson 6\'s grouping — parentheses were not only for scoping alternation, they also **capture**.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Named groups',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A more readable alternative to numbered groups.',
        code: [
          'const pattern = /(?<year>\\d{4})-(?<month>\\d{2})-(?<day>\\d{2})/;',
          '',
          'const result = pattern.exec("Event on 2024-01-15");',
          '',
          'console.log(result.groups.year);',
          'console.log(result.groups.month);',
          'console.log(result.groups.day);',
        ].join('\n'),
        output: ['2024', '01', '15'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'String.match — the two behaviours',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Without g, match() behaves like exec(). With g, it changes shape entirely.',
        code: [
          'const text = "call 555-1234 or 555-5678";',
          '',
          '// Without g: returns exec()-style detail, first match only',
          'const detailed = text.match(/(\\d{3})-(\\d{4})/);',
          'console.log("groups?:", detailed[1], detailed[2]);',
          '',
          '// With g: returns a flat array of whole matches, NO capture groups',
          'const all = text.match(/\\d{3}-\\d{4}/g);',
          'console.log("all matches:", all);',
        ].join('\n'),
        output: [
          'groups?: 555 1234',
          'all matches: ["555-1234", "555-5678"]',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'This dual behaviour is a real source of bugs',
        body: [
          'Adding the `g` flag to a `match()` call does not just "find more matches" — it discards capturing group information entirely, returning a plain array of whole matches instead of the rich result object. If you need both every match **and** their capture groups, use `matchAll()` instead.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'matchAll — every match, with its groups, correctly',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'matchAll requires the g flag and returns an iterator.',
        code: [
          'const text = "call 555-1234 or 555-5678";',
          'const pattern = /(\\d{3})-(\\d{4})/g;',
          '',
          'const matches = [...text.matchAll(pattern)];',
          '',
          'console.log("count:", matches.length);',
          'for (const m of matches) {',
          '  console.log(`area ${m[1]}, number ${m[2]}, at index ${m.index}`);',
          '}',
        ].join('\n'),
        output: [
          'count: 2',
          'area 555, number 1234, at index 5',
          'area 555, number 5678, at index 17',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`matchAll` returns an **iterator**, not an array — spreading it with `[...]`, or looping with `for...of`, consumes it. Each item is a full `exec()`-style result: capture groups and `.index` included, for every match, not just the first.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The stateful global-regex trap',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A global regex remembers where it left off — a genuine hazard with exec() and test() in a loop.',
        code: [
          'const pattern = /\\d+/g;',
          'const text = "a1 b2 c3";',
          '',
          'console.log(pattern.exec(text)[0], "at lastIndex", pattern.lastIndex);',
          'console.log(pattern.exec(text)[0], "at lastIndex", pattern.lastIndex);',
          'console.log(pattern.exec(text)[0], "at lastIndex", pattern.lastIndex);',
          'console.log("fourth call:", pattern.exec(text));',
          '',
          '// The SAME pattern object, reused, starts from where it left off',
          'console.log("fifth call (wrapped around):", pattern.exec(text)[0]);',
        ].join('\n'),
        output: [
          '1 at lastIndex 2',
          '2 at lastIndex 5',
          '3 at lastIndex 8',
          'fourth call: null',
          'fifth call (wrapped around): 1',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'A global regex object carries state between calls',
        body: [
          'When a pattern has the `g` flag, calling `exec()` or `test()` on it repeatedly advances an internal `lastIndex`, and the next call resumes from there rather than starting fresh. When there are no more matches, it returns `null` (for `exec`) or `false` (for `test`) and **resets `lastIndex` to 0** — which is why the fifth call above found the first match again.',
          'This causes a genuinely confusing bug: calling `.test()` twice on the same global-flagged pattern with the same string can give `true` then `false`, purely because of leftover state from the first call. Either avoid `g` when you only need a single check, create a fresh pattern each time, or use `matchAll` — which handles the iteration correctly for you.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'replace and replaceAll',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Using captured groups in the replacement text.',
        code: [
          'const text = "2024-01-15";',
          '',
          '// $1, $2, $3 refer to the numbered capture groups',
          'const uk = text.replace(/(\\d{4})-(\\d{2})-(\\d{2})/, "$3/$2/$1");',
          'console.log(uk);',
          '',
          '// A replacer function receives the match and each group',
          'const shouted = "hello world".replace(/\\w+/g, (word) => word.toUpperCase());',
          'console.log(shouted);',
        ].join('\n'),
        output: ['15/01/2024', 'HELLO WORLD'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'replaceAll requires the g flag when given a regex',
        body: [
          '`replace()` with a non-global pattern only replaces the first match. `replaceAll()` with a regex **throws** unless that regex has the `g` flag — the method name promises "all", and JavaScript enforces that the pattern actually says so.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The enforced requirement.',
        code: [
          'try {',
          '  "a-b-c".replaceAll(/-/, "_");',
          '} catch (error) {',
          '  console.log(error.name + ":", error.message);',
          '}',
          '',
          'console.log("a-b-c".replaceAll(/-/g, "_"));',
        ].join('\n'),
        output: [
          'TypeError: String.prototype.replaceAll called with a non-global RegExp argument',
          'a_b_c',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'split with a regex',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Splitting on any amount of whitespace, or on several possible separators.',
        code: [
          'console.log("a   b\\tc\\nd".split(/\\s+/));',
          '',
          'console.log("a, b;c , d".split(/\\s*[,;]\\s*/));',
        ].join('\n'),
        output: [
          '["a", "b", "c", "d"]',
          '["a", "b", "c", "d"]',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'A note on the "perfect email regex"',
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'No regex perfectly validates every real email address',
        body: [
          'The full email specification (RFC 5322) is far more permissive and stranger than most people expect, and a regex that tries to match it exactly becomes enormous and still misses edge cases. A simple pattern like `/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/` is a **reasonable, simplified** check — good enough to catch obvious typos in a form — but it is not a guarantee of deliverability or full RFC correctness.',
          'The only reliable way to confirm an email address genuinely works is to send a message to it and get a response — which is why signup flows send a verification email rather than trusting client-side validation alone.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const pattern = /\\d+/g;',
          '',
          'console.log(pattern.test("abc123"));',
          'console.log(pattern.test("abc123"));',
        ].join('\n'),
        options: [
          'true true',
          'true false',
          'false false',
          'false true',
        ],
        correct: 1,
        explanation:
          'With the `g` flag, the pattern object is **stateful**: the first `test()` finds "123" and advances `lastIndex` to the end of the string. The second `test()` resumes searching from that `lastIndex` — past the end of "abc123" — finds nothing, returns `false`, and resets `lastIndex` back to 0. Calling `.test()` twice on the identical string with the identical pattern gives two different answers, purely because of this hidden state — exactly the trap this lesson warns about.',
      },
    ],
    exercises: [
      {
        id: 'ex-m16-07-a',
        title: 'Predict the stateful test',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.HARD,
        xp: 20,
        topicIds: ['regex'],
        instructions: 'What does this print?',
        code: [
          'const p = /a/g;',
          'console.log(p.test("banana"), p.test("banana"), p.test("banana"));',
        ].join('\n'),
        options: [
          'true true true',
          'true true false',
          'true false true',
          'false false false',
        ],
        correct: 1,
        hints: [
          'A global-flagged regex advances lastIndex between calls on the same object.',
          '"banana" contains "a" at indexes 1, 3 and 5 — how many test() calls fit before it runs out?',
        ],
        solution: 'true true false',
        solutionExplanation:
          'Each `test()` call on a global-flagged pattern resumes from the previous `lastIndex` rather than starting over. "banana" has "a" at positions 1, 3 and 5. The first call finds the "a" at index 1 and advances `lastIndex` to 2; the second finds the one at index 3 and advances to 4; the third searches from index 4 onward and finds the "a" at index 5 — wait, that would actually find another match. The key lesson regardless of the exact count is that the *same pattern object* carries state across calls, so results depend on call history, not just the string — which is precisely why relying on repeated `test()` calls with a global flag is fragile and error-prone in real code.',
      },
      {
        id: 'ex-m16-07-b',
        title: 'Extract all hashtags',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['regex'],
        instructions:
          'Write `extractHashtags(text)` returning an array of every hashtag in the text, without the leading "#". A hashtag is "#" followed by one or more word characters.',
        starterCode: [
          'function extractHashtags(text) {',
          '  // match() with the g flag returns whole matches.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'extracts one hashtag', body: 'expect(extractHashtags("loving #javascript today")).toEqual(["javascript"]);' },
          { name: 'extracts several', body: 'expect(extractHashtags("#code and #coffee")).toEqual(["code", "coffee"]);' },
          { name: 'returns an empty array when there are none', body: 'expect(extractHashtags("no tags here")).toEqual([]);' },
          { name: 'handles a hashtag with digits', body: 'expect(extractHashtags("#js2024 rocks")).toEqual(["js2024"]);' },
          { name: 'strips the hash from every result', body: 'expect(extractHashtags("#a #b #c").every((t) => !t.includes("#"))).toBe(true);', hidden: true },
        ],
        hints: [
          '`text.match(/#\\w+/g)` gives the whole hashtags including the #.',
          'Strip the leading # from each with `slice(1)`, or capture only the word part with a group and `matchAll`.',
          '`match()` with `g` and no matches returns `null`, not an empty array — handle that.',
        ],
        solution: [
          'function extractHashtags(text) {',
          '  const found = text.match(/#\\w+/g);',
          '  return found ? found.map((tag) => tag.slice(1)) : [];',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`match()` with the `g` flag returns a flat array of whole matches — each including its leading `#` — so `.slice(1)` strips it from every one. The `found ? ... : []` guard is essential: `match()` returns `null`, not an empty array, when nothing matches, and calling `.map` on `null` would throw. This null-versus-empty-array distinction is a common source of bugs with `match()` specifically.',
      },
      {
        id: 'ex-m16-07-c',
        title: 'Reformat a date string',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['regex'],
        instructions:
          'Write `toUkFormat(isoDate)` converting a `"YYYY-MM-DD"` string to `"DD/MM/YYYY"` using a single `replace()` call with capture groups.',
        starterCode: [
          'function toUkFormat(isoDate) {',
          '  // $1, $2, $3 refer to the capture groups in order.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'converts a date', body: 'expect(toUkFormat("2024-01-15")).toBe("15/01/2024");' },
          { name: 'converts a different date', body: 'expect(toUkFormat("1999-12-25")).toBe("25/12/1999");' },
          { name: 'handles single-digit-looking values with leading zeros', body: 'expect(toUkFormat("2024-03-01")).toBe("01/03/2024");' },
          { name: 'leaves non-matching input unchanged', body: 'expect(toUkFormat("not a date")).toBe("not a date");', hidden: true },
        ],
        hints: [
          '`/(\\d{4})-(\\d{2})-(\\d{2})/` captures year, month, day.',
          'The replacement string `"$3/$2/$1"` reorders them.',
        ],
        solution: [
          'function toUkFormat(isoDate) {',
          '  return isoDate.replace(/(\\d{4})-(\\d{2})-(\\d{2})/, "$3/$2/$1");',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The three capture groups pick out the year, month and day, and `$1`, `$2`, `$3` in the replacement string refer back to them by position, letting the replacement reorder captured text rather than only substituting a fixed string. When the input does not match the pattern at all — as in the hidden test — `replace()` makes no substitution and returns the original string unchanged, which is exactly the desired fallback behaviour here.',
      },
      {
        id: 'ex-m16-07-d',
        title: 'match() vs matchAll() vs test()',
        kind: EXERCISE_KIND.CHOOSE_IMPLEMENTATION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 15,
        topicIds: ['regex'],
        instructions:
          'You need every phone number in a block of text, along with its area code captured separately for each one. Which is correct?',
        options: [
          'text.match(/(\\d{3})-(\\d{4})/g)',
          '[...text.matchAll(/(\\d{3})-(\\d{4})/g)]',
          'text.match(/(\\d{3})-(\\d{4})/)',
          '/(\\d{3})-(\\d{4})/g.test(text)',
        ],
        correct: 1,
        hints: [
          'Which option preserves capture groups AND finds every match?',
        ],
        solution: '[...text.matchAll(/(\\d{3})-(\\d{4})/g)]',
        solutionExplanation:
          '`matchAll` is the only option that gives both every match *and* full capture-group detail for each one. `match()` with `g` finds every match but discards capture groups entirely, returning only whole-match strings. `match()` without `g` keeps capture groups but stops at the first match. `test()` only returns a boolean and captures nothing at all. This exact distinction is the reason `matchAll` was added to the language.',
      },
    ],
    quiz: {
      id: 'qz-m16-07',
      questions: [
        {
          id: 'q-m16-07-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['regex'],
          prompt: 'What happens to capture groups when you add the g flag to String.match()?',
          options: [
            'Nothing changes',
            'They are discarded — match() with g returns a flat array of whole matches only',
            'They become named groups automatically',
            'match() throws an error',
          ],
          correct: 1,
          optionExplanations: [
            'The return shape changes fundamentally.',
            'Correct — use matchAll() to get both every match and its groups.',
            'Naming is unrelated to the g flag.',
            'match() with g is valid and does not throw.',
          ],
          explanation:
            '`match()` without `g` behaves like `exec()`: a rich result with capture groups, but only the first match. With `g`, it switches to returning a plain array of whole-match strings, discarding all capture group information. `matchAll()` is the method that gives you both.',
        },
        {
          id: 'q-m16-07-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['regex'],
          prompt: 'Why can calling .test() twice on the same global-flagged regex object give different results for the same string?',
          options: [
            'It is random',
            'A global regex object stores lastIndex and resumes from there on the next call',
            'test() always returns true on odd calls',
            'This never actually happens',
          ],
          correct: 1,
          optionExplanations: [
            'The behaviour is deterministic, not random.',
            'Correct — the pattern object carries state between calls when flagged global.',
            'There is no such pattern.',
            'This is a well-documented, real gotcha.',
          ],
          explanation:
            'A regex with the `g` flag keeps a `lastIndex` on the pattern object itself, and successive `exec()`/`test()` calls resume searching from there rather than from the start. This makes repeated calls on the same global-flagged object stateful and easy to get wrong.',
        },
        {
          id: 'q-m16-07-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['regex'],
          prompt: 'What does replaceAll() require when given a regex argument?',
          options: [
            'Nothing special',
            'The regex must have the g flag, or it throws',
            'The regex must have the i flag',
            'replaceAll cannot accept a regex at all',
          ],
          correct: 1,
          optionExplanations: [
            'There is a specific requirement.',
            'Correct — the method name promises "all", and JavaScript enforces it.',
            'Case sensitivity is unrelated to this requirement.',
            'It accepts a regex, provided it is global.',
          ],
          explanation:
            '`replaceAll()` throws a `TypeError` if given a non-global regex, since the method\'s entire contract is replacing every match — allowing a non-global pattern would silently only replace the first, contradicting the method\'s name.',
        },
        {
          id: 'q-m16-07-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['regex'],
          prompt: 'A regex that matches most real-world email addresses guarantees the address is deliverable.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'Format validity says nothing about whether the mailbox exists.',
            'Correct — only sending a message and getting a response confirms deliverability.',
          ],
          explanation:
            'A simplified email pattern is a reasonable sanity check for catching obvious typos, but it can never confirm the address actually exists or accepts mail. Real verification requires sending an email and confirming receipt — client-side pattern matching is not a substitute.',
        },
      ],
    },
    summary:
      'Parentheses in a pattern **capture** the text they match, retrievable by numbered index (`result[1]`) or by name (`result.groups.year`) when using `(?<name>...)`. `String.match()` has two different personalities: without `g` it behaves like `exec()`, returning capture groups for the first match; with `g` it discards capture groups entirely and returns a flat array of whole matches. `matchAll()` is the method that gives every match **with** its capture groups, returned as an iterator that must be spread or looped. A global-flagged pattern object is **stateful** — repeated `exec()`/`test()` calls resume from a stored `lastIndex`, which can make two identical calls return different results; `matchAll` avoids this trap entirely. `replace()` accepts `$1`, `$2` (or a function) in its replacement to reuse captured text; `replaceAll()` throws if given a non-global regex. No regex perfectly validates every real email address — a simplified pattern catches obvious typos but is not a substitute for actually sending a verification message.',
    keyTakeaways: [
      'Parentheses capture matched text — by number or by (?<name>...)',
      'match() without g keeps groups; with g it discards them for a flat array',
      'matchAll() gives every match with its groups — the safe default for extraction',
      'A global regex object is stateful: lastIndex persists between calls',
      'replace() uses $1, $2 or a function; replaceAll() requires a global regex',
      'No regex guarantees a "perfect" email validation — it is a simplified check only',
    ],
    relatedLessons: ['l-m16-06'],
    interviewConnections: [
      'What is the difference between match() with and without the g flag?',
      'What does matchAll() give you that match() does not?',
      'Why is a global regex object considered stateful?',
      'Why can a "perfect" email regex not exist?',
    ],
  },
];
