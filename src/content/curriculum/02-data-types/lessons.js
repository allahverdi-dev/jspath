import { SECTION, CALLOUT_TONE, DIFFICULTY, EXERCISE_KIND, QUIZ_KIND } from '../../schema/types.js';

const M = 'm02';

export default [
  /* ================================================================== */
  {
    id: 'l-m02-01',
    slug: 'primitive-types',
    moduleId: M,
    order: 1,
    title: 'The Seven Primitive Types',
    description: 'Every value in JavaScript is either a primitive or an object. These are the seven primitives, and what dynamic typing means for you.',
    difficulty: DIFFICULTY.BEGINNER,
    estimatedMinutes: 15,
    xp: 25,
    topicIds: ['types'],
    prerequisites: ['l-m01-06'],
    learningObjectives: [
      'Name all seven primitive types and describe what each represents',
      'Explain what dynamic typing means and what it does not mean',
      'Recognise that primitives are immutable',
      'Explain why `"hi".toUpperCase()` works even though a string is not an object',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'JavaScript divides every possible value into exactly two categories: **primitives** and **objects**. There is no third option. Understanding this split explains a great deal that otherwise looks arbitrary — why `typeof` behaves the way it does, why some values can be compared with `===` and others cannot, and why copying sometimes shares data and sometimes does not.',
          'This lesson covers the primitives. The next few cover objects and the consequences of the split.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'The seven primitives',
      },
      {
        kind: SECTION.TABLE,
        headers: ['Type', 'Represents', 'Example'],
        rows: [
          ['`string`', 'Text of any length, including empty', '`"Ada"`, `\'\'`, `` `hi` ``'],
          ['`number`', 'All ordinary numbers — integers and decimals alike', '`42`, `-7`, `3.14`, `NaN`, `Infinity`'],
          ['`boolean`', 'One of exactly two values', '`true`, `false`'],
          ['`undefined`', 'A value exists but nothing was supplied', '`undefined`'],
          ['`null`', 'A deliberate, intentional absence of a value', '`null`'],
          ['`bigint`', 'Integers larger than `number` can hold safely', '`9007199254740993n`'],
          ['`symbol`', 'A guaranteed-unique identifier', '`Symbol("id")`'],
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'One number type, not several',
        body: [
          'If you have used Java, C# or Python, you may expect separate integer and floating-point types. JavaScript has one: `number`, which is a 64-bit floating-point value. `1` and `1.0` are the identical value, and `10 / 4` gives `2.5` rather than truncating to `2`.',
          'This simplifies a great deal, but it has consequences for precision that you will meet in lesson 4 of this module and in full detail in the Numbers module.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'All seven, declared and inspected.',
        code: [
          'const name = "Ada";',
          'const age = 36;',
          'const isEngineer = true;',
          'let notSupplied;',
          'const deliberatelyEmpty = null;',
          'const hugeNumber = 9007199254740993n;',
          'const uniqueKey = Symbol("id");',
          '',
          'console.log(typeof name);',
          'console.log(typeof age);',
          'console.log(typeof isEngineer);',
          'console.log(typeof notSupplied);',
          'console.log(typeof deliberatelyEmpty);',
          'console.log(typeof hugeNumber);',
          'console.log(typeof uniqueKey);',
        ].join('\n'),
        output: ['string', 'number', 'boolean', 'undefined', 'object', 'bigint', 'symbol'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Six of those seven results are exactly what you would expect. The fifth is not: `typeof null` reports `"object"`, even though `null` is definitely a primitive. That is a genuine, well-known bug in the language, and the next lesson is devoted to it.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Dynamic typing',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'JavaScript is **dynamically typed**. That means the *variable* has no type — only the *value* does. The same name can refer to a string now and a number later, and the language will not object.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Perfectly legal. Rarely a good idea.',
        code: [
          'let thing = "text";',
          'console.log(typeof thing);',
          '',
          'thing = 42;',
          'console.log(typeof thing);',
          '',
          'thing = true;',
          'console.log(typeof thing);',
        ].join('\n'),
        output: ['string', 'number', 'boolean'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Dynamic typing does not mean "no types"',
        body: [
          'A common misreading is that JavaScript is "untyped". It is not. Every value has a definite type, and operations genuinely fail or behave differently based on it — `"5" * 2` and `"5" + 2` give completely different answers because the types matter.',
          'What is dynamic is *when* the type is checked: at run time, as the operation happens, rather than ahead of time by a compiler. This is why a type mistake in JavaScript surfaces as a wrong value or a TypeError while the program is running, instead of a build error before it starts.',
        ],
      },
      {
        kind: SECTION.PROSE,
        body: [
          'In practice, professional code avoids letting a variable change type. A name that might hold a string or a number forces every reader — and every later line of code — to handle both possibilities. Keeping a variable\'s type stable is a convention, not a rule, but it is a strong one.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Primitives are immutable',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'A primitive value can never be changed. Not "should not be" — *cannot be*. There is no operation anywhere in JavaScript that turns the number 5 into the number 6, or modifies the string `"hello"` in place.',
          'This sounds abstract until you try it.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The string is not modified. It cannot be.',
        code: [
          'let greeting = "hello";',
          '',
          'try {',
          '  greeting[0] = "H";      // strict mode: throws',
          '} catch (error) {',
          '  console.log(error.name + ":", error.message);',
          '}',
          'console.log(greeting);',
          '',
          'greeting.toUpperCase();   // returns a NEW string, which is discarded',
          'console.log(greeting);',
          '',
          'greeting = greeting.toUpperCase();   // reassignment — this works',
          'console.log(greeting);',
        ].join('\n'),
        output: [
          'TypeError: Cannot assign to read only property \'0\' of string \'hello\'',
          'hello',
          'hello',
          'HELLO',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Strict mode makes the failure loud',
        body: [
          'Assigning to a string index fails either way, but *how* it fails depends on the mode. In **strict mode** it throws a `TypeError`, as above. In old-style non-strict code it fails silently — the line does nothing at all and execution continues.',
          'Strict mode is not an obscure setting: every ES module is strict automatically, as is every class body, and bundled application code is effectively always strict. The silent version is legacy behaviour you will mostly meet in old scripts. JSPath runs your code in strict mode, which is why you see the error rather than nothing.',
        ],
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Every string method returns a **new** string and leaves the original untouched. The only way to make a variable hold different text is reassignment — moving the label to a different value, exactly as you learned in the previous module.',
          'This is why `const` feels genuinely constant for primitives: reassignment is the only route to change, and `const` blocks it. With objects, mutation is also available, which is why `const` feels weaker there.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'So why does `"hi".toUpperCase()` work?',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'A fair question. Primitives are not objects, and only objects have methods — yet strings, numbers and booleans all appear to have them.',
          'JavaScript performs a trick called **autoboxing**. When you access a property on a primitive, the engine temporarily wraps it in a corresponding object (`String`, `Number`, `Boolean`), reads the property from that wrapper, and immediately discards the wrapper.',
        ],
      },
      {
        kind: SECTION.ANNOTATED_CODE,
        language: 'javascript',
        code: [
          'const word = "hi";',
          'console.log(word.toUpperCase());',
          '',
          'word.custom = 123;',
          'console.log(word.custom);',
        ].join('\n'),
        annotations: [
          { line: 2, text: 'The engine wraps `"hi"` in a temporary `String` object, calls `toUpperCase` on it, returns `"HI"`, then throws the wrapper away.' },
          { line: 4, text: 'A fresh temporary wrapper is created, `custom` is set on *it*, and the wrapper is immediately discarded — taking the property with it. In strict mode this throws instead.' },
          { line: 5, text: 'Prints `undefined`. Yet another new wrapper is created, and it has never heard of `custom`.' },
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Never use the object wrappers explicitly',
        body: [
          'You can write `new String("hi")` to create an actual String *object*. Do not. It produces something that behaves confusingly: `typeof` reports `"object"`, and `new String("hi") === "hi"` is `false` because you are comparing an object with a primitive.',
          'Calling `String(value)` **without** `new` is completely different and entirely fine — that converts a value to a primitive string, and you will use it constantly in the next module.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'let text = "abc";',
          'text.toUpperCase();',
          '',
          'console.log(text);',
        ].join('\n'),
        options: ['ABC', 'abc', 'undefined', 'TypeError'],
        correct: 1,
        explanation:
          'Strings are immutable, so `toUpperCase()` cannot modify `text` — it builds and returns a brand-new string `"ABC"`. Nothing captures that return value, so it is immediately discarded and the original is untouched. This is one of the most common early mistakes: calling a string method and expecting the variable to change. You need `text = text.toUpperCase();`.',
      },
      {
        kind: SECTION.TERMS,
        terms: [
          { term: 'Primitive', definition: 'One of the seven basic value types. Immutable, and not an object.' },
          { term: 'Dynamic typing', definition: 'Types belong to values rather than variables, and are checked at run time.' },
          { term: 'Immutable', definition: 'Cannot be changed after creation. All primitives are immutable.' },
          { term: 'Autoboxing', definition: 'The engine temporarily wrapping a primitive in an object so a method can be called on it.' },
          { term: 'BigInt', definition: 'A primitive for integers beyond the safe range of `number`. Written with a trailing `n`.' },
          { term: 'Symbol', definition: 'A primitive whose every instance is unique, used for collision-free property keys.' },
        ],
      },
    ],
    exercises: [
      {
        id: 'ex-m02-01-a',
        title: 'Count the primitives',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['types'],
        instructions: 'How many primitive types does JavaScript have?',
        options: ['Five', 'Six', 'Seven', 'Eight'],
        correct: 2,
        hints: [
          'String, number, boolean, undefined, null — and two that were added more recently.',
        ],
        solution: 'Seven',
        solutionExplanation:
          'The seven are string, number, boolean, undefined, null, bigint and symbol. Older material often says five or six because `symbol` arrived with ES2015 and `bigint` with ES2020. Everything that is not one of these seven is an object — including arrays and functions.',
      },
      {
        id: 'ex-m02-01-b',
        title: 'Predict the immutability result',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['types'],
        instructions: 'What does this print?',
        code: [
          'let word = "cat";',
          'word[0] = "b";',
          'console.log(word);',
        ].join('\n'),
        options: ['bat', 'cat', 'undefined', 'TypeError'],
        correct: 1,
        hints: [
          'Can a primitive string ever be modified in place?',
          'This snippet is non-strict, so the assignment fails quietly rather than throwing.',
        ],
        solution: 'cat',
        solutionExplanation:
          'Strings are immutable, so assigning to an index cannot change them. In non-strict code — as here — the assignment fails silently and `word` is unchanged, which makes the mistake particularly easy to miss. In strict mode, which every ES module and class body uses, the same line throws `TypeError: Cannot assign to read only property`. Either way the string is not modified. To get `"bat"` you must build a new string: `word = "b" + word.slice(1);`.',
      },
      {
        id: 'ex-m02-01-c',
        title: 'Uppercase without mutating',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 15,
        topicIds: ['types', 'strings'],
        instructions:
          'Write a function `shout(text)` that returns the text in uppercase with an exclamation mark appended. `shout("hello")` should return `"HELLO!"`. The original string passed in must not be altered — which, given that strings are immutable, will happen naturally if you return a new value rather than trying to modify.',
        starterCode: [
          'function shout(text) {',
          '  // Return a new string. Do not try to modify text in place.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'shout("hello") returns "HELLO!"', body: 'expect(shout("hello")).toBe("HELLO!");' },
          { name: 'shout("Ada") returns "ADA!"', body: 'expect(shout("Ada")).toBe("ADA!");' },
          { name: 'handles an already uppercase string', body: 'expect(shout("HI")).toBe("HI!");' },
          { name: 'handles an empty string', body: 'expect(shout("")).toBe("!");' },
          { name: 'leaves the caller\'s value untouched', body: 'const original = "quiet"; shout(original); expect(original).toBe("quiet");', hidden: true },
        ],
        hints: [
          '`toUpperCase()` returns a new string rather than changing the one you called it on.',
          'Join the result to `"!"` with the `+` operator.',
          'You can do it in a single `return` statement.',
        ],
        solution: [
          'function shout(text) {',
          '  return text.toUpperCase() + "!";',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Because strings are immutable, `toUpperCase()` has no choice but to return a new string — there is no in-place option to get wrong. The hidden test makes the point explicit: the caller\'s variable is untouched no matter what the function does, since the function received a copy of the value and could not have modified the original even deliberately. That guarantee is exactly what makes primitives safe to pass around freely, and it is a guarantee objects do not give you.',
      },
    ],
    quiz: {
      id: 'qz-m02-01',
      questions: [
        {
          id: 'q-m02-01-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['types'],
          prompt: 'What does "dynamically typed" mean in JavaScript?',
          options: [
            'Values have no types at all',
            'Types belong to values rather than variables, and are checked at run time',
            'Types are declared ahead of time and enforced by a compiler',
            'Every variable is automatically converted to a string',
          ],
          correct: 1,
          optionExplanations: [
            'Every value has a definite type — that is why `"5" * 2` and `"5" + 2` differ.',
            'Correct — the same variable can hold a string then a number.',
            'That describes static typing, as in TypeScript or Java.',
            'No automatic conversion of that kind happens on declaration.',
          ],
          explanation:
            'JavaScript values are strongly typed; it is the *variables* that are untyped. The type is checked when an operation runs rather than ahead of time, which is why type mistakes surface as runtime errors or surprising values rather than build failures.',
        },
        {
          id: 'q-m02-01-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['types', 'strings'],
          prompt: 'What is printed?',
          code: [
            'const s = "hi";',
            's.length = 10;',
            'console.log(s.length);',
          ].join('\n'),
          options: ['10', '2', 'undefined', 'TypeError'],
          correct: 1,
          optionExplanations: [
            'The assignment targets a temporary wrapper object that is immediately discarded.',
            'Correct — the string is unchanged, so its length is still 2.',
            '`length` is always defined on a string.',
            'In a non-strict context the assignment fails silently rather than throwing.',
          ],
          explanation:
            'Assigning a property to a primitive creates a temporary wrapper object, sets the property on that wrapper, then throws the wrapper away. The underlying string is immutable and unchanged, so `s.length` reads 2 from a fresh wrapper on the next access.',
        },
        {
          id: 'q-m02-01-3',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['types'],
          prompt: 'Which of these are primitive types? (Select all that apply.)',
          options: ['symbol', 'array', 'bigint', 'function'],
          correct: [0, 2],
          explanation:
            '`symbol` and `bigint` are two of the seven primitives. Arrays and functions are both objects — arrays are a specialised object with integer keys and a `length`, and functions are objects that happen to be callable. Only the seven listed types are primitive; everything else is an object.',
        },
      ],
    },
    summary:
      'Every JavaScript value is either one of seven primitives — string, number, boolean, undefined, null, bigint, symbol — or an object. There is one number type covering integers and decimals alike. JavaScript is dynamically typed, meaning the type belongs to the value rather than the variable and is checked at run time; this is not the same as being untyped, since operations genuinely depend on type. All primitives are immutable: no operation modifies a string or number in place, so every string method returns a new string and reassignment is the only route to change. Methods appear to work on primitives because the engine temporarily autoboxes them in a wrapper object it then discards.',
    keyTakeaways: [
      'Seven primitives: string, number, boolean, undefined, null, bigint, symbol',
      'Everything that is not a primitive is an object — including arrays and functions',
      'One `number` type covers integers and decimals; `1` and `1.0` are identical',
      'Dynamic typing means types belong to values and are checked at run time',
      'Primitives are immutable — string methods always return a new string',
      'Autoboxing is why `"hi".toUpperCase()` works; the wrapper is thrown away immediately',
    ],
    relatedLessons: ['l-m01-06', 'l-m02-02'],
    interviewConnections: [
      'What are the primitive types in JavaScript?',
      'What does it mean that JavaScript is dynamically typed?',
      'Are strings mutable in JavaScript?',
      'Why can you call methods on a primitive?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m02-02',
    slug: 'typeof-and-type-checking',
    moduleId: M,
    order: 2,
    title: 'typeof, Its Quirks, and How to Check Types Properly',
    description: 'The operator you will reach for constantly, the two results that surprise everyone, and what to use when typeof is not enough.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 15,
    xp: 30,
    topicIds: ['types'],
    prerequisites: ['l-m02-01'],
    learningObjectives: [
      'Use `typeof` and predict its result for any value',
      'Explain why `typeof null` returns `"object"`',
      'Detect arrays reliably with `Array.isArray`',
      'Choose the right type check for a given situation',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          '`typeof` is a unary operator that returns a string naming the type of its operand. It is the first tool you reach for, and it is genuinely useful — provided you know the two places it lies to you.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The complete behaviour of typeof, in one place.',
        code: [
          'console.log(typeof "text");        // string',
          'console.log(typeof 42);            // number',
          'console.log(typeof NaN);           // number  ← surprising but correct',
          'console.log(typeof true);          // boolean',
          'console.log(typeof undefined);     // undefined',
          'console.log(typeof 10n);           // bigint',
          'console.log(typeof Symbol());      // symbol',
          '',
          'console.log(typeof {});            // object',
          'console.log(typeof [1, 2]);        // object  ← unhelpful',
          'console.log(typeof null);          // object  ← a genuine bug',
          'console.log(typeof function () {}); // function ← not a real type',
        ].join('\n'),
        output: [
          'string', 'number', 'number', 'boolean', 'undefined', 'bigint', 'symbol',
          'object', 'object', 'object', 'function',
        ].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Quirk 1: `typeof null` is `"object"`',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`null` is unambiguously a primitive, yet `typeof null` reports `"object"`. This is not a subtlety or a design decision to be defended — it is a bug from the very first version of JavaScript in 1995.',
          'In that original implementation, values were stored with a small type tag in their low bits, and the tag `000` meant "object". The value `null` was represented as the null pointer — all zero bits — so its type tag read as `000` and `typeof` dutifully reported `"object"`.',
          'It has never been fixed because doing so would break an enormous amount of existing code on the web. A proposal to correct it was formally rejected for exactly that reason.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INTERVIEW,
        title: 'A very common interview question',
        body: [
          '"Why does `typeof null` return `"object"`?" A strong answer: "It is a bug from the original 1995 implementation. Values carried a type tag, `000` meant object, and `null` was the all-zero null pointer, so it matched. It was never fixed because too much existing code depends on the behaviour. To test for null, use `value === null`."',
          'The follow-up is usually "so how *do* you check for null?" — and the answer is a strict equality check, because it is the only value that equals `null` under `===`.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        caption: 'Checking for null correctly.',
        code: [
          'const value = null;',
          '',
          '// Wrong — matches objects and arrays too',
          'if (typeof value === "object") { /* ... */ }',
          '',
          '// Right',
          'if (value === null) { /* ... */ }',
          '',
          '// Checking for "neither null nor undefined"',
          'if (value != null) { /* ... */ }',
        ].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'That last one deserves a note. `value != null` uses loose inequality, which normally you should avoid — but this is the one idiomatic exception. Because of how loose equality treats these two values, `!= null` is true for everything except `null` and `undefined`, which is exactly the check you usually want. It is widely used and widely understood.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Quirk 2: `typeof` cannot see arrays',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'An array is an object, so `typeof [1, 2]` correctly but uselessly reports `"object"`. Since you very often need to know whether something is an array specifically, JavaScript provides a dedicated check.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Array.isArray is the only reliable answer.',
        code: [
          'console.log(Array.isArray([1, 2]));   // true',
          'console.log(Array.isArray({}));       // false',
          'console.log(Array.isArray("abc"));    // false',
          'console.log(Array.isArray(null));     // false',
          '',
          '// Why not use instanceof? It fails across execution contexts',
          '// such as iframes, where each has its own Array constructor.',
          'console.log([1, 2] instanceof Array); // true here, but not always',
        ].join('\n'),
        output: ['true', 'false', 'false', 'false', 'true'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Quirk 3: `"function"` is not a real type',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'There is no "function" type in the specification\'s list of types — a function is an object that happens to be callable. `typeof` special-cases it as a convenience, and it is a genuinely useful one: checking whether something can be called is a common need.',
          'So while `typeof fn === "function"` is technically reporting something that is not a primitive type, it is entirely correct to rely on and is the standard way to test callability.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'A practical decision guide',
      },
      {
        kind: SECTION.TABLE,
        headers: ['You want to check', 'Use', 'Notes'],
        rows: [
          ['A string', '`typeof v === "string"`', 'Reliable'],
          ['A number', '`typeof v === "number"`', 'Also true for `NaN` and `Infinity`'],
          ['A usable number', '`Number.isFinite(v)`', 'Excludes `NaN`, `Infinity`, and non-numbers'],
          ['A boolean', '`typeof v === "boolean"`', 'Reliable'],
          ['`undefined`', '`v === undefined`', '`typeof` also works and is safe on undeclared names'],
          ['`null`', '`v === null`', 'The only correct check'],
          ['Null or undefined', '`v == null`', 'The one idiomatic use of loose equality'],
          ['An array', '`Array.isArray(v)`', '`typeof` says `"object"`; works across iframes'],
          ['A callable', '`typeof v === "function"`', 'Reliable'],
          ['A plain object', '`typeof v === "object" && v !== null && !Array.isArray(v)`', 'Must exclude null explicitly'],
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'A robust type-describing helper, built from the table above.',
        code: [
          'function describeType(value) {',
          '  if (value === null) return "null";',
          '  if (Array.isArray(value)) return "array";',
          '  return typeof value;',
          '}',
          '',
          'console.log(describeType(null));',
          'console.log(describeType([1, 2]));',
          'console.log(describeType({ a: 1 }));',
          'console.log(describeType("hi"));',
          'console.log(describeType(undefined));',
          'console.log(describeType(() => {}));',
        ].join('\n'),
        output: ['null', 'array', 'object', 'string', 'undefined', 'function'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Notice the order matters. `null` must be tested first, because `typeof null` would otherwise send it down the `"object"` path. Arrays must be tested before the general `typeof`, for the same reason. Getting the order wrong is the usual bug in hand-written type checkers.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: '`typeof` never throws on an undeclared name',
        body: [
          '`typeof someUndeclaredName` returns the string `"undefined"` instead of throwing a ReferenceError. It is the only operator with this property, and it is occasionally useful for feature detection in environments where you cannot be sure a global exists.',
          'The one exception is a `let` or `const` inside its Temporal Dead Zone — `typeof` on those still throws, because the binding genuinely exists but is uninitialized.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const values = [null, [], {}, "0", 0];',
          '',
          'const types = values.map((v) => typeof v);',
          'console.log(types.join(", "));',
        ].join('\n'),
        options: [
          'null, array, object, string, number',
          'object, object, object, string, number',
          'object, array, object, string, number',
          'null, object, object, string, number',
        ],
        correct: 1,
        explanation:
          '`typeof` reports `"object"` for all three of `null`, `[]` and `{}` — the first because of the 1995 bug, the second because arrays really are objects, and the third correctly. Only the string and the number are reported distinctly. This is precisely why `typeof` alone is insufficient for distinguishing object-like values, and why `Array.isArray` and an explicit `=== null` check exist.',
      },
    ],
    exercises: [
      {
        id: 'ex-m02-02-a',
        title: 'Predict typeof null',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['types'],
        instructions: 'What does this print?',
        code: ['console.log(typeof null);'].join('\n'),
        options: ['"null"', '"object"', '"undefined"', 'TypeError'],
        correct: 1,
        hints: [
          'This is the language\'s most famous bug, preserved for backwards compatibility.',
        ],
        solution: '"object"',
        solutionExplanation:
          '`typeof null` returns `"object"` because of a bug in the original 1995 implementation: values carried a type tag in their low bits, `000` meant object, and `null` was represented as the all-zero null pointer. It has never been fixed because too much existing code relies on the behaviour. `null` is genuinely a primitive; to test for it, use `value === null`.',
      },
      {
        id: 'ex-m02-02-b',
        title: 'Choose the array check',
        kind: EXERCISE_KIND.CHOOSE_IMPLEMENTATION,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['types'],
        instructions: 'Which check reliably determines whether `value` is an array?',
        options: [
          'typeof value === "array"',
          'typeof value === "object"',
          'Array.isArray(value)',
          'value.length !== undefined',
        ],
        correct: 2,
        hints: [
          'There is no `"array"` result from `typeof`.',
          'Strings also have a `length`.',
        ],
        solution: 'Array.isArray(value)',
        solutionExplanation:
          '`Array.isArray` is purpose-built and works correctly even across iframes, where `instanceof Array` fails because each context has its own `Array` constructor. `typeof` never returns `"array"`, and `"object"` is also true for plain objects and for `null`. Checking `length` is a poor heuristic because strings, function objects and any object with a `length` property would all pass.',
      },
      {
        id: 'ex-m02-02-c',
        title: 'Write a reliable type describer',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['types'],
        instructions:
          'Write `describeType(value)` that returns `"null"` for null, `"array"` for arrays, and otherwise the result of `typeof`. Order your checks carefully — `typeof null` is `"object"`, so null must be handled first.',
        starterCode: [
          'function describeType(value) {',
          '  // Handle null first, then arrays, then fall back to typeof.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'null returns "null"', body: 'expect(describeType(null)).toBe("null");' },
          { name: 'an array returns "array"', body: 'expect(describeType([1, 2])).toBe("array");' },
          { name: 'an empty array returns "array"', body: 'expect(describeType([])).toBe("array");' },
          { name: 'a plain object returns "object"', body: 'expect(describeType({ a: 1 })).toBe("object");' },
          { name: 'a string returns "string"', body: 'expect(describeType("hi")).toBe("string");' },
          { name: 'undefined returns "undefined"', body: 'expect(describeType(undefined)).toBe("undefined");' },
          { name: 'a function returns "function"', body: 'expect(describeType(() => {})).toBe("function");' },
          { name: 'NaN still reports as a number', body: 'expect(describeType(NaN)).toBe("number");', hidden: true },
        ],
        hints: [
          'Test `value === null` before anything else.',
          '`Array.isArray(value)` must come before the general `typeof` fallback.',
          'The final line can simply be `return typeof value;`.',
        ],
        solution: [
          'function describeType(value) {',
          '  if (value === null) return "null";',
          '  if (Array.isArray(value)) return "array";',
          '  return typeof value;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Order is everything here. If the `typeof` fallback came first, `null` and arrays would both be swallowed as `"object"` and the two special cases would never be reached. Guard clauses that return early are the clearest way to express this — each one removes a case from consideration before the next check runs. The hidden test is a reminder that `NaN` is genuinely a number, so this function correctly reports it as one rather than treating it as a special case.',
      },
      {
        id: 'ex-m02-02-d',
        title: 'Fix the broken null check',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['types'],
        instructions:
          'This function should return `true` only when given a plain object — not an array, not null, not any other type. It currently returns `true` for arrays and for null. Fix it.',
        starterCode: [
          'function isPlainObject(value) {',
          '  return typeof value === "object";',
          '}',
        ].join('\n'),
        tests: [
          { name: 'a plain object is true', body: 'expect(isPlainObject({ a: 1 })).toBe(true);' },
          { name: 'an empty object is true', body: 'expect(isPlainObject({})).toBe(true);' },
          { name: 'null is false', body: 'expect(isPlainObject(null)).toBe(false);' },
          { name: 'an array is false', body: 'expect(isPlainObject([1, 2])).toBe(false);' },
          { name: 'a string is false', body: 'expect(isPlainObject("hi")).toBe(false);' },
          { name: 'a number is false', body: 'expect(isPlainObject(42)).toBe(false);' },
          { name: 'a function is false', body: 'expect(isPlainObject(() => {})).toBe(false);', hidden: true },
        ],
        hints: [
          'Two extra conditions are needed alongside the `typeof` check.',
          '`typeof null` is `"object"`, so null slips through — exclude it explicitly.',
          'Arrays are objects too. `Array.isArray` tells them apart.',
        ],
        solution: [
          'function isPlainObject(value) {',
          '  return typeof value === "object" && value !== null && !Array.isArray(value);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'All three conditions are necessary. The `typeof` check admits objects, arrays and null; `value !== null` removes the 1995 bug\'s false positive; `!Array.isArray(value)` removes arrays. Functions are excluded automatically because `typeof` reports them as `"function"`, so the first condition already fails for them. This exact three-part check appears in countless real codebases and utility libraries, which is a good sign that the awkwardness is inherent rather than something you have overcomplicated.',
      },
    ],
    quiz: {
      id: 'qz-m02-02',
      questions: [
        {
          id: 'q-m02-02-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['types'],
          prompt: 'Why does `typeof null` return `"object"`?',
          options: [
            'Because `null` is genuinely an object',
            'Because of a bug in the original 1995 implementation that was never fixed for compatibility reasons',
            'Because `null` inherits from `Object.prototype`',
            'Because `null` is a wrapper around an empty object',
          ],
          correct: 1,
          optionExplanations: [
            '`null` is one of the seven primitives.',
            'Correct — the null pointer\'s all-zero bits matched the type tag for objects.',
            '`null` has no prototype at all; it is the end of every prototype chain.',
            'There is no wrapper object involved; `null` is a primitive value.',
          ],
          explanation:
            'In the first implementation, values carried a type tag in their low bits and `000` meant "object". `null` was the all-zero null pointer, so it matched. Fixing it would break too much existing web code, so it remains. Use `value === null` to test for null.',
        },
        {
          id: 'q-m02-02-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['types'],
          prompt: 'What does this print?',
          code: [
            'console.log(typeof [], typeof {}, typeof null);',
          ].join('\n'),
          options: [
            'array object null',
            'object object object',
            'object object null',
            'array object object',
          ],
          correct: 1,
          optionExplanations: [
            '`typeof` never returns `"array"` or `"null"`.',
            'Correct — all three report as `"object"`, which is why extra checks are needed.',
            '`typeof null` is `"object"`, not `"null"`.',
            'Arrays report as `"object"`, not `"array"`.',
          ],
          explanation:
            'All three produce `"object"`: arrays because they genuinely are objects, plain objects correctly, and `null` because of the historical bug. Distinguishing them requires `Array.isArray` and an explicit `=== null` comparison.',
        },
        {
          id: 'q-m02-02-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['types'],
          prompt: 'Which is the correct way to test whether `value` is neither null nor undefined?',
          options: [
            'value !== null',
            'value != null',
            'typeof value !== "undefined"',
            'Boolean(value)',
          ],
          correct: 1,
          optionExplanations: [
            'This catches null but lets undefined through.',
            'Correct — loose inequality treats null and undefined as equivalent, which is exactly what is wanted here.',
            'This catches undefined but lets null through.',
            'This also rejects 0, empty strings and false, which are perfectly valid values.',
          ],
          explanation:
            '`value != null` is the one idiomatic use of loose comparison. Under loose equality, `null == undefined` is true and nothing else equals either of them, so `!= null` means "has some actual value". `Boolean(value)` is a different and usually wrong check, because it also rejects `0`, `""` and `false`.',
        },
        {
          id: 'q-m02-02-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['types'],
          prompt: '`typeof someUndeclaredVariable` throws a ReferenceError.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'Reading an undeclared name normally throws, but `typeof` is deliberately exempt.',
            'Correct — it returns the string `"undefined"` instead.',
          ],
          explanation:
            '`typeof` is the only operator that can be applied to an undeclared name without throwing; it returns `"undefined"`. The exception is a `let` or `const` in its Temporal Dead Zone, where `typeof` does throw, because the binding exists but is uninitialized.',
        },
      ],
    },
    summary:
      '`typeof` returns a string naming a value\'s type, and it is reliable for string, number, boolean, undefined, bigint, symbol and function. It has two well-known problems: `typeof null` returns `"object"` because of a 1995 implementation bug preserved for compatibility, and arrays report as `"object"` because they genuinely are objects. Test for null with `value === null`, for arrays with `Array.isArray`, and for "neither null nor undefined" with the idiomatic `value != null`. When writing your own type checker, order matters — null and arrays must be handled before falling back to `typeof`, or they will be swallowed as plain objects.',
    keyTakeaways: [
      '`typeof null === "object"` is a preserved bug from 1995',
      '`typeof` reports arrays as `"object"` — use `Array.isArray`',
      '`"function"` is not a real type, but the check is reliable and idiomatic',
      'Test for null with `=== null`; for null-or-undefined with `!= null`',
      'A plain-object check needs all three parts: typeof, not null, not array',
      '`typeof` is the only operator safe to use on an undeclared name',
    ],
    relatedLessons: ['l-m02-01', 'l-m02-03'],
    interviewConnections: [
      'Why does typeof null return "object"?',
      'How do you check if a value is an array?',
      'How would you write a reliable type-checking function?',
      'What is the difference between == null and === null?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m02-03',
    slug: 'undefined-vs-null',
    moduleId: M,
    order: 3,
    title: 'undefined versus null',
    description: 'Two ways of representing "nothing", where each one comes from, and which to use when.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 13,
    xp: 25,
    topicIds: ['types'],
    prerequisites: ['l-m02-02'],
    learningObjectives: [
      'Explain the intended meaning of `undefined` and of `null`',
      'List the situations in which JavaScript produces `undefined` automatically',
      'Explain why `null == undefined` but `null === undefined` is false',
      'Choose the right one when designing your own functions',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Most languages have one way to represent "no value". JavaScript has two, and the distinction between them is genuinely meaningful once you know it.',
          'The short version: **`undefined` means the language found nothing there. `null` means a programmer deliberately put nothing there.**',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Where `undefined` comes from',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'You will very rarely write `undefined` yourself. It is what JavaScript hands you when something was not supplied. There are exactly five common sources.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The five places undefined appears on its own.',
        code: [
          '// 1. A declared variable with no value',
          'let a;',
          'console.log(a);',
          '',
          '// 2. A missing object property',
          'const user = { name: "Ada" };',
          'console.log(user.email);',
          '',
          '// 3. A parameter the caller did not supply',
          'function greet(name) { return name; }',
          'console.log(greet());',
          '',
          '// 4. A function that returns nothing',
          'function doWork() { const x = 1; }',
          'console.log(doWork());',
          '',
          '// 5. An array slot that was never filled',
          'const list = [1, 2, 3];',
          'console.log(list[10]);',
        ].join('\n'),
        output: ['undefined', 'undefined', 'undefined', 'undefined', 'undefined'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Every one of those is the language saying the same thing: "you asked for something, and there was nothing there". None of them is an error. `undefined` is a perfectly ordinary value that you can store, pass around and compare.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Where `null` comes from',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`null` is almost always written by a person. It expresses intent: "this exists, and it is deliberately empty."',
          'The distinction becomes practical when you are reading data. A user record where `middleName` is `undefined` suggests the field was never part of the record. A user record where `middleName` is `null` suggests someone deliberately recorded that this user has no middle name. Those are different facts.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        caption: 'null used deliberately.',
        code: [
          '// "We looked, and there genuinely is no manager."',
          'const employee = { name: "Ada", manager: null };',
          '',
          '// "No user is currently signed in."',
          'let currentUser = null;',
          '',
          '// Browser APIs return null for "no match found".',
          'const missing = document.querySelector("#does-not-exist");',
          'console.log(missing); // null',
        ].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INFO,
        title: 'Browser APIs prefer null',
        body: [
          'DOM methods such as `querySelector` and `getElementById` return `null` when nothing matches, not `undefined`. This is deliberate: the search definitely happened and definitely found nothing, which is exactly what `null` means.',
          'It is also the source of the most common front-end error message you will ever see — `Cannot read properties of null` — which means a selector did not match and the code carried on as if it had.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Comparing them',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The comparison behaviour, which is worth memorising.',
        code: [
          'console.log(null == undefined);    // true',
          'console.log(null === undefined);   // false',
          '',
          'console.log(typeof null);          // object',
          'console.log(typeof undefined);     // undefined',
          '',
          'console.log(null == 0);            // false  ← surprising',
          'console.log(null >= 0);            // true   ← very surprising',
          '',
          'console.log(Number(null));         // 0',
          'console.log(Number(undefined));    // NaN',
        ].join('\n'),
        output: ['true', 'false', 'object', 'undefined', 'false', 'true', '0', 'NaN'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`null == undefined` is `true` because the loose equality rules define these two as equal to each other and to nothing else. That single fact is what makes `value != null` such a useful idiom — it rejects exactly these two values.',
          '`null === undefined` is `false` because strict equality compares types first, and their types differ.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'The `null >= 0` oddity',
        body: [
          'Look at those two lines again: `null == 0` is `false`, yet `null >= 0` is `true`. That looks like a contradiction, and it is a genuinely popular interview curiosity.',
          'The reason is that equality and relational operators follow different rules. Loose equality has a special case saying `null` is only ever equal to `undefined` — it never converts to a number. Relational operators such as `>=` have no such special case, so they convert `null` to `0`, and `0 >= 0` is true.',
          'You do not need to memorise this to write good code. You need to know that comparing `null` with numbers is a bad idea, and to check for it explicitly instead.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Which should you use?',
      },
      {
        kind: SECTION.TABLE,
        headers: ['Situation', 'Use', 'Why'],
        rows: [
          ['A variable you will fill in later', '`let x;`', 'Let the language supply `undefined`'],
          ['"Deliberately no value" in your own data', '`null`', 'Signals intent to the next reader'],
          ['"No user is signed in"', '`null`', 'The absence is a real, known state'],
          ['A function with nothing to return', 'return nothing', '`undefined` is returned automatically'],
          ['A function reporting "not found"', '`null`', 'Matches the convention of the DOM APIs'],
          ['A default parameter value', 'omit the argument', 'Defaults only trigger on `undefined`, not `null`'],
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Default parameters trigger on undefined only — a real, practical difference.',
        code: [
          'function greet(name = "friend") {',
          '  return "Hello, " + name;',
          '}',
          '',
          'console.log(greet());',
          'console.log(greet(undefined));',
          'console.log(greet(null));',
        ].join('\n'),
        output: ['Hello, friend', 'Hello, friend', 'Hello, null'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This catches people out regularly. A default parameter is applied when the argument is `undefined` — including when it is omitted entirely — but **not** when it is `null`. Passing `null` is treated as deliberately supplying a value, and the default is skipped.',
          'The same rule governs the nullish coalescing operator `??`, which you will meet in the operators module and which uses exactly this "null or undefined" notion.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'function setLevel(level = 1) {',
          '  return level;',
          '}',
          '',
          'console.log(setLevel(), setLevel(null), setLevel(0));',
        ].join('\n'),
        options: ['1 1 1', '1 null 0', '1 1 0', '1 null 1'],
        correct: 1,
        explanation:
          'Omitting the argument passes `undefined`, so the default `1` applies. Passing `null` explicitly supplies a value, so the default is skipped and `null` comes straight through. Passing `0` also supplies a value — and although `0` is falsy, default parameters do not care about truthiness, only about whether the argument is `undefined`. The result is `1 null 0`.',
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'A defensible team convention',
        body: [
          'Many codebases adopt a simple rule: **never assign `undefined` yourself; use `null` when you mean "deliberately empty", and let `undefined` only ever arrive from the language.**',
          'That keeps the distinction meaningful. If you see `undefined` while debugging, you know something was missing or unset rather than deliberately cleared — which narrows the search considerably.',
        ],
      },
    ],
    exercises: [
      {
        id: 'ex-m02-03-a',
        title: 'Which comparison is true?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['types'],
        instructions: 'Which of these evaluates to `true`?',
        options: [
          'null === undefined',
          'null == undefined',
          'typeof null === "null"',
          'null === 0',
        ],
        correct: 1,
        hints: [
          'Strict equality compares types first. Do these two have the same type?',
        ],
        solution: 'null == undefined',
        solutionExplanation:
          'Loose equality defines `null` and `undefined` as equal to each other and to nothing else, so `null == undefined` is `true`. Strict equality compares types first, and `"object"` differs from `"undefined"`, so `null === undefined` is `false`. `typeof null` is `"object"`, not `"null"`. And `null == 0` is `false`, because loose equality never converts `null` to a number.',
      },
      {
        id: 'ex-m02-03-b',
        title: 'Predict the default parameter behaviour',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['types', 'functions'],
        instructions: 'What does this print?',
        code: [
          'function label(text = "none") {',
          '  return text;',
          '}',
          '',
          'console.log(label(null));',
        ].join('\n'),
        options: ['none', 'null', 'undefined', 'TypeError'],
        correct: 1,
        hints: [
          'Default parameters apply only when the argument is one specific value. Which one?',
          'Is `null` that value?',
        ],
        solution: 'null',
        solutionExplanation:
          'A default parameter is applied only when the argument is `undefined` — either omitted entirely or passed explicitly as `undefined`. Passing `null` counts as deliberately supplying a value, so the default is skipped and `null` is returned. This is a very common source of confusion when data arrives from an API that uses `null` for empty fields, because the defaults you carefully wrote will never fire.',
      },
      {
        id: 'ex-m02-03-c',
        title: 'Write a nullish check',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['types'],
        instructions:
          'Write `hasValue(v)` that returns `true` when `v` is anything other than `null` or `undefined`, and `false` for those two. Note that `0`, `""` and `false` are real values and must return `true`.',
        starterCode: [
          'function hasValue(v) {',
          '  // Careful: do not use Boolean(v) — 0 and "" are real values.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'null is false', body: 'expect(hasValue(null)).toBe(false);' },
          { name: 'undefined is false', body: 'expect(hasValue(undefined)).toBe(false);' },
          { name: 'zero is true', body: 'expect(hasValue(0)).toBe(true);' },
          { name: 'an empty string is true', body: 'expect(hasValue("")).toBe(true);' },
          { name: 'false is true', body: 'expect(hasValue(false)).toBe(true);' },
          { name: 'NaN is true', body: 'expect(hasValue(NaN)).toBe(true);' },
          { name: 'an object is true', body: 'expect(hasValue({})).toBe(true);' },
          { name: 'a missing argument is false', body: 'expect(hasValue()).toBe(false);', hidden: true },
        ],
        hints: [
          'The idiom `v != null` is true for everything except null and undefined.',
          'If you prefer to be explicit, write `v !== null && v !== undefined`.',
          'Avoid `Boolean(v)` or `!!v` — those reject `0`, `""` and `false`, which the tests require to be true.',
        ],
        solution: [
          'function hasValue(v) {',
          '  return v != null;',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`v != null` uses loose inequality, which is normally worth avoiding — but this is the one well-established exception, because loose equality treats `null` and `undefined` as equal to each other and to nothing else. The explicit form `v !== null && v !== undefined` is exactly equivalent and some teams prefer it for clarity. What matters is avoiding the truthiness trap: `Boolean(v)` would wrongly reject `0`, `""`, `false` and `NaN`, all of which are genuine values. The hidden test confirms that an omitted argument arrives as `undefined` and is handled correctly.',
      },
    ],
    quiz: {
      id: 'qz-m02-03',
      questions: [
        {
          id: 'q-m02-03-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['types'],
          prompt: 'What is the intended difference between `undefined` and `null`?',
          options: [
            'They are identical; `null` is just an older spelling',
            '`undefined` means the language found nothing there; `null` means a programmer deliberately put nothing there',
            '`undefined` is for objects and `null` is for primitives',
            '`null` is an error state and `undefined` is not',
          ],
          correct: 1,
          optionExplanations: [
            'They have different types and different behaviour with default parameters.',
            'Correct — one is produced by the language, the other expresses intent.',
            'Neither is tied to a particular category of value.',
            'Neither represents an error; both are ordinary values.',
          ],
          explanation:
            '`undefined` is what JavaScript supplies when something was not provided — an unset variable, a missing property, an omitted argument. `null` is written by a programmer to mean "this is deliberately empty". Keeping the distinction makes debugging faster, because seeing `undefined` tells you something was missing rather than intentionally cleared.',
        },
        {
          id: 'q-m02-03-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['types'],
          prompt: 'What does this print?',
          code: [
            'console.log(null == undefined, null === undefined);',
          ].join('\n'),
          options: ['true true', 'false false', 'true false', 'false true'],
          correct: 2,
          optionExplanations: [
            'Strict equality compares types, which differ here.',
            'Loose equality has an explicit rule making these two equal.',
            'Correct — loosely equal, strictly unequal.',
            'This has the two operators the wrong way round.',
          ],
          explanation:
            'The loose equality algorithm contains a specific rule that `null` and `undefined` are equal to each other and to nothing else, so `==` gives `true`. Strict equality compares types first — `"object"` versus `"undefined"` — so `===` gives `false`.',
        },
        {
          id: 'q-m02-03-3',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['types'],
          prompt: 'Which of these produce `undefined`? (Select all that apply.)',
          options: [
            'Reading a property that does not exist on an object',
            'A function that has no return statement',
            'A DOM query that finds no matching element',
            'A parameter the caller did not supply',
          ],
          correct: [0, 1, 3],
          explanation:
            'Missing properties, functions with no return, and omitted arguments all produce `undefined` — the language supplying "nothing was there". A DOM query that finds nothing returns `null` instead, because the search definitely ran and definitely found nothing, which is the meaning `null` carries. This is why the classic front-end error is "Cannot read properties of null" rather than "of undefined".',

        },
        {
          id: 'q-m02-03-4',
          kind: QUIZ_KIND.TRUE_FALSE,
          topicIds: ['types', 'functions'],
          prompt: 'Passing `null` to a parameter that has a default value will use the default.',
          options: ['True', 'False'],
          correct: 1,
          optionExplanations: [
            'Defaults trigger on `undefined` only.',
            'Correct — `null` is treated as a deliberately supplied value.',
          ],
          explanation:
            'Default parameters apply only when the argument is `undefined`, whether omitted or passed explicitly. Passing `null` counts as supplying a value, so the default is skipped. This bites regularly when API responses use `null` for empty fields.',
        },
      ],
    },
    summary:
      '`undefined` is what JavaScript produces when something was not supplied — an unset variable, a missing property, an omitted argument, a function with no return, an out-of-range index. `null` is written deliberately by a programmer to mean "this is intentionally empty", and it is what DOM queries return when nothing matches. They are loosely equal to each other and to nothing else, which makes `value != null` the standard "has a real value" check, but they are not strictly equal because their types differ. The most practical difference is that default parameters and the `??` operator trigger on `undefined` only, so passing `null` skips your defaults.',
    keyTakeaways: [
      '`undefined` comes from the language; `null` comes from a programmer',
      'The five sources of `undefined`: unset variable, missing property, omitted argument, no return, out-of-range index',
      'DOM queries return `null` for "not found"',
      '`null == undefined` is true; `null === undefined` is false',
      'Default parameters trigger on `undefined` only — never on `null`',
      '`value != null` is the idiomatic "has a real value" check',
    ],
    relatedLessons: ['l-m02-02', 'l-m02-04'],
    interviewConnections: [
      'What is the difference between null and undefined?',
      'Why is null == undefined true but null === undefined false?',
      'When does a default parameter actually apply?',
      'What does querySelector return when nothing matches?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m02-04',
    slug: 'nan-infinity-precision',
    moduleId: M,
    order: 4,
    title: 'NaN, Infinity and Floating-Point Reality',
    description: 'The three number values that behave unlike any other, and why 0.1 + 0.2 is not 0.3.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 16,
    xp: 30,
    topicIds: ['types', 'numbers'],
    prerequisites: ['l-m02-03'],
    learningObjectives: [
      'Explain what `NaN` is and why `NaN !== NaN`',
      'Detect `NaN` correctly with `Number.isNaN`',
      'Explain what produces `Infinity` and `-Infinity`',
      'Explain why `0.1 + 0.2 !== 0.3` and compare decimals safely',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'JavaScript\'s single `number` type is a 64-bit IEEE 754 floating-point value — the same representation used by essentially every modern language for decimals. That format includes three special values that behave unlike ordinary numbers, and one limitation that surprises every newcomer.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'NaN — Not a Number',
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`NaN` is the result of a numeric operation that could not produce a meaningful number. Despite the name, its type is `number` — it is a numeric value representing a failed numeric result.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Where NaN comes from.',
        code: [
          'console.log(Number("hello"));   // NaN — cannot be parsed',
          'console.log(0 / 0);             // NaN — mathematically undefined',
          'console.log(Math.sqrt(-1));     // NaN — outside the reals',
          'console.log(undefined + 1);     // NaN — undefined converts to NaN',
          'console.log("abc" * 2);         // NaN — no numeric meaning',
          '',
          'console.log(typeof NaN);        // number',
        ].join('\n'),
        output: ['NaN', 'NaN', 'NaN', 'NaN', 'NaN', 'number'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'NaN is contagious',
        body: [
          'Any arithmetic involving `NaN` produces `NaN`. A single bad value early in a calculation silently poisons everything downstream, and you often only notice several steps later when a total renders as `NaN` on screen.',
          'This is why validating numeric input at the boundary — as soon as it arrives from a form or an API — saves far more time than chasing a `NaN` backwards through a chain of calculations.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'NaN is not equal to itself',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This is the strangest fact about it, and a reliable interview question.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The only value in JavaScript that is not equal to itself.',
        code: [
          'console.log(NaN === NaN);   // false',
          'console.log(NaN == NaN);    // false',
          '',
          'const x = NaN;',
          'console.log(x === x);       // false',
          '',
          '// Which is why this check can never work:',
          'function brokenCheck(v) { return v === NaN; }',
          'console.log(brokenCheck(NaN));  // false — always',
        ].join('\n'),
        output: ['false', 'false', 'false', 'false'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The reasoning comes from the IEEE 754 standard rather than from JavaScript. `NaN` represents "the result of an invalid computation", and two invalid computations are not necessarily the same invalid computation. `Math.sqrt(-1)` and `Number("hello")` both produce `NaN`, but claiming they are equal would be asserting something meaningless.',
          'The practical consequence is that you can never test for `NaN` with `===`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Detecting NaN correctly',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Number.isNaN is the one to use. The global isNaN is a trap.',
        code: [
          '// The global isNaN coerces its argument first — almost never what you want',
          'console.log(isNaN("hello"));          // true  ← "hello" is not NaN!',
          'console.log(isNaN(undefined));        // true  ← undefined is not NaN!',
          'console.log(isNaN("42"));             // false',
          '',
          '// Number.isNaN checks the actual value, with no conversion',
          'console.log(Number.isNaN("hello"));   // false ← correct',
          'console.log(Number.isNaN(undefined)); // false ← correct',
          'console.log(Number.isNaN(NaN));       // true  ← correct',
          '',
          '// Object.is also works, and reads clearly',
          'console.log(Object.is(NaN, NaN));     // true',
        ].join('\n'),
        output: ['true', 'true', 'false', 'false', 'false', 'true', 'true'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'The global `isNaN` answers a different question',
        body: [
          'The global `isNaN(v)` converts `v` to a number first and then asks whether the *result* is `NaN`. So it effectively answers "would this value fail to convert to a number?", not "is this value NaN?".',
          'That is why `isNaN("hello")` is `true` even though the string `"hello"` is obviously not the value `NaN`. Always prefer `Number.isNaN`, which performs no conversion and answers the question you actually asked.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Infinity and -Infinity',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Division by zero does not throw in JavaScript.',
        code: [
          'console.log(1 / 0);             // Infinity',
          'console.log(-1 / 0);            // -Infinity',
          'console.log(0 / 0);             // NaN — this one is genuinely undefined',
          '',
          'console.log(typeof Infinity);   // number',
          'console.log(Infinity > 1e308);  // true',
          '',
          '// Overflow produces Infinity rather than an error',
          'console.log(1e308 * 10);        // Infinity',
          '',
          '// Infinity is equal to itself, unlike NaN',
          'console.log(Infinity === Infinity); // true',
        ].join('\n'),
        output: ['Infinity', '-Infinity', 'NaN', 'number', 'true', 'Infinity', 'true'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Unlike many languages, dividing by zero in JavaScript does not throw — it produces `Infinity`. This means a bug involving an unexpected zero denominator propagates silently rather than crashing loudly, which is worth remembering when a value renders as `Infinity` on screen.',
          '`Number.isFinite(v)` is the single check that rejects `NaN`, `Infinity`, `-Infinity` **and** non-numbers in one go. It is usually what you want when validating that a value is a usable number.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Floating-point precision',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The most famous surprise in the language.',
        code: [
          'console.log(0.1 + 0.2);            // 0.30000000000000004',
          'console.log(0.1 + 0.2 === 0.3);    // false',
          '',
          'console.log(0.1 + 0.7);            // 0.7999999999999999',
          'console.log(1.005 * 100);          // 100.49999999999999',
        ].join('\n'),
        output: ['0.30000000000000004', 'false', '0.7999999999999999', '100.49999999999999'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This is not a JavaScript flaw. It happens in Python, Java, C, Ruby and every other language using IEEE 754 doubles — which is nearly all of them.',
          'The cause is base conversion. Computers store numbers in binary, and just as `1/3` cannot be written exactly in decimal (0.333… forever), `0.1` cannot be written exactly in binary. It becomes a repeating fraction that must be truncated to 64 bits. The tiny rounding error is invisible on its own, but becomes visible when two such approximations are added.',
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INTERVIEW,
        title: 'A 30-second answer',
        body: [
          '"JavaScript numbers are IEEE 754 doubles. `0.1` and `0.2` have no exact binary representation, so they are stored as very close approximations, and adding them produces a value slightly off from `0.3`. It affects every language using the same format. For comparisons I use a small epsilon tolerance; for money I work in integer minor units such as cents."',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Comparing decimals safely',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Compare within a tolerance rather than exactly.',
        code: [
          'function nearlyEqual(a, b, tolerance = Number.EPSILON * 10) {',
          '  return Math.abs(a - b) < tolerance;',
          '}',
          '',
          'console.log(0.1 + 0.2 === 0.3);          // false',
          'console.log(nearlyEqual(0.1 + 0.2, 0.3)); // true',
          '',
          '// For money, work in integer minor units instead',
          'const priceInCents = 1999;',
          'const taxInCents = Math.round(priceInCents * 0.2);',
          'console.log("Total cents:", priceInCents + taxInCents);',
        ].join('\n'),
        output: ['false', 'true', 'Total cents: 2399'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          '`Number.EPSILON` is the smallest difference the format can represent near 1, and it makes a reasonable basis for a tolerance. For financial calculations, the standard professional answer is not tolerance at all — it is to avoid decimals entirely by storing amounts as integer cents, pence or the equivalent minor unit.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const results = [NaN, Infinity, 0.1 + 0.2];',
          '',
          'console.log(results[0] === results[0]);',
          'console.log(results[1] === results[1]);',
          'console.log(results[2] === 0.3);',
        ].join('\n'),
        options: [
          'true true true',
          'false true false',
          'false false false',
          'true true false',
        ],
        correct: 1,
        explanation:
          '`NaN` is the only value in JavaScript not equal to itself, so the first comparison is `false`. `Infinity` is an ordinary comparable value and is equal to itself, so the second is `true`. And `0.1 + 0.2` produces `0.30000000000000004` rather than exactly `0.3`, so the third is `false`. Those three lines cover the three number surprises in one go.',
      },
    ],
    exercises: [
      {
        id: 'ex-m02-04-a',
        title: 'Why does the NaN check fail?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['numbers', 'types'],
        instructions: 'Why does `value === NaN` never return `true`, even when `value` really is `NaN`?',
        options: [
          'Because `NaN` is not a number',
          'Because `NaN` is not equal to any value, including itself',
          'Because `===` does not work with numbers',
          'Because `NaN` is actually a string',
        ],
        correct: 1,
        hints: [
          'Try `NaN === NaN` mentally. What does it give?',
        ],
        solution: 'Because `NaN` is not equal to any value, including itself',
        solutionExplanation:
          'The IEEE 754 standard specifies that `NaN` compares unequal to everything, itself included — two invalid computations are not necessarily the same invalid computation. This makes `===` useless for detecting it. Use `Number.isNaN(value)`, which checks the value directly with no conversion, or `Object.is(value, NaN)`. Note that `typeof NaN` is `"number"`, so the first option is wrong on its own terms.',
      },
      {
        id: 'ex-m02-04-b',
        title: 'Predict the precision result',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['numbers'],
        instructions: 'What does this print?',
        code: ['console.log(0.1 + 0.2 === 0.3);'].join('\n'),
        options: ['true', 'false', 'NaN', 'TypeError'],
        correct: 1,
        hints: [
          '`0.1` and `0.2` cannot be represented exactly in binary.',
          'The sum is `0.30000000000000004`.',
        ],
        solution: 'false',
        solutionExplanation:
          'Neither `0.1` nor `0.2` has an exact binary representation, so both are stored as very close approximations. Adding them produces `0.30000000000000004`, which is not the same value as the stored approximation of `0.3`. Strict equality compares those exact stored values and finds them different. This affects every IEEE 754 language, not just JavaScript. Compare with a small tolerance, or work in integer minor units for money.',
      },
      {
        id: 'ex-m02-04-c',
        title: 'Write a safe number validator',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.EASY,
        xp: 20,
        topicIds: ['numbers', 'types'],
        instructions:
          'Write `isUsableNumber(v)` that returns `true` only for values that are genuinely usable numbers. It must reject `NaN`, `Infinity`, `-Infinity`, and anything that is not of type number — including numeric strings such as `"42"`.',
        starterCode: [
          'function isUsableNumber(v) {',
          '  // One built-in method does all of this in a single call.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'accepts an ordinary integer', body: 'expect(isUsableNumber(42)).toBe(true);' },
          { name: 'accepts a decimal', body: 'expect(isUsableNumber(3.14)).toBe(true);' },
          { name: 'accepts zero', body: 'expect(isUsableNumber(0)).toBe(true);' },
          { name: 'accepts negatives', body: 'expect(isUsableNumber(-7)).toBe(true);' },
          { name: 'rejects NaN', body: 'expect(isUsableNumber(NaN)).toBe(false);' },
          { name: 'rejects Infinity', body: 'expect(isUsableNumber(Infinity)).toBe(false);' },
          { name: 'rejects -Infinity', body: 'expect(isUsableNumber(-Infinity)).toBe(false);' },
          { name: 'rejects a numeric string', body: 'expect(isUsableNumber("42")).toBe(false);' },
          { name: 'rejects null and undefined', body: 'expect(isUsableNumber(null)).toBe(false); expect(isUsableNumber(undefined)).toBe(false);', hidden: true },
        ],
        hints: [
          '`typeof v === "number"` accepts NaN and Infinity, so it is not enough on its own.',
          'There is a single `Number` method that rejects all four unwanted cases at once.',
          'Note it must be `Number.isFinite`, not the global `isFinite` — the global one converts its argument first and would accept `"42"`.',
        ],
        solution: [
          'function isUsableNumber(v) {',
          '  return Number.isFinite(v);',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`Number.isFinite` does all four jobs at once: it returns `false` for anything that is not of type number (so `"42"`, `null` and `undefined` are rejected without conversion) and `false` for `NaN`, `Infinity` and `-Infinity`. The global `isFinite` would be wrong here, because it converts its argument first and therefore accepts `"42"` and even `null`, which converts to `0`. The `Number.` prefixed versions of both `isNaN` and `isFinite` were added in ES2015 specifically to fix the coercion behaviour of the older globals, and they are what you should reach for.',
      },
      {
        id: 'ex-m02-04-d',
        title: 'Compare decimals safely',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 20,
        topicIds: ['numbers'],
        instructions:
          'Write `nearlyEqual(a, b, tolerance)` that returns `true` when `a` and `b` differ by less than `tolerance`. If `tolerance` is not supplied, default it to `0.000001`. `nearlyEqual(0.1 + 0.2, 0.3)` must return `true`.',
        starterCode: [
          'function nearlyEqual(a, b, tolerance = 0.000001) {',
          '  // Compare the size of the difference against the tolerance.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'the classic case passes', body: 'expect(nearlyEqual(0.1 + 0.2, 0.3)).toBe(true);' },
          { name: 'identical values pass', body: 'expect(nearlyEqual(5, 5)).toBe(true);' },
          { name: 'clearly different values fail', body: 'expect(nearlyEqual(1, 2)).toBe(false);' },
          { name: 'works regardless of argument order', body: 'expect(nearlyEqual(0.3, 0.1 + 0.2)).toBe(true);' },
          { name: 'respects a custom tolerance', body: 'expect(nearlyEqual(1, 1.4, 0.5)).toBe(true); expect(nearlyEqual(1, 1.6, 0.5)).toBe(false);' },
          { name: 'handles negative numbers', body: 'expect(nearlyEqual(-0.3, -(0.1 + 0.2))).toBe(true);', hidden: true },
        ],
        hints: [
          'Subtract one from the other, then take the absolute value with `Math.abs`.',
          'Using `Math.abs` is what makes the argument order irrelevant.',
          'Compare that distance against `tolerance` with `<`.',
        ],
        solution: [
          'function nearlyEqual(a, b, tolerance = 0.000001) {',
          '  return Math.abs(a - b) < tolerance;',
          '}',
        ].join('\n'),
        solutionExplanation:
          '`Math.abs` turns the signed difference into a distance, which is what makes the comparison symmetric — without it, `nearlyEqual(0.3, 0.31)` and `nearlyEqual(0.31, 0.3)` would disagree. The default parameter handles the common case while still allowing a caller to widen the tolerance for values of a different magnitude. That last point is a real limitation worth knowing: a fixed absolute tolerance works well for numbers near 1 but poorly for very large or very tiny ones, where a *relative* tolerance scaled to the magnitude of the inputs is more appropriate.',
      },
    ],
    quiz: {
      id: 'qz-m02-04',
      questions: [
        {
          id: 'q-m02-04-1',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['numbers'],
          prompt: 'What does this print?',
          code: ['console.log(typeof NaN);'].join('\n'),
          options: ['"NaN"', '"number"', '"undefined"', '"object"'],
          correct: 1,
          optionExplanations: [
            '`typeof` never returns `"NaN"`.',
            'Correct — `NaN` is a numeric value representing a failed numeric result.',
            '`NaN` is a definite value, not an absent one.',
            'That is the result for `null` and for objects.',
          ],
          explanation:
            'Despite standing for "Not a Number", `NaN` has type `number`. It is the numeric value that represents the result of an operation that could not produce a meaningful number.',
        },
        {
          id: 'q-m02-04-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['numbers'],
          prompt: 'Why is `Number.isNaN` preferred over the global `isNaN`?',
          options: [
            'The global version is deprecated and will be removed',
            'The global version converts its argument to a number first, so `isNaN("hello")` is true even though the string is not NaN',
            'The global version is slower',
            'They are identical; the prefixed one is just newer',
          ],
          correct: 1,
          optionExplanations: [
            'It is not deprecated, merely misleading.',
            'Correct — it answers "would this fail to convert?" rather than "is this NaN?".',
            'Performance is not the concern.',
            'They behave genuinely differently for non-number arguments.',
          ],
          explanation:
            'The global `isNaN` coerces its argument first, so it effectively asks "would this value fail to convert to a number?". `Number.isNaN` performs no conversion and returns `true` only for the actual `NaN` value, which is almost always the question you meant to ask.',
        },
        {
          id: 'q-m02-04-3',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['numbers'],
          prompt: 'What does this print?',
          code: ['console.log(1 / 0, -1 / 0, 0 / 0);'].join('\n'),
          options: [
            'Infinity -Infinity NaN',
            'Error Error Error',
            'Infinity Infinity Infinity',
            'NaN NaN NaN',
          ],
          correct: 0,
          optionExplanations: [
            'Correct — only `0 / 0` is genuinely undefined and gives NaN.',
            'JavaScript does not throw on division by zero.',
            'The sign of the numerator is preserved.',
            'Dividing a non-zero number by zero has a defined result in IEEE 754.',
          ],
          explanation:
            'Division by zero produces `Infinity` or `-Infinity` depending on the numerator\'s sign, and does not throw. Only `0 / 0` is mathematically undefined, so it produces `NaN`. This means an unexpected zero denominator propagates silently rather than crashing.',
        },
        {
          id: 'q-m02-04-4',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['numbers'],
          prompt: 'What is the standard professional approach to representing money in JavaScript?',
          options: [
            'Use floating-point numbers and round at the end',
            'Store amounts as integers in the smallest unit, such as cents',
            'Store amounts as strings',
            'Use `toFixed(2)` everywhere',
          ],
          correct: 1,
          optionExplanations: [
            'Rounding at the end does not undo errors accumulated during the calculation.',
            'Correct — integer arithmetic has no representation error.',
            'Strings cannot be arithmetically combined without converting back to numbers.',
            '`toFixed` formats for display and returns a string; it does not fix the underlying arithmetic.',
          ],
          explanation:
            'Because `0.1` and similar decimals cannot be represented exactly in binary, repeated floating-point arithmetic accumulates error. Working in integer minor units — cents, pence — sidesteps the problem entirely, since integers within the safe range are exact. Convert to a decimal string only at the point of display.',
        },
      ],
    },
    summary:
      '`NaN` is a `number` representing a failed numeric result; it is contagious through arithmetic and is the only value not equal to itself, so it must be detected with `Number.isNaN` rather than `===`. The global `isNaN` coerces first and answers a different question. Division by zero produces `Infinity` or `-Infinity` rather than throwing, and `Number.isFinite` is the single check that rejects `NaN`, both infinities and non-numbers at once. Finally, `0.1 + 0.2 !== 0.3` because those decimals have no exact binary representation — a property of IEEE 754 shared by nearly every language. Compare decimals within a tolerance, and represent money as integer minor units.',
    keyTakeaways: [
      '`typeof NaN` is `"number"`, and NaN is contagious through arithmetic',
      '`NaN !== NaN` — use `Number.isNaN`, never `===`',
      'The global `isNaN` coerces first and gives misleading answers',
      'Division by zero gives Infinity, not an error; `0 / 0` gives NaN',
      '`Number.isFinite` rejects NaN, both infinities and non-numbers in one check',
      '`0.1 + 0.2 !== 0.3` in every IEEE 754 language — compare with a tolerance',
      'Represent money as integer cents, not decimals',
    ],
    relatedLessons: ['l-m02-01', 'l-m02-05'],
    interviewConnections: [
      'Why is NaN not equal to itself?',
      'How do you check whether a value is NaN?',
      'What is the difference between isNaN and Number.isNaN?',
      'Why does 0.1 + 0.2 not equal 0.3?',
      'How would you handle currency in JavaScript?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m02-05',
    slug: 'objects-arrays-functions-as-values',
    moduleId: M,
    order: 5,
    title: 'Objects, Arrays and Functions Are All Values',
    description: 'The non-primitive side of the divide: what makes something an object, and why functions being values changes how you write JavaScript.',
    difficulty: DIFFICULTY.EASY,
    estimatedMinutes: 14,
    xp: 30,
    topicIds: ['types', 'objects'],
    prerequisites: ['l-m02-04'],
    learningObjectives: [
      'Explain what makes arrays and functions objects',
      'Store functions in variables, arrays and object properties',
      'Explain what "first-class functions" means',
      'Recognise that objects are mutable, unlike primitives',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'Everything that is not one of the seven primitives is an object. That includes some things you might not expect: arrays are objects, functions are objects, dates are objects, regular expressions are objects.',
          'An **object** is a collection of key–value pairs, called properties. That definition is the whole of it — the different kinds of object simply add specialised behaviour on top.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Plain objects',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Properties can hold any value, including other objects and functions.',
        code: [
          'const user = {',
          '  name: "Ada",',
          '  age: 36,',
          '  isActive: true,',
          '  address: { city: "London" },',
          '  greet: function () { return "Hello"; },',
          '};',
          '',
          'console.log(user.name);',
          'console.log(user.address.city);',
          'console.log(user.greet());',
          'console.log(Object.keys(user).length);',
        ].join('\n'),
        output: ['Ada', 'London', 'Hello', '5'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Arrays are objects with a twist',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'An array is an object whose keys are numeric indexes, plus an automatically maintained `length` property and a large set of specialised methods. You can prove this to yourself.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'An array really is an object underneath.',
        code: [
          'const list = ["a", "b"];',
          '',
          'console.log(typeof list);          // object',
          'console.log(Array.isArray(list));  // true',
          'console.log(Object.keys(list));    // the indexes, as strings',
          '',
          '// You can even attach a non-index property',
          'list.label = "letters";',
          'console.log(list.label);',
          'console.log(list.length);          // still 2 — labels do not count',
        ].join('\n'),
        output: ['object', 'true', '["0", "1"]', 'letters', '2'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'Do not attach arbitrary properties to arrays',
        body: [
          'The example above demonstrates that it works, not that you should do it. Array methods and `length` only consider indexed elements, so a property like `label` is invisible to `map`, `filter`, `forEach` and `JSON.stringify`. It will silently disappear when the array is serialised or copied.',
          'If your data needs both a list and some metadata about that list, use an object with an array property: `{ label: "letters", items: ["a", "b"] }`.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Functions are values',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This is the single most consequential idea in this lesson. A function in JavaScript is an object that can be called. Because it is a value like any other, it can be stored in a variable, put in an array, assigned to an object property, passed as an argument, and returned from another function.',
          'Languages where this is true are said to have **first-class functions**, and it is the foundation of callbacks, array methods, event handlers, promises and virtually every modern JavaScript pattern.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The same function, treated as data five different ways.',
        code: [
          'function double(n) { return n * 2; }',
          '',
          '// 1. Stored in a variable',
          'const alias = double;',
          'console.log(alias(5));',
          '',
          '// 2. Put in an array',
          'const operations = [double, (n) => n + 1];',
          'console.log(operations[0](5));',
          '',
          '// 3. Assigned to an object property',
          'const math = { double: double };',
          'console.log(math.double(5));',
          '',
          '// 4. Passed as an argument',
          'console.log([1, 2, 3].map(double));',
          '',
          '// 5. Returned from another function',
          'function makeMultiplier(factor) {',
          '  return function (n) { return n * factor; };',
          '}',
          'const triple = makeMultiplier(3);',
          'console.log(triple(5));',
        ].join('\n'),
        output: ['10', '10', '10', '[2, 4, 6]', '15'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.TIP,
        title: 'The parentheses make all the difference',
        body: [
          '`double` is the function itself — a value you can pass around. `double(5)` is a *call*, and its value is the result, `10`.',
          'Confusing the two is a classic bug: `button.addEventListener("click", handleClick())` calls the function immediately and passes its return value, usually `undefined`, as the handler. What you meant was `handleClick` with no parentheses. Whenever a callback "runs at the wrong time" or "does nothing", check the parentheses first.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Functions are objects, so they can carry properties.',
        code: [
          'function greet(name) { return "Hello, " + name; }',
          '',
          'console.log(typeof greet);       // function',
          'console.log(greet.name);         // greet',
          'console.log(greet.length);       // 1 — the parameter count',
          '',
          'greet.callCount = 0;             // a function is an object',
          'greet.callCount += 1;',
          'console.log(greet.callCount);',
        ].join('\n'),
        output: ['function', 'greet', '1', '1'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Objects are mutable',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'The other defining difference from primitives: objects can be changed in place. A primitive can only be replaced; an object can be modified while remaining the same object.',
        ],
      },
      {
        kind: SECTION.COMPARISON,
        left: {
          title: 'Primitive — replaced, never modified',
          code: [
            'let s = "hello";',
            's.toUpperCase();',
            'console.log(s);  // "hello"',
            '',
            '// The only way to change it:',
            's = s.toUpperCase();',
            'console.log(s);  // "HELLO"',
          ].join('\n'),
        },
        right: {
          title: 'Object — modified in place',
          code: [
            'const o = { text: "hello" };',
            'o.text = "HELLO";',
            'console.log(o.text);  // "HELLO"',
            '',
            '// Same object throughout —',
            '// no reassignment needed,',
            '// which is why const allowed it.',
          ].join('\n'),
        },
        note: 'This is the mechanical reason `const` behaves so differently for the two categories, and it sets up the value-versus-reference lesson that follows.',
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const handlers = [];',
          '',
          'function log() { return "logged"; }',
          '',
          'handlers.push(log);',
          'handlers.push(log());',
          '',
          'console.log(typeof handlers[0], typeof handlers[1]);',
        ].join('\n'),
        options: [
          'function function',
          'function string',
          'string string',
          'function undefined',
        ],
        correct: 1,
        explanation:
          '`handlers.push(log)` pushes the function value itself, so `handlers[0]` is a function. `handlers.push(log())` *calls* `log` first and pushes its return value, the string `"logged"`, so `handlers[1]` is a string. Those two lines look almost identical and do completely different things — which is exactly the parentheses bug that breaks event listeners and callbacks.',
      },
      {
        kind: SECTION.TERMS,
        terms: [
          { term: 'Object', definition: 'A collection of key–value pairs. Everything that is not one of the seven primitives.' },
          { term: 'Property', definition: 'A single key–value pair on an object.' },
          { term: 'First-class function', definition: 'A function that can be stored, passed and returned like any other value.' },
          { term: 'Higher-order function', definition: 'A function that takes a function as an argument or returns one.' },
          { term: 'Mutable', definition: 'Able to be changed in place. Objects are mutable; primitives are not.' },
        ],
      },
    ],
    exercises: [
      {
        id: 'ex-m02-05-a',
        title: 'Is an array an object?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.BEGINNER,
        xp: 10,
        topicIds: ['types'],
        instructions: 'Which statement about arrays is accurate?',
        options: [
          'An array is a primitive type',
          'An array is an object with numeric keys, a length property and specialised methods',
          'An array is a separate type reported by typeof as "array"',
          'An array is a string containing multiple values',
        ],
        correct: 1,
        hints: [
          'What does `typeof [1, 2]` actually return?',
        ],
        solution: 'An array is an object with numeric keys, a length property and specialised methods',
        solutionExplanation:
          'Arrays are objects — `typeof [1, 2]` returns `"object"`, which is why `Array.isArray` exists as a separate check. What makes an array special is that its keys are numeric indexes, it maintains a `length` automatically, and it inherits array methods from `Array.prototype`. The seven primitives do not include arrays.',
      },
      {
        id: 'ex-m02-05-b',
        title: 'Predict the parentheses bug',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 15,
        topicIds: ['types', 'functions'],
        instructions: 'What does this print?',
        code: [
          'function makeTag() { return "tag"; }',
          '',
          'const a = makeTag;',
          'const b = makeTag();',
          '',
          'console.log(typeof a, typeof b);',
        ].join('\n'),
        options: [
          'function string',
          'function function',
          'string string',
          'undefined string',
        ],
        correct: 0,
        hints: [
          'One of these two lines calls the function. Which?',
          'The parentheses are the call operator.',
        ],
        solution: 'function string',
        solutionExplanation:
          '`const a = makeTag;` stores the function value itself — no call happens, so `a` is a function. `const b = makeTag();` calls it and stores the returned string, so `b` is a string. This one-character difference is behind a great many callback bugs: passing `handler()` to `addEventListener` runs it immediately and registers its return value instead of the function.',
      },
      {
        id: 'ex-m02-05-c',
        title: 'Build a function dispatcher',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 20,
        topicIds: ['types', 'objects', 'higher-order'],
        instructions:
          'Write `applyOperation(name, a, b)` that looks up an operation by name and applies it to the two numbers. Support `"add"`, `"subtract"` and `"multiply"` by storing the functions as properties of an object. If the name is unknown, return `null`.',
        starterCode: [
          'function applyOperation(name, a, b) {',
          '  const operations = {',
          '    // Store one function per operation name.',
          '  };',
          '  // Look up the function, then call it if it exists.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'add works', body: 'expect(applyOperation("add", 2, 3)).toBe(5);' },
          { name: 'subtract works', body: 'expect(applyOperation("subtract", 10, 4)).toBe(6);' },
          { name: 'multiply works', body: 'expect(applyOperation("multiply", 3, 4)).toBe(12);' },
          { name: 'an unknown name returns null', body: 'expect(applyOperation("divide", 8, 2)).toBe(null);' },
          { name: 'handles negatives', body: 'expect(applyOperation("add", -5, 2)).toBe(-3);' },
          { name: 'does not confuse a missing name with a zero result', body: 'expect(applyOperation("subtract", 5, 5)).toBe(0);', hidden: true },
        ],
        hints: [
          'Object values can be functions: `{ add: (a, b) => a + b }`.',
          'Bracket notation looks up a property by a name held in a variable: `operations[name]`.',
          'Check whether the looked-up value exists before calling it — `typeof fn === "function"` is a robust test.',
        ],
        solution: [
          'function applyOperation(name, a, b) {',
          '  const operations = {',
          '    add: (a, b) => a + b,',
          '    subtract: (a, b) => a - b,',
          '    multiply: (a, b) => a * b,',
          '  };',
          '',
          '  const operation = operations[name];',
          '  if (typeof operation !== "function") return null;',
          '',
          '  return operation(a, b);',
          '}',
        ].join('\n'),
        solutionExplanation:
          'This is only possible because functions are values that can live in object properties. The pattern — an object mapping names to functions, looked up with bracket notation — is called a dispatch table, and it replaces long `if`/`else if` chains in a great deal of real code. The `typeof` guard matters: a missing key gives `undefined`, and calling `undefined(a, b)` throws a TypeError. The hidden test guards against a subtle bug — if you had written `if (!operation) return null;` that would still be fine, but checking the *result* for falsiness instead would wrongly return null when a valid subtraction produced 0.',
      },
    ],
    quiz: {
      id: 'qz-m02-05',
      questions: [
        {
          id: 'q-m02-05-1',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['types', 'functions'],
          prompt: 'What does it mean that JavaScript has first-class functions?',
          options: [
            'Functions run faster than other operations',
            'Functions are values that can be stored, passed as arguments and returned from other functions',
            'Functions must be declared before everything else in a file',
            'Only functions can be stored in variables',
          ],
          correct: 1,
          optionExplanations: [
            'The term has nothing to do with performance.',
            'Correct — this is what makes callbacks and array methods possible.',
            'Declaration order is a separate matter, governed by hoisting.',
            'Every type of value can be stored in a variable.',
          ],
          explanation:
            'First-class means functions are ordinary values. They can be assigned to variables, held in arrays and object properties, passed into other functions, and returned. Callbacks, `map`, event handlers and promises all depend on this.',
        },
        {
          id: 'q-m02-05-2',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['types', 'functions'],
          prompt: 'What is the difference between these two lines?',
          code: [
            'const a = calculate;',
            'const b = calculate();',
          ].join('\n'),
          options: [
            'They are identical',
            '`a` holds the function itself; `b` holds the value the function returned',
            '`a` is invalid syntax',
            '`b` holds the function and `a` holds its result',
          ],
          correct: 1,
          optionExplanations: [
            'The parentheses change the meaning entirely.',
            'Correct — parentheses are the call operator.',
            'Referring to a function without calling it is perfectly valid and very common.',
            'This has the two the wrong way round.',
          ],
          explanation:
            'Without parentheses you refer to the function value; with them you call it and get its return value. Passing `handler()` where `handler` was meant is one of the most common callback bugs in JavaScript.',
        },
        {
          id: 'q-m02-05-3',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['types'],
          prompt: 'Which of these are objects rather than primitives? (Select all that apply.)',
          options: ['[1, 2, 3]', 'function () {}', '"text"', '{ a: 1 }'],
          correct: [0, 1, 3],
          explanation:
            'Arrays, functions and plain objects are all objects — arrays being objects with numeric keys and a `length`, functions being objects that can be called. Only `"text"` is a primitive. Everything that is not one of the seven primitive types is an object.',
        },
      ],
    },
    summary:
      'Everything that is not one of the seven primitives is an object — a collection of key–value pairs. Arrays are objects with numeric keys, an automatically maintained `length` and specialised methods, which is why `typeof` reports them as `"object"` and `Array.isArray` exists. Functions are objects that can be called, and because they are ordinary values they can be stored in variables, arrays and properties, passed as arguments and returned — the property called first-class functions, and the basis of callbacks and array methods. The parentheses distinguish referring to a function from calling it, and confusing the two is a classic bug. Unlike primitives, objects are mutable: they can be changed in place while remaining the same object.',
    keyTakeaways: [
      'Everything that is not a primitive is an object',
      'Arrays are objects with numeric keys and a maintained `length`',
      'Functions are callable objects, and can carry properties of their own',
      'First-class functions can be stored, passed and returned like any value',
      '`fn` refers to a function; `fn()` calls it — a classic source of callback bugs',
      'Objects are mutable; primitives are not',
    ],
    relatedLessons: ['l-m02-01', 'l-m02-06'],
    interviewConnections: [
      'Is an array an object in JavaScript?',
      'What does first-class functions mean?',
      'What is the difference between passing `fn` and `fn()` to addEventListener?',
      'Can a function have properties?',
    ],
  },

  /* ================================================================== */
  {
    id: 'l-m02-06',
    slug: 'value-vs-reference',
    moduleId: M,
    order: 6,
    title: 'Value versus Reference',
    description: 'The consequence of the primitive/object split that causes more real bugs than anything else in this module.',
    difficulty: DIFFICULTY.MEDIUM,
    estimatedMinutes: 18,
    xp: 35,
    topicIds: ['types', 'objects', 'copying'],
    prerequisites: ['l-m02-05'],
    learningObjectives: [
      'Predict whether an assignment copies a value or shares a reference',
      'Explain why two identical-looking objects are not `===` equal',
      'Copy an object or array shallowly, and explain the limits of that',
      'Explain how arguments are passed to functions in JavaScript',
    ],
    sections: [
      {
        kind: SECTION.PROSE,
        body: [
          'This lesson brings the whole module together. Everything so far — primitives versus objects, immutable versus mutable — leads to one practical question: **when you copy a variable, what actually gets copied?**',
          'The answer differs for the two categories, and getting it wrong produces bugs where changing one thing mysteriously changes another.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Primitives copy the value',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Two entirely independent values.',
        code: [
          'let a = 10;',
          'let b = a;',
          '',
          'b = 99;',
          '',
          'console.log(a);   // 10 — untouched',
          'console.log(b);   // 99',
        ].join('\n'),
        output: ['10', '99'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Nothing surprising. `b = a` copied the value 10, and the two names have had no connection since.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Objects copy the reference',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'One object, two names.',
        code: [
          'const first = { count: 10 };',
          'const second = first;',
          '',
          'second.count = 99;',
          '',
          'console.log(first.count);    // 99 — changed too!',
          'console.log(second.count);   // 99',
          'console.log(first === second); // true — the very same object',
        ].join('\n'),
        output: ['99', '99', 'true'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This surprises everyone the first time. `second = first` did not copy the object — it copied the **reference** to it. Both names now point at one single object, and modifying it through either name is visible through the other.',
          'Return to the label model: you attached a second label to the same thing. There is still only one thing.',
        ],
      },
      {
        kind: SECTION.DIAGRAM,
        diagram: 'value-vs-reference',
        caption: 'The left column is what happens with primitives; the right is what happens with objects.',
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.WARNING,
        title: 'Where this bites in real code',
        body: [
          'You take a copy of some application state so you can experiment with it safely, modify the copy, and discover that the original changed too. Or you store a default configuration object and hand it to several parts of your program, and one of them mutates it for everybody.',
          'Both are the same bug: an assignment that looked like a copy was actually a second reference to one shared object.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Equality compares references, not contents',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Identical contents, different objects.',
        code: [
          'const x = { a: 1 };',
          'const y = { a: 1 };',
          'const z = x;',
          '',
          'console.log(x === y);   // false — two separate objects',
          'console.log(x === z);   // true  — the same object',
          '',
          'console.log([1, 2] === [1, 2]);   // false',
          'console.log("ab" === "ab");       // true — primitives compare by value',
        ].join('\n'),
        output: ['false', 'true', 'false', 'true'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'For objects, `===` asks "are these the same object?" rather than "do these look the same?". Two separately-created objects are never `===` equal no matter how identical their contents.',
          'For primitives, `===` compares the values themselves, so `"ab" === "ab"` is `true`.',
          'There is no built-in operator for comparing object contents. You either compare the specific properties you care about, or use a deep-equality helper — which is why testing libraries provide `toEqual` alongside `toBe`, exactly the distinction you have been using in this course\'s exercises.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'Making a real copy',
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Spread syntax creates a new object with the same properties.',
        code: [
          'const original = { name: "Ada", age: 36 };',
          '',
          'const copy = { ...original };',
          'copy.age = 99;',
          '',
          'console.log(original.age);      // 36 — safe',
          'console.log(copy.age);          // 99',
          'console.log(original === copy); // false — genuinely different objects',
          '',
          '// Arrays work the same way',
          'const nums = [1, 2, 3];',
          'const numsCopy = [...nums];',
          'numsCopy.push(4);',
          'console.log(nums.length, numsCopy.length);',
        ].join('\n'),
        output: ['36', '99', 'false', '3 4'].join('\n'),
      },
      {
        kind: SECTION.HEADING,
        text: 'Shallow copies only go one level deep',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'Spread syntax and `Object.assign` produce a **shallow** copy. The new object gets its own copies of each property value — but if a property\'s value is itself an object, what gets copied is that nested object\'s *reference*.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'The nested object is still shared.',
        code: [
          'const original = {',
          '  name: "Ada",',
          '  address: { city: "London" },',
          '};',
          '',
          'const copy = { ...original };',
          '',
          'copy.name = "Grace";              // top level — independent',
          'copy.address.city = "New York";   // nested — SHARED',
          '',
          'console.log(original.name);         // Ada  — safe',
          'console.log(original.address.city);  // New York — changed!',
          'console.log(original.address === copy.address); // true',
        ].join('\n'),
        output: ['Ada', 'New York', 'true'].join('\n'),
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This is the shallow-copy trap, and it is genuinely common in real applications because real data is usually nested.',
          'For a deep copy, modern JavaScript provides `structuredClone`, which is built into browsers and Node.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'structuredClone copies every level.',
        code: [
          'const original = {',
          '  name: "Ada",',
          '  address: { city: "London" },',
          '  tags: ["engineer", "mathematician"],',
          '};',
          '',
          'const deep = structuredClone(original);',
          '',
          'deep.address.city = "New York";',
          'deep.tags.push("pioneer");',
          '',
          'console.log(original.address.city);  // London — safe',
          'console.log(original.tags.length);   // 2 — safe',
          'console.log(deep.address.city, deep.tags.length);',
        ].join('\n'),
        output: ['London', '2', 'New York 3'].join('\n'),
      },
      {
        kind: SECTION.TABLE,
        headers: ['Technique', 'Depth', 'Notes'],
        rows: [
          ['`{ ...obj }`', 'Shallow', 'Concise and idiomatic; the usual first choice'],
          ['`Object.assign({}, obj)`', 'Shallow', 'Older equivalent of spread'],
          ['`[...arr]` or `arr.slice()`', 'Shallow', 'For arrays'],
          ['`structuredClone(obj)`', 'Deep', 'Built in; handles Dates, Maps, Sets, cycles. Cannot clone functions'],
          ['`JSON.parse(JSON.stringify(obj))`', 'Deep', 'Legacy trick. Destroys Dates, `undefined`, functions; throws on cycles'],
        ],
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.MISTAKE,
        title: 'The JSON round-trip is not a good deep copy',
        body: [
          'You will see `JSON.parse(JSON.stringify(obj))` recommended widely as a deep-copy trick. It works for plain data, but it silently corrupts anything else: `Date` objects become strings, `undefined` values and functions vanish entirely, `NaN` and `Infinity` become `null`, and a circular reference throws.',
          'Use `structuredClone` instead. It handles all of those correctly. Its own limitation is that it cannot clone functions — but neither can the JSON approach, and it at least tells you by throwing.',
        ],
      },
      {
        kind: SECTION.HEADING,
        text: 'How arguments are passed',
      },
      {
        kind: SECTION.PROSE,
        body: [
          'This follows directly, and it is a very common interview question with a frequently-muddled answer.',
          'JavaScript is **always pass-by-value**. What varies is *what the value is*: for a primitive it is the data itself; for an object it is a reference to that object. So a function receives a copy of the reference — which means it can mutate the object, but it cannot make the caller\'s variable point somewhere else.',
        ],
      },
      {
        kind: SECTION.CODE,
        language: 'javascript',
        runnable: true,
        caption: 'Mutation is visible to the caller; reassignment is not.',
        code: [
          'function mutate(obj) {',
          '  obj.changed = true;      // affects the caller',
          '}',
          '',
          'function reassign(obj) {',
          '  obj = { changed: true };  // purely local',
          '}',
          '',
          'const a = {};',
          'mutate(a);',
          'console.log(a.changed);    // true',
          '',
          'const b = {};',
          'reassign(b);',
          'console.log(b.changed);    // undefined',
        ].join('\n'),
        output: ['true', 'undefined'].join('\n'),
      },
      {
        kind: SECTION.CALLOUT,
        tone: CALLOUT_TONE.INTERVIEW,
        title: 'Is JavaScript pass-by-value or pass-by-reference?',
        body: [
          '"Always pass-by-value. For objects the value being copied is a reference, so the function can mutate the shared object and the caller sees it — but reassigning the parameter only re-points the local copy of the reference and the caller is unaffected. True pass-by-reference would let a function reassign the caller\'s variable, which JavaScript cannot do."',
          'Some people call the object case "pass-by-sharing", which is a more precise name for the same behaviour.',
        ],
      },
      {
        kind: SECTION.PREDICT,
        code: [
          'const settings = { theme: "dark", nested: { size: 10 } };',
          'const copy = { ...settings };',
          '',
          'copy.theme = "light";',
          'copy.nested.size = 99;',
          '',
          'console.log(settings.theme, settings.nested.size);',
        ].join('\n'),
        options: [
          'dark 10',
          'light 99',
          'dark 99',
          'light 10',
        ],
        correct: 2,
        explanation:
          'Spread makes a shallow copy. The top-level `theme` property was genuinely copied, so changing it on `copy` leaves `settings.theme` as `"dark"`. But `nested` is an object, and only its *reference* was copied — both objects point at the same nested object, so `settings.nested.size` becomes 99 too. The result is `dark 99`, and this mixed behaviour is exactly what makes shallow-copy bugs so confusing to debug.',
      },
    ],
    exercises: [
      {
        id: 'ex-m02-06-a',
        title: 'Predict the shared reference',
        kind: EXERCISE_KIND.PREDICT_OUTPUT,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['types', 'copying'],
        instructions: 'What does this print?',
        code: [
          'const a = { n: 1 };',
          'const b = a;',
          'b.n = 5;',
          'console.log(a.n);',
        ].join('\n'),
        options: ['1', '5', 'undefined', 'TypeError'],
        correct: 1,
        hints: [
          'Did `const b = a` create a new object, or a second name for the existing one?',
        ],
        solution: '5',
        solutionExplanation:
          'Assigning an object copies the reference, not the object. `a` and `b` are two names for one single object, so mutating it through `b` is fully visible through `a`. There is no TypeError because `const` forbids reassigning the binding, not mutating the object. To get independent objects you need `const b = { ...a };`.',
      },
      {
        id: 'ex-m02-06-b',
        title: 'Why are these not equal?',
        kind: EXERCISE_KIND.CONCEPTUAL,
        difficulty: DIFFICULTY.EASY,
        xp: 10,
        topicIds: ['types'],
        instructions: 'Why does `{ a: 1 } === { a: 1 }` evaluate to `false`?',
        options: [
          'Because objects cannot be compared at all',
          'Because `===` compares object identity, and these are two separately created objects',
          'Because the properties are in a different order',
          'Because `===` does not work with objects, only `==` does',
        ],
        correct: 1,
        hints: [
          'What question is `===` actually asking about two objects?',
        ],
        solution: 'Because `===` compares object identity, and these are two separately created objects',
        solutionExplanation:
          'For objects, `===` asks "are these the same object in memory?" rather than "do these have the same contents?". Two object literals create two distinct objects, so the answer is no. Property order is irrelevant, and `==` behaves identically to `===` for two objects. There is no built-in content comparison — you compare the specific properties you care about, or use a deep-equality helper, which is exactly why test libraries offer `toEqual` alongside `toBe`.',
      },
      {
        id: 'ex-m02-06-c',
        title: 'Copy without sharing',
        kind: EXERCISE_KIND.WRITE_FUNCTION,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 20,
        topicIds: ['types', 'copying', 'objects'],
        instructions:
          'Write `withUpdatedName(user, newName)` that returns a **new** object with the same properties as `user` but with `name` set to `newName`. The original object must not be modified in any way.',
        starterCode: [
          'function withUpdatedName(user, newName) {',
          '  // Build and return a new object. Do not mutate user.',
          '}',
        ].join('\n'),
        tests: [
          { name: 'the returned object has the new name', body: 'expect(withUpdatedName({ name: "Ada", age: 36 }, "Grace").name).toBe("Grace");' },
          { name: 'other properties are preserved', body: 'expect(withUpdatedName({ name: "Ada", age: 36 }, "Grace").age).toBe(36);' },
          { name: 'the original is untouched', body: 'const u = { name: "Ada", age: 36 }; withUpdatedName(u, "Grace"); expect(u.name).toBe("Ada");' },
          { name: 'a genuinely different object is returned', body: 'const u = { name: "Ada" }; expect(withUpdatedName(u, "Grace")).not.toBe(u);' },
          { name: 'works when the object has many properties', body: 'const u = { name: "A", a: 1, b: 2 }; const r = withUpdatedName(u, "B"); expect(r).toEqual({ name: "B", a: 1, b: 2 });', hidden: true },
        ],
        hints: [
          'Spread the original into a new object literal: `{ ...user }`.',
          'Properties listed after the spread override the spread ones.',
          '`{ ...user, name: newName }` does the whole job in one expression.',
        ],
        solution: [
          'function withUpdatedName(user, newName) {',
          '  return { ...user, name: newName };',
          '}',
        ].join('\n'),
        solutionExplanation:
          'Spreading copies every own property into a brand-new object, and listing `name` afterwards overrides the copied value — order matters, since `{ name: newName, ...user }` would let the original name win. This "copy and override" pattern is the standard way to update state immutably and is used constantly in React, Redux and similar tools. Note the two different test matchers: `not.toBe` checks it is a different object, while `toEqual` checks the contents match. Bear in mind this copy is shallow, so any nested objects would still be shared.',
      },
      {
        id: 'ex-m02-06-d',
        title: 'Fix the shared-state bug',
        kind: EXERCISE_KIND.FIX_BUG,
        difficulty: DIFFICULTY.MEDIUM,
        xp: 25,
        topicIds: ['types', 'copying'],
        instructions:
          'This function is meant to return a fresh settings object each time it is called, based on a default. Instead, every caller ends up sharing and corrupting the same object. Fix it so each call returns an independent copy — including the nested `display` object.',
        starterCode: [
          'const DEFAULTS = { theme: "dark", display: { fontSize: 14 } };',
          '',
          'function createSettings(overrides) {',
          '  const settings = DEFAULTS;',
          '  if (overrides && overrides.theme) {',
          '    settings.theme = overrides.theme;',
          '  }',
          '  return settings;',
          '}',
        ].join('\n'),
        tests: [
          { name: 'returns the default theme when nothing is overridden', body: 'expect(createSettings().theme).toBe("dark");' },
          { name: 'applies an override', body: 'expect(createSettings({ theme: "light" }).theme).toBe("light");' },
          { name: 'an override does not leak into later calls', body: 'createSettings({ theme: "light" }); expect(createSettings().theme).toBe("dark");' },
          { name: 'each call returns a different object', body: 'expect(createSettings()).not.toBe(createSettings());' },
          { name: 'the nested display object is not shared', body: 'const a = createSettings(); const b = createSettings(); a.display.fontSize = 99; expect(b.display.fontSize).toBe(14);' },
          { name: 'the default itself is never modified', body: 'const s = createSettings({ theme: "light" }); s.display.fontSize = 1; expect(createSettings().display.fontSize).toBe(14);', hidden: true },
        ],
        hints: [
          '`const settings = DEFAULTS;` does not copy anything — it creates a second name for the same object.',
          'A shallow copy with spread fixes the theme leak but leaves `display` shared.',
          '`structuredClone(DEFAULTS)` copies every level in one call.',
        ],
        solution: [
          'const DEFAULTS = { theme: "dark", display: { fontSize: 14 } };',
          '',
          'function createSettings(overrides) {',
          '  const settings = structuredClone(DEFAULTS);',
          '  if (overrides && overrides.theme) {',
          '    settings.theme = overrides.theme;',
          '  }',
          '  return settings;',
          '}',
        ].join('\n'),
        solutionExplanation:
          'The original bug is that `const settings = DEFAULTS;` copies a reference, so every call mutated the one shared default — meaning the second call saw the first call\'s override. A shallow `{ ...DEFAULTS }` fixes the theme tests but fails the nested `display` test, because that inner object would still be shared across every copy. `structuredClone` copies the whole structure and satisfies both. This exact bug — a shared mutable default object — is one of the most common sources of "state leaking between requests" problems in real applications.',
      },
    ],
    quiz: {
      id: 'qz-m02-06',
      questions: [
        {
          id: 'q-m02-06-1',
          kind: QUIZ_KIND.OUTPUT,
          topicIds: ['types', 'copying'],
          prompt: 'What does this print?',
          code: [
            'const a = [1, 2];',
            'const b = a;',
            'b.push(3);',
            'console.log(a.length);',
          ].join('\n'),
          options: ['2', '3', '0', 'TypeError'],
          correct: 1,
          optionExplanations: [
            'This assumes `b = a` copied the array, which it did not.',
            'Correct — both names refer to one array, so the push is visible through both.',
            'The array still contains its original elements.',
            '`const` forbids reassigning the binding, not mutating the array.',
          ],
          explanation:
            'Assigning an array copies the reference, so `a` and `b` are two names for the same array. Pushing through one is visible through the other. A real copy requires `[...a]` or `a.slice()`.',
        },
        {
          id: 'q-m02-06-2',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['types', 'copying'],
          prompt: 'What does `{ ...obj }` copy?',
          options: [
            'Every level of the object, recursively',
            'The top-level properties only — nested objects are still shared by reference',
            'Nothing; it creates another reference to the same object',
            'Only the properties that are primitives',
          ],
          correct: 1,
          optionExplanations: [
            'That would be a deep copy, which requires `structuredClone`.',
            'Correct — this is the shallow-copy trap.',
            'Spread genuinely creates a new object.',
            'All own enumerable properties are copied, including object-valued ones — it is their references that are copied.',
          ],
          explanation:
            'Spread creates a new object and copies each own enumerable property\'s value into it. When a property\'s value is an object, the value being copied *is a reference*, so the nested object ends up shared between the original and the copy.',
        },
        {
          id: 'q-m02-06-3',
          kind: QUIZ_KIND.SINGLE,
          topicIds: ['types'],
          prompt: 'Is JavaScript pass-by-value or pass-by-reference?',
          options: [
            'Pass-by-reference for objects, pass-by-value for primitives',
            'Always pass-by-value — but for objects the value copied is a reference',
            'Always pass-by-reference',
            'It depends on whether the parameter is declared const',
          ],
          correct: 1,
          optionExplanations: [
            'A common shorthand, but it wrongly predicts that reassigning a parameter would affect the caller.',
            'Correct — sometimes called pass-by-sharing.',
            'Reassigning a parameter never affects the caller, which rules this out.',
            'Parameters are always ordinary bindings regardless of how the caller declared its variable.',
          ],
          explanation:
            'JavaScript always copies the value into the parameter. For an object, that value is a reference, so the function can mutate the shared object — but reassigning the parameter only re-points the local copy, leaving the caller\'s variable untouched. True pass-by-reference would allow a function to reassign the caller\'s variable.',
        },
        {
          id: 'q-m02-06-4',
          kind: QUIZ_KIND.MULTIPLE,
          topicIds: ['types', 'copying'],
          prompt: 'Which problems does `JSON.parse(JSON.stringify(obj))` have as a deep-copy technique? (Select all that apply.)',
          options: [
            '`Date` objects become strings',
            'Properties holding `undefined` disappear',
            'It throws on circular references',
            'It only copies the first level',
          ],
          correct: [0, 1, 2],
          explanation:
            'The JSON round-trip does copy every level, so the last option is wrong — but it corrupts anything JSON cannot represent. Dates serialise to strings and never come back as Dates, `undefined` values and functions are dropped entirely, `NaN` and `Infinity` become `null`, and a circular reference throws a TypeError. `structuredClone` handles all of these correctly and should be preferred.',
        },
      ],
    },
    summary:
      'Assigning a primitive copies its value, leaving the two names entirely independent. Assigning an object copies the *reference*, so both names point at one shared object and mutating through either is visible through both. For the same reason `===` on objects compares identity rather than contents, so two identically-shaped objects are never equal. Spread syntax makes a genuine but **shallow** copy — nested objects remain shared — while `structuredClone` copies every level and handles Dates, Maps and cycles correctly, unlike the JSON round-trip. Argument passing follows directly: JavaScript is always pass-by-value, but because an object\'s value is a reference, a function can mutate the caller\'s object while being unable to reassign the caller\'s variable.',
    keyTakeaways: [
      'Primitives copy the value; objects copy the reference',
      '`===` on objects asks "the same object?", not "the same contents?"',
      '`{ ...obj }` is a genuine copy but only one level deep',
      'Nested objects stay shared after a shallow copy — the classic trap',
      '`structuredClone` is the built-in deep copy; the JSON round-trip corrupts Dates, undefined and cycles',
      'JavaScript is always pass-by-value — for objects, the value is a reference',
    ],
    relatedLessons: ['l-m02-05', 'l-m01-03'],
    interviewConnections: [
      'What is the difference between value and reference types?',
      'Why does `{a:1} === {a:1}` return false?',
      'What is the difference between a shallow and a deep copy?',
      'Is JavaScript pass-by-value or pass-by-reference?',
      'How do you deep clone an object, and what are the trade-offs?',
    ],
  },
];
